source   (원료)

이 폴더는 **원본 그림**이다. 게임이 안 부르는 것이 정상이다.
집 원화(`houses/`)처럼 **고르기 전 후보**와, 3D 로 넘기기 전의 밑그림이 들어 있다.

⇒ 골라 쓴 것은 `assets/house/` · `assets/gen/…` 같은 제자리로 옮겨진다.
   여기 남은 것은 **안 고른 것**이지 **버릴 것**이 아니다 — 다시 고를 수 있게 둔다.

(같은 규약: `assets/gen/_role.txt` · 판정 근거 `docs/assets/gen20260812_ghost_verdict.md`)
