source   (원료)

이 폴더는 게임 에셋이 아니라 **원료**다. 코드가 안 부르는 것이 정상이고, 부르면 그게 잘못이다.

  흐름:  원화(2D)  →  Meshy 3D(GLB)  →  tools/glb_thumb 로 납작하게 찍기  →  게임
         ~~~~~~~~
         여기가 이 폴더다

⇒ `tools/find_ghost_assets.py` 는 이 줄을 보고 아래 것들을 「유령」에서 뺀다.
   하위 폴더(날짜별)에 다시 적을 필요 없다 — 자가 위로 거슬러 찾는다.

★ 왜 이 파일이 생겼나 (2026-08-23)
  자가 `assets/gen/2026-08-12/` 206장 중 87장을 **유령**으로 찍었다. 전부 아니었다 —
  manifest 등록 0/206 · 게임 코드 참조 0줄로, **전량이 「아직 안 붙음」**이었다.
  ⇒ 자는 *"에셋은 코드가 불러야 정상"* 을 전제하는데 **원료 폴더는 전제가 반대**다.
    밖에서는 알 수 없고 **만든 사람만 안다.** 그래서 만든 쪽이 여기 적어 둔다.
    (`thumbs/index.json` 과 같은 규약 — 만든 쪽이 적고 읽는 쪽은 찾아본다)

⚠ 「아직 안 붙음」은 「필요 없음」이 아니다. **지우지 마라.** 3D 로 만들 재료다.
   무엇을 만들지는 [Plan]·[core] 의 「이번 판에 쓰는 물건」 목록을 따른다.
   판정 근거: docs/assets/gen20260812_ghost_verdict.md
