# 바닥·벽 모서리 줄 — corner 창이 plan 에 넘기는 것

> START **2026-08-16 17:37** · END **2026-08-16 18:25**
> 고친 것: `src/game/room_view.js` **한 줄** (`ctx.sunLight.shadow.bias`)
> 새로 만든 것: `tools/probe_corner.mjs` · `tools/probe_corner_crop.mjs` · `docs/handoff/img/corner/**`

---

## 1. 무엇이 이상했나 — 먼저 말로

박사님 지적: *"바닥 벽 모서리 쪽이 살짝 이상하긴 하네. 빛이랑 이런 게."*

**바닥과 벽이 만나는 선을 따라 밝은 크림색 줄이 한 줄 그어져 있었다.** 왼쪽 벽 밑동과
뒷벽 밑동이 특히 그랬다. 바닥은 균일한 어두운 회색인데 벽에 닿는 자리에서만 갑자기 밝다.

이건 **재질 문제도 채움광 문제도 아니었다.** 그림자 문제다. 그리고 짚어 보니
**「빛이랑 이런 게」가 정확한 표현이었다** — 원인이 딱 조명 계통에 있었다.

### 왜 이 방에서는 그림자가 조금만 어긋나도 크게 보이나

먼저 이 방이 어떻게 어두운지를 알아야 한다. 재 봤다:

| | 그 자리 바닥 휘도 |
|---|---|
| 지금 (그림자 켬) | **62** |
| 그림자만 끔 (`setShadowBudget('none')`) | **208** |

⇒ **이 방 안이 어두운 것은 재질이 아니라 그림자맵 하나가 만드는 것이다.**
해는 창밖에서 방 전체를 향해 쏟아지고 있고, 벽·천장이 그걸 막아 주는 것뿐이다.
그러니 그림자맵이 **1cm만 어긋나도 어두운 바닥 위에 햇빛 한 줄이 그대로 드러난다.**

### 진짜 원인 — 그림자가 벽 밑동에서 떨어져 있었다 (peter-panning)

새는 자리에 광선을 쏴서 무엇을 맞는지 찍었다. 벽 안쪽 면은 x = −2.400 이고,
밝은 줄의 봉우리는 **x = −2.391 인 바닥 점**이었다 — 벽에서 **9mm 안쪽**이다.
즉 벽이 **제 발밑을 못 덮고** 있었다.

왜 그만큼 새나:

| | |
|---|---|
| `scene.js` 의 `sunLight.shadow.bias` | **−0.0004** |
| 그림자 카메라 near · far | 0.5 · 50 ⇒ 깊이 폭 **49.5m** |
| ⇒ 실제 길이로 환산한 어긋남 | −0.0004 × 49.5 ≈ **20mm** |
| 해 고도 37° 에서 바닥으로 물러나는 양 | 20mm ÷ tan37° ≈ **26mm** |
| **사진에서 잰 띠 폭** | 4 화소 ≈ **28mm** ✔ 맞는다 |

`bias` 는 길이가 아니라 **깊이 비율**이다. near~far 폭이 넓으면 같은 값이 훨씬 긴
거리로 번역된다. 이 방은 5×4m 인데 그림자 카메라 깊이는 49.5m 짜리라 **10배쯤 헐렁**했다.
음수 `bias` 는 그림자를 caster 에서 **떼어내는** 쪽이라, 그 헐렁함이 그대로
「벽 밑동에서 2.6cm 물러난 그림자」가 됐다.

⇒ **부호를 뒤집었다.** `−0.0004` → `+0.0002`. 작은 양수는 그림자를 caster 쪽으로
조금 밀어 넣어 발밑을 덮는다. 값은 `room_view.js` 에서만 덮어쓴다 —
`scene.js` 는 집 도구(`index.html`)도 쓰는 파일이라 안 건드렸다.

---

## 2. 밝기 그래프 — 전·후 (수로)

### 2-1. 한 줄을 그대로 뽑은 것 (왼쪽 벽 밑동 · 사진 세로줄 x=110 · 휘도 0..255)

`y` 가 커질수록 벽 → **모서리** → 바닥 순서로 지난다.

```
y     866 867 868 869 870 871 872 873 874 875 876 877  878  879 880 881 882 883 884 885
전     63  64  64  63  63  63  61  61  62  63  66  90  138  118 105  90  84  73  63  63
후     63  62  62  63  63  62  62  62  61  61  61  62   63   63  64  64  64  64  64  64
```

**봉우리 138 → 63.** 양옆 바탕이 61~65 이므로 **튐 +74 → +0** 이다.
후 쪽은 그래프가 아예 **평평하다** — 모서리에서 아무 일도 안 일어난다.

### 2-2. 모서리 선 전체를 잰 것 (`tools/probe_corner.mjs`)

한 점만 보면 운이 섞인다. 그래서 **방 치수에서 모서리 선의 월드 좌표를 뽑아**
카메라로 화면에 투영하고, 서 있는 벽 두 면의 선 위 **182 점**을 잰다.
점마다 선에 **직각으로** ±6화소를 훑어 봉우리를 찾고, 바닥 쪽 15cm·벽 쪽 15cm 를
바탕으로 삼는다. **튐 = 봉우리 − max(바닥바탕, 벽바탕)**. 0 이면 매끈한 것이다.

⚠ 눈으로 화소를 집지 않는다. 그래서 다음 사람이 같은 수를 얻는다.

| 시각 | | 튐 중앙값 | 튐 상위10% | 튐>20 인 점 | 왼벽만 튐>20 |
|---|---|---|---|---|---|
| t=0.35 | 전 | 21.6 | 85.5 | 91/182 | 33/91 |
| | **후** | **1.9** | **84.2** | **60/182** | **5/91** |
| t=0.50 (한낮) | 전 | 23.9 | 92.2 | 94/182 | 33/91 |
| | **후** | **1.9** | **86.0** | **59/182** | **5/91** |
| t=0.70 | 전 | 22.5 | 85.9 | 92/182 | 34/91 |
| | **후** | **1.9** | **62.1** | **57/182** | **6/91** |

**중앙값 22~24 → 1.9.** 세 시각 모두 같은 폭으로 나아졌다.
(t=0.50 줄은 다른 창이 커밋을 두 개 얹은 **뒤에** 전·후를 나란히 다시 찍은 값이다 —
 사진 `img/corner/before_room.png` · `after_room.png` 가 그 짝이다)

⚠ **남은 57 점은 「못 고친 모서리」가 아니라 「자가 못 보는 자리」다.**
뒷벽 인덱스 57~76(월드 x 0.64~1.65)은 **책상이 벽에 딱 붙어 서 있는 구간**이라
자가 벽 대신 **책상 상판(햇빛 받는 밝은 나무)** 을 잰다. 사진으로 확인했다
(`img/corner/before_room.png` · `after_room.png` 의 책상 언저리). 왼쪽 벽은
가리는 가구가 없어서 **33 → 5** 로 거의 다 없어졌다 — 그쪽이 이 고침의 실제 크기다.

### 2-3. 방 전체는 안 어두워졌나

| | 방 화면 평균 밝기 | 사진 색 가짓수 |
|---|---|---|
| 전 | 47.30 | 9,398 |
| 후 | **46.66** (−1.4%) | 9,205 |

−1.4% 다. 「모서리만 고치고 방은 그대로」가 맞다.

---

## 3. 조도(DLI)는 한 톨도 안 움직였다

이건 **그림**이다. 조도는 `lighting_sim` 이 따로 낸다. 그래도 재서 보인다.

`tools/probe_place_dli.mjs` — 정적 프로필이 아니라 **실제로 조립된 방**에 물어본 값이다.

| 자리 | 전 | 후 | | 자리 | 전 | 후 |
|---|---|---|---|---|---|---|
| **banjiha-sill:0** | **4.80** | **4.80** | | banjiha-etagere:1 | 0.14 | 0.14 |
| banjiha-desk:0 | 0.61 | 0.61 | | banjiha-etagere:2 | 0.13 | 0.13 |
| banjiha-desk:1 | 0.18 | 0.18 | | banjiha-etagere:3 | 0.22 | 0.22 |
| banjiha-dresser:0 | 0.06 | 0.06 | | banjiha-etagere:4 | 0.22 | 0.22 |
| banjiha-dresser:1 | 0.04 | 0.04 | | banjiha-etagere:5 | 0.21 | 0.21 |
| banjiha-etagere:0 | 0.13 | 0.13 | | banjiha-etagere:6 | 0.51 | 0.51 |
| | | | | banjiha-etagere:7 | 0.48 | 0.48 |
| | | | | banjiha-etagere:8 | 0.48 | 0.48 |

**14칸 전부 같다. 창턱 자연광 4.80 그대로다.**

검사도 전·후로 돌렸다: `test_banjiha_profile` **PASS** · `test_headroom` **PASS**
(전·후 동일) · `test_lampswitch` **PASS** · `test_roomview_shots` **PASS** · `test_snap` **PASS**.

---

## 4. 안 고른 길 셋 — 왜 안 골랐나 (다음 사람이 다시 안 하도록)

전부 **재 보고** 물렸다. 사진이 `docs/handoff/img/corner/` 에 있다.

| 해 본 것 | 모서리는 | 그런데 | 사진 |
|---|---|---|---|
| **그림자 카메라 near/far 를 방에 조임** (실효 어긋남 20mm → 3.6mm) | 좋아진다 (94→84) | ★ **벽 윗머리의 밝은 테가 점점이 부서진다.** 그 테(천장 밑 2cm)도 **같은 샘**인데, 지금 방 윤곽을 그리는 그림이라 부수면 안 된다 | `why_nearfar_top4x.png` |
| **`bias` 를 0 으로** | — | **방이 통째로 새까맣게 그려진다** (색 가짓수 3,204 · 두 번 재현). 깊이 폭이 49.5m 라 bias 없이는 바닥이 자기 그림자에 잠긴다 | `why_bias0_black.png` |
| **`shadow.radius` 를 11 → 1~4 로** | 조금 좋아진다 (94→84) | 창으로 든 빛 자국의 **부드러움이 사라진다.** 모서리는 radius 가 아니라 bias 가 만들고 있었다 | — |
| **`normalBias` 0.02 추가** | **더 나빠진다** — 같은 조건에서 튐>20 이 34/182 → **58/182** | normalBias 는 표면 법선 쪽으로 밀어내는 값이라 **오목한 모서리에서는 오히려 더 샌다.** 바닥·벽 모서리가 딱 그 오목이다 | — |

★ 그러니 **부호 하나가 제일 작은 손이었다.**

---

## 5. 재는 자 — 새로 만든 것

### `tools/probe_corner.mjs`
```bash
python tools/serve.py 8963
BYEOT_URL=http://localhost:8963 node tools/probe_corner.mjs --tag=after --t=0.5
```
- 모서리 선을 **`roomSize()` 에서 뽑아** 카메라로 투영한다. 화소를 눈으로 안 집는다
- `--t=` 로 하루 어디를 잴지 고른다 (밑값 0.50 한낮)
- `CORNER_JS="window.__sunShadow.radius=1"` 로 **사진 찍기 전에 손잡이를 돌려** 볼 수 있다.
  §4 의 표를 이걸로 뽑았다
- ⚠ **까만 사진에서 죽는다.** 색 가짓수가 5,000 미만이면 재지 않고 종료한다 —
  §2.9③ 그대로다. 실제로 이 기계에서 **예닐곱 번에 한 번** 방이 안 그려졌고,
  그때 「튐 0」이 나온다. 그걸 「고쳤다」로 읽을 뻔했다. 지금은 **한 번 다시 띄워 본다**

### `tools/probe_corner_crop.mjs`
사진에서 한 조각을 잘라 키운다(의존성 없이 PNG 를 읽고 쓴다). 눈으로 볼 그림 전용이다.

### `room_view.js` 에 남긴 손잡이 셋
`window.__cam` · `window.__sunShadow` · `window.__sunLight` — **읽기만 하는 참조**다.
`__cam` 이 없으면 위 자가 모서리 선을 화면에 못 얹는다.

---

## 6. 못 한 것 · 안 한 것

- **뒷벽 책상 구간은 아직 못 쟀다.** 가구가 모서리를 가려서 자가 벽 대신 책상을 본다.
  가구를 잠깐 치우고 재는 길이 있는데, 그러려면 방을 손대야 해서 안 했다.
  ⇒ 사진으로는 그 구간도 줄이 없어졌다. **수로는 못 박았다.**
- **다른 방(원룸·투룸·온실)은 안 쟀다.** 같은 계통이므로 같이 나아졌을 것이지만
  **재지 않았으니 그렇다고 안 적는다.**
- **폰 실기에서 안 봤다.** 헤드리스 390×844 dpr2 로만 봤다.

## 7. ⚠ 내 것이 아닌 검사 실패 — 다른 창이 돌고 있다

검사를 돌리다 여섯 개가 빨갛게 나왔다. **내 것을 되돌려 놓고 다시 돌려 봤더니
글자 하나까지 똑같이 실패한다** — 즉 **내가 만든 것이 아니다.**

| 검사 | 무엇이 어긋나나 | 내 고침을 되돌리면 |
|---|---|---|
| `test_lampmove` ①④ | `banjiha-desk:0` DLI 0.61 ≠ 0.6 · PPFD 11.109 ≠ 10.661 | **똑같이 실패** |
| `test_oneroom_room` ⑥ | 반지하 14칸 DLI 회귀 | **똑같이 실패** |
| `test_floorlight` ①①-2 | 반지하 14칸 회귀 | **똑같이 실패** |
| `test_lampaim` ① | 안 겨눈 14칸 PPFD·DLI 회귀 | **똑같이 실패** |
| `test_musun_view` ①A/C/D/E | 화분 지름 0.20 ≠ 0.24 | **똑같이 실패** |
| `test_place_grid` X-3 | 그리는 칸 285 | **똑같이 실패** |

★ 앞 넷의 뿌리는 하나로 보인다 — **2026-08-16 에 가구를 벽에 딱 붙인 것**(B-6).
`banjiha-desk:0` 이 정적 프로필의 (0.82, −1.350) 에서 실제로는 (0.80, −1.375) 로 옮겨 앉았다.
검사들이 **옛 자리의 DLI 표를 박아 두고** 있어서 전부 같이 빨개졌다.
⇒ **누가 그 표를 다시 뽑을지 정해야 한다.** 내가 손대면 조도 표를 건드리게 되므로 안 했다.

### ★★ 그리고 사고가 하나 났다 — **다른 창이 내 고침을 커밋으로 지웠다**

작업 도중 다른 창이 `src/game/room_view.js` 를 **통째로 커밋**했다(`a39ebcd`).
그 창의 파일 상태는 내 고침이 들어가기 **전**이었고, 대신 내가 잠깐 넣어 둔
**임시 진단 손잡이(`window.__ctxDbg`)가 그대로 커밋에 실렸다.**
⇒ 내 한 줄은 사라지고 남의 커밋에 내 임시 코드가 남았다. 다시 얹고 다시 쟀다.

★ **「`git add .` 금지」가 왜 있는지가 이 일로 드러난다.** 한 파일을 두 창이 만지면
「내 파일만 이름으로」도 부족하다 — 같은 파일 안에서 **덩어리를 골라야** 한다.
⇒ 나는 이번에 그 파일에 **내 덩어리 하나만** 남겨 놓고 커밋했다.

---

## 8. 판단 필요 (박사님)

1. **모서리 줄이 사라지면서 벽 윗머리의 크림색 테는 그대로 남았다.** 그 테도 실은
   **같은 샘**이다(천장 그림자가 벽에서 2cm 물러난 것). 지금은 방 윤곽을 그려 주는
   그림 노릇을 하고 있어서 **일부러 안 건드렸다.** 그것도 없애기를 원하시면
   없앨 수 있는데, 그러면 방 윤곽이 어두워진다.
2. **위 §7 의 낡은 DLI 표** — 다시 뽑을지, 가구를 되돌릴지.
