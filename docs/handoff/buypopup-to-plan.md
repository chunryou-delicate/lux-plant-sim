# 2026-08-18 · buypopup → plan (주문 개수 창 · 한 달 가계부)

> START 18:56:37 · **END 19:56:19**

**일이 셋이다.** §0~§7 · **§월 장부** · **§다시보기**(맨 아래).

1. 박사님: *"상점 주문시 **개수 입력창** 뜨게. 개수는 **직전 주문량이 디폴트**로"* → §0~§7
2. 박사님: *"월세 나가는 날에 **월 장부 정리**가 팝업으로 (…) **가계부처럼**"* → **§월 장부**
3. 박사님: *"**이전 것도 볼 수 있게** 해 줘. **가방 탭에 안에 버튼** 만들면 될 듯"* → **§다시보기**

★ **셋 다 같은 틀을 쓴다.** 어제 만든 `.pop`/`.popcard` 를 이제 **넷**이 나눠 쓴다 —
곳간 팔기 · 오늘 밥상 · **주문 개수** · **한 달 가계부**.
⚠ **틀(`.pop`/`.popcard`) 자체에는 CSS 를 한 줄도 안 보탰다.** 새로 지은 규칙은 셋뿐이고
전부 **그 틀 밖**이다: 가계부 줄(`.lhead` · `.lrow` 선택자 넓히기 — 살림 장부 규칙을
그대로 쓴다) · 가계부 **[✕]**(`#monthClose` — 이 카드에만) · [가방] 탭
**[가계부 다시 보기]**(`#ledgerBackBox`). 달 넘기개는 고르개들의 `.prow` 를 그대로 입는다.

---

## 0. 먼저 네 줄

1. **틀을 새로 안 지었다.** 어제 만든 `.pop`/`.popcard` 를 그대로 쓴다 — 이제 그 틀을
   **셋**이 나눠 쓴다(곳간 팔기 · 오늘 밥상 · **주문 개수**). 새 CSS 를 한 줄도 안 보탰다.
2. **셈을 한 줄도 새로 안 만들었다.** 값은 `catalogList().buyWon`, 배송은 같은 표의
   `leadDays`, 주문은 `orderItem(S, id, qty, …)` 다 — **저쪽이 이미 개수를 받고 있었다.**
   여기서 새로 세는 것은 「소지금으로 몇 개까지인가」 하나뿐이고 그것도 한 함수 안이다.
3. ★ **디폴트를 어디에 기억할지 재서 골랐다 — localStorage 다**(§2-2).
   세이브에 넣으면 스키마가 바뀌고, 화면 변수면 새로고침에 사라진다.
   **품목마다 따로** 기억한다(씨앗 3개를 시킨 뒤 시루를 열면 1이다 — 실측 §3-2).
4. **검사 신설 1벌(67항목) 전부 통과 · 지시된 기존 검사도 전부 통과 · 부팅 예외 0.**
   ⚠ `test_quiet` 한 절을 고쳤다 — **그 검사가 옛 손버릇(두 번 누르기)을 재고 있었다**(§4-2).

---

## 1. 무엇이 없었나

[주문]을 누르면 `confirmOnce('정말 주문?')` 가 돌았다 — **단추 글자가 바뀌고, 둘째 탭에
한 개**가 나갔다. 그래서:

| | 고치기 전 | 지금 |
|---|---|---|
| 씨앗 5시루분을 시키려면 | [주문]×2 를 **다섯 번** = **손 10번** | [주문] → [＋]×4 → [5개 주문] = **손 6번**. ★ 그 다음부터는 **손 2번**(디폴트가 5다) |
| 총액 | **화면 어디에도 없다** | 창 한가운데와 **단추 글자**에 |
| 언제 오나 | 목록 줄의 `1일 뒤 도착`(누르면 사라진다) | 창 안에 **계속** 떠 있다 |
| 돈이 모자라면 | 단추가 **회색 + `title`** | 창이 떠서 **얼마가 모자란지** 말한다 |

★★ **회색 단추가 이 저장소의 지병 그대로였다.** `title` 은 폰에서 안 뜬다 —
누른 사람이 보는 것은 「눌러도 아무 일이 없다」다(`quiet-to-plan §1` *하는 중은 그 자리*).
그래서 `disabled` 를 걷고 **창 안에서 까닭을 말하게** 했다.

---

## 2. 바뀐 것 (파일별)

| 파일 | 무엇을 |
|---|---|
| `game.html` | **`#buyPanel` 신설**(같은 `.pop`/`.popcard` · body 바로 밑) · `drawBuyPanel`·`openBuyPanel`·`closeBuyPanel`·`buyMaxQty`·`lastOrderQty*` 신설 · `[data-buy]` 이 `confirmOnce` 대신 창을 연다 · **`disabled` 를 걷음** · 배너가 **개수와 총액**을 말함 · 닫는 길 셋에 등록 · **낡은 주석 셋 정정** |
| `tools/test_buypopup.mjs` | **신규 · 67항목** |
| `tools/test_quiet.mjs` | **A·B 절 한 곳** — 옛 손버릇(같은 단추 두 번)을 창 여는 손으로 고침(§4-2) |
| `docs/engine/shots/buypopup/*.png` | 그림 8장 |
| `docs/handoff/buypopup-to-plan.md` | 이 문서 |

**둘째 일(§월 장부)에서 더 바뀐 것**

| 파일 | 무엇을 |
|---|---|
| `game.html` | **`#monthPanel` 신설**(같은 틀) · `.lrow` CSS 선택자를 `.popcard` 까지 넓힘(규칙은 그대로) · `monthSnapNow`·`monthLoad/Save/Start/Ensure`·`monthNoteDay`·`monthNoteHarvest`·`monthCloseNow`·`drawMonthPanel`·`openMonthWhenQuiet` 신설 · `commitNextDay`·`dlgPaint`·`noteHarvest` 에 한 줄씩 · 닫는 길 셋에 등록 |
| `tools/test_monthly.mjs` | **신규 · 48항목** |
| `docs/engine/shots/monthly/*.png` | 그림 4장 |

⛔ **안 건드린 것**: `src/game/**` **전부**(shop.js 한 줄도 안 고쳤다) · `first_play.js` ·
`state.js` · `save.js` · `room_view.js` · `plant_grow.html` · `render3d/*` · `data/**` ·
`src/game/ui.js`(쓰기 영역이었지만 고칠 일이 없었다) · 그 밖 기존 검사 전부.

### 2-1. ★ 새 틀을 안 지었다 — **셋이 같은 옷을 입는다**

```
.pop      화면 고정 + 스크림 + 가운데 정렬   (z-index 84)
.popcard  카드 — ptitle / psub / plots / prow / pquote / pbtns
```

`#pantryPanel`(곳간 팔기) · `#mealPanel`(오늘 밥상) · **`#buyPanel`(주문 개수)** 셋이
**같은 클래스**를 쓴다. 검사가 셋의 `z-index` 가 같은지를 실제로 잰다(§A).

⚠ **자리도 같다 — `#sheet`·`#bottom` 밖, body 바로 밑이다.** 시트는 `transform` 으로
미끄러지는데 변형된 조상 안에서는 `position:fixed` 가 화면 기준이 아니게 된다.
어제 상점 고르개가 **y=1211**(화면 844)에 떠서 안 보였던 사고가 그것이다(sellpopup §2-2).
검사가 `#sheet.contains(#buyPanel) === false` 를 못 박는다.

### 2-2. ★★★ 디폴트를 **어디에 기억하나** — 재서 골랐다

지시가 콕 집은 물음이다. 길이 셋이었다.

| | | 판정 |
|---|---|---|
| ㉮ **세이브**(`S.shop`) | 스키마가 바뀐다. `save.js` 는 **쓰기 영역 밖**이고 옛 판을 여는 길까지 건드려야 한다 | ⛔ |
| ㉯ **화면 변수만** | 새로고침 한 번에 사라진다. *"직전 주문량"* 이 그 판 안에서만 사는 값이 된다 | ⛔ |
| ㉰ **localStorage** (`byeot.lastOrderQty`) | **채택** | ★ |

★ **㉰ 를 고른 진짜 까닭은 「이게 살림이 아니라 손버릇」이라는 것이다.**
이 파일이 이미 같은 결의 값을 거기 두고 있고, 그 주석이 이유를 적어 두었다 —
`byeot.pointerMode`(§PTR_KEY): *"판을 지우거나 새로 시작해도 남아야 하는 **조작 취향**이지
게임 진행이 아니다"*. 음악 켜짐(`MUSIC_KEY`)·안내판 본 것(`GUIDE_SEEN`)도 같은 자리다.
**주문 개수가 정확히 그 줄에 선다** — 「나는 씨앗을 다섯 개씩 산다」는 지갑이 아니라 습관이다.

⚠⚠ **품목마다 따로 기억한다.** `{itemId: qty}` 표다. 한 칸에 담으면 씨앗(500원) 5개를
시킨 뒤 시루(5,000원)에 5가 떠서 **25,000원이 디폴트**가 된다 — 지시서가 못 박은 자리고
검사 §B-2 가 실제로 잰다.

⚠ **기억은 주문이 실제로 나간 뒤에** 적는다(`r.qty` — **코어가 받은 개수**로 적는다).
고르다 그만둔 값은 손버릇이 아니다.

⚠ **기억한 값이 지금 소지금으로 안 되면 살 수 있는 데까지 내려서 연다.** 그리고
**그 사실을 창이 말한다** — 조용히 줄이면 「5를 시켰는데 3이 나갔다」가 된다(실측 §3-4).

★ **알아 둘 것**: 이 기억은 [지금 판을 지웁니다]로 안 지워진다(그쪽은 세이브 키만 지운다).
지워지는 것은 [완전 초기화](`hardReset` · `localStorage.clear()`) 때다. **일부러 그렇다** —
새 판을 시작해도 손버릇은 그 사람 것이다. ⏸ 싫으시면 판 지우기에 한 줄 붙이면 된다(§5-3).

### 2-3. ★ 「돈이 모자란다」를 **그 자리에서** 말한다

`disabled` + `title` 을 걷고 창이 말하게 했다. 두 갈래다.

| 언제 | 화면이 하는 말 |
|---|---|
| **한 개도 못 살 때** | 창은 뜬다. `pquote.warn`(주황)으로 *"돈이 모자랍니다 — 한 개에 5,000원인데 소지금이 100원입니다 (4,900원 모자랍니다)"* · [주문]은 회색 |
| **더는 못 올릴 때** | *"— 소지금으로는 2개까지입니다 (더 시키려면 돈이 더 있어야 합니다)"* · [＋]와 [전부]가 회색 |

★ 회색이 남은 자리는 **[＋]·[전부]·[주문]** 셋인데, 셋 다 **바로 옆 한 줄이 까닭을
적고 있다**. 「회색인데 왜인지 모른다」가 안 생기는 것이 이 창의 값어치다.
⚠ 회색 [주문]을 눌러도 돈이 안 나가는 것을 실측으로 확인했다(§E).

### 2-4. ★ 되묻기가 없어진 것이 아니라 **더 무거워졌다**

`confirmOnce('정말 주문?')` 를 걷었다. 팔기 팝업이 이미 같은 판단을 했고(§pantryGo:
*"고르는 자리 자체가 되묻기"*), 여기서는 그것이 더 분명하다 — **총액과 도착일을 적어
놓은 창**이 뜨고, 화면이 어두워지고, [N개 주문]이라 적힌 단추를 눌러야 나간다.
⇒ `confirmOnce` 는 아직 산다. 남은 손님은 **[몬스테라 팔기]와 [지금 판을 지웁니다]** 둘이다.

---

## 3. ★ 실측 — **화면에 실제로 뜬 글자**

**무엇을 켜고 무엇을 껐나**

| | |
|---|---|
| 서버 | `python tools/serve.py 8963` (이미 떠 있었다 · 다른 워커와 같이 씀) |
| 화면 | Chrome CDP · **360·390·430 × 844 · dpr 2** · `mobile:false` |
| 판 | `localStorage.clear()` 로 **새 판**(⚠ `goto` 뒤에). 반지하 · 첫 플레이 켬 |
| 대사 | `#dlgSkip` → `#dlgBox` → `#guideClose` 를 실제로 눌러 걸었다 |
| 지름길 | **하나뿐**이다 — `S.tutorial.cashWon` 을 직접 낮췄다(§3-4·§3-5). 돈이 모자란 판을 실제로 만들려면 150만원을 다 써야 하는데 그건 이 일이 재는 것이 아니다. **그 밖은 전부 화면 단추로** 밟았다 |

**끈 것은 없다.** 빨리감기도 안 썼다. **값·판매율·배송 일수를 한 자리도 안 건드렸다.**

### 3-1. 처음 여는 창 — 화면 글자 그대로 (390px)

```
콩 씨앗 (1시루분) — 몇 개 주문할까요
한 개 500원 · 1일 뒤 도착 · 소지금 1,500,000원
가방에 없습니다
[−] [1개] [＋] [전부]                       ← [−] 는 회색 (1개가 아래끝)
500원 × 1개 = 500원 · 1일 뒤에 옵니다 · 주문하면 1,499,500원이 남습니다
[1개 주문 · 500원] [그만두기]
```

★ **디폴트가 1개**다(처음 시키는 품목).
★ **배송 일수가 두 곳**(psub·quote)에 뜬다 — 둘 다 `catalogList().leadDays` 를 읽는다.

### 3-2. ★★★ 디폴트가 「직전 주문량」이고 **품목마다 따로**다

실제로 밟은 순서와 그때 뜬 개수다.

| 손 | 화면 | 판정 |
|---|---|---|
| 씨앗 [주문] | **1개** | 처음이라 1 |
| [＋][＋] → [3개 주문 · 1,500원] | 나감 (코어 `qty=3` · `totalWon=1500`) | |
| 씨앗 [주문] 다시 | **3개** | ★★★ **직전 주문량이 떴다** |
| 시루 [주문] | **1개** | ★★★ ⚠ **씨앗 3이 안 새어 왔다** |
| [＋] → [2개 주문] | 나감 (`siru` 2개) | |
| 시루 [주문] 다시 | **2개** | |
| 씨앗 [주문] 다시 | **3개** | ★★ 뒤에 시킨 시루가 **안 덮어썼다** |
| **새로고침 뒤** 씨앗 | **3개** · 시루 **2개** | ★★ 화면 변수였다면 1로 돌아간다 |

### 3-3. 시루 창 — **배송이 이틀이라 다르게 뜬다**

```
콩나물 시루 (차광 용기) — 몇 개 주문할까요
한 개 5,000원 · 2일 뒤 도착 · 소지금 1,498,500원
```
⇒ 씨앗은 `1일`, 시루는 `2일`. **품목마다 다른 값을 표에서 읽는다**(손으로 안 적었다).
주문 뒤 코어의 `arrivesOnDay − S.day` 와 대조해 같은 것을 확인했다(§D).

### 3-4. ★ 돈이 모자랄 때 — 두 갈래 다 화면 글자 그대로

**㉠ 기억한 3개는 못 사고 2개는 살 수 있는 돈**(지갑을 `500×2` 로 낮춤)

```
[−] [2개] [＋] [전부]                       ← [＋]·[전부] 회색
500원 × 2개 = 1,000원 · 1일 뒤에 옵니다 · 주문하면 0원이 남습니다
  — 소지금으로는 2개까지입니다 (더 시키려면 돈이 더 있어야 합니다)
[2개 주문 · 1,000원] [그만두기]
```
★★★ **기억한 3이 조용히 사라진 것이 아니라 2로 내려오고 까닭이 적혔다.**

**㉡ 한 개도 못 사는 돈**(지갑 100원 · 시루)

```
돈이 모자랍니다 — 한 개에 5,000원인데 소지금이 100원입니다 (4,900원 모자랍니다).
채소를 팔거나 며칠 지나야 시킬 수 있습니다.          ← 주황 칠(.pquote.warn)
[주문] [그만두기]                                    ← [주문] 회색
```
★ **창이 뜬다.** 예전에는 목록의 단추가 회색이라 **여기까지 올 수가 없었다.**
★ 회색 [주문]을 눌러 봤다 — 지갑도 주문 목록도 **안 움직였다**.

### 3-5. [전부] — 살 수 있는 최대

지갑 1,488,500원 · 씨앗 500원일 때 [전부] → **2977개**
(`floor(1488500 / 500) = 2977` — 화면이 적은 수와 코어 값이 맞는 것을 검사가 잰다).

### 3-6. 배너 — **개수가 늘면 그 말도 맞아야 한다**

```
주문했습니다
콩 씨앗 (1시루분) 3개 · 1,500원 · 1일 뒤 도착
```
⚠ 고치기 전 배너는 **`콩 씨앗 (1시루분) · 1일 뒤 도착`** 이었다 — 개수도 총액도 없었다.
개수를 고를 수 있게 된 지금 그대로 뒀으면 5개를 시켜도 화면은 같은 말을 한다.
★ 수는 전부 `orderItem` 의 반환(`r.qty`·`r.totalWon`)에서 읽는다. 손으로 안 적었다(§2.8).

### 3-7. ★ 세 크기 — **단추가 44px 아래로 안 내려간다**

팝업 안 단추 전부를 재서 적는다(가로 × 세로 px · `[−] [N개] [＋] [전부] [주문] [그만두기]`).

| 폭 | 잰 값 | 카드 |
|---|---|---|
| **360** | 64×44 · 96×44 · 64×44 · 64×44 · **172×44 · 126×44** | x12 y272 336×300 |
| **390** | 71×44 · 106×44 · 71×44 · 71×44 · **189×44 · 139×44** | x12 y272 366×300 |
| **430** | 80×44 · 119×44 · 80×44 · 80×44 · **213×44 · 155×44** | x12 y282 406×280 |

⇒ **44px 아래가 하나도 없다.** 제일 좁은 것이 360px 의 64×44 다.
★ 셋 다 **카드가 화면 안**이고 **가로로 안 넘친다**(`scrollWidth <= innerWidth`).
★ 어제 판 팔기 고르개(360 에서 64×44·96×44·…)와 **한 자리도 안 다르다** — 같은 틀이라 그렇다.

### 3-8. 그림

★ 그림은 **씩앗 5개**로 밟았다(검사는 3개로 밟는다 — 같은 길이다).
찍을 때 화면에 뜼던 글자를 그대로 적어 둔다:
`500원 × 5개 = 2,500원 · 1일 뒤에 옵니다 · 주문하면 1,495,000원이 남습니다` ·
단추 `5개 주문 · 2,500원` · 배너 `콩 씨앗 (1시루분) 5개 · 2,500원 · 1일 뒤 도착`

| 무엇 | 경로 |
|---|---|
| ★★ **처음 열었을 때(디폴트 1개)** | `docs/engine/shots/buypopup/buy_default1_390.png` |
| 5개로 올린 것 (총액이 단추에) | `docs/engine/shots/buypopup/buy_five_390.png` |
| 주문 배너 (개수·총액·도착일) | `docs/engine/shots/buypopup/buy_banner_390.png` |
| ★★★ **다시 열었을 때 — 5개가 떠 있다** | `docs/engine/shots/buypopup/buy_remembered5_390.png` |
| ★★ **다른 품목(시루)은 1개다** | `docs/engine/shots/buypopup/buy_other_item_390.png` |
| ★ **돈이 모자랄 때** | `docs/engine/shots/buypopup/buy_short_390.png` |
| 360 · 430 | `buy_360.png` · `buy_430.png` |

---

## 4. 검사

```
python tools/serve.py 8963 · BYEOT_URL=http://localhost:8963
```

| 벌 | 결과 |
|---|---|
| ★ **`test_buypopup`(신규)** | **잰 것 67개 · 어긋난 것 0개** |
| `test_sellpopup` | **잰 것 54개 · 어긋난 것 0개** |
| `test_uiwire` | **55개 모두 통과** |
| `test_quiet` | **26/26 통과** (§4-2 로 한 절을 고쳤다) |
| `test_pantrysale` | **13벌 전부 통과** |
| `test_bagcell` | **잰 것 55개 · 어긋난 것 0개** |
| `test_nextday_gate` | **25/25 통과** — ⚠ 둘째 일 뒤 다시 돌렸을 때 한 번 **부팅 대기에서 지쳐** 떨어졌다(`!!window.__rv` 180초). 단독으로 다시 돌리니 **25/25**다 — 기계가 바쁠 때 나는 흔들림이고, 같은 판에서 `dawn6`·`first_play`·`buypopup` 은 멀쩡히 부팅했다 |
| `test_dawn6` | **46/46 통과** |
| `test_first_play` | **PASS** (`loop` · `harvest_atomic` 포함) |
| ★ **`test_monthly`(신규 · §월 장부)** | **잰 것 48개 · 어긋난 것 0개** |
| ★ **`game.html` 부팅 예외** | **0** (`Runtime.exceptionThrown` 0건 — 새 검사 둘·`sellpopup`·`quiet`·`dawn6` 이 각자 잰다) |

⚠ **고친 검사는 `test_quiet` 하나뿐**이고 그 까닭은 §4-2 다. 나머지는 한 줄도 안 고쳤다.

### 4-2. ⚠⚠ `test_quiet` 한 절을 고쳤다 — **검사가 옛 손버릇을 재고 있었다**

`test_quiet` A·B 절(배송)이 이렇게 눌렀다:

```js
const b = document.querySelector('[data-buy="bean_seed"]');
b.click(); b.click();          // ← confirmOnce 를 태우는 두 번 누르기
```

지금은 첫 누름이 창을 열고 **둘째 누름이 그 창을 다시 열 뿐**이라 주문이 한 건도 안 나간다.
그러면 이 절이 재려던 **배송**이 아니라 「주문이 되나」에서 멎고, 화면에는 아무 문제가
없는데 검사만 붉어진다. ⇒ [주문] → `#buyGo` 로 고쳤다(개수는 디폴트 1 그대로 둔다 —
**이 절이 재는 것은 배송이지 개수가 아니다**).

★ 이것이 START-HERE §2 가 말하는 **「재는 자」** 자리다. 고친 것은 자이고, 자가 재던
약속(*배송이 온 날 화면이 말한다*)은 **한 줄도 안 느슨해졌다.**

### 4-3. 새 검사 `tools/test_buypopup.mjs` — 67항목

| | 무엇을 못 박나 |
|---|---|
| **A** | ★★ **팝업이 뜬다** — 팔기 팝업과 **같은 틀·같은 층(z 84)** · `#sheet`·`#bottom` **밖** · 카드가 화면 안 |
| **B·D** | ★★★ **처음엔 1개** · [＋]로 오르고 **총액 = 개당값 × 개수** · 그 셋이 주문 뒤 **코어 값**(`unitWon`·`totalWon`·`qty`)과 같다 · 지갑이 **총액만큼만** 준다 |
| **B-2** | ★★★ **디폴트가 직전 주문량이다** · ⚠ **품목마다 따로**(씨앗 3 → 시루 1 → 씨앗 여전히 3) |
| **B-3** | ★ **[전부]가 `floor(소지금 ÷ 개당값)`** 이고, 위끝에서 **왜 더 못 올리는지**를 말한다 |
| **C** | ★★ **새로고침해도 남는다**(localStorage 라는 증거) |
| **E** | ★★ **닫는 길 셋**(뒤 누르기 · [그만두기] · Esc)이 다 열려 있고 **셋 다 돈이 안 나간다** · 카드 **안**을 누르면 **안** 닫힌다 · Esc 는 팝업만 닫고 시트를 남긴다 |
| **F** | ★★ **돈이 모자라면 말한다** — 창이 뜨고 · 기억한 개수가 **내려오고** · 얼마가 모자란지 적고 · 회색 [주문]을 눌러도 **아무 일도 안 난다** |
| **G** | ★ **배송 일수**를 적고 그 값이 코어와 같다 · **배너가 개수와 총액**을 말한다 |
| **H** | ★ **360·390·430 셋 다** 단추 44px 이상 · 카드가 화면 안 · 가로로 안 넘침 |

⚠ **숫자를 이 검사에 안 박았다.** 값도 배송일도 총액도 **화면 글자에서 뽑아**
`window.__S()` 의 코어 값과 대조한다 — START-HERE §2.8 의 사고가 그 반대다.
⚠ **지름길은 §F 한 곳뿐**이고 그 자리에 적어 두었다(지갑을 직접 낮춘다).
★ ⚠ **§C 를 §F 보다 앞에 두었다.** 뒤에 두면 §F 가 낮춰 놓은 지갑 때문에 기억한 3이
1로 내려오고, 그러면 이 절이 「기억이 안 남았다」로 **거짓 실패**한다 —
재는 자가 거짓말하는 자리라 검사 안에 까닭을 적어 두었다.

---

## 5. ☐ 판단 필요 (박사님·plan 몫)

### 5-1. ★ ± 눈금이 **한 개씩**이다

시루·화분은 한두 개, 씨앗도 시루 수만큼이라 자릿수가 작아 한 개씩으로 뒀다.
[전부]가 위끝으로 한 번에 데려다 주므로 갇히지는 않는다.
⇒ 씨앗을 열 개씩 사는 판이 되면 **±5 나 ±10 이 필요**할 수 있다. 지금은 이르다고 봤다.

### 5-2. 개수를 **직접 쳐 넣는 칸**을 둘지

지시서가 *"개수 **입력창**"* 이라 하셨는데 **± 와 [전부]로 갔다.** 까닭 둘:
① 폰에서 숫자 자판이 올라오면 창의 절반이 가려진다 ② 이 게임의 다른 고르개(팔기·밥상)가
전부 ± 라 여기만 자판이면 손버릇이 갈린다. ⇒ **틀린 판단이면 `<input type=number>` 를
`pcount` 자리에 끼우면 된다** — 나머지(총액·한도·기억)는 그대로 돈다.

### 5-3. 「판 지우기」로 기억을 같이 지울지

[지금 판을 지웁니다](`firstRun`)는 세이브 키만 지우므로 **주문 개수 기억은 남는다.**
일부러 그렇게 뒀다(손버릇은 판이 아니라 사람 것이다 — 음악 켜짐과 같은 결).
⇒ 싫으시면 한 줄이다: `localStorage.removeItem('byeot.lastOrderQty')`.

### 5-4. 여러 품목을 **한 번에 담는 장바구니**

지금은 품목 하나씩이다. 시루 5 + 씨앗 5 를 시키려면 창을 두 번 연다.
⇒ 장바구니는 `orderItem` 을 여러 번 부르면 되지만 **주문이 여러 건으로 갈린다**
(배송일이 품목마다 달라 어차피 따로 온다). 지금 이 일의 범위 밖이라 안 건드렸다.

---

## 6. ✗ 못 한 것

1. **진짜 손가락으로 안 눌러 봤다.** 44px 은 **잰 것**이지 눌러 본 것이 아니다.
2. **가로 화면(landscape)을 안 쟀다.** 세 폭 다 세로(844)로만 쟀다.
3. **화분 넷·수경병·재배판 창을 안 열어 봤다.** 씨앗(1일)과 시루(2일) 둘로만 밟았다 —
   ⚠ 다만 창은 품목을 **안 본다**(`catalogList()` 가 낸 한 줄을 적을 뿐이다).
4. **무순 품목은 안 밟았다** — 몬스테라 도착 뒤에 열려 판이 길다. 같은 까닭으로 창은 같다.
5. **잉여 판매·[몬스테라 팔기]는 그대로다** — 둘 다 고르개가 없어 이 일의 대상이 아니었다.

---

## 7. 안 건드린 것 — **지킬 것을 어떻게 지켰나**

| 지시 | 어떻게 |
|---|---|
| **값·판매율·배송 일수를 건드리지 마라** | `src/game/**` 을 **한 줄도 안 고쳤다.** `data/**` 도 안 열었다 |
| **셈을 새로 만들지 마라** | 값 `buyWon` · 배송 `leadDays` · 주문 `orderItem(S,id,qty)` · 재고·배송중 `shopStatus` — 전부 그대로 부른다. 새로 세는 것은 `buyMaxQty` 하나이고 그것도 **한 자리**다 |
| **숫자를 문구에 손으로 적지 마라** | 창의 모든 수가 코어가 낸 것이다. 배너의 개수·총액도 `orderItem` 반환에서 읽는다 |
| **어제 만든 틀을 그대로 써라** | `.pop`/`.popcard` 에 CSS 를 한 줄도 안 보탰다. 검사가 z 가 같은지를 잰다 |
| **`transform` 조상 안에 두지 마라** | body 바로 밑(`#pantryPanel` 옆)이다. 검사 §A 가 `#sheet.contains` 로 못 박는다 |
| **폰에서 눌리는 크기** | 세 폭 다 44px 이상(§3-7) |
| **쓰기 영역** | `game.html` · `tools/test_buypopup.mjs`(신규) · `tools/test_quiet.mjs`(한 절) · `docs/engine/shots/buypopup/` · 이 문서. **그 밖은 안 열었다** |
| ⛔ **`first_play.js` 를 건드리지 마라** | 안 열었다 |

---

# §월 장부 — 월세 낸 날 **한 달 가계부**가 뜬다 (같은 날 이어서)

박사님: *"월세 나가는 날에 **월 장부 정리**가 팝업으로 뜨도록 하는 건 어때? 지출, 먹은 거,
제한 거, 번 거, 늘어난 거, 줄어든 거(돈 등) 이런 거… **가계부처럼**"*

## M-0. 먼저 네 줄

1. **틀도 줄 모양도 새로 안 지었다.** 팝업은 위 `.pop`/`.popcard`(이제 **넷**이 나눠 쓴다),
   줄은 살림 장부(`#ledger .lrow`)를 **그대로 입는다** — 선택자만 넓혔고 규칙은 한 줄도 안 바꿨다.
2. **월세는 재 보니 「살림 1일차 · 그 뒤 30일마다」다**(§M-1). 하루의 **맨 끝**에 빠진다.
3. ★★ **한 달 치를 셀 칸이 코어에 없었다**(§M-2). 있는 것은 「시작부터의 총 누적」이거나
   「그날 하루치(휘발)」뿐이다. ⇒ **월초에 누적값을 찍어 두고 하루치는 모아** 두 갈래로 냈다.
   **코어를 한 줄도 안 고쳤고**, 코어에 낼 칸의 코드는 §M-6 에 적어 두었다.
4. ★★★ **대차가 맞는다.** `들어온 것 − 나간 것 = 지갑 증감`이 **실측으로 정확히** 맞고,
   안 맞는 몫이 있으면 **「장부에 안 잡힌 것」으로 적는다**(감추면 가계부가 아니다).
   검사 신설 1벌 **48항목 전부 통과 · 부팅 예외 0**.

---

## M-1. ★ 재서 적는 것 — **월세가 언제 나가나**

`src/game/tutorial.js` 를 읽어 잰 것이다. 값을 여기 베끼지 않고 화면도 규칙에서 읽는다.

| | 값 | 어디 |
|---|---|---|
| 첫 청구일 | **살림 1일차** | `TUTORIAL_RULES.rentFirstDueDay = 1` |
| 주기 | **30일** | `rentPeriodDays = 30` |
| 액수 | 사는 방이 정한다 | `rentWonOf(ts)` — 반지하 `rentWon`(200,000) · 원룸 `oneroomRentWon`(아직 `null`) |
| 유예 | **없다** | 2026-08-09 에 없어졌다. 예고(`rent_soon`)는 이레 전 · **둘째 달부터** 산다 |

⚠⚠ **날짜가 둘이다.** 위의 「1일차」는 `ts.day`(**살림 시계**)이고 `S.day`(게임 날짜)가 아니다.
살림 시계는 첫 플레이가 끝나야 돈다(`tutorialDay` 첫 줄 `if (!firstPlayDone) return`) —
첫 플레이가 16일이라 **살림 1일차 ≈ 게임 17일**이다. 창의 「N~M일차」는 살림 시계다.

**하루 안에서의 자리** (`tutorialDay` 순서 · 실측):

```
① 수입  ② 하루 지출(식비·공과·전기 — 절감 반영)  ③ ★ 월세 목돈  ④ 파산 클램프(0원 아래로 안 감)
```

그리고 `stepTutorial` 은 `loop.nextDay` 의 **거의 마지막**이라 — **밥을 먹고 절감이 확정된 뒤**에
월세가 빠진다. ⇒ 그래서 이 창은 **하루가 다 간 뒤**에 뜬다. 되돌릴 것이 하나도 없다.

**화면이 「오늘이 월세 날인가」를 아는 창구**: `turn.tutorial.rentWon > 0` (0이면 안 낸 날).
전용 술어(`isRentDueToday` 같은 것)는 **없다.** 이벤트 `{id:'rent', first, count}` 도 오는데
⚠ **금액 필드가 없다**(액수가 `ko` 문자열 안에만 있다). 그래서 `rentWon` 을 본다.

---

## M-2. ★★★ 한 달 치를 셀 재료 — **무엇이 있고 무엇이 없었나**

### 있던 것 ㉮ — **누적값** (월초에 찍어 두고 월말에 뺀다)

| 무엇 | 경로 | 세이브 |
|---|---|---|
| 지갑 | `S.tutorial.cashWon` | ○ |
| 상점에서 산 것 | `S.shop.spentWon` | ○ |
| 판 것 **전부** | `S.shop.earnedWon` | ○ |
| 곳간 채소 판 것 | `S.firstPlay.food.totalPantrySoldWon` | ○ |
| 잉여 채소 판 것 | `S.firstPlay.food.totalSurplusSoldWon` | ○ |
| 아낀 밥값 | `S.firstPlay.food.totalFoodSavedWon` | ○ |

⚠ `earnedWon` 은 **판매 네 종류(그루·삽수·잉여·곳간)를 한 칸에** 넣는다(`shop.credit`).
⇒ **식물 판 것 = 판 것 전부 − 채소 판 것**으로만 갈린다. 화면이 그렇게 나눈다.

### 있던 것 ㉯ — **하루치**(그날만 살고 사라진다 · 하루가 갈 때 더한다)

`turn.tutorial` 의 `spentWon`(하루 지출) · `electricityWon`(등 전기) · `rentWon` ·
`savedWon` · `incomeWon` · `turn.firstPlayEvent.portions[]`(**먹은 것 — 작물별 g**) ·
수확 때 `perPot[].grams`(**거둔 것 — 작물별 g**).

★ **식비·공과 = `spentWon − electricityWon`** 이다. `spentWon` 이 `max(0, 하루지출 − 절감) + 전기`
라서 전기만 빼면 남는 것이 살림비다. **새 셈이 아니라 이미 있는 두 수의 차**다.

### ✗ 없던 것 — 그리고 **그래서 못 하는 것**

| 없는 것 | 지금 상태 |
|---|---|
| **날짜별 스냅샷·이력** | 저장소 어디에도 없다. 있는 것은 전부 「시작부터의 총 누적」이거나 「그날 하루치」다 |
| 월세 지출 **금액** 누계 | `rent.paidCount`(횟수)만 있다 |
| 식비 현금 지출 누계 | 오늘치 `cashFoodWon` 만 |
| 공과 2,500원/일 | `dailySpendWon` 상수 안에 녹아 있어 **분리 불가** — 그래서 화면도 「식비 · 공과」로 **묶어서** 적는다 |
| 식물등 사기(25,000×n) · 이사비(200만) | ⚠ **어느 장부에도 안 적힌다.** `buyLamp`·`moveOut` 이 `cashWon` 만 깎는다 |
| 작물별 먹은 g / 거둔 g 누계 | 그날치만 (⇒ 화면이 **직접 모은다**) |
| `S.ledger.today{in,out}`·`S.ledger.total` | **만들고 세이브 왕복까지 하는데 쓰는 데가 한 곳도 없다**(늘 0). 수 하나씩이라 한 달 장부를 담을 모양도 아니다 |

---

## M-3. ⚠⚠ 어디에 쌓나 — **재서 골랐다. localStorage 다**

`state.js`·`save.js` 는 이 창의 **쓰기 영역 밖**이고 세이브 스키마를 바꾸는 일이다.
⇒ **주문 개수와 같은 자리**에 뒀다: `byeot.month`(`byeot.lastOrderQty` 옆).

```
{ v:1, fromDay, snap:{cash,shopSpent,shopEarned,pantrySold,surplusSold,foodSaved},
  run:{days,out,power,rent,saved,income,harvests,ateG:{작물:g},gotG:{작물:g}}, prevNet }
```

- **월초 스냅샷**은 **부팅 때** 열린다(`monthEnsure`) — 하루가 가기 **전**이라야 첫날이 안 샌다.
- 하루가 갈 때 `run` 에 더하고(`monthNoteDay`), 수확할 때 `gotG` 에 더한다(`monthNoteHarvest` —
  ★ **수확은 턴 밖**이라 이 길로만 셀 수 있다).
- 월세를 낸 날 마감하고(`monthCloseNow`) **그 자리에서 새 달을 연다**.

⚠ **대가를 적어 둔다 — 세이브가 아니라 브라우저에 남는다.** 옛 세이브를 열면 스냅샷이
「앞날」을 가리킬 수 있어 이번 달이 음수가 된다 ⇒ **`fromDay > 지금`이면 버리고 새로 연다**
(그러면 첫 달처럼 군다. **거짓말을 하지 않는다**). ⏸ 코어에 칸을 내는 쪽이 맞다 — §M-6.

---

## M-4. ★ 언제 뜨나 — **재서 정했다. 하루의 맨 끝이다**

지시가 콕 집은 자리다(*"한 줄에 세 개가 겹치면 안 된다"*). 시간축으로 갈랐다:

| | 창 | 언제 |
|---|---|---|
| ① | **오늘 밥상**(`#mealPanel`) | 하루가 가기 **전** — 오늘 먹을 것을 정한다 |
| ② | **넘어가는 중**(`#dayAnim`) | 하루가 가는 **동안** — 시계가 새벽 6시로 |
| ③ | 배너 · 대사 | 정산 **직후** — 무슨 일이 났나 |
| ④ | ★ **한 달 가계부** | 그 **뒤** — 지난 한 달이 어땠나 |

⇒ **넷이 시간축에서 안 겹친다.** ①과 ④는 뜻이 정반대라(정하는 창 / 끝난 것을 보는 창)
순서가 뒤집히면 아직 안 지난 달을 정산하게 된다 — `dawn6-to-plan` 이 ①의 자리를 정한 것과 같은 자다.

⚠⚠ **대사보다 뒤다.** 월세 낸 날은 대사가 반드시 하나 뜬다(`rentFirst`/`rentAgain`).
겹치면 표가 몬이를 덮는다. ⇒ **대사 줄이 비는 순간**(`dlgPaint` 의 `!line` 갈래)에 연다.
대사가 아예 없으면 `commitNextDay` 끝에서 바로 연다. **검사가 「뜬 순간 `#stage.talking` 이
아니다」를 실제로 잰다**(§G).

⚠ **닫아도 하루는 그대로다.** 이미 끝난 일을 보는 창이라 되돌릴 것이 없다 —
닫는 길 셋([알겠습니다]·뒤 누르기·`Esc`) 다 확인했고, 닫은 뒤 `S.day`·지갑·살림 날짜가
한 자리도 안 움직이는 것을 실측했다.

---

## M-5. ★ 실측 — **화면에 실제로 뜬 글자** (390×844 · dpr 2)

```
첫 달 가계부
살림 1~1일차 · 월세는 30일마다 나갑니다

이번 달 −206,600원 — 지갑이 1,500,000원에서 1,293,400원으로 줄었습니다.
(첫 달이라 견줄 것이 없습니다)

나간 돈
  월세                200,000원
  식비 · 공과           7,500원
  상점에서 산 것          500원
들어온 돈
  채소 판 것            1,400원
  식물 판 것                0원
아낀 돈
  밥으로 아낀 식비      −5,000원      ← 초록. **나간 돈에서 이미 빠진 값**이라 또 더하면 두 번 센다
먹은 것 · 거둔 것
  밥이 된 것          콩나물 600g
  거둔 것        콩나물 1kg (2회전)
────────────────────────────────
  이번 달 지갑   1,500,000원 → 1,293,400원
[알겠습니다]
```

★★★ **대차 검산**: `1,400 − (200,000 + 7,500 + 500) = −206,600` = **지갑 증감과 정확히 같다.**
그래서 「장부에 안 잡힌 것」 줄이 안 떴다(0이면 안 뜬다).

**둘째 달** — 지난달과 견준다:
```
이번 달 −208,333원 — 지갑이 1,293,400원에서 1,085,067원으로 줄었습니다.
지난달은 −206,600원이었으니 1,733원 나빠졌습니다.
```

**어떻게 밟았나**: 시루 둘을 끌어다 놓고 → 심고 → 물 주고 → [다음 날]로 익혀 → 거두고 →
[상점]에서 **곳간 채소를 팔고**(1,400원) → **씨앗을 주문하고**(500원) → 월세 날.
심기·물주기·거두기·팔기·주문·[다음 날]은 **전부 화면 단추**다.
⚠ **지름길 둘**(검사·그림 다 같다): ㉮ `S.firstPlay.completed = true`(살림 시계를 켠다 —
첫 플레이 완주는 콩나물 3회전이 필요해 이 일과 무관하게 길다) ㉯ 둘째 달을 보려고
`rent.nextDueDay` 를 하루 앞으로(주기·월세액은 **안 건드렸다**).

### 세 크기 — 단추 44px 이상 · 카드가 화면 안

| 폭 | [알겠습니다] | 카드 |
|---|---|---|
| **360** | 306×44 | x12 y140 336×565 |
| **390** | 336×44 | x12 y140 366×565 |
| **430** | 376×44 | x12 y150 406×545 |

★ 셋 다 화면 안이고 가로로 안 넘친다. ⚠ 줄이 아홉이라 카드가 565px 로 높다 —
`.popcard` 가 `max-height:86dvh` 라 더 길어지면 카드 **안에서** 구른다(작물이 늘면 그렇게 된다).

### 그림

| 무엇 | 경로 |
|---|---|
| ★★ **첫 달** | `docs/engine/shots/monthly/month_first_390.png` |
| ★ **둘째 달**(지난달과 견줌) | `docs/engine/shots/monthly/month_second_390.png` |
| 360 · 430 | `docs/engine/shots/monthly/month_360.png` · `month_430.png` |

---

## M-6. ☐ 코어에 붙일 것 — **내가 안 고쳤다. 코드째로 적는다**

지금 화면이 **못 말하는 것**이 셋이고, 셋 다 코어에 칸이 없어서다.
⚠ 아래는 **내가 안 붙였다**(`tutorial.js`·`state.js`·`save.js` 는 쓰기 영역 밖).

### ㉮ 식물등 사기·이사비가 **어느 장부에도 안 적힌다**

지금 화면은 그 몫을 **「장부에 안 잡힌 것」**으로 적어 대차를 맞춘다. 제대로 하려면:

```js
/* src/game/tutorial.js §buyLamp — `ts.cashWon -= …` 바로 뒤에 */
  ts.spent = ts.spent || { lampWon: 0, moveWon: 0, rentWon: 0 };
  ts.spent.lampWon += ts.rules.lampPriceWon;

/* src/game/tutorial.js §moveOut — 이사비를 뺀 자리 뒤에 */
  ts.spent = ts.spent || { lampWon: 0, moveWon: 0, rentWon: 0 };
  ts.spent.moveWon += cost;

/* src/game/tutorial.js §tutorialDay 월세 블록 — `ts.rent.paidCount += 1;` 뒤에 */
  ts.spent = ts.spent || { lampWon: 0, moveWon: 0, rentWon: 0 };
  ts.spent.rentWon += rentPaid;
```
```js
/* src/game/save.js §packTutorial — rent 옆에 한 칸 */
  spent: {
    lampWon: needNum((t.spent || {}).lampWon ?? 0, 'tutorial.spent.lampWon', { min: 0 }),
    moveWon: needNum((t.spent || {}).moveWon ?? 0, 'tutorial.spent.moveWon', { min: 0 }),
    rentWon: needNum((t.spent || {}).rentWon ?? 0, 'tutorial.spent.rentWon', { min: 0 })
  },
/* 되세우는 쪽도 같은 모양으로(없으면 0 셋) */
```

### ㉯ 「한 달」을 자를 **스냅샷 칸**이 없다 — 그래서 지금은 브라우저에 산다

가장 작은 해법은 **월세를 낸 그 순간에 누적값을 한 번 찍어 두는 것**이다:

```js
/* src/game/tutorial.js §tutorialDay 월세 블록 안 — `ts.rent.nextDueDay += …` 뒤에
   ⚠ 누적값은 tutorial 이 안 갖는 것도 있어(shop·firstPlay) 부르는 쪽이 넘겨야 한다.
     그래서 `loop.stepTutorial` 이 opt 로 넣어 주는 편이 맞다. */
  ts.rent.close = {
    day: ts.day, cashWon: ts.cashWon,
    shopSpentWon: opt.shopSpentWon ?? null, shopEarnedWon: opt.shopEarnedWon ?? null,
    foodSavedWon: opt.foodSavedWon ?? null,
    prevNet: ts.rent.close ? ts.cashWon - ts.rent.close.cashWon : null
  };
```
⇒ 그러면 「이번 달 = 지금 누적 − `rent.close`」가 **세이브 안에서** 나오고, 판을 옮겨도 살아남는다.
지금 화면 코드(`monthSnapNow`/`monthCloseNow`)는 **그대로 그 칸을 읽게만** 고치면 된다.

### ㉰ 공과(2,500원/일)가 식비와 **안 갈린다**

`TUTORIAL_RULES.dailySpendWon` 이 「월세몫 + 공과·전기 + 식비」를 **한 수로** 갖는다.
⇒ 화면도 「식비 · 공과」로 묶어서 적는다. 가르려면 규칙에 `utilityWon` 을 따로 둬야 한다
(`data/balance/homes.json` 에 `utility 75,000` 이 이미 있다 — 읽는 데만 없다).

---

## M-7. ☐ 판단 필요 (박사님·plan 몫)

### M-7-1. ★ 지난 장부를 **다시 볼 길이 없다**

닫으면 다음 달까지 못 본다. 창구(`window.__byeotMonthShow`)는 냈는데 **단추가 없다** —
붙일 자리는 [살림] 탭의 살림 장부(`#ledger`) 밑이 자연스럽다.
⏸ 「매달 한 번 뜨는 창을 다시 볼 일이 있나」가 판단이라 안 붙였다.

### M-7-2. 30일에 한 번이면 **너무 뜸한가**

지금은 월세 주기(30일)에 묶여 있다. 반지하 한 판이 대략 100일 안팎이라 **서너 번** 뜬다.
⇒ 주마다 뜨게 하려면 주기를 따로 정해야 하는데, 그건 **월세와 무관한 새 눈금**이라 안 지었다.

### M-7-3. 「식물 판 것」이 **뺄셈으로만** 나온다

`shop.earnedWon` 이 판매 넷을 한 칸에 담아서다(§M-2 ⚠). 지금은 맞지만 **판매 갈래가
하나만 늘어도 조용히 틀린다.** ⇒ `credit()` 이 갈래별로 쌓으면 그 자리가 없어진다.

### M-7-4. 첫 달이 **첫 플레이 16일을 품는다**

살림 시계로는 1일차이지만, 그 앞의 첫 플레이 동안 거두고 먹은 것은 **「첫 달」에 잡힌다**
(스냅샷이 부팅 때 열려서다). 돈 줄은 안 그렇다(살림이 안 돌아 지갑이 안 움직인다).
⇒ 「첫 달은 배우는 기간까지 포함」이 맞다고 보고 그렇게 뒀는데, **판단이다.**

---

## M-8. ✗ 못 한 것

1. **30일을 실제로 밟아 보지 않았다.** 둘째 달은 청구일을 하루 앞으로 당겨 밟았다 —
   주기 자체(`rentPeriodDays`)는 안 건드렸지만 **30일 치가 쌓인 장부는 못 봤다.**
2. **원룸에서 안 봤다.** `rentWonOf` 를 쓰므로 이사하면 원룸 월세가 나와야 하는데
   `oneroomRentWon` 이 아직 `null` 이라(START-HERE §6) **반지하 값이 그대로 나온다.**
3. **식물등을 사 보지 않았다** — 「장부에 안 잡힌 것」 줄이 실제로 뜨는 것은 못 봤다
   (코드로는 `gap ≠ 0`일 때 뜬다).
4. **무순으로 안 밟았다.** 「먹은 것·거둔 것」은 작물별로 갈라 적는데 콩나물만 확인했다.
5. **가로 화면(landscape)을 안 쟀다.**

---

## M-9. 검사 · 안 건드린 것

### 새 검사 `tools/test_monthly.mjs` — **48항목 · 어긋난 것 0**

| | 무엇을 못 박나 |
|---|---|
| **A** | ★ **월초 스냅샷이 부팅 때 열린다** (하루가 가기 전이라야 첫날이 안 샌다) |
| **B** | ★★ **월세 낸 날 뜬다** — 주문 팝업과 **같은 틀·같은 층(84)** · `#sheet` 밖 · 카드가 화면 안 |
| **C** | ★★★ **요약이 지갑 증감과 정확히 같다** — 월초 지갑·지금 지갑·그 차 셋 다 코어 값과 대조 |
| **D** | ★★ **월세 줄이 `ts.rules.rentWon` 과 같다** · 주기도 규칙에서 읽어 적는다 |
| **E** | ★★★ **대차가 맞는다** — `들어온 것 − 나간 것 = 지갑 증감` · 「상점에서 산 것」이 `shop.spentWon` 델타와 같다 · 아낀 밥값이 `totalFoodSavedWon` 델타와 같다 |
| **F** | ★★ **닫는 길 셋** · 카드 **안**을 누르면 안 닫힌다 · **닫아도 `S.day`·지갑·살림 날짜가 안 움직인다** |
| **G** | ★★ **대사와 안 겹친다** — 대사 중에는 안 뜨고, 뜬 순간 `#stage.talking` 이 아니다 |
| **H** | ★★ **둘째 달은 지난달과 견준다**(첫 달은 「견줄 것이 없습니다」) |
| **I** | ★ **360·390·430** 단추 44px 이상 · 카드가 화면 안 · 가로로 안 넘침 |

⚠ **숫자를 안 박았다** — 월세도 지갑도 g 도 `window.__S()` 와 화면 글자를 대조한다.
⚠ **줄을 글자로 안 찾는다.** `textContent` 는 이름과 값이 **붙어서** 나온다(`월세200,000원`) —
`.lrow` 의 `span`/`b` 를 따로 읽는다. 처음에 글자로 잘랐다가 **그것 자체가 「재는 자」가 됐고**
멀쩡한 화면을 네 번 FAIL 로 적었다(START-HERE §2).
⚠ **자를 한 번 더 잘못 잡았다** — 「아낀 밥값」을 **마지막 하루 전** 값과 견줘서 하루치가
빠졌다. 월초 스냅샷과 견주는 것이 맞다. 고친 자리에 까닭을 적어 두었다.

### 안 건드린 것

| 지시 | 어떻게 |
|---|---|
| **셈을 새로 만들지 마라** | 값은 전부 코어가 낸 것을 **더하거나 뺀 것**이다. 새로 지은 셈은 「식비·공과 = 하루지출 − 전기」 하나이고 그것도 이미 있는 두 수의 차다 |
| **살림 장부와 두 벌이 되지 마라** | 저쪽은 **오늘 하루**, 이쪽은 **한 달**이다. 읽는 칸이 겹치는 것은 `cashWon` 하나뿐이고 그건 잔액이다. 줄 모양(`.lrow`)은 **같은 CSS 를 쓴다** |
| **숫자를 문구에 손으로 적지 마라** | 월세·주기·지갑·g 이 전부 코어에서 온다 |
| **팝업 틀은 같은 것을 쓰라** | `.pop`/`.popcard` 에 CSS 를 한 줄도 안 보탰다 |
| **닫는 길이 있어야 하고, 닫아도 하루는 그대로** | 셋 다 열려 있고 실측으로 확인했다 |
| **⛔ `first_play.js`·`tutorial.js`·`state.js`·`save.js`·`loop.js`** | **하나도 안 고쳤다.** 붙일 코드는 §M-6 에 적었다 |

---

> START 18:56:37 · **END 19:56:19**

---

# §다시보기 — 지난 가계부를 다시 본다 · ± 걸음 (같은 날 이어서)

> START 20:12:41 · **END 20:45:46**

박사님이 판단필요 셋에 답하셨다.

| 물음 | 답 | 어디 |
|---|---|---|
| 개수를 직접 쳐 넣는 칸을 둘까 | *"업다운 버튼을 두는 게 나을 듯?"* — **± 그대로** | §R-1 |
| 지난 가계부를 다시 볼 길 | *"이전 것도 볼 수 있게 (…) **가방 탭에 안에 버튼**"* | §R-2 |
| 「식물 판 것이 뺄셈으로만」 | 주석으로 박고 고칠 코드는 문서에 | §R-4 |

⚠ **종이 모양은 안 만들었다.** 중간에 박사님이 화면을 보시고 *"가계부는 (…) **잘 나왔네.**
**다시 보기만** 만들면 될 듯"* 하셔서 취소됐다. **`.popcard` 를 한 줄도 안 건드렸다** —
잘 나온 것을 고치면 되돌릴 일만 생긴다.

---

## R-1. ★ ± 걸음 — **재 보니 ±10 단추는 못 넣는다. 그래서 「누른 채로」로 갔다**

지시: *"큰 수를 고르기 쉬운지 재 봐라. (…) ±10 같은 큰 걸음이 있으면 손이 준다."*

### 먼저 잰 것 — **단추를 늘리면 44px 아래로 내려간다**

지금 줄은 `[−][N개][＋][전부]` 넷이고 `.prow button{flex:1 1 0}` · `.pcount{flex:1.5}` 다.
**360px 에서 실측 64px**씩 나온다. 여기에 `[−10][+10]` 을 더하면:

```
카드 안폭 308  −  틈 6px × 5  =  278
278 ÷ (1 + 1 + 1 + 1 + 1 + 1.5 단위)  =  278 ÷ 6.5  ≈  42.8px
```

⇒ **42.8px — 지시서가 못 박은 44px 아래다.** 360px 에서는 **원리적으로 막혀 있다.**
(가로로 두 줄을 만들면 카드가 길어지는데, 가계부 카드가 이미 618px 이라 그 길도 좁다)

### ⇒ **자리를 안 먹는 길**을 골랐다 — 누른 채로 있으면 빨라진다

| | |
|---|---|
| 0.4초까지 | 아무 일도 없다(그냥 한 번 누르기) |
| 그 뒤 | 0.09초마다 **한 걸음** |
| 여덟 걸음 뒤 | **다섯 걸음씩** |

★ **실측: 1.2초 누르고 있으니 3개 → 20개.** 시루 천장 25개(`drawLedger` 의 `handCap`
= 체력 × 주기)까지 **손가락 하나로 1.3초**다.
⚠ **손을 뗄 때 오는 클릭은 한 번 먹는다**(`holdAte`) — 화면에 뜬 수를 보고 뗐는데 그 뒤에
하나가 더 붙으면 **고른 수가 아니게 된다.** 검사가 그것을 못 박는다(§J).
⚠ 위끝·아래끝에 닿아 회색이 되면 **저절로 멈춘다** — 안 멈추면 눌린 채로 헛돈다.

### ★ 그리고 **큰 수를 고르는 값은 한 번만 치른다**

디폴트가 직전 주문량이라(§주문 개수) **두 번째부터는 손이 0**이다. 즉 「시루 12개」를
고르는 값은 그 품목을 **처음 시킬 때 한 번**뿐이다. [전부]도 그대로 산다.

---

## R-2. ★★ 다시 보기 — **[가방] 탭 단추 + ‹ 이전 달 · 다음 달 ›**

### R-2-1. [가방] 탭 안 단추 — 박사님이 자리를 짚으셨다

가방 격자 밑에 줄 하나를 긋고 그 아래 놓았다(`#ledgerBackBox`).

```
─────────────────────────────────
📒 가계부 다시 보기 (2달)
```

⚠ **쌓인 것이 없으면 감추지 않고 회색**이다. 그리고 **왜 회색인지**를 바로 밑에 적는다:

```
📒 가계부 다시 보기                        ← 회색
월세를 처음 내는 날 첫 장부가 생깁니다 — 그때부터 여기서 다시 볼 수 있습니다
```

★ 감추면 「그런 길이 있다」를 배울 자리가 아예 없다 — 곳간 팔기 단추가 같은 판단으로
서 있다(pantrysale §1-8 ②). 대신 회색인 까닭을 적어 「눌러도 아무 일이 없다」를 막는다.
★ 단추 글자가 **몇 달 쌓였는지**를 말한다 — 수는 목록 길이에서 읽는다(손으로 안 적었다).

### R-2-2. 달 넘기기 — **‹ 이전 달 · N / M · 다음 달 ›**

★ **`.prow` 를 그대로 입는다** — 고르개들이 쓰는 그 줄이다. 새 CSS 를 한 줄도 안 지었고
**44px 도 그것이 지킨다**(실측 360에서 84×44 · 126×44 · 84×44).
⚠ **쌓인 달이 둘 이상일 때만 뜬다.** 하나뿐인데 ‹›가 있으면 회색 둘이 서 있는 것이다.
⚠ 끝에 닿으면 그쪽이 회색이 된다(맨 앞에서 ‹ · 맨 뒤에서 ›).

### R-2-3. [✕] — **닫는 길을 늘렸다. 줄이지 않았다**

★ **했다.** 「지금이 좋다」에 걸리는지 재 봤는데 **안 걸린다**고 판단했다. 까닭 셋:

| | |
|---|---|
| ① | 박사님이 **직접 짚으신 것**이고(*"x 눌러서 끄게"*), 취소된 것은 종이 모양뿐이었다 |
| ② | 이 창은 **읽는 창**이고 아홉 줄이라 카드가 **618px**로 길다(실측). 위를 읽는 동안 아래 [알겠습니다]는 엄지에서 멀다 |
| ③ | 카드 모양이 **안 바뀐다** — 오른쪽 위 빈자리에 동그라미 하나다. 상세창(`#dClose`)이 이미 같은 자리를 쓴다 |

⇒ 닫는 길이 **넷**이 됐다: **[✕] · [알겠습니다] · 뒤 누르기 · `Esc`**. 하나도 안 없앴다.
⚠ ✕ 는 **44×44** 다 — `#dClose` 는 34px 인데 그건 지시서 위끝 아래다. 여기는 44로 뒀다.
⚠ `position:relative` 는 **이 카드에만** 걸었다(`#monthPanel .popcard`). 넷이 쓰는
`.popcard` 를 통째로 바꾸면 앞으로 절대 배치를 넣는 사람이 어디에 걸릴지가 달라진다.

---

## R-3. ⚠⚠ 재서 정한 것 둘 — **몇 달까지 쌓나 · 판이 바뀌면 어떻게 가르나**

### R-3-1. **열두 달**까지 쌓는다

세 가지를 재서 정했다.

| | 잰 것 | |
|---|---|---|
| ① **한 판이 몇 달인가** | 월세 주기 30일 · 살림 시계는 첫 플레이(16일) 뒤에 돈다 ⇒ 반지하 한 판(게임 100일)이 **월세 3번**쯤. 원룸까지 넉넉히 잡아 **다섯~여덟 달** | 열두 달이면 **한 판을 통째로 담고도 남는다** |
| ② **자리가 모자라나** | 장부 한 장이 **269바이트**(실측). 열두 장이면 **3.2KB** — localStorage 한도(대개 5MB)의 **0.06%** | ⇒ **자리는 자르는 까닭이 아니다** |
| ③ **넘겨 보는 손** | ‹›로 **한 장씩** 넘기는 창이다. 스무 장이 넘으면 「거기까지 어떻게 가나」가 새 문제가 된다. 목록·구르기를 만들면 그건 **새 화면**이다 | ★ **이것이 진짜 까닭이다** |

⇒ **열두 달(한 해)**. 넘치면 **오래된 것부터** 버린다 — 가까운 달이 더 자주 보는 것이다.
검사가 실제로 못 박는다: 열네 장으로 부풀린 뒤 한 달을 더 마감하면 **열두 장**이 남고
**맨 앞 셋이 밀려나 있다**(§L).

### R-3-2. ★★ 판이 바뀌면 지난 장부가 **안 남는다** — 셋으로 갈랐다

장부는 **그 판에 딸린 것**이다. 새 판에 지난 판의 월세가 뜨면 그건 거짓말이다.

⚠ 먼저 잰 것: **세이브에 「이 판이 언제 시작됐나」가 없다.** `save.peek` 이 내주는 것은
`savedAt`(저장할 때마다 바뀐다)뿐이고 판 도장 같은 칸이 없다. `save.js` 는 쓰기 영역 밖이라
칸을 낼 수도 없다. ⇒ **도장을 내가 찍는다**(`runId` — `byeot.month` 안에 산다).

| | 언제 | 어떻게 잡나 |
|---|---|---|
| **㉮** | 세이브가 **없거나 못 읽는데** 장부만 있다 | 부팅 때 `resumeSave()` 가 거짓이면 새 판이다. ★ **제일 튼튼한 자다 — 장부는 판에 딸린 것이고 판이 없으면 장부도 없다** |
| **㉯** | **날짜가 뒤로 갔다**(옛 세이브를 열었다) | 이번 달 시작일이 지금 살림 날짜보다 **앞날**이면 버린다. 안 그러면 이번 달이 음수가 된다 |
| **㉰** | **[지금 판을 지웁니다]** | 그 자리에서 직접 지운다(`monthResetRun`). ⚠ 세이브 키만 지우던 길이라 **안 걸면 장부가 살아남는다.** [완전 초기화]는 `localStorage.clear()` 라 저절로 간다 |

⚠ **주문 개수 기억(`byeot.lastOrderQty`)은 안 지운다** — 그건 판이 아니라 **손버릇**이다
(`byeot.pointerMode`·음악 켜짐과 같은 결). 검사가 그것도 못 박는다.

⚠ 저장 모양이 바뀌었으므로 `v` 를 **2** 로 올렸다. 옛(v1) 것은 버리고 새로 연다 —
억지로 이어 붙이면 「이번 달만 있고 지난 달은 없는」 반쪽 목록이 된다.

### ★★ R-3-3. 재다가 알게 된 것 — **`pagehide` 가 지운 세이브를 되살린다**

㉮ 를 재려고 「세이브 키를 지우고 새로고침」을 했는데 **장부가 안 지워졌다.**
까닭은 내 코드가 아니었다: **떠나면서 `pagehide` 가 저장을 한 번 더 한다**
(`addEventListener('pagehide', saveNow)`). 그래서 방금 지운 자리가 **다시 채워진 채로**
새 판이 열리고, `resumeSave()` 는 참을 낸다.

★ **이 저장소가 이미 아는 사고다** — `hardReset` 이 `saveOff` 를 **먼저** 세우는 까닭이
바로 이것이고, 그 주석에 *"실제로 그래서 「초기화를 눌러도 Day 44 그대로」였다"* 라고
적혀 있다. 나는 그 주석을 읽고도 **검사에서 같은 함정을 밟았다.**
⇒ 검사는 **부팅이 세이브를 읽기 전에** 지우도록 고쳤다(부팅은 growth 를 세우느라 몇 초가
걸리고 `resumeSave()` 는 그 뒤에 돈다). 그 자리에 까닭을 적어 두었다.
⇒ ★ 그리고 이것이 **㉰ 가 반드시 있어야 하는 까닭**이기도 하다 — 사람이 [지금 판을
지웁니다]를 누르는 길에서는 `pagehide` 가 안 끼므로 그 자리에서 직접 지워야 한다.

---

## R-4. 「식물 판 것이 뺄셈으로만 나온다」 — **주석으로 박았다**

`shop.earnedWon` **한 칸**이 판매 **넷**(그루·삽수·잉여·곳간)을 다 받는다(`shop.js §credit`).
그래서 화면은 「식물 판 것 = 판 것 전부 − 채소 둘」로 낸다. 지금은 맞다.

⚠⚠ **언제 조용히 틀리나** — 그 자리(`monthCloseNow`)에 코드째로 적어 두었다:

> **판매 갈래가 하나만 늘면 이 줄이 조용히 틀린다** — 늘어난 그 갈래를 「식물 판 것」으로
> 세고, 화면은 아무 말도 안 한다(**합이 여전히 맞아서 대차로도 안 잡힌다**).
> 예: 「씨앗 되팔기」·「가구 팔기」가 생기면 그날부터 식물값이 부풀어 있다.

### ☐ 고칠 코드 (§M-6 ㉱) — ⛔ `shop.js` 는 쓰기 영역 밖이라 **안 붙였다**

```js
/* src/game/shop.js §credit — 지금은 한 칸에 다 넣는다.
   ⇒ 갈래를 받아 따로 쌓으면 화면의 뺄셈이 통째로 없어진다. */
function credit(S, won, kind /* 'plant' | 'cutting' | 'surplus' | 'pantry' */) {
  const shop = shopOf(S);
  shop.earnedWon += won;                       // 옛 칸은 그대로 둔다(읽는 데가 여럿이다)
  shop.earnedBy = shop.earnedBy || { plant: 0, cutting: 0, surplus: 0, pantry: 0 };
  if (kind && shop.earnedBy[kind] != null) shop.earnedBy[kind] += won;
}
/* 부르는 자리 넷에 갈래를 붙인다: sellPot → 'plant' · sellCutting → 'cutting' ·
   sellCropSurplus → 'surplus' · sellPantryCrop → 'pantry' */
```
```js
/* src/game/save.js §packShop — earnedWon 옆에 한 칸.
   ⚠ 없으면 0 넷으로 되세운다(옛 세이브가 이 칸을 모른다) */
  earnedBy: {
    plant:   needNum((sh.earnedBy || {}).plant   ?? 0, 'shop.earnedBy.plant',   { min: 0 }),
    cutting: needNum((sh.earnedBy || {}).cutting ?? 0, 'shop.earnedBy.cutting', { min: 0 }),
    surplus: needNum((sh.earnedBy || {}).surplus ?? 0, 'shop.earnedBy.surplus', { min: 0 }),
    pantry:  needNum((sh.earnedBy || {}).pantry  ?? 0, 'shop.earnedBy.pantry',  { min: 0 })
  },
```
⇒ 붙이면 `monthCloseNow` 의 그 두 줄이 **`earnedBy` 를 그대로 읽는 것**으로 바뀐다.
⚠ **`earnedWon` 은 안 지운다** — 읽는 데가 여럿이고, 갈래 합이 그것과 같은지를 검사가
못 박는 자리가 하나 더 생긴다.

---

## R-5. ★ 실측 — **화면에 실제로 뜬 글자** (390×844 · dpr 2)

**[가방] 탭 · 아직 장부가 없을 때**
```
📒 가계부 다시 보기                       ← 회색
월세를 처음 내는 날 첫 장부가 생깁니다 — 그때부터 여기서 다시 볼 수 있습니다
```

**[가방] 탭 · 두 달이 쌓인 뒤**
```
📒 가계부 다시 보기 (2달)                 ← 살아난다. 까닭 줄은 사라진다
```

**누르면 — 가장 가까운 달이 먼저 뜬다**
```
2번째 달 가계부                                              [✕]
살림 2~2일차 · 월세는 30일마다 나갑니다
…
[‹ 이전 달] [ 2 / 2 ] [다음 달 ›]        ← [다음 달 ›]이 회색(끝이다)
[알겠습니다]
```

**[‹ 이전 달] — 첫 달로 넘어간다**
```
첫 달 가계부                                                 [✕]
살림 1~1일차 · 월세는 30일마다 나갑니다

이번 달 −206,600원 — 지갑이 1,500,000원에서 1,293,400원으로 줄었습니다.
(첫 달이라 견줄 것이 없습니다)
…
[‹ 이전 달] [ 1 / 2 ] [다음 달 ›]        ← [‹ 이전 달]이 회색(맨 앞이다)
```

### 세 크기 — 단추 44px 이상 · 카드가 화면 안

| 폭 | [✕] · ‹ · N/M · › · [알겠습니다] | 카드 |
|---|---|---|
| **360** | 44×44 · 84×44 · 126×44 · 84×44 · 306×44 | x12 y113 336×618 |
| **390** | 44×44 · 93×44 · 139×44 · 93×44 · 336×44 | x12 y113 366×618 |
| **430** | 44×44 · 104×44 · 156×44 · 104×44 · 376×44 | x12 y123 406×598 |

★ 셋 다 화면 안이고 가로로 안 넘친다. [가방] 단추도 **44px**(실측).
⚠ 줄 넘기개가 붙어 카드가 565 → **618px** 이 됐다. `max-height:86dvh`(844에서 726)라
아직 여유가 있는데, **작물이 늘면 넘친다** — 그때는 카드 **안에서** 구른다(눈으로는 못 봤다).

### 그림

| 무엇 | 경로 |
|---|---|
| [가방] 탭 · 회색 단추와 까닭 | `docs/engine/shots/monthly/back_btn_empty_390.png` |
| ★ **[가방] 탭 · 살아난 단추 (2달)** | `docs/engine/shots/monthly/back_btn_390.png` |
| ★★ **다시 보기 — 2 / 2** | `docs/engine/shots/monthly/back_open_2of2_390.png` |
| ★★★ **‹ 이전 달 — 1 / 2 · [✕] 가 보인다** | `docs/engine/shots/monthly/back_open_1of2_390.png` |
| 360 · 430 | `docs/engine/shots/monthly/back_360.png` · `back_430.png` |

---

## R-6. 검사 — **열 벌 전부 통과**

| 벌 | 결과 |
|---|---|
| ★ **`test_monthly`** | **잰 것 85개 · 어긋난 것 0개** (48 → 85 로 늘렸다) |
| ★ **`test_buypopup`** | **잰 것 71개 · 어긋난 것 0개** (67 → 71) |
| `test_sellpopup` | **잰 것 54개 · 어긋난 것 0개** |
| `test_uiwire` | **55개 모두 통과** |
| `test_quiet` | **26/26 통과** |
| `test_pantrysale` | **13벌 전부 통과** |
| `test_bagcell` | **잰 것 55개 · 어긋난 것 0개** |
| `test_nextday_gate` | **25/25 통과** |
| `test_dawn6` | **46/46 통과** |
| `test_first_play` | **PASS** (`loop` · `harvest_atomic` 포함) |
| ★ **`game.html` 부팅 예외** | **0** |

⚠ **이번엔 기존 검사를 하나도 안 고쳤다.** 늘린 것은 내가 지은 둘뿐이다.

### 늘린 절

| | 무엇을 못 박나 |
|---|---|
| **A(추가)** | ★ [가방] 단추가 **회색 + 까닭**이다 · 44px · 판 도장이 찍혀 있다 · 목록이 비어 있다 |
| **J·K** | ★★★ 마감한 장부가 **목록으로 쌓인다** · 단추가 **살아나고 몇 달인지 적는다** · 누르면 **가장 가까운 달**이 뜬다 · **‹›로 내용이 실제로 바뀐다** · 끝에서 회색이 된다 · **[✕] 44px 로 닫힌다** · ✕로 닫아도 하루는 그대로다 |
| **L** | ★ **열두 장까지만** 남고 **오래된 것부터** 버린다 (장부 한 장 269B 도 잰다) |
| **M** | ★★★ **판이 바뀌면 목록이 비워진다** — ㉮(세이브 없음)·㉯(날짜 역행) 둘 다 · 판 도장이 새로 찍힌다 · ⚠ **주문 개수 기억은 남는다** |
| **J(buypopup)** | ★★ **누른 채로 있으면 오른다**(3 → 20개 · 1.2초) · **뗄 때 하나가 더 안 붙는다** · 다음 한 번 누르기는 정상 · 고르다 그만둔 값은 기억에 안 남는다 |

⚠ **지름길을 둘 더 썼고 둘 다 검사 안에 적었다**: §L 은 열두 달을 실제로 밟는 대신
**쌓인 목록에만** 가짜 장부를 밀어 넣는다(360일이 든다) · §M 은 가짜 장부 한 장을 넣는다.
**코어도 값도 안 건드린다 — 건드린 것은 localStorage 뿐이다.**

---

## R-7. ✗ 못 한 것

1. **열두 달을 실제로 밟지 않았다.** 상한은 목록을 부풀려 쟀다(360일이 든다).
2. **㉰([지금 판을 지웁니다])를 화면 단추로 안 밟았다** — 그 단추는 첫 플레이 자동
   시뮬레이션을 통째로 돌린다(길다). ㉮·㉯ 는 밟았고, ㉰ 는 **같은 함수**(`monthResetRun`)를
   부르므로 코드로는 한 줄이다. ⚠ **눈으로 본 것은 아니다.**
3. **가로 화면(landscape)을 안 쟀다.** 카드가 618px 이라 가로에서는 반드시 구를 것이다.
4. **진짜 손가락으로 「누른 채로」를 안 해 봤다** — 합성 포인터 사건으로 쟀다.
   0.4초·0.09초가 손가락에 맞는지는 실기기 확인이 필요하다.
5. **판이 여럿인 경우(세이브 슬롯)는 없다** — 이 게임은 판이 하나뿐이라 그 물음이 없다.

---

## R-8. ☐ 남은 판단 (박사님·plan 몫)

### R-8-1. 카드가 618px 이다 — **줄이 더 늘면 구른다**

작물이 셋째로 늘면 「먹은 것·거둔 것」 줄이 길어지고, `earnedBy` 를 붙이면 「들어온 돈」이
넷으로 갈린다. ⇒ 그때는 `max-height:86dvh` 안에서 구르게 되는데 **구르는 가계부**가
읽기 좋은지는 판단이다. 접는 묶음(나간 돈 ▸)으로 가는 길도 있다.

### R-8-2. 「다시 보기」를 **[방] 탭에도** 둘지

박사님이 [가방]을 짚으셔서 거기 뒀다. 다만 **오늘치 살림 장부(`#ledger`)는 [방] 탭**의
「반지하 살림」 상자 안에 있다(재서 확인했다 · `#pageRoom`). ⇒ 「오늘 장부」와
「지난 달 장부」가 **서로 다른 탭**에 산다. 한자리에 모으는 길이 있는데 —
박사님이 짚으신 자리를 마음대로 안 옮기는 것이 맞다고 봐서 그대로 뒀다.

---

> 첫 일 START 18:56:37 · END 19:56:19
> **둘째 일 START 20:12:41 · END 20:45:46**
