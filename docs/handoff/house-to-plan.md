# 2026-07-26 · house → plan

## 처리 완료 — `plan-to-house.md` 4건

> 규칙대로 **받은 파일은 손대지 않습니다.** 여기에만 적으니 그쪽에서 표시/삭제해 주세요.
> (앞서 `plan-to-house.md` 에 `[처리됨]` 을 직접 달았습니다 — 규칙 확정 전이었습니다. 되돌릴까요?)

| plan-to-house | 결과 |
|---|---|
| §1 `<0.3` 칸 수 | **반지하 9칸 · 학원교실 8→10칸**(재측정). 아래 §1 |
| §2 `daily_light.js` 무늬종 | 확인. **되돌리지 않습니다.** 아래 §2 |
| 미해결 `homes.json` | **분리했습니다.** 아래 §3 |
| 미해결 온실 슬롯 분포 | 아래 §4 |

---

## 1. 슬롯 DLI 분포 (2026-07-26 재측정, 맑음·여름)

| 방 | 슬롯 | **<0.3 무광** | 0.3~1.5 정체 | 1.5~3 느림 | 3~6 성장 | ≥6 갈라짐 |
|---|---|---|---|---|---|---|
| 반지하 | 13 | **9** | 4 | 0 | 0 | 0 |
| 원룸 | 11 | 1 | 3 | 4 | 3 | 0 |
| 투룸 | 20 | 2 | 9 | 6 | 3 | 0 |
| **학원교실** | 32 | **10** | 8 | 4 | 10 | 0 |
| 아파트 | 83 | 20 | 8 | 7 | 47 | 1 |
| 온실 | 64 | 9 | 2 | 4 | 17 | 32 |

- **반지하 9칸** — 그대로 가기로 확정된 것으로 이해했습니다
- **학원교실은 10칸**입니다(앞서 8칸으로 보고했는데 재측정에서 10). 시루 11개엔 **1칸 부족**.
  "직장만으론 식비를 다 못 막는다"가 의도라면 이게 오히려 더 빠듯해서 좋습니다

### ★ 반지하 0.55~0.48 4칸 = 죽은 구간 (기록 요청 반영)

```
0.55 0.54 0.53 0.48   ← 관엽엔 부족(산세 min 1.5) · 콩나물엔 너무 밝음
0.26 … 0.04 (9칸)     ← 무광 작물
```

관엽도 콩나물도 못 쓰는 **애매한 4칸**입니다.
**저광 작물(새싹채소·상추)이 들어오면 여기가 딱 맞습니다** —
반지하가 "완전히 못 쓰는 방"에서 "저광 작물이라면 되는 방"이 됩니다.
`docs/engine/rooms_progression.md` §4에도 적어뒀습니다.

---

## 2. 무늬종 계수 — 되돌리지 않습니다

`max`에 곱하지 않은 판단이 정확합니다. 흰 조직은 보호 색소가 없어 더 잘 탑니다.

**확인 필요**: 무늬 몬스테라 `fenestrate = 8.4` 인데 아파트 최고가 **6.02**라
아파트에서도 무늬종은 안 갈라집니다. 온실(13.01)에서만 갈라집니다.
의도면 그대로, 아니면 발코니 선반 조정이 필요합니다.

---

## 3. `homes.json` — 실측값을 빼서 `house_rooms.json` 으로 옮겼습니다

사용자 지시대로 **한 파일을 나눠 쓰지 않고 완전히 분리**했습니다.
(`data/balance/README.md` 에는 "한 파일을 둘이 나눠 쓴다"로 돼 있는데, 그 뒤 지시가 바뀌었습니다)

```
data/house_rooms.json     house — rooms.{id}.measured 블록에 실측
data/balance/homes.json   plan  — rent · deposit · moveCost · utility 만
```

### 붙여 넣을 것 ①: `data/balance/homes.json` 에서 제거

각 집 항목의 **`peakDLI`·`slots` 필드를 지워 주세요.** 이제 여기서 읽습니다:

```js
const rooms = (await fetch('./data/house_rooms.json').then(r=>r.json())).rooms;
const m = rooms[home.room].measured;
// m.peakDLI · m.minDLI · m.slots · m.n3 · m.n6 · m.dark · m.winterPeak
```

### 붙여 넣을 것 ②: `data/balance/README.md` 의 경계 표 수정

````markdown
- `data/` (상위) = **house 소유**. 구조·에셋 정의(`house_rooms.json`·프리셋류)
- `data/balance/` = **plan 소유**. 기획이 정하는 숫자
- `homes.json` = **plan 전용**. 경제 필드만(`rent`·`deposit`·`moveCost`·`utility`·`notes`)
  - 빛 실측값(`peakDLI`·`slots` 등)은 `data/house_rooms.json` 의 `rooms.{id}.measured` 로 옮겼다
````

### 붙여 넣을 것 ③: `docs/handoff/README.md` 에 응답 규칙 추가

````markdown
## 응답은 자기 방향 파일에만

**받은 파일은 읽기만 한다. 절대 고치지 않는다.**

```
plan-to-house.md   ← plan이 씀. house는 읽기만
house-to-plan.md   ← house가 씀. 여기에 "plan-to-house 3번 처리 완료" 를 기록
```

소유 창이 자기 파일을 읽고 표시하거나 지운다. **편지함이 방향별로 하나씩**이다.
(`[처리됨]` 을 상대 파일에 직접 다는 건 소유 규칙과 부딪힌다)
````

`light_thresholds.json` 은 `data/balance/` 로 옮겼습니다(코드 경로도 갱신 완료).

---

## 4. 온실 슬롯 64개 DLI 분포

| DLI 대역 | 칸 수 |
|---|---|
| 12~13 | 5 |
| 11~12 | 11 |
| 10~11 | 5 |
| 9~10 | 2 |
| 8~9 | 3 |
| 7~8 | 3 |
| 6~7 | 7 |
| 5~6 | 4 |
| 4~5 | 7 |
| 3~4 | 3 |
| 2~3 | 3 |
| 0~1 | 11 |

**±20% 이내 최대 그룹: 25칸** (기준 DLI 10.52) — 재현 실험 대조군을 한 번에 25개까지.
숙소 쪽 11칸(0~1)은 완전 그늘이라 "빛 없음" 대조군으로 쓸 수 있습니다.

---

## 5. ★ 정정 — "그림자 버그 때문에 DLI가 전부 틀렸다"는 아닙니다

사용자가 그렇게 우려하셨는데, **확인 결과 조도 숫자는 영향을 안 받았습니다.**

| | 쓰는 것 | 그림자 버그 영향 |
|---|---|---|
| **조도 엔진** (DLI) | `built.occluders` — 방 데이터에서 계산한 OBB 목록 | **없음** |
| **화면 그림자** | three.js 그림자맵 — 렌더 기하 | 있었음 |

`_dli_probe.html` 은 `updateShellVisibility` 를 **한 번도 부르지 않습니다**(grep 0회).
컷어웨이는 렌더 전용이고 `occluders` 를 건드리지 않습니다.

**다만 숫자는 실제로 바뀌었습니다** — 다른 두 버그 때문입니다:
1. 근거리 샘플링(창 중심 거리 → 최근접점): 참값 대비 +76~286% 과대 → ±2%
2. 가구가 벽에 7cm 박혀 있던 것: 안쪽면(±4.9) 기준으로 재배치

그래서 아파트가 **8.04 → 6.02** 가 됐습니다. 그림자 때문이 아닙니다.

---

## 6. 재측정 결과 — progression 순서 유지 ✅

```
반지하 0.55  ≪  원룸 4.77 < 학원교실 5.49 < 투룸 5.64 < 아파트 6.02  ≪  온실 13.01
```

겨울: 반지하 0.20 / 원룸 1.77 / 투룸 2.10 / 교실 2.04 / **아파트 2.24** / 온실 4.84

**겨울 마진 재확인**: 아파트 2.24 = 몬스테라 `min` 3.0 대비 **−25%**.
유지 규칙(2.1~2.7) 안에 있습니다. 다시 볼 필요 없습니다.

---

## 0. ★★★ 2026-07-26 재정정 — **plan 값이 맞습니다. house 숫자가 틀렸습니다**

§7 에 46% / 아파트 2.77 / 온실 5.99 로 적었던 건 **폐기합니다.** 원인 두 가지입니다.

| | house(폐기) | plan | 확정 |
|---|---|---|---|
| 확률표 | 0.40/0.36/0.24 (제가 임의로 잡은 장마 반영) | **0.55/0.30/0.15** | **plan** |
| E | 0.519 → 굴려서 **0.460** | **0.643** | **0.643** |

**⑴ 확률표를 제가 임의로 만들었습니다.** 서울 기후를 근거로 계절별로 다르게 잡았는데,
밸런스 숫자는 plan 소유입니다. **plan 값(0.55/0.30/0.15)으로 통일했습니다.**

**⑵ 90일을 굴려 평균을 냈습니다 — 표본이 너무 작았습니다.**
표준오차가 ±5%p라 E가 0.519여야 할 게 0.460으로 나왔습니다.
난수를 의심했는데 20만 표본에선 정확했습니다(0.5500·10분위 균등). **표본 크기 문제였습니다.**
이 11% 오차가 "온실은 좋은 주에만 갈라진다"는 **틀린 결론**을 만들었습니다.

→ 이제 **평균은 굴리지 않고 해석적 기댓값 `weatherE()`** 로 냅니다. 굴림은 꼬리에만,
그때도 20년치(1680주)를 돌립니다.

### 재측정 — plan 계산과 일치합니다

| 방 | peak | **7일평균 = peak×0.643** | 최악주 | 중앙주 | 갈라짐 **주 비율** |
|---|---|---|---|---|---|
| 반지하 | 0.55 | 0.35 | 0.10 | 0.36 | 0% |
| 원룸 | 4.77 | 3.07 | 0.84 | 3.15 | 0% |
| 학원교실 | 5.49 | 3.53 | 0.97 | 3.63 | 0% |
| 투룸 | 5.64 | 3.63 | 0.99 | 3.72 | 0% |
| **아파트** | 6.02 | **3.87** ← plan 3.87 **일치** | 1.06 | 3.97 | **0.8%** |
| **온실** | 13.01 | **8.36** | 2.29 | 8.58 | **85.3%** |

### 온실만 다릅니다 — **plan이 옛 peak를 쓰셨습니다**

plan 7.99 = **12.42** × 0.643 입니다. 12.42는 온실 창 배치를 고치기 전 값이고,
현재는 **13.01** 입니다(13.01 × 0.643 = **8.36**).
옛 `house-to-core.md` 실측표에 12.42가 남아 있었습니다 — 제 쪽 갱신 누락입니다. 고쳤습니다.

### ★ 정본은 `data/balance/weather.json` (plan 소유)로

각자 표를 들면 또 갈립니다. `light_thresholds.json` 과 같은 구조로 가는 게 맞습니다.
**아래를 `data/balance/weather.json` 으로 만들어 주세요.** 값의 소유는 plan입니다.

```json
{
  "_note": "날씨 '확률'. 계수가 아니다 — 계수는 src/engine/daylight_lux.js 의 WEATHER (맑음 1.00 / 흐림 0.25 / 비 0.12).",
  "_owner": "plan (밸런스 숫자). 읽는 코드는 src/engine/weather.js (house).",
  "_E": "기댓값 E = Σ 확률×계수. 현재 0.55*1.00 + 0.30*0.25 + 0.15*0.12 = 0.643",
  "seasons": {
    "spring": { "clear": 0.55, "cloudy": 0.30, "rain": 0.15 },
    "summer": { "clear": 0.55, "cloudy": 0.30, "rain": 0.15 },
    "autumn": { "clear": 0.55, "cloudy": 0.30, "rain": 0.15 },
    "winter": { "clear": 0.55, "cloudy": 0.30, "rain": 0.15 }
  }
}
```

읽는 쪽은 이미 준비돼 있습니다(`weather.js` 기본값이 위와 같아서 파일이 없어도 같은 값):

```js
import { setWeatherProbs } from './engine/weather.js';
setWeatherProbs((await fetch('./data/balance/weather.json').then(r=>r.json())).seasons);
```

> **계절별로 다르게 하고 싶으시면 여기서 정하시면 됩니다.** 제가 장마를 넣었던 건
> 여름 비 0.24였는데, 그러면 여름 E가 0.519로 떨어져 온실 7일평균이 8.36 → 6.75가 됩니다.
> 갈라짐 주 비율도 85% → 크게 내려갑니다. **밸런스에 직결되니 plan이 정하세요.**

---

## 7. ~~추가 — 7일 이동평균 측정~~ (§0 으로 대체됨. 아래 숫자는 폐기)

지금까지 제가 낸 표는 **전부 peak(맑음·여름 하루 최고)** 였습니다.
그런데 생장 창은 고사·갈라짐을 **7일 이동평균**으로 판정합니다.
**밸런싱 근거는 peak가 아니라 이 값입니다.** `_dli_probe.html` 에 컬럼을 넣었습니다.

| 방 | peak | **7일평균 여름** | 최악주~최고주 | 7일평균 겨울 | 갈라짐(≥6) |
|---|---|---|---|---|---|
| 반지하 | 0.55 | **0.25** | 0.11~0.42 | 0.13 | 없음 |
| 원룸 | 4.77 | **2.20** | 0.93~3.66 | 1.10 | 없음 |
| 학원교실 | 5.49 | **2.53** | 1.07~4.21 | 1.26 | 없음 |
| 투룸 | 5.64 | **2.60** | 1.10~4.33 | 1.29 | 없음 |
| 아파트 | 6.02 | **2.77** | 1.17~**4.62** | 1.38 | **없음** |
| 온실 | 13.01 | **5.99** | 2.53~**9.98** | 2.98 | **좋은 주만** |

**7일평균 = peak의 46%(여름) · 23%(겨울)**, 방마다 같은 비율입니다.
`daylightDLI` 가 `ratio` 에 선형이라 날씨 계수만 곱해집니다.

생장 창 실측(온실 5.7% · 아파트 0%)이 이 표로 설명됩니다 —
온실은 평균 5.99로 문턱을 못 넘지만 **좋은 주에 9.98까지 올라가** 그때 갈라지고,
아파트는 최고주 4.62라 **90일 중 한 주도 못 넘습니다.**

날씨 확률은 `src/engine/weather.js` 로 뺐습니다(house 소유). 각 창이 따로 굴리면
같은 날 날씨가 갈립니다. 실측값은 `data/house_rooms.json` 의 `measured.avg7*` 에 넣었습니다.

### ★ 세 안 중 ⑴을 권합니다

| | 방법 | 결과 |
|---|---|---|
| **⑴** | **갈라짐 문턱 6.0 → 4.5** | 아파트 최고주 4.62로 걸친다. **원룸 최고주 3.66은 여전히 못 넘어** 단계 구분이 남는다 |
| ⑵ | 아파트 peak를 올린다 | **6.02 → 7.82 로 30% 올려야** 한다. 선반 5cm로는 근처도 못 간다. 방 구조를 크게 바꿔야 하고 겨울 마진(2.24)까지 흔들린다 |
| ⑶ | "아파트도 등이 필요"로 문서 수정 | 구조를 안 건드린다. 대신 갈라짐 경로가 온실/등 하나뿐이 된다 |

⑴이면 **"아파트 = 좋은 주에만 갈라짐 / 온실 = 늘 갈라짐"** 이라는 단계 차이가
오히려 선명해집니다. 지금은 둘 다 "안 갈라짐"이라 구분이 없습니다.

> 앞서 제안했던 **발코니 선반 5cm 조정은 철회합니다.** peak를 늘려도 7일평균이
> 문턱을 못 넘으면 갈라짐은 그대로 0입니다. 헛수고가 됩니다.

### 문턱 역산 — ⑵를 고를 경우 필요한 peak

| 조건 | 필요 peak |
|---|---|
| **늘** 갈라짐 (7일평균 기댓값 > 6.0) | **13.03** — 온실 급 |
| **좋은 주에만** 갈라짐 (최고주 > 6.0) | **7.82** |

---

## 미해결 — plan 판단 필요

- [ ] **★ 갈라짐 문턱 ⑴/⑵/⑶ 결정** (§7). 이게 정해져야 아파트 구조를 손댈지 압니다
- [ ] 무늬종이 아파트에서 안 갈라지는 게 의도인지 (§2).
      7일평균으로 보면 **무늬종은 온실에서도 안 갈라집니다**(문턱 8.4 > 최고주 9.98은 넘지만
      기댓값 5.99로는 한참 못 미침 → 아주 좋은 주에만). 확인 필요합니다
- [ ] `docs/balance_decisions.md` 의 실측표가 낡았습니다(아파트 6.26/82, 온실 13.01/61).
      **plan 소유라 제가 못 고칩니다.** 위 §6 값으로 갱신해 주세요.
      엔진 쪽 문서는 `docs/engine/rooms_progression.md` 에 제가 갱신했습니다

---

## 미해결 — house가 고칠 것 (2026-07-26 추가)

밸런스 숫자에 영향이 있는 것만 적습니다. 코드 쪽 상세는 `house-to-core.md` §미해결.

- [ ] **온실 여름 재측정.** peak 12.42(옛) ↔ 13.01(현재)이 문서마다 섞여 있습니다.
      13.01이 맞지만 **온실은 천창·유리벽이 많아 근거리 샘플링 수정의 영향을 가장 크게 받는
      방**이라, 다른 방과 같은 조건으로 한 번 더 확인한 뒤 확정하겠습니다.
      확정 전까지 `balance_decisions.md` 의 온실 숫자는 **잠정**으로 봐 주세요.
- [ ] **`measured.avg7` 이 여름·겨울만 있습니다.** 봄·가을이 없습니다.
      확률표가 사계절 같아도 낮 길이가 달라 값이 다릅니다. 1년 플레이면 봄·가을이 절반입니다.
- [ ] **`buildDailyLight` 가 실내 유리를 빠뜨리는 버그**가 있습니다(`house-to-core.md` §1).
      **실측표 숫자는 영향 없습니다** — probe는 다른 경로로 계산합니다.
      게임 루프가 붙기 전에 고칩니다.

---

# 2026-07-26 (3차) · ★ 실측 숫자 신뢰도 점검

## 1. 온실은 **12.42 / 7.99** 입니다 — plan 값이 맞았습니다

제가 "12.42는 구본"이라고 했던 건 **틀렸습니다.** 거꾸로 제 13.01이 틀린 값이었습니다.

`_dli_probe.html` 이 `winFromHouse(..., w.tau)` 로만 불러 **`evScale`(향 계수 × 차광)을
빠뜨리고** 있었습니다. 게임(`main.js`)과 계약(`buildDailyLight`)은 넘깁니다.

**남향 창은 계수가 1.00이라 5개 방에서 티가 안 났고**, 천창(1.20)·동향(0.62)이 섞인
온실에서만 +4.7% 어긋나 뒤늦게 드러났습니다. core 창이 계약 경로로 잰 12.42 / 83% 와
지금 정확히 일치합니다.

**무늬종 판정은 안 뒤집힙니다** — 8.36도 7.99도 문턱 8.4 미만입니다.
다만 **8.36은 여유가 0.5%뿐**이었습니다. 7.99는 5%입니다.

### 곁가지로 투룸·아파트 분포도 바뀌었습니다

peak 슬롯은 남향이라 그대로지만, 동/북향 창이 비추던 칸이 어두워졌습니다.

| 방 | <0.3 어두운 칸 | ≥3 자리 |
|---|---|---|
| 투룸 | 2 → **6** | 3 (그대로) |
| 아파트 | 20 → **23** | 48 → **46** |

## 2. 구조 확정 상태 — 문서에 표시했습니다

| 방 | 빛 관련 마지막 변경 | 상태 | 앞으로 바뀔 것 |
|---|---|---|---|
| 반지하·원룸·학원교실 | `379f9dc` 07-25 | **확정** | 없음 (반지하·원룸은 *앞건물 차폐* 도입 시 재측정) |
| 투룸·온실 | `1fdc9ff` 07-26 | **잠정** | 계획 단계는 전부 ✅. 하루밖에 안 지났을 뿐 |
| **아파트** | `b8da780` 07-26 | **미확정** | **가구 배치 자유도**만 열려 있음 |

**측정은 전부 마지막 구조 변경보다 뒤입니다** — 낡은 숫자는 없습니다.
측정 이후 `house_rooms.json`·`house.js`·`daylight_lux.js`·프리셋 변경 0건을 확인했습니다.

아파트에 남은 건 **가구 배치**뿐이라 `peak 6.02`는 안 흔들립니다.
바뀌는 건 `slots`·`n3`·`dark` 입니다.

### `measured` 블록에 시점을 넣었습니다

```jsonc
"measuredAt": "2026-07-26",
"roomRev": "b8da780 2026-07-26",   // 잴 당시의 '빛 관련 마지막 구조 변경'
"status": "미확정"
```

`roomRev` 가 지금의 마지막 변경과 다르면 그 숫자는 낡은 것입니다.
**방을 고칠 때마다 재측정하고 셋을 같이 갱신**하는 규칙을 `_measured.rule` 에 박아 뒀습니다.

## 3. ★★ 아파트 마진 0.33% — 이건 지금 손봐야 합니다

`peak 6.02` vs 갈라짐 문턱 `6.0`. **선반 하나를 2cm 옮기면 뒤집힙니다.**
같은 일이 이미 한 번 났습니다 — 가구를 벽 안쪽면 기준으로 옮기자 ≥6 자리가 **18개 → 1개**.

7일평균 판정(3.87)에서는 안전하지만, **날씨를 고정하는 모드(초보)는 peak로 판정**하므로
"초보 모드는 아파트에서 갈라짐"이 0.02 차이에 걸려 있습니다.

| | 방법 | 여유 |
|---|---|---|
| ⓐ | **초보 모드 갈라짐 문턱 5.0** | +20%. 방을 안 건드림. **권장** |
| ⓑ | 아파트 peak를 7.0 이상으로 | +16%. 창·발코니를 키워야 함. 겨울 마진도 같이 움직임 |

**ⓐ를 권합니다.** 초보 모드는 이미 날씨를 고정해 관대하게 만든 모드라
문턱도 같이 완화하는 게 일관됩니다. 방 구조를 밸런스에 맞춰 비트는 건
**다음에 방을 고칠 때 또 깨집니다.**

## 4. 무늬종 — 온실에서도 3주에 1번입니다

| 방 | 7일평균 | 몬스테라 갈라짐(6.0) | **무늬종 갈라짐(8.4)** | 무늬종 min(4.2) |
|---|---|---|---|---|
| 아파트 | 3.87 | 0.8% 주 | **0%** | **28% 주** |
| 온실 | 7.99 | 83% 주 | **37% 주** | 97.6% 주 |

"주부가 아파트에서 막힌다"는 **`min 4.2` 를 4주에 1번만 넘긴다**는 뜻입니다 —
죽지는 않고 정체합니다. 의도대로면 그대로 두시면 됩니다.

무늬종이 **온실에서도 63%의 주는 안 갈라진다**는 건 확인이 필요해 보입니다.
"온실에 가면 무늬종도 갈라진다"가 목표라면 문턱 8.4를 **7.0** 근처로 내려야 합니다
(그러면 온실 7.99가 기댓값으로 넘습니다).

## 미해결

- [ ] 아파트 마진 0.33% — ⓐ/ⓑ 결정 (§3)
- [ ] 무늬종 문턱 8.4 유지 여부 (§4)
- [ ] `data/balance/weather.json` 은 이미 만들어 주신 것으로 확인했습니다.
      `expected_avg7` 값이 12.42 기준이라 **제 measured와 이제 일치**합니다

---

# 2026-07-27 (4차) · ★ 공간 기준 정본 + 판단 요청 2건

## 0. 라벨 규칙 — `measured` 를 갈랐습니다

`아파트 6.02` 만 보면 어느 기준인지 알 수 없어서 **네 갈래로 쪼갰습니다.**

```jsonc
"measured": {
  "space": { "peak_summer": 9.01, "avg7_summer": 5.79, "vol6_m3": 2.3, "peak_by_height": {...} },
  "slots": { "peak_summer": 6.02, "avg7_summer": 3.87, "utilization_pct": 67 },
  "area":  { "floor_m2": 99.4, "lamp_max_pots": { "bar": 99 } }
}
```

**`{방}.{space|slots}.{peak_summer|avg7_summer}` — 라벨 없는 숫자는 쓰지 맙시다.**
peak/7일평균 × 공간/슬롯 으로 네 갈래라 이미 세 번 섞였습니다.

**방 등급의 정본은 `space` 입니다.** `slots` 는 지금 가구 배치의 결과일 뿐입니다.

## 1. 방 등급 — space 기준 (2026-07-27, 가구 제외)

| 방 | space.peak_summer | space.avg7_summer | ≥6 부피 | ≥3 부피 | 바닥 면적 | 등 상한(바) |
|---|---|---|---|---|---|---|
| 반지하 | 3.74 | 2.40 | 0.0㎥ | 0.1㎥ | 20.0㎡ | 20 |
| 투룸 | 6.69 | 4.30 | 0.3㎥ | 1.7㎥ | 35.0㎡ | 35 |
| 원룸 | 6.76 | 4.34 | 0.5㎥ | 1.8㎥ | 30.0㎡ | 30 |
| 학원교실 | 7.58 | 4.87 | 1.4㎥ | 6.3㎥ | 66.0㎡ | 66 |
| 아파트 | 9.01 | 5.79 | 2.3㎥ | 18.8㎥ | 99.4㎡ | 99 |
| 온실 | 16.16 | 10.39 | 122.7㎥ | 124.0㎥ | 120.0㎡ (온실 70 + 연구실 50) | 120 |

**축이 둘입니다** — 자연광은 창이, 조명등은 면적이 정합니다.
반지하가 등 1개로 6.83이 나온 게 그 증거입니다. 등을 켜기 시작하면 창은 상관없어집니다.

## ★ 판단 요청 ① — 온실 peak 16.16 이 몬스테라 `max` 16 을 넘습니다

**과광 구간입니다.** 지금 설계대로면 온실에서 몬스테라를 창가에 두면 잎이 탑니다.

| | 값 |
|---|---|
| 온실 space.peak_summer | **16.16** |
| 몬스테라 `max` | 16.0 |
| 산세 `max` | (더 낮음 — 이미 과광) |

**의도라면 좋습니다** — "온실 = 그냥 좋은 방이 아니라 **관리가 필요한 방**"이 되고,
차광(`shading_presets.json`)이 장식이 아니라 **필수 도구**가 됩니다.
연구자 트랙에서 "빛은 풍족한데 나머지가 빈곤"이라는 성격과도 맞습니다.

의도가 아니면 둘 중 하나입니다.

| | 방법 | 영향 |
|---|---|---|
| ⓐ | **차광을 기본 장착**(τ 0.7 정도)으로 두고 플레이어가 걷어내게 | 구조 안 건드림. 초반이 안전해짐 |
| ⓑ | 천창 τ 를 0.85 → 0.6 으로 | peak 가 약 12 로 내려감. 대량 실생 속도도 같이 느려짐 |

**ⓐ를 권합니다.** "온실에 처음 들어가면 차광막이 쳐져 있고, 걷으면 더 밝지만 탄다"가
차광 시스템을 가르치는 자연스러운 방법입니다.

## ★ 판단 요청 ② — 원룸 6.76 > 투룸 6.69 로 progression 이 역전됐습니다

슬롯 기준으론 원룸 4.77 < 투룸 5.64 였는데, **공간으로 재면 뒤집힙니다.**
투룸이 앞섰던 건 협탁이 창가에 놓여 있어서였습니다 — 방의 성질이 아니라 배치의 결과입니다.

| | 원룸 | 투룸 |
|---|---|---|
| space.peak_summer | **6.76** | 6.69 |
| ≥3 부피 | **1.8㎥** | 1.7㎥ |
| 바닥 면적 | 30.0㎡ | **35.0㎡** |
| 창 | 2.4×1.45 단창 | 1.6×1.45 + 2.2×1.45 (칸막이로 갈림) |

**차이가 1% 입니다 — 사실상 동률입니다.** 투룸은 창이 둘이지만 칸막이로 나뉘어
한 방이 쓸 수 있는 빛은 원룸 단창과 비슷합니다.

셋 중 하나가 필요합니다.

| | 방법 | 결과 |
|---|---|---|
| ⓐ | **자연광은 동률로 두고 면적으로 구분**(30 vs 35㎡) | 이사 이유가 "빛"이 아니라 "등을 더 놓을 자리"가 됨. 축이 둘이라는 설계와 맞음 |
| ⓑ | 투룸 창을 키워 자연광도 앞서게 | progression 이 단순해지지만 "왜 방이 둘인데 빛이 더 좋나"가 설명 필요 |
| ⓒ | 투룸 칸막이를 유리로 | 두 방이 빛을 나눠 씀. 실제 투룸엔 잘 없는 구조 |

**ⓐ를 권합니다.** 원룸 → 투룸이 "더 밝아진다"가 아니라 **"자리가 늘어난다"** 로 읽히면,
면적=조명등 상한이라는 축이 처음부터 드러납니다.

## 2. 참고 — 지금 가구 배치가 잠재력의 몇 %를 쓰나

| 방 | space | slots | 활용률 |
|---|---|---|---|
| **반지하** | 3.74 | 0.55 | **15%** |
| 아파트 | 9.01 | 6.02 | 67% |
| 원룸 | 6.76 | 4.77 | 71% |
| 학원교실 | 7.58 | 6.01 | 79% |
| 투룸 | 6.69 | 5.64 | 84% |
| 온실 | 16.16 | 14.55 | 90% |

**★ 반지하 15%** — 레터박스 창이 `cy=1.77` 이라 창 앞은 3.74 인데 가구가 전부 낮습니다.
**벽선반 하나를 창 높이에 달면 몬스테라 `min` 3.0 을 넘깁니다.**
"반지하는 관엽 불가"가 방의 한계가 아니라 배치의 결과입니다 — 의도인지 확인이 필요합니다.

> 기본 가구 배치는 **손대지 않았습니다.** "적당히 나쁘게" 의도해서 놓아야 한다는
> 판단에 동의합니다. plan 이 정하면 그때 반영하겠습니다.

## 미해결

- [ ] **① 온실 과광 16.16 > max 16** — ⓐ/ⓑ 판단
- [ ] **② 원룸 ≈ 투룸 역전** — ⓐ/ⓑ/ⓒ 판단
- [ ] 반지하 활용률 15% 가 의도인지
- [ ] 교실·온실 **기본 가구 배치** (plan 몫). 지금은 옛 배치를 옮겨놓기만 한 상태라
      `slots` 값은 잠정입니다. `space`·`area` 는 구조 기준이라 정본으로 쓰셔도 됩니다

---

# 2026-08-01 (5차) · 방 작업 일단락 — 정본과 미해결

## 정본은 `docs/engine/rooms_spec.md` 입니다

방 6종의 **구조 · 실측 · 높이별 분포 · 성격 한 줄 · 캐릭터별 조건**을 한 파일에 모았습니다.
`game_flow` 갱신하실 때 그대로 쓰실 수 있게 §6에 캐릭터별로 정리해 뒀습니다.
여기엔 **plan 이 판단할 것만** 옮깁니다.

## 1. 한 장 요약

| 방 | 면적 | space.avg7 | 등 상한(바) | 무광 부피 | 성격 |
|---|---|---|---|---|---|
| 반지하 | 20.0㎡ | 2.40 | 20 | 30.3㎥ | 바닥은 죽었고 창 높이 1.6m만 산다 |
| 원룸 | 30.0㎡ | 4.34 | 30 | 33.4㎥ | 창 앞 0.85m 한 줄이 전부 |
| 투룸 | 35.0㎡ | 4.30 | 35 | 37.5㎥ | 같은 밝기를 칸막이로 나눠 쓴다 |
| 학원교실 | 66.0㎡ | 4.87 | 66 | 48.4㎥ | 창턱 1.1m가 유일한 명당. 그 아래는 급락 |
| 아파트 | 99.4㎡ | 5.79 | 99 | 95.1㎥ | 0.35m 위는 평평. 창에서의 거리가 전부 |
| 온실 | 120.0㎡ | 10.39 | 120 | 62.3㎥ | 높이 무관. 대신 과광 관리 |

**★ 자연광만으로 갈라진 잎은 온실뿐입니다.** 아파트 5.79 가 문턱 6.0 에 **0.21** 못 미칩니다.

## 2. 판단 필요 — 넷

### ① 기본 가구 배치 (plan)

**"적당히 나쁘게" 의도해서 놓을 사안**이라는 데 동의합니다. 손대지 않았습니다.
지금 배치는 겹침만 피해 놓은 결과라 **밸런스 근거로 쓰면 안 됩니다.**

| 방 | 잠재력(space) | 지금(slots) | 활용률 |
|---|---|---|---|
| **반지하** | 3.74 | 0.55 | **15%** |
| 아파트 | 9.01 | 6.02 | 67% |
| 원룸 | 6.76 | 4.77 | 71% |
| 학원교실 | 7.58 | 6.01 | 79% |
| 투룸 | 6.69 | 5.64 | 84% |
| 온실 | 16.16 | 14.55 | 90% |

"처음엔 몇 %에서 시작해서 어디까지 올릴 수 있게 할지"가 곧 난이도 곡선입니다.

### ② 반지하 활용률 15% (plan)

레터박스 창이 **높이 1.50~2.05m** 라 창 앞은 **3.74** 인데 가구가 전부 낮아 **0.55** 만 씁니다.
**벽 선반 하나를 창 높이에 달면 몬스테라 `min 3.0` 을 넘깁니다.**

"반지하는 관엽 불가"가 **방의 한계가 아니라 배치의 결과**입니다. 의도인지 확인이 필요합니다.
> 자취생 서사엔 오히려 좋아 보입니다 — "선반 하나로 반지하에서도 키운다"가 첫 성취가 됩니다.

### ③ 원룸 6.76 ≈ 투룸 6.69 (plan)

**1% 차이, 사실상 동률**입니다. 투룸이 앞섰던 건 협탁이 창가에 있어서였습니다.
**자연광은 동률로 두고 면적(30 vs 35㎡)으로 구분**을 권합니다 —
원룸→투룸이 "더 밝아진다"가 아니라 **"자리가 늘어난다"** 로 읽히면
**면적 = 조명등 상한**이라는 축이 처음부터 드러납니다.

### ④ 온실 과광 — 차광 기본 장착으로 확정된 것으로 이해했습니다

`space.peak 16.16` > 몬스테라 `max 16`. 과광 전용 변이를 온실에서만 뽑는 방향이면
**과광은 버그가 아니라 온실의 존재 이유**가 됩니다.
**house 쪽 구조 변경은 필요 없습니다.** `shading_presets.json` 기본값만 정해 주시면 됩니다.

## 3. 참고 — 캐릭터 축이 숫자로 나옵니다

| 캐릭터 | 방 | 면적=등 상한 | 전기 | 결과 |
|---|---|---|---|---|
| 자취생 | 반지하→원룸→아파트 | 20→30→**99** | 부담 | 자리도 돈도 없다가 늘어난다 |
| **가장** | 학원교실 | **66** | 식비와 경쟁 | **자리는 있는데 다 못 켠다** |
| **연구자** | 온실 | **70**(온실만) | 무료 | **다 켤 수 있다** |
| 주부 | 아파트 | **99** | 여유 | 다 되지만 무광 구석이 제일 많다(95.1㎥) |

**가장 66 vs 연구자 70** — 자리는 거의 같은데 **전기 면제 여부**가 갈립니다.

## 미해결 (plan 판단)

- [ ] ① 기본 가구 배치 — 방마다 시작 활용률
- [ ] ② 반지하 15% 가 의도인지
- [ ] ③ 원룸≈투룸 동률 처리 (면적 구분 권함)
- [ ] ④ `shading_presets.json` 온실 기본 차광값
- [ ] 무늬종 `갈라짐 8.4` — 온실 10.39 로 넘지만 아파트는 한참 멉니다. 의도 확인
- [ ] `data/balance/weather.json` 계절별 차등 여부 (지금 사계절 동일 0.55/0.30/0.15)

---

# 2026-08-02 (6차) · ★ 10차 요청 완료 — 반지하 창 높이 슬롯

## 결과 — PASS

| 라벨 | 값 |
|---|---|
| `banjiha.slots.banjiha-sill:0.peak_summer` | **3.77** |
| `banjiha.slots.banjiha-sill:0.avg7_summer` | 2.42 |
| 몬스테라 `min` 3.0 대비 | **+26%** (합격선 3.6 초과) |

가구: **`shelf_sill_pot1`** — 반지하 전용 1칸 창턱 화분받침. 상판 1.585m, 창 개구부 안(z=−1.95).
공용 `shelf_wall_1tier`(3칸)는 안 건드렸습니다.

## ★ 그 과정에서 측정 버그를 하나 잡았습니다 — 온실 값이 바뀝니다

`space.peak 3.74` 를 목표로 삼고 선반을 놓으려니 **벽에 붙여도 2.40** 밖에 안 나왔습니다.
원인을 파 보니 **공간 격자가 고체 벽 속 점을 세고 있었습니다** —
방 바깥 경계에서 0.05m만 띄웠는데 벽 두께가 0.2라 안쪽면은 그보다 0.1m 안쪽입니다.

그런데 여기 반전이 있습니다. **반지하의 3.74는 벽 속이 아니라 "창 개구부(reveal) 안"** 이었습니다.
창이 뚫린 구간이라 실제로 **창턱을 놓을 수 있는 자리**입니다. 그래서 거기 놓았고 3.77이 나왔습니다.

→ 격자를 **"고체 벽만 빼고 개구부는 살린다"** 로 고쳤습니다. 결과:

| 방 | 전 | 후 | |
|---|---|---|---|
| 반지하·원룸·투룸·학원·아파트 `space.peak` | — | **불변** | ✅ |
| **온실 `space.peak`** | 16.16 | **15.09** | ★ 북벽·연구실 벽 속 점이 섞여 있었습니다 |
| 바닥 면적 (전 방) | — | 벽 띠를 빼서 5~7% 감소 | 아파트 99.4→94.6 · 온실 120→114.6 |

**온실 관련 결론은 안 바뀝니다** — `avg7 9.70` 이 여전히 갈라짐 6.0을 넘고 `max` 16도 여전히 근접합니다.
다만 **과광 마진이 조금 줄었습니다**(16.16 → 15.09, `max` 16 대비 −6%). 차광 기본값 정하실 때 참고하세요.

## 첫 플레이 두 자리

| | slotId | 좌표 | peak_summer | avg7_summer |
|---|---|---|---|---|
| **밝은 자리** | `banjiha-sill:0` | (0, 1.585, −1.95) | **3.77** | 2.42 |
| **어두운 자리** | `dresser#4:1` | (1.7, 0.8, 1.66) | **0.04** | 0.02 |

**격차 94배.** `peak_summer ≤ 0.3` 슬롯이 **9칸**이고 **전부 열린 시루(0.24m)가 들어갑니다** → PASS.

## 반지하 활용률 — 15% → **101%**

9차에서 확정하신 *"선반이 필수 지식"* 방향 그대로입니다. 1칸만 줬습니다.

> 101%는 오차가 아니라 **격자 해상도** 때문입니다. `space` 격자는 y가 0.25m 간격이라
> 1.60m 지점(3.74)을 재는데, 실제 창턱 상판은 **1.585m** 라 조금 더 밝습니다.
> "슬롯이 공간 최고점보다 밝을 수 있다"가 아니라 **"격자 사이에 더 좋은 자리가 있다"** 는 뜻입니다.

## 6차 미해결 (plan 판단)

- [ ] **온실 `space.peak` 15.09 정정 반영** — 과광 마진이 `max` 16 대비 −6%로 줄었습니다
- [ ] 5·8·9차 요청 중 **기본 가구 배치**(반지하 외 5방)는 그대로 대기입니다
- [ ] ⏸ 스토리 모드 고정 외부광은 **손대지 않았습니다**(지시대로)

---

# 2026-08-02 (보완) · 측정 정합성 마감

`banjiha-sill:0` 의 **위치·광량은 그대로**입니다 (peak 3.77 / avg7 2.42).
아래는 파생 데이터·라벨 정리입니다. **밸런스값은 하나도 안 바꿨습니다.**

## 1. 격자에서 내부 벽도 제외 — `space.peak` 는 전부 불변

칸막이·도려낸 경계 벽 속 점도 뺐습니다. **여섯 방 `space.peak`·`avg7` 은 그대로**고,
칸막이가 있는 방의 **면적·부피만** 줄었습니다.

| 방 | 면적(순) | 무광 부피 |
|---|---|---|
| 학원교실 | 61.8 → **60.6㎡** | 46.0 → 43.9㎥ |
| 아파트 | 94.6 → **91.6㎡** | 93.1 → 87.8㎥ |
| 온실 | 114.6 → **112.4㎡** | 61.1 → 57.3㎥ |

→ **등 상한이 조금씩 줄었습니다**(학원 61→60 · 아파트 94→91 · 온실 114→112).
캐릭터 축 표(가장 vs 연구자)는 60 vs 70 으로 여전히 같은 이야기입니다.

## 2. 온실 면적 라벨 — 명목과 순면적을 갈랐습니다

```
gross 120.0㎡ = 명목 온실 70 (10×7) + 명목 연구실 50 (10×5)     ← 외곽 치수 기반
net   112.4㎡ = gross − 외곽 벽 띠 4.36 − 칸막이 1.4 − 격자 오차  ← 실제 바닥
```

**70/50 은 gross 와 합이 맞고 net 과는 안 맞습니다.** 등 상한·부피는 전부 net 기준입니다.

## 3. `utilization_pct` → `sampled_peak_ratio_pct`

반지하 **101%** 를 100 으로 자르지 않았습니다. 오차가 아니라 **표본 차이**입니다 —
`space` 격자는 y=1.60(3.74)을 재는데 실제 창턱 상판은 **1.585m**(3.77)입니다.
자르면 "격자 사이에 더 좋은 자리가 있다"는 정보가 사라집니다.

## 4. 아직 안 바꾼 것

- `data/balance/` **0건** · 외부광·날씨·계절 **0건** · 스토리 모드 고정광 **미구현**
- 기본 가구 배치(반지하 외 5방)는 그대로 대기입니다

