# 맨몸 시험 3차 — **조건은 다 갖췄다.** ⛔ 「된다」고는 안 적는다

2026-09-07 · [Char] · **크레딧 0** · 자 둘을 겹쳐 봄

## ■ 한 줄

★ **기존 클립 128개가 붙을 «조건»이 다 갖춰졌다.** 이름 24본 일치 · 부모 관계 일치 ·
기존 `sit` 클립이 건드리는 24노드가 **새 몸에 전부 있다.**
⇒ ⛔ 그러나 **「된다」고 적지 않는다.** 붙은 뒤 모양은 눈으로 봐야 한다.

## ■ 잰 것 — 자 둘을 겹쳐서

    ┌ 자 ① probe_body_fit.py (내 것) ────────────────────────────────
    │ 키       ★ 1.7000        ✔ 기존 8캐릭과 «같다»
    │ 뼈       24개 · 이름 «차례» 같다
    └────────────────────────────────────────────────────────────────
    ┌ 자 ② check_skeleton_match.py (2026-08-23 자) ──────────────────
    │ [1] 본 이름   O   24본 «완전 일치»
    │ [2] 부모 관계 O                      ⇐ ★ 내 자는 이것을 «안 잰다»
    │ [3] 본 길이   -   전체 배율 1.126 · 최대 편차 95%
    │                  (기존 8종끼리도 69%까지 벌어진다 — 불합격 아님)
    │ 판정: O  「클립이 붙을 조건은 갖췄다」
    └────────────────────────────────────────────────────────────────

> ★★ **겹쳐 본 값이 여기 있다** — 내 자는 「이름 차례」만 보고 **부모-자식을 안 잰다.**
> 저 자의 글: 「부모 관계가 다르면 **회전이 엉뚱한 데로 전파된다. 진짜 불합격**」
> ⇒ ⇒ 내 자만 돌렸으면 **그 관문을 통째로 빠뜨렸다.**

## ■ ★★★ 그리고 **직접 대 보았다** — 기존 클립 파일로

    char_jachwi_f_sit.glb 가 건드리는 노드      24개
    ⇒ 그 가운데 새 몸에 «없는» 것              ★ 0개

⇒ ★ 자의 판정을 믿지 않고 **기존 클립 파일을 열어 노드 이름을 대 보았다.** 전부 있다.

### 메시가 붙여 준 클립도 «같은 꼴»이다

    anim_walking_withskin.glb   클립 `Armature|walking_man|baselayer`
                                채널 translation 24 · rotation 24 · scale 24
    anim_running_withskin.glb   클립 `Armature|running|baselayer`   같은 꼴

⇒ ★★ 2026-08-23 자가 적어 둔 것과 **정확히 같다** — 「translation·scale 채널이 24본 전부에 걸린다」.
  ⇒ ⇒ 곧 **메시의 리깅과 우리 판이 «같은 말»을 쓴다.**

## ■ ⚠ 두 자가 **다른 말을 했다** — 그것을 풀었다

    내 자   키 1.7000  ⇒ 「기존과 같다」
    저 자   배율 1.126 ⇒ 「키가 다르게 구워졌다」

★ 둘 다 맞다. **다른 것을 재고 있었다** — 내 자는 «메시», 저 자는 «뼈».

### ★★★ 그런데 `rescale_char_glb.py` 를 열어 보니 이 물음 자체가 닫힌다

    "Meshy는 **모든 모델을 같은 높이로 정규화해 내보내므로** ... 190cm로 너무 크다.
       여캐 -> 1.40m, 남캐 -> 1.50m, 마스코트 -> 0.375m (고정)
     방식: 정점을 건드리지 않고 씬 최상단에 래퍼 노드('__scale_root')를 끼워 scale 을 얹는다.
       스킨드 메시: 관절이 래퍼 아래로 들어가므로 스키닝 결과가 함께 스케일된다.
       **클립은 관절의 '로컬' 변환을 구동하므로 수정할 필요가 없다.**"

⇒ ★★ 곧 **기존 8캐릭도 「1.7 로 나왔다가」 래퍼로 1.40m 로 줄인 것**이다.
⇒ ⇒ 새 몸의 1.7000 은 **흠이 아니라 «같은 출발선»**이다. 같은 자를 돌리면 된다. **크레딧 0.**

### ⛔ 그리고 여기서 **내 자의 두 번째 흠**이 드러난다

내 자는 `POSITION` 의 min/max 만 읽는다. ⇒ **`__scale_root` 래퍼를 «안 본다».**
⇒ 그러니 내가 낸 「✔ 기존과 같다」는 **«래퍼를 걷어낸» 값끼리 같다**는 뜻이었다.
⇒ ⇒ ★ 우연히 이번엔 그것이 **더 쓸모 있는 답**이었다(같은 출발선임을 보여 주므로).
   ⇒ ⛔ 그러나 **내가 그렇게 «의도한» 것이 아니다.** 게임에서 도는 실제 키를 물었다면 틀린 답이었다.

> ★★★ 오늘 두 번째로 **「자가 낸 수는 답이 아니라 «물음의 그림자»」**를 겪었다.
> 같은 「1.7000」이 「같은 출발선이다」도 되고 「게임 키가 1.7m다」도 된다. **다른 수다.**

## ■ ⇒ 판정

    ④ 뼈대가 24뼈 표준인가        ⇒ ✔✔ **그렇다.** 이름·차례·부모 관계 다 맞다
    ★ 기존 동작 128개를 쓰는가     ⇒ ★ **조건은 다 갖췄다**
    ③ 키                          ⇒ ✔ 기존과 «같은 출발선». rescale 자가 이미 있다 (크레딧 0)

## ■ ⛔⛔ 그러나 **「된다」고 적지 않는다** — 저 자가 못박아 두었다

    "★ 이 자가 **못 재는 것**: 붙은 다음이 «보기에» 맞는가.
     A포즈로 구운 클립을 T포즈 바인드에 얹으면 **붙기는 붙어도 살이 접히는 모양이 달라진다.**
     그건 motion_library.html 로 **눈으로** 봐야 한다.
     **이 자가 O 를 내도 「된다」고 적지 말 것.** O 는 「조건은 갖췄다」까지다."

⇒ ★ 오늘 내가 계속 물린 자리가 바로 이것이다 — **「잰 것」과 「그것이 뜻하는 것」을 섞는 것.**
  ⇒ ⇒ 저 자가 그 칸을 **자기 안에 미리 파 두었다.** 나는 그 칸을 지킨다.

★ 눈으로 볼 길은 있다: `assets/characters/motion_library.html` (+ `walk_check.html`·`scale_check.html`)
⇒ ⛔ 다만 그러려면 `_bodytest/` 파일에 rescale 을 먹여야 한다. **총괄 것이라 내가 안 건드린다.**

## ■ ⇒ 남은 것 — **옷은 여전히 막혀 있다**

    몸   ✔ 리깅됨 · 24본 표준 · 조건 갖춤
    옷   ⛔ 리깅 «거절»(422 · Pose estimation failed) ⇒ 메시가 안 해 준다
         ⇒ ★ ㉠(무게 옮기기)은 **[core] 의 새 일감**이다. 이 저장소에 코드가 없다

⇒ ⚠ 그리고 ㉠ 이 서더라도 **UV 가 없어 색을 못 입힌다.** 차례는 총괄이 정한 대로:
  **뼈(끝남) → ㉠ → UV.**

## ■ ⛔ 이 글이 «못» 하는 것

  · **「보기에 맞나」를 안 봤다.** 이것이 제일 크다 — 눈으로 볼 것
  · 배율 1.126 이 실제로 무엇을 일으키는지 — 안 봤다. 저 자는 「클립이 덮으므로 무관」이라 한다
  · 옷을 «어떻게» 물릴지 — 코드가 없다는 것만 쟀다
