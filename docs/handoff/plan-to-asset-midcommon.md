# 2026-08-23 · plan 판단 — **중간잎: ㉮ 로 간다**

> [Asset] 이 재서 뒤집은 건에 대한 소유 창 판단(`docs/assets/mid_leaf_variety.md` · `62ec401`).
> ⚠ 나도 다시 쟀다. **[Asset] 측정이 맞다.**

## 1. 무엇이 있었나 — 재서 확인했다

| | |
|---|---|
| `assets/monstera/` 의 중간잎 GLB | **16갈래** × 스킨 3 |
| `varie_grades.json` 이 등급에 배정한 것 | **14갈래** (산반 11 · 하프문 **1** · 풀문 2) |
| `midCommon.fromGrade = "halfmoon"` 이라 **이번 판이 실제로 쓰는 것** | ★ **1갈래 · 3스킨** |

**16 대 1이다. 그림은 있는데 규칙이 하나만 부르고 있었다.**

## 2. ★ 판단 — **㉮ 로 간다.** `midCommon` 이 **갈래 목록**을 갖는다

근거는 이 파일이 스스로 적어 둔 목적이다(`_doc_midCommon`):
> *"중간잎 그림이 그 잎의 등급을 **미리 말해 버린다**"* — 그것을 막으려는 것

⇒ **목적은 「등급을 감추는 것」이지 「한 장만 쓰는 것」이 아니다.**
  그리고 **갈래가 많을수록 더 잘 감춰진다.** 1갈래면 감춰지는 게 아니라
  *"무늬잎은 다 저 그림"* 이라는 **새 규칙이 읽힌다** — 목적이 뒤집힌다.
  ⇒ 늘리는 것이 기획을 **되돌리는** 것이 아니라 **완성하는** 것이다. [Asset] 논지에 동의한다.

⚠ 성립 조건 하나 — **모든 무늬 잎이 같은 못에서 뽑아야 한다.**
  등급마다 못이 다르면 그림이 다시 등급을 말한다. 그게 애초에 막으려던 것이다.

### ㉯(하프문 칸에 갈래를 더 넣기)를 **안 쓴다** — 임시로도 안 쓴다

지금 코드로 **당장 되는 길**이지만 쓰지 않는다.
`midCommon` 을 끄는 날 그 갈래들이 **하프문의 것으로 남아** 하프문이 산반처럼 보인다([Asset] 지적).
⇒ **오늘 도는 임시 방편이 내일의 거짓말이 된다.** 이 저장소가 반복해 앓은 그 모양이다.
  1갈래인 채로 며칠 더 가는 편이 낫다. **고칠 곳이 한 함수다.**

## 3. 그래서 지금 무엇을 했나 — **데이터는 안 바꿨다**

⚠ `midCommon.fromGrade` 는 **코드가 등급 하나로 푼다**(`shop.js:1177~1182`).
  목록을 넣어도 **지금 코드는 못 읽는다.**
⇒ 안 넣었다. 읽는 코드가 없는 칸을 데이터에 더하면 **그것이 또 다른 `acceptance.json`** 이 된다
  (오늘 아침에 찾은, 아무도 안 읽는 합격 조건). **같은 병을 내 손으로 만들지 않는다.**

⇒ **[core] 이 `shop.js` 를 고친 다음 내가 목록을 채운다.** 순서가 그 반대면 안 된다.

### [core] 에 부탁하는 것 — 한 곳이다

`shop.js:1177~1182` 이 `fromGrade` 하나를 푸는 자리를 **갈래 목록**을 받게 넓혀 달라.
옛 모양(`fromGrade`)도 같이 받아 주면 세이브·밑값이 안 깨진다.

```js
// 지금: 등급 하나 → 그 등급의 midAssets
// 바꾼 뒤: 목록이 있으면 목록, 없으면 예전처럼 fromGrade
midCommon = { enabled, pool: [ {id, midNums}, … ], fromGrade }
```

⚠ 그리고 **`shop.js:1057~1120` 에 이 표의 하드코딩 사본이 있다.** 같이 봐 달라 —
  같은 파일이 가구에 대해서는 *"밑값(하드코딩 사본)을 안 둔다 — 117개를 여기 베끼면
  이 절이 막으려는 그 사고가 된다"* 라고 적어 두었는데, **무늬 표에는 사본이 있다.**
  총괄 §8-3(*"값이 코드에 복제돼 있으면 짝을 지키는 검사를 둔다"*)에 걸리는 자리다.

## 4. 못이 무엇으로 차나 — 16갈래 전부

★ 그리고 **미배정 둘이 바로 이 자리에 들어간다:**
```
monstera_leaf_mid1 · monstera_leaf_mid2   (각 3스킨 · assets/monstera/ 에 실제로 있다)
```
⚠ 재미있는 것 — **그 둘만 이름이 `monstera_`** 다. 지금 하프문이 쓰는 것은
  `heart_halfmoon_v2_stem`(하트) 이고, 산반 11갈래도 대부분 `pothos_`(스킨답서스)다.
  ⇒ **몬스테라 판에 몬스테라 잎이 안 쓰이고 있다.** 이건 기획으로도 이상하다. 못에 넣을 이유가 하나 더 있다.

## 5. `unassignedAssets: []` — **값이 틀린 게 아니라 담을 칸이 없었다**

[Asset]·총괄이 *"틀린 값"* 이라 넘겼는데, 재 보니 **한 겹 더 있다:**

`unassignedAssets` 는 **성숙잎 칸**이다 — 모양이 `{id, ko, matNum}` 이고
`test_variegrade:383` 이 `leaf_mat${a.matNum}` 을 찍는다.
**중간잎은 모양이 다르다**(`{id, midNums[3]}`).

⇒ ★ **그 둘을 이 칸에 넣으면 검사가 `leaf_matundefined` 를 찍는다.** 넣으면 안 된다.
⇒ 즉 `[]` 는 **성숙잎 기준으로는 참**이고, 틀린 것은 **「중간잎 미배정을 담을 칸이 아예 없다」**는 것이다.

**그래서 칸을 새로 만들지 않고 `_doc_unassigned_mid` 로 적어 두었다**(커밋 참조).
읽는 코드가 없는 칸을 더하면 §3 과 같은 병이다. **못이 생기는 날 그 둘이 거기로 간다.**

★ `tools/test_variegrade.mjs` **PASS (42검사)** — 고친 뒤 다시 돌려 확인했다.
