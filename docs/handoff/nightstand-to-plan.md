# 반지하에 협탁을 넣었다 · 창턱은 못 했다 — 워커 보고 (G-14)

> START 2026-08-17 · 맡은 것 둘
> ① **반지하에 협탁을 한 자리 넣는다** — **했다.** 조도가 한 톨도 안 움직였고 세 겹이 실제로 선다.
> ② **창턱 받침을 0.25 정도 튀어나오게 한다** — ⛔ **안 했다.** 재 보니 **창턱이 「자라는 자리」에서
>   「새 잎이 안 나는 자리」로 떨어진다.** 값과 사진을 아래 §B 에 냈다. **박사님 판단이 필요하다.**

---

## 0. 먼저 — 이 두 일이 왜 한 창에 묶였나 (설명 먼저, 숫자 나중)

둘 다 **`data/house_rooms.json` 의 반지하 절 한 곳**을 건드리는 일이다. 그리고 둘 다
**「보기」와 「밝기」가 같은 줄에 매달려 있다** — 반지하는 창이 작은 방이라, 물건을
방 쪽으로 조금만 빼도 하늘이 덜 보인다. 그래서 두 일의 결말이 갈렸다:

- **협탁**은 키가 0.48m 라 **어느 자리의 위도 안 가린다.** 그림자는 위에서 오는데,
  이 방에서 협탁보다 낮은 자리는 3단 선반 맨 아래 칸(y 0.03) 하나뿐이고 그것도
  좌우로 비켜 앉히면 안 닿는다. ⇒ **공짜로 들어간다.**
- **창턱 받침**은 그 자체가 **이 방에서 제일 밝은 자리**다. 받침을 방 쪽으로 빼면
  자리가 통째로 따라 나오고, **그 자리의 밝기가 곧 게임의 밸런스**다. ⇒ **공짜가 아니다.**

---

# A. 협탁 — 했다

## A-1 무엇이 막고 있었나

어제(G-13 · `stack2-to-plan.md` §2) **가구 두 겹 쌓기**가 열렸는데 **반지하에서는 안 섰다.**
이 방에서 옮길 수 있는 가구가 이것뿐이었기 때문이다:

| 가구 | 발자국 | 키 |
|---|---|---|
| 침대 | 1.00 × 2.00 | 0.94 |
| 의자 | 0.50 × 0.50 | 0.89 |
| 서랍장 | 1.00 × 0.50 | 0.80 |
| 3단 선반 | 0.75 × 0.25 | 0.794 |
| 책상 | 1.25 × 0.50 | 0.74 |

천장이 **2.30m** 다. 제일 낮은 둘을 겹쳐도 1.534m 고, 남는 머리 0.766m 보다 낮은 가구가
**이 방에 하나도 없었다.** ⇒ **코드가 아니라 방 데이터가 막고 있었다.**

**협탁(`nightstand`)은 0.42 × 0.36 × 0.48 이다.** 새로 만들지 않았다 — **원룸이 이미 쓰던
프리셋**이고(`oneroom-nightstand`), 프리셋 값은 **한 톨도 안 건드렸다**(건드리면 원룸이 같이 흔들린다).

## A-2 어디에 넣었나 · 왜 거기인가

```json
{ "preset": "nightstand", "x": 0.425, "z": -1.675, "rot": 0, "uid": "banjiha-nightstand" }
```

3단 선반(x −0.725~0.025)과 책상(x 0.675~1.925) 사이 **뒷벽(창 아래) 빈 자리**다.

| 왜 | |
|---|---|
| **창 쪽이다** | 두 겹 쌓기의 뜻이 「창턱에 가까워져 밝아진다」이므로 어두운 구석에 두면 뜻이 죽는다. 실측으로도 이 자리 윗면(y 0.48)이 **0.29**, 서랍장 자리(어두운 구석)는 **0.06·0.04** 다 |
| **창을 안 가린다** | 키가 0.48m 라 창 개구부(y 1.495~2.045)에 한 톨도 안 닿는다 |
| **z −1.675 는 걸음 선이다** | `snapSpanStep` 이 깊이 0.36 을 격자 8칸으로 세므로 앞 모서리가 0.125 걸음에 떨어지는 선이 −1.675 다. 뒷벽 안쪽 면(−1.9)에서 0.025 띄운 자리이고, 이보다 벽에 붙는 선은 벽을 뚫는다 |
| ⚠ **x 0.425 는 걸음 선이 아니다** | 걸음 선은 0.350 과 0.475 다. **둘 다 재 보고 물렀다** — 0.350 은 3단 선반 맨 아래 칸을 가려 `etagere:2` 가 0.13 → **0.11** 로 떨어지고, 0.475 는 책상과 **1cm 겹친다**. 0.425 는 **15칸 전부가 한 톨도 안 움직이는 유일한 자리**였다. 이 방 데이터에는 걸음 선이 아닌 좌표가 원래 여럿이다(3단 선반 z −1.72 도 아니다) |

## A-3 ★★ 조도 전·후 — **창턱 4.80 이 한 톨도 안 움직였다**

**무엇을 켜고 무엇을 껐나**: 실제 `game.html` · 반지하 · **등 0개(자연광만)** · 맑음·여름 ·
`litHours` 12 · `localStorage` 비움 · 게임은 `novice` 로만 도므로 **peak 이 곧 그날 값**이다
(§2.9-④) · 자: `tools/probe_banjiha_nightstand.mjs`

| 자리 | y | 전 | 후 | Δ |
|---|---|---|---|---|
| ★ **`banjiha-sill:0`** | 1.585 | **4.80** | **4.80** | **0** |
| `banjiha-desk:0` | 0.740 | 0.61 | 0.61 | 0 |
| `banjiha-desk:1` | 0.740 | 0.18 | 0.18 | 0 |
| `banjiha-dresser:0` | 0.800 | 0.06 | 0.06 | 0 |
| `banjiha-dresser:1` | 0.800 | 0.04 | 0.04 | 0 |
| `banjiha-etagere:0` | 0.030 | 0.13 | 0.13 | 0 |
| `banjiha-etagere:1` | 0.030 | 0.14 | 0.14 | 0 |
| `banjiha-etagere:2` | 0.030 | 0.13 | 0.13 | 0 |
| `banjiha-etagere:3` | 0.412 | 0.22 | 0.22 | 0 |
| `banjiha-etagere:4` | 0.412 | 0.22 | 0.22 | 0 |
| `banjiha-etagere:5` | 0.412 | 0.21 | 0.21 | 0 |
| `banjiha-etagere:6` | 0.794 | 0.51 | 0.51 | 0 |
| `banjiha-etagere:7` | 0.794 | 0.48 | 0.48 | 0 |
| `banjiha-etagere:8` | 0.794 | 0.48 | 0.48 | 0 |
| **`banjiha-nightstand:0`** | 0.480 | 없음 | **0.29** | 신설 |

**⇒ 값이 움직인 자리 0곳 / 14곳. 새로 생긴 자리 1곳.**

★ **자를 둘로 댔다.** 위는 실제 게임(브라우저)에서 잰 것이고, `node tools/gen_room_profile.mjs`
(스텁 DOM · 헤드리스 · 다른 길)도 **같은 답**을 냈다. 두 자가 다른 데서 왔는데 둘 다
「14칸 그대로 · 새 칸 0.29」다.

⚠ 등을 켠 값도 얼려 뒀다(`test_lampaim`·`test_lampmove` 표): 협탁 자리는 등1 **0.56** ·
등2 **0.84** 다. **나머지 14칸의 등 0/1/2 값도 글자 하나 안 바뀌었다.**

## A-4 길찾기 — 15칸 전부 닿는다

`standProbe` 로 **자리마다 제일 나쁜 출발점**(방 네 귀퉁이 + 지금 선 자리)에서 쟀다.
`ACT_REACH` 는 1.45m 다.

| | 전 | 후 |
|---|---|---|
| 못 닿는 자리 | **0곳** / 14 | **0곳** / 15 |
| `banjiha-sill:0` bestGap | 1.10 | **1.10** (안 바뀜) |
| `banjiha-nightstand:0` bestGap | — | **0.90** |

**나머지 13칸의 bestGap 도 소수 둘째 자리까지 전부 같다.** 창턱·시루·문으로 가는 길이
한 칸도 안 막혔다.

## A-5 ★★ 세 겹이 이 방에서 **실제로 선다**

실제 게임에서 `commitFurnitureAt` 으로 쌓아 봤다 (자: `probe_banjiha_nightstand.mjs` §4).

| 가구 | 키 | 밑동 y | 윗면 | snap 이 잡은 받침 |
|---|---|---|---|---|
| `banjiha-dresser` | 0.800 | **0.000** | 0.800 | (바닥) |
| `banjiha-nightstand` | 0.480 | **0.800** | 1.280 | `banjiha-dresser` |
| `banjiha-chair` | 0.890 | **1.280** | 2.170 | **`banjiha-nightstand`** ← ★ 얹혀 있는 협탁이 상판이 됐다 |

**탑 꼭대기 2.170m · 천장 2.30m · 남는 머리 0.130m.**

```
ridersOf → { banjiha-dresser    : [nightstand, chair],
             banjiha-nightstand : [chair] }
```

★ 어제 열린 두 겹 규칙(`supportCandidates` 가 `furnNodes` 를 본다)이 **이 방에서 처음으로
실제로 쓰인다.** 어제는 「코드는 열렸는데 이 방에서는 못 세운다」였다.
⚠ **`src/game/room_view.js` 는 한 글자도 안 건드렸다.** 바뀐 것은 방 데이터뿐이다.

사진 `docs/handoff/img/banjiha_ns_after_stack3.png` — 오른쪽 아래에 서랍장 → 협탁 → 의자
세 겹이 서 있다(색 가짓수 6,680 · 게임 시각 **아침 10:20**).

## A-6 협탁이 방에 실제로 서는가 — 사진

| 사진 | 무엇이 보이나 | 색 가짓수 |
|---|---|---|
| `img/banjiha_ns_before_room.png` | 넣기 전 반지하 | 5,670 |
| `img/banjiha_ns_after_room.png` | **넣은 뒤** — 창 아래 3단 선반과 책상 사이에 협탁이 서 있다 | 6,200 |
| `img/banjiha_ns_after_stack3.png` | **세 겹**(서랍장 → 협탁 → 의자) | 6,680 |

⚠⚠ **여기서 자가 한 번 거짓말했다 — 적어 둔다.**
처음 찍은 사진들은 **게임 시각 「새벽 06:13」**의 어두운 방이었다. `rv.setDaylight(0.50)` 을
불러도 안 밝아졌는데, 까닭은 **`game.html` 의 평소 시계(§idleClockStart)가 매 프레임
`roomView.setDaylight(dayPhase)` 를 다시 부르기** 때문이다 — 밖에서 넣은 값은 한 프레임도
못 버틴다. 그런데 **색 가짓수는 5,662 라 §2.9-③ 검사를 통과했다.**
⇒ ★ **「색 가짓수 3,000 넘음」은 「까맣지 않다」만 말하지 「해가 들었다」는 안 말한다.**
  자를 고쳤다(`probe … §waitNoon`) — HUD 시계를 읽어 **10~16시에 이를 때까지 기다렸다가** 찍는다.

---

# B. 창턱 받침 — ⛔ 못 했다. 박사님 판단이 필요하다

## B-1 박사님이 보신 것은 실제로 있다 — 재서 확인했다

> *"그래픽상으로 **창턱이 덜 튀어나와 있어서** 식물이 **창문 위턱에 걸려서 뚫어버려서**
> 걸리는 것처럼 보였어."*

**맞다.** 지금 창턱 받침(`shelf_sill_pot1` 0.36 × 0.24)은 z −1.95 에 있어
**뒷벽 안쪽 면(−1.9)에서 겨우 0.07m 나와 있다.** 즉 화분이 **창 개구부(reveal) 속에 서 있다.**

| 유효일 | 잎 | 꼭대기 y | 창 윗변(2.045) 위로 | 그루 뒤끝 z | 벽 속으로 | |
|---|---|---|---|---|---|---|
| 45 | 1 | 1.809 | −0.236 | −2.049 | +0.149 | 창 아래 |
| 100 | 2 | 2.003 | −0.042 | −2.088 | +0.188 | 창 아래 |
| **150** | **3** | **2.159** | **+0.114** | −2.176 | **+0.276** | ⛔ **뚫는다** |
| 190 | 3 | 2.215 | +0.170 | −2.220 | +0.320 | ⛔ 뚫는다 |
| 290 | 4 | 2.245 | +0.200 | −2.215 | +0.315 | ⛔ 뚫는다 |

★ 그리고 **bbox 는 과대추정**이라 따로 더 잘게 쟀다 — **창 윗변보다 위에 있는 꼭짓점만**
모아 제일 뒤를 보면:

| 유효일 | 잎 | 창 위 꼭짓점 수 | 창 위 몸의 뒤끝(자리 기준) | ⇒ 벽 앞에 있으려면 자리 z ≥ |
|---|---|---|---|---|
| 100 | 2 | **0개** | — | (안 걸린다) |
| 150 | 3 | 3,137 | −0.189 | **−1.712** |
| 190 | 3 | 4,467 | −0.221 | −1.679 |
| 290 | 4 | 9,358 | −0.242 | −1.658 |
| 365 | 5 | 12,868 | −0.285 | −1.615 |

지금 자리는 **−1.95** 다. 잎 3장부터 확실히 걸린다.

사진 `img/banjiha_sill_sillbefore_focus.png` — 잎이 창 윗턱에서 잘려 사라진다.

## B-2 ⚠⚠ 그런데 빼면 **빛이 이만큼 준다**

같은 높이(y 1.585)에서 **z 만 바꿔 가며** 실제 엔진에 물은 값이다
(맑음·여름·`litHours` 12 · 자연광).

| 자리 z | 방 쪽으로 나온 길이 | 등 0개 DLI | 등 1개 DLI | 지금 대비 |
|---|---|---|---|---|
| **−1.950 (지금)** | 0.07 | **4.80** | **7.07** | 100% |
| −1.900 | 0.10 | 4.23 | 6.56 | 88% |
| −1.875 | 0.13 | 3.96 | 6.30 | 83% |
| −1.850 | 0.15 | **3.68** | **6.02** | 77% |
| −1.800 | 0.20 | 3.22 | 5.54 | 67% |
| **−1.775** | **0.25** ← 박사님 말씀 | **2.97** | **5.28** | **62%** |
| −1.700 | 0.325 | 2.40 | 4.59 | 50% |
| −1.650 | 0.375 | 2.07 | 4.15 | 43% |

## B-3 ★★ 그 손해가 **문턱 둘을 넘어간다** — 이것이 못 한 까닭이다

`data/balance/light_thresholds.json §monstera_deliciosa` 가 정한 선이다.
**정체 3.0 · 갈라짐 6.0 · 최적 5.0~11.0.**

| | 등 0개 | 밴드 | 등 1개 | 갈라지나 |
|---|---|---|---|---|
| **지금 (0.07)** | **4.80** | **느림**(자란다) | **7.07** | **✔ 갈라진다** |
| 0.20 나오게 (−1.85) | 3.68 | 느림 | 6.02 | ✔ (여유 **0.02**) |
| **0.25 나오게 (−1.775)** | **2.97** | ⛔ **정체 — 새 잎이 안 난다** | **5.28** | ⛔ **안 갈라진다** |

**⇒ 박사님이 말씀하신 0.25 를 그대로 하면 반지하 창턱이 이렇게 된다:**

1. **등 없이는 그루가 멈춘다.** 4.80 은 `min 3.0` 위라 「느림」이지만 2.97 은 그 아래다.
   첫 플레이가 가르치는 **「높은 창가 자리」가 자라지 않는 자리**가 된다.
2. **등 하나로 잎이 안 갈라진다.** 어제(G-16) 식물등을 창 위로 옮긴 것이 바로
   **창턱을 7.07 로 만들어 갈라짐 문턱 6.0 을 넘기려던 일**이었다(`house_rooms.json
   §banjiha-growlight-bar`). 5.28 이면 그 일이 통째로 되돌아간다.
3. `house_rooms.json §skyViewK_note` 가 **"tau 0.70 · 창턱 4.80 · 등1 12.31 이 확정이고
   그 위에 밸런스가 얹혀 있다"** 고 못 박아 두었다.

⇒ **그래서 안 바꿨다.** 이건 「보기 좋게 고치기」가 아니라 **밸런스 변경**이다.

## B-4 ⚠ 안전한 폭은 **0.15까지**고, 거기까지는 **안 고쳐진다**

- 「등 하나로 갈라진다」를 지키려면 → 자리 z ≥ **−1.85** (등1 6.02 · 여유 0.02밖에 없다)
- 「등 없이 자란다(min 3.0)」를 지키려면 → 자리 z ≥ **−1.80**

그런데 **잎 3장이 안 걸리려면 자리 z ≥ −1.712** 가 필요하다(§B-1).
**둘이 안 겹친다.** 즉 **문턱을 지키면서 안 걸리게 하는 값은 없다.**

실제로 0.20 까지 빼서 찍어 봤다 — `img/banjiha_sill_sill020_focus.png` :
**잎이 여전히 창틀에 잘린다.** 빛만 23% 내주고 보기는 거의 안 나아진다.

## B-5 그러면 무엇을 내주고 무엇을 얻나 — **박사님이 고르실 표**

| | 보기 | 등 0개 창턱 | 밴드 | 등 1개 | 갈라짐 | 나머지 14칸 | 창턱 화분 한도 |
|---|---|---|---|---|---|---|---|
| **㉠ 지금 그대로** | ⛔ 잎 3장부터 창을 뚫는다 | **4.80** | 느림 | 7.07 | **✔** | 그대로 | 0.21 |
| ㉡ 0.20 나오게 | ⛔ **여전히 잘린다** | 3.68 (−23%) | 느림 | 6.02 | ✔(여유 0.02) | 그대로 | 0.27 |
| **㉢ 0.25 나오게**(말씀하신 값) | **✔ 깨끗하다** | **2.97 (−38%)** | ⛔ **정체** | 5.28 | ⛔ | **12칸이 밝아진다**(아래) | 0.22 |

⚠ **㉢ 에는 딸려 오는 것이 하나 더 있다 — 나머지 칸이 밝아진다.** 받침이 개구부에서
빠져 나오면 그만큼 빛을 덜 막는다. 실측(15칸 중 **13칸이 움직인다** — 창턱 하나가 내려가고
**열두 칸이 올라간다**. 서랍장 두 칸만 그대로다):

| 자리 | 지금 | ㉢ 0.25 |
|---|---|---|
| `desk:0` | 0.61 | **0.64** |
| `etagere:6` | 0.51 | **0.60** |
| `etagere:7` | 0.48 | **0.65** |
| `etagere:8` | 0.48 | **0.66** |
| `nightstand:0` | 0.29 | **0.39** |
| `etagere:3~5` | 0.22/0.22/0.21 | **0.29/0.31/0.33** |

즉 ㉢ 은 **「제일 밝은 한 칸을 헐어 나머지 방을 조금 밝힌다」**이기도 하다.
그것이 좋은 거래인지는 **밸런스 판단**이라 내가 못 정한다.

★ **말씀만으로는 갈리는 것도 하나 있었다** — 「0.25 더 나오게」인지 「깊이를 0.25 로」인지.
  받침 뒤를 벽 안쪽 면(−1.9)에 붙이면 **두 읽기가 같은 값이 된다**(깊이 0.25 = 나온 길이 0.25).
  그래서 ㉢ 을 그 값으로 재고 찍었다.

## B-6 사진 (전·후 · 같은 각도 · 같은 게임 시각)

| 사진 | 무엇 | 색 가짓수 | 게임 시각 |
|---|---|---|---|
| `img/banjiha_sill_sillbefore_focus.png` | **지금** — 대만 보이고 **잎이 창 윗턱에 잘려 사라진다** | 4,947 | 아침 10:09 |
| `img/banjiha_sill_sill025_focus.png` | **0.25 나오게** — 잎 세 장이 통째로 벽 앞에 선다 | 5,269 | 아침 10:22 |
| `_wide.png` · `_side.png` (두 판 다) | 방 전체 · 방위를 45° 튼 옆모습 | 3,345~6,276 | 같음 |
| `img/banjiha_sill_sill020_focus.png` | 0.20 — **아직 잘린다**(참고) | 5,692 | ⚠ 새벽에 찍은 판이다 |

⚠ 세 판 모두 **잎 3장(유효 150일) 몬스테라를 창턱에 실제로 세우고**, **해가 든 뒤에**(§A-6) 찍었다.
  0.20 판만 자를 고치기 전에 찍은 것이라 어둡다 — 결론(아직 잘린다)은 그 사진으로도 읽힌다.

## B-7 ⇒ 남긴 것

**`data/house_rooms.json` 의 창턱 줄은 한 글자도 안 바꿨다.** ㉢ 을 고르시면 바꿀 것은
두 줄이다(프리셋은 안 건드려도 된다 — 방 정의의 `d` 가 프리셋을 덮는다: `house.js:757`
`{ ...preset, ...f }`):

```json
{ "preset": "shelf_sill_pot1", "uid": "banjiha-sill", "x": 0, "z": -1.775, "d": 0.25, "y": 1.55 }
```

⚠ 그러면 **얼린 표 넷을 다시 뽑아야 한다** — `test_floorlight` · `test_lampaim` ·
`test_lampmove` · `test_banjiha_profile` (전부 `BYEOT_REGEN=1` 문이 있다) + `gen_room_profile --write`.
⚠ 그리고 `data/balance/acceptance.json §light.banjiha_lamp1_goodspot_fenestrates` 가
**expect:true** 라 같이 봐야 한다.

★ `shelf_sill_pot1` 은 **반지하 전용 프리셋**이다(쓰는 곳 `banjiha-sill` 하나 · 전수 확인).
  형제 `shelf_sill_pot4`(원룸)와는 **별개 항목**이라 원룸은 어느 쪽으로 가도 안 흔들린다.

---

## C. 검사 — 전·후로 각각 돌렸다

`git stash push -- data/house_rooms.json` 로 **HEAD 에서 먼저 재고**, 넣은 뒤 다시 쟀다.

### C-1 ⚠ 그냥 넣으면 검사 여섯이 빨개진다 — 전부 **「반지하는 14칸」**을 박아 둔 것이다

START-HERE §2.5 가 미리 적어 둔 그대로다: *"검사 9개가 「14칸」을 박아 두었다 — DLI 표까지
슬롯 이름별로 얼려 놨다. **다시 뽑아야 한다**."* 자리가 하나 늘었으니 다시 뽑았다.

| 검사 | 무엇이 빨개졌나 | 어떻게 했나 |
|---|---|---|
| `test_floorlight` | ①·①-2 — `BANJIHA_FROZEN` 에 새 자리가 없다 | `BYEOT_REGEN=1` 로 다시 뽑아 **한 줄만 보탰다**. ⚠ 기존 14줄은 글자 하나 안 바뀌었다 |
| `test_lampaim` | ① — 슬롯 수 15 ≠ 14 | 같음. 등 0/1/2 표에 한 줄 보탬 |
| `test_lampmove` | ①·④·B — 표에 없는 자리 · 상판 6 → 7군데 | 같음. 상판 수를 7 로(협탁 윗면 0.480) |
| `test_lampswitch` | F-1 추천 자리 14 → 15 | 수만 고침 |
| `test_pots` | C-0·C-1×5·D-2b·D-3·D-3b·E-8 | 수만 고침. ★ **네모 화분(0.2755)이 가는 자리가 4 → 5칸**으로 는다(협탁 한도 0.33) |
| `test_banjiha_profile` | 슬롯 15 ≠ 14 | 수 + LIVE 표. **⚠ 이 검사는 원래도 빨갰다** — 아래 |

### C-2 ★ `test_banjiha_profile` 은 **내 변경 전에도 빨갰다** — 갈라 적는다

HEAD 에서 돌리면 이렇게 터졌다:
`AssertionError: banjiha-desk:0: static 0.61 vs live 0.6 오차 0.01000 > 0.005`

그 `LIVE` 표의 `desk:0` 0.6 · `desk:1` 0.19 · `dresser:0` 0.07 · `dresser:1` 0.05 는
**B-1·B-6**(가구를 모서리로 붙이고 상판 자리를 칸 한가운데로 옮긴 것 · `d1986cd`)이 낸 값인데
표가 안 따라온 것이다. 그리고 `data/profiles/room_profile.banjiha.json` 도 같은 이유로 낡아 있었다.

⇒ **여기서 같이 바로잡았다**(`gen_room_profile.mjs --write` + LIVE 표 갱신).
  ⚠ **내 변경이 낸 값이 아니다.** 검사는 이제 **통과**한다(빨강 하나가 줄었다).

### C-3 최종 — 판정 줄을 통째로 견줬다

얼린 표를 다시 뽑은 뒤, **판정 줄(PASS/FAIL + 이름)을 통째로 `diff`** 해서 견줬다.

| 검사 | HEAD | 넣은 뒤 | |
|---|---|---|---|
| `test_floorlight` | PASS 8/8 | **PASS 8/8** | ✔ |
| `test_lampaim` | PASS 15/15 | **PASS 15/15** | ✔ |
| `test_lampswitch` | 38건 · 통과 38 · 실패 0 | **38건 · 통과 38 · 실패 0** | ✔ |
| `test_lampmove` | PASS 32/32 | **PASS 32/32** | ✔ |
| `test_headroom` | PASS | **PASS** | ✔ |
| `test_banjiha_profile` | **FAIL**(낡은 LIVE 표) | **PASS** | ★ 빨강 하나 줄었다 |
| `test_pots` | FAIL 69/70 (E-6) | **FAIL 69/70 · 같은 E-6** | ✔ |
| `test_roomview_place` | FAIL 67/74 | **FAIL 67/74 · 판정 줄 diff 0** | ✔ |
| `test_free_place` | FAIL 2건 | **FAIL 2건 · 판정 줄 diff 0** | ✔ |
| `test_musun_view` | FAIL 17/21 | **FAIL 17/21 · 판정 줄 diff 0** | ✔ |
| `test_oneroom_room` | FAIL 7/9 | **FAIL 7/9 · 판정 줄 diff 0** | ✔ |
| `test_multisiru` | FAIL 10/16 | **FAIL 10/16 · 같은 여섯** | ⚠ 아래 |

★ 글자가 달라진 줄은 **검사 이름 안의 칸 수**뿐이다(`…자리 14칸` → `…15칸` ·
  네모 화분 `4칸` → `5칸`). **PASS/FAIL 이 뒤집힌 줄은 `test_banjiha_profile` 하나**이고
  그것은 빨강 → 초록이다.

⚠ `test_multisiru` ⑥ 은 **원래도 빨갰다**(`14 ≠ 13`). 자리가 늘어 문구가 `15 ≠ 13` 으로
바뀌었을 뿐, 같은 실패다. `GOLD.ringsFit.beansprout` 가 낡은 것인데 **내 일이 아니라 안 건드렸다** —
고치려면 그 골든이 왜 13 인지부터 다시 재야 한다.

---

## D. 파일

| 파일 | 무엇 |
|---|---|
| **`data/house_rooms.json`** | ★ `banjiha.furniture` 에 `banjiha-nightstand` 한 줄. **창턱 줄은 한 글자도 안 바꿨다** |
| `data/profiles/room_profile.banjiha.json` | 다시 뽑았다(`gen_room_profile.mjs --write`) — 새 자리 + 낡아 있던 desk/dresser 값 |
| `tools/probe_banjiha_nightstand.mjs` | 새로 만든 자. `BYEOT_PART=sill` 로 창턱 몫도 잰다 · `--diff [전] [후]` |
| `tools/test_floorlight.mjs` · `test_lampaim.mjs` · `test_lampmove.mjs` · `test_lampswitch.mjs` · `test_pots.mjs` · `test_banjiha_profile.mjs` | 얼린 표·칸 수를 14 → 15 로 다시 뽑았다(§C-1) |
| `docs/handoff/img/banjiha_ns_*.png` · `banjiha_sill_*.png` | 사진 |

⛔ **`data/furniture_presets.json` 은 한 글자도 안 고쳤다** — 협탁 프리셋은 원룸이 쓰고 있다.
⛔ **`src/game/room_view.js` · `game.html` · `propagation.js` · `state.js` · `plant_grow.html` 도 안 건드렸다.**

---

## E. 못 한 것 · 판단필요

| | 무엇 | 왜 |
|---|---|---|
| ⛔⛔ | **창턱 받침 0.25 빼기** | **밸런스가 문턱 둘을 넘는다** — 등 0개 4.80 → 2.97(정체) · 등 1개 7.07 → 5.28(안 갈라짐). §B-5 표로 **박사님이 고르셔야 한다.** 「보기」와 「밝기」 중 무엇을 살릴지가 이 결정의 전부다 |
| ⚠ | **문턱을 지키면서 안 걸리게 하는 값은 없다** | 안전한 최대는 −1.85(0.15 나옴)인데 잎 3장이 안 걸리려면 −1.712 가 필요하다. **겹치는 구간이 없다.** 세 번째 길(창을 키운다 · 그루 키를 줄인다)은 둘 다 더 큰 변경이라 안 재 봤다 |
| ⚠ | `test_multisiru` ⑥ 의 골든 13 | 원래 빨갛던 것. 문구가 14 → 15 로 따라 움직였다. 내 일이 아니라 안 건드렸다 |
| ⚠ | 협탁이 **어디서 나오는지**(상점·가방) | 방에 **처음부터 있는 가구**로만 넣었다. 사서 놓는 물건으로 할지는 안 정해진 일이다 |
| ⚠ | **협탁 위 자리 한도가 0.33m** 라 네모 화분(0.2755)이 올라간다 | 늘어난 것이지 잘못된 것은 아니지만, 「창턱은 못 쓰는데 협탁은 되는」 대비가 새로 생겼다. 화면이 그걸 말하지는 않는다 |
