# 2026-08-23 밤 — 무인 운영 규칙과 결과

사용자 지시(자러 가시기 전):
> *"한 6시간 정도 계속 왔다갔다 하면서 세팅하고 최적화해서 돌려.
>  아그리고 혹시 가능하면 **실제 게임을 코어나 플랜이 처음부터 실제로 반복적으로 돌려서
>  이벤트나 어색한거 찾아내는게** 가능하면 그렇게해줘. **확실한 버그는 수정도 하면좋고.**
>  **행동의 경우의수를 감안해서 대량으로 돌리는게** 가능하면 그렇게도 해주고."*

⇒ **주 임무는 「실제 게임을 대량으로 돌려 이벤트·어색함 찾기」다.** 나머지는 그 다음이다.

---

## 0. 밤샘 규칙 여섯 — 전 창 공통

### ① ★ 멈추지 마라. 판단이 필요하면 **적고 넘어가라**
사용자가 주무신다. 총괄도 메시지가 와야 움직인다.
**「이건 물어봐야 한다」로 멈추면 여섯 시간이 통째로 빈다.**
⇒ 아래 §9 「아침에 물을 것」에 **한 줄 적고 다음 일로** 가라.

### ② ⛔ 되돌릴 수 없는 것 금지
· **파일·폴더 삭제** — 워크트리 55GB · `_pipeline_test` 357MB · 중복 PNG 52MB 전부 **아침으로**
· **크레딧** — Meshy 한 톨도 쓰지 마라
· **밸런스 값** — 갈래 수 · 확률 · 자리 수 · 가격 · 일수. **재는 것은 되고 바꾸는 것은 안 된다**
· **히스토리 되감기** — `reset --hard` · `push --force` · `rebase`

### ③ ✅ 확실한 버그는 고쳐도 된다 — **조건 셋을 다 채울 때만**
사용자가 *"확실한 버그는 수정도 하면좋고"* 라 했다. 다만:
1. **검사가 초록으로 증명될 것** — 고치기 전후를 찍어 붙일 것
2. **커밋을 잘게 쪼갤 것** — 한 커밋에 한 가지. 아침에 하나만 물릴 수 있게
3. **밸런스에 안 닿을 것** — 닿으면 §9 에 적고 손 떼라

### ④ 보고는 **이 파일에** 쌓아라
총괄에게 보내는 것과 별도로, **§10 아래에 자기 절을 만들어 계속 쌓아라.**
아침에 사용자가 이 파일 하나만 읽으면 되게. 짧게, 사실만.

### ⑤ 직통 자유 — 시작 알림도 생략해도 된다
밤새는 총괄이 병목이 된다. **필요하면 바로 붙어라.** 결과만 이 파일에 남겨라.

### ⑥ 막히면 다른 일로
남의 답을 기다리게 되면 **기다리지 말고 다음 일감으로 가라.** 각 창에 여러 개를 줬다.

---

## 9. 아침에 물을 것 (창들이 여기 쌓는다)

> ### ★ 읽는 순서 (plan · 2026-08-23 밤) — **아래 줄은 하나도 안 고쳤다. 묶기만 했다**
>
> 판단거리가 쌓였는데 **따로 물으면 서로 어긋난 답이 나오는 것들**이 있다. 묶음을 먼저 적는다.
>
> #### 묶음 1 ★★★ **「이 게임은 무엇인가」 — 셋이 한 물음이다. 따로 물으면 안 된다**
> ```
> 낮  모주 하나가 이사비의 127%          — 대박이 너무 쉽다
> 밤  시루 1개면 무늬를 팔아도 못 나간다  — 채소를 안 늘리면 그 여유까지 생활비로 녹는다
> 밤  등은 사도 아무것도 안 바뀐다        — 셋째 길이 통째로 막혀 있었다
> ```
> ⇒ ★ **이 판에는 길이 셋이다** — 「채소를 늘린다 / 모주를 기다린다 / **등으로 밝힌다**」.
>   **셋째가 안 돌고 있었다.** 그래서 앞의 둘만 보고 *"대박이 너무 쉽다"* 와
>   *"채소 없이는 못 나간다"* 가 **동시에** 나왔다. **따로 보면 정반대로 읽힌다.**
> ⇒ ⚠ **한 물음으로:** *"이 게임은 「채소로 버티며 무늬를 기다리는」 게임입니까?"*
> ⇒ ★ 곁들일 것 — **지금은 운이 거의 안 걸린다.** 프롤로그가 산반·하프문을 **보장**해서
>   설계의 *"몬스테라는 쉽고 **랜덤**"* 에서 **랜덤이 없어졌다.**
>
> #### 묶음 2 ★★ **삽수 — 셋이 한 결정. 하나만 통과하면 어중간해진다**
> ```
> ① 민무늬 팔기만 막는다 (통째로 막으면 이사 둘째 축이 안 열려 튜토가 안 끝난다)
> ② 팔 수 없게 된 삽수는 죽지도 않게 한다 (팔기를 막았으면 죽는 것도 막아야 한다)
> ③ first_cut(물에 꽂아 본다)을 원룸으로 옮긴다
> ```
>
> #### 묶음 3 ★★ **「자리 수」를 움직이는 것들 — 한 저울에 같이 올려야 한다**
> §9 안에 흩어져 있다: **창턱 밑 6칸(ⓐ/ⓑ)** · **협탁 15→18** · **학원교실 112칸** ·
> **집게형을 없앨까** · **창턱에 화분과 등이 겹치는 것** · **거치등 기본 자리**
> ⇒ ★ **여섯이 다 「몇 칸을 키울 수 있나」를 움직인다.** 따로 정하면 **합이 안 맞고**,
>   그 합이 곧 **묶음 1의 셋째 길**이다.
>
> #### 묶음 4 **잎 그림 — 눈으로 봐야 하는 것** (leaf 소관)
> 크림 쨍판 아홉 · `mon_star_pinkmint_v1` 이 붉다
>
> #### ⚠ 따로 물어야 하는 것 — **묶으면 답이 흐려진다**
> | | 왜 따로인가 |
> |---|---|
> | **디스크 정리 승인** | 되돌릴 수 없다. 예/아니오 하나 |
> | **「벽등」이 무엇을 뜻하나** | 말뜻 확인이다. 정하기 전에 물어야 한다 |
> | ★ **퀘스트에 「무슨 손짓을 시키는지」 칸을 붙일까** | **계약이 는다.** 「시킨 대로 했는데 손해」를 재려면 필요한데, 지금은 `todo` 가 **사람 말**이라 기계가 못 잇는다 |
> | **`test_balance_routes` 이름을 바꿀까** | 바꾸면 `test_` 글롭에서 빠져 **스위트에서 사라진다.** 지금은 머리말로 막아 뒀다 |
> | **대사 규율 둘** | 가볍다 — `brokeTalk` 을 처음 한 번만 · 「둘 다 모자랄 때」 할 말(문안은 다 써 뒀다) |
>
> ⚠ **그리고 §10 의 「등 배치 버그가 있던 동안 잰 값」 표시를 먼저 보십시오** —
>   흔들리는 것과 안 흔들리는 것을 갈라 뒀다. **뭉뚱그려 다 버리면 멀쩡한 셈까지 나간다.**


- (총괄) **대박이 노가다보다 쉽고 빠른 것이 의도입니까** — 모주 89~175일 vs 채소 200일+
- (총괄) **디스크 정리 승인** — 워크트리 55GB · `_pipeline_test` 357MB · 중복 PNG 52MB
- (총괄) **협탁 자리 15→18** — 버그는 고쳤고, 자리 늘리기는 밸런스라 남겨 둠
- (총괄) 온실 기록 정정 · 몬이 놀란 표정 1장 · 무순 퀘스트 순서
- (총괄) **학원교실 창턱 112칸** — 옛 설계가 정본과 싸움. 고치면 자리 수가 바뀜
- (사용자) **반지하 셋째 등(거치형)의 기본 자리를 옮길까요** — 지금 (−1.15, −0.6) 은 15칸 DLI 증가합 **6.12** 인데, 0.9m 옮긴 (−0.25, −1.39) 는 **20.93**(3.4배)이고 거기서는 선반 한 칸이 무늬종 최소 4.2 를 넘습니다. 자리를 옮기면 밸런스라 안 건드렸습니다.
- (총괄→plan·growth) ⚠ **「아파트 6.02」를 쓴 판단이 있으면 다시 보십시오** — 실측은 **9.75**(avg7 3.87 → 6.27, +62%)입니다. classroom 6.01→7.21 · tworoom 5.64→7.89 도 같습니다. 값 고치는 것은 아침이지만 **그 숫자 위에서 내린 판단**이 있으면 지금 아셔야 합니다.
- (총괄) **가구 셋이 서로 박혀 있습니다 — 옮길까요** · 교탁 × 사물함(1.05m 깊이로 박힘) · 온실 선반 둘이 2.5cm 차이로 포개짐 · 온실 침대 × 의자. 자리 수는 안 바뀌고 좌표만 몇 cm 움직입니다. ⚠ 온실은 38% 답 대기 중이라 손 안 댔습니다.
- (총괄→plan) ★★ **뒷문의 벽은 15칸이 아니라 «6칸»입니다** — 시루(0.24)가 올라가는 칸이 지금 여섯뿐이고, 창턱 밑·책상 위 배치로 늘어나는 13칸은 전부 `maxPotD 0.22` 라 **몬스테라만 되고 시루는 안 됩니다.** siru16 은 자리 수가 아니라 **화분 지름**에서 막힙니다.
- (사용자) **그림의 «흰 판»이 책상 앞으로 얼마나 걸쳐 있습니까** — 저는 5cm 걸치게 두고 쟀습니다(z −1.30). **걸치는 정도가 밝기를 바꿉니다** — 더 튀어나오면 창에서 멀어져 어두워집니다. 판 위치를 알려 주시면 그 배치 그대로 재 드리겠습니다.
- (사용자) ★★★ **「책상 위」까지 합치면 자랄 수 있는 칸이 1 → 9 입니다** — 창턱 밑 3단선반 + 책상 위 납작한 판. ⚠ 그런데 **집게등은 책상 위에 그대로 두는 편이 낫습니다** — 창턱으로 옮기면 창턱 한 칸은 6.02→7.79 로 오르지만 **책상 위가 3칸 → 1칸으로 죽습니다.** 등을 어디 두느냐가 「선택」이 되는 셈인데, 지금은 아무도 안 알려 줍니다.
- (사용자) ★★★ **「창턱 밑 4칸」 재 봤더니 6칸입니다** — 3단선반 위에 같은 3단선반을 하나 더 쌓으면(꼭대기 1.588 = 창턱 높이) 새 자리 9칸이 생기고, **등 1개에 3칸 · 등 3개에 6칸**이 몬스테라 최소를 넘습니다(방 전체 1 → 7). 맨 윗단 3칸은 무늬종 최소도 넘습니다. ⚠ 자연광만으로는 0칸이라 **등이 있어야** 합니다. **지금도 되는 기능**이고 안내만 없습니다 — ⓐ 기본으로 놔 드릴까요 ⓑ 사서 놓게 할까요(퀘스트).
- (사용자) ★★ **집게형을 없애기 전에 한 번 더 보시겠습니까** — 「붙이기 어렵다」의 원인이 버그였고(창턱을 찍어도 75%는 선반으로 감), 고치면 집게등이 창턱에서 **6.02 → 7.79** 가 됩니다(8.4 는 못 넘습니다). 선반 윗단에 물리면 자랄 수 있는 칸이 **1 → 2**, 15칸 합이 13.07 → **20.4** 입니다. 지금 기본 자리(책상 위)는 최고칸을 **+0.04** 올릴 뿐이라 「사도 안 변한다」가 맞습니다.
- (사용자) **「벽등」이 무엇을 뜻합니까** — 벽에 붙는 «식물»등 프리셋은 **없습니다**. `lamp_wall` 은 장식등(ppfd 5)이고, `growlight_bar` 가 이름은 「선반밑」인데 반지하에서는 **창 위 벽**에 붙어 실질 벽등입니다. ⓐ 그 바를 말씀하신 것인지 ⓑ 새 벽걸이 식물등을 만들자는 것인지에 따라 일이 다릅니다.

- (leaf) **「크림이 따뜻해진 쨍판」 아홉 장을 고칠까** — `_v1` 은 채도 ×1.55라 흰·크림 무늬가
  베이지가 된다. **크림 무늬로 읽을 여지가 있어 내가 안 정했다.** 목록은 `docs/assets/leaf_colorway_audit.md`
- (leaf) **`mon_star_pinkmint_v1` 이 아직 붉다** — 탄색은 뺐는데 분홍이 벽돌 쪽이다.
  색상 +18°가 로즈를 빨강으로 민다. 분홍 품종만 색상을 안 돌리게 할지 판단 필요

- (core) ★ **창턱에 화분과 등이 같이 못 선다 — 막는 것이 없다.** 집게등 겨눔 버그를 고치니(`dfc904c`)
  드러났다. 창턱 상판 0.36 인데 화분 0.27 + 등 0.20 = 0.47 이 필요하다. `potFits` 는 등을 안 보고
  `lampFit` 은 화분을 안 본다 — 밀리지도 막히지도 경고도 없이 **서로 뚫고 들어간다**([House] 실측).
  ★★ 그리고 **못 놓는 한가운데가 8.99 로 제일 밝게 나온다**(놓을 수 있는 가장자리는 7.79).
  **값이 사람을 겹치는 자리로 부른다.** 겹침을 막을지 · 창턱을 넓힐지 · 그대로 둘지 — 기획 판단이다.
- (core) **삽수가 잎 등급을 잃던 버그를 고쳤다**(`9f298c8`). 그 전에 잰 삽수 값 숫자는 다시 봐야 한다 —
  하프문 잎을 잘라도 산반 값(750,000 → 350,000)으로 팔리고 있었다.

- (leaf) ★ **`shoot_screens.mjs` 가 기본으로 「올려둔 사이트」를 찍는다** — `BYEOT_URL` 을 안 주면
  `github.io` 를 본다. **오늘 고친 것이 하나도 안 든 판을 찍고 있었다.** 기본값을 로컬로 바꿀까
- (leaf) ⚠⚠ **취소 — 「다음 날」이 폰에서 흐린 것은 「일부러」다. 내가 틀렸다**
  `game.html:839` 에 규칙이 있다: `#stage.talking #next{opacity:.35;pointer-events:none}`.
  그 위 주석이 **08-22 민원(「하루 넘기기 버튼 클릭 오류」)을 고친 기록**이다 —
  *"단추는 밝게 켜져 있는데 손가락은 대사 상자로 간다 ⇒ 넷도 똑같이 흐려지고 안 눌리게 한다.*
  *★ 화면이 거짓말을 안 한다(흐리면 못 누르는 것이다)"*
  ⇒ ★ **내가 「병」이라 본 것이 「그 병을 고친 자국」이었다.** 그림만 보고 올렸다
  ⇒ ⚠ **[Char] 자의 `next ← dlgText` 「가려짐」도 같은 헛걸림일 수 있다** —
    `elementFromPoint` 는 `pointer-events:none` 인 요소를 **건너뛴다.**
    일부러 끈 단추는 **언제나** 「남이 잡힌다」로 나온다. 그 창에 알렸다
  ⇒ **아직 남는 물음**: PC 샷은 대사창이 떠 있는데도 「다음 날」이 또렷하다.
    그 순간 `.talking` 이 붙어 있었는지 **모른다.** 붙어 있었다면 **PC 쪽이 규칙 밖**이다

- (leaf) **같은 문구가 두 번 나온다** — 위 「할 일」 상자와 화면 맨 아래에 똑같이
  *"시루를 방 안에 놓아 보세요"*. 일부러인지 모른다

- (leaf) **가로(844x390)에서 대사창이 방을 거의 덮는다** — 방이 보이는 띠가 얇은데
  그 위를 대사창이 덮는다. **방을 보는 게임인데 방이 안 보인다.** 일부러인지 모른다
- (leaf) **가로에서 「할 일」 상자와 「건너뛰기」가 겹쳐 글자가 잘린다**(844x390)
- (leaf) **「할 일」 상자가 안내문을 덮는다**(430x932·390x844) — 대체 화면의 첫 줄을 가린다

- (leaf) **새싹이 화분에 비해 너무 작다** — 사용자 화면에서 왼쪽 첫 화분은 **흙만 보인다.**
  ★ **자라고 있는데 안 자란 것처럼** 보인다. 크기·성장 표현이라 [Plan]·사용자 판단
- (leaf) **무순이 「흰 실」로 보인다** — 실제 무순이 흰 줄기라 **맞는데 「자란 것」으로 안 읽힌다.**
  ⚠ 에셋 탓인지 화면 크기 탓인지 **안 갈랐다**
- (leaf) **화면이 온통 흰색·회백인 것이 일부러인가** — 집 방향이 「A 미니멀」이라
  일부러일 수 있다. 다만 **사각 화분에 색이 없던 것은 그와 별개로 흠이었고 고쳤다**

- (leaf) ⛔ **취소 — 「320 에서 탭 여섯이 화면 밖」은 틀렸다. 낡은 판을 봤다**
  [Char] 최신 판(`run02`)에서는 `offscreen: []` 이고 탭 여섯은 **닫힌 시트 안**이다.
  시트를 열면 목록에서 아예 빠진다 ⇒ **320 에서 가방을 열 수 있다.**
  ★ *"판정이 아니라 판이 낡았다"* — 오늘 여러 번 나온 그 모양이 화면에서도 났다
- (leaf) ★ **1920×1080 에서 ♪ 단추가 대사창에 덮인다** — ⚠ **08-19 주석은 *"1920 은 대사 상자가
  안 닿는다"* 고 적었는데 **지금은 닿는다.** 그때와 지금이 다르다. ([core] 로 넘어감)
  ★ 320 에서는 `next` 와 나란히 꺼진다 — 그건 정상이다
- (leaf) **「건너뛰기」 68×30 · ⓘ 19×19** — 손가락에 작다. 일부러인가

## 10. 밤새 나온 것 (창별)

### [house] ★ 「빛 분포 보는 거 있잖아」 — **겨눌 때는 보이고 «옮길 때는 안 보입니다»**

박사님이 벽부등 범위를 「모든 곳」으로 여시며 *"어차피 **빛 분포 보는 거 있잖아.** 알아서
잘하겠지"* 라 하셨다. ⇒ **그 전제가 반만 섭니다.**

```
겨눌 때  paintAim()      → highlightSlots(rankSlots(…))   ★ «조도 등급»으로 칠한다
옮길 때  beginLampMove() → highlightSlots(ids)            «붙일 수 있는 상판»만 알린다
         lampDragTo()    → 유령 + 「여기에 물립니다」        ⇒ 조도를 «안» 칠한다
```
⇒ ★ 곧 **끌고 다니는 동안 「거기 놓으면 얼마나 밝아지나」가 안 보입니다.**
  놓아 봐야 압니다. **「모든 곳」이 열리면 그만큼 여러 번 놓아 봐야 합니다.**

**왜 이렇게 됐나 — 겨누기는 싸고 옮기기는 비쌉니다**
```
겨누기   setLampAim + 15칸 다시 재기        19 ms
옮기기   방을 «다시 짓고» + 15칸 다시 재기   40 ms   ← 2.1배
⚠ 조도 엔진 몫만이다. 화면 다시 그리는 비용은 «안» 들어 있다 — 아래쪽 한계다.
```
`setLampAim` 은 **겨눔 벡터만** 바꾸는데, 옮기기는 `setFurnitureOverrides` → **방 재조립**이라
그렇습니다. **매 프레임 하면 무겁습니다.**

**⇒ 그런데 매 프레임 할 «필요가 없습니다»**
`lampFit` 이 내주는 `mountId` 는 **띄엄띄엄합니다**(상판 개수만큼). 손가락이 같은 상판 위를
움직이는 동안은 **붙을 자리가 안 바뀝니다.**
⇒ ★ **`mountId` 가 바뀔 때만 다시 칠하면** 됩니다. 반지하는 상판이 7개라 **최대 일곱 번**입니다.
  (벽까지 열리면 벽 칸도 띄엄띄엄하니 같은 셈이 섭니다.)
⇒ ⛔ **고칠 곳은 `game.html §lampDragTo` 라 [core] 몫입니다.** 저는 재기만 했습니다.

⚠ 그리고 **무릎 높이(0.5m) 그림**은 답이 안 왔습니다. 「모든 곳」에 포함된 것으로 읽되,
**실제로 붙여 놓고 이상하면 그때 다시** 여쭐 일로 열어 둡니다.


### [house] ★ 프로필 재생성 — **박사님이 보실 표** (⛔ 아직 안 썼습니다)

**「조도가 얼마나 움직이나」가 물음이라 하셔서 그것부터 냅니다.**

**① 반지하 — 15칸 중 «세 칸»만 움직입니다. 그것도 작습니다**

| 자리 | 지금 프로필 | 다시 뽑으면 | 차이 |
|---|---|---|---|
| `banjiha-desk:0` | 0.58 | 0.56 | −0.02 |
| `banjiha-desk:1` | 0.18 | 0.16 | −0.02 |
| `banjiha-nightstand:0` | 0.29 | **0.44** | **+0.15** |
| 나머지 12칸 | — | — | **한 톨도 안 움직임** |

★ **창턱(3.68)은 안 움직입니다.** 첫 플레이의 그 자리는 그대로입니다.
★ 세 칸 다 **오늘 우리가 고친 것**입니다 — `ca3f8f8`(책상 다섯 열·협탁 2×2)와 오늘 칸 정렬.
  ⇒ **값이 틀려지는 게 아니라 «맞아지는» 것**입니다. 종이가 방을 따라잡습니다.

**② 원룸 — 「딴 방」이라 칸이 통째로 바뀝니다**
```
저장본 11칸  이름이 oneroom-shelf-«6»:0 · desk-«3»:0 …  ← 옛 «순번» 이름
지금   15칸  oneroom-shelf:0 · desk:0 · sill:0…
겹치는 칸 «0»  — 한 칸도 안 겹칩니다
```
| | 지금 프로필 | 다시 뽑으면 |
|---|---|---|
| 자리 수 | 11 | **15** |
| **화분 치수 있는 칸** | **0 / 11** | **15 / 15** |
| 시루가 오르는 칸 | 잴 수 없음 | **11** |
| 자연광으로 자라는 칸 | 잴 수 없음 | **10** |
| 제일 밝은 자리 | `desk-3:0` 3.54 | **`sill:1` 6.69** |

★★ **치수가 0/11 이라 지금은 「화분이 하나도 안 올라간다」로 나옵니다.** 그게 이 일의 목적입니다.
⚠ 손으로 놀면 멀쩡합니다 — **재려고 할 때만** 안 됩니다.

**③ 「두 벌」이 무엇인가 — 찾았습니다**
```
① data/profiles/room_profile.*.json      정적 프로필 «파일»       ← --write 가 쓴다
② tools/test_banjiha_profile.mjs §LIVE   검사 «안»에 박힌 표      ← 사람이 손으로 붙인다
```
`tools/gen_room_profile.mjs` 이 ②를 **화면에 찍어 주지만 쓰지는 않습니다.** 거기가 갈리는 자리입니다.
★ ②는 `live_vs_static` 검사가 **기준값**으로 읽습니다. ⇒ **①만 쓰면 「새 프로필 ↔ 낡은 기준」**이 됩니다.
⚠⚠ **그리고 ②도 이미 낡았습니다** — 같은 세 칸입니다(0.58 · 0.18 · 0.29).
  ⇒ ★ **낡은 프로필과 낡은 기준이 서로 맞아서 지금 통과하고 있습니다.**
    **①만 고치면 그 검사가 붉어집니다** — 고장이 아니라 드러나는 것이고, ②를 같이 고치면 돌아옵니다.

**④ 승인하시면 이렇게 합니다 — 다섯 걸음**
```
1  gen_room_profile --rooms=banjiha,oneroom --write        두 방을 «한 번에»
   ⚠ 따로 돌리면 그 사이에 어긋납니다
2  그 출력의 LIVE 표를 test_banjiha_profile §LIVE 에 붙임   ← ②를 «같이» 씁니다
   ★ 값은 손으로 안 적습니다. 도구가 찍어 준 것을 그대로 붙입니다
3  다시 대조 — 「SAME」이 뜨는지                            ★ 고친 값도 잰다
4  검사 전부 — 지금 붉은 넷 중 «몇이 초록으로 바뀌는지» 셉니다
5  ★ 원룸이 «헤드리스로 재지는지» — maxPotD 가 채워졌는지. **이게 이 일의 목적입니다**
```
⇒ ★ **되돌리는 길**: 쓰기 전 파일을 떠 뒀고, **프로필 쓰기만 한 커밋 하나**로 가릅니다.
  되돌릴 때 한 줄입니다.

⚠ **안 하면 어떻게 되나** — 원룸을 **숫자로 못 잽니다**(치수 0/11). [Plan] 이 원룸 퀘스트라인을
재려는 순간 그 벽입니다. 반지하는 세 칸이 낡은 채로 남고, **검사 둘이 서로 맞아 계속 통과**합니다.

**⛔ 지금 상태** — `data/profiles/` **손 안 댔습니다**(해시로 확인). 쓰기 전 파일도 떠 뒀습니다.
⇒ 승인 오면 **①②를 한 번에** 쓰고, 쓴 뒤 **다시 대조해서 「SAME」이 뜨는지**까지 보겠습니다.


### [house] 원룸 프로파일 대조 — **⛔ 안 썼습니다. 표만 냅니다**

`tools/gen_room_profile.mjs` 를 **`--write` 없이** 돌렸습니다. **둘 다 DIFF 입니다.**

**① 반지하 — 바뀝니다. 그런데 «설명되는» 변화입니다**
```
~ banjiha-desk:0        x 0.800 → 0.875   ratio 0.03794 → 0.03656
~ banjiha-desk:1        x 1.800 → 1.875   ratio 0.01144 → 0.01014
~ banjiha-nightstand:0  (0.425, -1.675) → (0.375, -1.375)   ratio 0.01872 → 0.02842
```
★ **오늘 움직인 그 세 칸 그대로**입니다 — `ca3f8f8`(책상 다섯 열·협탁 2×2)와
2026-08-23 의 칸 정렬. **낡은 것은 프로필 쪽**입니다.
⇒ 총괄이 *"반지하가 바뀌면 멈추라 — 다른 이야기다"* 하셨는데, **다른 이야기가 아니라
  「오늘 고친 것이 아직 프로필에 안 들어간 것」**입니다. 그래도 **안 썼습니다**(아래 ⛔).

**② 원룸 — 15칸 «전부» 새 자리입니다**
```
저장된 것   11칸 · slotId  oneroom-nightstand-2:0 · oneroom-desk-3:0 · oneroom-shelf-6:0…
            uidStable «없음» · roomRev «없음» · maxPotD 전부 null
지금        15칸 · slotId  oneroom-nightstand:0 · oneroom-desk:0 · oneroom-shelf:0…
```
★ [Plan] 말이 맞습니다 — *"부분이 낡은 게 아니라 «딴 방»이다."*
  저장된 이름에 **`-2`·`-3`·`-6` 이 붙어 있습니다.** 그건 **옛 순번 이름**입니다
  (`light_adapter` 가 uid 없을 때 붙이던 것). ⇒ **오늘 uid 를 못박기 «전»의 방**입니다.
⇒ 그래서 **한 칸도 안 겹칩니다.** 대조 표가 「+ 새 자리 15」로만 나옵니다.

**⛔ 왜 안 썼나 — 서 있는 지시가 둘입니다**
1. 박사님: *"**정적 프로필(`data/profiles/*`)은 아직 쓰지 마십시오.** 그건 지뢰가 걸려 있고
   **쓰기 전에 사용자 승인을 따로 받겠습니다**."*
2. 박사님: *"`tools/gen_room_profile.mjs` 를 `--write` 로 그냥 돌리지 마십시오. **얼린 표가 두 벌인데 `--write` 는
   한 벌만 씁니다**."*
⇒ ★ 그리고 **반지하까지 같이 바뀝니다.** `--rooms=oneroom` 만 써도 원룸 파일만 바뀌지만,
  **반지하 프로필은 낡은 채로 남습니다** — 그러면 **여섯 중 둘이 서로 다른 시점**이 됩니다.
⇒ ⇒ **승인 주시면 둘 다 한 번에** 쓰겠습니다. 하나만 쓰는 것은 권하지 않습니다.

**⚠ 그리고 [Plan] 이 짚은 「치수가 없다」는 여섯 방 전부입니다**
저장된 원룸 11칸의 `maxPotD` 가 **전부 `null`** 입니다. 반지하만 15/15 갖고 있습니다.
⇒ `place.js:319` 가 치수를 못 받으면 **「못 올린다」로 판정**하므로 헤드리스로 재면 **0칸**입니다.
⚠ **손으로 놀면 멀쩡합니다** — 화면은 방을 실시간으로 지어 치수가 삽니다.
  **재려고 할 때만 안 됩니다. 그래서 아무도 안 걸렸습니다.**


### [house] 「안 보는 둘」을 세었다 — **하나는 +36칸, 하나는 «없는 것»이었다**

**① 창턱 상자 — 학원교실 하나뿐. 순증 +36칸이지만 «자리가 다르다»**
```
classroom-sill  7.20m · 높이 1.00 · 깊이 1.00
  앞면(방을 향한 수직면)   116칸
  옆면 둘                   32칸
  ⚠ 그런데 그 상자가 뒷벽 112칸을 «가린다» — 앞에 서 있으니 그 벽엔 못 붙는다
  ⇒ 상쇄 +36칸
```
★ 수는 작지만 **자리가 다르다** — 앞면은 벽보다 **1.00m 방 쪽으로 나와 있다.**
  창 밑이라 밝은 자리이기도 하다. **수로만 보면 작고, 위치로 보면 안 작다.**
⛔ **「붙일 수 있다」고 정하지 않았다.** 면이 있다는 것과 크기만 낸다.

**② 경사 천장 — «없다». 「안 보는 것」이 아니었다**
`roomDef.ceiling` 은 `'solid'` 아니면 `'glass'` 둘뿐이다. **경사라는 개념이 코드에 없다.**
⇒ ★ 「아직 안 본다」로 적어 두었는데 **볼 것이 아예 없었다.**
  ⚠ **「안 본다」와 「없다」는 다르다.** 앞엣것은 크기를 모르는 것이고 뒤엣것은 0 이다.
  세어 보지 않았으면 **「나중에 볼 것」 목록에 없는 것을 하나 얹어 둘 뻔했다.**

⇒ 이로써 벽 면 쪽에서 **「안 본다」로 남은 것이 없다.** 여섯 방 표가 전부다.


### [house] [Plan] 이 물은 둘 + 볕 얼룩 재측정 — **셋 다 답이 나왔다**

**① 바닥 격자는 「늘」 그리나 「끌 때만」 그리나 → 「끌 때만」**
`game.html §rings 바로 아래 주석` 에 그대로 적혀 있다:
> *"★바닥 격자 (박사님 2026-08-03: 「격자도 안 보이고」). **API 는 있는데 아무도 안 불렀다** —
>  **놓는 동안만 켜서** 놓을 수 있는 칸과 막힌 칸을 보여 준다. **늘 켜 두면 방을 보는 화면을 덮는다.**"*

`showSlotRings(on, …)` 이 `on=false` 면 `guideGroup.visible = false` 로 끈다.
⇒ ★ **[Plan] 의 답이 선다.** 벽도 「끌 때만」이면 **608칸이든 202칸이든 화면에 보이는 것은
  늘 끌고 있는 것 하나** 곁이다. **「자의 문제」가 아니라 「그리는 문제」였다**는 말이 맞다.
⇒ 그리고 ⓐ(바닥과 같은 자)가 흔들릴 까닭이 없어진다.

**② 등 0.70 이 0.25 로 안 나눠떨어져 「한 칸이 남는」 것 → 바닥이 이미 그렇게 산다**
`place.snapSpan` 이 이렇게 앉힌다:
```
n    = unitsFor(0.70) = ceil(0.70/0.25) = 3칸
half = 3 × 0.25 / 2 = 0.375
중심 = round((중심 − 0.375)/0.25) × 0.25 + 0.375
⇒ «묶음»(0.75)이 격자에 물리고, 0.70 짜리가 그 «한가운데»에 앉는다. 양쪽에 0.025 씩 남는다.
```
★ 그리고 **바닥에 이미 그런 가구가 열넷 있다** — `test_furn_size §NOT_YET` 에 이름으로 적혀 있다
(`shelf` · `shelf_low` · `shelf_ladder_4tier` · `plant_step_3` · `lectern` · `coffee_table` …).
⇒ **새로 생기는 문제가 아니다.** 다만 그 검사가 **「안 물린 것은 목록에 적힌 것뿐」**을 지킨다 —
  벽에도 같은 자를 쓰면 **그 목록이 벽 물건까지 지키게 하는 편**이 낫다.
⚠ 남는 0.025 가 실제로 아픈 자리: 등이 **칸 한가운데가 아니라 묶음 한가운데**에 앉으므로,
  칸 하나만 겨눠서는 「그 칸에 붙는다」가 아니다. **끌 때 밝히는 것도 「묶음」이라야 한다.**
  ⇒ 바닥이 이미 그렇게 한다(`room_view §cellSpanFor` — *"2칸짜리는 칸 경계에 앉으므로 칸 한가운데를 그리면
    표시가 거짓말이 된다"*). 벽도 같이 가면 된다.

**③ 볕 얼룩을 밝기 1.5 에서 다시 봄 → `radius 4` 로 충분하다**
```
화면 밝기 = { "api": { "value": 1.5, "min":0.6, "max":2.2, "base":0.72 }, "saved": null }
```
★ **밝기를 「짐작」이 아니라 값으로 찍었다.** ⚠ 그리고 `localStorage` 를 **먼저 지웠다** —
안 지우면 앞서 돌린 값이 남아 **밑값을 잰 줄 알고 딴 값을 잰다**(그 파일 주석이 경고한 그것).
⇒ 밝기 1.5 에서도 볕 자국이 **깨끗하다.** `radius 2` 로 더 내릴 까닭이 없다.

**⚠ 다만 밝기 1.5 에서 새로 보이는 것 하나** — **새 LED 천장등이 눈에 띄게 떠 있다.**
[Asset] 이 그림으로 보고한 그것을 **내 자에서도 봤다.** 침대 위 허공에 판이 하나 떠 있다.
⇒ 답 대기 중인 건이고, 내 안(작은 천장 판을 같이 그리기)은 이미 올렸다.


### [house] 벽 면 표 — 여섯 방 **전부**. 그리고 **두 번 더 틀렸다가 잡았다**

0.25m 격자 · 창·문·유리벽·**도려냄** 뺀 「붙일 수 있는 칸」:

| 방 | 네 벽 | 칸막이(양면) | **합** |
|---|---|---|---|
| 반지하 | 608 | — | **608** |
| 원룸 | 788 | — | **788** |
| 학원교실 | 1194 | 480 | **1674** |
| 투룸 | 846 | 352 | **1198** |
| 아파트 | 850 | 1384 | **2234** |
| 온실 | 776 | 816 | **1592** |

★ **아파트가 제일 넓다**(2234) — 칸막이가 여섯이라 **칸막이만 1384칸**이다.
★ **온실은 뒷벽이 통째로 유리**라 그 벽에서 한 칸도 못 붙인다.
★ **아파트 베란다 칸막이도 통유리**라 960칸 중 **0칸**이다.

**⚠⚠ 두 번 틀렸다. 둘 다 「표가 스스로 앞뒤가 안 맞아」 잡았다.**
1. `glassWalls` 이 `"back"` 로 오면 **벽 전체**가 유리인데 `from`/`to` 만 읽어 **하나도 안 걸렀다.**
   ⇒ 온실 **1216 → 776**. 「유리벽인데 뚫린 칸 0」이 이상해서 잡았다.
2. `cutouts` 의 외벽은 **아예 없다**(`house.js:331` — *"그 자리에 닿는 외벽 구간도 없앤다"*).
   안 빼고 세었다. ⇒ 아파트 **1380 → 850**. **없는 벽에 칸을 그리고 있었다.**
   앞벽 12m 중 **7.58m** 가 도려낸 자리다.

**⚠ 그리고 「아직 안 본다」를 「세어서」 바꿨다**
처음엔 *"칸막이는 아직 안 본다"* 고만 적었다. 그러면 **얼마나 빠졌는지를 모른다** —
실제로 아파트는 **칸막이가 벽만큼(1384 vs 850)** 이었다. **「안 본다」는 크기를 가린다.**

⚠ 아직 안 보는 것: **창턱 상자**(학원교실의 깊은 창턱 같은 것)와 **경사 천장**. 없다고 안 적는다.


### [house] 벽 격자 — **고를 수 있는 안 셋** (답이 아니라 안이다)

물음이 「0.25 냐 0.5 냐」가 아니라 **「무엇을 기준으로 나누나」**였다. 벽부등이 **0.70m** 라
0.25 칸에 안 들어간다. ⇒ 저장소에 **이미 있는 두 가지 셈**과 견줘 안을 냈다.

| 안 | 반지하 네 벽 칸 | 등이 차지하는 칸 | 저장소에 이미 있나 |
|---|---|---|---|
| **ⓐ 0.25m — 바닥과 «같은 자»** | **608** | 3칸을 «걸친다» | ✅ `place.unitsFor` · `place.snapSpan` 가 이미 한다 |
| **ⓑ 물건 크기로 나눔** | **202** | 한 칸에 «딱» | ✅ `room_view §surfaceAxis` — 화분이 쓰는 셈 |
| ⓒ 0.5m 고정 | 168 | 2칸을 걸친다 | ✖ 새 눈금 |

**ⓐ** — 바닥이 **이미 넓은 물건을 다룬다.** `place.unitsFor` 가 칸 수를 세고 `place.snapSpan` 가 그 **묶음**을 격자에
앉힌다. 화분 큰 것이 바닥에서 두세 칸을 먹는 것과 **같은 일**이다.
 · 좋은 점: **한 방에 자가 하나**다. 새로 만들 셈이 없다.
 · ⚠ 608칸이라 **다 그리면 벽이 칸으로 뒤덮인다** — 그리는 쪽에서 「가까운 데만」 같은 손질이 필요하다.

**ⓑ** — **화분이 쓰는 셈 그대로**다. 칸이 물건보다 크거나 같아 **「칸 한가운데」가 늘 뜻을 갖는다**
(오늘 낮에 협탁에서 터진 그 문제가 여기서는 안 난다).
 · 가로 칸이 벽마다 **0.714m / 0.800m** 로 달라진다. 바닥 칸(0.25)과도 다르다.
 · ⚠ **등 크기가 바뀌면 칸도 바뀐다** — 화분에서 이미 겪은 성질이다(시루 0.24 기준으로 못박아 둔 것).

**ⓒ** — 수는 제일 적지만 ⚠ **바닥 0.25 와 벽 0.5 로 한 방에 자가 둘**이 된다. 권하지 않는다.

⇒ ⛔ **고르는 것은 기획 몫이다.** 내 눈에는 **ⓐ**가 「자가 하나」라 안전해 보이고,
  **ⓑ**가 「칸 한가운데가 늘 뜻을 갖는다」라 사고가 덜 날 것 같다. **둘 다 근거가 있다.**


### [house] 벽 격자 — 칸 크기 · 여섯 방 면 수 · **내 자가 또 한 번 틀렸다**

**① 칸 크기 — 「608칸은 너무 잘다」에 대한 수**

| 칸 크기 | 반지하 네 벽 붙일 수 있는 칸 | 등(가로 0.70)이 차지하는 칸 |
|---|---|---|
| **0.25m** (바닥과 같은 눈금) | **608** | **3칸** |
| 0.50m | 168 | 2칸 |
| 1.00m | 31 | 1칸 |

⚠ **바닥 격자는 0.25m 다**(`game/place.js §GRID_CELL`). **벽만 다른 눈금이면 한 방에 자가 둘이다.**
⚠⚠ ★ **등이 칸보다 크다** — 벽부등은 **0.70 × 0.06 × 0.046m**(조립된 실제 크기)라
  0.25 칸에 **안 들어가고 세 칸을 걸친다.**
  ⇒ 화분이 그래서 `room_view §surfaceAxis` 가 **화분 지름을 보고** 칸을 나눈다.
    **벽도 같은 셈이 필요하다: 칸을 나누는 자가 「등 크기」를 봐야 한다.**
  ⇒ 곧 「0.25 냐 0.5 냐」가 아니라 **「무엇을 기준으로 나누나」**가 물음이다.
⇒ ⛔ **고르는 것은 기획 몫이다.** 수만 낸다.

**② 여섯 방 — 붙일 수 있는 칸(0.25m · 창·문·유리벽 뺌)**
```
반지하  608 / 648      원룸    788 / 880      학원교실 1194 / 1496
투룸    846 / 960      아파트 1380 / 1760     온실      776 / 1936
```
★ **온실이 제일 적다** — 뒷벽이 통째로 유리라 **한 칸도 못 붙인다**(440칸 전부 뚫림).
⇒ 조도는 **안 쟀다**(그 방들은 아직 안 연다 — 지시대로).

**⚠⚠ 그리고 내 자가 또 틀렸다 — 올리기 «전에» 잡았다**
`glassWalls` 은 두 모양이다: `"back"` (**벽 전체**) 와 `{wall:"back", from, to}` (그 구간만).
처음엔 `from`/`to` 를 그냥 읽어서 **전체 유리인 벽에서 `undefined` 와 견주게 되어 하나도 안
걸렀다.** ⇒ 온실이 **1216칸**으로 나왔다. 제대로 세니 **776칸**이다.
★ 「유리벽」이라 찍혀 있는데 뚫린 칸이 **0** 인 것이 눈에 걸려서 잡았다 —
  **표가 스스로 앞뒤가 안 맞았다.** 그게 아니었으면 440칸을 부풀려 올릴 뻔했다.


### [house] ★★ 벽 격자 ①②  — 면을 냈고 재 봤다. **③ 표는 결정 대기**

박사님: *"벽부등은 **바닥 및 벽 모든 곳에** 붙을 수 있게 (**벽에도 격자가 생기게**)"*

**① 붙일 수 있는 면** — `tools/wall_mounts.mjs` (커밋 `0a2e995`)
0.25m 격자 · 벽 안쪽 면 · 창·문 뺀 칸:
```
반지하 608 · 원룸 788 · 투룸 846 · 학원교실 1194 · (아파트·온실은 도구가 낸다)
```
⚠ **반지하만 608칸**이다. 그대로 그리면 너무 잘다 — **어디가 값이 있나**를 ②에서 잰다.
⚠ 아직 **칸막이·도려낸 벽**은 안 본다. 그 방들을 열 때 같이 봐야 한다.

**② 벽 칸마다 무엇이 사나** — 반지하 · 벽부등(바) **하나만** 켠 상태
```
지금 자리(창 위 벽 y 2.15)      자랄칸 1 · 무늬 1 · 최고  6.02 · 합 13.1
★ back u 0.00 높이 0.50        자랄칸 4 · 무늬 3 · 최고 13.06 · 합 37.0
  back u 0.00 높이 1.00        자랄칸 3 · 무늬 3 · 최고  8.20 · 합 29.5
  front u 2.00 높이 1.00       자랄칸 2 · 무늬 1 · 최고 11.05 · 합 19.9
  (제일 나쁜 쪽)                자랄칸 1 · 최고  3.69 · 합  7.9
```
★★ **지금 자리가 제일 나쁜 축이다.** 창 위 벽은 자랄칸 **1**, 뒷벽 아래 한가운데는 **4**.
  ⇒ 「등을 어디 두느냐가 선택이 된다」가 벽에서도 그대로 선다.

**⚠⚠ 밸런스 — 앞서 드린 것보다 큽니다**
앞 보고에서 *"창턱 위 벽이면 창턱 10.34"* 라 했는데, **뒷벽 아래 한가운데는 최고 13.06** 이다.
```
몬스테라 최소 2.7 · 갈라짐 6.0 · 무늬 최소 3.78 · 무늬 갈라짐 8.4 · 과광 16.0
```
⇒ **첫 등 하나로 무늬 갈라짐(8.4)을 크게 넘긴다.** 다만 **과광선 16.0 은 안 넘는다**(성긴 자 기준).
⇒ ⛔ **막을지 말지는 내가 안 정한다.** 표만 낸다.

**⚠ 그림 쪽 걱정 하나** — 제일 좋은 자리가 **높이 0.5m**(무릎 높이)다. 값은 좋은데
**식물등이 무릎 높이 벽에 붙어 있는 그림**이 자연스러운지는 따로 봐야 한다.

**⚠ 성긴 자로 못 본 것** — 벽을 따라 **1.0m**, 높이 **넷**만 쟀다(608칸 중 40여 곳).
그 사이에 더 좋은 자리가 있을 수 있다. **좁히려면 말씀만 주시면 촘촘히 다시 잰다.**


### [house] ★ D-2 「바닥 흰 얼룩」 — **텍스처 깨짐이 아니라 «볕»이다**

**갈랐다: 해를 움직이니 얼룩이 따라 움직인다.**
```
daylight 0.10   얼룩이 통째로 «없다»
daylight 0.50   침대 발치·책상 위·의자 앞에 줄무늬 흰 조각 셋
daylight 0.75   «자리가 바뀐다» — 책상 쪽은 사라지고 침대·스탠드 밑에 새로 생긴다
```
⇒ **창으로 든 볕이 바닥·가구에 만든 무늬**다. 물건도 텍스처도 아니다.
⇒ ★ 그래서 *"Day 0 · 45 · 120 전부 같은 자리"* 였던 것도 설명된다 — **그 세 장이 다 아침**이라
  해 자리가 같았다. 시간이 다르면 무늬도 옮겨진다.

**⚠ 다만 「보기 싫은 것」은 남는다.** 깨끗한 볕 네모가 아니라 **오돌토돌한 얼룩**이다.
창살·바깥 건물이 만드는 그림자를 2048 맵으로 18m 를 덮어 찍으니 가장자리가 지저분하다.
⇒ **버그로 볼지 그림 취향으로 볼지는 [Asset]·박사님 몫**이다. 나는 **원인만 가른다.**

**⚠⚠ 그리고 내가 한 번 틀렸다 — 적어 둔다.**
줄무늬를 보고 **그림자 여드름(shadow acne)**으로 짚고 `shadow.normalBias` 를 넣어 봤다. **안 들었다.**
(여드름은 밝은 면에 **어두운** 줄을 만드는데, 이건 어두운 바닥에 **밝은** 조각이다 —
모양만 보고 이름을 붙였다.) ⇒ **되돌렸다. `src/render3d/scene.js` 는 손 안 댄 상태다.**
★ 안 고쳐진 것을 고친 척 남기지 않는다. 계율 ㉘ 의 그 자리다.

### [house] ⚠ playshot 28장 중 **방이 보이는 것은 한 장**뿐이다 — 다음 창이 알아야 한다

`tools/playshot.mjs --tag house` 로 28장을 찍었는데 **008 이후 27장을 「할 일」 시트가 덮었다.**
```
003_dlg_done   ← 방이 보이는 유일한 장
007_tab_room   방 시트가 방을 덮음
009_room_only  이름은 room_only 인데 할 일 시트가 덮여 있음
012~028        같은 시트가 그대로 — 하네스가 그 시트를 못 닫고 도는 것으로 보인다
```
⇒ ★ **다음 순번(core·Plan)이 그대로 돌리면 같은 27장을 얻는다.** 시트를 닫고 찍는 걸음이
  필요하다. ⇒ 하네스는 [core] 몫이라 넘긴다.
⇒ 나는 대신 `tools/_probe_roomshot.mjs` 를 만들어 **방만 띄워 시간을 바꿔 가며** 찍었다. D-2 는 그것으로 갈랐다.

### [house] 눈으로 본 것 — 방·조명 쪽 (숫자로만 보던 것)

- ★ **첫 플레이 아침(06:16)의 반지하가 매우 어둡다.** 게임이 「화면 밝기」 슬라이더를 주고
  *"폰에서 방이 까맣게 보이면 올려 보세요"*라고 적어 둔 것이 그 방증이다.
  ⇒ **의도된 어둠인지(반지하니까) 너무 어두운 건지**는 박사님 판단이다. 나는 못 정한다.
- 벽등(창 위 바)이 **켜져 있지 않아도 흰 막대로 또렷하다** — 첫 화면에서 제일 눈에 띈다.
  「아직 못 사는 물건」이 제일 잘 보이는 것이 맞는지는 기획 쪽 물음이다.
- ⏸ **캐릭터가 방 밖 벽에 걸쳐 서 있다**(003 왼쪽) — [Char]·[core] 쪽으로 보인다. 넘긴다.


### [house] 「자리 수」와 「시루가 오르는 칸」은 다른 수다 — 여유 한 칸을 지키는 자를 세웠다

반지하 자리는 **15칸**인데 열린 콩나물 시루(0.24)가 오르는 것은 **여섯**뿐이다.
`maxPotD` 가 단 깊이로 정해지므로(`house.js §tier_max_pot_d`) 3단선반 칸은 **0.22** 라 안 오른다.
```
오르는 여섯    sill:0(0.27) · desk:0/1 · nightstand:0 · dresser:0/1   (전부 0.47)
안 오르는 아홉  전부 3단선반
```
⚠⚠ 그런데 퀘스트 `quest.js:474 siru5_cycle5`(총괄이 찾음)는 시루 **다섯**을 각각 다섯 바퀴 돌리라고 한다.
돌리려면 **실제로 놓여야** 하므로 **다섯 칸이 필요하다.** ⇒ **여유가 한 칸이다.**
깊은 상판 가구를 하나만 빼거나 얕은 것으로 바꿔도 거기서 막힌다.
⇒ ★ **그것을 지키는 자가 없었다.** 자리 **수**만 세는 자(15칸)는 이 벽을 못 본다.

**`test_floorlight` ⑥ 신설** (8/8 → 9/9)
- ★ **퀘스트가 요구하는 수(5)는 못박지 않았다** — 그건 이 창 것이 아니다.
  **「지금 여섯이고, 줄면 알린다」**만 한다. 줄어든 것이 **옳은 변경일 수도** 있으니
  붉어지면 **퀘스트 쪽과 같이 보라**는 뜻으로 적었다.
- ★ 화분 지름도 **데이터에서 찾게** 했다. 못 찾으면 house 실측 0.24 로 떨어지는데
  **그 사실도 주석에 적었다** — 숫자를 박으면 시루가 바뀔 때 조용히 갈린다.
- ★ **떨어지는지 확인했다** — 협탁을 얕은 선반으로 바꾸니 붉어졌고 되돌리니 9/9.

⚠ 참고: 총괄이 `siru16` 은 `placed` 를 안 본다는 것을 찾았다(가방에 두기만 해도 닫힌다).
그러니 **「6칸이 벽」은 그쪽에는 지금 안 걸린다.** ⑥ 은 `placed` 를 **보는** 쪽을 근거로 적었다.


### [house] ★★★ 뒷문 물음 — **두 배치 다 「받습니다」. 그런데 시루 칸은 한 칸도 안 늡니다**

총괄 물음: *"지금 게임이 저 두 배치를 실제로 받습니까?"* — **받습니다.** 둘 다.
**그런데 그것이 뒷문(`siru16`)을 못 엽니다.** 까닭이 「자리 수」가 아니라 **「화분 지름」**입니다.

| 안 | 받나 | 자리 | 몬스테라(0.202) 올라가는 칸 | **시루(0.24) 올라가는 칸** | 자랄칸 등3 | 등1 |
|---|---|---|---|---|---|---|
| 지금 그대로 | — | 15 | 15 | **6** | 1 | 1 |
| ⓐ 창턱 밑 3단선반 | **받음** | 24 | 24 | **6** | 7 | 4 |
| ⓑ 책상 위 판 | **받음** | 19 | 19 | **6** | 3 | 1 |
| **ⓐ + ⓑ** | **받음** | **28** | **28** | **6** | **9** | 4 |

★★ **새로 생기는 13칸이 전부 `maxPotD 0.22` 입니다.** 몬스테라(0.202)는 올라가고
**시루(0.24)는 안 올라갑니다.** 그래서 **시루 칸은 6 → 6, 한 칸도 안 늡니다.**

**★ 그리고 벽은 15칸이 아니라 6칸입니다** — 지금 시루가 올라가는 칸 전부:
```
banjiha-sill:0        0.27   banjiha-desk:0/1     0.47
banjiha-nightstand:0  0.47   banjiha-dresser:0/1  0.47
⇒ 여섯. 나머지 아홉 칸은 선반(0.22)이라 애초에 시루가 안 올라간다.
```
⇒ **`siru16` 이 21칸을 요구한다면 지금은 15가 아니라 6에서 막힙니다.**
  ⇒ 두 배치는 **몬스테라 자리를 늘리는 길**이지 **시루 자리를 늘리는 길이 아닙니다.**

**⇒ 뒷문을 열려면 셋 중 하나입니다** (전부 밸런스 — ⛔ 안 건드렸다)
1. **시루를 작게** — 0.24 → 0.22 이하면 선반 칸이 전부 열린다(13칸이 한 번에 붙는다)
2. **상판 넓은 가구를 늘린다** — `maxPotD` 는 단 깊이가 정한다(`tier_max_pot_d`).
   책상·서랍장 같은 깊은 상판이라야 0.24 가 오른다
3. **시루를 바닥에 놓게 한다** — 바닥은 지름 제한이 없다(자리도 아니지만)

### [house] ⓐ·ⓑ 를 갈라 답한다 — 총괄이 「제안인지 실제인지」를 갈라 달라 하셨다

- **ⓑ(책상 위 판)는 실제로 되는 길이 맞습니다.** 사진대로입니다.
  까닭은 `furnitureFit` 의 `const flatMe = sz.h <= 0.05` — **납작한 것은 가구 겹침 판정을
  아예 안 받습니다.** 흰 판(`shelf_windowsill` 1.00×0.25×**0.03**)이 그 길로 갑니다.
  **무엇 위에나, 앞으로 걸쳐서도, 집게등을 뚫고도** 놓입니다.
- **ⓐ(창턱 밑 겹쳐 놓기)도 받습니다.** 다만 **「겹쳐서」가 아니라 「쌓아서」**입니다 —
  바닥끼리는 높이가 겹쳐 막히고(`vClear`), **위로 쌓는 것**은 통과합니다.
  3단선반 위에 같은 3단선반을 올리면 꼭대기가 창턱 상판과 **같은 높이**라 창을 안 가립니다.
  ⇒ 박사님 말씀이 **제안이었는데 실제로도 됩니다.**

⚠ 그리고 **창턱은 `colliders` 에 없습니다** — 창턱 밑은 애초에 안 막혀 있습니다(재서 확인).

⛔ **자리 수·좌표 한 톨도 안 건드렸습니다.** 재기만 했습니다.


#### [Char] 마무리 — 7종 전부 정상. 헛것 셋을 물렸다 (`solo2` · 커밋 `b43078d`)

**★ 결론부터 — 해상도 때문에 깨지는 것은 하나도 못 찾았다**
```
하나씩 띄워 찍은 7종 전부  ready=true · offscreen=[] · occluded=[]
320 · 390 · 430 · 768 · 844 · 932 · 1920
```
「가방을 못 연다」·「다음 날이 안 눌린다」·「가로에서 게임이 안 뜬다」 **셋 다 헛것이었다.**

**★ 무엇이 헛것을 만들었나 — 넷**
| 까닭 | 무엇이 헛것으로 보였나 |
|---|---|
| **동시에 띄운 부하** | 「네 해상도에서 게임이 안 뜬다」 → 하나씩이면 전부 뜬다 |
| **닫힌 시트** | 「탭 여섯이 화면 밖」 → 서랍이 닫혀 아래 있던 것 |
| **대사 잠금** | 「다음 날이 덮였다」 → `#stage.talking` 이 일부러 끈 것(08-19 처방) |
| **연출** | 덮는 중에 찌르면 남이 잡힌다 → 멎기를 기다려 찍는다 |

⇒ ★★ **넷 다 「안 보인다」였는데 까닭이 달랐다. 까닭을 안 찍으면 전부 「가려짐」이 된다.**

**★ 대사를 끝낸 한 컷(`03_after_dlg`)이 둘을 동시에 풀었다** ([Asset] 청)
```
320×568  03_after_dlg (talking=false)
  offscreen [] · occluded [] · disabledOff []      ← 넷 다 비었다
  01_boot 에서 disabledOff 였던 next·btnMusic 이 여기서 빠졌다
```
⇒ **320 에서 가방을 열 수 있고 [다음 날]도 켜진다.** 찍는 시점 하나가 판정 둘을 정했다.

**★ 남은 진짜 후보 — [core] 몫**
```
① game.html:827 의 08-19 실측이 지금과 다르다
   그때 "1920 → 대사 상자가 안 닿는다" · 지금은 닿는다(btnMusic 이 dlgText 에 덮임)
   ⇒ 그때 참이던 것이 그 뒤 변경으로 깨졌다. 320 에서는 next 와 나란히 꺼진다
② 문구 중복 — 시트를 열면 사라진다. [Plan] 설계대로다. PC 에서만 시트가 안 열리는 건 미상
③ tabDev(검수) 가 sheettabs 밖으로 옆으로 32px (320). 다른 탭도 곧 넘칠 자리
④ 320 에서 navPlants 가 방 캔버스를 7~9/9 덮는다
```

**⚠ 내 자가 한동안 반쪽이었다**
`eval` 문자열 안에 **진짜 개행**을 박아 브라우저 파싱이 깨졌는데 **`catch` 가 삼켜서**
`{ready:…}` 한 칸만 남았다. **그림은 찍혔고 아무 소리도 안 났다.**
⇒ [growth] 의 `qa_firstrun` 과 **같은 병**이다. `run03` 과 첫 `solo` 곁파일은 **버렸다.**
⇒ **곁파일이 반쪽이면 소리 내게** 고쳤다.


**⚠⚠ 마지막에 내 갈래 하나를 폐기했다 — `--compare`(해상도끼리 견주기)**
「아무것도 안 걸렸다」를 **세 번 보고했는데 실은 잰 적이 없었다.**
일부러 위아래를 뒤집은 그림을 넣어 보니 **못 잡았다.** 값을 찍으니:
```
뒤집어 놓은 430x932   0.374
멀쩡한   768x1024     0.391    ← ★ 멀쩡한 쪽이 더 멀다
```
⇒ **지표가 아니라 설계가 틀렸다.** 종횡비가 다른 화면을 128×128 로 눌러 견주면
**「배치가 닮았나」가 아니라 「종횡비가 닮았나」**를 잰다. 진짜 차이가 그 밑에 묻힌다.
⇒ 옳은 길은 **DOM** 이다 — 요소마다 「화면의 어디에 몇 %」를 곁파일에 남기고 **요소 단위로** 견주면
  종횡비를 넘어 비교된다. `shoot_screens.mjs` 가 이미 rect 를 재고 있으니 남기기만 하면 된다.
★ **한 번도 떨어져 본 적 없는 검사는 검사가 아니다** — 내가 남에게 한 말이다.

**도구 (전부 푸시됨)**
```
tools/shoot_screens.mjs        7해상도 촬영 · settle · 판 번호 · 시트/대사 컷 · DOM 곁파일
tools/check_shot_anomaly.py    깨짐 + 가려짐 여섯 갈래 + ready/talking 게이팅 + 해상도 견주기
tools/find_ghost_assets.py     유령 세기
tools/char/check_skeleton_match.py
```
⚠ **하나씩 띄울 것.** 동시에 띄우면 로드가 밀려 헛것이 나온다. `SHOT_BATCH=1`.

### [house] 검사를 한 번에 도는 자리를 냈다 — 없으면 새 검사는 세운 사람만 돌린다

저장소에 `tools/test_*.mjs` 가 **83개**인데 **전체를 도는 자리가 없었다.** 하나씩 손으로 돈다.
⇒ 오늘 밤 셋을 새로 세웠는데(`test_furn_clash` · `test_measured_fresh` · `test_floorlight` ⑤) 그대로 두면
**다음 사람이 있는 줄도 모른다.** 세운 사람이 자리를 뜨면 아무도 안 돈다.

`node tools/run_house_checks.mjs` — house 검사 8개를 한 번에. 줄마다 「왜 house 것인가」를 적었다.
```
house 검사 8개 · 초록 5 · 붉음 3 · 건너뜀 있음 1
  ⚠ BYEOT_URL 을 안 넘겨 브라우저 부분을 건너뛴 검사가 있습니다: test_lampmove.mjs
  붉은 것: test_furn_clash · test_measured_fresh · oneroom_room
```
- ⚠ **house 것만 돈다** — 남의 검사를 여기서 돌리면 **남의 붉음을 내가 지고**,
  그러면 이 자를 아무도 안 본다.
- ★ **건너뛴 것도 말한다.** 브라우저 없이 돌면 2·3부가 조용히 빠지는데,
  그러면 「19/19 초록」이 **19개를 다 본 것처럼** 읽힌다.
- ⛔ **「알려진 붉음은 봐준다」를 안 넣었다.** 붉은 셋은 전부 밸런스라 아침 결정이고,
  여기서 초록으로 만들면 **고쳐진 것과 미룬 것을 못 가린다.**


### [core] — 실제 게임을 밟는 하네스 · 고친 것 여섯

**★ 제일 큰 것 — 시키는 대로 논 사람만 판이 잠긴다** (`ac030ee`)
퀘스트 사슬은 시루를 5 → 8 → 16 으로 늘리라 하는데 반지하 자리는 **15칸**이다.
자리를 다 채운 판에서 **첫 수확 때 `harvestCrop` 이 던지고 판이 통째로 잠긴다**
(`hardLock=1` · [다음 날]이 영영 안 눌린다). 실측으로 **Day 8 에 죽었다.**
★ **안내를 무시한 판(시루 하나)은 안 죽는다.** 한 판만 돌려서는 못 찾는 것이었다.
까닭: 문지기가 「방에 자리가 원래 없다(데이터 문제)」와 「지금 작물이 다 차지했다(판의 형편)」를
못 갈랐다. 그리고 화분은 08-17 부터 **가방으로 온다** — 자리가 필요 없어졌는데
**자리를 요구하는 문지기만 남아 있었다.**

**고친 것 다섯**
- `dfc904c` **집게등이 겨눈 높이를 버리고 있었다** — 창턱을 찍어도 선반에 붙었다(적중 25%).
  되먹임까지 있어 **등이 아래에 갇혔다.** 브라우저에서 앞뒤로 쟀다(4/6 → 6/6).
  ⇒ 사용자께서 *"집게형은 없애버리고 싶다"* 하신 까닭이 이 한 줄일 수 있다.
- `9f298c8` **자른 삽수가 잎 등급을 잃고 있었다** — 하프문 잎을 잘라도 산반 값(750,000 → 350,000).
  ⚠ 그 전에 잰 삽수 값 숫자는 다시 봐야 한다.
- `fb5298b` **둘 다 멀 때 게임이 침묵했다** — 제일 막막한 순간에 할 말이 없었다([Plan] 문안).
- `5e9a050` `probe_qa_full` 이 **첫날에 멈추고 있었다** — [다음 날] 뒤의 문 둘(가계부·밥상)을 몰랐다.
  ⇒ *"뒤쪽 절반을 아무도 안 봤다"* 는 게을러서가 아니라 **자가 첫날에 멈췄기 때문**이다.
- 삽수 「모주 값 −N원」 한 줄 · 재현이 등급 장부를 채우게 함.

**⚠ 헛것으로 판명난 것 — 해상도 문제는 없었다**
「320×568 에서 탭이 통째로 화면 밖」은 **닫힌 서랍 안**이었다(탭은 `#sheet` 안에 있다).
찌를 때를 맞추고 연출이 멎기를 기다리니 **320·390 둘 다 못 닿는 자리 0개.**
⇒ ★ **안 보이는 데는 까닭이 여럿이고, 까닭을 안 찍으면 전부 「가려짐」이 된다** —
꺼짐 · 닫힌 시트 · 열린 시트 · 모달 · 연출. 다섯을 갈라 찍게 고쳤다.

**★★ 그리고 늦게 나온 큰 것 — QA 프로브 일곱이 저장소에 아예 없었다** (`d42cd84`·`0638ed6`)
문서 **쉰두 자리**가 이 자들을 이름으로 부르는데 **새로 받은 사람에게는 그 파일이 없다.**
그러니 **아무도 고친 적이 없고, 낡아도 아무도 모른다** — 오늘 실측이 그 증거다:
`qa_perf` 문법 오류로 17초 만에 즉사 · `qa_firstrun` 아무것도 못 하고 **종료코드 0** ·
`qa_full` 첫날에 멈춤 · `qa_break/play/slowphone` 제한에 걸려 죽음(까닭 미상).
⇒ ★ `qa-to-plan.md:233` **「뒤쪽 절반을 아무도 안 봤다」의 진짜 크기가 이것이다** —
뒤쪽만이 아니라 **자들이 저장소에 있지도 않았다. 없는 파일은 고칠 수가 없다.**
지금은 **있는 그대로** 넣었다. 고치는 것은 따로다.

**★ 400일 가이드 판 — 141일에 파산. 다만 「가이드대로」가 아니었다**
본 퀘스트 사슬은 `crop_mix`(한 상에 두 가지 · **무순을 길러야 한다**) → `siru5_cycle5`
→ `siru8` → `siru16` 이다. 하네스가 **콩나물만 길러** `crop_mix` 를 못 닫았고,
**그 뒤 사슬이 통째로 잠긴 채** 141일을 굴러 지갑 1,500,000 → 0 이 됐다.
⇒ ★ 그러니 이 숫자는 **「가이드대로 놀아도 파산한다」가 아니라
「첫 줄만 따르고 그 뒤를 안 따르면 141일에 파산한다」**이다.
⇒ 무순을 사고 놓게 고쳤으나(`a59f7dd`) **놓는 데서 막혔다** — 가방에 재배판 칸이 안 뜬다.
  ⚠ 재고를 손으로 꽂은 판이라 **정상 경로로 다시 봐야 한다.** 앞문 판정은 **미정**이다.

**★ 긴 판에서도 화면 문제는 없었다**
141일 통틀어 못 닿는 자리 **1가지**, 그것도 **게임오버 덮개**(정상)다.
`offWhileTalking×33` · `coveredByModal×3` 은 전부 정상 갈래로 갈렸다.
⇒ [Char] 의 부팅 컷 7종 결과와 **같은 답**이다 — 짧은 판과 긴 판이 같은 말을 한다.

**⚠ 정정 — 「재배판이 안 놓인다」는 재는 자의 오독이었다** (`88e0cef`)
`fp.crops` 는 **배열**인데(`CROP_KINDS.slice(1).map(...)`) `fp.crops.musun` 으로 물었다.
늘 `undefined` 라 **놓고도 「안 놓았다」로 읽어** 매일 다시 놓으려 들었다(35일에 18번).
⇒ 고치고 재니 **Day 23 놓임 · Day 24 심어짐**. 시도 18번 → 1번.
⇒ 앞문의 남은 한 걸음은 **거두어 「한 상에 두 가지」를 올리는 것**이다 —
하네스가 밥상에서 기본값만 누르고 **갈래를 안 고른다**(`#mealKinds`). **앞문은 여전히 미정.**

**아직 답 못 한 것**
- **「가이드대로 논 사람이 이사에 닿는가」** — 긴 판을 돌리는 중이다. 이것이 「튜토가 끝까지 도는가」의 답이다.
- 삽수 차단(민무늬 팔기)은 **작업물만 있고 커밋 안 했다** — 짝이 되는 「팔 수 없는 삽수는 안 죽는다」가
  일수라 밤에 못 만진다([Plan] 판단).



#### [Char] 3차 — 시트를 연 컷으로 셋을 답했다 (`run02` · 커밋 `5eaf0f7`)

**★ ① 문구 중복은 「설계대로」다 — [Plan] 판정이 닫혔다**
```
01_boot        같은 문구가 두 번 나온다: 시루를 방 안에 놓아 보세요
02_sheet_open  ← 안 뜬다
390×844        [보여야] dlgText 9/9 ← baghelp     (아홉 점 전부 덮임)
320×568        [보여야] dlgText 9/9 ← bagslot
```
**시트를 열면 아래 줄이 덮여서 둘이 동시에 안 보인다.** [Plan] 설명 그대로다 —
*"같은 한 줄을 두 곳이 나눠 볼 뿐"*.
⚠ 다만 확인한 것은 `dlgText` 다. `#quest`(아래 띠) 자체는 셀렉터에 없어 **직접 못 봤다**[추론].
  셀렉터에 넣었으니 다음 판에 닫힌다.

**★★ ② `btnMusic` — 진짜 가려짐. 08-19 처방의 빠진 다섯째로 보인다**
```
1920×1080  두 컷 모두  occluded: btnMusic(♪) ← dlgText
           disabledOff 에 안 들어감 = 꺼져 있지 않다
           연출도 시트도 모달도 아니다
```
`game.html:839` 이 대사 중에 `#next` 등 **넷**을 껐는데 **음악 단추는 안 껐다.**
그런데 대사창이 그 위를 덮는다 ⇒ **화면은 눌릴 것처럼 보이는데 안 눌린다.**
그 주석이 *"화면이 거짓말을 안 한다(흐리면 못 누르는 것이다)"* 라고 적은 바로 그것이다.
⇒ **[core] 에 넘겼다.** 선택자에 `#btnMusic` 을 더하면 될 것으로 보인다.

**★★ ③ 새 발견 — PC(1920)에서 시트가 안 열린다**
```
1920×1080  02_sheet_open : 앞 컷과 사실상 같다(평균차 0.10)
320·390    같은 호출로 열렸다(화면이 완전히 바뀜)
```
`window.__byeotSheet.open('tabBag')` 을 똑같이 불렀는데 **PC 에서만 화면이 안 바뀐다.**
원인 미상 — **단정 안 한다.**
⇒ ★ 이게 맞으면 **①의 가림도 PC 에서는 안 도는 것**이고,
  **PC 에서만 문구가 계속 두 번 보인다.** [Plan] 처방에 닿는다.

**④ `tabDev(검수)` 가 `sheettabs` 밖으로 옆으로 32px** (320×568). 급하지 않으나
**다른 탭도 곧 넘칠 자리**다.

**⚠ 자를 또 둘 고쳤다**
· `--compare` 가 아무것도 안 짚었다 — 기준이 **평균**이라 **이상치가 평균을 끌었다.**
  일곱 중 넷이 오류 화면인데 차이가 0.509~0.550 으로 거의 같았다 ⇒ **중앙값**으로.
· ★ **한 장도 못 찍어도 종료코드 0** 이었다. [growth] 의 `qa_firstrun` 과 **같은 병**이다.
· 부팅 대기 240s → 90s. 안 뜨는 판이 배치마다 240초를 먹어 **7종을 다 돌기 전에 제한**에 걸렸다.

**남은 것** — 나머지 4종(430·768·844·932) 촬영. 부하 없이 단독이면 부팅도 풀릴 것으로 본다.

### [house] ⓑ 남은 13칸 — 사물함 3칸 고쳤고, **가구 셋이 서로 박혀 있는 것**을 찾았다

**고친 것 하나** (`013574f`) — 사물함 `tierSlots(w, h, 3, **0**)` 인데 상판 깊이 0.50 은
칸 2개라 한가운데(z=0)가 **경계**였다. 책상·서랍장이 쓰는 `frontCellZ(d)` 를 준다.
```
안 붙는 자리   학원교실 115 → 112     자리 수 325 → 325 (안 늘고 안 줆)
DLI 움직인 칸  1개 (locker-12:1  0.15 → 0.12) · 여섯 방 최고 전부 그대로
```

**★ 그러다 다른 것을 찾았다 — 방 데이터의 가구가 서로 박혀 있다**

카트 자리(`greenhouse-shelf-cart-3tier:4·5`)의 상판이 **0.75×0.25** 로 잡혔다.
카트는 0.5m 다. ⇒ 광선이 **옆 가구 판때기**를 맞고 있었다. 두 가구가 같은 자리에 있다.

`tools/probe_furn_clash.mjs` 를 만들어 전 방을 훑었다:

| 방 | 박힌 짝 | |
|---|---|---|
| 학원교실 | 1 | `classroom-lectern` × `classroom-locker-12` — 겹침 0.355 × 0.800 × **1.050m** |
| 온실 | 2 | `shelf-etagere-3tier-2` × `shelf-cart-3tier` — 겹침 0.500 × 0.250 × **0.794m** |
| | | `bed-single` × `chair` — 겹침 0.188 × 0.500 × 0.890m |
| 반지하·원룸·투룸·아파트 | 0 | |

★ **교탁이 사물함 속에 1m 넘게 박혀 있다.** 온실은 선반 둘이 2.5cm 차이로 포개져 있다
(`(−4.475, 0.4)` 와 `(−4.5, 0.4)`).

⇒ **왜 아무도 못 봤나** — `room_view.furnitureFit` 은 겹침을 막지만 **플레이어가 옮길 때만**
  돈다. **처음부터 그렇게 적힌 데이터는 아무도 안 본다.** 그 자리가 비어 있었다.

⚠⚠ **그리고 내 자가 또 틀렸다 — 처음엔 12건이었다.**
  창턱·칸막이는 **그룹이 원점에 있고 상자만 옮겨져 있다**(`room_view` 가 그렇게 적어 뒀다).
  `position` 으로 재니 방 한가운데에 7.2×1.0 짜리가 있는 셈이 되어 **학원교실에서 9건이
  거짓으로 떴다.** `furniture` 배열에 있는 것만 보게 고치니 **3건**이다.
  ★ 오늘 여섯 번째다. **틀린 자는 작은 답만 주는 것이 아니라 큰 답도 준다.**

⛔ **안 고쳤다** — 가구를 옮기면 DLI 가 움직이고, 온실은 38% 답 대기 중이다.
⇒ 아침에 정할 것: **셋을 옮길까요.** 자리 수는 안 바뀌고 좌표만 몇 cm 움직입니다.

**남은 10칸**(사다리 6 · 에타제르 2 · 카트 2) — 에타제르 2·카트 2는 **위 겹침 때문**으로 보인다.
겹침을 풀면 같이 풀릴 수 있다. 사다리 6칸은 **자리가 칸보다 많아서**다(상판 0.66 = 칸 2개인데
자리 3개) — 그건 `tierSlots` 가 **일부러** 고르게 펴는 경우이고, 고치면 **자리 수가 준다.**
⇒ 밸런스라 안 건드렸다.


### [house] ⓐ 등 나머지 전수 — **이상 없음.** 다만 내 자가 두 번 틀렸다

총괄이 준 목록 중 헤드리스로 잴 수 있는 것을 전부 훑었다(`tools/_probe_lampsweep.mjs` ·
`tools/_probe_aim.mjs` · `tools/_probe_pierce.mjs`). **여섯 방 · 등 5종.**

| 본 것 | 결과 |
|---|---|
| 켜면 밝아지나 (어두워지는 칸이 있나) | 이상 없음 — 등 하나 늘 때마다 내려가는 칸 0 |
| 껐는데 요금이 나가나 | 이상 없음 — 등 0개 와트 0. 와트는 `rigsOn(n)` 합과 같다 |
| 기구 수보다 많이 켤 수 있나 | 이상 없음 — 넘겨도 값이 안 는다 |
| 겨누기 표시가 프리셋과 맞나 | 이상 없음 — `aim` 있는 것만 `aimRange` 가 있다(바 등은 없다) |
| **방향이 반대인가 (yaw)** | **이상 없음** — 오른쪽으로 꺾으면 오른쪽이 밝아진다 |
| **위아래 (tilt)** | 이상 없음 — 꺾으면 먼 칸이 밝아진다 |
| 겨누기를 풀면 원래대로 | 이상 없음 |
| 범위 밖 각 | 이상 없음 — 던진다(조용히 안 먹는다) |
| 등이 벽·가구를 뚫나 / 떠 있나 | 이상 없음 — 반지하·원룸 등 8개 전부 |

⚠ **자를 두 번 틀렸다. 적어 둔다 — 오늘 네 번째·다섯 번째다.**
- **`tilt 0` 에서 `yaw` 를 돌려 놓고 「yaw 가 안 먹는다」로 봤다.** 빔이 똑바로 아래를 볼 때
  수직축으로 돌리는 것은 **기하학적으로 아무 일도 안 난다.** 먼저 꺾고 재야 한다.
  ⇒ 꺾은 상태에서 재니 좌우가 정확히 맞다.
- **반지하 스탠드의 tilt 를 「먼 칸이 밝아지나」로 쟀는데 「가까운 칸」이 0 이었다.**
  물리가 아니라 **곁 0.8m 안에 자리가 하나도 없어서**다(그 등이 빈 구석에 선 것은 따로 보고했다).
  ⇒ **표본이 없는 것을 실패로 세면 안 된다.** 「못 잰다」로 말하게 고쳤다.

★ 오늘 다섯 번 다 같은 모양이다 — **자를 안 의심하고 답부터 냈다.**
  발자국/판때기 · 발광점/노드 · 아무 자리/놓을 수 있는 자리 · tilt 0 에서 yaw · 표본 없는 0.
  ⇒ 다음부터 값을 내기 전에 셋을 묻는다: **이 자가 저쪽이 읽는 것인가 · 이 자리에 실제로
    놓을 수 있나 · 이 0 은 「없다」인가 「비었다」인가.**


### [house] ★★★ 「책상 위에 삼단장과 서랍장」 — 되는 까닭을 찾았습니다. **납작한 판은 겹침 판정을 안 받습니다**

`room_view.furnitureFit`:
```
const flatMe = sz.h <= 0.05;
…
if (!flatMe) for (const n of furnNodes()) { … 겹치면 막는다 … }
```
⇒ **높이 0.05m 이하인 물건은 가구 겹침 판정을 통째로 건너뜁니다.** 벽만 봅니다.
⇒ 그림의 **흰 판**이 그것입니다 — `shelf_windowsill`(1.00 × 0.25 × **0.03**).
  **아무 것 위에나, 걸쳐서도, 등을 뚫고도 놓입니다.** 박사님이 쓰시는 것이 이 성질입니다.

**⇒ 그러니 제가 「책상 위는 집게등에 막힌다」고 한 것은 반만 맞았습니다.**
**높이 있는 가구**(3단선반·서랍장)는 실제로 막힙니다. **납작한 판은 안 막힙니다.**

### [house] ★★ 그리고 결론이 뒤집혔습니다 — **집게등을 창턱으로 옮기면 손해입니다**

놓을 수 있는 것만 세었습니다(`room_view.furnitureFit` 을 옮겨 적어 먼저 통과시켰습니다).
바탕: 자리 15 · **자랄 수 있는 칸 1**.

| 안 | 집게등 **지금 자리** | 집게등 **창턱으로 옮김** |
|---|---|---|
| ① 창턱 밑 3단선반 | 자리 24 · **자랄칸 7** | 자리 24 · 자랄칸 7 |
| ② 책상 위 판(걸침) | 자리 19 · **자랄칸 3** | 자리 19 · 자랄칸 **1** |
| ④ 책상 위 3단선반 | ⛔ 집게등에 막힘 | 자리 24 · 자랄칸 2 |
| ⑥ 책상 위 3단선반 둘 | ⛔ 막힘 | 자리 33 · 자랄칸 2 |
| ⑦ 책상 위 서랍장 | ⛔ 막힘 | 자리 17 · 자랄칸 1 |
| ⑧ 책상→서랍장→3단선반 | ⛔ 막힘 | ⛔ **천장 위**(2.33 > 2.3) |
| ⑨ 창턱밑 + 책상 3단선반 둘 | 1/3 놓임 · 7 | 자리 42 · 자랄칸 8 |
| **⑩ 창턱밑 + 책상 판** | 자리 28 · **자랄칸 9** ★ | 자리 46 · 자랄칸 8 |

★★ **집게등을 창턱으로 옮기면 책상 위가 죽습니다** — ② 가 **3 → 1**.
  창턱 한 칸은 6.02 → 7.79 로 오르지만 **책상 위 여러 칸이 어두워집니다.**
⇒ **집게등의 제일 좋은 자리는 「무엇을 놓았느냐」에 달렸습니다.**
  · 아무것도 안 놓았으면 → 창턱(그 한 칸이 7.79)
  · **책상 위에 판을 놓았으면 → 책상 위 그대로**(그 판을 밝힙니다)
⇒ ★ 이건 **가르쳐야 할 것**이지 고칠 것이 아닙니다. 등을 어디 두느냐가 **선택**이 됩니다.

**★ 지금 제일 나은 배치 — 자랄 수 있는 칸 1 → 9**

| | |
|---|---|
| 창턱 밑 | `shelf_etagere_3tier` (−0.30, −1.72) y 0.794 — 꼭대기 1.588 = 창턱 높이 |
| 책상 위 | `shelf_windowsill` (1.375, −1.30) y 0.74 — 앞으로 걸침. 납작해서 등을 안 탑니다 |
| 집게등 | **그대로 둡니다**(책상 위 x 1.26~1.44) |
| 결과 | 자리 15 → **28** · 자랄 수 있는 칸 1 → **9** · 무늬종 최소 넘김 1 → **4** |
| 등 1개만 사도 | 자랄 수 있는 칸 **4** |

⛔ 아무것도 안 바꿨습니다. **적용은 `house_rooms.json §banjiha.furniture` 에 두 줄**입니다:
```
{"preset":"shelf_etagere_3tier","x":-0.30,"z":-1.72,"y":0.794,"rot":0,"uid":"banjiha-etagere2"}
{"preset":"shelf_windowsill",   "x":1.375,"z":-1.30,"y":0.74, "rot":0,"uid":"banjiha-deskboard"}
```

⚠ **못 놓는 것도 적습니다** — 「서랍장을 책상 위에」는 집게등이 비켜도 자랄 칸이 **안 늡니다**(+0).
  그 위에 3단선반을 또 올리면 **천장을 뚫습니다**(2.334 > 2.3).

**잰 것**: `tools/_probe_desk2.mjs`(놓을 수 있는 것만) · `tools/_probe_desk.mjs`(전수). 임시 도구.


### [house] ★★★ 박사님 지시 — 「가구 겹쳐 창턱 밑 4칸」 **됩니다. 그리고 6칸입니다**

**① 가구를 겹쳐 놓을 수 있나 — 「높이가 안 겹치면」 됩니다**

`room_view.furnitureFit` 의 규칙(2026-08-16 G-12):
```
가구끼리   XZ 가 겹쳐도 **높이가 안 겹치면 통과**한다   ← vClear
붙박이     colliders(kind!=='furn') 는 **XZ 만** 본다 (높이를 안 본다)
```
⇒ **바닥끼리는 못 겹칩니다**(둘 다 y0=0 이라 높이가 겹친다). **쌓는 것은 됩니다.**
★ 그리고 **창턱은 `colliders` 에 없습니다.** 반지하 colliders 는 `furn` 8개와 `wall` 5개뿐입니다.
  ⇒ **창턱 밑은 안 막혀 있습니다.** (막혔을까 의심했는데 아니었다 — 재서 확인)

**② 무엇을 어디에 — 3단선반 위에 «같은 3단선반» 하나 더**

| | |
|---|---|
| 놓을 자리 | `(−0.30, −1.72)` y **0.794** — 지금 있는 3단선반 바로 위 |
| 물건 | `shelf_etagere_3tier` 0.75 × 0.25 × 0.794 (선반-다단-3단) |
| 꼭대기 | **1.588** — 창턱 상판 1.585 와 **같은 높이**. 창을 안 가린다 |
| 놓을 수 있나 | **✔ 막는 것 없음** (가구·벽·천장 전부 확인) |
| 새로 생기는 자리 | **9칸** — y 0.824 · 1.206 · **1.588** 에 각 3칸 |

**등 개수별 — 새 자리 중 몬스테라 최소(2.7)를 넘는 칸**

| 등 | 새 자리 중 2.7↑ | 무늬 최소 3.78↑ | **방 전체 2.7↑** |
|---|---|---|---|
| 0 (자연광) | 0 | 0 | 1 |
| **1 (바 34,000원)** | **3** | 2 | **4** |
| 2 (+집게) | 3 | 2 | 4 |
| **3 (+거치)** | **6** | 3 | **7** |

맨 윗단(1.588 · 창턱 높이) 세 칸: **4.91 · 4.46 · 3.83** — 셋 다 **무늬종 최소 3.78 을 넘는다.**
아랫단(1.206): 3.25 · 3.06 · 2.72. 맨 아랫단(0.824): 1.8 대 — 안 넘는다.

★★ **박사님이 4칸이라 하셨는데 등 3개면 6칸입니다.** 등 1개면 3칸입니다.
★★ ⚠ **자연광만으로는 0칸**입니다. **등이 있어야 합니다.**

**③ ★★★ 그리고 이것이 오늘 밤의 두 실을 잇습니다**

앞에서 잰 것: *"반지하는 등을 셋 다 사도 키울 수 있는 칸이 1개 그대로다."*
**선반을 쌓으면 그것이 풀립니다** — 등을 살 때마다 칸이 는다:
```
지금        등 0→1→2→3   칸 1 · 1 · 1 · 1     ← 등을 사도 안 는다
선반 쌓으면  등 0→1→2→3   칸 1 · 4 · 4 · 7     ← 등이 칸으로 바뀐다
```
⇒ **등이 안 돌던 까닭의 절반이 「올려놓을 데가 없어서」**였습니다.

**④ 겹침 문제를 우회하나 — 예**
창턱 상판 0.36×0.30 에는 화분과 등이 같이 못 섭니다(0.47m 필요).
**선반을 쌓으면 화분은 선반에, 등은 창턱에** 섭니다. 서로 안 겹칩니다.
⇒ 아침 물음 「겹침을 막을까」가 **급하지 않아집니다.**

**⑤ 지금도 되나 — 됩니다. 아무도 안 알려 줄 뿐입니다**
- 살 수 있나: **예.** 가구 프리셋은 전부 상점 품목이 됩니다(`shop.installFurniturePresets` —
  `kind==='furniture'` 이면 크기로 값이 매겨져 목록에 오릅니다). 「선반-다단-3단」이 그렇습니다.
- 쌓을 수 있나: **예.** 「올려 놓은 가구」는 2026-08-16(G-12·G-13)부터 옮기고 놓을 수 있습니다.
- ⇒ ★ **기능은 이미 있습니다. 안내가 없습니다.** → **[Plan] 몫**(퀘스트·안내)으로 봅니다.

⚠ 덧 — **책상 위에 쌓는 안은 집게등에 막힙니다**(`banjiha-growlight-clip` y 0.750~1.170).
  core 가 고친 뒤 집게등을 창턱으로 옮기면 **책상 위가 열립니다.** 두 일이 맞물려 있습니다.

⛔ **아무것도 안 바꿨습니다** — 자리 수는 밸런스입니다.
**적용하려면**: `data/house_rooms.json §banjiha.furniture` 에 한 줄이면 됩니다 —
`{"preset":"shelf_etagere_3tier","x":-0.30,"z":-1.72,"y":0.794,"rot":0,"uid":"banjiha-etagere2"}`
(⚠ 기본으로 놔 주는 것과 사서 놓게 하는 것은 다른 결정입니다. 그건 [Plan]·사용자 몫입니다.)

**잰 것**: `tools/_probe_under.mjs`(전수) · `tools/_probe_under2.mjs`(확정안 검증). 임시 도구.


### [house] ★★★ ② 집게형 — **없앨 게 아니라 못 쓰고 있던 것이다**

> ⚠ **2026-08-23 정정.** 처음에 창턱 **8.99**(무늬 갈라짐 8.4 넘김) · 선반 **1→3칸** 으로
> 적었는데, **둘 다 등을 «화분 자리 한가운데»에 두고 잰 값**이었다. 거기엔 화분이 선다 —
> **놓을 수 없는 자리다.** 화분과 안 겹치는 자리에서 다시 재서 아래 표를 갈았다.
> 제대로 잰 값은 창턱 **7.79**(8.4 **못 넘음**) · 선반 **1→2칸** 이다.
> ★ 결론은 그대로다 — 기본 자리(+0.04)와 견주면 여전히 크다. **크기가 줄었을 뿐이다.**

「없애버리고 싶다」 하신 근거를 모으려다 **반대 결론이 나왔다.** 집게등은 값이 큰 등인데
위 §lampFit 버그 때문에 **그 값이 잠겨 있었다.**

**반지하 · 바 하나는 켠 상태(창턱 6.02 · 자랄 수 있는 칸 1개)에서 집게등을 어디에 무느냐**

| 자리 | 창턱칸 | 자랄 수 있는 칸(≥2.7) | 15칸 합 | |
|---|---|---|---|---|
| (바 하나만) | 6.02 | 1 | 13.07 | |
| **기본 자리(책상 위)** | 6.06 | 1 | 16.09 | **+0.04** |
| **창턱 가장자리에 물림** | **7.79** | 1 | 16.9 | ★ 놓을 수 있는 자리 |
| 창턱 화분 한가운데 | 8.99 | 1 | 18.21 | ⛔ 화분과 겹친다 — 못 놓는다 |
| **선반 맨 윗단 (0.1, −1.55)** | — | **2** | **20.4** | ★ 놓을 수 있는 자리 |
| 선반 맨 윗단 자리 한가운데 | — | 3 | 23.93 | ⛔ 화분과 겹친다 |

★★ 집게등에는 **서로 다른 두 쓰임**이 있다 — 「창턱 한 칸을 6.02 → **7.79**」와
「자랄 수 있는 칸을 1 → **2**, 합 13.07 → **20.4**」.
★★ **그런데 기본 자리(책상 위)는 둘 다 아니다.** 최고칸을 **+0.04** 올린다. 18,000원에.
⇒ **「사도 아무것도 안 변한다」로 느껴지는 것이 당연하다.** 그리고 좋은 자리 중 하나(창턱)는
  §lampFit 버그로 **75% 는 못 붙는다.** 값이 있는데 닿을 수가 없었다.

### [house] ⓑ 창턱 0.36×0.30 — 화분과 등은 **같이 못 선다.** 그런데 막는 것이 없다

| | |
|---|---|
| 창턱 상판 | **0.36 × 0.30** |
| 화분(그 자리 최대) | 0.27 |
| 집게등 발자국 | 0.20 × 0.24 |
| 나란히 놓으려면 | **0.47m** ⇒ ★ **0.36m 로는 안 된다** |

집게는 **가장자리에 무는** 물건이라 상판 밖으로 걸칠 수 있다(`lampFit` pad 0.25).
그래서 물릴 수 있는 1720점 중 **67%는 화분과 안 겹친다.** 나머지 **33%는 겹친다.**

★ **겹치면 어떻게 되나 — 그냥 겹쳐 보인다.**
- `potFits`(room_view:4179) 는 **자리의 최대 지름과 상판 밖으로 나가나**만 본다. 등을 안 본다.
- `lampFit` 은 **상판 안인가·높이·천장**만 본다. 화분을 안 본다.
- ⇒ **어느 쪽도 상대를 모른다.** 밀려나지도, 막히지도, 경고도 없다. **서로 뚫고 들어간다.**
- ⇒ 화면에서 화분 속에 등이 박혀 보인다. 그리고 그 자리가 **제일 밝은 값(8.99)을 낸다** —
  곧 **못 놓는 자리가 제일 좋아 보인다.**

**아침에 정할 것**: ⓐ 겹침을 막을까(둘 중 하나가 거절) ⓑ 창턱 상판을 넓힐까
ⓒ 그대로 두고 사용자 눈에 맡길까. ⛔ 셋 다 밸런스·크기라 안 건드렸다.

### [house] 등을 다 사도 «키울 수 있는 화분 수»가 안 는다 (반지하)

| 등 수 | 최고칸 | 자랄 수 있는 칸(≥2.7) | 갈라짐(≥6) | 15칸 합 |
|---|---|---|---|---|
| 0 | 3.68 | **1** | 0 | 7.46 |
| 1 (바) | 6.02 | **1** | 1 | 13.07 |
| 2 (+집게) | 6.06 | **1** | 1 | 16.09 |
| 3 (+거치) | 6.17 | **1** | 1 | 22.21 |

**124,000원 · 68W 를 다 써도 키울 수 있는 칸은 1개 그대로다**(기본 자리 기준).
등이 하는 일은 그 한 칸을 3.68 → 6.17 로 밝히는 것뿐이다.
⇒ 위 표대로 **옮기면 달라진다**(선반 윗단 3칸). 곧 이 게임에서 등은 **사는 것이 아니라
  «두는 것»이 값**인데, 지금은 **둘 수가 없다.**

**아침에 정할 것** — 「집게형을 없앤다」의 답이 바뀝니다:
- 버그를 고치면 집게등이 **첫 플레이의 열쇠**가 된다(창턱 8.99 = 무늬 갈라짐).
- 그래도 남는 것: ⓐ 못 놓는 곳이 바닥의 80~90% ⓑ 창턱 상판이 **0.36×0.30** 으로
  좁아서 **화분(최대 0.27)과 등이 같은 상판을 나눠 써야 한다** — 겹침 판정이 없다.
- ⇒ **「고쳤는데도 안 되면」이 진짜 제거 근거**라는 총괄 말이 맞다. 지금은 «고치면 된다» 쪽이다.

**잰 것**: `tools/_probe_clipworth.mjs` · `tools/_probe_marginal.mjs`. 임시 도구.


### [house] ★ 거치형이 «지금 자리»에서 제 값의 3분의 1만 한다 (72,000원 · 36W)

반지하 `banjiha-growlight-stand` 를 등2 → 등3 으로 늘렸을 때 **15칸 DLI 증가합**:

| 자리 | 좌표 | 증가합 | 제일 큰 한 칸 |
|---|---|---|---|
| **지금 자리** | (−1.15, −0.6) | **6.12** | etagere:6 +0.80 |
| 자리를 제일 많이 덮는 점 | (−0.25, −1.39) | **20.93** | etagere:7 **+3.59** |
| 3단 선반 앞 | (−0.6, −1.3) | 16.94 | etagere:6 +3.16 |
| 창 앞 가운데 | (0, −1.2) | 16.60 | etagere:8 +2.59 |
| 서랍장 앞 | (1.6, 1.3) | 6.41 | dresser:0 +2.73 |
| 방 한가운데 | (0, 0) | 5.23 | nightstand:0 +0.55 |

**0.9m 옮기면 3.4배**다. 그리고 그 차이가 등급을 가른다 —
지금 자리에서는 제일 밝아진 칸이 **1.75** 로 아무 문턱도 안 넘는데,
(−0.25, −1.39) 에서는 `etagere:7` 이 1.00 → **4.59** 로 **무늬종 최소 4.2 를 넘는다.**

★ 왜 이렇게 됐나 — 데이터 주석에 까닭이 적혀 있다:
> *"방 왼쪽은 벽등(창 위)도 집게(오른쪽 선반)도 안 닿는 **유일한 구석**이다.
>  앞 둘이 이미 밝히는 데를 또 밝히면 셋째 등을 사는 뜻이 없다."*

뜻은 옳다. **그런데 그 구석에는 화분 자리가 하나도 없다.** 반지하에서 스탠드가 설 수 있는
점 1005개 중 **85%(854개)가 화분 자리를 0.7m 안에 하나도 안 둔다.** 겹치지 않게 두려다
**아무것도 안 밝히는 자리**로 갔다.

⛔ **안 옮겼다** — 등 자리는 DLI 를 움직이고 그건 밸런스다(밤샘 규칙 ②).
⇒ 아침에 정하실 것: **셋째 등의 기본 자리를 (−0.25, −1.39) 근처로 옮길까요.**
   (스탠드는 `movable:true` 라 사용자가 옮길 수는 있다. 다만 **기본이 제일 나쁜 자리**다.)

**잰 것**: `tools/_probe_stand.mjs`(설 수 있는 자리) · `tools/_probe_stand2.mjs`(자리별 DLI). 임시 도구.

### [house] ⚠ 정정 — 「등이 떠 있다」는 없다. 그리고 창턱 적중률은 25% 다

한때 「식물등이 데이터보다 +0.3864m 떠 있다」로 보였는데 **아니다.**
`house.js:864` 에서 `rig.pos` 는 **발광점**(`emitY = yBase + h*0.92`)이고 `rig.base` 가
**노드**다. 발광점을 노드로 읽어서 그렇게 보였다. **등은 안 떠 있다.**

같은 착오로 창턱 적중률을 **36%** 로 적었는데, `lampFit` 이 보는 것은 노드(0.75)이므로
제대로 재면 **25%** 다 — 더 나쁘다. 결론과 고칠 곳은 그대로다. 자를 고쳤다.
★ 오늘 아침 것과 같은 종류다 — **이름이 비슷한 두 값 중 저쪽이 실제로 읽는 것**을 골라야 한다.


### [Char] — 자 둘. 크레딧 0 · 삭제 0

**① `tools/check_shot_anomaly.py` 신설** (`001fd8d`) — core 하네스용.
찍은 그림에서 **깨진 화면**을 짚는다: 빈/단색·새까맘·새하얌·안 그려짐·잘림·반쪽·
**앞 컷과 동일(화면이 안 넘어감)**. 문턱은 이미 찍힌 **437장에서 재어** 정했다.
자 검사가 양쪽을 본다 — 만든 깨진 그림 **6/6 잡고** 정상 437장은 **0장** 걸린다.
```
python tools/check_shot_anomaly.py <폴더>
python tools/check_shot_anomaly.py --selftest
```
⚠ **통과는 「괜찮다」가 아니라 「깨지진 않았다」다.** 손가락이 엉뚱한 데를 짚거나
대사가 안 맞는 것은 사람만 안다. 문서 맨 앞에 적어 뒀다.

만들면서 **두 번 물렀고 둘 다 실제 그림이 잡아 줬다** — ①엣지 문턱을 고정값으로 두어
**밤 장면이 통째로** 걸렸다 ②고쳐도 또렷한 방 그림이 걸렸다(1136px 는 인접 화소 차가
작아 경계가 안 잡힌다) → **384px 로 줄여서 잰다.**

**② `tools/find_ghost_assets.py` 보강** ([Asset] 되먹임) — `archive/보관` 갈래 신설 ·
★ **선언으로 뺀 것을 요약에 드러냄.** 선언하면 보고에서 사라져 **잘못 선언해도 표시가
안 나던 것**을 [Asset] 이 짚었다. **선언이 면죄부가 되면 안 된다.**

**대기 중** — [core] 에 「뼈대가 어디까지 섰나 · 더 도울 자리」 물어 두고 답 기다리는 중.
그동안 자 검사 사례를 늘렸다(6종 + 논리 4종).

### [house] ★★ 집게등 — 「붙이기 어렵다」의 정체를 찾았다. **창턱을 찍어도 64%는 선반에 붙는다**

**재현**: 반지하에서 집게등을 끌어 **창턱 위에 놓으면 3단 선반(y 0.794)에 붙는다.**
창턱 범위 306점 중 **창턱에 붙는 점은 25%** 뿐이다. 나머지 75%는 3단 선반으로 간다.

**까닭** — `room_view.js §lampFit`:
```
const wantY = Number.isFinite(pos.y) ? pos.y : g.position.y;   // ← 등의 **지금** 높이
const cands = mounts.filter(k => pointInMountXZ(k, pos.x, pos.z, 0.25));
cands.sort((a, b) => Math.abs(a.y - wantY) - Math.abs(b.y - wantY));
```
그런데 `game.html:12786` 이 **`y` 를 안 넘긴다**:
```
r = roomView.previewLampAt(this.uid, { x: at.x, z: at.z, lift: 0 });
```
⇒ 손가락이 가리킨 **높이**가 버려지고, 등이 **지금 서 있는 높이**가 기준이 된다.
반지하 집게등 노드는 y **0.75** 에 있고(창턱 범위에서 겹치는 후보 중) —
```
|0.794(3단선반) - 0.75| = 0.044     ← 이긴다
|1.585(창턱)    - 0.75| = 0.835
```
⚠ **정정**: 처음에 이 값을 1.1364(36%)로 적었다. 그건 `rig.pos`(**발광점**)이고
`lampFit` 이 보는 것은 `rig.base`(**노드**, 0.75)다 — `house.js:864` 에서 갈린다.
제대로 재면 **25%** 로 더 나쁘다. 결론은 같다. 자를 고쳤다(`probe_lampfit.mjs` 머리말).
⚠ 그리고 **등이 떠 있는 버그는 없다** — 처음에 그렇게 보인 것은 발광점을 노드로 읽어서였다.
**창턱을 정확히 찍어도 선반이 이긴다.**

★★ 그리고 **되먹임이 있다.** 한 번 선반(0.794)에 붙으면 다음번 `wantY` 가 0.794 가 되어
창턱은 **더 멀어진다.** 등이 아래에 갇힌다. 「영 붙이기 어렵다」가 이것으로 보인다.

★ 값으로 보면 이것이 왜 아픈지가 보인다 — **창턱 3.68 · 선반 맨 윗칸 0.51.** 7배다.
반지하에서 창턱은 **유일한 밝은 자리**인데, 거기가 제일 안 붙는다.

**고칠 곳**: `game.html:12786` 이 `at.y`(= `freePlace.surfaceAt` 이 이미 내주는 높이)를
같이 넘기면 된다. `pos.y` 가 들어오면 `lampFit` 은 그 높이에 제일 가까운 단을 고르므로
**커서가 얹힌 상판이 그대로 뽑힌다.**
⛔ **[house]가 안 고쳤다 — `game.html`·`src/game/*` 은 [core] 소유다.** core 에 넘긴다.

**잰 것들** (임시 도구, 커밋 안 함): `tools/_probe_lampfit.mjs` · `tools/_probe_clipmount.mjs`

### [house] 집게등이 물릴 수 있는 넓이 — 방마다 9~25%

집게등은 **화분 자리가 있는 상판에만** 물린다(`lampMounts` 가 `built.plantSlots` 로 만들어진다).
바닥 평면을 0.1m 격자로 훑어 「여기를 찍으면 물리나」를 센 것:

| 방 | 물릴 상판 | 물리는 가구 | 바닥 대비 | 제일 넓은 상판 |
|---|---|---|---|---|
| 반지하 | 7 | 5 | **19.9%** | 책상 1.25×0.50 |
| 원룸 | 7 | 4 | **13.4%** | 책상 1.25×0.50 |
| 학원교실 | 9 | 9 | 24.6% | 창턱 7.20×1.00 |
| 투룸 | 10 | 5 | 16.1% | 원탁 0.75×0.75 |
| 아파트 | 40 | 18 | 14.8% | 탁자 1.25×0.75 |
| 온실 | 30 | 11 | **9.4%** | 탁자 1.25×0.75 |

⚠ 곧 **화면의 80~90% 는 집게등을 놓을 수 없는 곳**이다. 그 자체는 설계지만
(상판에 무는 물건이다), 위 §lampFit 버그와 겹치면 「아무 데나 놔도 안 붙는다」가 된다.
★ 반지하 창턱은 **0.36×0.30** — 유일한 밝은 자리인데 상판이 제일 작다.

### [house] ④ 벽등 — 저장소를 재 봤다

- `lamp_wall`(벽등)은 **있다. 다만 장식등이다** — ppfd_ref **5**, 22,000원, `grow` 없음.
- **식물등은 셋뿐이다**: `growlight_clip`(120ppfd·12W) · `growlight_bar`(180·20W) ·
  `growlight_stand`(250·36W). **벽에 붙는 «식물»등이라는 프리셋은 없다.**
- 다만 `growlight_bar` 는 이름이 **「바(선반밑)」**인데 반지하에서는 **창 위 벽**에 붙어 있다
  (`banjiha-growlight-bar` (0, 2.196, −1.85)). ⇒ **사실상 벽등으로 쓰이고 있는데 이름은 선반밑이다.**
- ⇒ §9 에 물음으로 올린다.



### [leaf] 썸네일이 낡았던 것 고침 · `assets/gen` 을 「원료」로 선언 (유령 177 → 70)

**① 썸네일 여덟 장** — `159d900` 이 `.glb` 만 담고 `thumbs/*.png` 를 안 담았다.
썸네일로 보는 사람은 고쳐진 잎을 **여전히 삭은 것으로** 봤다. [Plan] 이 잡아 줬다
(*"이번엔 **찍은 때가 자**"*). **여덟 장만** 다시 찍었다(`a32a039`).
⚠ 104장을 다 찍지 않았다 — 앞서 그랬다가 **103장이 같은 그림**이라 19MB 만 늘리고 되돌린 일이 있다.

**② `assets/gen/_role.txt`** (`8efb3aa`) — [Char] 이 자에 넣은 「원료」 갈래를 실제로 켰다.
```
assets/gen/2026-08-12   유령 87 -> 0
저장소 전체             유령 177 -> 70   [실측]
```
그리고 `assets/raw/_role.txt` 도 같이 세웠다(집 원화 등 원본 그림).
[Char] 자의 인코딩 고침도 확인했다 — `PYTHONIOENCODING` 없이 `--selftest` 가 돈다.

**★ 남은 유령 70 중 내 마당 넷 — 갈래가 모자라서 못 뺀다**
```
assets/monstera/skins/_rejected   9   폐기한 잎을 남겨 둔 것
assets/glb_test (+_analysis)     18   Meshy 변환 시험 산출물
assets/existing/_dupes            2   중복본
```
⇒ 셋 다 **일부러 안 쓰는 것**이다. 유령도 아니고 원료도 아니다.
  자가 아는 낱말은 **`원료`·`뷰어용` 둘뿐**이라 선언할 말이 없다.
  ⇒ ★ **[Char] 께 청함: `archive`·`보관` 을 셋째 갈래로 넣어 주십시오.**
    한 줄이면 되고, 그러면 남은 70 중 29개가 제자리를 찾는다.
    (내가 그쪽 파일을 안 고친다 — `tools/` 는 만든 창 소유다)

**⚠ 그리고 자에 한 가지 짚어 둔다** — 「원료」로 선언한 폴더가 **보고에서 통째로 사라진다.**
206장이 「1651개 중」에도 안 들어간다. 계율 ㉓ 의 그 모양이다 —
**「괜찮다」로 갈리면 아무도 다시 안 본다.** 잘못 선언해도 아무 표시가 안 난다.
⇒ 제안: 요약에 **「원료로 선언돼 뺀 것 206개(assets/gen)」 한 줄**만 찍어 주면
  사라지지 않으면서 유령 수도 안 늘어난다.



**③ `_v1`(쨍) 36장 훑기** — 36갈래를 **큰 판으로 다 봤다.**
★ 총괄 예상(*"너무 튀는 쪽일 것"*)과 **다르다.** 튀는 것은 없었다.
  실제 문제는 **흰·크림 무늬가 베이지·탄색이 되는 것**이다. `_v1` 이 채도 ×1.55 라
  **거의 흰 화소는 따뜻한 밑색이 드러난다.** 이름이 `*white`·`*silver` 인 잎이
  **「흰 데가 없는 잎」**이 됐다 — [Plan] 이 말한 *"이름과 색이 어긋나는 것 자체가 신호"* 다.
```
확실한 것 둘만 고쳤다 (7bbd55c)
  pothos_whitegreen_29_v1  ✅ 잎 가장자리의 그을린 갈색이 사라졌다
  mon_star_pinkmint_v1     ⚠ 탄색은 뺐는데 분홍이 아직 붉다 — 화면에서 다시 본다
고침: 채도 낮은 화소(s<0.18)는 ×1.12 만 올리고 색상도 안 돌린다
```
⚠ **나머지 아홉은 안 고쳤다.** `*cream`·`*silver` 계열이 따뜻해진 것인데
  **크림 무늬로 읽을 여지가 있다.** 실제 알보 몬스테라는 흰색이 아니라 크림이다.
  ⇒ **내가 정할 것이 아니라 §9 에 올렸다.**
⚠ 그리고 **썸네일로 봤다.** 화면에서 조명을 받으면 또 다를 수 있다.

> ## ⚠⚠ 2026-08-23 밤 · **등 배치 버그가 있던 동안 잰 값이다** ([House] 발견)
>
> [House]: *"등은 「사는 것」이 아니라 **「두는 것」**이 값인데 **지금은 둘 수가 없다.**"*
> **제일 좋은 자리가 `lampFit` 버그로 75%가 안 붙는다.** 등 셋을 다 사도 키울 수 있는 칸이
> **기본 자리에서는 1개 그대로**이고, 옮기면 3개다(거치등은 0.9m 옮기면 6.12 → **20.93**).
>
> ⇒ ★ **아래 숫자는 「등을 기본 자리에 둔 판」의 값이다.** [core] 가 고친 뒤 **다시 봐야 한다.**
>
> ⚠ **다만 전부가 흔들리는 것은 아니다 — 갈라 둔다:**
>
> | 흔들리는 것 | 안 흔들리는 것 |
> |---|---|
> | **경로 시뮬 값** — 유효생장 · 잎 수 · 이사율 · 막힌 축 | **순수 셈** — 모주 값(잎 수 × `leafWon`) · 삽수 −15,000(곱수 비) |
> | 등이 값을 갖느냐(`①-1` · `②-a`) | **`probe_crop_balance` 200일 표** — 머리글이 **「등 없음」**이라 애초에 등을 안 썼다 |
>
> ⇒ **뭉뚱그려 「다 못 믿는다」로 적지 않는다.** 그러면 멀쩡한 셈까지 버려진다.


**④ 튜토 에셋 전수 대조** (`f6cc0c7`) — **빈 화면 위험 0.**
코드가 이름을 박아 부르는 에셋 **32/32 있다**(2개는 `${stage}` 조립까지 풀었다) · 썸네일 17/17 ·
크기 전부 말이 됨 · **시루 계약 성립**(없으면 `throw` 라 첫 판이 못 열린다).
★ **재배판 칸이 두 벌**인 것을 찾아 자로 굳혔다 — `tools/leaf/check_tray_slots.py`.
지금은 12칸이 한 자리도 안 어긋난다. **안 갈렸을 때 굳히는 것이 제일 싸다.**

**⑤ 7해상도 화면 보기** — ⚠ **결론을 못 낸다. 도구가 딴 데를 찍고 있었다.**
```
BASE = process.env.BYEOT_URL || 'https://chunryou-delicate.github.io/lux-plant-sim'
```
⇒ 환경변수를 안 주면 **올려둔 사이트**를 찍는다. 지금 로컬로 다시 찍는 중이다.

그래도 **눈으로 본 것 넷**(주소와 무관하게 보인다):
```
① 「다음 날」 버튼이 폰에서 대사창 뒤에 깔린다 (320·390·932 가로). PC 는 또렷하다
② 같은 문구가 두 번 — 「할 일」 상자와 화면 맨 아래
③ 초상화가 방과 글자를 가린다. 좁을수록 심하다(320 에서 방 왼쪽 절반)
④ 가로에서 초상화가 대사창 위로 삐져나와 잘린다
★ PC(1920x1080)는 아주 좋다 — 방·가방 옆판·「다음 날」이 다 제자리다. 폰이 문제다
```
⚠ **「방이 안 뜬다」는 아직 원인을 모른다.** 규칙을 셋 세웠는데 셋 다 깨졌다
(화소수 · 세로길이 · DPR). 그리고 **1판에서 실패한 PC 가 2판에서 떴다** — **판마다 다르다.**
⇒ **「아이폰에서 방이 안 보인다」로 읽으면 안 된다.** 찍는 환경 이야기일 수 있다.

★ [Char] 자(`check_shot_anomaly.py`)는 **62장 중 0장** 걸렸다. 위 넷을 **하나도 못 잡는다.**
  자를 키울 자리는 **「눌러야 하는 것이 다른 것에 가려졌나」**로 보인다.


**⑥ ⚠ 내가 또 헛본 것 — 「오른쪽에 빈 띠가 있다」는 틀렸다**
안 본 셋(430x932·768x1024·844x390)을 한 판에 붙여 보다가
*"게임이 화면 폭을 다 안 쓴다. 오른쪽 28% 가 빈다"* 로 읽었다. **재 보니 아니었다:**
```
일곱 장 다 내용이 화면의 99~100% 를 채운다
```
⇒ ★ **빈 띠는 게임이 아니라 내가 만든 몽타주의 여백**이었다.
  세로로 긴 그림을 620px 에 맞춰 줄이니 짧은 것 옆이 바탕색으로 남은 것이다.
⇒ **내가 만든 그림을 증거로 쓰다가 내 그림의 흠을 게임의 흠으로 읽었다.**
  오늘 세 번째다(3q 썸네일 · 「다음 날」 · 이것). **셋 다 「내 자를 안 의심한 것」이다.**

**대신 그 셋에서 진짜로 걸린 것:**
```
844x390(가로)  ★ 대사창이 방을 거의 덮는다 — 방이 보이는 띠가 원래 얇은데 그 위를 덮는다
844x390        「할 일」 상자와 「건너뛰기」가 겹쳐 글자가 잘린다
430x932·390x844  「할 일」 상자가 안내문 첫 줄을 덮는다
```


**⑦ 사용자 화면을 보고 — 사각 화분에 민트·핑크를 만들었다** (`42b2b26` · 크레딧 0)
사진에 **네 번 나오는 화분**인데 색 변이가 없었다. 민트/핑크가 있는 것은 둥근·테라코타 둘뿐이다.
★ **규칙을 지어내지 않았다** — 이미 있는 색 판을 재서 옮겼다:
```
색상을 「돌리는」 게 아니라 한 색으로 덧칠하는 것이었다.
바탕이 테라코타(19°)든 회백(151°)이든 결과는 똑같이 민트 151° · 핑크 342~343°.
⇒ 잎의 _v1/_v2 와 방식이 다르다. 잎은 「옮기는 것」, 화분은 「칠하는 것」이다
```
만든 것: `_c1` 색상 150·채도 0.130 · `_c2` 색상 344·채도 0.128 — **기존과 한 자리 안에서 맞는다.**
⚠ 채도를 **0.13 으로 낮게** 뒀다. 콘크리트는 회백이라 올리면 **플라스틱처럼** 보인다(총괄 경고).
⚠ **아직 3D 로 안 봤다.** 텍스처로만 봤다 — 얼룩·재질감은 살아 있다.
  **브라우저가 [Char] 차례라 썸네일은 나중에 찍는다.** 그때 눈으로 다시 본다.
⇒ 배선(상점에 민트/핑크 칸)은 **[core] 몫**이다. `shop.js:546·557` 과 같은 모양이면 된다.


**⑧ ★★ 7해상도 — 「방이 안 뜬다」의 정체가 나왔다. 해상도가 아니다**
[Char] 가 DOM 을 같이 찍어 준 덕에 갈렸다. **넷은 `ready=false` 다:**
```
        ready  talking  무엇이 떠 있었나
1920x1080  ✅    true    (정상)
320x568    ✅    true    (정상)
390x844    ✅    true    (정상)
430x932    ❌            ⛔ 오류 상자
768x1024   ❌            ⛔ 오류 상자
844x390    ❌            ⛔ 오류 상자
932x430    ❌            ⛔ 오류 상자
```
오류 상자 전문(눈으로 읽었다):
> **"게임 코드를 불러오지 못했습니다 — 진행 버튼은 잠긴 채로 둡니다"**
> **"모듈 파일을 읽지 못했습니다 — 경로 미상"**
> *"파일을 직접 연 것이라면 `python tools/serve.py` 로 띄운 주소에서 열어 주세요."*

⇒ ★★★ **해상도 문제가 아니다.** GPU 도 화소수도 아니다.
⇒ ⚠ **다만 「모듈이 없다」도 아니었다** — [Char] 실측: **셋을 한꺼번에 띄우면 전부 false,
  하나씩 띄우면 전부 true.** 같은 서버·같은 코드다. ⇒ **부하로 로드가 밀린 것**이다.
  화면에 뜬 문구(*"모듈 파일을 읽지 못했습니다 — 경로 미상"*)는 **결과이지 원인이 아니었다.**
  ★ 그래도 **그 넷의 화면 판정은 못 쓴다**는 것은 그대로다.
  내가 규칙을 셋 세웠다 깨진 까닭이 이것이다 — **애초에 기하 문제가 아니었다.**
⇒ ★ 그리고 **판마다 다른 것**도 설명된다. 여럿을 한꺼번에 띄우면 모듈 읽기가 실패한다.

## ⚠⚠ 그래서 **판정을 갈라야 한다**
```
ready=true  인 셋만  레이아웃 판정에 쓸 수 있다
ready=false 인 넷은  ★ 못 쓴다 — 게임이 안 뜬 화면이다
```
⇒ ⚠ **[Char] 자의 `offscreen` 목록 중 넷이 그 상태에서 나온 것**이다.
  **탭 여섯이 화면 밖」은 `320x568` 한 곳에서만 확인된 것**이다(`ready=true`).
  나머지 넷은 **모른다.** 다시 찍어야 안다.

## ★ `ready=true` 인 셋에서 진짜로 나온 것
```
320x568     ★★ 탭 여섯이 화면 밖 (가방·식물·상점·방·할 일·검수) + 가방칸·ⓘ·가계부도
            「건너뛰기」 68×30 — 손가락에 작다
390x844     「건너뛰기」 68×30 뿐. 나머지 깨끗
1920x1080   ♪ 단추가 대사 글자에 가려짐 · ⓘ 가 19×19
```
⛔ **앞서 「320 에서 가방을 못 연다」고 적었던 것은 취소다.** 낡은 판을 본 것이다.
⇒ ★ 그러면 이 밤 화면 검사에서 **확정된 것은 「넷이 모듈을 못 읽었다」 하나**다.
  나머지는 **작은 것**(「건너뛰기」 68×30 · ♪ 가려짐 · ⓘ 19×19)이다.

### [plan] 물을 주게 고쳤는데 **이사는 여전히 0/24** 다 — 둘 중 하나가 또 다르다

`test_balance_routes` 에 `waterPot` 을 넣어 자를 살렸다(유효 54→335 · 잎 1→6 · FAIL 5→3).
**그런데 이사는 그대로 0/24 다.** `banjiha_routes` 는 **A 38% · B 60% · C 100%** 다.

내가 본 데까지:
- 두 자가 **같은 모양으로 판다** — `listCutting` · `listPot` · `canMoveOut` · `moveOut` 다 있다
- 갈리는 것은 **시루 수**로 보인다. `balance_routes` 는 **시루 1개 고정**이고
  `banjiha_routes` 는 퀘스트대로 늘린다. 오늘 실측에서 **시루 1개는 126일에 파산**했다
- ⚠ **그런데 못 나간 판이 「돈」에서 막혔는지 「무늬 판 적」에서 막혔는지 자가 안 찍는다**

★ **그 한 줄이 없어서 여기서 더 못 간다.** 이사 게이트는 축이 둘인데
  자는 「못 나갔다」만 말한다. **「돈이 모자랐다」와 「무늬를 한 번도 못 팔았다」는 완전히 다른 답**이다.

✅ **풀렸다 (같은 밤 · 자가 찍게 했다)** — **돈에서 막힌다.**
```
A 등 없이 — 못 나간 24판: 돈만 모자람 20 · 무늬만 못 판 0 · 둘 다 4
B 등 1개  — 못 나간 24판: 돈만 모자람 21 · 무늬만 못 판 0 · 둘 다 3
모자란 돈 중앙값 2,000,000원 (= 이사비 전액)
```
★ **무늬는 20/24 판이 이미 팔았다.** 막힌 것은 돈이고,
  모자란 돈이 **이사비 전액**이라는 것은 **지갑이 사실상 0** 이라는 뜻이다.
  시루 1개로는 하루 지출 16,667원을 못 당해 **무늬 삽수를 판 돈도 생활비로 다 나간다.**

⇒ **두 자는 다른 판을 재는 것이고 둘 다 맞다:**
  · `balance_routes` = **시루 1개 고정** = 「채소를 안 늘리는 사람」 = **바닥값**
  · `banjiha_routes` = **퀵스트대로 늘림** = 「안내를 따르는 사람」 = **표준**
  ⇒ **고칠 것은 셈이 아니라 이름·설명이다.** 내 밤샘 설계의 기준선은 뒷에것이다.

★★ **그리고 밀런스로 값진 것 하나 — 아침에 물어 주십시오:**
> **시루 1개로는 무늬를 팔아도 못 나간다.**
> 「채소를 안 늘리면 무늬만으로는 탈출 불가」이다. 설계인가 사고인가?


⚠ 값은 한 톨도 안 건드렸다. `③ 튜토 완주 300일·70.7분` 도 **이사를 못 해서 300일까지 돈 값**이라 아직 못 쓴다.

#### [Char] 추가 — 해상도 촬영과 「가려짐」 검출 (커밋 `59e8a73`)

**⚠⚠ 앞서 여기 적었던 「탭 여섯이 화면 밖 → 가방을 못 누른다」는 물립니다. 헛것이었습니다.**

조상 체인을 찍어 보니 `tabBag` 의 조상 `sheet` 가 **top 849 인데 창 높이가 844** 였습니다.
**가방 서랍이 닫혀서 화면 아래로 내려가 있던 것**이고, 그 안의 탭이 밖인 것은 **정상**입니다.
[core] 도 따로 같은 것을 찾았고, 그 창 320 판은 **6일을 정상으로 밟았습니다.**
⇒ **「닫힌 서랍 안의 물건이 안 보이는 것을 고장이라 한 셈」**입니다.

**⚠ 그리고 「다음 날 버튼이 대사창에 덮인다」도 물립니다.**
`game.html:839` 에 `#stage.talking #next { opacity:.35; pointer-events:none }` 이 있습니다.
**대사 중에는 일부러 끕니다.** 그리고 `elementFromPoint` 는 그런 요소를 **건너뛰고 밑을 잡습니다.**
⇒ 그 CSS 는 **08-19 에 그 민원을 고친 자국**이었습니다. **제가 「병」이라 올린 것이 「고친 흔적」이었습니다.**
[Asset] 이 CSS 를 읽어 잡아 줬습니다.

**★ 그래서 자를 고쳤습니다 — 「안 보이는 까닭」을 다섯으로 가릅니다** (커밋 `423cfdd`)
```
inClosedPanel    닫힌 패널 안        정상
coveredBySheet   시트가 열려 그 아래   정상
coveredByModal   모달이 떠 있음       정상
disabledOff      일부러 꺼둠          정상일 수 있음 (.talking 칸으로 가른다)
occluded         ★ 아무 까닭 없이 남이 잡힘 — 이것만 진짜
```
효과를 숫자로 확인했습니다 — **390×844 에서 「탭 3개 밖 + 문구 중복」 → 「문구 중복 하나」.**

★ **오늘 저는 같은 자리에서 세 번 틀렸습니다** — 꺼짐 · 닫힌 시트 · 부하로 덜 뜬 화면.
셋 다 「안 보인다」였는데 **까닭이 달랐습니다. 까닭을 안 찍으면 전부 「가려짐」이 됩니다.**

**★ 남은 진짜 후보 하나 — 문구 중복. 출처를 코드로 확정했습니다**
```
quest.js:297     todo: () => '시루를 방 안에 놓아 보세요'      ← 값은 한 곳
game.html:7871   <div class="qtodo">${esc(q.todo)}</div>       ← 목록이 그림
game.html:8878   $('quest').textContent = … questGoal …        ← 아래 띠도 그림
```
**글자가 두 벌인 게 아니라 한 값을 두 자리가 동시에 그립니다.** 320·390·1920 **전부**에서 납니다.
⇒ **해상도 문제가 아닙니다.** 그리고 `8868` 주석이 *"문구를 여기서 새로 짓지 않는다 —
`questTodo` 하나가 정본이다"* 라 합니다. **정본을 하나로 삼은 것의 부작용**입니다.
⇒ 의도인지 사고인지는 **[Plan] 몫**이라 단정 안 했습니다.

**어떻게 잡나** — 픽셀이 아니라 **DOM**. 버튼 가운데를 `elementFromPoint` 로 찔러
남이 잡히면 덮인 것이다. 화면밖·글자잘림·32px미만도 같이 잰다. 곁파일(`.json`)로 남긴다.
⇒ [Asset] 이 눈으로 짚은 넷을 자가 0장 잡은 것은 **문턱이 아니라 갈래가 없어서**였다.

**도구**
```
node tools/shoot_screens.mjs                    # 7해상도(가로 둘 포함) · 기본 로컬
SHOT_SIZES=320x568 node tools/shoot_screens.mjs # 골라 돌리기
python tools/check_shot_anomaly.py <폴더>       # 깨짐 + 가려짐
python tools/check_shot_anomaly.py --compare <폴더>   # 해상도끼리 견주기
```

**⚠ 물러선 것 둘**
· 기본 주소가 **올려둔 사이트**였다([Asset] 지적) → **로컬로 바꿨다.** 그러면 오늘 고친 것이
  하나도 안 든 판을 찍는데 **화면이 멀쩡히 떠서 아무도 눈치 못 챈다.**
· 일곱을 한꺼번에 띄우니 **부팅이 실패**했다. 단독으로 다시 재니 멀쩡 — **부하였다.**
  묶음 2개씩 + 실패는 혼자 다시 띄우게 고쳤다.

**남은 것** — [core] 400일 판이 끝나면 **7해상도 재촬영**. 나머지 6종에도 같은 것이 있는지는 그때 나온다.

---

## ⑦ ★★ 토큰 — 떨어지면 **멈췄다가 재개하라** (사용자 지시)

> *"진행하다 **토큰떨어지면 잠시 멈췄다가 토큰 리셋되면 다시진행해** (토큰리셋은 2시간 20분 뒤야)"*

· 한도에 걸리면 **끝내지 말고 멈춰라.** 리셋되면 하던 자리에서 이어라
· ⚠ **여섯 창이 같은 한도를 나눠 쓴다.** 하나가 크게 쓰면 다른 창이 못 돈다
· ⇒ **아껴라**: 보고는 짧게 · 같은 파일을 여러 번 읽지 마라 ·
  큰 출력은 파일로 남기고 요약만 올려라 · 총괄에게 보낼 것도 요점만
· ⇒ **멈추게 되면 이 파일 §10 에 「어디까지 했고 다음은 무엇인지」 한 줄 남기고 멈춰라.**
  그래야 다른 창이나 아침의 사람이 이어받는다

## ⑧ 사용자 추가 지시 (밤샘 중 도착)

· **확실한 버그 수정 — 정식 허락** ✅
· ★ **주 임무는 「튜토 부분이라도 확실하게 끝내는 것」** — 범위가 좁혀졌다
· ★ **삽수를 튜토에서 막는다** — *"어차피 원룸에서 삽수 가이드를 따로할거면"*
· **튜토가 빨리 끝나면 → 원룸 초반 스토리·퀘스트라인 기획·제작.** 가이드는 **삽수 위주**
· **가구 구매도 원룸에서 제대로 열리게**
· ★ **식물등 전반 상세 검토** — 벽등 설치 · 거치형 배치 추가 · **집게형은 없애고 싶다**(의향, 확정 아님)
· ★ **화면 비율·기종별로 다 잘 되게** — *"핸드폰 화면비율별(가로세로, 폭, 길이 등)
  기종별( ex 아이폰) 등에서 다 잘되도록 하는것도. 진행해줘."*
  ⇒ 하네스가 **같은 판을 여러 해상도로** 돌리면 한 번에 잡힌다. [core] 몫.
  ⚠ 08-22 민원 *"해상도에 따라 [다음 날] 버튼 클릭 오류(pc)"* 와 **같은 계통**이다.

### ⚠ 밤새 나온 함정 — **긴 커밋 메시지를 `-m` 여러 줄로 주면 멈춘다** ([Asset] 겪음)

커밋이 **2분 시간 초과로 죽으며 `.git/index.lock` 을 남겼다.** git 프로세스가 하나도 안 도는 것을
확인하고 지웠다(다른 창 것이 아니었다).
⇒ ★ **lock 이 남으면 다른 창이 막힌다.** 공유 트리에서 제일 아픈 자리다.
⇒ **긴 메시지는 `git commit -F -` 로 heredoc 을 주라.** [growth] 도 백틱 사고 뒤 그렇게 바꿨다.
⇒ `git add`·`git stash`·`push` 와 한 줄이다 — **「내 것만 다룬다」고 믿게 만드는데 저장소 전체를 건드린다.**

### [growth] 밤 결과 — ★ 먼저 읽을 요약 (2026-08-23)

> **이 절을 읽는 법** — 아래 긴 기록보다 이 표가 먼저다.
> **「확인」과 「후보」를 갈라 놨다.** 후보를 확인으로 읽으면 없는 일을 고치게 된다.
> 내가 오늘 그걸로 **여섯 번 틀렸고 여섯 번 다 스스로 잡았다**(맨 아래 목록).

#### ★ 마지막 결과 — 「루프가 0번 돌아 안 불리는 단언」은 **넷뿐이다**

**정적으로 안 하고 실행으로 쟀다.** `assert` 를 감싸 **호출된 줄 번호**를 세고 소스의 자리와 견줬다.
```
통과 검사 28개 · 볼 수 있는 단언 자리 1,573 · 한 번도 안 불린 자리 8
  그중 4 = assert.fail(...)  → 원래 안 불려야 하는 안전장치다. 병이 아니다
  ★ 진짜 후보 4 (파일 둘)
```
| 자리 | 모양 | 뜻 |
|---|---|---|
| `test_quest:64·66` | `for (const q of QUESTS.filter(x => !questSpeaks(x)))` | **목록이 비면 0번 돈다.** 「조용하기로 한 줄은 정말 대사가 없다」를 **한 번도 안 확인하고 통과**한다 |
| `test_ending_flow:518·519` | `if (real.trace.doneOnDay != null) { … }` | **조건이 거짓이면 0번.** 즉 **`mode:real` 판이 엔딩에 도달 못 했다** — 그게 진짜 물음이다 |

⇒ ★ **「목록이 왜 비었나」가 진짜 물음이다.** `ending_flow` 는 **재려던 것을 못 재고 초록**이다.

**★ 이 숫자는 551 → 178 → 80 → 8 → 4 로 줄었다. 매 단계가 내가 잡은 내 오류다:**
1. `node:assert/strict` 를 안 감쌌다(검사 26개가 그것을 쓴다) — 「실행 0」이 무더기로 나와 잡혔다
2. 통과/실패 목록이 **몇 시간 전 것**이었다 — 밤새 남들이 고쳐 갈렸다. **같은 실행에서 종료코드를 받게** 고쳤다
3. ★ **계측기가 대상을 바꿨다** — 아래 별도 항목
4. **못 보는 자리를 「안 불렸다」로 셌다** — `throws`·`match` 계열 72자리. **「안 보인다」와 「안 불렸다」는 다르다**

#### ★★ 계측기가 대상을 바꾼다 (계율감)

```
test_first_play   계측기 없이 rc0 · rc0      계측기 붙여 rc1 · rc1   (각 2회 재현)
```
`assert.match` 가 **`doesNotMatch` 처럼 동작**했다. node 의 `assert` 는 **함수 동일성**으로 갈래를 가른다 —
`internalMatch` 안에서 `fn === assert.match` 를 보는데, 감싸는 순간 그 비교가 거짓이 되어 **뜻이 뒤집힌다.**
⇒ `match·doesNotMatch·throws` 계열은 **안 감싼다.** 붙여도 결과가 같은 것을 2회 확인했다.
⇒ ★ **재는 자를 붙였을 때와 뗐을 때가 같은지부터 보라.**

#### ⚠ 밤새 검사 실패가 늘었다 — **12 → 17** (단독 재측정 기준)

```
새로 깨진 5   balance_routes · cutting_wiring · escapecut · market · saleledger
재현 안 됨 4   cutstamina · first_play · loop_errors · propagation   ← 단독으로는 통과
              (first_play 은 ★내 계측기 탓이었다)
```
· `test_saleledger` 는 원인이 보인다 — *"[중고] 민무늬 삽수는 아직 안 팝니다"*. **제품 규칙이 바뀐 것**이고
  검사 각본이 낡았다. ⚠ **언제 바뀐 규칙인지는 모른다** — 밤에 삽수 차단은 미결이라 하니
  낮의 삽수 등급 수정(`9f298c8`) 여파일 수 있다. **[core]/[Plan] 판정 필요.**
· ★ **「붉은 것은 단독으로 다시 재라」가 하루에 세 번 값을 했다.**

⚠ 계측기는 `tools/_assertcount.mjs` 에 있다(`_` 라 커밋 안 함). 다시 쓰려면 위 「안 감싸는 것」을 지킬 것.

---

#### ✅ 확인된 것 — 돌려서 봤다. 이건 사실이다

| 무엇 | 증거 | 조치 |
|---|---|---|
| **`test_cdp.goto` 가 못 붙어도 안 죽었다** — 검사 13개가 각각 10분을 태웠다(합 130분) | `test_ground` 45초 멈춤 → 29초 종료 · `test_lampswitch` → 5초 | ✅ 고침 `47d948c` |
| **`probe_qa_full` 이 일 1 에서 멈춘다** — 「첫 달 가계부」가 [다음 날]을 덮는다 | `덮은것={"txt":"✕ 첫 달 가계부…"}` | [core] 고침 `5e9a050` |
| **그 자가 낸 「34일 돌려도 몬스테라 안 옴」은 유령** — 날짜가 1 에 멈춰 있었다 | 같은 실행 로그 | — |
| **남은 다섯은 가계부에 닿기 **전에** 죽는다** | `firstrun` rc0 47초 · `perf` 문법오류 17초 · `break·play·slowphone` rc2 | [core] 로 넘김 |
| **`probe_qa_firstrun` 은 아무것도 안 하고 종료코드 0** | 버튼이 `vis:false · 0×0` → *"누를 수 없었다"* | [core] 로 넘김 |
| **떨어질 수 없는 단언 5건** | 일부러 어긋내 FAIL 확인 · 사본에서 고치니 PASS | ✅ 자 신설 `a5bc628` · 주인 [core] |
| **`test_elec` 이 등 값 배열을 안 봤다 · 기구 이름을 손으로 적었다** | 셋 다 일부러 어긋내 FAIL · 가짜 넷째 등으로 PASS | ✅ 고침 `6ae18a2` |
| **검사 문구의 옛 등값 25,000** (판정은 안 갈렸다) | 전후 실패 건수 불변 | ✅ 고침 `1f3b469` |
| **`test_buypopup` 은 멀쩡하다** | 단독 71/71 PASS | ⚠ 내 오보였다 |

#### ⚠ 후보 — 읽어서 그렇게 보인다. **확인 아님**

| 무엇 | 몇 | 왜 후보인가 |
|---|---|---|
| **0 이 아닌 값으로 죽을 길이 없는 `probe_`** | **79 / 115** | `probe_` 는 사람이 읽는 도구라 **0 이 설계일 수 있다.** 「고쳐야 할 것」으로 읽으면 안 된다 |
| 그중 **「못 했다」를 찍으며 0 을 내는 것** | **11** | 이건 설계가 아니다. 실증된 것은 `qa_firstrun` **하나** |
| `#next` 를 **좌표로** 누르는 자 | **6** (그중 1 고쳐짐) | 요소 직접 클릭은 덮개를 뚫어 안 걸린다 |
| ㉤ 검사가 아닌데 이름만 `test_` | **2** | `test_roomview_shots`(사진 도구) · `test_cdp`(헬퍼) |

#### ⚠ 못 본 것 — **「없다」가 아니라 「못 쟀다」**

- `break · play · slowphone` 이 **왜 안 끝나는지** — 내가 100초로 묶어 그 앞을 못 봤다
- 브라우저 검사 32개 중 **살아 있는 서버로 돌린 것은 10개뿐**
- 「루프가 0번 돌아 단언이 아예 안 실행되는 것」 — **이 계통은 아무도 안 봤다**

#### 🔁 내가 틀린 여섯 — 전부 스스로 잡았다

1. **정적 자로 관문을 판정하려다** — `assert` 는 던지면 종료 1 이다. 11개 중 **9개 오진**
2. **정규식으로 유령을 세다** — 검사 이름이 템플릿 문자열이라 하나를 놓쳤다. **괄호를 세는 파서**로 잡았다
3. **`test_buypopup` 오보** — 병렬 부하에서 붉었다. **단독으로 재니 71/71 PASS**
4. **반증 설명을 지어냈다** — *"세이브를 넣어 첫 플레이를 건너뛴다"*. 확인 안 하고 드렸고 틀렸다
5. **`index.lock` 규칙을 성급히 올렸다** — 내 두 번은 됐고 [Plan] 두 번은 막혔다. **모른다**
6. **`probe_` 훑는 자가 확인된 사례를 못 잡았다** — `process.exit(2)` 가 **watchdog 상용구**였다

⇒ ★ **여섯에서 배운 것 하나** — **자를 만들면 「이미 아는 정답」이 그 자에 걸리는지부터 보라.**
  6번은 아는 답이 하나 있어서 잡혔다. 없었으면 **40개짜리 틀린 목록**을 냈을 것이다.

---

### [growth] 검사가 「울 줄 아는지」 훑기 · 브라우저 검사 (2026-08-23 밤)

#### 고친 것 넷 (전부 전후를 찍어 견줬다)

| 커밋 | 무엇 |
|---|---|
| `a5bc628` | `tools/test_no_ghost_checks.mjs` 신설 — 조건 자리에 상수가 박힌 단언을 잡는다. **지금 5건 걸린다(빨간 게 맞다)** |
| `1f3b469` | 검사 문구의 옛 등값 25,000 → 정본에서 읽게. 값 판정은 원래 안 갈렸고 **문구만** 갈렸었다 |
| `47d948c` | ★ **`test_cdp.goto` — 서버가 없으면 그 자리에서 죽는다** |
| `6ae18a2` | `test_elec` — 등 값 배열 미검사 · 기구 이름 손으로 적던 자리 제거 |

#### 제일 큰 것 — **130분이 사라졌다** (`47d948c`)

> ***원인은 서버인데 화면은 조건 이야기를 한다.***

크롬은 **연결이 거부돼도 `Page.navigate` 에 성공으로 답하고** `errorText` 에만 사유를 담는다.
`goto` 가 그 답을 통째로 버려서, 못 붙어도 안 죽고 **다음 `waitFor` 의 제한시간을 통째로 태웠다.**
기본 `BYEOT_PROBE_TIMEOUT_MS` 가 600초라 **하나에 10분**이고 그런 검사가 **열셋**이었다.

화면에 뜨는 말은 `기다리다 지쳤습니다: window.thLoaded()...` 다. **사람은 조건을 고치러 간다. 서버가 없는 건데.**

```
전  test_ground 45초 제한 다 쓰고 멈춤 · test_lampswitch 같음
후  test_ground 29초에 종료 1(남은 29초는 크롬 뜨는 시간) · test_lampswitch 5초에 종료 1
회귀 살아 있는 8963 에서 test_multiplant 그대로 PASS — 붙는 경우엔 코드 경로가 안 바뀐다
```
- 404 는 안 잡는다. 크롬이 404 를 「열었다」고 본다. `8971` 이 빈 디렉터리를 서빙 중이라 실제로 해당된다.
- 열셋 — `boot_profile · ground · lampmove · lampswitch · multisiru · musun_view · outside · perf_budget · place_grid · roomview_perf · roomview_place · roomview_shots · roomview_walk`

#### 훑은 결과 (80개)

```
전체 80  ·  브라우저 32  ·  비브라우저 48
비브라우저 48  ->  PASS 36 · FAIL 12   [실측]
브라우저   32  ->  죽은 포트로 전수 · 살아 있는 서버로 10개  [실측]
```

| 갈래 | 몇 개 | 무엇 |
|---|---|---|
| ㉡ 안 운다 | **5** | `furnishop:336` · `monthly:206` · `roomview_place:740` · `sellpopup:336` · `siruinfo:235` |
| ㉤ 검사가 아니다 | **2** | `test_roomview_shots`(사진 도구) · `test_cdp`(헬퍼) — 이름만 `test_` |
| 관문 없음 | **1** | `test_balance_routes` — **의도된 것**. 다만 스크립트로 돌리면 절대 안 걸린다 |
| 「제때 우는가」 | **13** | 위 목록. `47d948c` 로 닫혔다 |
| 숫자가 부풀어 보임 | **1** | `test_siru_pick` 의 `ok(name)` — 조건 인자가 없다 |

㉡ 다섯은 **같은 병이 아니다.** `furnishop` 은 위 두 줄이 진짜로 재고 있어 **숫자만 부풀린다**(지우면 됨).
나머지 넷은 **「건너뛴 것」을 「통과」로 찍는다** — ㉣ 로 찍혀야 할 것이 ㉠ 으로 찍힌다(`skip` 으로 바꿔야 함).

#### 아직 못 가른 것 — 붉은 브라우저 검사

```
test_bagcell       FAIL 4건  M-1·M-2·M-3 무순 칸 게이지 없음(#musunGauge -> null) · A-10 탭 배치 뒤 moving:false
test_place_confirm FAIL      ①-2 하단 액션바에 [다음 날]이 남아 있다     (+ 찍고 나서 안 끝남)
test_place_grid    FAIL      R-1 격자에 물린 뒤에도 화분이 상판 밖으로    (+ 찍고 나서 안 끝남)
test_sellpopup     FAIL      팝업 관련 (자세히 못 봄)
test_siru_pick     FAIL      AssertionError free:crop_01_01
```
- **`test_buypopup` 은 오보였다.** 병렬 부하에서 FAIL 이었는데 **단독으로 다시 재니 71개 전부 PASS** 했다.
  ⇒ **부하가 걸리면 빨개지는 검사가 있다.** 「1.2초 안에 개수가 오른다」 같은 시간 재기가 그렇다.
    여러 검사를 동시에 돌리면 **없는 버그가 생긴다.** 붉은 것은 반드시 **단독으로 다시** 재야 한다.
- `place_confirm`·`place_grid` 는 **FAIL 을 찍고도 안 끝난다** — 붙긴 붙었으니 위 goto 병과 다른 결이다.
- **무순 칸 수 7 은 코드가 맞다**(`first_play.CROP_KINDS musun.harvestDays = 7`). 못 찾는 것은 **화면 요소**(`#musunGauge`)다.
- **넷 다 「튜토를 막는가」는 아직 판정 못 했다.** 전부 [core] 소유 화면이라 손대지 않았다.

#### 내가 틀린 것 (다음 사람이 같은 데 안 빠지게)

1. **정적 자로 판정하려다 세 번 틀렸다.** `assert` 는 던지면 그 자체로 종료 1 이라 `process.exit` 이 없어도 관문이다 — 「관문 없음」 11개 중 **9개가 오진**이었다. 정적 자는 **후보 고르기까지만**.
2. **정규식으로 유령을 세다 하나를 놓쳤다** — `test_sellpopup:336` 은 검사 이름이 템플릿 문자열이라 안 걸렸다. **괄호를 세는 파서**로 바꾸니 잡혔다.
3. **`test_multiplant` 이 404 에서 통과한 줄 알았다** — 그 검사의 기본 포트는 **8963(살아 있음)** 이었다. **검사마다 기본 포트가 다르다**(8963 / 8971). `BYEOT_URL` 을 반드시 줄 것.
4. **`git commit -m` 안의 백틱을 셸이 실행했다** — 커밋 메시지 한 줄을 잃었다(`a5bc628`). 이미 밀려서 `--amend` 안 했다. 코드 예시가 든 메시지는 **heredoc** 으로.
5. **`git stash` 로 「내 탓인지」 가르다 `.git/index.lock` 충돌로 작업이 잠깐 갇혔다.** 공유 트리에서는 **파일 복사**로 가를 것.

### [plan] A2·A4 원인 둘 — **튜토는 안 막는다. 다만 마지막 관문에서 게임이 침묵한다**

하네스 없이 지금 잴 수 있는 둘을 팠다. **둘 다 코드에서 원인을 찾았다.**

#### ① `brokeTalk` 이 두 번 뜬다 — **버그가 아니라 대사 규율 문제다**

`tutorial.js:674`:
```js
if (ts.cashWon < 0) { ts.cashWon = 0;
  if (!ts.bankrupt) { ts.bankrupt = true; ev.push({ id: 'broke', … }); } }
else if (ts.bankrupt && ts.cashWon > 0) ts.bankrupt = false;
```
⇒ **깃발이 제대로 막고 있다.** 두 번 뜨려면 **그 사이에 하루라도 수입이 있어야** 한다
  (0원 → 삽수를 팔아 잠깐 + → 다시 0원). **규칙상 맞는 일이다.**

⚠ **문제는 대사 쪽이다.** `brokeTalk` 은 *"지갑에 남은 게 없다"* → *"…미안. 나는 돈은 못 만들어"* 다.
  **두 번째로 들으면 첫 번째의 무게가 사라진다.**
⇒ ★ **파산은 여러 번 나도 되지만, 그 대사는 처음 한 번만** 나와야 한다. 두 번째부터는 짧은 배너로.
  **오늘 식물신에 낸 것과 같은 결이다 — 힘은 드묾에서 온다.**

**⇒ 튜토를 막나: 안 막는다.** 튜토는 시작 150만에 하루 실지출 10,000원이라
  33일이면 33만 + 첫 월세 20만 = **53만**이다. **97만이 남는다. 파산 근처도 안 간다.**

#### ② ★★ `move_short_money` 가 안 뜬다 — **코드가 까닭을 이미 적어 뒀다**

`dialogue.js:825`:
> `loop.js §③` 이 `state = c.varie ? 'money' : c.money ? 'varie' : null` 이라
> **둘 다 멀면 `null`(할 말이 없다)** 이다. **200일을 굴려도 `move_short_*` 가 0건이었다.**

⇒ ★★ **「돈도 모자라고 무늬도 못 팔았을 때」 게임이 아무 말도 안 한다.**
  그런데 **그때가 제일 도움이 필요한 순간**이다.
⇒ ★ 오늘 내 실측이 그 구간을 짚는다 — 못 나간 24판 중 **「둘 다」가 3~4판**이다.
  그 판들은 **마지막 관문에서 통째로 안내가 없다.**

**⇒ 튜토를 막나: 진행은 안 막는다**(대사가 없다고 날짜가 안 가는 것은 아니다).
  ⚠ **다만 「튜토를 확실하게 끝낸다」에는 정면으로 닿는다** — 못 나가는 사람이
  **왜 못 나가는지 들을 데가 없다.**

#### ⇒ 아침에 물을 것 (둘 다 대사 규율이라 값이 아니다)
```
① brokeTalk 을 「처음 한 번만」으로 할 것인가 (두 번째부터는 배너)
② ★ 「둘 다 모자랄 때」 할 말을 만들 것인가
   지금은 삼항이 null 로 떨어져 침묵한다. 셋째 갈래가 필요하다
```
⚠ 고칠 곳은 `loop.js §③`(삼항)과 `dialogue.js` 라 **[core] 몫**이다. **손 안 댔다.**
★ ②는 §2.9-⑫(*"삼항으로 갈래를 이으면 앞 갈래가 빈손일 때 뒷길을 안 본다"*)와 **같은 모양**이다.
  그 계율이 **다른 자리에서 다시 났다.**

### ⚠⚠ 밤샘 함정 ② — **여러 검사를 동시에 돌리면 없는 버그가 생긴다** ([growth] 오보로 드러남)

`test_buypopup` 을 **FAIL** 로 올렸다 — *"★★ 누르고 있는 동안 개수가 오른다"*.
총괄이 **「돈이 걸린 자리라 제일 급하다」**고 최우선으로 잡았다.

⇒ **오보였다.** 단독으로 다시 재니 **71개 전부 PASS.** 그 줄도 초록이다
(`3개 → 20개 (1.2초)`). 붉게 나온 것은 **죽은포트 스윕과 라이브 스윕을 동시에 돌리던 중**이었다.

⇒ ★ **부하가 걸리면 빨개지는 검사가 있다** — **「1.2초 안에 개수가 오른다」 같은 시간 재기**가 그렇다.
⇒ ★★ **그리고 밤새 여러 창이 크롬을 띄운다.** [core] 하네스 · [Char] 7해상도 촬영 ·
  [growth] 브라우저 스윕 — **서로의 결과를 오염시킨다.**
⇒ ⇒ **규칙 둘:**
```
① 붉은 것은 반드시 단독으로 다시 재라. 그 전에는 보고하지 마라
② 브라우저 검사를 돌리기 전에 다른 창이 돌리고 있는지 알려라 (또는 겹치는 시각을 피하라)
```
⚠ **오늘 밤 나온 붉은 것은 전부 다시 봐야 한다.** 특히 **시간·애니메이션·부드러움**을 재는 것.
★ 다행히 **고치기 전에 잡혔다.** 안 잡혔으면 **멀쩡한 코드를 고쳤을** 것이다.

#### [growth] 이어서 — 붉은 것 단독 재측정 · 유령 다섯의 주인 · 밥상 안 밟는 자

**「튜토를 막는 것」은 지금까지 재본 것 중 없다.**

**`[다음 날]` 은 멀쩡하다** [실측 · 직접 재봄] — `#next` `byId true · visible true · disabled false ·
text "다음 날 ▸" · rect x277 y664 101×56`(390×844 안). `test_place_confirm ①-2` 는
`.actionbar button` 을 훑는데 **버튼이 그 밖으로 옮겨간 것**이다. **자가 낡았다.**

| 붉은 것 | 단독 재측정 | 판정 |
|---|---|---|
| `test_buypopup` | **71/71 PASS** | ★ **오보였다.** 병렬 부하 탓 |
| `test_place_confirm` | 4/31 FAIL | ①-2·①-3 선택자 낡음 · ②-3·②-4 `moving:false` |
| `test_place_grid` | 1/22 FAIL | R-1 **1091/1096** — 5건이 상판 밖. 셋 다 같은 자리 `x−0.87 z−0.6` |
| `test_bagcell` | 4 FAIL | `#musunGauge` 없음(값 7 은 코드가 맞다) · A-10 `moving:false` |
| `test_questui` | PASS | |

★ **`moving:false` 가 세 검사에서 같은 값으로 나온다** — 탭 배치가 이동 상태로 안 들어간다.
그런데 **그 다음 단계는 전부 PASS**(`[확인]` 뜸 · 칸 줆 · 흐름 완료). **흐름은 온전하고 중간 상태만 다르다.**
⇒ 세 건이 아니라 **한 건**이다. 의도된 변경인지 [core] 판정 필요.

#### 유령 다섯의 주인 — **전부 [core]** [실측]

모든 커밋이 같은 계정이라 **건드린 소스로** 갈랐다.
```
test_furnishop      84d3a3f  src/game/shop·save·state   → [core]
test_monthly        105b5d7  game.html                  → [core]
test_sellpopup      73f7696  game.html                  → [core]
test_siruinfo       3fc27d8  game.html                  → [core]
test_roomview_place 41892fb  src/game/room_view.js      → [core] (3D 라 [House] 경계일 수 있음)
```

#### ★ 밥상을 안 밟는 자 — **`probe_qa_full` 은 거짓 치명을 내고 런을 멈춘다**

`#next` 를 다루는 자 **33개 중 28개**가 `mealPanel`/`mealGo` 를 모른다 [정적 · 판정 아님].
그중 `probe_qa_full` 을 **읽어 보니** 이렇다:
```
tools/probe_qa_full.mjs:494  const nr = await tap('next'); await sleep(2200); await calm();
                      :496  if (nr.ok && na.day === nb.day) {
                      :498    dead.push({btn:'next', day:nb.day});
                      :499    found('치명', '[다음 날]을 눌렀는데 날이 안 넘어감', …);
                      :500    break;          ← ★ 런이 통째로 멈춘다
tools/probe_qa_full.mjs:144  async function calm() { await clearTalk(); await clearGuide(); }
                             ⇒ 대사와 가이드만 걷는다. **밥상은 안 건드린다**
```
⇒ ★ 밥상이 실제로 뜬다면 이 자는 **① 없는 「치명」을 내고 ② 첫날에 런을 멈춘다.**
  **반쪽이 아니라 아예 못 도는 것**이다. `:563` 에도 같은 모양이 하나 더 있다(몬스테라 구간).
⚠ **밥상이 매번 뜨는지는 내가 확인 안 했다.** [core] 보고를 근거로 읽은 것이고,
  브라우저는 [core] 400일 판 때문에 껐다. **돌려서 확인해야 판정이다.**

#### ★★★ [growth] 확인 완료 — `probe_qa_full` 은 **일 1 에서 멈춘다.** 밥상이 아니라 **가계부**다

실제로 돌려서 확인했다(8963 · 한 판). **읽어서 세운 가설이 반만 맞았다.**

```
[일 1] 물=0 나이=1 회전=0 돈=1290000 재고={"bean_seed":0} 말풍선=[]
  ⚑[치명] [다음 날]을 눌렀는데 날이 안 넘어감 — 일 1 · 버튼 탭
          (덮은것={"id":null,"cls":"popcard","tag":"DIV","txt":"✕ 첫 달 가계부 살림 0~1"})
  … break …
몬스테라가 34일 안에 안 왔습니다 — 이 구간 못 쟀습니다
  ⚑[보통] 34일을 돌려도 몬스테라가 안 옴(못 쟀다) — ① 각본을 34일까지 돌림
```

**★ 막는 것은 밥상이 아니라 `#monthPanel`(첫 달 가계부)다.** `game.html:2027·2048` —
닫으려면 **`#monthGo`(「알겠습니다」)** 를 눌러야 한다. 첫 달 가계부는 **0~1일차에 뜬다**
(`:5393` — 박사님 *"첫 달 가계부는 0에서 1일째 떠야지"*).
⇒ 그래서 **곳간이 차는 날이 아니라 첫날부터** 막힌다. 내 앞선 진단(`maxGrams > 0`)은
  **밥상 갈래는 맞지만 첫 걸림돌이 아니었다.** 문 앞에 문이 하나 더 있었다.

**★★ 두 번째 보고도 유령이다.** `⚑[보통] 34일을 돌려도 몬스테라가 안 옴` 은
**진짜 버그가 아니라 「일 1 에서 못 넘어가서」 생긴 것**이다. 날짜가 1 에 머물러 있으니
34일이 갈 리가 없다. **자가 자기가 만든 증상을 버그로 보고했다.**

⇒ ★ **`qa-to-plan.md:233` 의 「뒤쪽 절반을 아무도 안 봤다」에 원인이 붙는다.**
  누가 게을러서가 아니라 **자가 첫날에 멈췄기 때문**일 수 있다.

#### 처방 — **제품이 아니라 자를 고친다.** 세 줄이면 된다

`game.html:10036` 이 그 길을 **일부러 남겨 뒀다** — *"두 번째 갈래는 코드로 `#next` 를
두 번 부르는 길(검사·재현)을 위해 그대로 남겨 둔다"*.

```js
// probe_qa_full.mjs · calm() 에 두 줄 (또는 tap('next') 뒤에)
const g1 = await rectOf('monthGo'); if (g1 && g1.vis) await tapXY(g1.x, g1.y, 70, 350);   // 가계부
const g2 = await rectOf('mealGo');  if (g2 && g2.vis) await tapXY(g2.x, g2.y, 70, 350);   // 밥상
```
⚠ **`tools/probe_qa_full.mjs` 는 [core] 소유**(추적 중 · 최근 `1556f53`)라 **고치지 않았다. 목록으로만.**
⚠ 같은 병을 앓을 후보 — `#next` 를 다루는 33개 중 **28개가 `mealPanel`/`mealGo` 를 모른다.
  `monthPanel`/`monthGo` 까지 세면 더 늘 것이다**(안 세어 봤다).

#### [growth] `#next` 를 누르는 자 23개를 갈랐다 — **[정적 · 후보. 판정 아님]**

이름만 나오는 것은 빼고 **실제로 누르는 것**만 셌다.

| 갈래 | 몇 | probe | test |
|---|---|---|---|
| ★ 둘 다 모른다(가계부·밥상) — 첫날에 멈춘다 | **18** | **16** | 2 |
| 가계부만 안다 — 곳간 차면 멈춘다 | 0 | 0 | 0 |
| 밥상만 안다 — 여전히 첫날에 멈춘다 | 4 | 1 | 3 |
| 둘 다 안다 | 1 | 0 | 1 |

- **둘 다 모르는 16 probe** — `arrival_ingame · cutting_ui · detail_pick · giftflow · growth_visible ·
  hintaim · move_audit · qa_break · qa_firstrun · qa_full · qa_perf · qa_play · qa_slowphone ·
  questvoice · seedgrow · water_walk`
- **둘 다 모르는 2 test** — `test_quiet` · `test_siruinfo`
- **밥상만** — `probe_terms` · `test_dawn6` · `test_questui` · `test_sellpopup`
- **둘 다** — `test_monthly`(가계부 전용 검사라 당연하다)

★ **`probe_` 17개가 전부 후보다.** 하나도 가계부를 모른다.
`probe_` 가 멈추면 **그 도구가 낸 문서가 흔들린다** — `qa-2026-08-08.md` · `qa-to-plan.md` 가 그 산물이다.

⚠⚠ **이 숫자를 그대로 쓰면 안 된다. 반증이 있다.**
`test_questui` 는 「밥상만 안다」에 들어갔는데 **실제로 돌려 PASS 했다.**
읽어 보니 `#next` 를 내 표가 잡은 형태로 안 누르고 `:251` 에서 `mealGo` 를 직접 누른다.
⇒ **「가계부를 모른다」가 「첫날에 멈춘다」를 뜻하지 않는 경우가 있다.**
  가계부는 **첫 달·첫 플레이 상태**에서만 뜨는데, 세이브를 넣거나 첫 플레이를 건너뛰면 아예 안 만난다.
⇒ ★ 실제로 확인된 것은 **`probe_qa_full` 하나뿐**이다(돌려서 봤다).

⇒ **먼저 볼 것은 `probe_qa_*` 일곱**이다. 그 자들이 **첫 플레이를 처음부터 굴리는** 자라
가계부를 반드시 만난다. 나머지는 상태를 넣고 시작할 수 있어 안 만날 수 있다.

⚠ 오늘 내 정적 자가 **네 번째** 반증을 만났다(관문 3회 + 이번). **후보와 판정을 갈라 적는다.**

### [plan] ⓒ 원룸 살림 대사 — ★ **내 전제가 틀렸다. 이미 있고, 배선도 깨끗하다**

내가 이렇게 올렸다: *"반지하엔 `chatDailySpend` 가 있는데 **원룸엔 없다**"*.
**재 보니 있다. 그것도 잔뜩.**

```
chat 대사 39줄 중 원룸 것 15줄 —
  Morning · Dark · Siru · Bright · Window · Empty · Lamp · NoLamp ·
  Varie · Winter · PotDark · Harvest · Money · Fenestrate · Settled
```

⇒ ★ **없는 것을 쓰러 갔다가 있는 것을 찾았다.** 오늘 하루 배운 그것을 내가 또 했다 —
  **재기 전에 「없다」고 말했다.**

## ⇒ 그래서 물음을 바꿨다 — **「있는데 안 뜨는 것이 있나」**

오늘 `move_short_*` 가 **있는데 안 떴다**(삼항이 `null` 로 떨어져서). **같은 병이 여기 있나** 봤다.

**한 겹 — `when` 이 없는 칸을 읽나** [실측]
```
chatterContext 가 내는 칸  16개
CHATTER                    42줄
없는 칸을 읽는 줄          ✅ 0
```
⇒ **`move_short_*` 같은 「영영 거짓」은 없다.**

**두 겹 — 원룸 잡담이 걸린 `movedInOnDay` 가 세이브에 실리나** [실측]
```
save.js:909  packStory → movedInOnDay: optDay(story.movedInOnDay, …)   ✅ 실린다
```
⇒ ★ **이걸 본 까닭**: `START-HERE §5` 에 *"`arrivalSlotId` 가 세이브에 안 실린다 —
  앱을 껐다 켜면 안내가 되돌아간다"* 가 있다. **같은 사고면 원룸 잡담 15줄이 통째로 안 뜬다.**
  **안 그렇다.**

## ⇒ 결론: **고칠 것이 없다. 확인한 것이 소득이다**

⚠ 다만 **셋째 겹은 못 봤다** — 칸도 있고 세이브도 되는데 **값이 그 범위에 안 가는** 경우.
  그건 **판을 돌려야** 보인다. **하네스 몫**이다.

★ 그리고 **[core] 가 침묵 건을 이미 붙였다**(`fb5298b` — *"둘 다 멀 때 게임이 침묵하던 자리를 채운다"*).
  내 두 갈래가 `shortBothCrop` · `shortBothLight` 로 들어갔다. **그 건은 닫혔다.**

#### [growth] ★ 진짜 갈림은 「누르는 방식」이었다 — 18 이 아니라 **다섯**

앞서 낸 「둘 다 모르는 18개」는 **과대였다.** 내가 든 반증 설명(*"세이브를 넣어 첫 플레이를 건너뛴다"*)도
**틀렸다** — `test_questui` 도 `localStorage.clear()` 하고 첫 플레이를 지난다. 지어낸 설명이었다.

**진짜 갈림:**
```
el.click()   요소를 직접 누른다  → 덮개를 뚫는다  → 안 막힌다
tapXY(x, y)  좌표를 누른다       → 덮개가 먼저 받는다 → ★ 막힌다
```
첫 실행 로그가 이미 그렇게 말하고 있었다 — `덮은것={"cls":"popcard","txt":"✕ 첫 달 가계부"}`.
**가계부 팝업이 [다음 날] 버튼을 물리적으로 덮는다.** `test_questui` 가 통과한 것은
가계부를 알아서가 아니라 **손가락처럼 안 눌러서**다.

| 파일 | next 누름 | 좌표 tap | clearDayGates | |
|---|---|---|---|---|
| `probe_qa_boot` | 0 | 0 | 0 | 안 걸림 |
| `probe_qa_ghostmouse` | 0 | 0 | 0 | 안 걸림 |
| `probe_qa_full` | 4 | 4 | **2** | [core] 가 고침 |
| `probe_qa_firstrun` | 2 | 2 | 0 | ★ 남음 |
| `probe_qa_break` | 2 | 2 | 0 | ★ 남음 |
| `probe_qa_play` | 1 | 1 | 0 | ★ 남음 |
| `probe_qa_perf` | 1 | 1 | 0 | ★ 남음 |
| `probe_qa_slowphone` | 1 | 1 | 0 | ★ 남음 |

`#next` 를 누르는 자 **24개 중 좌표로 누르는 것은 6개뿐**이고, 그중 하나는 이미 고쳐졌다.
⇒ **남은 다섯.** `clearDayGates()` 를 그대로 복사하면 끝난다([core] 소유).

★ 양쪽이 다 필요하다 — `el.click()` 은 **로직**(핸들러가 옳은가)을, `tapXY` 는 **사람이 닿는가**를 잰다.
**둘을 섞어 쓰면서 어느 쪽인지 안 적은 것**이 병의 뿌리다.
⇒ 검사 이름·주석에 **「손가락으로 잰다 / 로직으로 잰다」**를 적으면 다음 사람이 안 헷갈린다.

#### ⚠ `index.lock` — 내 관찰이 늘 참은 아니다

내가 *"`git commit <경로>` 는 잠금과 무관하다"* 고 적었는데 **[Plan] 이 반증을 냈다** —
*"경로 지정으로 두 번 시도했는데 막혔습니다."* 내 쪽은 두 번 됐다.
⇒ **「대개 된다」가 맞는 표현이다.** 될 때도 있고 막힐 때도 있다.
⇒ ★ 두 사람의 관찰이 갈리면 **적은 쪽(안전한 쪽)으로 적는다.** 내 두 번을 규칙으로 올리려 한 것이 성급했다.

### [plan] 퀘스트 문구가 두 자리에 뜨는 것 — **㉮(의도)로 보인다. 다만 한 겹 갈라야 한다**

[Char] 가 `quest.js:297` 한 값을 `game.html:7871`(목록)·`8878`(아래 띠) **둘이 동시에 그린다**고 넘겼다.

## ★ 코드가 까닭을 이미 적어 뒀다 — `game.html:8880`

> *"★ [가방]에 있던 진행 안내가 [식물] 맨 위로 왔다 (2026-08-06).
>  없애면 처음 하는 사람이 시트 안에서 길을 잃는다 — **아래 퀘스트 줄은 시트를 열면 가린다.**
>  ⚠ 문구를 여기서 새로 짓지 않는다. **같은 한 줄을 두 곳이 나눠 볼 뿐이다.**"*

⇒ ★ **의도다.** 그리고 까닭이 분명하다 — **아래 띠가 「시트를 열면 가려지기」 때문에**
  가려지는 동안 다른 자리가 **대신** 말하는 것이다.
⇒ **중복이 아니라 「가려지는 것을 메우는 것」**이다.

## ⚠ 다만 — **자리가 셋이다. [Char] 가 본 둘과 주석이 말하는 것이 다르다**

```
7871  qrow 를 짓는 곳            = 퀘스트 **목록**(오른쪽 위 쪽지)
8878  $('quest').textContent     = **아래 띠**
주석  "[식물] 맨 위"             = ★ **세 번째 자리**
```
⇒ 주석이 정당화하는 짝은 **「아래 띠 ↔ [식물] 맨 위」**다. **[Char] 가 본 짝이 아니다.**

## ⇒ ★★ 그래서 갈라야 하는 것은 이것이다

> **「코드가 둘 다 넣는 것」과 「화면에 둘이 동시에 보이는 것」은 다르다.**

| | 판정 |
|---|---|
| 코드가 두 자리에 같은 문구를 넣는다 | ✅ **㉮ 의도.** 가림 규칙이 있어 **한 번에 하나만 보이게** 되어 있다 |
| **화면에 둘이 동시에 보인다** | ❌ **㉯ 사고.** 그건 **가림 규칙이 안 도는 것**이다 |

⇒ **[Char] 가 본 것이 어느 쪽인지 확인해야 판정이 선다.**
  · 코드를 읽어서 *"둘 다 그린다"* 로 본 것이면 → **문제 아니다**
  · **사진에서 둘이 같이 보인 것**이면 → **가림이 안 도는 것**이고 그건 고칠 것이다

★ **오늘 이 저장소가 배운 그대로다 — 「그리는 것」과 「보이는 것」은 다르다.**
  ([Asset] 3q 썸네일 · [growth] `pointer-events:none` 이 `elementFromPoint` 에서 건너뛰이는 것)

⇒ ⚠ **나는 화면을 안 봤다. 그래서 여기서 멈춘다.** 사진 한 장이면 끝난다 —
  **퀘스트 목록을 연 상태의 화면**에서 아래 띠가 보이나.

### [plan] 판정 — **퀘스트 쪽지 ↔ 아래 띠. 사고도 의도도 아니다**

[Asset] 이 그림을 다시 봐서 셋을 확정했다: **쪽지는 퀘스트 목록** · **[식물] 탭은 화면에 없었음** ·
**시트는 닫혀 있었음**. ⇒ `8880` 주석(*"시트를 열면 가린다"*)이 **이 판에 안 걸린다.**

## ⇒ 판정: **설계는 맞다. 화면이 그 설계를 안 보여 준다**

**두 자리는 역할이 다르다:**
```
쪽지(목록)   여러 줄 + 상태(✓ ▶ 🔒) + 까닭   → 「전체 중 어디」
아래 띠      한 줄                            → 「지금 무엇」
```
★ **역할이 다르면 같은 문구를 말해도 중복이 아니다** —
  띠는 *"이것 하나"*, 쪽지는 *"이것이 일곱 중 셋째"* 다.
★ 그리고 `guide-event-quest-map §0` 이 **둘을 같은 통로(②)로 묶어 뒀다.** 원래 한 통로다.

⚠ **그런데 화면에서 그렇게 안 읽힌다.** 쪽지의 `▶` 줄과 띠의 **글자가 똑같으면**
  사람은 **「같은 걸 두 번 말한다」**로 읽는다. **역할 차이가 안 보인다.**

⇒ ★★ **그러니 ㉮(의도)도 ㉯(사고)도 아니다. 셋째다:**
> **설계는 옳고, 그 설계가 화면에 안 드러난다.**

## ⇒ 고칠 방향 — **문구를 지우지 않는다. 「같은 것임을 보이게」 한다**

지우면 **띠나 쪽지 중 하나가 쓸모를 잃는다.** 대신 **띠에 「자리」를 붙인다:**
```
지금:  물을 준다
바꾼 뒤: 물을 준다            (2/7)
```
⇒ 그러면 **띠 = 「지금 · 몇 번째」**, **쪽지 = 「목록」**이 되어 **역할이 눈에 갈린다.**
⇒ ★ **중복이 아니라 「대응」으로 읽힌다.**

★ **값을 새로 안 만들어도 된다** — `questView` 가 이미 낸다:
```js
chain: { index: …, total: FIRST_PLAY_CHAIN_IDS.length }   // 「일곱 중 셋」
```
⚠ **다만 첫 플레이 사슬에서만 나온다**(그 뒤는 `null`).
  ⇒ **본 퀘스트에서는 자리 표시가 없다.** 거기서는 **다른 표시**가 필요하다 —
    ★ 그건 화면 결정이라 **[core] 와 같이 본다.** 나는 **첫 플레이 구간까지만** 정한다.
  ⇒ 그래도 값은 있다 — **「튜토를 확실하게」의 대상이 바로 그 구간**이다.

⚠ **급하지 않다.** 진행을 안 막고, 글이 겹칠 뿐이다.

#### [growth] ② 고치기 전 증거 — **다섯 다 가계부에 닿기 전에 죽는다**

```
qa_firstrun   rc0   47초  ★ [첫 플레이 자동 시뮬레이션] 버튼이 vis:false · 0×0 → "누를 수 없었다"
                          ⇒ 아무것도 안 하고 종료코드 0
qa_break      rc2  101초  자가 제한(내가 100초로 묶음)에 걸려 죽음
qa_play       rc2  100초  같음 · 마지막 [일 0] — 하루도 못 감
qa_slowphone  rc2  101초  같음
qa_perf       rc1   17초  ★ 자체 문법 오류로 즉사 — Uncaught SyntaxError (probe_qa_perf.mjs:44)
치명 보고: 다섯 다 0건
```
★ **`clearDayGates()` 를 복사해도 이 다섯은 안 살아난다.** 가계부에 닿기 전에 각자 다른 이유로 죽는다.
실제로 가계부에 막혔던 것은 **`qa_full` 하나**이고 [core] 가 `5e9a050` 으로 고쳤다.
⚠ `break·play·slowphone` 이 **왜 안 끝나는지는 모른다** — 내가 100초로 묶어 그 앞을 못 봤다.

★★ 더 큰 것 — **`qa_firstrun` 은 아무것도 안 하고 종료코드 0** 이다. 화면엔 「누를 수 없었다」라고
적는데 코드는 0이라 `for f in …; do node $f || echo FAIL; done` 로는 **절대 안 걸린다.**
`test_balance_routes` 와 증상이 같지만 **그건 의도된 것이고 이건 아니다.**

#### [growth] 「아무것도 안 하고 0 을 내는」 probe 훑기 — **[정적 · 후보. 판정 아님]**

⚠ **이 자를 두 번 고쳤다.** 첫 판은 `process.exit(2)` 가 있으면 「관문 있다」로 뺐는데,
그게 **자가 제한(watchdog) 안의 상용구**라 거의 모든 probe 가 갖고 있다.
⇒ **확인된 사례(`qa_firstrun`)를 내 목록이 못 잡았다.** 그래서 watchdog 안의 exit 를 빼고 다시 셌다.
⇒ ★ **자를 만들면 「아는 정답」이 그 자에 걸리는지부터 보라.** 안 그러면 조용히 틀린 목록이 나온다.

```
probe 115 개 중 79 개가 「0 이 아닌 값으로 죽을 길이 없다」
그중 11 개는 「못 했다」를 찍으면서 0 을 낸다 — 더 위험하다:
  char_moni(2) · crop_cases(1) · cutleaf(1) · dayhold(1) · guide_walk(1) · hintgone(1) ·
  leafgrade_wire(2) · midcommon_browser(1) · midreveal(1) · move_audit(2) · varie_lineage(5)
```
⚠ **probe 는 사람이 읽는 도구라 0 을 내는 것이 설계일 수 있다.** 문제는 **스크립트로 돌릴 때**다.
⇒ 갈래를 나눌 값이 있다 — **「사람이 읽는 자」와 「기계가 읽는 자」**. 지금은 이름이 같다.

### [house] ⓐ 임시 도구 목록 — ⛔ 밤에 안 지운다. 아침에 한 번에

밤새 `tools/_probe_*.mjs` 를 **스물여섯 개** 만들었다(전부 `_` 로 시작 = 임시, 커밋 안 함).
⛔ **밤에는 안 지운다**(되돌릴 수 없는 것 금지). 아침에 한 번에 처리할 목록만 남긴다.

**정식으로 승격한 것 둘**(커밋됨 — 지우지 말 것)
- `tools/probe_lampfit.mjs` — 집게등이 찍은 데 안 붙는 것을 잰다. **core 가 고친 뒤 다시 돌릴 자**
- `tools/probe_furn_clash.mjs` — 가구가 서로 박혔나(사람이 읽는 판). 검사는 `test_furn_clash.mjs`

**검사로 남긴 것 셋**(커밋됨)
- `tools/test_measured_fresh.mjs` — 낡은 표에 켜지는 경고등
- `tools/test_furn_clash.mjs` — 가구 겹침
- `test_floorlight ⑤` · `test_lampmove 3부` — 자리 이름 안정 · 화면의 그 손짓

**남길지 말지 아침에 정할 것**
- `_probe_tiercell` `_probe_uidstable` `_probe_clash` — 위 검사들과 겹친다. **지워도 된다**
- `_probe_under2` `_probe_desk2` `_probe_clipworth2` — 배치안을 잰 자. **아침 결정에 쓰일 수 있다**
- 나머지 스물 — 한 번 쓰고 답이 나온 것. **지워도 된다**

⚠ 지울 때 `_probe_lampfit.mjs`(밑줄 있는 옛것)와 `probe_lampfit.mjs`(승격본)를 **헷갈리지 말 것.**

---

## §10.9 자리 조도 — 바닥을 잴 자가 없었다 (2026-08-23 [growth])

### 먼저 고칠 것 — 내가 novice 값을 real 이라 옮겼다
`0.58` 은 `banjiha-desk:0` 의 **novice·등0·7일평균**이다. real 등0 의 같은 자리는
**0.26**(겨울 0.07)이다. [Plan] 의 「무순 다섯」은 원래 novice 표였고, 내가 그것을
real 이라 되읊어 근거가 흔들린 것처럼 보이게 했다. 잘못은 이쪽이다.

### 바닥은 표에 없었다. 그리고 없는 것이 0 으로 보인다
반지하 프로필의 자리는 이름 붙은 15칸뿐이고 바닥 자리가 없다. 헤드리스
`room_profile` 은 자유 좌표(`free:…`)를 모르므로 0 을 낸다(room_profile.js:164).

그 0 은 「어둡다」가 아니라 「모른다」다. 그런데 콩나물 최상 대역은
`minDli 0 · maxDli 0.3` 으로 **하한이 없다**(first_play.js:204).
⇒ **「모른다」가 「최상」으로 읽힌다.** 무순은 반대로 「웃자라 밍밍」이 된다.
⇒ 바닥은 라이브 엔진의 `dliAt({x,y,z})` 로 재야 한다. 슬롯 판정과 같은 함수다.

### 잰 값

바닥 221점 · 0.3m 격자 · y=0.10 · 등0 · **하루 DLI**

| 계절·날씨 | 폭 |
|---|---|
| 여름 맑음 | 0.00 ~ 0.32 |
| 봄 맑음 | 0.00 ~ 0.24 |
| 가을 맑음 | 0.00 ~ 0.21 |
| 겨울 맑음 | 0.00 ~ 0.12 |
| 흐림 | 각각 0.08 / 0.06 / 0.05 / 0.03 |

0.30 을 넘는 점 5/221 · 0.35 이상인 점 **0/221**.
⇒ 바닥은 사철 콩나물 자리이고, 무순 자리가 **아니다**.

이름 붙은 15칸 · 400일 · **7일평균**

| 자리 | novice등0 | real등0 (연평균/겨울) | real등1 |
|---|---|---|---|
| sill:0 | 3.68 | 1.63 / 0.44 | 3.97 / 2.78 |
| desk:0 | 0.58 | 0.26 / 0.07 | 0.54 / 0.35 |
| etagere:8 | 0.48 | 0.21 / 0.05 | 0.65 / 0.49 |

무순 0.35 를 겨울에도 넘는 자리 — novice 15/15 · real등0 **1/15** · real등1 **4/15**.

### 눈금이 셋이다. 섞으면 안 된다
- 몬스테라 관문 — **7일 이동평균**
- 작물 등급 — 자라는 **5일간의 하루 조도 평균**(`dliHist` 평균, first_play.js:2442)
- 프로필 `peak_summer` — 맑은 여름 **하루값**. `avg7 = peak × 0.643`

작물 문턱과 자리 표를 맞댈 때 이 셋을 섞으면 5배 넘게 틀린다. 오늘 밤에 두 번 그랬다.

### 남긴 자
- `tools/probe_floor_dli.mjs` — 바닥. 라이브. 서버 필요(`python tools/serve.py 8971`)
- `tools/probe_slot_year.mjs` — 15칸 연중. 헤드리스. 바닥은 못 잰다고 머리에 적어 뒀다

커밋 `635e64e` · main

## §10.10 하한 없는 등급 세기 (2026-08-23 [growth] · 총괄 숙제)

### 함정의 꼴을 먼저 좁혔다
「하한이 없다」만으로는 함정이 아니다. 제일 아래 칸은 원래 하한이 없어야 한다.
**함정은 제일 좋은 칸에 하한이 없을 때**다. 그건 「적을수록 좋은 축」에서만 나고,
이 게임에 그 축은 콩나물 하나다.

### 하한이 0 인 칸 넷 — 그중 함정은 하나

| 자리 | 값 | 판정 |
|---|---|---|
| first_play.js:204 콩나물 `crisp_white` | minDli 0 · maxDli 0.3 | ⚠ **최상. 함정** |
| first_play.js:205 콩나물 `slightly_green` | minDli 0 · maxDli 1.0 | 앞칸이 먼저 먹어 안 닿는다 |
| first_play.js:206 콩나물 `green_bitter` | minDli 0 · Infinity | 최하 폴백. 옳다 |
| first_play.js:280 무순 `leggy_bland` | minDli 0 · Infinity | 최하. 0 이면 웃자라 밍밍. 옳다 |

`data/balance` 전체를 훑었다(min/max 꼴 등급 배열). 한 건도 없다. 작물 품질표는 코드 한 곳뿐이다.

### 함정이 「품질 이름」에서 안 멈춘다 — 돈까지 간다
그램 표가 별도 표가 아니라 그 품질의 끼니에 얹혀 있다(first_play.js:145 —
"새 밝기 표를 만들지 않았다 — 판정은 여전히 cropQualityOf 하나다"). 500g / 350g / 200g.
⇒ 「모름 = 0」은 최상 품질이자 최대 수확 500g 이자 최대 돈이다. 최하와 2.5배.

### ★ 울타리 — 이건 게임의 병이 아니라 검사의 병이다
브라우저는 `light_adapter` 라 바닥을 안다. 헤드리스 `room_profile` 만 0 을 낸다.
⇒ 게임은 멀쩡한데 검사만 틀린 초록을 낸다. **그래서 사람이 볼 계기가 없다.**
⇒ 브라우저로 돈 잰 판은 안 걸린다. **헤드리스로 돈을 잰 것만** 다시 봐야 한다.

### 옳게 만든 본보기가 이미 저장소 안에 있다
`loop.js:145 growthSpeedOf` — `tuning` / `default` / `unknown` / `unsupported`.
「모름」이 값이 아니라 **이름**으로 나온다. 그래서 검사가 볼 수 있다.
빛 대역도 같다 — `contract.js:30` 에 `'unknown'` 갈래가 따로 있다.
`cropQualityOf` 만 그 갈래가 없다. `avgDli` 가 `Number.isFinite` 아니면 조용히 0 이 된다(:407).

### 안전 확인한 것 (모름이 좋은 칸으로 안 간다)
- `varieLightStepOfBand(null)` → null → `varieChanceFromLight` null. 모름 = 보상 없음
- `waterIntervalOf(null)` → `WATER_INTERVAL_DEFAULT`. 중립이고 이름으로 드러난다
- `judgeDLI(0)` → `'die'`. 최악. 시끄럽다

### ⏸ 지금은 병이 아니지만 적어 두는 것 하나
`shop.js:1282` — `plainId: (grades.find(g => !g.varie) || grades[0]).id`

지금 `varie_grades.json` 은 `[0] plain(varie=false)` 이라 `find` 가 늘 맞고 폴백이 안 돈다.
⚠ **정본에서 plain 이 빠지는 날** 조용히 `grades[0]`(그때의 첫 칸 = 무늬 등급)을 쓴다.
「민무늬 id」 자리에 무늬 등급이 앉는 것이라, 값이 아니라 **이름이 어긋난다.**
⇒ 고칠 때는 폴백을 없애지 말고 **소리를 내게** 하라 — 위 `unknown` 갈래와 같은 꼴.
⇒ 지금 안 고친다(총괄 판단). **정본에서 plain 을 뺄 사람이 이 줄을 보면 된다.**
