# house → 총괄 · 2026-08-23

바로잡은 것 둘, 멈춘 것 넷. 멈춘 넷은 전부 **자리 좌표나 등 개수를 움직여야** 하고,
그건 곧 DLI 가 움직인다는 뜻이라 밸런스에 닿는다. 재기만 하고 올린다.

---

## 1. skyViewK 는 결백하다 — 자가 엉뚱한 데를 가리켰다  [고쳤다 · 387e7a9]

검사 ①-2 가 「skyViewK: 1.0 이 값을 움직였습니다」로 붉었다. **아니다.**

    키 삭제한 방 vs 1.00 을 적은 방 → 반지하 15칸 한 톨도 안 다르다

`house.js:462` 이 `Number.isFinite(skyViewK) ? skyViewK : 1` 이므로 당연한 결과다.
`house_rooms.json §banjiha.skyViewK_note` 에 적혀 있던 말이 맞았다.

붉었던 진짜 까닭 둘 —

  · 그 검사는 **얼린 표**(`BANJIHA_FROZEN`)와 견주었다. 곧 ① 과 같은 것을 재고 있었고,
    표가 낡기만 해도 skyViewK 에게 **없는 죄**가 씌워졌다.
  · `makeEngine` 은 값을 **넣을** 수만 있고 지울 수 없었다. 곧 이 검사는
    제 이름에 적힌 것("안 적은 방과 같다")을 **애초에 잴 수가 없었다.**

이제 두 벌을 직접 견준다. `makeEngine({room: null})` 이 키를 지운다.

> ⚠ 이 사고의 값은 「검사가 틀렸다」가 아니다. **자가 안 틀린 곳을 가리키면
> 못 찾는 것보다 나쁘다** — 안 틀린 데를 고치게 된다. 하마터면 skyViewK 를
> 만질 뻔했다.

## 2. 창턱 measured — 베끼지 않고 다시 쟀다  [고쳤다 · 387e7a9]

날짜·roomRev 는 08-02 인데 어두운 자리 좌표만 누군가 고쳐 두었다. **반만 낡은 표**다.
다른 두 파일에 이미 있던 3.68 을 베끼지 않고 직접 쟀다(등 0개·맑음·여름).

| 줄 | 있던 값 | 잰 값 | 왜 |
|---|---|---|---|
| `bright_slot.point.z` | −1.95 | **−1.85** | b725b8c 가 받침을 방 쪽으로 0.20 밀었다 |
| `peak_summer` | 3.77 | **3.68** | 위와 같음 |
| `avg7_summer` | 2.42 | **2.37** | 3.68 × 0.643 |
| `maxPotD` | 0.21 | **0.27** | 이 줄도 낡아 있었다 (검사가 안 보던 줄) |
| `slots.count` | 14 | **15** | 23521a1 협탁 |
| `sampled_peak_ratio_pct` | 101 | **98** | 3.68 ÷ 3.74 |

`space` 쪽은 안 건드렸다 — 가구를 빼고 재므로 위 커밋들에 안 흔들린다.
`measuredAt 2026-08-23` · `roomRev d0bc365`.

---

# 멈춘 넷 — 답을 주십시오

## ★ A. 얼린 표를 다시 얼려야 하는데, 한 번에 얼리려면 B 가 먼저다  (검사 ①)

`BANJIHA_FROZEN` 이 **두 가지로** 낡았다. 둘 다 다른 창이 한 일이고, 둘 다 표를 안 얼렸다.

  · `ca3f8f8` 협탁 2×2 · 책상 다섯 열 → **등2 칸** desk:0 1.63→1.77 · desk:1 1.32→1.11 ·
    nightstand:0 0.84→0.96
  · `d0bc365` 반지하 **셋째 등**(거치형) → 검사의 둘째 칸이 「등 전부」라 등2→등3 이 되었다.
    창턱 6.06 → **6.17**

지금 다시 얼리면 B 를 고칠 때 **또 얼려야 한다.** B 가 정해지면 한 번에 얼린다.

## ★★ B. 자리가 «칸» 밖에 앉아 있다 — 겨눠도 안 붙는다  (검사 ①-1)

CORE 가 정본을 뒤집었다(`room_view.js:4255` — *"이제 **칸이 정본이고 놓는 자리가 칸
한가운데**다"*). 커서는 칸 한가운데에만 내려앉는다. 그런데 `furniture_pastel.js §tierSlots`
의 `n===1` 갈래는 **옛 정본**(0.125 걸음) 시절 그대로 «상판 한가운데»에 자리를 낸다.

협탁이 `ca3f8f8` 로 0.50×0.50 = **2×2 칸**이 되면서 상판 한가운데가 **칸 교차점**이 되었다.

    자리 ↔ 가장 가까운 칸 한가운데 = 0.1768m   (SLOT_GOVERN_R 0.04)
    ⇒ 어느 칸을 겨눠도 이 자리로 안 붙는다. **영영 못 쓰는 추천 자리다.**

파급 범위(상판=발자국인 «상자» 가구만 셌다. 사다리·계단형은 이 자로 재면 거짓 실패다):

    상자가구 34칸 중 **16칸**이 0.04 밖 — 4개 방
      협탁(0.50×0.50, 칸 2×2)   반지하·원룸·투룸·아파트  각 1칸  0.1768m
      shelf_low(1.00×0.30, 4×1) 투룸·아파트              각 6칸  0.065~0.125m

대안을 재 뒀다. 협탁에 **네 칸(2×2 한가운데)**을 주면 —

    peak 0.44 · 0.43 · 0.32 · 0.32   넷 다 겨눠서 닿는다
    지금은 못 닿는 한 칸(0.29)뿐이다

박사님이 협탁을 2×2 로 하신 뜻("2*2 크기로 해서 주던지")에도 이쪽이 맞다.
⚠ 다만 `tierSlots` 는 **한 축짜리**다. 진짜 2×2 를 내려면 두 축 도우미가 새로 필요하고,
반지하 자리가 15 → 18칸이 된다. **코드·자리수·DLI 가 같이 움직인다 — 그래서 멈춘다.**

물음: ① 협탁을 네 칸으로 여는가, 아니면 한 칸을 칸 한가운데로 옮기는가?
      ② `shelf_low` 12칸도 같이 보는가, 나중인가?

## C. 원룸에 거치등이 없다  (검사 oneroom_room ③)

    반지하 [bar, clip, **stand**] · 원룸 [bar, clip]

`d0bc365` 의 셋째 등이 반지하에만 갔다. 그런데 같은 검사 ⑤ 가 이미
**「등 1개에서 원룸 최고 10.68 이 반지하 최고 6.02 를 넘었다 — 과합니다」**로 붉다.
여기에 셋째 등을 더 얹으면 더 과해진다. 등 하나 넣는 일이 아니라 **원룸 밝기 재조정**이다.
plan 몫으로 본다.

## D. 정적 프로필과 house_rooms.json 의 roomRev 가 갈렸다  [손 안 댐]

    house_rooms.json §banjiha.roomRev   d0bc365 2026-08-17   ← 방금 내가 맞췄다
    room_profile.banjiha.json .roomRev  a9e2ec4 2026-08-02   ← 그대로

「정적 프로필은 아직 쓰지 마십시오. 지뢰가 걸려 있다」는 지시에 따라 안 건드렸다.
`test_banjiha_profile` 은 PASS 인데, 그 검사가 **프로필 쪽 roomRev 를 그대로 읽어 찍는다.**
곧 지금 초록 글씨가 낡은 날짜를 말하고 있다.

## E. 온실 38% — 안 건드렸다 (박사님 답 대기)

---

## 덧 — 검사 하나가 더 낡았다 (안 고쳤다)

`test_oneroom_room ⑥` 이 「반지하 **14칸**」을 못박고 있다. 23521a1 이후로 15칸이다.
A 를 다시 얼릴 때 같이 본다.
