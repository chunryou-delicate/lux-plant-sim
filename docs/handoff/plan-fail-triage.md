# 2026-08-23 · FAIL 12개 가르기 — **고치지 않았다. 가르기만 했다**

> 총괄 승인. 순수 모듈 검사 51개 중 FAIL 12개(+`test_elec` 은 총괄이 이미 고침)를 갈랐다.
> ⚠ **한 줄도 안 고쳤다.** 고칠 것은 주인 창에 넘긴다.
> ⚠ 판정 근거는 전부 **코드·데이터 원문**이다. 「아마」로 적은 줄은 **「확인 필요」**라 표시했다.

## 0. 갈래 — 총괄이 준 셋에 **넷째를 더했다**

| | 갈래 | 뜻 | 손대는 곳 |
|---|---|---|---|
| ㉠ | **자가 낡음** | 계약이 바뀌었는데 자만 옛것 | 만든 창 |
| ㉡ | **진짜 회귀** | 코드가 계약을 깼다 | 계통 창 |
| ㉢ | **자가 못 갖춤** | 픽스처가 지금 규칙을 못 넘는다 | 하네스 |
| ㉣ | ★ **박사님 결정을 자가 못 따라감** | **고치면 결정이 되돌아간다** | ⚠ **사람 확인 먼저** |

★ 넷째가 왜 필요한가 — ㉠ 과 겉이 똑같은데 **고치는 방향이 반대**다.
  ㉠ 은 자를 새 계약에 맞추면 되고, ㉣ 는 **자를 맞추면 박사님이 정하신 것이 지워진다.**
  오늘 `test_elec` 이 그 함정이었다(붉은 줄을 보고 코드에서 거치등 36W 를 빼면 초록이 된다).

---

## 1. ㉣ **박사님 결정을 자가 못 따라감 — 7건. 여기를 먼저 보라**

### ⓐ 몬스테라는 이제 **가방으로 온다** — 4건이 한 뿌리다

`first_play.js:3309` 박사님 원문(2026-08-17):
> *"처음에 몬스테라 주는 거 **인벤에 줘서** 드래그해서 배치하거나 누르면 좌측이나 우측 바닥에 배치되도록 해"*
`state.js:1596` 도 같은 날:
> *"몬스테라 주는 거 **인벤으로 안 들어오고** 또 바로 설치되는데?"*

⇒ 도착 화분은 `{ slotId: null, at: null }` 이다. **책상에 안 앉는다.**

| 검사 | 무엇이라 찍히나 |
|---|---|
| `crop_seat F` | 「몬스테라가 책상이 아니라 **null** 에 도착했습니다」 |
| `crop_seat F-2` | 「책상이 없는 방인데 가장 어두운 자리가 아닙니다 — **null**」 |
| `fastforward ⑵` | 「몬스테라는 어두운 자리에 도착한다 — **null !== 'arrival'**」 |
| `fastforward ⑵-b` | 같은 뿌리(`maxDays` vs `blocked`) |

⚠⚠ **그리고 문서 셋이 같이 낡았다 — 이게 더 크다:**

| 문서 | 적혀 있는 것 |
|---|---|
| `START-HERE §6` | *"몬스테라 도착 · 유효 45 · 잎 1장 · **책상(`banjiha-desk:0`)**"* |
| `guide-event-quest-map §2` | *"**책상**(`banjiha-desk:0`) · 유효 45일 · 잎 1장"* · 손가락 3 *"몬스테라가 **도착 자리 그대로**"* |
| **`plan-to-asset-thisrun.md §1`**(★ 내가 오늘 쓴 것) | *"`desk` — **몬스테라가 도착하는 자리**"* |

⇒ **내 것부터 고쳤다**(아래 §5). 앞의 둘은 소유 창에 넘긴다.

### ⓑ `oneroom H` — 문턱 3.0 이 **자에 박혀 있다**

```js
tools/test_oneroom.mjs:364   assert.equal(g.min, 3.0);     ← 실제 2.7
tools/test_oneroom.mjs:52    plantMinDli: 3.0              ← 픽스처에도
```
박사님 2026-08-17 확정으로 **min 3.0 → 2.7** 이다(`light_thresholds.json §monstera_deliciosa`).
⇒ **이 자를 「고쳐서」 코드를 3.0 으로 되돌리면 그 결정이 지워진다.**

### ⓒ `test_tutorial A` — 첫 플레이 중 시계 정지

`tutorial.js:583` 이 지운 줄을 그대로 적어 두었다:
> *"⚠ 여기 있던 것: `if (!firstPlayDone) return { skipped: '첫 플레이 진행 중' };`"*
> 박사님: *"월세가 안 나가 첫째 날."* → *"**살림 시계 첫날부터.**"*

### ⓓ `first_play_attacks` — 수확 g 이 **확정 눈금의 정확히 2배**다 ⚠ 확인 필요

```
기대 [500, 350, 200]      ← 박사님 "콩나물은 200-500" (2026-08-18)
실제 [1000, 700, 400]     ← 정확히 2배
```
★ **원과 g 이 10배로 맞물리는 것은 여전히 성립한다** — 바로 위 파생 assert(`cropCycleSavedWon/10`)는
  통과한다. 즉 **둘이 같이 2배가 됐다.**
⇒ 박사님이 눈금을 또 넓히셨는지(㉣), 아니면 어딘가 2배가 붙은 것인지(㉡) **못 갈랐다.**
  ⚠ 하루 식비 절감이 통째로 2배가 되는 자리라 **아무나 고치면 안 된다.** 사람 확인 필요.

---

## 2. ㉡ **진짜 회귀 — 6건. 코드를 고쳐야 한다**

### ★★ ⓐ `banjiha_routes G-3` · `G-4` — **기획 규칙 둘이 아무 일도 안 하고 있다**

```
G-3 → ★확정 무늬가 없어도 같은 비율로 나갑니다(12/20) — 이 규칙이 아무 일도 안 하고 있습니다
G-4 → ★삽수를 안 팔아도 같은 비율로 나갑니다 — 꾸준수입이 아무 일도 안 하고 있습니다
```
**「이사 = 돈 + 무늬 판 적」은 확정 규칙**이다(`START-HERE §6` · `shop.md §4` · 이 창 정본).
⇒ 그 게이트가 **실제로는 안 걸리고 있다.** 자가 낡은 게 아니라 **규칙이 빠진 것**이다.
⇒ ★ **이 판의 마지막 관문이 지금 열려 있다.** 12개 중 제일 무겁다고 본다.
⚠ 그리고 `crop_seat E`·`E-2` 는 **반대로** 그 게이트에 막혀 있다(§3) — **한 판에서 둘이 서로 어긋난다.**
  둘을 같이 봐야 한다.

### ⓑ `floorlight ①-1` — 자리가 칸 한가운데를 벗어났다 · **자가 고칠 곳까지 짚었다**
```
→ 최대 0.1768m @ banjiha-nightstand:0   (SLOT_GOVERN_R 0.04 초과)
고칠 곳: src/render3d/furniture_pastel.js §tierSlots (여백은 칸 반쪽이어야 한다)
```
⇒ 넘으면 **그 자리 네모를 겨눠도 자리로 안 붙는다.** 협탁은 2026-08-17 에 크기가 바뀐 가구다. **[House]**

### ⓒ `floorlight ①-2` — `skyViewK: 1.0` 이 값을 움직인다
`house_rooms.json §banjiha.skyViewK_note` 는 *"**재서 확인**: 이 줄을 넣기 전과 후 반지하 14칸
DLI 가 **한 톨도 안 바뀐다**"* 라고 적어 두었다. **자는 바뀐다고 말한다.** 둘 중 하나가 틀렸다. **[House]**

### ⓓ `oneroom_room ③` — 원룸에 거치등이 없다
`반지하 [bar, clip, stand]` vs `원룸 [bar, clip]`. 2026-08-17 셋째 등이 **반지하에만** 들어갔다. **[House]**

### ⓔ `dialogue_coverage ⑵` — `move_short_money` 가 **어느 경로에서도 안 난다**
「돈이 모자랄 때」 대사가 안 뜬다. ⚠ 위 G-3 과 같은 계통(이사 게이트)일 수 있다. **[core]**

### ⓕ `dialogue_coverage ⑶ C` — `brokeTalk` 이 연달아 두 번 난다
한 번뿐이어야 할 대사가 다시 난다. **[core]**

---

## 3. ㉢ **자가 못 갖춤 — 2건**

`crop_seat E` · `E-2` → 「**[튜토] 무늬 삽수를 아직 못 팔았습니다**」
픽스처가 이사 게이트를 못 넘어 **이사 장면에 도달조차 못 한다.** 재려는 것(거둔 시루가 새 방에 서나)은
멀쩡할 수 있다. **픽스처에 「무늬 삽수 판 적」을 심어야 잰다.**
⚠ 위 §2-ⓐ 와 같이 봐야 한다 — **자는 게이트에 막히는데 실제 판은 안 막힌다.**

---

## 4. ㉠ **자가 낡음 — 5건. 자를 고치면 된다**

| 검사 | 자가 무엇을 기다리나 | 지금 |
|---|---|---|
| `oneroom C-2` | 화분이 새 방 자리로 **옮겨지기**를 | 2026-08-17 `at`/`onUid` 규칙이 생겼다. 좌표가 살아 있고 새 방 안이면 **그 자리를 지킨다**(`state.js:1613`). 픽스처 화분이 `at` 만 있고 `onUid` 가 없어 「안 사라졌다」로 읽힌다 |
| `free_place F` | 좌표 칸에 `x·y·z` 만 | 지금은 `occIdx`·`onUid` 가 같이 실린다 |
| `free_place L` | 가구가 **옛 좌표**에 | 2026-08-16~17 에 침대·책상·서랍장·협탁이 다 옮겨졌다 |
| `oneroom_room ⑥` | 반지하 **14칸** | **15칸**이다 |
| `floorlight ①` | 창턱 등2 = **6.06** (얼린 기준선) | **6.17** ⚠ 거치등 추가 파급으로 **보이나 안 쟀다 — 확인 필요** |

★ 아래 셋은 **한 뿌리로 보인다** — 2026-08-17 거치등(36W)이 들어오면서 반지하 슬롯이 늘고 값이 밀렸다.
  `oneroom_room ⑥`(14→15) · `floorlight ①`(6.06→6.17) · (미추적 `test_room_map` 「반지하 슬롯 14칸」)
  ⚠ **기준선을 새 값으로 다시 얼리기 전에 「이 변화가 옳은가」를 사람이 봐야 한다.**
    회귀 검사의 기준선을 말없이 갱신하는 것은 **검사를 끄는 것과 같다.**

## 4-2. 아직 못 가른 것 — 3건. **모른다고 적는다**

| 검사 | 찍힌 것 | 왜 못 갈랐나 |
|---|---|---|
| `dialogue_coverage ⑸` | 식물신 대사가 **6줄**(3줄이어야) | ★ `portrait:false`·이름 `?` **계약은 지켜지고 있다**(실측). 줄이 는 것이 의도인지 모른다 |
| `banjiha_routes G-2c` | 이사한 판 96개 중 **3개**가 여름에 끝남(가장 빠른 것 튜토 354일) | 합격선(96 중 몇 개까지 봐주나)이 어디에도 없다. **밸런스 판단** — 내 몫일 수 있다 |
| `pantrysale` · `pots E-6` | `7000 !== 0` · `0.05 / 0.075` | 안 팠다 |

---

## 5. 내가 고친 것 — **하나뿐이다**

`plan-to-asset-thisrun.md` 의 *"`desk` — 몬스테라가 도착하는 자리"* 를 고쳤다. **내 파일이고 오늘 내가 틀리게 쓴 것**이다.
그 밖에는 **한 줄도 안 건드렸다.**

## 6. 넘기는 곳

| 갈래 | 건수 | 어디로 |
|---|---|---|
| ㉣ 박사님 결정 | **7** | ⚠ **사람 확인 먼저.** 자를 고치면 결정이 지워진다 |
| ㉡ 진짜 회귀 | 6 | [House] 3 · [core] 2 · **[core]+plan 공동 2**(이사 게이트) |
| ㉢ 자가 못 갖춤 | 2 | 하네스(`crop_seat` 만든 창) |
| ㉠ 자가 낡음 | 5 | 만든 창 — 단 기준선 셋은 **얼리기 전에 사람 확인** |
| 못 가름 | 3 | — |

---

# 7. `banjiha_routes G-2c` — **합격선을 정할 일이 아니었다. 재는 식이 틀렸다**

> 총괄이 *"합격선이 어디에도 없다 — plan 몫"* 으로 넘긴 건.
> **숫자를 정하러 갔다가 재는 식이 틀린 것을 찾았다.** 그래서 숫자를 안 정한다.

## 7-1 자가 무엇이라 말하나

```
FAIL G-2c → ★이사한 판 96개 중 3개가 아직 여름에 끝납니다 (가장 빠른 것 튜토 354일)
             — 그 판은 계절 전환도 식물등 해금도 못 봅니다
```
식은 `assert.equal(summer.length, 0)` — **여유 0**이다.
뜻은 이렇게 적혀 있다(2026-08-03):
> *"여기가 빨개지면 **가을·식물등·겨울 콘텐츠를 아무도 못 보는** 상태로 돌아간 것이다."*

## 7-2 ★ 그런데 그 세 판은 **다 보고 나갔다**

계절은 **90일씩 · 한 바퀴 360일**이고 게임 0일은 연중 **135일**(여름 45일째)이다
(`weather.js §DAYS_PER_SEASON` · `tutorial.js §yearDay0Of`).

| 튜토일 | `yearDay` | 계절 |
|---|---|---|
| 57 | 192 | 가을 |
| 189 | 324 | 겨울 |
| 314 | 89 | 봄 |
| **315** | 90 | **여름** ← 여기서 되돌아온다 |
| **354** | **129** | **여름** (두 번째 · 39일째) |

⇒ **세 판은 전부 튜토 354일에 끝났다.** 가을(45)·겨울(135)·봄(225)을 **다 지나고** 두 번째 여름이다.
⇒ 자가 찍은 내역이 스스로 그것을 증명한다 — **`확정무늬 45일`**. 확정 무늬는 **가을 게이트**라
  가을에 닿아야 온다(`varieGrantOpensDay`). **가을을 봤다는 증거가 같은 줄에 있다.**

## 7-3 ⇒ 판정: **㉠ 자가 낡음.** 합격선이 아니다

`r.season === 'summer'` 를 「계절을 못 봤다」로 읽는 등식은 **판이 튜토 315일 안에 끝날 때만 참**이다.
2026-08-03 에는 그게 참이었다(그때 판은 57~189일에 끝났다). **지금은 꼬리가 315일을 넘는다.**
⇒ **한 바퀴 돌아온 판을 「한 바퀴도 못 돈 판」으로 읽고 있다.** 정반대다.

### 고칠 식 — **새 숫자를 안 만든다**

```js
// 지금: 끝날 때 계절이 여름인가
const summer = all.filter(r => r.season === 'summer');
// 이렇게: 가을에 닿기 전에 끝났나 — 바로 아래 줄이 이미 쓰는 값이다
const OPEN = varieGrantOpensDay(A.ok[0].S.tutorial);   // = 45 (가을 진입)
const tooEarly = all.filter(r => r.lastDay < OPEN);
```
★ `OPEN` 은 **이 검사가 바로 다음 줄에서 이미 쓰는 값**이다. 문턱을 새로 짓지 않는다.
★ 그리고 **여유 0 을 그대로 둔다.** 「가을 전에 끝난 판」은 한 판도 있으면 안 되는 것이 맞다 —
  느슨하게 할 이유가 없다. **내가 정할 숫자가 없다는 것이 이 건의 결론이다.**

## 7-4 ⚠ 다만 **따로 볼 것 하나** — 꼬리가 354일까지 간다

G-2c 가 잘못 잡았을 뿐, **그 세 판 자체는 눈에 띈다.**

| | |
|---|---|
| 중앙값 | A 89일 · C 175일 |
| 이 세 판 | **튜토 354일** — 중앙값의 두 배 |
| 그 판의 모습 | 잎 7장(무늬 2) · 삽수 **2개**만 팜 · 그루로 3,762,500원 |

⇒ **삽수를 거의 안 팔고 모주를 키워서 통째로 판 길**이다. 되긴 되는데 **두 배 넘게 걸린다.**
⇒ 이게 **의도된 느린 길인지, 아니면 막다른 길에 가까운지**는 밸런스 판단이고,
  **재현이 그 길을 제대로 못 잰다**고 이 파일 자신이 §1320 에 적어 두었다:
  > *"모주 하나가 이미 2,133,333원이라 그 문턱이 삽수가 자라기 전에 먼저 걸린다. (…)
  >  **재는 자를 고쳐야 재진다.** 고치는 곳은 §④ 한 줄이고 (…) **plan 몫이다.**"*

⇒ **그것이 진짜 내 몫이다.** G-2c 합격선이 아니라 **§④ 의 「사람이 어떻게 파나」**다.
  아직 안 정했다 — 정하려면 「모주를 통째로 파는 것을 정상 경로로 볼 것인가」부터 정해야 하고,
  그건 박사님 판단이 필요해 보인다. **근거만 모아 올린다.**

---

# 8. **못 가른 셋을 마저 갈랐다 — 셋 다 ㉣ 이었다** (2026-08-23)

## 8-1 ★★ `first_play_attacks` · `pots E-6` — **한 뿌리다. 그리고 박사님 결정이다**

앞에서 *"수확 g 이 정확히 2배 — 못 갈랐다"* 로 두었다. **재서 찾았다.**

```
first_play.js:680   const harvestMultOf = (rules) => …
first_play.js:679   CROP_HARVEST_MULT = { banjiha: 2.0, oneroom: 1.3 }
first_play.js:694   base × cropTiredMultiplier × harvestMultOf(rules)   ← 여기서 2배가 붙는다
```

**박사님 원문**(`first_play.js:665`):
> *"**2배로 하고 난이도 올라갈수록 줄이는 건 어때?**"* · *"어쨌든 **게임오버는 되면 안 되니까**"*

까닭도 적혀 있다 — 체력 5 로는 **「열심히 하면 되는 구간」이 아예 없었다.**

실측:
```
콩나물  1끼 4,000 / 2끼  7,000 / 3끼 10,000     savedWonPerCycle 3,500
무순    1끼 2,000 / 2끼  5,000 / 3끼  8,000     savedWonPerCycle 2,500
```
⇒ 두 자가 붙들고 있는 것은 **배수가 붙기 전 값**이다. `pots E-6` 의 `500/5000`·`600/4000` 도,
  `first_play_attacks` 의 `[500,350,200]` 도 전부 **×2 이전**이다.

### ⚠⚠ 그리고 내가 총괄에 올린 경보가 **틀렸다**

내가 이렇게 적었다:
> *"**하루 식비 절감이 통째로 2배가 되는 자리**라 아무나 고치면 안 됩니다"*

**아니다.** 같은 절이 바로 그것을 막아 두었다(`first_play.js:673`):
> ⚠ **밥으로 아끼는 것은 안 늘어난다** — 하루 몫 상한이라 g 이 늘어도 그대로다.
>   늘어난 것은 **전부 팔린다.** 그래서 배수의 효과는 **판매 수입에만** 온다.
> ⚠ `savedWonPerCycle` 은 **안 건드린다.** 같이 올리면 하루 몫이 흔들린다.

⇒ ★ **식비는 한 푼도 안 움직인다.** 움직이는 것은 **파는 쪽**이다.
⇒ 내가 「2배」만 보고 **어디에 붙는 2배인지를 안 읽었다.** 경보의 방향이 반대였다.

★ 그리고 이 사실은 **§7 의 물음과 붙는다** — 수확량 2배가 **판매 수입에만** 온다면,
  채소로 버는 돈이 그만큼 늘고 **이사비에 닿는 길이 하나 더 있다**는 뜻이다.
  `plan-questions-moveout.md` 의 물음 ①(모주가 열쇠 하나) 을 볼 때 같이 봐야 한다.

## 8-2 `pantrysale` — 같은 뿌리의 파급

```
assert.equal(fp.food.pantryWon, 0)   → 7000 !== 0
for (let d = 0; d < 10 && fp.food.pantryWon > 0; d++) eatFromPantry(fp);
```
**열흘 먹으면 곳간이 빈다**고 가정한 고리다. 수확량이 2배가 되어 **열흘로 못 비운다.**
⇒ ㉣ 파급. 고칠 것은 고리 횟수이지 값이 아니다.

## 8-3 `dialogue_coverage ⑸` 식물신 6줄 — **㉣ 이면서, 내가 정할 것이 하나 있다**

`portrait:false` 와 이름 `?` **계약은 지켜지고 있다**(실측). 깨진 것은 **줄 수**다.

```
god1#0  한 바퀴는 운이다. 두 바퀴는 손이지.
god1#1  너는 어두운 데서 먹을 것을 얻었다. 이제 반대를 해 …
god1#2  이건 못 먹는다. 대신 잘 둔 만큼 값이 된다.
god1#3  ★ 가방에 넣어 두었다.          ← 2026-08-17 「가방행」 때문에 늘어난 줄
spearFurled#2 · movedOut#10           ← 자리마다 한 줄
```
자는 **「자리마다 한 줄」= 3줄**을 기다린다. 자리는 여전히 셋인데 `god1` 만 넷이다.

### ★ 내 판단 — **식물신은 한 줄로 되돌린다. 다만 그 한 줄을 지우지는 않는다**

`docs/first_play.md §193` 이 **「대사 한 줄. 외형 없음」**이라 적어 두었다. 내 정본이다.
식물신의 힘은 **드묾**에서 온다. 넷을 말하면 그냥 또 하나의 화자가 된다.

⚠ 그런데 `가방에 넣어 두었다` 는 **기능적으로 필요하다** — 08-17 부터 몬스테라가 가방으로 오므로
  **어디서 찾는지를 말해 주지 않으면 플레이어가 잃어버린다.**

⇒ **그 줄을 몬이에게 옮긴다.** 주는 것은 식물신이고, **어디 있는지 알려 주는 것은 안내자 몫**이다.
  그렇게 나누면 계약도 살고 안내도 산다. `dialogue.js` 는 [core] 소유라 **문안만 넘긴다.**
⇒ 나머지 `god1#0~#2` 중 어느 한 줄로 줄이는 것은 **글의 문제**라 [core] 와 같이 본다.
  ★ 다만 **줄 수를 셋으로 되돌린다는 것**은 내가 정한다.

## 8-4 그래서 최종 셈

| 갈래 | 처음 | **지금** |
|---|---|---|
| ㉣ 박사님 결정 | 7 | **10** |
| ㉡ 진짜 회귀 | 6 | **4** (G-3·G-4 가 빠졌다 — §0 정정) |
| ㉠ 자가 낡음 | 5 | 5 |
| ㉢ 자가 못 갖춤 | 2 | 2 |
| **못 가름** | 3 | **0** |

⇒ ★ **12개 중 10개가 「고치면 박사님 결정이 지워지는」 것이었다.**
  총괄이 준 갈래 셋으로만 갈랐으면 그 열이 조용히 「자낡음」으로 들어갔을 것이다.
