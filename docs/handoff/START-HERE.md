# 여기서 이어서 — 새 창이 제일 먼저 읽을 것

> 갱신 **2026-08-11 밤** · 검사 통과 · 원격에 **아직 안 밀었다**(커밋만 돼 있다)
>
> ★ **`game.html` 널 바이트는 없앴다** — `grep` 이 이제 끝까지 읽는다.
> 전에는 6,869줄에서 멈춰 뒤 250줄을 통째로 안 봤다.
>
> ★ 이 날 바뀐 큰 것 넷: **무순이 「놓고 나서 심기」로** · **가구 윗면에 칸이 깔림**(14 → 31) ·
> **끌어 놓기 실패가 말을 하게** 됨 · **화면이 멎던 사고**(자유 좌표 화분) 고침.
> 각각 `musunsow-` · `slotgrid-` · `guidefix-to-plan.md` 에 상세가 있다.
>
> 이 파일 하나로 이어진다. 더 깊이 알아야 할 때만 아래 링크를 연다.

---

## 0. 지금 어디까지 왔나

**놀 수 있다.** https://chunryou-delicate.github.io/lux-plant-sim/game.html
(공개 주소. 누구나 링크만 열면 된다. 저장은 각자 브라우저 안에만 남는다)

반지하에서 콩나물을 돌려 밥값을 아끼고, 몬스테라를 받아 키우고, 무늬 잎 셋을 모아
하프문을 만들면 원룸으로 이사한다 — **여기까지 실제로 돌아간다.**

⚠ **원룸은 아직 덜 봤다.** 반지하까지가 제대로 된 구간이다.

---

## 1. 말투·규칙 (먼저 맞출 것)

- 대화는 **"박사님"** · 존댓말. **문서 본문은 평서체**
- **`git add .` 금지.** 여러 창이 한 저장소를 쓴다 — **내 파일만 이름으로 지정**해서 커밋
- 결과는 **파일로** 남긴다: `docs/handoff/{내창}-to-plan.md`
- **설명 먼저, 숫자 나중.** 새 계통은 "뭔지"부터. 표부터 내면 소통이 끊긴다
- 커밋 메시지는 한국어 평서체로 **무엇이 왜 잘못돼 있었는지**

## ⚠ 검사 돌리는 법 — 안 읽으면 반드시 헤맨다

브라우저를 띄우는 검사가 18개고 **로컬 서버가 있어야** 한다.

```bash
python tools/serve.py 8963
export BYEOT_URL=http://localhost:8963
for f in tools/test_*.mjs; do node "$f" || echo "FAIL $f"; done
```

- ⚠ **`tools/test_boot_profile.mjs` 만 `BYEOT_URL` 을 안 읽는다** — `--url=http://localhost:8963` 로 넘겨라
- 전체는 10분을 넘긴다. **백그라운드로** 돌려라
- 자세한 것은 `docs/handoff/README.md` 맨 아래

---

## 2. ★ 이 저장소에서 제일 자주 나는 사고 — 열 번 났다

**숫자나 문구가 이상하면 코드가 아니라 「재는 자」를 먼저 의심하라.**

2026-08-09~11 사이에 측정이 **열 번** 틀렸는데 **열 번 다 「무엇을 재는지」**가 문제였다.
서버 없이 돌린 것 · 분모가 후보마다 다른 것 · 「분배」가 분배가 아니었던 것 ·
「체력 5로 쟀다」는데 실제로는 8~9였던 것 · 「세 층」이라 해 놓고 한 층만 켠 것 ·
하네스가 모주를 말려 죽인 것 · 검사가 옛 상수를 박아 둔 것 ·
**고장난 상태를 검사가 정상으로 못 박아 둔 것**(제일 위험하다 — 고치는 쪽이 검사를 깨게 된다).

그래서 이 셋을 지킨다:

1. **모든 표 머리에 「무엇을 켜고 무엇을 껐는지」를 적는다**
2. **「지금 그렇다」와 「그렇게 하기로 했다」를 갈라서 적는다**
3. **모르면 모른다고 적는다. 짐작으로 원인을 쓰지 않는다**

★ 그리고 **「고쳤다」를 화면 확인 없이 쓰지 않는다.** 그것 때문에 두 번 되돌아갔다.
   코드가 바뀐 것과 화면이 바뀐 것은 다른 말이다.

---

## 2.5 ★ 놓을 자리(노란 칸) — 내가 틀리게 옮긴 걱정을 여기서 지운다

**「칸을 잘게 나누면 큰 화분이 갈 곳을 잃는다」는 틀렸다.** 2026-08-11 에 내가 그렇게 보고했고
박사님이 바로잡으셨다: *"큰화분이 왜 갈곳을잃어? 1개반 2개칸 4칸 차지하먄되자나"*

맞는 말이고, **그 계통이 이미 `src/game/place.js` 에 다 있다.**

| | |
|---|---|
| `GRID_UNIT = 0.05` | 크기를 세는 최소 칸 |
| `unitsFor(m)` | 길이 → **칸 수**(`ceil`) |
| `snapSpan(center, size)` | **앞 모서리**를 격자선에 맞춘다 — 중심에 맞추면 짝수 칸짜리가 반 칸 어긋나 정수 점유가 깨진다 |
| `cellBox(rect)` | 발자국이 차지하는 **칸 상자** `{i0,j0,ni,nj}` — 「겹침 판정을 정수로 만든다」 |
| `snapAngleDeg` | 90° 단위 (45°면 정수 점유가 무너져서) |

0.25 격자로 치면 무순 재배판(0.43)은 **2×2 = 4칸**, 콩나물 시루(0.24)는 1칸, 시루 무리(0.97)는 16칸이다.

⚠ **그런데 이걸 가구에만 쓴다.** `snapFurniture → snapSpanStep` 으로 가구는 이미 정수 칸 점유로
겹침을 판정한다(`room_view.js:2695`). 화분 쪽만 옛 모델 —— **자리 하나 = 점 하나 + `maxPotD` 한도 +
찼나/비었나** —— 에 남아 있다. 그래서 책상 윗면 0.72㎡ 에 점이 둘뿐이다.

⇒ **할 일은 새로 짓는 게 아니라 「가구가 이미 쓰는 자를 화분에도 쓰는 것」이다.**

진짜로 남는 걸림돌은 둘뿐이다:

1. **검사 9개가 「14칸」을 박아 두었다** — DLI 표까지 슬롯 이름별로 얼려 놨다. 다시 뽑아야 한다
   (`test_banjiha_profile:70` · `test_lampaim:138` · `test_lampswitch:284` · `test_headroom:97` ·
    `test_floorlight:148` · `test_musun_view:146` · `test_multisiru:159,418` · `test_lampmove:100` · `test_oneroom_room:108`)
2. **세이브가 `banjiha-desk:1` 같은 이름을 저장한다**(`save.js` 여섯 곳) — 번호 뜻이 바뀌면
   **옛 판의 화분이 조용히 다른 자리로 간다.** 없는 이름은 옮겨 주지만 **뜻만 바뀐 경우는 아무도 못 잡는다**

밝기 계산이 느려지는 건 걱정거리가 아니다 — 온실 창턱이 이미 112칸으로 돈다.

---

## 2.6 ★ 「재배판이 서랍장에 못 올라간다」는 없는 일이었다 (2026-08-12)

이것이 이 저장소에서 **열두 번째** 「재는 자」 사고다. 그리고 **낡은 주석이 범인**이라
앞의 것들과 결이 다르다 — 적어 둘 값어치가 있다.

`slotgrid-to-plan.md §7-5` 가 이렇게 진단했다: *새싹 재배판은 회전무관 지름 0.4327 이라
서랍장(한도 0.42)에 못 올라간다. 실물 0.36 × 0.24 는 들어가는데 지름으로 재서 막힌다.*
그래서 「`maxPotD` 를 칸 수로 재는 큰 일」이 다음 할 일 1번에 올라갔다.

**재 보니 그런 일이 없다.**

| | |
|---|---|
| 게임이 쓰는 무순 지름 | **0.20** (`roomView.plantPotD('musun')` 실측) |
| 반지하에서 제일 좁은 자리 | 창턱 **0.21** |
| ⇒ 올라가는 자리 | **14칸 전부** (드롭다운 14줄 · 책상·선반·창턱에 실제로 놓아 봄) |

0.4327 은 **에셋 원본의 대각선**(`TRAY_S_D`)일 뿐이고 무순 판정에 안 쓰인다.
방뷰가 판을 **0.20 으로 줄여서** 그린다(`room_view §MUSUN_D`). 그것도 2026-08-05 에
**바로 이 문제를 풀려고** 정한 값이다 — 원문 주석: *"무순은 밝아야 좋은 작물이고 이 방에서
밝은 곳은 창턱 하나인데 창턱은 0.21m 다. 즉 **제일 좋은 자리에 못 올라가는 작물**이 되어 있었다."*

**왜 헛짚었나** — `game.html` 세 군데에 낡은 주석·문구가 남아 「0.43」이라 말하고 있었다.
그중 하나는 화면에 뜨는 글이었다(「재배판(0.43m)이 올라갈 자리가 이 방엔 없습니다」 —
반지하에서는 **뜰 수가 없는 문장**이다). 지금은 셋 다 고쳤고, 지름은 방뷰에 물어서 적는다.

★ **교훈: 주석이 낡으면 재는 자가 된다.** 값을 옮긴 뒤 그 값을 설명하던 주석을 안 고치면,
다음 사람이 코드가 아니라 주석을 읽고 진단한다. 실제로 그렇게 됐다.

⇒ 지금 게임의 물건은 전부 둥글거나(몬스테라 0.20 · 콩나물 시루 0.24) 둥글게 줄여 그린다
(재배판 0.20). **네모난 발자국 때문에 막히는 물건이 하나도 없다.** 그러니 지름 판정을
칸 수 판정으로 옮기는 일은 **지금 고칠 것이 없는 일**이다 — 네모난 물건이 실제로 생기면
그때 §2.5 의 `cellBox` 를 그대로 쓰면 된다.

---

## 2.7 에셋 — GLB 를 찍어 썸네일을 만든다 (2026-08-12)

**`tools/glb_thumb.mjs` 가 GLB 를 썸네일로 찍는다.** AI 로 다시 그리지 않는다 —
다시 그리면 판 모양이 GLB 와 미세하게 갈려 **가방 그림과 방 안 물건이 다른 물건**이 된다.

```bash
python tools/serve.py 8963
node tools/glb_thumb.mjs --all=assets/pots            # 폴더째 (있는 것은 건너뛴다)
node tools/glb_thumb.mjs <glb> <png> --force          # 한 장
```

규약(재서 맞춘 것): **512×512 · 흰 배경 불투명 · 납작한 색 + 무늬(map)**.
`assets/crops/thumbs` · `assets/furniture/thumbs` 가 이미 그렇다.
⚠ `make_thumbs.py` 머리글은 「알파 있는 렌더」라고 적어 두었는데 **지금 파일들은 아니다**(알파 0%).

⚠ **투명한 물건은 그냥 찍으면 사라진다** — 유리 수경병이 흰 바탕에 흰 실루엣이 됐다.
  색을 조금 누르고 뒷면까지 그려 가장자리가 진해지게 했다.

### 지금까지 찍은 것 · 남은 것
- 찍었다 — `pots` 13 · `lighting` 6 · `items` 4 · `lamps` 3 · `house` 4 · 빈 재배판 1 = **31장**
- **가방의 「포」·「병」 글자가 없어졌다** — `BAG_LETTER` 는 빈 표가 됐다(품목이 늘 때를 위해 표만 남김)
- 남은 것 — `monstera/` 와 `monstera/skins/` 의 **잎·줄기 123장.** 화면 목록에 안 나오는
  부품이라 급하지 않다. 무늬 등급 도구가 잎을 격자로 펼 때 필요해지면 그때 찍으면 된다

---

## 3. 박사님 답을 기다리는 것

| | 무엇 |
|---|---|
| | **빈 재배판 원화** — 무순 가방 그림이 아직 「다 자란 판」이다. ★ 이제 **안 심은 판이 실제로 방에 선다**(2026-08-11 순서 바꿈) — 3D 도 progress01 0 에서 3포기를 세워서 **안 심은 판에 싹이 나 있다.** 그림 한 장이 있어야 글과 화면이 같은 말을 한다 |
| | **하나도 안 놓은 판을 튜토 끝난 뒤에도 막을지** |
| | **이사 뒤 시루를 새 방 첫 자리에 몰아 앉히는 게 맞는지** (밝기가 시루마다 갈린다) |
| | **몬스테라 도착 자리** — 지금 책상(0.61). 그대로 두는 것으로 정해졌다 |

---

## 4. 다음에 할 일 (급한 순)

1. **무늬 등급 매기기** — 도구는 준비됐다.
   https://chunryou-delicate.github.io/lux-plant-sim/tools/varie_grade.html
   박사님이 **34종**을 보고 고르시면 내보내기로 주신다 → 도감·값에 반영.
   ★ **「예쁜 게 비싸다」를 값에 넣는 첫 걸음**이다(지금은 무늬 잎 **개수**만 본다)
2. **초반 빈 날** — 시루 1~3개 구간이 39~77% 빈다.
   박사님 방향: **해금** (처음엔 가방만 → 상점 열리고 → 물품이 차차)
3. **퀘스트 완주 판정** — 「시루 5개 분배로 5주기 완주 → 최대체력 +1」. 창구·값은 준비됐고 부르는 데가 없다
4. **원룸** — 보증금 100만·월세 35만은 넣었다. 방 자체는 덜 봤다
5. ~~`maxPotD` 를 칸 수로 재기~~ — **안 한다.** 고칠 것이 없었다(2026-08-12 · 아래 §2.6).
6. ~~칸 눈금을 잘게 할지~~ — **끝났다**(박사님 "더쪼개서 되면 더쪼개").
   반 칸(0.125m)까지 쪼갰고 **책상 상판 8 → 21칸**. 사진 `docs/handoff/img/cells125_banjiha_*.png`
   ★ **더는 못 쪼갠다.** 물건은 놓는 걸음(0.125m)에만 앉으므로 그보다 잘게 그리면
     칸 한가운데를 겨눠도 거기 안 앉는다 — 표시가 거짓말이 된다. 걸음 자체를 잘게 하는 것은
     박사님이 2026-08-07 에 이미 물리신 길이다("너무 촘촘한 거 같아").
   ★ 그리고 **그린 자리가 곧 앉는 자리**가 됐다. 격자로 후보만 뽑고 `snapOnSurface` 에 물어
     **돌아온 자리에** 그린다 — `snapErr` 이 계산이 아니라 **구조적으로** 0 이다.

---

## 5. 알아 둘 것 — 안 고친 채 남은 것

- **빈 데를 눌러도 고르기가 안 풀린다.** 방뷰가 호스트에 신호를 안 낸다(`onEmptyTap` 창구 필요)
- **`arrivalSlotId` 가 세이브에 안 실린다** — 앱을 껐다 켜면 「옮겼습니다」 안내가 「옮겨 보세요」로 되돌아간다
- **`test_siru_pick` C 절이 흔들린다** — 기계가 바쁘면 이따금 실패한다. 3D 광선이 캐릭터를 맞는 **시간 의존** 검사다. 단독으로는 6회 연속 통과
- **무순 순서를 뒤집었다** (2026-08-11 · 박사님: *"왜 가방에서 무순 심기를 해야 돼? 재배판 배치 후"*)
  이제 **방으로 끌기 → 빈 판이 선다(재배판만 나감) → 놓인 판의 [🌱 심기](씨앗 나감) → [💧 물] → 7칸**이다.
  가방의 [🌱 무순 심기] 단추는 없앴다. **콩나물은 그대로**(놓기 = 심기)다 — 두 작물 손짓이 다르다.
  ⚠ 심기에 **체력을 안 물렸다.** 「처음 심기 공짜 · 다시 심기 손 하나」 비대칭이 남아 있다
- **끌어 놓기 실패가 조용하던 것을 고쳤다** (2026-08-11). `#dropLabel` 은 `body.dragging` 일 때만
  보이는데 `drag.end` 가 그 클래스를 **떼고 나서** 놓기를 시도해서, 실패 이유가 늘 안 보이는 데 적혔다.
  이제 배너로도 말한다. ★ 무순만이 아니라 **`placeFailed` 를 타는 모든 실패**가 그랬다

---

## 6. 확정된 값 (되묻지 말 것)

| | |
|---|---|
| 시작 소지금 | **150만원** |
| 반지하 월세 | **20만원** · 하루 지출 **16,667원** (식대 7,500 + 공과 2,500 + 월세 6,667) |
| 원룸 | 보증금 **100만** · 월세 **35만** |
| **이사비** | **200만원** (보증금 100 + 첫달 선납 35 + 이사비 65) |
| 잉여 판매가율 | **85%** |
| 콩 씨앗 / 시루 (실구매) | **500 / 5,000원** |
| 콩나물 회전분 / 주기 | 3,000원 / 5일 |
| 무순 회전분 / 주기 | **2,800원** (질림 2/3 = 1,867) / 7일 |
| 하루 곳간 상한 | 5,000원 |
| **하프문 배수** | **640/9 ≈ 71.1** (섹터의 두 배) |
| 시작 최대체력 | **5** · 상한 **없음** |
| 레벨업 | 5→10 은 **10·15·20·25·30** · 10 위는 **최대체력 × 10** |
| 경험치 | **그날 쓴 체력의 총량** (한 행동 = 1) |
| 잎 간격 | **30·40·50·70·100·150·200·300** (유효 생장일. 삽수는 1장부터 새로 센다) |
| 몬스테라 도착 | 유효 생장일 **45** · 잎 1장 · **책상**(`banjiha-desk:0`) |

★ **「이사비 200만」과 「하프문 71.1」과 「시작돈 150만」은 한 벌이다.**
  시작돈 = 이사비가 되면 첫날부터 이사 조건이 참이라 튜토가 48일에 끝난다.
  하프문이 200만을 못 넘기면 아무도 못 나간다(실측 0/40). **셋을 같이 움직여라.**

---

## 7. 더 깊이 볼 때

| 파일 | 무엇 |
|---|---|
| `plan-2026-08-09-decisions.md` | **결정 기록 전부** + 그날 저녁 측정 결과 + 정정 이력 |
| `econ-to-plan.md` §7.5~7.14 | 경제 측정 표 전부 (세 층 · 노가다 이사 · 체력 곡선) |
| `채소경제_1-120일.xlsx` | 파라미터를 바꾸면 120일이 다시 계산되는 통합문서 |
| `STATUS.md` | 창별 상태 요약 |
| `siru-to-plan.md` · `cropseat-to-plan.md` · `sirutap-to-plan.md` · `bagone-to-plan.md` | 시루 각개화 4연작 |
| `growth-to-plan.md §2026-08-09` | 잎 곡선 · 유도 · 붙일 코드 |
| `variegrade-to-plan.md` · `siruart-to-plan.md` | 무늬 등급 도구 · 빈 재배판 요청 |

---

## 8. 창을 어떻게 굴리나

**VS Code 의 옛 여섯 창(growth·leaf·char·house·plan·core)은 깨우지 않는다.**
그 구조는 조각을 따로 만들던 때의 것이고, 지금은 조각들이 서로 물려 있어 나눌 금이 없다.
게다가 08-01~04 에 멈춰 있어 깨우면 **낡은 믿음으로 판단한다.**

**마스터 창 하나 + 워커**로 간다. 워커 규칙 넷:

1. **보고를 짧게 받는다** — 상세는 인계 문서, 나에게는 「바뀐 것 · 실측 · 판단필요 · 못 한 것」만
2. **동시에 둘까지** — 합치는 건 어차피 직렬이다
3. **마스터는 큰 파일을 안 든다** — 작은 수술은 직접, 파일 하나를 통째로 뜯는 일은 워커
4. **차기 전에 넘긴다** — 꽉 차서 급히 넘기지 말고 이 파일을 갱신해 두고 넘긴다
