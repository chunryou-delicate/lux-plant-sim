# core 창 지도 — 2026-08-29

박사님 청: *"세션에서 지금 만든 것과 해야 될 것, 그리고 만든 것에 대해서는 그 내부 알고리즘이나
세팅들에 대해서도 보이게 해서 전체 지도로 한방에 파악하고 싶어. 부족한 것도 바로바로 파악하고."*

한 줄이 한 칸이다. 문장으로 풀지 않는다.

## ① 만든 것 — 무엇을 하나

| 커밋 | 파일 | 무엇을 하나 |
|---|---|---|
| 476aef9 | game.html CSS | 방→방 옮기기에서 안내글이 보인다 (`#stage.moving #dropLabel`) |
| f8c361c | game.html | 가방 몬스테라를 누르면 「여기에 둘까요?」가 뜬다 |
| f8c361c | game.html | [취소]가 몬스테라를 가방으로 되돌린다 (`placeConfirmCancel`) |
| 14882f7 | dialogue.js | 몬이가 "정해져 있어. 두 번째랑 세 번째 잎에" 라고 말한다 |
| 00e41f0 | game.html | 가방 칸이 "끌거나 눌러 방에 놓으세요" 라고 말한다 |
| d9bc1f8 | game.html | 가방 끌기 손짓을 칸이 아니라 창에 건다 |
| 1e8c67b | game.html | 놓은 그 순간에 할 일 줄을 다시 본다 (`afterPlace`) |
| 1e8c67b | game.html | 부팅에서 `checkQuests` 뒤에 `draw()` 를 부른다 |
| 1e8c67b | game.html | 몬스테라 손가락이 "했나"(`fp.phase`)를 본다 |
| 2b3dfe6 | game.html | 가방으로 간 그루를 방에서 걷는다 (`syncRoom`) |
| e8e1a09 | game.html | [옮기기] 뒤에 갈 자리를 손가락이 가리킨다 |

## ② 속의 수 — 조건과 함께

| 값 | 수 | 조건 |
|---|---|---|
| 무늬 잎 번호 | 2 · 3 (`PROLOGUE_VARIE_LEAVES`) | 선물 몬스테라만. 씨앗은 5% 굴림 |
| 도착 나이 | 45일 (`ARRIVAL.growthDays`) | 도착 그 순간 |
| 팔 수 있는 잎 수 | 3장 (`MARKET_MIN_LEAVES`) | 그루째 팔기 |
| 등급 값 | 민 20,000 / 산반 350,000 / 하프문 750,000 / 풀문 1,150,000 | `varie_grades.json` |
| 화분 배수 | 1.4 (`sale.potMult`) · 삽수 1.0 | 그루째 팔 때만 화분값이 붙는다 |
| 하루 밥값 상한 | 5,000원 (`dailyCropSaveWon`) | 시루를 아무리 늘려도 이 위로는 안 는다 |
| 이사비 | `TUTORIAL_RULES.moveOutCostWon` | 값은 tutorial.js 가 정본 |
| 등 | 120,000원 · 전기 하루 38원 | 첫 등 |
| 손가락 과녁(화분) | 몸통 36×30px | 폰 390×844 · 45일 그루 · 책상. 권장은 44×44 |
| 가방 시트 | 514/883 (스크롤됨) | 안 놓은 화분 12개일 때 |
| 놓을 수 있는 점 | 405 중 57 (몬스테라) · 58 (시루) | 반지하 · 6px 눈금 · 화분 지름 0.202/0.2 |
| 부팅 제한 | `io.growth.ready()` 30초 · 25초에 글이 바뀐다 | 깃 판 실측 부팅 2,886ms |

## ③ 아직 없는 것 — 뚫린 곳

### 돌긴 도는데 안 재 본 것
- 이사 뒤 원룸을 판으로 굴려 본 적이 없다. 원룸 방 데이터가 아직 못 쓰는 상태다.
- 도착 로그가 [검수] 안에서 실제로 보이는지 안 눌러 봤다 (1일차라 아직 안 온다).
- 물주기 손가락이 옳은 곳을 짚는지 안 쟀다 — 그건 말풍선(marks)이 맡는다.
- `walkTip` 쪽지가 실제로 뜨는 판을 안 만들어 봤다.

### 임시로 박아 둔 수
- 누르면 앉는 자리가 추천 자리가 아니라 `free:` 좌표다 (`provisionalSpot`). [다시 옮기기]가 있어 막다른 길은 아니다.
- 그루값 ⓒ(02eef1f)가 박사님 판단 대기 중이다 — 되돌릴까/밑값을 둘까/그대로.
- 750,000원이 `leaf_grade` 이벤트 글에 글자로 있다.

### 한쪽만 있는 것 (계율 37-e)
- `arrivalSlotId` 는 아직 모듈 변수다. `monsteraMoved` 대사가 그것으로 "옮겼나"를 가른다 — 새로고침하면 그 대사가 안 난다.
- 가이드의 "했나"를 적는 칸이 셋으로 나뉘어 있다: 퀘스트 `done` · 쪽지 `coachSeen` · `fp.phase`. `coachPending` 은 저장이 안 된다.
- `coachMark` 는 "봤다"를 적는다. "했다"는 아무도 안 적는다.
- 빈 화분(`startPhonePlacePot`, 씨앗 심기)은 아직 고르는 화면으로 안 들어간다. 가방 몬스테라만 고쳤다.
- [옮기기] 뒤 아래글이 "아무 데나" 인데 손가락은 창턱을 짚는다. 문안을 [Plan]에 청해 뒀다.

### 자가 못 재는 것
- CDP 터치는 브라우저의 진짜 손짓 처리(스크롤 가로채기 · touch-action · 제스처 취소)를 안 지나간다. 폰에서만 나는 병은 이 자로 영영 안 잡힌다.
- 자는 Chrome 이다. 박사님이 쓰신 Firefox 는 못 잰다.
- 손가락이 꺼져 있어도 `.say` 글자는 옛 글이 남는다. 그 글을 "지금 하는 말"로 읽으면 틀린다 — 오늘 두 번 그랬다.

### 다른 창 것이라 안 건드린 것
- `test_dialogue_coverage` 2건 FAIL: `brokeTalk` 이 다시 나온다 · 식물신 대사가 6줄.
- `test_roomview_place` 68/74: 가구 옮기기 쪽. 내 변경 전에도 같은 수다.
- `test_crop_seat` F/F-2 (STATUS.md 에 이미 적혀 있다).

## ④ 다음에 할 것 — 순서대로

1. [Plan]의 문안이 오면 [옮기기] 아래글을 조건에 매어 고친다.
2. `coachPending` 을 저장한다 — 못 뜬 쪽지가 새로고침에 사라지는 것.
3. `arrivalSlotId` 를 없애거나 저장한다 — `monsteraMoved` 대사가 새로고침 뒤에 안 난다.
4. 시루를 놓고 도착까지 실제로 굴려서 가이드 사슬을 통째로 잰다. 지금까지는 판을 세워서 쟀다.
5. 「했나」 칸을 하나로 모은다 (얼개. 크다. [Plan]·총괄과 같이 볼 일).
6. 화분 손가락 과녁을 넓힌다 — 박사님이 화분을 못 누르실 때만.
