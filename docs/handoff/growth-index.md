# [growth] 색인 — 「무엇을 재는 자가 있고, 무엇을 쟀나」

갱신: 2026-09-06 밤 · 이 창이 소유한 것만 적는다.
새 창이 「[growth] 가 무엇을 아나」를 찾으면 **여기부터** 보면 된다.

---
## ⚠ 먼저 — 이 창의 표를 읽는 법

**같은 방을 재도 조건이 다르면 다른 표다.** 안 밝히면 다른 창에서 반드시 오독된다.
실제로 그것으로 여러 번 갈렸다. 그래서 자 출력 머리에 넷을 박아 두었다:

```
① 모드      novice = 여름·맑음 «고정»(첫 플레이) / real = 계절이 돈다(끝까지 가는 판)
             ★ 「어느 쪽이 맞나」가 아니라 «다른 판»이다
② 등 개수    등은 «위치를 못 고른다» — 개수만 고른다(growRigs.slice)
             ⚠ 원룸은 2026-08-30 부터 다르다 — 사람이 놓는다(lampCounts [0])
③ 자         ★ 셋이다. 섞으면 몇 배 틀린다
               몬스테라  «7일 이동평균»
               콩나물    자라는 «5일» 동안의 하루 평균 (어두울수록 좋다 · 상한만 있다)
               무순      자라는 «7일» 동안의 하루 평균 (밝을수록 좋다 · 하한만 있다)
④ 프로필     자리 수 · roomRev · 잰 날. 프로필이 다르면 그것도 다른 표다
```
⚠ 그리고 **등 DLI 에는 날씨 계수를 안 곱한다**(엔진 확인 — 자연광은 날씨마다 12가지인데 등은 1가지).
밖에서 ×0.643 을 곱하면 틀린다.

---
## ① 자 — 무엇을 재나

| 자 | 무엇을 | 어디서 도나 |
|---|---|---|
| `probe_threshold_card.mjs` | ★ **「이 자리 빛이 얼마면 무엇이 되나」** — 배치가 바뀌어도 안 낡는다 | 헤드리스 |
| `probe_slot_year.mjs` | 이름 붙은 자리를 400일 굴려 밴드·문턱 넘는 칸 (`ROOM=` · `LAMPS=` · `PROFILE=<경로>`) | 헤드리스 |
| `probe_crop_quality.mjs` | 자리마다 작물이 실제로 몇 그램인지 · **0g 인 칸 세기** (`PROFILE=` · `LAMPS=`) | 헤드리스 |
| `probe_first_leaf.mjs` | ★ **첫 잎까지 며칠인가** — 자리 15칸을 400일 태운다 (`ROOM=`·`LAMPS=`·`MODES=`·`FROM=`) | 헤드리스 |
| `probe_grade_vs_skin.mjs` | ★ **등급과 그림이 만나나** — 공용 못에서 성숙했을 때 어느 등급 그림에 닿나 | 라이브 |
| `probe_leaf_when.mjs` | n번째 잎이 언제 오나 (`FROM=` · `EFF=`) | 헤드리스 |
| `probe_leaf_health_year.mjs` | 잎이 **바랬다가 돌아오나** — real 사계절 |
| `probe_arrival_phases.mjs` | 도착 뒤 **단계마다 첫날** — 어린잎→중간잎→줄기→새순 자리→말린 새순 (`ROOM=`·`SLOT=`) | 헤드리스 |
| `probe_floor_dli.mjs` | **바닥**을 격자로 훑는다 — 정적 프로필은 바닥을 모른다 | 라이브 |
| `probe_room_live_year.mjs` | ⚠ 프로필이 **낡았을 때** 400일 — 라이브 값을 날씨에 태운다 | 둘 다 |

## ② 검사 — 무엇을 지키나

| 검사 | 무엇이 깨지면 붉나 |
|---|---|
| `test_sill_winter_margin.mjs` | ★ **이 판에서 제일 아슬한 수** — 창턱 겨울 여유 0.084 |
| `test_crop_unknown_slot.mjs` | 「모르는 자리」가 최상 품질로 새지 않는가 |
| `test_coord_matches_slot.mjs` | 좌표로 잰 값이 슬롯으로 잰 값과 같은가 |
| `test_growth_parity.mjs` | 헤드리스 `plant_grow` 와 브라우저가 같은 수를 내나 |
| `test_multiplant_isolation.mjs` | ★ **그루끼리 상태가 새지 않는가** — A→B→A 로 돌아와서 본다 |

## ③ 잰 것 — 어디에 적혀 있나

`docs/handoff/night-20260823.md` (파일 이름은 첫날 것이고 그 뒤로 계속 쌓았다)

```
§10.9   자리 조도 — 바닥은 표에 없었고, 없는 것이 0 으로 보인다
§10.10  하한 없는 등급 넷  (+ ⛔ 정정: 그 함정은 없었다. 함수 둘을 섞었다)
§10.11  잎이 언제 오나 — 명세대로 돈다. 넷째는 d158
§10.12  등은 무엇인가 — 첫째와 둘째는 다른 물건이다
§10.13  「최상 칸」과 「놓을 수 있는 칸」은 다르다
§10.14  새 프로필 재측정 · 3단 선반은 「두 몫」을 한다
§10.15  C7 바닥 조도 — 「끝났나」를 재는 자는 이미 코드 안에 있다
§10.16  축복을 여는 것은 등이 아니라 자리다
§10.17  창턱을 격자에 붙이면 겨울 여유가 0.084 → 0.003
§10.18  원룸 — 낮출 것은 자연광이 아니라 등이다
§10.19  낮춘 것은 등이 아니라 「등 자리」였다
§10.20  빈손 원룸 — 등이 「길」에서 「속도」가 된다
§10.21  무순 자리 — 자리마다 임자가 있다
§10.22  (09-06 밤) 도착 뒤 빈 며칠 4·19·22·25 · 원룸 후보 A~D × 등 0·1·2 · 동치 검사 재PASS
```

### 2026-09-07 · 「어두운 자리는 벽인가」 — [leaf] 물음

```
반지하 · novice(첫 플레이) · 게임일 11 에 놓고 400일 · probe_first_leaf.mjs
              7일평균        첫 잎(누적 GROWTH 30)        나는 자리
등 0개 창턱     3.68     ✅ 게임일 40 (놓인 지 29일)          1/15
       나머지   0.04~0.56 ⛔ «누적 0/30» — 400일에도 안 난다
등 1개 창턱     6.02     ✅ 게임일 34                          1/15
등 2개 창턱     6.06     ✅ 게임일 34   · 책상 1.77 ⛔ 안 남   1/15
```
> ★ 「느리다」가 아니라 **눈금이 0** 이다. 0 을 400번 더해도 0 이다.
> ★★ **등을 둘 사도 1/15 그대로다.** 어두운 자리는 돈으로 못 옮기고 손으로 옮겨야 한다.
> ⇒ [[등이 곧 길]] 을 고치지 않고 한 겹 좁힌다 — 첫 관문은 «돈»이 아니라 «손»이다.

### 2026-09-07 · 「바닥」 값 — [leaf]·총괄이 청해서 좌표로 쟀다

```
반지하 · 맑음·여름 · 좌표는 원점 중심 ±2.4 · y=0.10 · dliAt (라이브)
자리                 좌표          등0     등1     등2     등3
한가운데 바닥        (0, 0)        0.27    0.37    0.44    0.92
창 아래 바닥         (0, -1.85)    0.07    0.27    0.39    0.74
창에서 먼 구석       (1.8, 1.8)    0.06    0.10    0.12    0.22
★ 바닥에서 제일 밝은 점 (-0.9,-0.6)                        1.44
─────────────────────────────────────────────────────────────
창턱(견줌)           (0, -1.85) y1.58  3.68  6.02  6.06  6.17
```
> ## ★★★ **바닥은 등을 «셋» 켜도 제일 밝은 점이 1.44 다. 관문 2.7 을 못 넘는다.**
> ⇒ 곧 **바닥 221점 어디에도 몬스테라의 첫 잎은 «없다».** 등을 몇 개 사든 같다.
> ⇒ ⇒ 한가운데 바닥은 등 없이 0.27 로 `die` 0.5 아래다.
> ⇒ ⇒ ⛔ **그런데 「죽는다」는 뜻이 아니다.** 2026-09-07 에 내가 그렇게 읽어서 틀렸다 —
>   `die` 는 밴드 «이름»이고 하는 일은 «멈춤»이다. 죽는 길이 이 저장소에 없다.
>   `loop.js:17` 「critical 로 죽이는 코드는 절대 넣지 않는다」 ·
>   `headroom.js:5` 「죽지 않는다. 시들지도 않는다. 그냥 멈춘다. 옮기면 다시 자란다」 ·
>   `first_play.js:1930` 「초보는 안 죽는다(박사님 확정)」
>   ⇒ ★ 그래서 바닥의 그루는 **죽지도 자라지도 않고 그냥 멈춰 있다.** 사람 눈에 제일 「고장」처럼 보이는 모양이다.
> ⇒ ⇒ ⇒ 책상(등2에 1.77)보다도 어둡다. **바닥이 이 방에서 제일 나쁜 자리다.**

⚠ 창턱 게임일 40 은 [core] 가 «걸어서» 낸 d39~44 와 맞았다. 다른 자가 같은 날을 냈다.
⛔ 「방 한가운데 바닥」은 이 열다섯 칸에 **없다**(자리는 전부 가구에 붙어 있다).
  ⇒ 남은 물음은 「놓기 UI 가 사람을 어두운 자리로 이끄나」이고, 그건 **걷는 일**이다.

### 2026-09-07 · 무늬 «등급»과 «그림»이 만나나 — [leaf] 와 맞물려 닫았다

```
✔ 무늬는 화면에 «뜬다»       어린잎 ✗ · 중간잎 ✔ · 성숙잎 ✔        ([leaf] 걸어서)
⛔ 그런데 «등급»은 안 갈린다   중간·성숙 둘 다 · 화면 화소 «0» 차이   ([leaf])
★ 까닭                      그림 고르개에 «등급 인자가 없다»        (내가 잼)
```
⚠ 중간잎에서 안 갈리는 것은 **흠이 아니다** — `varie_grades.json §midCommon` 의 박사님 확정
(2026-08-16): 「중간잎은 하프문만 있는 걸로 그냥 ㄱ, 성숙 때 확률적으로 분류되는 걸로.」
⇒ 등급마다 중간잎이 다르면 그림이 등급을 «미리 말해» 버리기 때문이다.

★★ 그런데 **성숙에서도 안 갈린다.** 설계가 스스로 막혀 있다 —
감추려고 midNum 에서 등급을 지웠는데, 성숙잎이 «바로 그 midNum» 에서 파생된다.
```
[잰 것] probe_grade_vs_skin — 공용 못 중간잎 33개에서 성숙했을 때 «그림»이 뜨는 등급
   sanban 43.6% · halfmoon 24.5% · ⛔ 무늬 «없는» 기본 성숙잎 18.2% · fullmoon 13.6%
   여러 등급에 걸치는 중간잎 = ★ 36/36 — 하나도 예외가 없다
```
> 값이 무엇이든 그림은 이 분포로 뜬다. **다섯에 하나 가까이는 무늬 잎이 민무늬로 그려진다.**
> ⇒ 이 파일이 스스로 「제일 나쁜 거짓말」이라 부른 그것이다(`plant_grow.html:1630`).

★ 잇는 자리는 `setLeafSkins` 하나뿐이고 게임이 **안 잡는다**(손잡이는 성하다 — 재서 확인).
⏸ **이을지 말지는 박사님 것이다.** 짝을 내가 지으면 반대쪽 거짓말을 내가 만드는 것이다.

⚠ 그리고 「무늬가 안 뜬다」의 원인은 **둘이고 화면만 보면 같아 보인다** —
```
㉠ 구조    matNum 이 «0»              ⇒ 애초에 안 골랐다. 기다려도 안 바뀐다
㉡ 때늦음  matNum≠0 인데 key 가 민무늬  ⇒ 그림이 «아직 안 왔다». 곧 바뀐다
[잰 것] leafBirth 36 을 바로 읽으면 'leaf_mature' · 에셋이 온 뒤 읽으면 'leaf_mat13'
```
⇒ ★ 그래서 `leafSkinUsedAll()` 이 `key` 와 `midNum/matNum` 을 **따로** 낸다.


## ④ ⛔ 물린 표 — 지금 쓰면 안 되는 것

```
원룸 「등1 최고 sill:0 · 등2 최고 sill:3」   등이 방에 «붙박이였을 때»의 표다.
                                            2026-08-30 에 방을 비워 lampCounts 가 [0] 이 됐다.
                                            ⇒ 사람이 어디에 놓느냐가 값을 정한다
「모름이 최상 품질로 새어 돈이 2.5배」        ⛔ 지어낸 것이다. cropDliFromReport 가 던진다
「등 세기를 낮춰 원룸을 조정하자」            ⛔ 등이 방마다 같은 프리셋이라 3.6% 만 낮춰도
                                            반지하가 겨울에 죽는다
```

## ⑤ ⏸ 대기

```
원룸 「기준 배치」   박사님 결정. 후보 A~D 의 real 400일 표는 §10.22 에 있다 — 고르시면 그 판이 정본이 된다
좌표 조도 계약      ✅ 닫힘 (D · 9d99c92). 「바닥에 시루를 둘 수 있게」는 다른 물음 — 박사님 묶음
```

---
## ⚠ 커밋 손 (2026-09-07 · [core] 가 찾고 총괄이 세움)

**「내 파일만 `add`」로는 안 막힌다.** 저장소가 하나면 **색인도 하나**라,
남이 `add`·`git mv` 로 세워 둔 것을 다음 사람의 `git commit` 이 전부 데려간다.
`-a` 를 안 써도, 폴더째 `add` 를 안 해도 그렇다.

```
① 커밋 «전»에 색인을 본다
   git diff --cached --name-only     ⇒ 내 것 아닌 이름이 있으면 멈추고 알린다
★② 커밋에 «경로를 박는다»
   git commit -m "..." -- <내 파일들>
   ⇒ 색인에 남의 것이 서 있어도 내 커밋은 깨끗하다. 이게 핵심이다
③ add 하고 «뜸 들이지 않는다» — 세워 둔 시간이 길수록 남이 데려간다
```
⚠ 되감지 않는다. **기록이 틀렸으면 지우지 말고 「틀렸다」고 적는다.**

★ 이 창은 2026-09-06 밤 커밋 일곱(`45c3091`~`6661479`)을 확인했다 —
경로를 박아 커밋해 와서 **남의 파일이 하나도 안 딸려갔고 색인도 비어 있다.**

---
## ⚠ 이 창이 오늘 틀린 것 — 다음 사람이 같은 데 안 빠지게

```
① novice 값을 real 이라 옮겼다                    ⇒ 모드를 표 머리에 박았다
② 없는 함정을 두 창에 보고했다(함수 둘을 섞음)    ⇒ 「붉어질 수 있나」를 먼저 본다
③ 씨앗이 고정인 줄 모르고 5판을 「다섯 번」으로 셌다 ⇒ 40판으로 다시 돌렸다
④ 「등을 낮추자」를 옆방 안 보고 냈다              ⇒ 고치는 방만 보면 안 고치는 방이 죽는다
⑤ rain 을 빠뜨려 겨울 값을 낙관으로 냈다          ⇒ 「내 자가 이미 아는 답을 내나」로 걸렀다
⑥ 내 검사의 빈 머리말을 보고 「PASS 가 거짓」이라 먼저 말했다 ⇒ 60칸 다 쟀었다. 틀렸다
⑦ `die` 밴드를 「죽는다」로 읽어 두 창에 말했다        ⇒ 낱말이 «이름»이고 하는 일이 달랐다
⑧ 바닥을 통째로 재 놓고 그 수를 «안 태우고» 셈만 했다 ⇒ 안 태우면 아직 [셈]이다
⑨ 자가 GROWTH_STEPS_MAX 를 JSON 에서 읽었다         ⇒ 그 열쇠가 JSON 에 없다. `?? 2` 가 메꿔 왔다
⑩ 문지기를 by_band 의 0 으로 삼았다        ⇒ 코어는 그 0 을 안 받는다. 금이 마침 같아서 안 갈렸다
⑫ 「누가 오류를 삼켰다」고 단정했다          ⇒ 삼킨 데는 없었다. 자는 찍었고 사람이 안 읽었다
```
> **자는 틀리지 않는다. 틀리는 것은 물음이다.**
> **그리고 「[셈]인지 [잰 것]인지」를 매번 붙이면 남이 확인할 수 있다.**

> ## ★★ 2026-09-07 · [core] 것을 받는다 — **「자가 던진 줄을 «그 자리에서» 읽어라.」**
> [core] 판이 90일을 헛돌았는데, 자는 첫 줄에 「배치 실패 — x 가 숫자가 아닙니다」를 **찍고 있었다.**
> 다음 줄이 그럴듯해서 앞줄을 건너뛴 것이다. ⇒ 조용한 데가 «없었다». 소리는 났고 듣는 사람이 없었다.
> ⇒ ★ 내 「빈칸과 조용함」의 **뒷면**이다. 내 것은 «화면이 갈라 주라»이고 이것은 «읽는 사람이 읽으라»다.
> ⇒ ⇒ ⛔ 그리고 나는 그 자리를 「누가 삼켰다」고 **단정했다.** 삼킨 데는 없었다.
>   ★ 안 보이는 것에 «까닭을 지어내지» 마라. 「소리가 났는데 안 들렸다」가 더 흔하다.
>
> ## ★★ 2026-09-07 · 하나 더 — **「같은 답이 나온다고 같은 길은 아니다.」**
> 내 자는 어두운 밴드를 `by_band` 의 0 으로 막고 있었다. 그런데 코어는 그 0 을 **안 받는다** —
> `loop.js §growthSpeedOf` 가 `m < 1` 이면 mult 1 을 낸다('unsupported').
> 진짜 문지기는 `plant_grow §growthBlockReason` 의 「7일평균 < TH.min」이다.
> ⇒ 금이 마침 둘 다 2.7 이라 답이 안 갈렸다. **한쪽만 움직이면 그날 갈린다.**
> ⇒ ⇒ ★ 자를 짤 때는 「무슨 답이 나오나」가 아니라 **「정본이 무엇을 타나」**를 봐야 한다.
> ⇒ ⇒ ⇒ 붙는 버릇 하나: **조용한 폴백(`?? 2`)을 두지 마라.** 정본이 없으면 없다고 붉어야 한다.
>
> ## ★★ 2026-09-07 · 하나 더 — **「범위를 넓혀 낱말을 피했으면, 그 넓힌 값도 «태워» 봐라.」**
> 「바닥」이 방마다 다른 것을 가리켜서, 점 하나를 고르는 대신 221점을 통째로 쟀다. 거기까지는 옳았다.
> ⛔ 그런데 그 수를 손에 들고 「1.44 는 2.7 보다 작으니 0」으로 «셈»만 했다. 안 태우면 아직 [셈]이다.
> ⇒ 그래서 `probe_first_leaf` 에 `DLI=` 손잡이를 달아 «잰 상수를 그대로 400일 태우게» 했다.
>
> ## ★★ 2026-09-07 에 하나 더 붙는다 — **「빈칸과 조용함이 같아 보이면 안 된다.」**
> `test_coord_matches_slot` 은 등 1·2·3 을 «어긋날 때만» 찍었다. 그래서 다 맞으면
> 「[등 1개]」 밑이 통째로 비었고, **짠 사람인 내가** 그걸 「안 쟀구나」로 읽었다.
> ⇒ 짠 사람이 못 읽는 화면은 «화면이 잘못된» 것이다. 이제 등마다 「15칸 쟀다」와
> 본보기 한 칸을 반드시 찍고, 0칸이면 그 자리에서 붉는다.
> ⇒ ⇒ 총괄의 「0 과 전부는 자가 고장 났을 때 제일 흔히 나오는 답이다」의 **사촌**이다.
