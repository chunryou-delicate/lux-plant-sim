# 아침 보고 — 2026-08-23

> 밤새 창 여섯이 돌았습니다. **읽는 순서: §1 → §2(결정) → 나머지는 필요할 때.**
> §2 만 읽으셔도 됩니다. 나머지는 근거입니다.

---

## §1. 한 줄

**커밋 241. 판정에 쓰이는 값은 하나도 안 건드렸습니다.**
확률·배수·문턱·가격·일수·자리 수 — 그 줄이 diff 에 하나도 없습니다. `[실측: git log --since]`

바꾼 값 파일은 **둘**뿐이고 둘 다 밝힙니다:

| 파일 | 무엇 | 왜 규칙 밖인가 |
|---|---|---|
| `varie_grades.json` `midCommon.pool` | 중간잎에 **어느 그림**을 쓰나 (1갈래 → 11갈래) | **그림 갈래**이지 등급 갈래가 아님 · 총괄+[Asset] 승인 |
| `house_rooms.json` `measured` 블록 | 반지하 **실측 기록** (`peak 3.77→3.68` 등) | **기록이지 입력이 아님** — 엔진은 `room.slots` 를 스스로 계산 |

★ 둘째 것은 **방향이 반대**입니다. 방을 바꾼 것은 어제까지의 커밋(받침 이동·협탁 추가)이고,
밤에 한 일은 **낡은 종이가 뒤늦게 따라잡은 것**입니다. 값이 움직인 게 아닙니다.

---

## §2. ★ 결정해 주실 것 — **여섯**

### ① ★★ 문이 둘입니다

```
앞문   crop_mix(무순 기르기)를 못 닫으면 살림 사슬이 통째로 잠긴다
뒷문   앞문을 열면 siru16 까지 간다 — 그때 21칸이 필요한데 방은 15칸
```

> **앞을 열면 뒤에서 막힙니다. 하나씩 풀면 두 번 놀라십니다.**

**앞문 제안 (기획):** `siru5_cycle5` 의 관문에서 `crop_mix` 를 뺀다. 까닭 셋:

- `crop_mix` 가 가르치는 것은 **이득**이지 생존이 아니다. 콩나물만으로도 살림은 **돌아야** 하고 **느릴 뿐이어야** 한다.
- `siru5_cycle5` 가 가르치는 것은 **「체력이 천장이다」**이고 **무순과 아무 상관이 없다**.
  → **상관없는 줄이 문을 잠그고 있다.**
- 무순을 안 사는 것은 **안내를 어긴 것이 아니다** — 그 줄이 가려졌을 수도, 돈이 없었을 수도 있다.
  그런데 벌이 「살림 사슬 전체 잠금」이다. → **벌이 죄보다 크다.**

⚠ **퀘스트 순서가 바뀌므로 손 안 댔습니다.**

**뒷문 — 박사님이 이미 답을 보여 주셨습니다:**
어젯밤 보내신 두 장(가구를 창턱 밑에 겹쳐 4칸 · 책상 위 삼단장+서랍장)이
**정확히 이 여섯 칸을 메우는 길**입니다. ⇒ **그 배치를 게임이 허용하는지**가 실제 질문입니다.
(현재 `furnitureFit` 이 겹침을 어디까지 받는지 미측정 — 아침 첫 일감으로 두었습니다.)

### ② ★ 삽수 — 셋을 **한 번에** 정해 주십시오 (나누면 반쪽이 됩니다)

- 민무늬 삽수를 **팔기만** 막을까
- 팔 수 없게 된 삽수는 **안 죽게** 할까
- `first_cut` 을 **원룸으로** 옮길까 (튜토에서는 삽수를 막자고 하신 그것)

### ③ ★ 선반 — 기본으로 줄까(ⓐ) 사서 놓게 할까(ⓑ)

⚠ **ⓑ 라면 안내가 필수입니다.** 안내 없이 ⓑ 로 가면 **퀘스트가 막힙니다** (선반이 뒷문의 열쇠).

### ④ ★ 「등 하나 사서 창턱에 놓고 기다리기」가 의도한 길입니까

```
등 0개 → 5%      등 1개 → 100%
모주 값 = 이사 비용의 127%
서장에서 무늬를 보장하므로 「랜덤」이 사실상 사라져 있음
```
⇒ 지금은 **사실상 외길**입니다. 의도하신 것이면 그대로 두고, 아니면 여기가 손댈 자리입니다.

### ⑤ 집게형 식물등 — 없애도 됩니까

말씀하신 그대로 *"영 붙이기 어려워서 차라리 없애 버리고 싶더라"*.
⇒ 벽등·거치형은 남기고 **집게형만** 뺄지, 아니면 **붙는 방식을 고칠지**.

### ⑥ 디스크 — 55GB

워크트리 55GB · `_pipeline_test` 357MB · 중복 PNG 52MB. **지우는 것은 손 안 댔습니다.**

---

## §3. 고친 것 — 큰 것 다섯

### ★★★ 안내를 따른 판만 죽었습니다 (`ac030ee`)

가이드대로 시루를 늘리면 **첫 수확에서 판이 잠겼습니다**(hardLock).
화분 자리를 요구하는 검문이 남아 있었는데, 08-17 부터 화분은 **가방으로** 옵니다.

> **안내를 무시한 판은 안 죽고, 시키는 대로 한 사람만 죽었습니다.**

### ★★ 식물등이 겨눈 높이를 버렸습니다 (`dfc904c`)

미리보기에 `at.y` 가 빠져서 창턱 적중률이 **25%**였습니다. → **87%**.
미리보기와 실제 놓임이 **다른 답**을 내던 것이라, 미리보기 쪽이 아니라 **바탕(ghost)** 을 고쳤습니다.

### ★ 자른 삽수가 잎 등급을 잃었습니다 (`9f298c8`)

하프문을 잘라도 산반 값이 나왔습니다.

### ★ 130분짜리 헛기다림 (`47d948c`)

검사 도구가 페이지 이동 실패를 **삼키고** 있었습니다. 이제 던집니다.

### ★ QA 프로브 일곱이 저장소에 없었습니다 (`d42cd84`)

> **없는 파일은 고칠 수가 없습니다.**

**그 밖:** 낡은 등 가격 두 곳 · G-mid-4 · 침묵하던 실패 둘 · uid 69개 · 칸 정렬 3건
(닿을 수 없던 137칸 수리, **칸 수는 325 → 325 그대로**) · 잎 색변이 10장 · 낡은 썸네일 8장 ·
유령 에셋 177 → 70.

---

## §4. 화면 비율 — **일곱 해상도 전부 통과**

320 / 375 / 390 / 414 / 768 / 1024 / 1920 에서 **한 번에 하나씩** 찍었습니다.
`ready=true` · 화면 밖 0 · 가려짐 0.

⚠ **처음에 버그로 보였던 넷은 전부 오진이었습니다** — 부하 · 닫힌 시트 · 대사 잠금 · 연출.
동시에 여러 창을 띄우면 **없는 고장이 만들어집니다.** 그래서 한 번에 하나씩 돌렸습니다.

**남은 것:** 1920 에서 음악 단추가 대사에 덮임 · PC 에서 시트가 스크립트로 안 열림 ·
`tabDev` 32px 넘침 · 320 에서 식물 탭이 방을 가림. **넷 다 아침 몫.**

---

## §5. 못 한 것 · 미정 — 숨기지 않습니다

- **앞문 판정은 「미정」입니다.** 하네스가 무순을 **사는 데까지** 갔고 **놓는 데서** 막혔습니다.
  다만 재고를 손으로 꽂은 판이라 **정상 경로로 다시 봐야** 합니다.
  ⇒ 막힌 자리는 **「자리 부족」이 아니라 「재고 칸」**으로 좁혀졌습니다.
- `probe_qa_full` 한 판 · 삽수 차단(②번 결정 대기) · 온실 기록 정정 ·
  `acceptance.json` 배선 · 학원교실 112칸 · 몬스테라 놀란 표정 1장.
- ⚠ **낡은 정적 프로필** — `room_profile.banjiha.json` 이 **08-02 방**을 담고 있습니다
  (방 데이터는 08-17). 정적 프로필은 승인 뒤에 건드리라 하셔서 **손 안 댔습니다.**
  ★ 대신 **「갈렸다」고 말하는 칸을 새로 넣었습니다**(`7813f9f`) — **갈린 채로 초록인 것보다
  붉은 편이 낫습니다.** ⇒ 그래서 **house 붉음이 3 → 4** 로 늘었습니다.
  **새로 깨진 것이 아니라 원래 붉었어야 할 것**입니다.

---

## §6. 밤에 배운 것

**계율이 14 → 34 로 늘었습니다.** 그중 오늘 밤 값이 컸던 넷:

> **㉛ 판이 멈추면 나머지 칸이 전부 「거짓 초록」을 낸다** — 재는 것에도 층이 있다.
> **㉜ 계측기가 대상을 바꾼다** — 검사를 감싸자 판정이 뒤집혔다.
> **㉘ 값을 내기 전에 자를 세 번 의심하라** — ①실제로 읽는 값인가 ②놓을 수 있는 자리인가 ③이 0은 「없다」인가 「비었다」인가.
> **㉝ 마무리 줄이 위의 붉음을 덮는다** — 새 칸이 붉게 떴는데 마지막 줄이 늘 `PASS` 를 찍고 있었다.
>   **읽는 사람은 마지막 줄만 봅니다.** ⇒ *막으려던 바로 그 모양이 검사 자기 안에 있었습니다.*

### ★ 그리고 오늘 밤의 문장 둘

> **「답이 맞으니 셈을 안 봅니다.」**
> 기획 정본의 한 줄이 **값도 낡고 문턱도 낡았는데** 결론이 여전히 맞아서 안 걸렸습니다
> (`avg7 2.42→2.37` · `문턱 3.0→2.7` · 결론 「정체」는 그대로).
> **자동 검사가 절대 못 잡는 종류입니다** — 검사는 결론을 보는데, 낡은 것은 결론에 이르는 길이니까요.

> **「재는 편이 판단을 덜 하는 길이었습니다.」**
> 같은 숫자가 세 파일에 있었는데, 골라 베끼면 **그 셋 중 하나를 정본으로 만드는 셈**이라
> 베끼지 않고 **직접 다시 쟀다**는 것.

### ★★ 그리고 이것이 창 여섯으로 돌린 값어치입니다

한 창이 밤새 **자기 판정을 여덟 번 물렸는데, 그중 여섯은 다른 창이 잡아 준 것**입니다.
**혼자였으면 여섯이 그대로 남았습니다.**

⇒ 다만 잡아 준 여섯도 **남이 잡을 수 있게 근거를 적어 뒀기에** 잡힌 것입니다.
그러니 진짜 값은 **창의 수**가 아니라 **적어 두는 습관** 쪽에 있습니다.
