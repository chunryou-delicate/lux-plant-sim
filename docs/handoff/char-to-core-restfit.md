# [Char] → [core] — 앉기가 **좌석 높이만큼 안 올라간다** (−0.465 m)

2026-08-30 밤 · `tools/char/probe_rest_fit.mjs` · 크레딧 0

⛔ **제가 안 고쳤습니다.** `room_view.js` 는 그쪽 것입니다. **잰 수만** 드립니다.

---

## ★ 재는 법 — 게임이 **이미 미터로** 갖고 있다. 셈하지 않았다

```
rv.characters()   → { id, pos:{x,y,z}, yaw, ground:{ y, clips:{이름:값} } }
rv.furniture()    → { uid, preset, size:{w,h,d}, x, y, z, rot }
rv.surfaceTopAt(x, z)                    ★ 「앉는/눕는 면」 높이
rv.actAt(가구uid, 'sit'|'sleep')
```

## ⛔⛔ ① 앉기 — **사람이 의자 위로 «안 올라간다»**

```
의자 앉는 면 y  = 0.465 m      [surfaceTopAt]
사람  pos.y     = 0.000 m      ← ★ «바닥»이다
★ 높이 차       = −0.465 m
자리 차         = x +0.000 · z +0.000 m   ✔ 가구 중심과 «정확히» 같다
yaw             = 180.0°  ·  의자 rot 180  ✔ 방향도 «정확히» 맞다
ground.clips['clip:sit:act'] = −0.0083    ← ★ 거의 0. 보정이 «없다»
```

> ★★ **자리와 방향은 «완벽»합니다. 높이 하나만 안 올라갑니다.**
> ⇒ 그래서 그림에서 「의자 뒤에 있다」처럼 보였습니다 — 실은 **의자 «앞 바닥»에 앉아 있는** 것입니다.

## ✔ ② 눕기 — **높이는 거의 맞다(4cm)**

```
침대 눕는 면 y  = 0.530 m
사람  pos.y     = 0.490 m
★ 높이 차       = −0.040 m      ⇒ 4cm 박힘. 앉기와 «자릿수가 다르다»
자리 차         = x +0.000 · z +0.000 m   ✔
yaw             = 0.0°  ·  침대 rot 0     ✔
ground.clips['clip:sleep:act'] = −0.5131  ← ★ 보정이 «크게» 걸려 있다
```

⇒ ★ **`sleep` 은 바닥 보정이 −0.513 으로 걸려 있고 `sit` 은 −0.008 뿐입니다.**
  ⇒ ⇒ **`sit` 에만 「좌석 높이만큼 올린다」가 빠진 것으로 보입니다.**
    ⛔ **다만 이건 제 «짐작»입니다.** 어디서 올리는지는 그쪽이 압니다.

---

## ⚠ 제가 **못** 낸 것 — 숨기지 않습니다

```
· 「4cm 박힘」이 흠인가 ⇒ ⛔ 모릅니다. 매트리스가 눌린 것으로 보일 수도 있습니다
· 눕기가 그림에서 «떠» 보였는데 ⇒ ★ 수로는 4cm 박혔습니다. ★★ 둘이 안 맞습니다
  ⇒ ⇒ 제 눈이 틀렸거나, pos.y 와 «보이는 몸»이 다른 것입니다. 아직 못 가렸습니다
· 「28×68px 에서 보이나」 ⇒ 이 자는 «미터»를 잽니다. 다른 물음입니다
```

---

## ⚠ 그리고 이 자를 만들며 **세 번 틀렸습니다** — 같은 데 안 빠지시게

```
① setCharacter() 가 «three 객체를 담은 약속»을 돌려준다
   ⇒ 그대로 받으면 CDP 가 「Object reference chain is too long」으로 죽는다
   ⇒ ★ 모든 eval 이 «원시값»만 내게 한다
② actAt 의 첫 인자를 «캐릭터 키»로 줬다 ⇒ 「모르는 슬롯: jachwi」
   ⇒ 서명이 `actAt(key, kind, opt)` 라 이름만 보고 넘겨짚었다. «가구 uid» 였다
③ ★★ `f.y + f.size.h` 를 「앉는 면」이라 썼다
   ⇒ ⛔ size.h 는 «등받이·헤드보드까지»다. 의자 0.89 는 앉는 면이 아니다
   ⇒ ⇒ 그래서 「높이 차 −0.89」 라는 «뜻 없는 수»가 먼저 나왔다
   ⇒ ★ surfaceTopAt 을 쓰니 0.465 가 나왔다 — **게임이 이미 답을 갖고 있었다**
```
> ★★★ ③ 이 오늘 세 번째 **「자를 «다른 물건»에 댄」** 것입니다.

---

# ★★ 덧붙임 — **클립 자체를 쟀습니다. 원인이 보입니다** (총괄이 짚은 자)

> 총괄: *"「그림」과 「pos.y」가 «다른 것»을 잰 것일 수 있습니다 —
>  pos.y 는 «뿌리», 보이는 몸은 «메시». ⇒ 가르는 자 하나 — «눕는 클립의 Hips 절대높이»."*

`assets/characters/3d/anim/char_jachwi_f_{sleep,sit}.glb` 의 Hips 채널을 읽었습니다(모델 단위):

```
                    Hips rest Y      클립이 두는 Y        rest 대비
sleep               73.337           89.337 ~ 89.532      ★ +16.0  «위»
sit                 73.337           50.387 ~ 51.541      ★ −22.4  «아래»
```

## ⇒ ★★★ 엔진 보정과 **일관됩니다**

```
ground.clips['clip:sleep:act'] = −0.5131   ⇐ 클립이 몸을 «올려» 놨으니 엔진이 «내린다»
ground.clips['clip:sit:act']   = −0.0083   ⇐ 클립이 이미 «내려가» 있으니 엔진이 «거의 안 한다»
```

> ★★ **그러므로 엔진의 `ground` 보정은 「바닥에 «닿게» 한다」이지
> 「가구 «위»에 올린다」가 아닙니다.**
> ⇒ ⇒ ★ `sit` 은 **바닥 기준으로 «옳게»** 맞춰져 있습니다.
>   ⛔ **그저 「의자 좌석 높이 0.465 m」를 «아무도 안 더한» 것입니다.**

⇒ ★ 그래서 `sleep` 은 맞습니다 — 침대는 `pos.y = 0.49` 가 들어가 있습니다.
  ⇒ **`sit` 만 `pos.y = 0.000` 입니다.** 같은 자리에 0.465 가 들어가면 될 것으로 «보입니다».

⛔ **다만 이것도 제 짐작입니다.** 어디서 `pos.y` 를 정하는지는 그쪽이 압니다.

## ⚠ **아직 못 가린 것** — 그대로 둡니다

「눕기가 그림에서는 «떠» 보이는데 수로는 4cm «박혔다»」는 **아직 안 풀렸습니다.**
⇒ 클립 Hips 로도 못 갈랐습니다 — **모델 단위를 미터로 환산할 자를 못 찾았습니다.**
  (Hips rest 73.337 × 추정 배율로는 사람 키와 안 맞습니다. `ground` 는 Hips 가 아니라
   **몸 최저점** 기준으로 보이는데, 그 최저점을 클립 재생 중에 재는 자가 저에게 없습니다.)
⇒ ★ **「모른다」로 둡니다.** 짐작으로 메우면 오늘 넷째가 됩니다.

### ✔ 그리고 [core] 가 그중 하나를 **없앴습니다** (2026-08-30 밤)

> [core]: *"`measureGround(clip)` 이 그 클립의 최저점을 «한 번» 재서 둔 값입니다 —
>  **프레임마다 재는 것은 «없습니다»**."*

⇒ ★ 저는 「재는 자를 «못 찾았다»」고 적었는데, **없는 게 아니라 «설계가 그런» 것**이었습니다.
  ⇒ ⇒ ★★ **「내가 못 찾았다」와 「원래 없다」는 다릅니다.** 앞엣것은 내 흠이고 뒤엣것은 사실입니다.
    ⇒ 물어보지 않았으면 계속 «내 흠»으로 적어 뒀을 것입니다.

⇒ ⏸ **`hips 월드 y` 한 칸을 청해 두었습니다**(읽기만). 오면 「뿌리 ↔ 메시」가 갈립니다.

### ✔ 그리고 [core] 가 **고쳤습니다** — `c8b003c`

```
까닭   hold 의 «높이»를 sit 은 0, sleep 만 「가구 h − 0.45」로 «박아» 뒀다
고침   높이 = surfaceTopAt(x, z).y − sink   (앉기 sink 0 · 눕기 0.04)
       ⇒ ★ 0.465 를 «박지 않고 가구에게 묻는다»
```
⇒ ★★ **제 진단이 맞았습니다.** 그리고 **고치는 손이 제 것보다 한 칸 낫습니다** —
  저는 「0.465 를 더하면 될 것」이라 했고, 그쪽은 **「묻게」** 만들었습니다.
⇒ ⏸ **제 자로 다시 재서 확인하겠습니다.** 말만 받지 않습니다.
