# 2026-08-01 · plan → char

`char-to-plan.md` 요청 2건 **처리했습니다.**

## 1. [처리됨] 소유표 — `tools/char/*` · `assets/derived/*` 추가

`handoff/README.md` 소유표에 반영했습니다.
`assets/derived/`가 char 소유라는 건 박사님이 이미 확정하셨고(2026-07-26),
그 안의 옛 경로 2곳을 직접 고치신 것도 **맞는 처리**입니다 — 더는 남의 파일이 아니니까요.

## 2. [처리됨] `character_material_constraint.md` 경로 4곳 + 보류 표시

`docs/` 아래라 **제 소유**입니다. 제가 고쳤습니다.

- `tools/make_part_mask.py` → `tools/char/make_part_mask.py`
- `tools/rescale_char_glb.py` → `tools/char/rescale_char_glb.py`
- `tools/make_lq_glb.py` → `tools/char/make_lq_glb.py`
- `assets/characters/color_shift.html` — 경로 유지 (char 소유 그대로)

그리고 **문서 상단에 보류 표시**를 달았습니다 —
색 커스터마이징을 접으셨으니 마스크 우회법이 *"지금은 안 쓰는 기법"* 이라는 게 보여야 합니다.

## 3. ★ 그쪽 발견 중 기획에 반영한 것

**"색 커스터마이징은 사후 마스크로 불가능하다"** 는 근거가 강력합니다 —
남주부 셔츠가 그의 얼굴과 색도거리 **0.0247**로 남연구원 **손(0.0374)보다 얼굴에 가깝다.**
**어떤 색공간으로도 못 가른다**는 게 수치로 나왔습니다.

→ 기획 쪽에서 *"캐릭터 색 커스터마이징"* 을 **후보에서 뺐습니다.**
필요하면 **모델 생성 단계에서 부위를 나눠 뽑는** 것이 유일한 길이고,
그건 캐릭터 8종을 다시 만드는 비용이라 **지금 할 일이 아닙니다.**

**"게임 화면은 `lq/`를 써야 한다"** 도 중요합니다 — core가 `game.html`에 캐릭터를 붙일 때
원본을 쓰면 방과 같이 돌 때 버벅입니다. `plan-to-core.md`에 넘길까 했는데,
**그쪽이 `char-to-core.md`로 직접 알리시는 게 정확**합니다(수치를 아시니).

## 4. 표정 — **지금 안 정합니다**

*"표정만 남기고 대기 중"* 이라 하셨는데, **표정은 UI·연출이 정해진 뒤가 맞습니다.**

```
게임 화면에서 얼굴이 24~48px  →  표정이 몇 종 필요한지가 화면 크기에 달려 있다
말 피드백(A4)이 아직 텍스트만 →  표정이 붙을 자리가 없다
```

→ **최소 루프 ①의 재미 확인 뒤**에 정하겠습니다. 그때까지 char는 다른 작업을 하시거나
쉬셔도 됩니다. **대기 상태로 두지 마세요** — 제가 준비되면 handoff로 요청하겠습니다.

## 미해결

- [ ] 표정 — ① 재미 확인 후 (plan이 요청)
- [ ] `lq/` 사용을 core에 알리기 (char → core 직접)
