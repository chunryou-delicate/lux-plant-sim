# 2026-08-05 · qa → plan (배포본을 폰으로 해 본 기록)

대상: <https://chunryou-delicate.github.io/lux-plant-sim/game.html> (커밋 `4908256`)
방식: 헤드리스 크롬 390×844 · dpr 2 · `Emulation.setTouchEmulationEnabled` 를 켠 뒤
**CDP `Input.dispatchTouchEvent` 로 진짜 터치를 넣었다.** DOM 이벤트를 흉내 내지 않았으므로
크롬이 touchend 뒤에 쏘는 호환 마우스 이벤트("유령 마우스")도 실제로 같이 났다.

새로 만든 도구(기존 파일은 하나도 안 건드렸다):
`tools/probe_qa_boot.mjs` · `probe_qa_play.mjs` · `probe_qa_break.mjs` ·
`probe_qa_firstrun.mjs` · `probe_qa_ghostmouse.mjs` · `probe_qa_perf.mjs` · `probe_qa_slowphone.mjs`
스크린샷: `docs/engine/shots/qa/`

**나는 아무것도 안 고쳤다.** 아래는 **본 것**과 **코드를 읽고 짐작한 것**을 갈라 적었다.

---

## 이 기록의 한계 — 먼저 밝힌다

1. **다른 작업창 넷이 같은 기계에서 헤드리스 크롬을 동시에 돌리고 있었다.**
   재는 동안 크롬 프로세스가 30~44개까지 올라갔고, 부팅이 240초 제한을 넘겨 한 번 죽었다.
   ⇒ **프레임·시간 숫자는 못 믿는다.** 그래서 이 문서에 성능 수치를 안 적었다.
   "렉이 있나 · 프레임이 언제 떨어지나"는 **기계가 조용할 때 다시 재야 한다.**
2. 뒤쪽(**삽수 자르기 · 분갈이 · 팔기 · 식물등 · 가을 · 잭팟 · 원룸 이사**)은
   손으로 20일 넘게 눌러야 닿는데, 위 사정으로 **거기까지 못 갔다.** 안 본 것은 안 봤다고 적는다.

---

# ■ 막힘 (게임이 안 굴러감)

### [막힘] 가방을 열고 초록 버튼 [▶ 첫 플레이 자동 시뮬레이션]을 누르면 지금 하던 판이 지워진다

- **무엇을 했나**: 첫 화면 → [가방] → 가방 탭 맨 아래 초록색 큰 버튼을 누른다.
- **무엇을 기대했나**: 플레이어가 볼 버튼이 아니거나, 적어도 확인을 묻는다.
- **무엇이 일어났나(코드)**: `game.html:3592 playFirstPlayScenario()` 첫 줄이
  `S = newState({ room:'banjiha', mode:'novice', firstPlay:true, ... })` 다.
  **지금 상태를 통째로 새 판으로 갈아친다** — 날짜·소지금·화분·삽수·상점이 다 날아간다.
  묻지 않는다. 그리고 이어지는 `draw()` 가 `saveSoon()` 을 태우므로
  **지워진 판이 세이브에 덮여 쓰인다.**
  그 뒤 4일을 `nextDay` 로만 돌리는데(물을 안 준다), 물이 회전 시작이 된 지금 규칙에서는
  콩나물이 자라지 않아 `game.html:3620` 의
  `if (!pot0(S) || !sill) throw new Error('첫 플레이 자동 시뮬: 몬스테라 또는 창턱 슬롯이 없습니다')`
  로 떨어질 것으로 보인다. 그 예외는 `isRecoverable` 이 아니어서 `game.html:3757` 이
  `hardLock='1'` 을 걸고 `#play`·`#firstRun` 을 끈다.
- **얼마나 자주**: 코드 경로상 늘. **실측은 못 했다**(아래 §못 한 것).
- **어디가 원인일 것 같나**: 버튼이 검수용인데 `#pageBag`(플레이어의 가방 탭) 안에 있다
  — `game.html:819`. HTML 에는 `disabled` 로 적혀 있지만 부팅 끝의
  `GATED.forEach(id => $(id).disabled = false)`(`game.html:3774`, GATED 에 `'firstRun'` 이 있다)
  이 **다시 켜 준다.** 그 뒤 아무도 안 끈다.
- **스크린샷**: `docs/engine/shots/qa/cpu1_02_bag.png` (초록 버튼이 그대로 보인다)
- ★같은 이유로 [30일 자동]·[▶ 돌려보기]·[검수] 탭도 플레이어에게 열려 있다.

### [막힘] 첫 플레이가 끝난 뒤 [▶▶ 빨리감기]를 누르면 판이 잠긴다 (짐작 — 코드만 읽었다)

- **무엇을 했나**: 첫 플레이(말린 새순)를 끝낸 뒤 [빨리감기]를 누른다.
- **무엇이 일어날 것 같나**: `loop.timeModeOf` 는 `fp.completed` 면 `'fast'` 를 낸다
  (`src/game/loop.js:941`). `'fast'` 모드는 **maxDays 가 반드시 있어야 한다** —
  ```js
  if (!Number.isFinite(maxDays))
    throw new Error('[빨리감기] 배속에는 maxDays 가 필요합니다 — 무한 진행은 없습니다');
  ```
  (`loop.js:1004`). 그런데 화면(`game.html:3410`)은 `msPerDay` 만 주고 **maxDays 를 안 준다.**
  이 예외는 `firstPlayInput`·`tutorialInput`·`turnState` 가 없어서
  `isRecoverable` 이 false → `guard()` 가 `GATED` 를 전부 끄고 `hardLock='1'` 을 건다
  (`game.html:2504`). `unlockAfterFix()` 는 `hardLock==='1'` 이면 **안 풀어 준다**(`game.html:2496`).
  ⇒ [다음 날]도 [빨리감기]도 죽고, **새로고침 말고는 살아날 길이 없다.**
- **얼마나 자주**: 코드 경로상 늘. 실측은 못 했다.
- **원인 짐작**: 튜토 이후 배속에 상한을 넣으면서(`FAST_MODE_MAX_DAYS = 30`)
  호출부에 maxDays 를 안 붙였다. 코어는 "조용히 낮추지 않는다"는 뜻으로 던지는데,
  화면이 그 던짐을 **고칠 수 있는 실수**로 안 읽는다.

### [막힘] 오류가 나도 **화면에 아무 말이 안 뜬다** — `#side` 가 없다

- **무엇을 했나**: 아무 오류(돈 부족 주문, 재고 없이 자르기, 위의 빨리감기)를 낸다.
- **무엇을 기대했나**: "⚠ 오늘은 진행되지 않았습니다 …" 안내가 뜬다.
- **무엇이 일어나나**: `game.html:1110` 이 `const side = $('side')` 인데
  **문서에 `id="side"` 인 요소가 없다**(폰 세로로 바꾸면서 없어졌다 — 부팅 실패 처리
  `game.html:3800` 에는 "★#side 는 폰 세로로 바뀌며 없어졌다"고 적혀 있고 그쪽만 고쳤다).
  그래서 `guard()` 의 catch 마지막 부분
  ```js
  side.insertAdjacentHTML('afterbegin', `<div class="err">…`);   // game.html:2522
  console.error(e);                                              // 여기까지 못 온다
  ```
  가 **TypeError 로 다시 터진다.** 결과는 셋이다:
  ① 플레이어에게 아무 말도 안 뜬다 ② `console.error` 도 안 남는다
  ③ catch 안에서 던진 예외가 클릭 핸들러 밖으로 새어 나간다.
  같은 줄이 `game.html:3750`(시나리오)·`3763`(첫 플레이 시뮬)에도 그대로 있다.
- **원인 짐작**: 확실하다고 본다 — `id="side"` 로 검색하면 문서에 없다.
- ⇒ 앞의 두 [막힘]이 **"버튼이 그냥 죽었다"** 로만 보이는 이유가 이것이다.

---

# ■ 틀림 (굴러가지만 잘못됨)

### [틀림] 콩나물 시루를 어두운 데 놓는데 "**고사**"라고 말한다

- **무엇을 했나**: 첫 화면 → [가방] → 시루 그림을 방 바닥으로 끌었다.
- **무엇을 기대했나**: 첫 학습이 "어두운 자리에 놓으세요"이므로 좋은 자리라고 말해 준다.
- **무엇이 일어났나**: 손가락 옆 라벨이 `여기 · DLI 0.2 · **고사**` 였다.
  가르치려는 자리에서 죽는다고 말한다.
- **얼마나 자주**: 늘 (놓아 본 모든 자리에서 DLI 로만 판정한다).
- **어디가 원인일 것 같나**: `game.html:2934` 근처 `drag.label(hit)` 이
  ```js
  const s = io.light.dliAt({...}, { …, plantId: 'monstera_deliciosa' });
  return `여기 · DLI ${s.dli.toFixed(1)} · ${s.ko}`;
  ```
  **무엇을 끌고 있든 몬스테라 기준으로 판정한다.** 콩나물을 끌 때도 마찬가지다.
- **관측 로그**: `drag.on(끄는 중)={"on":true,"best":true,"lb":"여기 · DLI 0.2 · 고사"}`

### [틀림] 첫 수확 뒤 "새로운 화분이 옵니다"라고 해 놓고 **두 회전을 더 돌아야** 온다

- **무엇을 했나**: 시루를 놓고 물 → 5일 → 거두기. 그 뒤 계속 [다음 날].
- **무엇을 기대했나**: 말한 대로 곧 몬스테라가 온다.
- **무엇이 일어났나**: 6일차부터 22일차까지 화면 아래 안내가 계속
  `콩나물을 수확했습니다. 새로운 화분이 옵니다.` 인데 화분은 0개였다.
- **얼마나 자주**: 늘.
- **어디가 원인일 것 같나**: `fp.phase` 가 **첫 수확에서 바로** `'monstera_gift'` 로 넘어간다
  (`src/game/first_play.js:886`). 그런데 도착 규칙은
  `MONSTERA_ARRIVAL_RULE = { harvestCount: 3, sirus: 2, sirusHarvestCount: 2 }`
  (`first_play.js:349`)라 **3회전**(또는 시루 2개로 2회전)이 필요하다.
  화면 문구표(`game.html:2339`)는 단계 이름만 보고 적는다.
  ⇒ 단계 이름과 도착 조건이 어긋나 있다. 남은 회전 수를 말해 주는
  `monsteraArrivalLeft(fp)` 가 이미 있는데(`first_play.js:373`) 화면이 안 쓴다.
- **관측 로그**: `docs/engine/shots/qa/cpu1_log.txt` (일 6~22, `화분:0`, `phase:"monstera_gift"`)

### [틀림] [초기화]를 눌러도 **사 둔 식물등은 그대로 남는다**

- **무엇을 했나**: (코드만 읽었다) [가방] → [방] 탭 → [초기화].
- **무엇이 일어나나**: `game.html:3515-3517`
  ```js
  const keep = { room: S.home.room, mode: S.sim.mode, lamps: { ...S.lamps } };
  S = newState({ … });
  S.lamps = keep.lamps;        // ★ 등 개수를 되돌려 놓는다
  ```
  새 판의 `S.tutorial.lamp.owned` 는 0 인데 조도 계산이 읽는 `S.lamps.count` 는 옛 값 그대로다.
  ⇒ 처음부터 시작한 반지하가 **이미 밝다.** [식물등 사기]를 또 사면 두 배가 된다.
- **얼마나 자주**: 등을 산 적 있는 판에서 초기화하면 늘. **실측은 못 했다.**
- ★[초기화]는 **묻지 않는다**(confirm 이 없다). 한 번 잘못 누르면 세이브까지 지운다
  (`save.clear(localStorage, save.SAVE_KEY)`, `game.html:3530`).

---

# ■ 거슬림 (보기 나쁨)

### [거슬림] 손가락 안내 말풍선이 화면 **왼쪽 밖으로 잘린다**

- **무엇을 했나**: 첫 화면에서 [가방]을 열었다.
- **무엇이 일어났나**: 주황 말풍선이 `…끌어서 방 안 어두운 자리에` 로 **앞이 잘린 채** 떴고,
  그 위의 제목 "첫 플레이 · 자리의 차이"와 설명 줄을 덮었다.
- **얼마나 자주**: 늘 (시루 썸네일의 화면 x 가 63px 인데 말풍선이 200px 남짓이다).
- **어디가 원인일 것 같나**: `hintAt()` (`game.html:1292`)
  ```js
  h.style.left = (r.left + r.width / 2) + 'px';    // #hint 는 transform:translate(-50%,0)
  ```
  **좌우를 화면 안으로 물리지 않는다.** `#hint .say` 는 `white-space:nowrap` 이라 줄도 안 바뀐다.
  ★같은 파일 `buildGuide()` 는 30줄 아래에서
  `Math.max(8, Math.min(innerWidth - tw - 8, …))` 로 이미 물리고 있다 — 그 처리만 빠졌다.
- **스크린샷**: `docs/engine/shots/qa/cpu1_02_bag.png`

### [거슬림] 위쪽 자원 칩 석 장이 폰 세로에서 두 줄로 접힌다

- 390px 에서 `아침/Day` · `첫 수확일 식비` 까지가 한 줄, `아낀 식비` 가 다음 줄로 내려가고
  그 오른쪽이 통째로 빈다. 방 화면 높이를 200px 가까이 먹는다.
- **스크린샷**: `docs/engine/shots/qa/cpu1_01_start.png`
- 원인은 안 팠다. **모르겠다.**

### [거슬림] 콘솔에 계속 뜨는 경고 둘

- `THREE.WebGLRenderer: Texture marked for update but image is undefined` — 부팅 동안 열 번 넘게.
- `[방] 몬스테라 갱신 실패 — 모르는 슬롯: null (방 banjiha)` — 화분이 아직 없는데 부른다.
- 게임은 굴러간다. 다만 진짜 오류가 났을 때 이 소음에 묻힌다.
- (`https://chunryou-delicate.github.io/favicon.ico` 404 도 뜨는데 이건 GitHub Pages 루트 문제다.)

---

# ■ 눌러 봤는데 **멀쩡했던** 것 — 이것도 정보다

### 물 주기 — 캐릭터는 **걸어갔다.** 박사님 증상을 재현 못 했다

박사님이 "물부으로캐릭이 이동안하는거같어"라 하신 건데, 배포본에서 **한 번도 안 났다.**
1일차에 [💧 콩나물에 물 주기]를 손가락으로 누른 결과:

```
actAt = { key:"free:crop_01", kind:"water", walk:1, phase:"act", done:true, fail:null, ms:3681 }
게이지 = { acting:true, bar:"가는 중" }
캐릭터 = (-1.14, 1.26) → (-0.38, 0.45)   이동 1.61m
```

걷고(게이지 "가는 중") · 서고 · 모션하고 · 끝났다. **계약 순서대로 돌았다.**

★그래도 **아니라고 단정하지 않는다.** 내가 못 본 조건이 셋 남아 있다:
1. **느린 폰**(CPU 4배)에서는 못 재 봤다 — 기계가 다른 창들 때문에 이미 느렸다.
2. **시루가 여럿**일 때, **다시 심은 뒤**의 물주기는 못 봤다(씨앗 사는 길을 내가 놓쳐서
   1회전에서 멈췄다).
3. **화분을 구석에 몰아넣었을 때** `onFail('가지 못했습니다')` 로 빠지는 경우를 못 만들었다.
   이때 `doAct` 는 **논리도 안 돌린다**(`game.html:2043`) — 즉 물이 안 들어간다.
   박사님이 보신 게 이것이면 "캐릭이 안 움직인다"가 아니라 "물이 안 들어간다"로 보였을 수 있다.
   ⇒ **이 셋을 조용한 기계에서 다시 재야 한다.**

### 그 밖에 확인된 것

- 시루를 **끌어서** 방 아무 데나 놓는 자유 배치가 된다(`free:crop_01` 열쇠로 붙었다).
  끄는 동안 격자·링·미리보기가 켜진다.
- 첫 플레이 중에도 상점 상자가 열려 있다(`drawShop` 이 `S.tutorial.enabled` 만 본다) —
  2e59f63 에서 고친 그대로다.
- 씨앗이 없으면 [🌱 다시 심기]가 회색으로 남고 문구가 `🌱 콩 씨앗이 없습니다` 로 바뀐다.
  말풍선도 `🌱 씨앗이 없습니다` 로 뜨고 누르면 [방] 탭이 열린다. 막다른 길은 아니다.
- 대사·안내판은 손가락 탭으로 잘 넘어간다(13번 탭해서 인트로를 다 넘겼다).

---

# ■ 못 한 것 (다음 사람이 이어서 할 것)

기계가 다른 작업창들의 헤드리스 크롬으로 가득 차서 **부팅이 240초를 넘겨 죽는** 상황이었다.
아래는 도구는 다 짜 뒀지만 **결과를 못 받은 것**이다. 조용할 때 그대로 돌리면 된다.

| 무엇 | 도구 | 왜 중요한가 |
|---|---|---|
| [첫 플레이 자동 시뮬레이션]을 실제로 눌러 보기 | `probe_qa_firstrun.mjs` | 위 첫 번째 [막힘]의 실측 |
| 버튼 연타 · 동작 중 다른 버튼 · 돈 0원 주문 · 대사 중 뒤 누르기 · 빨리감기 도중 · 세이브 이어하기 · [초기화] | `probe_qa_break.mjs` | "판이 잠기나"의 실측 |
| 터치로 고른 것이 유령 마우스에 풀리나 (가구·캐릭터·시루) | `probe_qa_ghostmouse.mjs` | 박사님이 못 박아 두신 함정 |
| 느린 폰(CPU×4)에서 물주기 걷기 | `probe_qa_slowphone.mjs` | 박사님 증상 재현 |
| 프레임이 언제 떨어지나 (rAF 간격·longtask 실측) | `probe_qa_perf.mjs` | "렉이 있나"의 숫자 |
| 몬스테라 도착 → 창가로 옮기기 → 자르기 → 분갈이 → 팔기 → 식물등 → 가을 → 잭팟 → 이사 | `probe_qa_play.mjs` (도착까지 자동) | **뒤쪽 절반을 아무도 안 봤다** |
| 시차(시루 여럿 · 날 달리 물주기)로 매일 3,000원이 들어오나 | 없음 | 코드상으로는 `dailyCropSaveWon` 이 시루 수와 무관한 3,000원 상한이고, 같은 날 거두면 3,000/2,000/1,000 으로 깎인 뒤 곳간 상한을 넘는 몫이 쉰다(`first_play.js:842~880`). **화면에서는 안 봤다.** |

돌리는 법:
```
node tools/probe_qa_firstrun.mjs
node tools/probe_qa_break.mjs
node tools/probe_qa_ghostmouse.mjs
QA_CPU=4 node tools/probe_qa_slowphone.mjs
node tools/probe_qa_perf.mjs
node tools/probe_qa_play.mjs
```
모두 배포본(`https://chunryou-delicate.github.io/lux-plant-sim`)을 본다.
`BYEOT_URL` 로 바꿀 수 있다. 전부 워치독이 붙어 있고 `_wd.unref()` 를 부른다.
