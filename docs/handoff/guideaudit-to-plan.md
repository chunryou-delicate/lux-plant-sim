# 2026-08-15 · guideaudit → plan (한 바퀴를 밟고 화면이 말하는지 봤다)

박사님 지시: *"가이드 다시 잘보면서 누락된거나 필요한거나 버그 없는지 봐봐.
여분 구매부터 채소 잔여분 판매까지."*

**아무 파일도 안 고쳤다.** 이 문서 하나만 새로 썼다.

## 먼저 세 줄

1. **잉여 판매는 실제로 돈다.** 겹쳐 거두면 잉여가 쌓이고, [상점]의 [잉여 채소 넘기기]가
   뜨고, 눌리고, 지갑에 들어가고, 곳간은 한 푼도 안 줄고, 껐다 켜도 안 사라진다 — 전부 실측했다.
   **화면이 말하는 값은 85% 다**(정본과 같다). 「70%」는 소스 주석에만 남은 낡은 글이다.
2. **그런데 잉여가 생긴 것을 아무도 안 알려 준다.** 아래 한 줄도 배너도 [식물]도 [방]도 침묵한다.
   [상점] 탭을 우연히 열어야 안다. 수확 기록은 *"1,000원을 못 받았습니다"* 라고만 하고
   **팔 수 있다는 말을 안 한다**(`cropsale-to-plan §7-3` 이 이미 적어 둔 그대로다).
3. **판을 깨는 버그를 하나 찾았다** — 이미 찬 추천 자리에 화분이 **겹쳐 앉는다**(§2 버그 1).
   한 자리에 시루 셋을 완전히 같은 좌표로 세웠다.

---

## 0. 어떻게 쟀나

| | |
|---|---|
| 서버 | `python tools/serve.py 8963` (이미 떠 있었다 · 다른 워커와 같이 씀) |
| 화면 | Chrome CDP · **390×844 · dpr 2** · `mobile:false` |
| 판 | 매번 `localStorage.clear()` 로 **새 판**. 반지하 · 첫 플레이 켬 |
| 도구 | 임시 프로브 `tools/_probe_gaudit{,2,3,4,5}.mjs` 5벌 — **다 지웠다** |
| 대사 | `#dlgSkip` → `#dlgBox` 눌러 걷음 → `#guideClose`. 대사 문구는 이 감사의 대상이 아니다 |

**끈 것은 없다.** 빨리감기도 안 썼다 — [다음 날] 단추를 눌렀다.

### 지름길을 쓴 곳 (셋)

| 어디 | 무엇을 |
|---|---|
| 프로브 2·3·4 | 상점을 안 거치고 `S.shop.stock` 에 시루·씨앗을 **직접 넣었다**(배송 2일을 기다리지 않으려고).<br>★ **프로브 1은 지름길이 하나도 없다** — 상점에서 실제로 주문하고 배송을 기다렸다 |
| 프로브 3 §살림 장부 | `S.firstPlay.completed = true` 를 손으로 세워 「첫 플레이가 끝난 판」을 흉내 냈다.<br>**진짜 50일을 밟은 것이 아니다** |
| 프로브 4 | 놓기는 `window.__drag.begin/move/end` 로 했다(`test_uiwire.mjs` 방식) |

★ 아래에서 **「화면 실측」**은 브라우저에서 실제로 읽은 글자이고, **「코드에서 읽은 것」**은
파일을 읽어 안 것이다. 이 저장소가 그 둘을 섞어 여러 번 헛짚었으므로 갈라 적는다.

---

## 1. 한 바퀴 — 대목별로

### 1-1. 상점에서 여분 주문 (시루 3 · 씨앗 4)

**화면이 하는 말** (실측 · Day 0)

```
인터넷 주문 · 소지금 1,500,000원
주문하면 하루 이틀 뒤에 옵니다. 취소는 안 됩니다.
콩 씨앗 (1시루분)  1일 뒤 도착   500원  [주문]
콩나물 시루 (차광 용기)  2일 뒤 도착  5,000원  [주문]
```

[주문]을 누르면 그 자리에서 `정말 주문?` 으로 바뀌고, 한 번 더 눌러야 나간다.
배너 `주문했습니다 / 콩 씨앗 (1시루분) · 1일 뒤 도착` · 기록 `🛒 콩 씨앗 (1시루분) 1개 주문 — 500원 · 1일 뒤(Day 1) 도착`

**실측** 시루 3 + 씨앗 4 = 17,000원. 소지금 1,500,000 → **1,483,000**. 맞다.

**빠진 말** — [주문]은 늘 **1개씩**이다. 넷을 사려면 여덟 번 눌러야 한다(되묻기 때문에).
코드에서 읽은 것: 「오는 중」 줄(`#shopPending` · game.html:3471~3476)은 `qty > 1` 일 때만 개수를 적으므로
1개짜리 주문 넷은 **이름이 네 번 늘어선다.** 화면으로는 안 봤다.

**판정** ○ (걸음 자체는 잘 돈다) · **고칠 안** 수량 고르개는 이 대장 밖이다. 판단 필요(☐).

---

### 1-2. 배송을 기다림 → **도착**  ★조용히 지나간다

**화면이 하는 말 — 없다.** (프로브 5 · 지름길 없음)

Day 0 에 콩 씨앗을 주문하고 [다음 날]을 눌러 Day 1 로 갔다. 그날 화면은 —

| 자리 | 실측 |
|---|---|
| 배너 `#event` | **안 뜬다** (`classList.contains('on') === false`) |
| 아래 한 줄 `#quest` | `시루에 물을 주면 칸이 돌기 시작합니다 — 5칸이 하루에 하나씩 찹니다.` (도착과 무관) |
| 아래 띠 [가방] | 아무 표시 없음 (`class=""`) |
| 가방 개수 `#bagCount` | `· 1개` 로 는다 |
| 기록 `#log` | `📦 콩 씨앗 (1시루분) 1개가 도착했습니다` ← **여기만 말한다** |

**코드에서 읽은 것** — `shop.js:523` 이 `shop_arrived` 사건을 만들지만,
배너를 내는 `showFirstPlayEvents`(game.html:5040~5058)가 배너로 올리는 것은
**수확 · 몬스테라 도착 · 말린 새순** 셋뿐이다. 배송 도착은 그 목록에 없다.

**판정** **✕** — 「하루 뒤에 옵니다」라고 약속해 놓고 **온 날 화면이 말하지 않는다.**
기록칸을 안 여는 사람은 며칠 뒤 상점에 들어가서야 안다.

**고칠 안** 도착한 날 배너 한 줄 — `📦 콩 씨앗 1봉지가 왔습니다` / `가방에서 꺼내 심으세요` (17자·13자)

---

### 1-3. 가방에서 방으로 놓기

**화면이 하는 말** (실측 · 끄는 동안 손가락 위 `#dropLabel`)

```
빈 바닥      여기 · 하얗고 아삭 · 시루 1개 · 씨앗 1봉지 (가방에 2개 남음)
추천 자리    서랍장 1번 칸 · 하얗고 아삭 · 시루 1개 · 씨앗 1봉지
못 놓는 곳   벽에는 못 놓습니다 — 위를 향한 면이 아닙니다
겹치는 곳    다른 화분과 겹칩니다 (0.11m 모자랍니다)
```

**판정** ○ — **P-05 는 실제로 붙어 있다.** 추천 자리에서도 이름 **뒤에** 밝기 말(`하얗고 아삭`)이 따라온다.
`guide-to-plan` 이 지적한 「추천 자리에서 밝기 말이 사라진다」는 고쳐졌다. 화면으로 확인했다.

⚠ **그런데 그 라벨이 거짓말을 할 때가 있다** — §2 버그 1.

---

### 1-4. 심기 · 물 주기

콩나물은 **놓기 = 심기**다. 놓자마자 배너 `시루를 하나 놓았습니다 / 씨앗 1봉지 · 방에 4개`,
말풍선 `💧 물 주기`, 아래 한 줄 `시루에 물을 주면 칸이 돌기 시작합니다 — 5칸이 하루에 하나씩 찹니다.`

**★ 누른 뒤 3초 동안 무슨 일이 나나 — 재 봤다** (프로브 3 · 0.3초마다 잼)

```
누르기 전   💧 물 주기            acting=false
+0.3s      💧 물 주기            acting=true   게이지 0%
+1.2s      💧 물 주기            acting=true   게이지 14%
+2.1s      💧 물 주기            acting=true   게이지 42%
+3.0s      💧 물 주기            acting=true   게이지 100%
```

하단 막대 `#actBar` 는 **한 번도 안 뜬다**. 코드에서 읽은 것: `actBar()`(game.html:3691~3697)가
**말풍선이 진행을 보여 줄 수 있으면 하단 막대를 일부러 감춘다.** 설계 그대로다.

**판정** △ — 진행을 **보여 주기는 한다**(말풍선 테두리 게이지). 그런데 **글자가 안 바뀐다** —
3초 내내 `💧 물 주기` 다. 게이지는 가늘고 말풍선 안에 있어 눈에 잘 안 띈다.
⇒ 「안 눌렸나?」 하고 다시 누르게 되고, 그때 §2 **버그 2**가 난다. 두 사고가 이어져 있다.

**고칠 안** 걷는 동안 글자를 `🚶 가는 중…` 으로 바꾼다(하단 막대가 쓰던 그 말이 이미 있다 — 3777행).

---

### 1-5. 며칠 → 수확

자라는 동안 아래 한 줄 `콩나물이 자라는 중입니다. 수확까지 3일.` · 시루 줄 `2/5칸 · 3일 남음`.
익은 날 아래 한 줄 `콩나물을 거둘 때가 됐습니다.` · 말풍선 `🥣 거두기` · 손가락 `거둘 때가 됐습니다`.

**판정** ○

**⚠ 빠진 말 — 물을 안 준 시루가 있어도 아래 한 줄이 한 번도 말하지 않는다.**
실측(프로브 1): 시루 4를 Day 2 에 놓고 물을 못 줬다. Day 7 까지 아래 한 줄은
「자라는 중」 → 「거둘 때가 됐습니다」 → 「씨앗이 왔습니다」만 했고 **닷새 동안 그 시루를 안 말했다.**
[식물] 탭을 열면 `물을 기다립니다 (5일째) — 칸이 안 찹니다` 라고 아주 잘 말한다(좋은 문구다).
말풍선 `💧 물 주기` 도 떠 있다. **하지만 아래 한 줄에는 없다.**
수확할 때만 기록에 `⚠ [수확] 아직 물을 안 준 시루가 1개 있습니다 — 물을 줘야 회전이 시작됩니다` 가 뜬다.

**고칠 안** 아래 한 줄에 한 갈래를 더 넣는다 — `물을 안 준 시루가 1개 있습니다 (5일째)` (22자 · 한 줄)

---

### 1-6. 수확 → 곳간에 쌓임

배너 `🥣 콩나물을(를) 거뒀습니다 / 하얗고 아삭 · 곳간에 3,000원`
기록 `🥣 수확 — 콩나물 1개 · 하얗고 아삭 · 3끼 상당 · 3,000원`
아래 한 줄 (G-06a ①) 화면 실측 `5일이면 한 판입니다 · 식비 3,000원을 아꼈습니다` — **뜬다.** 확인.

**⚠ 곳간에 얼마가 쌓였는지는 첫 플레이 동안 어디에도 안 뜬다.**
살림 장부(`#ledger`)가 `곳간에 남은 것 4,200원` 을 적기는 한다 — 화면으로 확인했다(지름길로 만든 판).
그런데 `drawLedger`(game.html:2958)가 **`fp.completed` 일 때만** 그린다.
⇒ 첫 플레이 50일 동안 곳간을 말하는 것은 **수확 배너의 「곳간에 3,000원」 한 줄이 전부**다.
그 줄은 6초 뒤 사라진다.

⚠ 지시서의 「곳간이 화면에 한 글자도 안 뜬다」는 **반만 맞다** — 첫 플레이 동안은 맞고,
끝난 뒤에는 [방] 탭 살림 장부에 뜬다.

---

### 1-7. 겹쳐 거두기 → **잉여가 생긴다**

**실측** (프로브 2 · 시루 4개를 같은 날 물 주고 5일 뒤 한꺼번에 거뒀다)

| 거둔 차례 | 곳간에 들어간 값 | 곳간 누계 | 잉여 누계 | 그때 배너 |
|---|---|---|---|---|
| 1 | 3,000 | 3,000 | 0 | `곳간에 3,000원` |
| 2 | 2,000 | 5,000 | 1,000 | `곳간에 2,000원` |
| 3 | 1,000 | 6,000 | 3,000 | `곳간에 1,000원` |
| 4 | **0** | 6,000 | **6,000** | **`곳간에 0원`** |

기록은 매번 —

```
🥱 곳간이 안 비어 있어 3,000원을 못 받았습니다 (시루 1개가 겹쳤습니다)
   — 물을 **날을 달리해** 주면 거두는 날이 어긋나 온전히 받습니다
```

**세 가지가 걸린다.**

1. **별표 네 개가 화면에 그대로 뜬다** — §4-1
2. **「팔 수 있다」는 말이 한 글자도 없다.** 「못 받았습니다」로 끝난다. `cropsale-to-plan §7-3`
   이 *"이제 넘길 수 있다는 것을 같이 말하는 편이 낫다"* 라고 적어 둔 그대로 남아 있다
3. 배너 `🥣 콩나물을(를) 거뒀습니다 / 하얗고 아삭 · **곳간에 0원**` — 틀린 말은 아니지만
   「거뒀습니다」와 「0원」이 한 줄에 붙어 있어 **무슨 일이 났는지가 안 읽힌다**

**★ 곁가지 하나 — 몬스테라가 Day 5 에 왔다.**
같은 날 세 번 거두면 그날 바로 3회전이 차서 몬스테라가 도착한다.
그 순간 아래 한 줄이 `몬스테라는 어두운 자리에서…` 로 갈아치워지고 **잉여 · 안 거둔 시루 ·
물 안 준 시루 이야기가 통째로 사라진다.** 「자리가 밥값이 됐다」를 배우기 전에 다음 단계가 온다.
☐ 판단 필요 — 「3회전」을 「서로 다른 날 3회」로 셀지.

---

### 1-8. **잉여 채소 넘기기**  ★박사님이 콕 집으신 곳

**어디 있나** [상점] 탭 · [몬스테라 팔기] **왼쪽**. 잉여가 0이면 `display:none` 이라 통째로 사라진다.

**화면 실측** (잉여 6,000원일 때)

```
[잉여 채소 넘기기 (5,100원)]   [몬스테라 팔기 (12,000원)]
겹쳐서 못 받거나 쉰 몫 6,000원 — 85%에 넘깁니다 (끼니로 쓸 몫은 안 팔립니다)
무늬 잎 0/1장 — 지금 잎 수로는 이사 자금에 못 미칩니다. 더 키우거나 삽수를 파세요
```

버튼 실측 **166.8 × 44px** (손가락 최소 44px 을 넘는다) · `display:block`

**★ 값이 몇 %인지 — 화면은 85% 라고 말한다.**
지시서에 적힌 *"화면 주석이 「값은 아직 미확정, 지금은 70%」라는데 정본은 0.85"* 를 눈으로 확인했다.
결론: **화면은 안 틀렸다.**

| | |
|---|---|
| 정본 `data/balance/characters.json._meta.cropSurplusSaleRate` | **0.85** |
| 폴백 `src/game/first_play.js:306` | **0.85** (정본과 같다) |
| **화면에 뜨는 글자** | **`85%에 넘깁니다`** — `Math.round(sq.rate*100)` 로 그때그때 읽는다 |
| ⚠ 낡은 것 | **소스 주석** `game.html:3457~3458` — *"값은 아직 미확정… 지금은 70% 로 돌고 있고, 그 사실을 화면이 말해 준다"*. **화면에는 안 나온다.** 주석만 낡았다 |

**눌렀을 때 무슨 일이 나나** (실측)

- **되묻지 않는다.** 한 번 누르면 바로 나간다 (몬스테라 팔기는 `정말 팝니까?` 로 되묻는다 — 일부러 다르다)
- 배너 `잉여 채소를 넘겼습니다 / 5,100원`
- 기록 `💰 잉여 채소를 넘겼습니다 — 정가 6,000원어치를 85% 에 넘겨 5,100원 (곳간에 든 몫은 그대로 밥입니다)`
- 지갑 **+5,100원** · **곳간 6,000원은 한 푼도 안 줄었다** (실측 · 넘기기 전후 `pantryWon` 둘 다 6,000)
- 버튼은 그 자리에서 사라진다. 두 번 못 판다
- **전량 일괄이다.** 「몇 개 팔지」가 없다

**★ 세이브 — 붙어 있고 실제로 지킨다.**
잉여 6,000원을 안 넘긴 채 새로고침 → **6,000원 그대로**. 실측했다.
`cropsale-to-plan §4` 의 *"이것 하나는 꼭 붙여야 한다"* 던 세 줄이 `save.js:483~486` 에 붙어 있다.

**빠진 말 셋**

| | 무엇이 없나 |
|---|---|
| ① ★ | **잉여가 생겼다는 것을 아무도 안 알려 준다.** 아래 한 줄 · 배너 · [식물] · [방] 전부 침묵.<br>[상점] 탭을 열어야 안다. 실측 — 잉여 6,000원을 안고도 아래 한 줄은 `몬스테라는 어두운 자리에서…` 였다 |
| ② | 잉여가 0이면 버튼이 **통째로 사라져** 「그런 것이 있다」를 배울 자리가 아예 없다.<br>(늘 회색으로 두면 「왜 회색이지」가 된다는 것이 원래 판단이다 — game.html:1483~1484) |
| ③ | **왜** 잉여가 생겼는지를 그 자리에서 안 말한다. 「겹쳐서 못 받거나 쉰 몫」이라고만 한다 —<br>내가 한 일(같은 날 몰아 거뒀다)과 이어지지 않는다 |

**고칠 안**

1. 수확 기록 `🥱 …못 받았습니다` 줄 꼬리에 한 마디 — `— [상점]에서 넘길 수 있습니다`
   (`loop.js:1064~1066` · 이 창의 쓰기 영역 밖이라 안 붙였다)
2. 잉여가 생긴 날 배너 한 줄 — `🥱 못 받은 몫 1,000원` / `[상점]에서 넘기면 850원` (11자 · 19자)
3. 버튼 밑 설명에 **까닭**을 붙인다 — `같은 날 겹쳐 거둔 몫입니다 · 85%에 넘깁니다`

---

## 2. 버그

### 버그 1 · ★이미 찬 추천 자리에 화분이 **겹쳐 앉는다**

> ## ✅ 2026-08-15 — **고쳤다.** 아래 재현은 이제 안 된다
>
> 고친 곳은 `game.html` 의 `freePlace.snap` 이다(`b640821`). 이 문서 §「고치는 법」이
> 적어 둔 그대로 — **차 있으면 붙이지 말고 raw 좌표 그대로 둔다.** 그러면 겹침 판정이 잡는다.
> `slotTaken(slotId)` 을 새로 두고, 옮기는 중인 자기 자리는 `fromSlotId` 로 빼 준다
> (안 빼면 자기 자리로 되돌아갈 수가 없다).
>
> **다시 재서 확인**(2026-08-15 · 390×844 · 새 판 · ⚠ 시루 재고는 손으로 넣었다 —
> 재는 것이 「살 수 있나」가 아니라 「겹쳐 앉나」라서):
>
> | | 라벨 | 결과 |
> |---|---|---|
> | ① 빈 자리 한가운데 | `서랍장 1번 칸 · 하얗고 아삭 · 시루 1개` | 앉음 |
> | ② **같은 자리에 또** | **`다른 화분과 겹칩니다 (0.22m 모자랍니다)`** | **안 앉음** |
> | 좌표가 같은 화분 | | **없음** |
>
> ★ 그리고 이제 **거절한다고 말도 한다** — 배너 `여기엔 못 놓았습니다 / 다른 화분과 겹칩니다`.
>   예전에는 조용히 실패했다(`c76f0ff` · §조용한 것들).
>
> ⚠ **§4-2 「놓기 라벨이 찬 자리 이름을 댄다」도 같이 사라졌다** — 라벨이 이제 겹침을 댄다.

**재현** (프로브 4 · 390×844 · 새 판 · 100% 재현 — ⚠ **위 정정을 먼저 읽어라. 지금은 재현 안 된다**)

1. 가방에 시루 4 · 씨앗 9 를 넣는다
2. `banjiha-dresser:0`(서랍장 1번 칸)의 **화면 좌표 (226.6, 473)** 에 시루를 끌어 놓는다
   → 라벨 `서랍장 1번 칸 · 하얗고 아삭 · 시루 1개 (가방에 4개 남음)` · **앉는다**
3. **같은 점**에 또 놓는다 → 라벨 `다른 화분과 겹칩니다 (0.11m 모자랍니다)` · **막힌다** (맞는 동작)
4. **20px 오른쪽** (246.6, 473) 에 놓는다
   → 라벨 `서랍장 1번 칸 · 하얗고 아삭 · 시루 1개 · 씨앗 1봉지` · **그대로 앉는다** ← 여기가 사고다
5. **20px 위** (226.6, 453) 에 또 놓는다 → **또 앉는다**

**결과**

```
pots = [ banjiha-dresser:0 @ 1.700,0.940
         banjiha-dresser:0 @ 1.700,0.940
         banjiha-dresser:1 @ 1.700,1.660
         banjiha-dresser:0 @ 1.700,0.940 ]
```

**한 자리에 시루 셋이 소수점까지 똑같은 좌표로 섰다.**

**원인** (코드에서 읽은 것 · game.html:5863~5873)

```js
snap(hit, px, py) {
  ...
  if (n.maxPotD != null && hit.potD != null && n.maxPotD < hit.potD) return hit;  // 크기만 본다
  return { ...hit, ...n.pos, ... , snappedTo: n.slotId };                          // 찼는지 안 본다
}
```

겹침 판정(`다른 화분과 겹칩니다`)은 `surfaceAt` 이 **붙이기 전 raw 좌표**로 한다.
그래서 **44px(`SNAP_PX`) 안이되 raw 좌표는 안 겹치는 점**에서 놓으면,
검사를 통과한 뒤 `snap` 이 **이미 찬 자리로 옮겨 앉힌다.**

**왜 나쁜가**

1. 라벨이 「서랍장 1번 칸」이라고 **거짓말**을 한다 — 이미 찬 자리다
2. 3D 에서 시루가 완전히 겹쳐 **눌러 고르기가 어렵다**
3. 밝기·품질이 **같은 자리 값**으로 계산돼 「자리마다 밝기가 다르다」는 이 게임의 뼈대가 그 자리에서 무너진다

**못 본 것** 노란 마름모가 **찬 자리에도 계속 뜨는지**는 안 봤다.
그 마름모가 안 뜨는데도 붙는 것이면 더 나쁜 일이다.

**고칠 안** `snap` 이 `maxPotD` 를 보는 그 줄 옆에서 **자리가 비었는지**도 본다.
차 있으면 붙이지 말고 raw 좌표 그대로 둔다(그러면 겹침 판정이 잡는다).
⚠ **어느 창의 몫인지 확실하지 않다** — `n.pos`·`occIdx` 는 방뷰가 주는 값이고,
「찼나」를 아는 정본이 `room_view` 인지 `place.js` 인지 확인 안 했다.

---

### 버그 2 · 걷는 중에 다시 누르면 **틀린 실패 문구**가 뜬다

**재현** 시루 둘이 같은 날 익은 판에서, [🥣 거두기] 말풍선을 누르고 **3초 안에**
다른 시루의 [🥣 거두기]를 누른다. (사람이 실제로 하는 일이다 — §1-4 에서 본 대로
누른 뒤 3초 동안 말풍선 **글자가 안 바뀌므로** 안 눌린 줄 안다.)

**화면 실측**

```
가지는 못했습니다
다른 동작이 시작됐습니다 — 자리는 손이 닿는 데로 옮겨 보세요
```

**실제로는 일이 다 됐다.** 앞 수확도 뒤 수확도 들어갔다 —
코드에서 읽은 것: game.html:3779~3786 `onFail` 이 배너를 내고도 `runOnce()` 로 **규칙은 돌린다**
(주석이 *"그래도 일은 한다. 연출은 보여 주기고 규칙은 규칙이다"* 라고 적어 두었다).

**왜 나쁜가**

1. 「못했습니다」인데 **됐다** — 사람이 또 누르면 이번엔 정말 헛손질이 된다
2. 붙은 처방 `자리는 손이 닿는 데로 옮겨 보세요` 가 **이 원인과 아무 상관이 없다.**
   그 꼬리말은 「구석에 밀어 넣은 화분이라 못 갔다」를 위한 말이다(3781행 주석이 그렇게 적혀 있다).
   원인이 「내가 방금 다른 걸 눌렀다」일 때 자리를 옮기라는 말이 나온다.

**고칠 안** `why === '다른 동작이 시작됐습니다'` 일 때는 —
① 배너를 아예 안 내거나 ② G-16 에 이미 있는 문구로 돌린다 —
`아직 하는 중입니다` / `앞 동작이 끝나면 다시 눌러 주세요`.
그리고 §1-4 의 「걷는 동안 글자를 바꾼다」를 같이 하면 **원인 자체가 줄어든다.**

---

## 3. 조용히 지나가는 것들 ← 이 저장소의 고질병

급한 순.

| | 무엇이 조용한가 | 어디서 확인 |
|---|---|---|
| **1** | **배송이 도착한 것** — 배너 없음 · 아래 한 줄 없음 · 띠에 표시 없음. 기록 한 줄뿐 | §1-2 (실측) |
| **2** | **잉여가 생긴 것** — 6,000원이 쌓여도 [상점]을 안 열면 모른다 | §1-8 (실측) |
| **3** | **곳간에 얼마가 쌓였나** — 첫 플레이 동안 안 보인다. 배너 한 줄이 6초 뒤 사라지면 끝 | §1-6 (실측) |
| **4** | **물을 안 준 시루** — 아래 한 줄이 닷새 동안 한 번도 안 말했다 | §1-5 (실측) |
| **5** | **누른 뒤 3초** — 게이지는 차는데 **글자가 안 바뀐다.** 그래서 또 누른다 | §1-4 (실측) |

⚠ 「배송 도착」과 「잉여」는 **둘 다 상점 탭 안에서만 확인되는 일**이다.
아래 띠에 [상점]이 생겼으니(G-26) 그 단추에 점 하나를 찍는 길도 있다 — 판단 필요(☐).

---

## 4. 틀린 말을 하는 곳

### 4-1. ★기록칸에 **별표가 그대로 뜬다**

화면 실측 (기록칸 `#log` 의 글자 그대로) —

```
1일   물을 **날을 달리해** 주면 거두는 날이 어긋납니다
```

`renderLog`(`src/game/ui.js:147`)가 `l.msg` 를 **그대로 `innerHTML` 에 넣고** 마크다운을 안 푼다.
별표를 쓴 곳은 둘 —

| 파일 | 줄 |
|---|---|
| `src/game/loop.js:1066` | `— 물을 **날을 달리해** 주면 거두는 날이 어긋나 온전히 받습니다` |
| `src/game/state.js:834` | `…를 한꺼번에 심었습니다 — 물을 **날을 달리해** 주면 …` |

**둘 다 플레이어가 실제로 만나는 줄이다** — ①은 겹쳐 거둔 날(§1-7), ②는 [다시 심기]를 눌렀을 때.

**고칠 안** 별표를 걷고 `<b>날을 달리해</b>` 로 쓰거나(같은 파일 다른 줄들이 이미 `<b>` 를 쓴다),
그냥 평서체로 푼다 — `물을 날을 달리해 주면`.

### 4-2. 놓기 라벨이 **이미 찬 자리 이름**을 댄다 → 버그 1

### 4-3. `가지는 못했습니다 … 자리는 손이 닿는 데로 옮겨 보세요` → 버그 2

### 4-4. 배너 `🥣 콩나물을(를) 거뒀습니다 / 하얗고 아삭 · 곳간에 0원`

틀린 말은 아니다. 그런데 그 0원은 **3,000원이 통째로 잉여로 빠진 것**이고,
그 사실이 같은 화면에 안 나온다. 「거뒀는데 0원」만 남는다.

### 4-5. ⚠ **소스 주석**이 낡았다 (화면은 안 틀렸다)

`game.html:3457~3458` — *"값(정가의 몇 %)은 **아직 미확정**이다. 지금은 70% 로 돌고 있고,
그 사실을 화면이 말해 준다"*. 정본은 **0.85** 고 화면도 **85%** 라 말한다.
`START-HERE §2.6` 이 적은 교훈 그대로다 — **주석이 낡으면 재는 자가 된다.**
(지시서가 「화면 주석이 70%라는데」로 읽은 것도 이 주석 때문이다.)

---

## 5. 급한 순 목록

| | 무엇을 | 왜 |
|---|---|---|
| **1** | **버그 1 — 찬 자리에 겹쳐 앉는 것** | 판이 깨진다. 「자리마다 밝기가 다르다」가 그 자리에서 무너진다 |
| **2** | **잉여를 알리기** — 수확 기록 꼬리에 `— [상점]에서 넘길 수 있습니다` + 배너 한 줄 | 박사님이 콕 집으신 줄기인데 **아무도 안 알려 준다** |
| **3** | **배송 도착 배너** | 「하루 뒤에 옵니다」의 뒷말이 없다 |
| **4** | **버그 2 — 틀린 실패 문구** (+ 걷는 동안 글자 바꾸기) | 「못했습니다」인데 됐다. 둘이 한 사고다 |
| **5** | **기록칸 별표** | 두 줄만 고치면 된다. 제일 싸다 |
| **6** | **첫 플레이 동안 곳간 보여 주기** | 「왜 두 번째부터 2,000원이지」를 아무도 안 말한다 |
| **7** | 아래 한 줄에 「물 안 준 시루」 한 갈래 | 회전이 조용히 밀린다 |

---

## 6. 못 본 것 / 확실하지 않은 것

1. **★쉰 몫(`spoiledWon`)을 한 번도 못 봤다.** 두 판을 굴렸는데 둘 다 0 이었다.
   코드에서 읽은 것 — 곳간 한도 `pantryCapWon`(`first_play.js:1737`)은 **한 회전분 × 시루 수**고,
   하루 배출 `dailyCropSaveWon` 은 **3,000원**(콩나물만일 때)이다.
   시루가 늘면 **한도도 같이 늘어서**, 같은 날 몰아 거두는 것만으로는 한도에 못 닿는다 —
   시루 4개면 한도 12,000 인데 몰아 거두어 들어가는 값의 최대가 6,000 이다.
   ⇒ **잉여의 실질은 「겹침 손실」 하나로 보인다.** ⚠ **재서 확인한 것이 아니다.** 산수로만 그렇다.
2. **무순으로도 잉여가 나는지 안 봤다.** 이번 감사는 콩나물 한 바퀴에 집중했다.
3. **노란 마름모가 찬 자리에도 뜨는지 안 봤다** (버그 1 과 붙어 있는 물음).
4. **아래 한 줄이 무순을 말하는지 안 봤다** — 지시서의 G-25 ☐ 는 확인하지 못했다.
5. **G-06a 의 다섯 갈래 중 셋만 봤다.** ①`5일이면 한 판입니다 · 식비 3,000원을 아꼈습니다` ·
   ③`씨앗이 왔습니다. 시루에 다시 심으세요.` · ⑤(기본)는 화면으로 봤다.
   ②`씨앗을 주문하세요` · ②b`오는 중입니다` · ④`자리를 옮겨도 됩니다` 는 **안 봤다**.
6. **살림 장부는 지름길로 봤다** — `completed = true` 를 손으로 세운 판이다. 진짜 50일이 아니다.
7. **G-20~G-24 는 이번에도 못 밟았다** (삽수 · 식물등 · 월세·파산 · 확정 무늬 · 이사).
   ⇒ 참고: 잉여 판매로는 이사비 200만원에 못 닿는다. 한 판이 **5,100원**이다.
8. **P-01~P-04 · P-08~P-10 은 확인 못 했다.** 첫 플레이 후반(몬스테라 정체·말린 새순·살림 구간)이라
   이번 한 바퀴에 안 들어왔다.
   확인한 것은 넷 — **P-05**(드롭 라벨 밝기 · §1-3) · **P-06/G-06a**(씨앗 없을 때 아래 한 줄 · §1-6) ·
   **P-07**(`창가` → `창턱` · 실측 `몬스테라는 어두운 자리에서 자라지 않습니다. 높은 창턱 자리로 옮겨 보세요.`
   손가락 `탭 → 옮기기 → 창턱으로`) · **P-00**(이사비 200만 기준 · 실측
   `무늬 잎 0/1장 — 지금 잎 수로는 이사 자금에 못 미칩니다. 더 키우거나 삽수를 파세요`).
   **G-26 아래 띠**도 [상점]·[방]을 실제로 눌러 열었다 — 돈다.
9. **다른 워커가 `game.html` 을 쥐고 있다.** 이 문서의 줄 번호는 **2026-08-15 낮에 읽은 판** 기준이라
   지금은 어긋나 있을 수 있다. 문구로 찾는 편이 안전하다.
