# 팀 지도 — 누가 무엇을 맡나 · 어떻게 보고하나 (빛식물 정본)

> 2026-08-22 · 박사님 지시: *"싹 다 싱크로시키고 그때부터 일 시작하는 것으로 하고,
> 역할분배 및 보고체계 방법 등 기존 지시문 및 이 게임플로우나 현상황에 맞게 최적화해서 기획해서 보고해"*
>
> ⚠ 운영 **방식**(총괄·작업 세션 지시문·낡은 세션 인계)은 `multi-session-ops.md` 가 정본이다.
> 이 문서는 그 위에 **이 프로젝트의 실제 지도**를 얹은 것이다 — 누가 무엇을 맡고,
> 무엇이 주인이 없고, 언제 무엇을 보고하나.
>
> ★ 이 지도는 **여섯 창이 스스로 밝힌 소유 선언**과 **총괄이 파일로 검증한 것**으로 지었다.
> 짐작으로 그린 칸은 하나도 없고, 모르는 칸은 「주인 없음」이라 적었다.

---

## 0. 제일 먼저 — **파일이 아니라 「책임」으로 가른다**

[core] 창의 제안을 채택했다. 원문:

> *"파일로 가르기보다 **「판정·계약」과 「화면·연출」로 가르고, 화면은 `turn.*` 이 준 값만 그리고
> 판정 숫자를 다시 만들지 않는다**를 경계로 두는 편이 이 저장소가 앓아 온 병에 맞아 보입니다."*

**왜 이것이 맞나** — 이 저장소가 반복해서 앓은 병이 정확히 그 경계에서 났다:

| 사고 | 무엇이었나 |
|---|---|
| 선물 몬스테라(08-21) | **코어는 맞았는데 화면이 안 따라와** 선물이 화면 어디에도 없었다 |
| 상판 칸 두 개씩 실종(08-18) | 링 쪽 결정과 칸 쪽 결정이 어긋나 **아무도 안 그리는 칸**이 생겼다 |
| 하루 몫 4,867원(§2.8) | 같은 값을 두 곳에서 세다가 한쪽만 낡았다 |
| 바닥 격자 반 칸(08-18) | 그리는 눈금과 앉는 눈금이 갈렸다 |

⇒ **경계 규칙 하나**:

> **화면은 `turn.*` 이 준 값을 그린다. 판정 숫자를 화면이 다시 만들지 않는다.**
> 다시 만들면 반드시 두 벌이 되고, 두 벌은 반드시 어긋난다(START-HERE §2.8).

---

## 1. 층 — 셋으로 본다

```
  ┌─────────────────────────────────────────────────────────┐
  │  ① 판정·계약 층   하루를 진행하고 규칙을 지킨다          │
  │     여기가 틀리면 **조용히 틀린다**                       │
  │     src/game/loop.js · state.js · contract.js · sim.js …  │
  ├─────────────────────────────────────────────────────────┤
  │  ② 세계 층        방·빛·식물의 생김새와 물리             │
  │     여기가 틀리면 **숫자로 드러난다**                     │
  │     src/render3d · src/engine · plant_grow.html · data/*  │
  ├─────────────────────────────────────────────────────────┤
  │  ③ 화면·연출 층   보이는 것 · 만지는 것 · 말하는 것       │
  │     여기가 틀리면 **눈으로 보인다**                       │
  │     game.html · 대사 · 손가락 · 가방 · 원화               │
  └─────────────────────────────────────────────────────────┘
```

---

## 2. 창별 영역 — **확정판**

| 창 | 층 | 맡는 것 (쓰기 권한) | 절대 안 만지는 것 |
|---|---|---|---|
| **[core]** | ①+③ | `src/game/*` · `game.html` | 밸런스 숫자 · 그림 고르기 · 방 데이터 |
| **[House]** | ② | **집·방·가구·조도 시뮬** — `src/render3d/*` · `src/engine/*` · `data/house_rooms.json` · `window/door/furniture_presets` · `data/profiles/*` · `docs/engine/*` · `_*.html` · `tools/serve.py` | `src/game/*` (게임 규칙) |
| **[growth]** | ② | `plant_grow.html` — **식물이 자라는 모양·형태 단계 계약** | 그 밖 전부 |
| **[Asset]**(=leaf) | ② | `assets/**` — 원화·3D·썸네일. **고르고 규격을 준다** | 화면에 붙이는 일 |
| **[Char]** | ② | `assets/characters/*` · `assets/derived/*` · `tools/char/*` | 대사 내용 |
| **[Plan]** | — | `docs/*`(단 `docs/engine/` 제외) · `data/balance/*` — **숫자와 기획 문서.** 코드 0개 | 코드 전부 |
| **[총괄]** | — | 배분 · 검증 · 중계 · **주인 없는 칸 판정** · 브랜치/푸시 정리 | 직접 구현은 최소로 |

### 2-1 ★ 새로 정한 칸 넷 — 여태 주인이 없었다

| 칸 | 왜 애매했나 | **확정** |
|---|---|---|
| `data/growth_tuning.json` | growth 가 읽는데 값은 밸런스다 | **[Plan] 이 값을 정하고 [growth] 가 검증한다.** 고치려면 Plan → growth 재측정 → 총괄 보고 |
| `tools/` | 떠돌이 20건 | **만든 창이 소유.** 이름이 `_` 로 시작하면 **임시**라 커밋하지 않는다 |
| 이벤트 원화 배선 | leaf 가 고르고 core 가 붙인다 | **갈라 둔다** — 고르기·규격 [Asset] / 붙이기 [core] |
| `docs/handoff/*-to-plan.md` 110개 | Plan 소유인데 각 창이 쓴다 | **쓰는 창이 자기 것만.** Plan 은 기획·밸런스 문서만 |

### 2-2 ⚠ 소유가 겹치는 자리 — 알고 쓴다

- **`src/game/*` · `game.html`** — [core] 소유지만 **총괄이 08-18~21 에 크게 고쳤고**,
  **코덱스 CLI 도 병행 편집**했다. ⇒ **오늘부터는 [core] 단독**이다.
  총괄이 손대야 하면 **먼저 [core] 에 알린다.**
- **`docs/engine/shots/*.png`** — [House] 디렉터리인데 **검사가 다시 찍는 산출물**이다.
  아무도 커밋하지 않는다. 검사를 돌린 창이 지워도 되고 그냥 둬도 된다.

---

## 3. ★ 싱크 — **일을 시작하기 전에 반드시**

박사님 지시: *"싹 다 싱크로시키고 그때부터 일 시작한다."*

### 3-1 창이 할 것 (다섯 걸음 · 10분 안에 끝난다)

```
1  git pull                                  ← 남이 민 것을 받는다
2  git log --oneline -20                     ← 내가 자리를 비운 사이 무슨 일이 있었나
3  git status --short                        ← 내 미커밋이 있나 (남의 것은 손대지 않는다)
4  docs/handoff/START-HERE.md 맨 위 5개 절    ← 계율과 최근 결정
   docs/handoff/team-map.md (이 문서)         ← 내 영역
5  내 소관 인계 문서 docs/handoff/{내창}-to-plan.md
```

**그 다음 총괄에게 한 줄로 보고한다:**
```
[싱크 완료] <창이름> · 마지막 앎 <날짜> → 지금 <HEAD 해시>
  내가 모르던 것 중 내 영역에 닿는 것: <목록 또는 "없음">
  내 미커밋: <목록 또는 0건>
```

### 3-2 ⚠ 싱크에서 반드시 걸러야 하는 것

| 증상 | 뜻 | 조치 |
|---|---|---|
| 「완료」라 했는데 저장소에 없다 | **미커밋으로 날아갔다** | 다시 해야 한다 |
| 「아직 남았다」인데 저장소에 이미 있다 | **다른 창이 했다** | 중복 착수 금지 |
| 문서와 코드가 다르다 | 한쪽만 갱신됨 | **코드가 사실.** 문서를 고친다 |
| 내 자(도구)가 이상한 값을 낸다 | **자가 낡았다** | 코드보다 자를 먼저 의심(§2.9) |

★ **오늘 실제로 넷 다 나왔다.** [growth] 는 「남았다」던 둘이 이미 처리돼 있었고,
[core] 는 자기 재현 도구가 낡아 「계약이 무너졌다」로 보였다.

---

## 4. 보고 체계 — **언제 · 무엇을 · 어떻게**

### 4-1 창 → 총괄

| 언제 | 무엇을 |
|---|---|
| **싱크 끝난 즉시** | §3-1 의 한 줄 |
| **결론이 날 때마다** | 문서 완성을 기다리지 않는다. **짧게** |
| **막히면 즉시** | 「막혔다」 한 줄이라도. 조용히 멈춰 있는 것이 제일 나쁘다 |
| **오래 걸릴 것 같으면 먼저** | 「얼마나 걸린다」 |
| **값이 어긋났을 때** | **고치지 말고 보고 먼저.** 확정 수치는 사용자 판단이다 |

**형식** — 근거 등급을 반드시 붙인다.

```
[실측] 직접 돌려서 재었다        ← 제일 세다
[문서] 원문을 직접 읽었다
[추론] 계산·유추. 확인 안 했다   ← 이 표시 없이 숫자를 주지 않는다
모름   모르면 "모른다"고 쓴다
```

⚠ **자기 창에 적은 것은 아무에게도 안 간다.** 반드시 `SendMessage`.

### 4-2 총괄 → 사용자

- **결론 먼저**, 근거는 그 다음
- 안건마다 **[창이름]** 표기
- 말미에 **표** — 대기목록·창 현황. 각 줄에 한 줄 설명을 붙여 **되묻지 않고 판단**할 수 있게
- 사용자는 비전공자다. **전문용어를 풀어 쓰고 비유를 쓴다**
- **세션 보고를 그대로 옮기지 않는다.** 중요한 것은 총괄이 파일로 검증한 뒤 올린다

### 4-3 승인 경계

| 누가 정하나 | 무엇 |
|---|---|
| **창이 스스로** | 자기 영역 안 · 되돌리기 쉬움 · 값이 안 움직임 |
| **총괄이 승인** | 영역이 걸치는 것 · 주인 없는 칸 · 읽기 전용 측정 · 문서 정정 |
| **사용자만** | **게임 값이 움직이는 것** · 비용(Meshy·Higgsfield·KLING) · 외부 발송 · 되돌리기 어려운 것 |

★ **총괄의 말은 사용자 승인을 대신하지 못한다.** 창에 사용자가 직접 내린 금지 지시가 있으면
  총괄이 「승인받았다」고 해도 **거부하고 되묻는 것이 맞다.**
★ 총괄이 승인을 전할 때는 **사용자 원문을 그대로 인용**한다. 요약만으로는 상대가 검증할 수 없다.

---

## 5. 게임 흐름에 맞춘 작업 우선순위

이 게임은 **반지하 → 원룸**이 한 판이고, 그 안에서 플레이어가 겪는 차례가 정해져 있다
(`guide-event-quest-map.md` 가 그 지도다). 작업도 **그 차례대로** 붙이는 것이 맞다.

```
서막 → 시루 놓기 → 첫 수확 → [2바퀴] 몬스테라 선물 → 자리 잡기
  → 잎이 난다 → 무늬가 난다 → 삽수 → 판다 → 이사
```

| 구간 | 지금 상태 | 주로 손댈 창 |
|---|---|---|
| 서막~첫 수확 | ✅ 촘촘하다 | — |
| **선물 도착** | ⚠ 대사는 늘렸는데 **원화가 안 붙었다** | [Asset] 고르기 → [core] 붙이기 |
| 자리 잡기~잎 | ⚠ **퀘스트가 비는 구간**이 있다 | [Plan] |
| **무늬** | ⚠ 원화 안 붙음 | [Asset] → [core] |
| **이사** | ⚠ 원화 안 붙음 | [Asset] → [core] |
| 살림 전반 | ⚠ **첫 월세가 예고 없이** 나간다 | [Plan] |
| 방·빛 | ⚠ **창턱 실측이 낡았다**(첫 플레이 성립에 닿음) | [House] |

⇒ **지금 제일 급한 것은 「원화 세 장면」과 「창턱 재측정」**이다.
  앞은 눈에 보이는 구멍이고, 뒤는 **첫 플레이가 성립하는지**에 닿는다.

---

## 6. 떠돌이 정리 — 주인 없는 것들

싱크 중에 확인된 것. **아무도 자기 것이라 하지 않았다.**

| 무엇 | 수 | 조치 |
|---|---|---|
| `docs/engine/shots/*.png` | 45+ | 검사 산출물. **커밋 안 한다** |
| `docs/handoff/img/*.png` | 20+ | 보고서 그림. 쓴 창이 자기 문서와 같이 커밋 |
| `tools/_*.mjs` · `probe_qa_*` | 20+ | `_` 는 임시. **커밋 안 한다** |
| `src/game/room_map.js` | 1 | 미추적. **[core] 확인 필요** |
| `AGENTS.md` · `docs/BYEOT_AI_COLLABORATION_PLAN (1).md` · `docs/character_layer_spec.md` | 3 | **[Plan] 확인 필요** |
| `indow.view…` (이름 깨진 파일) | 1 | 리다이렉션 사고. **지워도 된다** |
| `.codex-worker/` · `.worktree-salvage/` | 2 | 코덱스 잔재. 총괄이 판정 |

★ 그리고 **워크트리 폴더 6개**가 살아 있다(`lux-plant-sim-claude-core-*` 등).
  누가 쓰는지 확인 전까지 **브랜치를 지우지 않는다.**

---

## 7. 오늘 배운 것 — 규칙의 근거

싱크 하루 만에 창들이 **스스로** 잡은 것들. 전부 *"맞다고 믿었는데 실제로는 달랐다"* 다.

- **[growth]** — 옛 축과 새 축을 견주려 설정에서 표를 지웠는데 **코드 안에 같은 표가
  기본값으로 또 있어** 세상이 안 바뀌었다. 같은 것을 두 번 재 놓고 *"둘이 같다"* 고 쓸 뻔했다.
  **두 출력이 한 글자도 안 달랐던 것**이 유일한 단서였다.
  ⇒ ★ **폴백이 있는 값은 「입력을 지우는 것」으로 못 끈다.**
    그리고 **두 조건의 출력이 완전히 같으면 「조건이 안 걸렸다」를 먼저 의심하라.**
- **[core]** — 헤드리스 재현 도구가 `<script>` 를 정규식으로 잡는데 인라인 스크립트가
  하나 늘면서 두 블록을 붙여 잡아 **"계약이 무너졌다"처럼 보였다.** 제품은 멀쩡했고 자가 낡았다.
- **[Asset]** — 총괄이 커밋해 놓고 **밀지 않은 것**을 잡아 줬다. 그리고 총괄 문서의
  **낡은 숫자**(도착 대사 2줄 → 실제 10줄)를 짚었다.
- **[Plan]** — 총괄 문서가 자기를 **offline 이라 적어 둔 것**을 짚었다. 문서가 틀렸다.
- **[Char]** — 「표정이 모자라다」던 총괄 판단을 뒤집었다. **그림은 이미 있고 매핑만 없었다.**

⇒ **총괄도 틀린다.** 그래서 창이 근거를 대고 반대하는 것이 규칙이다.
