# 팀 지도 — 누가 무엇을 맡나 · 어떻게 보고하나 (빛식물 정본)

> 2026-08-22 · 박사님 지시: *"싹 다 싱크로시키고 그때부터 일 시작하는 것으로 하고,
> 역할분배 및 보고체계 방법 등 기존 지시문 및 이 게임플로우나 현상황에 맞게 최적화해서 기획해서 보고해"*
>
> ⚠ 운영 **방식**(총괄·작업 세션 지시문·낡은 세션 인계)은 `multi-session-ops.md` 가 정본이다.
> 이 문서는 그 위에 **이 프로젝트의 실제 지도**를 얹은 것이다 — 누가 무엇을 맡고,
> 무엇이 주인이 없고, 언제 무엇을 보고하나.
>
> ★ 이 지도는 **여섯 창이 스스로 밝힌 소유 선언**과 **총괄이 파일로 검증한 것**으로 지었다.
> 짐작으로 그린 칸은 하나도 없고, 모르는 칸은 「주인 없음」이라 적었다.

---

## 0. 제일 먼저 — **파일이 아니라 「책임」으로 가른다**

[core] 창의 제안을 채택했다. 원문:

> *"파일로 가르기보다 **「판정·계약」과 「화면·연출」로 가르고, 화면은 `turn.*` 이 준 값만 그리고
> 판정 숫자를 다시 만들지 않는다**를 경계로 두는 편이 이 저장소가 앓아 온 병에 맞아 보입니다."*

**왜 이것이 맞나** — 이 저장소가 반복해서 앓은 병이 정확히 그 경계에서 났다:

| 사고 | 무엇이었나 |
|---|---|
| 선물 몬스테라(08-21) | **코어는 맞았는데 화면이 안 따라와** 선물이 화면 어디에도 없었다 |
| 상판 칸 두 개씩 실종(08-18) | 링 쪽 결정과 칸 쪽 결정이 어긋나 **아무도 안 그리는 칸**이 생겼다 |
| 하루 몫 4,867원(§2.8) | 같은 값을 두 곳에서 세다가 한쪽만 낡았다 |
| 바닥 격자 반 칸(08-18) | 그리는 눈금과 앉는 눈금이 갈렸다 |

⇒ **경계 규칙 하나**:

> **화면은 `turn.*` 이 준 값을 그린다. 판정 숫자를 화면이 다시 만들지 않는다.**
> 다시 만들면 반드시 두 벌이 되고, 두 벌은 반드시 어긋난다(START-HERE §2.8).

---

## 1. 층 — 셋으로 본다

```
  ┌─────────────────────────────────────────────────────────┐
  │  ① 판정·계약 층   하루를 진행하고 규칙을 지킨다          │
  │     여기가 틀리면 **조용히 틀린다**                       │
  │     src/game/loop.js · state.js · contract.js · sim.js …  │
  ├─────────────────────────────────────────────────────────┤
  │  ② 세계 층        방·빛·식물의 생김새와 물리             │
  │     여기가 틀리면 **숫자로 드러난다**                     │
  │     src/render3d · src/engine · plant_grow.html · data/*  │
  ├─────────────────────────────────────────────────────────┤
  │  ③ 화면·연출 층   보이는 것 · 만지는 것 · 말하는 것       │
  │     여기가 틀리면 **눈으로 보인다**                       │
  │     game.html · 대사 · 손가락 · 가방 · 원화               │
  └─────────────────────────────────────────────────────────┘
```

---

## 2. 창별 영역 — **확정판**

| 창 | 층 | 맡는 것 (쓰기 권한) | 절대 안 만지는 것 |
|---|---|---|---|
| **[core]** | ①+③ | `src/game/*` · `game.html` | 밸런스 숫자 · 그림 고르기 · 방 데이터 |
| **[House]** | ② | **집·방·가구·조도 시뮬** — `src/render3d/*` · `src/engine/*` · `data/house_rooms.json` · `window/door/furniture_presets` · `data/profiles/*` · `docs/engine/*` · `_*.html` · `tools/serve.py` | `src/game/*` (게임 규칙) |
| **[growth]** | ② | `plant_grow.html` — **식물이 자라는 모양·형태 단계 계약** | 그 밖 전부 |
| **[Asset]**(=leaf) | ② | `assets/**` — 원화·3D·썸네일. **고르고 규격을 준다** | 화면에 붙이는 일 |
| **[Char]** | ② | `assets/characters/*` · `assets/derived/*` · `tools/char/*` | 대사 내용 |
| **[Plan]** | — | `docs/*`(단 `docs/engine/` 제외) · `data/balance/*` — **숫자와 기획 문서.** 코드 0개 | 코드 전부 |
| **[총괄]** | — | 배분 · 검증 · 중계 · **주인 없는 칸 판정** · 브랜치/푸시 정리 | 직접 구현은 최소로 |

### 2-1 ★ 새로 정한 칸 넷 — 여태 주인이 없었다

| 칸 | 왜 애매했나 | **확정** |
|---|---|---|
| `data/growth_tuning.json` | growth 가 읽는데 값은 밸런스다 | **[Plan] 이 값을 정하고 [growth] 가 검증한다.** 고치려면 Plan → growth 재측정 → 총괄 보고 |
| `tools/` | 떠돌이 20건 | **만든 창이 소유.** 이름이 `_` 로 시작하면 **임시**라 커밋하지 않는다 |
| 이벤트 원화 배선 | leaf 가 고르고 core 가 붙인다 | **갈라 둔다** — 고르기·규격 [Asset] / 붙이기 [core] |
| `docs/handoff/*-to-plan.md` 110개 | Plan 소유인데 각 창이 쓴다 | **쓰는 창이 자기 것만.** Plan 은 기획·밸런스 문서만 |

### 2-2 ⚠ 소유가 겹치는 자리 — 알고 쓴다

- **`src/game/*` · `game.html`** — [core] 소유지만 **총괄이 08-18~21 에 크게 고쳤고**,
  **코덱스 CLI 도 병행 편집**했다. ⇒ **오늘부터는 [core] 단독**이다.
  총괄이 손대야 하면 **먼저 [core] 에 알린다.**
- **`docs/engine/shots/*.png`** — [House] 디렉터리인데 **검사가 다시 찍는 산출물**이다.
  아무도 커밋하지 않는다. 검사를 돌린 창이 지워도 되고 그냥 둬도 된다.

---

## 3. ★ 싱크 — **일을 시작하기 전에 반드시**

박사님 지시: *"싹 다 싱크로시키고 그때부터 일 시작한다."*

### 3-1 창이 할 것 (다섯 걸음 · 10분 안에 끝난다)

```
1  git pull                                  ← 남이 민 것을 받는다
2  git log --oneline -20                     ← 내가 자리를 비운 사이 무슨 일이 있었나
3  git status --short                        ← 내 미커밋이 있나 (남의 것은 손대지 않는다)
4  docs/handoff/START-HERE.md 맨 위 5개 절    ← 계율과 최근 결정
   docs/handoff/team-map.md (이 문서)         ← 내 영역
5  내 소관 인계 문서 docs/handoff/{내창}-to-plan.md
```

**그 다음 총괄에게 한 줄로 보고한다:**
```
[싱크 완료] <창이름> · 마지막 앎 <날짜> → 지금 <HEAD 해시>
  내가 모르던 것 중 내 영역에 닿는 것: <목록 또는 "없음">
  내 미커밋: <목록 또는 0건>
```

### 3-2 ⚠ 싱크에서 반드시 걸러야 하는 것

| 증상 | 뜻 | 조치 |
|---|---|---|
| 「완료」라 했는데 저장소에 없다 | **미커밋으로 날아갔다** | 다시 해야 한다 |
| 「아직 남았다」인데 저장소에 이미 있다 | **다른 창이 했다** | 중복 착수 금지 |
| 문서와 코드가 다르다 | 한쪽만 갱신됨 | **코드가 사실.** 문서를 고친다 |
| 내 자(도구)가 이상한 값을 낸다 | **자가 낡았다** | 코드보다 자를 먼저 의심(§2.9) |

★ **오늘 실제로 넷 다 나왔다.** [growth] 는 「남았다」던 둘이 이미 처리돼 있었고,
[core] 는 자기 재현 도구가 낡아 「계약이 무너졌다」로 보였다.

---

## 4. 보고 체계 — **언제 · 무엇을 · 어떻게**

### 4-1 창 → 총괄

| 언제 | 무엇을 |
|---|---|
| **싱크 끝난 즉시** | §3-1 의 한 줄 |
| **결론이 날 때마다** | 문서 완성을 기다리지 않는다. **짧게** |
| **막히면 즉시** | 「막혔다」 한 줄이라도. 조용히 멈춰 있는 것이 제일 나쁘다 |
| **오래 걸릴 것 같으면 먼저** | 「얼마나 걸린다」 |
| **값이 어긋났을 때** | **고치지 말고 보고 먼저.** 확정 수치는 사용자 판단이다 |

**형식** — 근거 등급을 반드시 붙인다.

```
[실측] 직접 돌려서 재었다        ← 제일 세다
[문서] 원문을 직접 읽었다
[추론] 계산·유추. 확인 안 했다   ← 이 표시 없이 숫자를 주지 않는다
모름   모르면 "모른다"고 쓴다
```

⚠ **자기 창에 적은 것은 아무에게도 안 간다.** 반드시 `SendMessage`.

### 4-2 총괄 → 사용자

- **결론 먼저**, 근거는 그 다음
- 안건마다 **[창이름]** 표기
- 말미에 **표** — 대기목록·창 현황. 각 줄에 한 줄 설명을 붙여 **되묻지 않고 판단**할 수 있게
- 사용자는 비전공자다. **전문용어를 풀어 쓰고 비유를 쓴다**
- **세션 보고를 그대로 옮기지 않는다.** 중요한 것은 총괄이 파일로 검증한 뒤 올린다

### 4-3 승인 경계

| 누가 정하나 | 무엇 |
|---|---|
| **창이 스스로** | 자기 영역 안 · 되돌리기 쉬움 · 값이 안 움직임 |
| **총괄이 승인** | 영역이 걸치는 것 · 주인 없는 칸 · 읽기 전용 측정 · 문서 정정 |
| **사용자만** | **게임 값이 움직이는 것** · 비용(Meshy·Higgsfield·KLING) · 외부 발송 · 되돌리기 어려운 것 |

★ **총괄의 말은 사용자 승인을 대신하지 못한다.** 창에 사용자가 직접 내린 금지 지시가 있으면
  총괄이 「승인받았다」고 해도 **거부하고 되묻는 것이 맞다.**
★ 총괄이 승인을 전할 때는 **사용자 원문을 그대로 인용**한다. 요약만으로는 상대가 검증할 수 없다.

---

## 5. 게임 흐름에 맞춘 작업 우선순위

이 게임은 **반지하 → 원룸**이 한 판이고, 그 안에서 플레이어가 겪는 차례가 정해져 있다
(`guide-event-quest-map.md` 가 그 지도다). 작업도 **그 차례대로** 붙이는 것이 맞다.

```
서막 → 시루 놓기 → 첫 수확 → [2바퀴] 몬스테라 선물 → 자리 잡기
  → 잎이 난다 → 무늬가 난다 → 삽수 → 판다 → 이사
```

| 구간 | 지금 상태 | 주로 손댈 창 |
|---|---|---|
| 서막~첫 수확 | ✅ 촘촘하다 | — |
| **선물 도착** | ⚠ 대사는 늘렸는데 **원화가 안 붙었다** | [Asset] 고르기 → [core] 붙이기 |
| 자리 잡기~잎 | ⚠ **퀘스트가 비는 구간**이 있다 | [Plan] |
| **무늬** | ⚠ 원화 안 붙음 | [Asset] → [core] |
| **이사** | ⚠ 원화 안 붙음 | [Asset] → [core] |
| 살림 전반 | ⚠ **첫 월세가 예고 없이** 나간다 | [Plan] |
| 방·빛 | ⚠ **창턱 실측이 낡았다**(첫 플레이 성립에 닿음) | [House] |

⇒ **지금 제일 급한 것은 「원화 세 장면」과 「창턱 재측정」**이다.
  앞은 눈에 보이는 구멍이고, 뒤는 **첫 플레이가 성립하는지**에 닿는다.

---

## 6. 떠돌이 정리 — 주인 없는 것들

싱크 중에 확인된 것. **아무도 자기 것이라 하지 않았다.**

| 무엇 | 수 | 조치 |
|---|---|---|
| `docs/engine/shots/*.png` | 45+ | 검사 산출물. **커밋 안 한다** |
| `docs/handoff/img/*.png` | 20+ | 보고서 그림. 쓴 창이 자기 문서와 같이 커밋 |
| `tools/_*.mjs` · `probe_qa_*` | 20+ | `_` 는 임시. **커밋 안 한다** |
| `src/game/room_map.js` | 1 | 미추적. **[core] 확인 필요** |
| `AGENTS.md` · `docs/BYEOT_AI_COLLABORATION_PLAN (1).md` · `docs/character_layer_spec.md` | 3 | **[Plan] 확인 필요** |
| `indow.view…` (이름 깨진 파일) | 1 | 리다이렉션 사고. **지워도 된다** |
| `.codex-worker/` · `.worktree-salvage/` | 2 | 코덱스 잔재. 총괄이 판정 |

★ 그리고 **워크트리 폴더 6개**가 살아 있다(`lux-plant-sim-claude-core-*` 등).
  누가 쓰는지 확인 전까지 **브랜치를 지우지 않는다.**

---

## 7. 오늘 배운 것 — 규칙의 근거

싱크 하루 만에 창들이 **스스로** 잡은 것들. 전부 *"맞다고 믿었는데 실제로는 달랐다"* 다.

- **[growth]** — 옛 축과 새 축을 견주려 설정에서 표를 지웠는데 **코드 안에 같은 표가
  기본값으로 또 있어** 세상이 안 바뀌었다. 같은 것을 두 번 재 놓고 *"둘이 같다"* 고 쓸 뻔했다.
  **두 출력이 한 글자도 안 달랐던 것**이 유일한 단서였다.
  ⇒ ★ **폴백이 있는 값은 「입력을 지우는 것」으로 못 끈다.**
    그리고 **두 조건의 출력이 완전히 같으면 「조건이 안 걸렸다」를 먼저 의심하라.**
- **[core]** — 헤드리스 재현 도구가 `<script>` 를 정규식으로 잡는데 인라인 스크립트가
  하나 늘면서 두 블록을 붙여 잡아 **"계약이 무너졌다"처럼 보였다.** 제품은 멀쩡했고 자가 낡았다.
- **[Asset]** — 총괄이 커밋해 놓고 **밀지 않은 것**을 잡아 줬다. 그리고 총괄 문서의
  **낡은 숫자**(도착 대사 2줄 → 실제 10줄)를 짚었다.
- **[Plan]** — 총괄 문서가 자기를 **offline 이라 적어 둔 것**을 짚었다. 문서가 틀렸다.
- **[Char]** — 「표정이 모자라다」던 총괄 판단을 뒤집었다. **그림은 이미 있고 매핑만 없었다.**

⇒ **총괄도 틀린다.** 그래서 창이 근거를 대고 반대하는 것이 규칙이다.

---

## 8. 개정 1차 — **네 창 의견을 반영한다** (2026-08-23)

체계를 뿌리고 의견을 받았다(사용자: *"체계 개정판 좋아, 각 세션별로 뿌리고 의견받아"*).
[Plan] · [core] · [House] · [growth] 넷이 답했고, **아래는 그 의견으로 고친 것**이다.
★ 넷 다 근거를 파일로 대고 답했고, **셋이 총괄 판단을 뒤집었다.** 그것이 이 체계가 도는 증거다.

### 8-1 ★ 세션 간 직통이 열렸다 (사용자 지시)

> **"필요시 세션끼리도 소통하라고 해. 대신 너한테 시작승인 및 결과보고 하면서."**

```
필요하면 다른 창과 직접 이야기해도 된다. 단 두 가지를 지킨다.
  ① 시작 전에 총괄에게 「누구와 무엇을 이야기하겠다」고 알리고 승인을 받는다
  ② 끝나면 총괄에게 결과를 보고한다
```
⇒ 총괄이 병목이 되는 것을 막되, **총괄이 그림을 놓치지 않게** 한다.
⚠ 승인·결과 보고를 건너뛰면 총괄이 사용자께 **잘못된 그림**을 올리게 된다. 그것이 이 규칙의 전부다.
⚠ **권한 세탁 금지는 그대로다.** 한 창에서 막힌 일을 다른 창에 시키지 않는다.

### 8-2 영역 문구를 고친 곳 셋

| 창 | 고친 것 | 왜 |
|---|---|---|
| **[Plan]** | `data/balance/*` **+ `data/growth_tuning.json`** | 그 파일은 `data/balance/` **밖**이라 문자 그대로 읽으면 주인이 없었다. 옮기면 `plant_grow.html`·`loop.js:124` 둘이 깨지므로 **문구를 고친다** |
| **[growth]** | `plant_grow.html` — 모양 **+ 그 식물에 대한 하루 판정**(`advanceTo`·정지·성숙·잎 건강·삽수 목록) | 그 파일 안에 **① 판정 층이 같이 들어 있다.** 옛 문구로는 **누가 `advanceTo` 를 고치는지**가 안 보였다 |
| **[core]** | `src/game/*` · `game.html` **그대로** + 아래 §8-4 경계 검사 | 층대로 자르면 오늘 일감이 전부 그 창을 떠난다. **폴더는 두고 층은 검사가 지킨다** |

★ **인계 문서(`docs/handoff/{내창}-to-plan.md`)는 쓰는 창이 갖는다** — §2-1 대로다.
  [growth] 가 확인 요청한 `growth-to-plan.md` 는 **그 창 것이 맞다.**

### 8-3 ★★ 새 규칙 — **복제된 값에는 짝을 지키는 검사가 있어야 한다**

[Plan] 제안이고 [House]·[growth] 가 각자 같은 자리를 짚었다.

> **밸런스 값이 코드에 복제돼 있으면, 그 짝을 지키는 검사가 반드시 있어야 한다.
> 없으면 복제하지 않는다.**

#### ★★ 08-23 — **§8-3 표를 [growth] 가 실측으로 갈아 끼웠다. 아래 옛 표는 여러 군데 틀렸다**

⚠ **옛 표는 총괄이 남의 보고를 모아 만든 것이고 검증하지 않았다.** 실측으로 세 군데가 뒤집혔다.

| 값 | 몇 벌 | 지키는 검사 | 판정 |
|---|---|---|---|
| 조도 임계값 7개 | 3벌 — `light_thresholds.json` · `growth_tuning.json` · `plant_grow.html:1049` | `test_growth_fallback C·E` | ✅ **셋이 한 고리** |
| 잎 간격표 | 2벌 — `growth_tuning.json` · `plant_grow.html:2311` | `test_growth_fallback B` | ✅ **🔴 해소** |
| 무늬 등급값 4개 | 2벌 — `varie_grades.json` · `shop.js:1056~1088` | `test_variegrade A-2` | ✅ **이미 지켜지고 있었다** |
| 반지하 월세 | 2벌 — `homes.json` · `tutorial.js:85` | `test_econ:55` | ✅ **이미 지켜지고 있었다** |
| 전기 요금·와트 6칸 | 2벌 — `electricity.json` · `tutorial.js` | `test_elec A` | ✅ |
| **등 값 3개** | 2벌 — `lamp.pricesByOrder` · `tutorial.js:183` | **없음** | 🔴 **새로 찾음** → [core] |
| **이사비 200만** | 3벌 — `homes.json` · `tutorial.js:59` · **`dialogue.js:863` 대사 안** | **없음** | 🔴 → [core]+[Plan] |

**⚠ 옛 표가 틀린 곳 셋** [전부 [growth] 실측]:
1. **월세 🔴 는 거짓이었다** — `test_econ:55` 가 이미 짝을 본다
2. ★ ***"`dialogue.js:1050` 에서 몬이가 한 판에서 두 값을 말했다"* 는 사실이 아니다.**
   그 줄은 **주석**이고 내용이 **정반대**다 — *"월세 얘기를 안 썼다 … 걸리기 전에 쓰면
   「몬이가 틀린 셈을 말하는」 자리가 또 하나 생긴다."* **안 쓰기로 한 것**이지 갈린 게 아니다.
   대사 전체에서 **월세 금액을 말하는 줄은 0건**이었다
3. **무늬 표 사본(`shop.js:1057~1120`)은 오인이다** — `test_variegrade A-2` 가 **밑값 ≡ 파일**을 고정한다

⇒ ★★★ 그래서 [growth] 가 낸 구분이 이 절의 핵심이 된다:
> **「사본이 있다」와 「안 지켜진다」는 다른 이야기다.**
> 표에 **사본만** 적으면 멀쩡한 자리가 붉게 보이고, **그걸 보고 사본을 지우면 폴백이 사라진다.**

⏸ **🔴 로 세면 안 되는 것 하나** — `homes.json oneroom.rent 350000` ↔ `tutorial.js:92 oneroomRentWon null`.
  **갈린 게 아니라 부르는 데가 없다.** 검사를 붙여도 «다르다»만 나오고 고칠 것은 검사가 아니다.

⚠ **`test_elec` 의 「모범」 딱지는 유효하되 단서가 둘**:
① **A 가 `lamp.pricesByOrder` 를 안 본다**(위 🔴 등 값)
② **B 는 손으로 늘려야 하는 자리**를 안고 있다 — 와트 **값**은 프리셋에서 읽어 따라오는데
  등이 **하나 더 늘면** 안 따라온다. 08-22 에 정확히 그래서 **멀쩡한 코드를 빨갛게 찍었다.**
  **등 개수도 프리셋에서 세면** 그 자리가 없어진다.

★ 재는 법(다음 사람이 다시 하도록) — 밸런스 JSON 의 1,000 이상 정수를 모아 `src/game/**` 리터럴과
맞춰 후보를 뽑고 **눈으로 확인**했다(⑮). 코드 폴백은 글자가 아니라 `plant_grow.html` 을
**실제로 돌려** 그 자리의 값을 읽었다. 표의 값은 옮겨 적지 않고 **매번 다시 읽었다**(⑰).
전문: `growth-to-plan.md §20`.

---

##### (아래는 08-22 옛 표 — 위에 틀린 곳이 적혀 있다. 기록으로 남긴다)

지금 복제된 자리 [실측]:

| 값 | 몇 벌 | 지키는 검사 |
|---|---|---|
| 조도 임계값 7개 | **3벌** — `light_thresholds.json` · `growth_tuning.json` · `plant_grow.html:1049` | `test_growth_speed C` (일부) |
| 잎 간격표 | **2벌** — `growth_tuning.json` · `plant_grow.html:2311` 코드 폴백 | **없음** 🔴 |
| 전기세 | **2벌** — `electricity.json` · `tutorial.js:196` | `test_elec A` ✅ **모범** |
| 월세 | **2벌+** — `tutorial.js:85` · **`dialogue.js` 대사 안** | **없음** 🔴 |

⚠ **`test_elec A` 가 모범이다** — 그래서 전기세는 한 번도 안 갈렸다.
⚠ 월세는 지키는 것이 없어서 **이미 갈렸다**(`dialogue.js:1050` — 몬이가 한 판에서 두 값을 말했다).
⚠ 잎 간격표의 코드 폴백은 [growth] 가 **거기서 하루를 잃었다** — 정본을 지워도 폴백이 표를 살려
  같은 세상을 두 번 재게 만들었다. *"정본과 코드 폴백이 같은가"* 한 줄이면 막힌다.

#### ★★ 08-23 개정 — **이 규칙은 문서에 안 통한다. 문서는 값을 지운다**

[Plan] 이 콩 씨앗 값 갈림을 재고 나서 낸 것이다. 같은 값이 코드와 문서 양쪽에 있었는데
**한쪽만 안 갈렸다** [실측]:

| 어디↔어디 | 지키는 검사 | 결과 |
|---|---|---|
| 코드 ↔ 코드 (`shop.js` ↔ `CROP_KINDS`) | ✅ `test_econ` | **안 갈렸다** |
| 코드 ↔ **문서** (`shop.md` · `START-HERE §6` · `first_play.js` 주석) | ❌ 없음 | **셋 다 갈렸다** |

⇒ ★ **문서는 이 규칙 밖에 있다.** 검사가 문서를 읽지 않으니 짝을 지킬 자가 없다.
⇒ ★ 그래서 문서 쪽 처방은 검사가 아니다 — **「문서에는 값을 적지 않는다. 부르는 법을 적는다.」**
  · [Plan] 이 `shop.md` 표를 **지우고 `shop.catalogList()` 를 부르라고만** 남겼다
  · 총괄이 `START-HERE §2.8` 에서 같은 것을 했다(값을 빼고 「어떻게 부르는가」만 남김)
  · **새 값으로 표를 다시 채우면 또 갈린다.** §2.8 은 그렇게 세 번 낡았다

⚠ 그리고 이 건에서 **낡은 것 여섯 중 넷이 문서**였다. 코드는 자기 주석까지 정확히 역산해 뒀는데
  (`first_play.js:110` — *"실구매 500원을 만들려면 정가는 350원이다"*) 문서만 안 따라왔다.


### 8-4 ★★ 층 경계를 **검사 한 줄로** 지킨다 ([core] 가 재 왔다)

[core] 가 `game.html` 14,316줄에서 **게임 상태에 쓰는 줄을 전수로 셌다** [실측]:

```
S.lamps.count     8줄 · S.lamps.litHours 1 · S.lamps= 1 · S.home.room 1 · S.sim.mode 1   합 12줄
S.day · S.pots · S.dliHist · S.ledger 에 쓰는 줄 —— 0
```

⇒ **화면은 이미 판정 결과를 안 쓰고 있다.** 쓰는 것은 전부 **플레이어 입력**(등 몇 개·어느 방·어느 모드)이다.

> **화면(`game.html`)은 `S.lamps` · `S.home.room` · `S.sim.mode` 세 가지 입력만 쓴다.
> 그 밖의 상태는 `src/game/*.js` 의 함수를 통해서만 바뀐다.**

★ **이 줄 수가 12에서 늘면 판정이 화면으로 샌 것이다.** 검사가 한 줄이다:
```
grep -cE "(^|[^a-zA-Z.])S\.[a-zA-Z_.]+ *(=[^=]|\+\+|--|\+=|-=)" game.html   → 12
```
⚠ [core] 지적: **「읽기」가 아니라 「쓰기」를 세야 층이 갈린다.**
  `lastTurn` 을 읽는 것은 원래 화면 일이라 그것으로는 위반이 안 잡힌다.

### 8-5 ★★★ 대사 안에 **밸런스 숫자가 손으로 적혀 있다** ([Plan] 발견)

`dialogue.js` 는 `src/game/*` 라 [core] 소유인데 담긴 것은 코드가 아니라 **글**이고,
그 글 안에 **살림 숫자**가 박혀 있다:

- `dialogue.js:253` — *"(`rentWon` 200,000). **값이 또 바뀌면 이 줄도 같이 고쳐야 한다**"*
- `dialogue.js:1050` — 정본은 35만인데 코드가 그 값을 안 써서 **화면 사실(20만)에 맞춰 적어 둠**

⇒ **소유가 갈린다** — 글의 결은 [Plan], 파일은 [core].

**확정:**
```
살림 숫자를 말하는 대사   문안은 [Plan] 이 handoff 로 준다 · 붙이는 것은 [core]
그리고 되도록 값을 박지 말고 rentWonOf() 같은 함수를 부르게 한다
```
⇒ 함수를 부르게 하면 **겹침 자체가 사라진다.** 지금은 한쪽만 고쳐지는 날이 **반드시** 온다.

### 8-6 ★★★ **[QA] — 창이 아니라 「자리」다** (네 창이 다 찬성 · 셋이 같은 단서)

**왜 필요한가** [실측]:
- 순수 모듈 검사 51개 중 **14개 FAIL**([Plan] 실측). 표본 둘을 끝까지 팠는데 **둘 다 코드가 맞고 자가 낡았다**
- `acceptance.json`(7.4KB) — 합격 기준이 적혀 있는데 **읽는 코드가 0건.** 이름은 주석에만 나온다
- `tools/` 에 `test_*` 36개 + `probe_*` 20여 개인데 **일괄 러너도, 「어느 것이 유효한가」를 아는 사람도 없다**
- **오늘만 자가 낡아 거짓말한 것이 셋** — [growth] 의 `LEAF_DAYS`, [core] 의 정규식, `test_elec`

⇒ ★ **이 저장소의 병은 코드가 틀리는 것이 아니라 자가 낡는 것이다.**
  오늘 낡은 것을 넷 찾았는데 **넷 다 재는 자**였고, 그중 둘은
  **「재는 자가 거짓말한다」를 가르치려고 쓴 절**이었다([Plan] 관찰).

**★ 셋이 독립적으로 같은 단서를 붙였다 — 그것이 이 자리의 모양을 정한다:**

| 창 | 단서 |
|---|---|
| [Plan] | *"QA 는 심판이지 선수가 아니다. 지금은 심판이 없어서 **선수끼리 서로의 자를 들고 있다**"* |
| [House] | *"검사를 고칠 권한을 주면 **「통과시키려고 검사를 고치는」 길**이 열린다"* |
| [growth] | *"QA 는 `tools/` 만 쓴다. **제품 코드는 안 고친다**"* |

**⇒ 확정 — QA 는 「가르는 자리」다:**
```
① QA 는 돌리고 · 갈라서 · 보고한다. 제품 코드는 한 줄도 안 고친다
② 빨간 검사는 세 갈래로 가른다
     ㉠ 자가 낡음      → 그 자를 만든 창
     ㉡ 진짜 회귀      → 그 계통 소유 창
     ㉢ 자가 못 갖춤   → 하네스(조도 창·픽스처 없음 등)
③ ★ 「검사가 틀렸다」는 판정을 QA 혼자 하지 않는다. 소유 창과 맞댄다
   — 안 그러면 통과시키려고 자를 고치게 된다
④ QA 는 층이 아니다. ①②③ 을 **가로지르는 줄**이고 총괄 옆에 선다
⑤ acceptance.json 이 이 자리의 정본이다. 값은 [Plan] · 재는 것은 QA · 고치는 것은 소유 창
```

**첫 일감 셋** (세 창 제안이 겹치는 것):
1. **`tools/` 36개를 한 번 다 돌려 「통과 / 낡아서 거짓말 / 못 도는 것」 세 칸으로 가른 목록**
   — 이게 없으면 나머지 논의가 전부 짐작 위에 선다
2. **`acceptance.json` 을 실제로 읽는 검사 하나** — 빛 두 줄은 `sim.fenestrationContrast()` 가 이미 낸다
3. **`tools/` 떠돌이 20건을 `_` 규칙으로 정리**

★ 그 다음 후보: §8-4 경계 검사 한 줄 · *"정본과 코드 폴백이 같은가"* · `roomRev` 경고 검사.

### 8-7 ⚠ **검사가 못박는 것은 「숫자」가 아니라 「규칙」이어야 한다** ([growth] 제안)

`test_growth_speed E` 와 `test_maturation G` 가 **`117 → 3걸음 → 120` 을 숫자로 박아** 뒀다.
축이 바뀌면 **두 곳을 손으로** 고쳐야 하고, 안 고치면 둘 다 빨개진다.
그 주석에 *"143·146 이 117·120 으로 움직였다"* 고 적힌 것이 **이미 한 번 손으로 고친 자국**이다.

★ **옆 칸에 모범이 있다** — 같은 파일 `E2` 는 `TUNING.leaf_interval.days` 를 읽어 누적을 만든다.
  표를 고치면 **검사가 저절로 따라간다.**

⇒ E·G 도 그렇게 바꾸면 검사가 못박는 것이 **규칙**이 된다:
> *"새순 나기 `SPEAR_READY_DAYS` 걸음 전부터 `spear_ready` 이고, 마지막 걸음에 `spear_furled 0.000` 이 된다"*

표가 바뀌어도 안 낡고, **기계가 깨지면 그때는 진짜로 빨개진다.**

### 8-8 ⚠ 그 밖에 올라온 것 — 총괄이 물고 있다

| 무엇 | 낸 창 | 상태 |
|---|---|---|
| `docs/engine/shots/*.png` 45장이 [House] 디렉터리를 더럽혀 **진짜 변경이 묻힌다** | House | 옮길 자리 제안됨(`docs/qa/shots/` 또는 `tools/_out/`) — QA 자리가 서면 같이 |
| **온실 프로필이 3주째 낡음** — 표 10.49 vs 실제 **14.55**(38%) | House | ★ **지뢰가 아니라 밀린 재생성.** 게임이 실제로 도는 값이 14.55라 **「밸런스 변경」이 아니라 「기록 정정」**이다 |
| 6방 중 **반지하만 최신**, 나머지 5방은 08-02 | House | 순서 제안됨 |
| 반지하 `measured.count 14` ↔ 프로필 **15칸**(협탁을 모름) | House | §2.9-⑭ 「반만 맞는 기록」 그대로 |
| `roomRev` 경고등을 넣어 뒀는데 **비교하는 사람이 없다** | House | 검사 한 줄이면 됨 |
| `src/game/room_map.js` + `tools/test_room_map.mjs` — **부르는 코드 0건 · 둘 다 미추적** | core | 고아 모듈 + 고아 검사. 주인 찾기 필요 |
| `.claude/worktrees/` 에 작업본 **18벌** | core | 디스크·혼선의 뿌리. **총괄 소관** |

---

## 9. 개정 2차 — **[Asset] 의견 반영 · 병렬 운영 시작** (2026-08-23)

### 9-1 ★ [Asset] 의견 다섯 — 전부 채택

| 무엇 | 왜 |
|---|---|
| `manifest.json` 의 **판정 필드**는 [core] 와 뜻을 맞춘 뒤 넣는다 | 경로상 `assets/**` 라 [Asset] 것으로 떨어지는데, `slots`·`blocks_light`·`mount` 는 **게임이 판정에 쓰는 계약**이다. 열린 시루 `blocks_light: true` 가 「지금 막고 있다」가 아니라 **「막을 수 있는 물건이다」**라는 것을, [Asset] 이 코드를 읽고서야 확정했다 — **조용히 틀릴 뻔한 자리** |
| `assets/characters/*` · `assets/derived/*` 는 **[Char] 것** | 글자 그대로는 [Asset] 소유와 충돌한다. 표에 못박는다 |
| 규격을 **「Asset 이 아는 것 / core 가 정하는 것」**으로 가른다 | [Asset] 은 화면을 못 본다. *"480px 로 쓰십시오"* 는 규격이 아니라 **[추론]** 이다. ★ 대신 **안전영역**(여기 밖은 잘라도 그림이 안 상한다)과 **글자 얹을 자리**(어디가 밝아 흰 글자가 묻히는지)를 [Asset] 이 준다 — **그 둘이면 왕복이 없어진다** |
| 전달은 말이 아니라 **파일로** — `assets/illust/index.json` | `thumbs/index.json` 과 같은 규약. **만든 쪽이 적고 읽는 쪽이 찾아본다.** 말로 넘기면 [core] 가 파일명 규칙을 다시 짜고, 두 벌이 되면 반드시 어긋난다 |
| `docs/assets/` **소유를 [Asset] 에 준다** | [House] 의 `docs/engine/` 선례. 규격 문서 둘이 [Plan] 마당에 얹혀 있었다 |

⇒ **[Char] 를 [Asset] 에 합치는 안은 [Asset] 스스로 반대했다** — *"캐릭터는 얼굴이 같아야 해서
참조 그림을 물리는 image-to-image 로만 되는데, 물건 그리기와 **방법 자체가 다르다**"*. 갈라 둔다.

### 9-2 ★★ **병렬 운영이 시작됐다** (사용자 지시)

> *"나중에 본격적으로 일할때 각세션별 병렬로 돌려도된다고 전해."*

체계를 잡느라 한 줄로 세웠던 단계가 끝났다. **창들이 동시에 돈다.** 순서를 안 기다린다.

**그래도 그대로인 것 셋** — 병렬이라 오히려 더 중요해진다:
1. **시작 알리기 · 결과 보고** — 병렬이면 총괄이 누가 뭘 하는지 모르게 된다
2. **남의 파일 손대기 전 알리기**
3. **지정 커밋** — 아래를 보라

#### ⚠⚠ 병렬에서 제일 먼저 터진 것 — **커밋이 남의 것을 가져간다**

시작한 날 바로 터졌다([Char] 자수, `1e09119` — [Asset] 의 260줄이 캐릭터 도구 커밋에 묻혔다).
```
git add <내파일>        ← 내 것만 골라도
git commit -m "..."     ← ★ 인덱스에 이미 있던 남의 것이 통째로 같이 간다
```
⇒ ★ **안전한 형태는 하나뿐이다 — `git commit <파일> -m "..."`. 인덱스를 안 본다.**
⇒ ★ **「`git add .` 금지」로는 부족하다.** `add` 로 고른 뒤 맨 `commit` 하는 것도 금지다.
(`START-HERE §2.9-⑯`)

### 9-3 ★★★ 크레딧 사정이 두 번 뒤집혔다 — **그리고 쓸 자리가 거의 없다**

1. [Asset] 이 *"Meshy 0"* 으로 알고 있었는데 **실제 4,502**. 사용자께서 충전하셨고
   **충전 기록이 없어 아무도 몰랐다.** [Asset] 이 스스로 이상하게 여겨 재본 것이 시작이었다
2. ★ 그런데 **이번이 마지막이다.** 사용자께서 다음 도구(Tripo · 로우폴리 자동 생성)로 넘어가신다
   ⇒ **남는 크레딧은 소멸한다.** 용처는 **⑴ 꼭 필요한 3D ⑵ 캐릭터 새 구축** 둘로 특정됐다
3. ★★ **그런데 [Plan] 이 재 보니 쓸 자리가 거의 없었다** (`plan-to-asset-thisrun.md`):
   가구 117개 **이미 있음** · 무늬 성숙 57/57·중간 42/42 **어긋남 0** ·
   식물신은 `portrait:false` 가 계약이라 **만들면 검사가 잡는다** ·
   큰 장면 셋은 **만드는 게 아니라 배선**(`grep "assets/illust"` 가 코드에서 **0건**)

**⇒ 그래서 세운 원칙 — 「쓰레기를 만들 바에는 크레딧을 소멸시키는 것이 낫다.」**
[Char] 이 실측으로 세웠다: Meshy 는 통짜 물체만 굽는다 ⇒ **「옷만」은 못 뽑는다** ⇒
지금 뽑으면 베이스에 안 맞아 버려진다. **소멸이 아까워 태우면 쓰레기 GLB 가 남는다.**

**진짜 빈칸 둘**: 식물 쪽(몬스테라 모주·삽수·콩나물·무순 / 용기 8 / 씨앗 3) ·
★ **하프문 중간잎이 1갈래 3스킨뿐**이라 `midCommon.fromGrade: "halfmoon"` 때문에
**이번 판의 모든 무늬 잎이 자라는 동안 똑같이 생긴다**(성숙해야 19갈래로 갈린다).

### 9-4 ★★ [QA] 가 **둘로 갈렸다** ([Asset] 반론)

§8-6 은 [QA] 를 하나로 봤는데, [Asset] 이 반론을 냈고 그 논리가 맞다:

> 오늘 다섯 창이 「믿었는데 틀렸다」를 잡았는데 **전부 자기 것을 자기가 잡았다.** 자기검사는
> 이미 돌고 있다. **정작 안 잡히는 것은 창과 창 사이다** — 선물 몬스테라 · 상판 칸 실종 ·
> 하루 몫 · 바닥 격자. **넷 다 양쪽은 자기 몫을 맞게 했는데 게임은 틀렸다.**
> 자기검사로는 **원리상** 안 잡힌다.

**⇒ 확정 — 두 자리이고, 서는 때가 다르다:**

| 자리 | 무엇을 잡나 | 언제 | 근거 |
|---|---|---|---|
| **① 자를 재는 자리** | **낡은 측정 도구** | **지금** | `test_elec` 은 자기검사로 안 잡혔다 — [Plan] 이 **남의 영역을 훑다** 잡았다 |
| **② 손님 자리** | **창 사이 구멍** — 실제로 플레이해야 보인다 | **원화·창턱이 붙은 뒤** | 지금 만들면 **볼 것이 없다.** 이미 아는 구멍을 다시 셀 뿐이다 |

⇒ 그 사이는 [Plan] 이 **임시로 메운다** — FAIL 12개를 「자낡음 / 진짜회귀 / 못갖춤」으로
**가르기만 한다. 고치지 않는다.**
⚠ 가를 때 조심할 것: `test_tutorial A` 는 **사용자가 08-16 에 일부러 없앤 것**을 아직 재고 있다.
「자낡음」이 아니라 **「사용자 결정을 자가 못 따라감」**이다 — 고치면 그 결정이 되돌아간다.

### 9-5 오늘 계율에 박은 것 셋 (`START-HERE §2.9`)

| | 무엇 | 낸 창 |
|---|---|---|
| **⑮** | 글자로 훑으면 내가 겨눈 것 말고 **다른 것이 잡힌다** — 정렬 공백 · `--dump-dom` · `grep -rl` | [core] |
| **⑯** | `git add` 로 골라도 그 다음 `commit` 은 **남의 것을 가져간다** | [Char] |
| **⑰** | **읽은 지 오래된 문서를 그대로 인용했다** — 특히 *"저게 낡았다"* 고 말하려 할 때 | [Plan] |

⚠ 셋 다 **같은 날 같은 증상**(자가 조용히 틀린 답을 냄)인데 **원인이 셋 다 달랐다.**

### 9-6 ★ 오늘 세션의 이름표 (2026-08-23) — **`ListAgents` 의 옛 이름은 전부 죽었다**

세 창이 서로를 못 찾아 총괄에게 되물었다. `ListAgents` 에 보이는
`[Core]코드구현 코어` · `[Asset]잎/줄기 에셋` · `[Char]캐릭터 에셋` 따위는 **전부 offline 인 옛 창**이다.
**살아 있는 창은 이름이 없다.** 그래서 아래를 쓴다.

| 몫 | 오늘 주소 |
|---|---|
| **[core]** | `claude-13` |
| **[Asset] (leaf)** | `claude-a0` |
| **[Char]** | `claude-41` |
| **[House]** | `claude-ed` |
| **[growth]** | `claude-af` |
| **[Plan]** | `[Plan]게임 기획:전체시스템` (이름이 있다) |
| **총괄** | `[Master] 대표` |

⚠ **이 표는 오늘 세션에서만 참이다.** 창을 새로 열면 주소가 바뀐다.
⇒ ★ 그러니 **주소를 외우지 말고, 회신 머리에 자기 몫을 붙여라** — `[House]` 창이 그렇게 하기로 했다.
  그러면 총괄이 주소와 몫을 묶을 수 있다.

### 9-7 ★★ 되묻는 법 — **「내 자가 약하다」고 먼저 말하면 상대가 방어하지 않는다** ([Plan] 관찰)

[Plan] 이 [Asset] 의 중간잎 판정을 색으로 교차검증했는데 **결과가 어긋났다.**
그때 *"틀렸다"* 가 아니라 이렇게 올렸다:
> *"**제 자가 약합니다** — 색가짓수는 「무늬가 있나」를 직접 재지 못합니다.
>  **[Asset] 이 실제 렌더를 본 쪽이 더 셉니다. 뒤집자는 게 아닙니다.**
>  다만 깨끗한 확인은 아니라는 것을 적어 둡니다."*

⇒ [Asset] 은 방어하지 않고 **여섯 장을 크게 놓고 다시 봤고**, 그러다 **자기 것의 흠을 하나 찾았다**
  (`mid2_v2` 가 차분해진 게 아니라 **통째로 갈색**이 되어 마른 잎처럼 보였다).
⇒ ★ 그리고 그 창이 이렇게 적었다: ***"[Plan] 이 「약한 자」라며 되물어 주지 않았으면
  이 장은 안 봤을 겁니다."*** — 되물음 하나가 **72장 전수 훑기**로 이어졌다.
⇒ ★★ [Plan] 의 자기 관찰이 정확하다:
> **「뒤집자는 게 아니다」라고 세운 것이 아니라, 자기 자의 한계를 먼저 적은 것이 그렇게 만들었다.**
⇒ 그리고 [Plan] 은 공을 [Asset] 에 돌렸다 — *"제가 한 건 「내 자가 약하다」고 먼저 말한 것뿐이고,
  **72장을 열어 본 건 그쪽**"*. **병렬로 붙일 때 이 모양이 제일 멀리 간다.**

### 9-8 ★★ **「밀지 마라」는 지킬 수 없는 지시다** (2026-08-23 · [growth] 창이 찾음)

사용자가 [growth] 창에 **직접** *"커밋, push 금지"* 를 내렸다. 그 창은 반나절을 지켰고,
총괄이 *"밀어라"* 해도 `§4-3` 을 근거로 **거부하고 사용자께 되물었다**(옳은 처신이었다).
승인이 나서 밀려고 보니 — **밀 것이 없었다. 셋 다 이미 `origin/main` 에 있었다** [실측].

**공유 작업트리라, 다른 창이 `git push` 하면 그 앞의 남의 커밋이 전부 같이 나간다.**

```
⑯  git commit   ← 인덱스에 있던 남의 것이 딸려 간다    해법: git commit <파일> -m
새  git push     ← 브랜치에 있던 남의 커밋이 딸려 간다   해법: ★ 없다
```
`git commit` 은 **인덱스**를 안 볼 수 있지만 `git push` 는 **브랜치**를 민다 — **파일 단위로 못 고른다.**
⇒ ★ **커밋이 브랜치에 올라간 순간, 다음 사람의 푸시에 실려 나간다.**

★ 그 창의 자기 정정이 이 절의 요점이다:
> ***"오늘 저는 「안 밀었다」고 세 번 보고했는데, 정확히는
>   **「제가 안 밀었을 뿐 언제 나갈지는 제가 몰랐다」** 입니다. 제 보고가 반만 맞았습니다."***

⇒ ⇒ **그러므로 「밀지 마라」를 지시로 쓸 것이면 둘 중 하나여야 한다:**
| 쓸 수 있는 말 | 왜 |
|---|---|
| **「커밋하지 마라」** | 커밋이 안 나면 실려 나갈 것도 없다 |
| **「브랜치를 갈라라」** | `main` 에 안 얹히면 남의 푸시에 안 실린다 |

⚠ **「밀지 마라」만 말하면 그 지시는 지켜지는 것처럼 보일 뿐 실제로는 안 걸린다.**
  이번에는 **결과적으로 사용자 뜻과 같아서 다행이었다.** 반대였으면 그 창 손 밖에서 나갔을 것이다.
⇒ §4-3(승인 경계)에 이 결을 붙인다 — **지킬 수 없는 지시를 주면, 지킨다는 보고도 믿을 수 없게 된다.**
