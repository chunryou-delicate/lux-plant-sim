# 손가락 조건표 — 언제 뜨고, 무엇을 짚고, 언제 안 뜨나

2026-08-30 · [House 마당: game.html §updateHint] → 총괄(머메이드 지도용) · [Plan]

총괄이 청한 「손가락 조건표」다. 트리를 그릴 때 이 표를 그대로 옮기면 된다.
값은 하나도 안 바꿨다. 여기 적힌 것은 지금 코드가 하는 그대로다.

---

## 0. 먼저 — 손가락이 **안 뜨는** 자리 (여기가 트리의 «가지치기» 지점이다)

순서대로 걸린다. 위에서 걸리면 아래는 아예 안 본다.

| # | 안 뜨는 까닭 | 자 |
|---|---|---|
| ⓐ | **쪽지가 떠 있다** | `coachNow` — 같은 순간에 안내가 둘이면 둘 다 안 읽힌다 |
| ⓑ | **대사 중이다** | `#stage.talking` — 박사님 「대사 끝나고 손가락이 나타나야지」 |
| ⓒ | **자세히 보기 중** | `#stage.zoom` |
| ⓓ | **집는 중 · 끄는 중** | `picked.mode` · `drag.on` — 지금 할 일이 이미 분명하다 |
| ⓔ | **첫 판이 꺼져 있다** | `!fp.enabled` — 안내는 첫 판에서만 돈다 |
| ⓕ | **짚을 것이 화면에 없다** | `onScreen(el)` 이 거짓 (좁은 화면의 [가방], 넓은 화면의 격자 등) |
| ⓖ | **시트가 그것을 덮는다** | `sheetCovers(el)` — ★ 2026-08-30 에 「시트가 열렸나」에서 바꿨다 |

⚠ ⓖ가 오늘 고친 구멍이다. 넓은 화면(1770×1188)은 시트가 **늘 펴져 있어서**
「열렸나」로 물으면 네 갈래가 통째로 죽었다. 「이것을 덮나」로 물으면 안 죽는다.

⚠ ⓑ는 구멍이 아니라 규칙이다. 재는 판에서 대사를 안 걷고 재면 언제나 「손가락 없음」이 나온다.

---

## 1. 뜨는 자리 — 위에서부터, 먼저 걸리는 하나만 뜬다

| 차례 | 조건 | 짚는 것 | 말 |
|---|---|---|---|
| 1 | 놓기 확인창이 떠 있다 | `#placeOk` | 여기에 둘까요? — [확인] |
| 2 | 가구 [옮기기] 중 | 갈 자리(점) | 여기로 끌어 보세요 |
| 3 | 몬스테라를 옮기는 중 · 이사 직후 | 밝은 칸(점) | 여기로 끌어 보세요 — «칸 이름» |
| 4 | **사람을 한 번도 안 눌렀다** (시루는 놓았다) | ①사람 → ②방바닥 → ③(걸어감) | 세 걸음짜리 캐릭 이동 안내 |
| 5 | 시루를 아직 안 놓았다 | `#cropThumb` / `#openBag` | 끌거나 눌러서 방 안 어두운 자리에 |
| 6 | 몬스테라가 가방에 있다 | 가방 칸 / `#tabBag` / `#openBag` | 끌거나 눌러서 방 안 밝은 자리에 |
| 7 | 씨앗을 살 때가 됐다 | `#openBag` → 상점 → 물건 → [주문] | 씨앗을 미리 주문해 두세요 |
| 8 | 시루를 하나 더 살 때 | 같은 사슬 | 거두는 날이 엇갈립니다 |
| 9 | 무순 재료를 살 때 | 같은 사슬 | 무순은 판과 씨앗 둘이 필요합니다 |
| 10 | 말풍선이 떠 있다 | `#marks .mark` | 거둘 때가 됐습니다 / 물을 줄 때입니다 |
| 11 | 거두기 단추가 산다 | `#harvestCrop` | 거둘 때가 됐습니다 |
| 12 | 물주기 단추가 산다 | `#waterCrop` | 물을 주면 자라기 시작합니다 |
| 13 | 몬스테라가 목마르다 | `#potWater` | 몬스테라에 물을 줍니다 |
| 14 | 살림을 아직 안 봤다 | `#navRoom` / `#openBag` | 여기서 살림을 봅니다 |
| 15 | **더 할 것이 없다** | `#next` | 씨앗은 내일 옵니다 / 자라는 중입니다 / 하루를 보냅니다 |

⚠ 15는 두 바퀴를 거두고 나면 스스로 그친다(`taughtCycles`). 다만 **씨앗이 오는 중**인 것은
예외로 계속 짚는다 — 그때는 누를 것이 아예 없는데 화면도 조용해서, 안 짚으면 사람이 멈춘다.

⚠ 4는 사람을 **고른 적이 있나**(`coachSeen().has('walk')`)로만 끈다. 「걸었나」로 세면
물 주다가 저절로 걸어간 것도 「배웠다」가 되어 안내가 안 뜬다(그렇게 틀렸었다).

---

## 2. 덮개(울타리)가 같이 뜨는 자리

손가락이 뜨면 덮개도 같이 뜬다(`dimAt`). 짚은 자리 **밖**에서 시작한 손짓은 삼키고,
손가락이 한 번 뛴다. ⚠ [다음 날]은 겹침이 아니라 **z-index 71** 로 늘 눌린다 —
구멍을 둘 뚫어 겹치면 evenodd 가 다시 메워 버린다(그렇게 하루가 안 넘어간 적이 있다).

---

## 3. 밖에서 읽는 문 (자를 짤 때 쓴다)

- `window.__hintLast` — 마지막으로 «무엇을·무슨 말로·어느 줄이» 짚었나. `{id, say, 보임, 자리}`
  ⇒ 「왜 손가락이 없나」를 갈래를 손으로 뒤지지 않고 한 번에 답한다.
- `window.__pickState()` — 집는 중 · 끄는 중 (위 ⓓ를 밖에서 가른다)
- 둘 다 읽기용이다. 게임은 안 쓴다.

---

## 4. ⚠ 따로 걸린 것 — 가구 옮기기가 죽는 자리 (내 것이 아니다 · 이미 있던 탈)

`tools/test_uiwire.mjs` **P-2** 가 떨어져 있다. 오늘 고친 것과는 무관하다 —
HEAD 판(내 고침을 뺀 판)으로 갈아 끼우고 재도 **똑같이 떨어진다**(실측).

- 증상: 시점을 돌린 뒤(F-9 가 112px 돌린다) `banjiha-bed` 를 **비껴 잡으면**
  유령이 안 선다(`ghost = null`) ⇒ 아래글이 「방 안으로 끌어 주세요」에서 안 나간다.
- 안 돌린 판에서 같은 자리를 잡으면 잘 선다(`probe_grabjump` · 세 번 다 1.02m).
  ⇒ 시점을 돌린 뒤에만 바닥 광선이 빗나간다.
- ★ 이것이 박사님 「피씨에선 옮기기가 잘 안 되네」의 절반일 수 있다.
- 자: `tools/probe_grabjump.mjs` (오늘 새로 지었다)
- 임자는 `game.html §furnPicked.dragTo` → `freePlace.surfaceAt`.
  ⛔ 내가 손대지 않았다 — 자리 임자가 [Plan]/총괄의 몫이라 여기 적어만 둔다.


---

## 5. 2026-08-30 덧 — 「보이는데 안 눌리는 것을 짚는다」

하루에 같은 병을 다섯 번 잡았다. 따로 난 벌레가 아니라 **한 뿌리**다:
화면이 「저것이 보인다」와 「저것이 눌린다」를 **같은 것으로 여겼다.**

| 잡힌 자리 | 무엇이 덮었나 | 고친 자 |
|---|---|---|
| [거두기]가 안 눌린다 | 잠긴 [물주기]가 같은 자리에 얹혀 있었다 (둘 다 `bottom:12px`) | 알림뿐인 줄이 물러난다 |
| 넓은 화면에서 손가락이 다 죽는다 | 시트가 늘 펴져 있다 | `sheetCovers` — 열렸나가 아니라 덮나 |
| [주문] 개수 창에서 손가락이 없다 | 시트 네모가 창과 겹친다 | `.pop`(z 84)은 시트(z 40) 위다 — 안 덮인다 |
| [다음 날]을 아홉 번 눌러도 날이 안 간다 | 밥상 창이 화면을 통째로 덮는다 | 창 «밖»은 아예 안 짚는다 · 창 안 [이대로 다음 날 ▸]을 짚는다 |
| 탭을 바꾼 뒤에도 손가락이 그 탭에 붙어 있다 | 아무것도 안 덮었다 — **다시 안 물었다** | 지킴이가 반 초마다 다시 고른다 |

★ 마지막 것이 뿌리다. 따라다니는 고리는 **자리만** 옮기고 「무엇을 짚을까」를 다시 안 물었다.
  탭 바꾸기·시트 열닫기·창 닫기처럼 **다시 그리는 계기가 없는 손짓**이 여럿이라,
  그 자리마다 `updateHint()` 를 부르는 대신 한 곳에서 반 초마다 다시 묻는다.

### ★ 그 뒤에 한 번 더 — 「밥상 창만」 고쳤다가 「가계부 창」에서 또 샜다

창 밖을 안 짚게 막고(위) 창 «안»은 **밥상 창 하나만** 짚게 했다.
⇒ 총괄이 Day 1 「첫 달 가계부」에서 멈췄다 — 밖은 못 짚고 안은 안 짚어 손가락이 통째로 쉬었다.

[Plan]이 이것을 계율로 올렸다:

| 짝이 **있는** 것 | 짝으로 고친다 | 잎·무늬 |
| 짝이 **늘어나는** 것 | **한 자리에서 묻는다** | 창 |

⇒ 그래서 창 id 를 한 군데도 안 쓴다. 묻는 것은 「이 창에서 **일하는 단추**가 무엇인가」다:
`button.go` → 없으면 `button.primary` → 그것도 없으면 **살아 있는 마지막 단추**
([✕]·[그만두기]는 앞쪽에 서므로 마지막이 곧 하는 쪽이다).

말도 같은 손으로 고른다 — **그 단추에 「다음 날」이라 적혀 있으면** 하려던 말(§wait)을 그대로 쓰고,
읽는 창(가계부)에서는 이미 쓰는 「여기를 눌러 보세요」다.
⚠ 처음엔 가계부에도 wait 을 붙였더니 [알겠습니다] 위에 「하루를 넘겨 보세요」가 떴다 —
가리킴과 말이 어긋났다. 재서 고쳤다.

### ✔ 폰 구멍은 [Plan] 문안으로 메웠다

「[다음 날]이 가려졌습니다 — 한 번 더 누르면 닫힙니다」 · 짚는 곳은 **지금 열린 그 탭 단추**.
⚠ 「시트」라는 말을 안 쓰는 것도, 「가방을」을 뺀 것도 [Plan]이 일부러 그렇게 지었다 —
손가락이 그 탭을 짚고 있으니 **가리킴이 말의 절반을 대신한다.**

### 지금 서 있는 자 — probe_force5

「손가락만 따라가면 판이 돈다」를 그대로 판정으로 낸다(떨어지면 나가는 값 1):
① 한 바퀴가 돈다 ② 제자리걸음이 없다 ③ 마지노 안에 끝난다 ④ 손가락이 끊긴 자리가 적다

⚠ ③의 날짜를 자에 안 박았다 — 콩나물 주기를 코어에게 묻는다.
⚠ ④를 0 으로 안 박았다 — 대사·연출 중에는 손가락이 일부러 쉰다.

실측(2026-08-30 · 두 바퀴):

| 판 | 끝난 날 | 손 | 끊긴 자리 |
|---|---|---|---|
| 390×844 | Day 11 | 37 | 0 |
| 1770×1188 (시트 연 채) | Day 11 | 40 | 0 |

### ⚠ 지난 구멍(메웠다) — 폰에서 [다음 날]이 시트에 가릴 때

폰(390×844)에서 **시트를 연 채**로 「하루를 넘길 일」만 남으면 손가락이 없다.
[다음 날]이 시트 밑이라 짚을 수 없고, **「시트를 닫으라」고 말할 자리가 없다.**

- 게임이 막히는 것은 아니다 — 손잡이를 끌거나 아래 띠의 **같은 단추를 한 번 더 누르면 닫힌다**.
- 넓은 화면에서는 안 난다(시트가 [다음 날]을 안 덮는다).
- ⇒ 짚을 곳은 이미 있다: **지금 열려 있는 그 탭 단추**(한 번 더 누르면 닫힌다).
  ⛔ **말을 내가 짓지 않았다.** 「한 번 더 누르면 …」 꼴이 이미 여럿 있으니(§COACH_NOTES)
  그 결로 한 줄 주시면 그 자리에 그대로 넣는다.


---

## 6. 2026-08-31 ~ 09-02 덧 — 몬스테라가 끌려서 창턱에 서기까지, 그리고 탈출구

박사님이 폰으로 걸으시며 막힌 자리 셋을 그림과 함께 주셨다. 셋 다 실측으로 갈랐고 걸어서 확인했다.

| 박사님 말씀 | 임자 | 고친 자 |
|---|---|---|
| 「옮기기 누른 뒤 드래그가 안 된다」 | 옮길 때 잡는 자리는 방바닥이고 그 손짓은 덮개 «밑»(z 28)이 받는다. 손가락은 «갈 자리»를 짚으니 잡는 손은 늘 구멍 밖에서 시작해 삼켜졌다 | 무대가 「옮기는 중」(moving·furnmoving·placing)이면 울타리가 물러선다 |
| 「거둘 때 시루에 수확 버튼에 손가락 가이드를」·「물주기도 시루 쪽에」 | 왼쪽 아래 단추는 «어느 시루»인지를 안 말한다. 곧 다섯이 된다 | 말풍선을 먼저 짚는다 — 다만 「그 말풍선이 시키는 일이 지금 되는가」를 코어에 물어서만(안 그러면 「열고 닫기 38번」) |
| 「첫 몬스테라 배치 후 뭘 할 수가 없어」(허공을 짚는 손가락) | 점으로 짚는 손가락은 «한 번» 놓고 끝이었다. 시점이 돌거나 판이 바뀌면 옛 자리에 남는다 | 부르는 쪽이 「지금 어디인가」(`pointOf`)를 주고 고리가 프레임마다 다시 놓는다 |

### 탈출구 — 총괄이 정한 안전 문턱

울타리 밖을 **세 번** 눌렀는데 판이 안 바뀌면 그 걸음 동안 울타리를 푼다. 손가락은 그대로 둔다.
셈은 「짚는 것이 바뀌면」 0 으로 돌아가 다음 걸음에서 울타리가 다시 선다.
⚠ «누름»만 센다 — 한 번 누르면 `pointerdown` 과 `click` 이 둘 다 오므로 같이 세면 「세 번」이 한 번 반이 된다.
⚠ 값이 아니라 안전이라 총괄이 단다. 실측(probe_pointfollow): 세 번째까지 손가락이 뛰고 그다음부터 풀린다.

### 「탭 → 옮기기 → 창턱으로」를 네 걸음으로

① 👉 그 화분 ② 👉 [옮기기] ③ 👇 「여기로 끌어 보세요 — 창턱 화분받침」(이미 있던 갈래) ④ 끌어서 선다.
[옮기기]를 «누른 순간»부터 ③으로 넘어가게 조건만 풀었다(여태 「이미 잡고 끄는 중」일 때만 떴다).
그리고 이미 그 자리면 안 짚는다 — `fp.phase` 는 하루가 지나야 꺼져서, 세운 뒤에도 계속 짚어 여섯 번 헛돌았다.
⚠ ①②의 말은 이미 쓰는 「여기를 눌러 보세요」로 세워 뒀다. 문안은 [Plan] 몫이다.

### 「시트가 열렸나」는 넓은 화면에서 거짓이다

시트가 클래스 없이 늘 펴져 있어 `sheetOpen` 이 거짓이다 ⇒ 선물이 온 날 가방 갈래가 죽어 손가락이 상점으로 샜고,
거둔 뒤 스무 날 내내 조용했다. ⇒ 「열렸나」가 아니라 「그 점에서 무엇이 잡히나」(`elementFromPoint`)로 묻고,
상점·가방으로 가는 문은 「지금 어느 문이 보이나」로 고른다(이미 열린 탭은 안 짚는다).

### 서 있는 자

- `probe_force5` — 손가락만 따라 걷는다. 말에 「끌어」가 있으면 «끈다». 판정에 「몬스테라가 창턱에 섰다」가 있다.
  두 바퀴를 거두면 손가락을 일부러 떼므로 그 뒤는 「안내가 끝났다」로 적고 멈춘다(예전엔 그것이 붉게 나왔다 — 자가 거짓으로 운 것).
- `probe_bagdragpot` — 가방의 그루를 누름·DOM 누름·끌기로 놓는다. 「울타리가 아직 막나」도 잰다.
- `probe_pointfollow` — 점 손가락이 시점을 돌린 뒤에도 따라오나. 탈출구도 같이 잰다.

실측(2026-09-02):

| 판 | 결과 |
|---|---|
| 1770×1188 · 시트 연 채 | 한 바퀴 2/2 · 제자리걸음 없음 · Day 17 · **몬스테라가 창턱에 섰다 → banjiha-sill:0** (끌어서) |
| 390×844 | 같은 자리까지 · 끊긴 자리 10번(대사·연출 중 잠깐 비는 것) |
| 점 손가락 따라오기 | 시점 0.79 → 0.02 로 돌린 뒤 어긋남 0px (폰·넓은 판 둘 다) |

⚠ 「끌기」는 데스크톱 진짜 마우스로 낸 값이다. 폰 손가락은 자로 못 낸다.
  다만 그루 칸의 `touch-action` 이 방 끌기(`#moveCatcher`)와 같은 값(`none`)인 것은 확인했다.


---

## 7. 2026-09-02 덧 — 무순 «다음 날» · 퀘스트 「뜬 날+열쇠」 표 · 밥상 상자 · 「2바퀴째」 줄 없앰

| 무엇 | 자 | 잰 값 |
|---|---|---|
| ② 무순은 몬스테라가 집을 잡은 «다음 날»부터 | 「왔나」→「옮겼나」(monsteraHomed) + 옮긴 뒤 하루(guide.movedDays≥1 ‖ grewOnce). 상점 무순 문도 같은 셈 | 창턱 d11 → crop_mix 뜬 **d12** (전에는 d11 · 같은 날 둘) |
| ③ 퀘스트가 «뜬 날» + «열쇠» | probe_force5 끝에 찍는다. 뜬 날은 걸어서, 열쇠는 코어 `opens` 그대로 | `monstera_home`은 열쇠가 `siru_two 끝`이라 그루가 창턱에 서고도 «영영» 안 뜬다 — 사슬 다시 짤 때 볼 자리 |
| ④ 밥상 설명 상자 | 창 «안» `<details>` · 문안 넷 [plan] 그대로 · 처음 사흘 펴고 그 뒤 접힘(3 · core 것) · 펴서 창이 구르면 접는다(「창은 안 구른다」가 윗법) | d5·6·7 펴짐 → d8 접힘 · questui 57/57 |
| ⑤ 「한 시루를 2바퀴째」 줄 없앰 | resow_siru → RETIRED_QUESTS(이름만) · order_seed.after = first_harvest · 문안 ㉮㉯㉯③ | first_harvest 끝 d5 → order_seed 뜬 **d5** · 사슬 안 끊김 |

⚠ 손가락은 두 바퀴까지만 이끈다(박사님 2026-08-17). order_seed 의 done(세 바퀴)·siru_two 는 그 너머라 걸음 자로 못 잰다 — 사람이 걸어야 한다.
⚠ 자 둘이 HEAD 에서도 떨어져 있다(내 손 아님): probe_questchain E-x/E-1(siru16 걸음) · probe_questorder C-1(모형에 무순 다섯을 사는 손이 없다).

### ⑥ 독촉 낯 넷 — 자리는 있고 «열린 날» 칸이 없다 (총괄 결정 대기)
「빈 날에만·맨 뒤」는 `dialogue.createStoryteller`가 이미 그 규칙이다(조용한 날 이틀 넘으면 작은 말 하나 · 가장 오래 안 나온 것부터). 낯 넷은 CHATTER 표에 넉 줄로 얹으면 된다.
없는 것: 퀘스트가 «며칠 전에» 열렸나 — `S._questOpen`은 세이브에 안 들어간다. ㉮ `questsOpenedOn` 칸을 적는다 / ㉯ 안 적고 조용한 날 수로만(끄면 잊는다). 내 눈엔 ㉮.


---

## 8. 2026-09-03 덧 — 사슬을 다시 짰다(㉱) · 독촉 낯 넷과 「열린 날」 칸(⑥ ㉮)

[plan] plan-quest-reframe ㉱(263b920) 그대로. 값은 하나도 안 바꿨다 — 열쇠(자리)만 옮겼다.

| 줄 | 예전 열쇠 | 지금 열쇠 | 걸어서 뜬 날(1770×1188 · 시트 연 채 · Day 0 부터 손가락만) |
|---|---|---|---|
| monstera_home | siru_two 끝 (영영 안 뜸) | 몬스테라 «왔나» | 뜬 **d11** · 끝 **d11** |
| leaf_two | monstera_home 끝 | 그대로 | 뜬 **d11** (저절로 살았다) |
| siru5_cycle5 | crop_mix 끝 | firstPlayDone ⇒ ⚠ §9 에서 또 바뀐다 | 뜬 d— (새순 말림이 Day 17 너머) |
| crop_mix | monsteraHomed+1일 | siru5 끝 | 뜬 d— |
| radish5 | — | crop_mix 끝 | 뜬 d— |
| siru_two | 퀘스트 | RETIRED — 「하나 더」는 손가락(강제 가이드)이 끈다 | — |

⑥ 독촉 — `stamina.questsOpenedOn[id] = 열린 날`(총괄 ㉮ · 세이브 왕복 자 PASS) · 낯 넷은 CHATTER 표에 `nudge:true` 넉 줄 · 문턱은 [plan] 밑값 `NUDGE_DAYS {ask:4, worry:7, back:14}` · ③은 몬이에 worry 낯이 없어 sad.
⛔ 걸어서 잰 것(probe_nudge · 18일 · 아무것도 안 함): 독촉이 **한 번도** 안 났다 — 스토리텔러가 「조용한 날 이틀 뒤」 하루만 말하고, 그날마다 알맹이 있는 잡담이 있어 «맨 뒤»인 독촉까지 차례가 안 왔다. ⇒ [plan]이 차례를 다시 세웠다(§9).


---

## 9. 2026-09-04 덧 — ㉲ 「세팅 끝」 열쇠 · 강제 가이드가 끝나는 곳 · 독촉 차례(사건 > 독촉 > 잡담)

[plan] 93a35e1(㉲) · d90ab11(독촉 ⓖ~ⓛ). 값 변경 0 — 열쇠와 차례만.

### ㉲ 열쇠 셋

| 무엇 | 예전 | 지금 | 걸어서(1770×1188 시트 연 채 · 390×844 · Day 0 부터 손가락만) |
|---|---|---|---|
| siru5_cycle5 열쇠 | firstPlayDone(새순 말림 · Day 17 너머) | 콩나물 시루 «둘 놓임»(placed) && 몬스테라 «창턱»(guide.moved) | 창턱 d11 → 「하나 더」 주문 d11 → 시루 도착 d13 → 놓음 d13 → **뜬 d13** (두 판 같음) |
| 「하나 더」 손가락 열쇠 | order_seed 끝(세 바퀴 · 못 닿음) | 창턱 && 시루 하나뿐 | 창턱 «그날»(d11) 씨앗 주문 바로 뒤에 켜진다 — 같은 날 셋(창턱·씨앗·시루) ⇒ [plan] 볼 자리 |
| 강제 가이드 끝 | 없음(늘 따라다님) | 「세팅 끝」(둘 놓임 && 창턱) ⇒ 손가락 «없음». 첫 새순(fp.completed)은 사건 | d13 놓는 순간 손가락이 떨어진다. 그 뒤 살림 안내(fp.completed 뒤)는 예전 그대로 |

걸어서 잡은 구멍 둘(고쳤다):
- 둘째 시루가 가방에 온 d13 — 놓는 갈래가 「아직 하나도 안 놓음」에만 걸려 손가락이 없었다 ⇒ 창턱 뒤 놓인 시루가 둘 미만 && 가방에 빈 시루면 같은 갈래.
- 넓은 판은 [가방] 문이 `openBag` 이 아니라 탭 `tabBag` 이다 ⇒ 몬스테라 가방 갈래와 같은 문 고르기. d12(시루 오는 중)에도 「시루는 내일 옵니다」가 선다.

### 독촉 차례

| 자 | 잰 값(probe_nudge · 26일 · 첫날만 심고 물 주고 그 뒤 아무것도 안 함) |
|---|---|
| ①②③ 안 한 날마다 한 줄 | d2~4 권함 · d5~7 궁금 · d8~14 걱정 — 안 한 날 13 · 독촉 13 · 하루 둘 없음 · 그날 다른 말 없음 |
| ④ 물러섬은 잡담 줄로 | d15 물러섬 · d18 잡담 · d21 물러섬 · d24 월세 사건 — 「잡담 하나 뒤에 물러섬 하나」(동률이면 표 앞엣것이 이겨 물러섬이 영영 안 났다 → 고침) |
| 「이틀 뒤」 규칙 | 독촉 ①②③은 그 값을 «안 거친다»(quiet 세기 밖) · 값 그대로 2 |

자: coverage PASS · questchain 16/16(E 옛 표는 placed·monsteraHomed 를 일부러 안 채우므로 siru5 뒤 넷이 undefined 인 것이 뜻대로 — 옛 값 옆에 남김) · questorder 6/7(C-1 은 HEAD 부터) · roundtrip PASS · force5 두 판 5/5.


---

## 10. 2026-09-04 덧 — ⑦ 침대 눕기·낮잠·의자 앉기 · 무순 손가락의 때 · [house] 계약 D

[plan] plan-nap-and-sit.md 그대로. 값(10%·올림·하루 1번·두 시간)은 **침대 프리셋**(`data/furniture_presets.json` bed_* 의 `nap_recover_frac`·`nap_hours`)에 두고 코어(`stamina.nap`)와 화면 시계가 읽는다. 없으면 던진다(0 으로 조용히 안 자는 침대를 안 만든다).

| 무엇 | 자리 | 걸어서(probe_nap · 1770×1188 · 첫 플레이 끔 · 진짜 마우스) |
|---|---|---|
| 앉기·눕기 | 방뷰 `ACT_SPEC.sit/sleep`(hold) — 물·심기·거두기와 «같은 길»(actAt)인데 끝 자세를 «잡고 있는다»(clampWhenFinished · idle 변주도 쉰다). 열쇠는 가구 uid(`resolveKey` 가 가구도 본다). 설 자리는 가구 «발자국 밖»(중심에서 사람 쪽으로 반너비+0.35m) — 화분처럼 둘레 0.7~1.35m 를 노리면 침대(1×2m) «안»이라 길이 안 간다 | 의자: 한가운데 앉음(그림 nap_sit.png) · 침대: 위(y 0.49)에 누움(nap_sleep.png) · 15/15 |
| 일어나기 | `standUp()` — 걸어갔던 자리로 돌아온다. 바닥을 눌러 걷게 해도 먼저 일어난다. ★ 앉아 있으면 사람이 가구 한가운데라 가구를 눌러도 «사람»이 잡힌다 ⇒ 앉은 사람을 누르면 «그 가구 메뉴»가 열린다(일어서기가 거기 있다) | [일어서기]·[일어나기] 글자 바뀜 · 바닥 걷기 OK |
| 낮잠 | `stamina.nap(S,{frac})` — ceil(max×몫) · `nappedOnDay`(세이브 왕복 자 PASS) · 시계 `napClockSkip(hours)`(하루는 안 넘긴다) | 체력 3→4(max 5 × 0.1 올림) · 시계 +2h · 같은 날 「오늘은 이미 잤습니다 — 내일 다시」 잠김 |
| 무순 손가락의 때 | [plan] cb60cc6 ②: crop_mix 「열림」(= siru5 끝) — 상점 무순 문과 같은 자. 할 일 글에 「무순」이 보이나로 재던 것을 뗐다 | (걸음 자 밖 — 세팅 끝 너머) |
| [house] 계약 D | `room_profile.js §dliOfSlot` 이 표에 없는 자리(특히 free:)에 0 대신 «던진다» — `cropDliFromReport`(302a4fe)와 같은 결 | probe_floor_contract ㉢ 「던짐」 · first_play PASS |

⚠ 헤드리스 크롬은 그림이 느려(2fps 안팎) 사람 걸음의 dt 가 0.1초로 잘린다 — 1.2~1.6초 클립이 열 몇 초 걸린다(앉기 45초). 자는 90초까지 기다린다. 진짜 브라우저의 값이 아니다.
⚠ 눕는 높이(`sleepLift` 0.45 = 가구 h − 매트리스 두께 어림)는 그림으로 맞춘 값이다. 침대가 바뀌면 다시 본다.
