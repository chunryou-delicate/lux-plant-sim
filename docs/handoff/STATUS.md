# 창별 상태 — 요약만. 상세는 {창}-to-{상대}.md

> **자기 섹션만 고친다.** 섹션이 나뉘어 있어 git 이 자동 병합해 준다.
>
> ⚠ **파일을 통째로 다시 쓰지 말 것.** 2026-08-01에 실제로 사고가 났다 —
> 한 창이 전체를 재작성하고 다른 창이 그걸 커밋하면서 **두 창의 섹션이 날아갔다.**
> (git 이력에서 복구함) **자기 `## {창}` 블록만 편집한다.**

```markdown
## {내창}
갱신: 2026-08-01 (커밋 abc1234)
완료: (이번에 한 것 2~4줄)
발견: (★다른 창이 알아야 할 것 — 없으면 생략)
대기: (누구의 무엇을 기다리나)
판단필요: (★사용자·plan이 정해야 할 것)
```

★ **"발견"과 "판단필요"가 핵심.** 완료는 커밋 로그에도 있지만 저 둘은 여기 없으면 어디에도 없다.
커밋·**푸시**까지 해야 읽힌다.


---

## growth
갱신: 2026-08-02 (커밋 f9c5c68)
완료:
- **형태 진행도와 활력은 다른 축.** novice 는 형태 진행도 한 축만 본다 — 적정광에서 오르고 저광에서 멈추며 **줄지 않는다**. 코드가 이미 그 모양이라(`GROWTH` 는 `advanceTo` 에서 +1 만 하고 감소 경로가 없다) 손댈 게 없었다. **vigor·HP·시듦·고사는 구현 안 함**(고수 모드 범위)
- **CFG_VER 되돌림** — `phase-draft` 로 올렸던 것을 `strict5` 로 복구. phase 로직·기본값 변경이 없는데 버전을 올려 `mcfg_default` 의 P·ADJ 가 버려질 뻔했다(시드만 살아남는다). 저장값은 삭제·초기화하지 않았고 복원 재현 PASS
- ⏸ **vigor 확정안 [취소·보류]** — `growth-to-core.md` 의 체력 모델 절을 정정했다. 유효한 건 "활력은 형태 진행도와 별개 축"이라는 방향뿐이고, 밴드별 증감·잎 손실 주기·죽음 임계값은 **전부 후보**다. `vigor()`·`isDead()` 는 구현·요청 대상이 아니니 **배선하지 말 것**
- `thLoaded()` 읽기 전용 접근자 추가. 세터는 안 둔다 — 밖에서 켜면 로딩 게이트가 장식이 된다. false→true 재현 PASS, A~I 회귀 없음
- **`growthPhase()` 계약 초안 회신**(⏸ 구현 안 함 — plan 의 게이지 표시 사상 결정 대기) — `{phaseId, progress01, nextPhaseId}`. 반환값에 생장일이 없어 **core 가 143·146 을 복제할 수 없다**. 전문은 `growth-to-core.md`
- ★ **정본 로딩과 렌더 오류를 갈랐다.** 다시 그리다 난 예외가 '정본 로딩 실패'로 둔갑해 `TH_LOADED` 를 꺼버리던 길을 막았다 — 멀쩡한 정본으로 게임이 정지하고 원인은 엉뚱한 데를 가리키던 자리다. 실제 HTTP 서버 성공 경로 + 렌더 예외 분리 재현 PASS
- ★ **BLOCKER 완료 — 저광이면 형태 진행이 멈춘다.** 우회로를 전부 막았다: 오늘 DLI 를 이동평균보다 먼저 검사 · 정본 JSON 검증 통과 뒤에만 게임 경로 개방 · 유효 입력이 들어오면 튜닝 모드 자동 해제(체크박스까지). 재현 I-성공/I-실패/H-UI/G/A~F PASS, data/ 변경 0
- (이전) **보완 —** 조용히 넘어가는 길을 전부 막았다: advanceTo 는 하루만(초과 시 오류·상태 불변) · growthMin 폴백 제거 · DLI 미연결이면 게임 경로에서 정지(튜닝은 setTuningMode 로 분리). 재현 A~F PASS, data/ 변경 0
- (이전) **BLOCKER — 저광이면 형태 진행이 멈춘다.** 달력 경과일과 유효 생장 진행도를 갈랐다(`advanceTo`). 재현 A~D 전부 PASS. **코어는 `setGrowth`→`advanceTo`로 바꿔야 한다**
- core가 잡아준 **슬롯 매칭 버그** 수정 — 계약 객체 키는 `slotId`인데 `id`로만 찾아 항상 best로 떨어졌다. 둘 다 보게 함(옛 호출부 호환). 반지하 13배·아파트 273배 차이가 나던 자리다
- **갈라짐 표시 기준 통일** — `canFenestrate()` 신설. `bandOf(오늘값).fenestrating`은 오늘만 반짝 넘어도 true라 "갈라짐 시작!"이 거짓말이 됐다(core 지적). 화면은 7일평균 기준
- `plantCtx` 18축 확장 + `fCoef` **null→계수 1.0** 규칙 + `calcVarieType()` 자리
- 밴드 키 house와 일치(`critical`/`poor`/`stagnant`) · 체력(vigor) 모델 **설계만** 기록(구현 v1)

발견:
- ★ **`varieProb`는 잎마다다(개체 단위 아님).** `MAT_TREE`도 잎마다. 최적광에서 잎 10장 중 3.91장이 무늬라 안 희귀하다. 더 큰 문제는 **매 잎이 독립이라 실생 100개를 심어도 좋은 개체를 고를 수 없다** — plan의 연구자 트랙 전제가 코드에 없다. 2층 구조(개체 소질×잎 발현) 제안을 `growth-to-plan.md` §8에 수치와 함께 올림
- ★ **NaN·음수 DLI가 확률을 NaN으로 만들던 버그.** `rng()<NaN`은 항상 false라 **오류 없이 조용히** 무늬·갈라짐이 영영 안 난다. 막아뒀다 — 계약에서 `dli`가 null/누락으로 오는 경우가 있으면 알려주기 바란다
- `plant_grow.html`은 **한 그루 전용**이다(개체 상태가 전부 전역). 다개체는 리팩터 필요
- 튜닝값은 `data/growth_tuning.json`. 밸런싱은 이 파일만 고치면 되고 `plant_grow.html`은 열 필요 없다. ⚠ `thresholds`·`variegated.need_mult`는 `light_thresholds.json`과 같은 값이어야 한다
- ★ **`judgeDLI`류 밴드 키가 바뀌면 반드시 알려주기 바란다** — 생장 창이 같은 키로 확률을 가른다

대기:
- **plan** — 개체 소질 값(등급·배율·분포) / `f_propagation` 배율 / `calcVarieType` 조건 / vigor 표시 토글 위치 / `matRare` 과광 조건부 / `growth_tuning.json` → `data/balance/` 이동 여부
- **core** — 루프가 다개체를 실제로 요구하는 시점. **설계는 끝났다**(`growth-multiplant-design.md`) — 지속 상태가 8개뿐이라 함수 42개를 안 고치고 전역 교체로 간다. 착수는 core 요청 시점
- **leaf** — 콩나물 게임 검증·`bumpFit` 재조정 요청 받았다. 다개체/작물 배선이 없어 **지금은 못 한다**. 코어 루프에 작물이 붙는 시점에 하겠다

확정:
- **첫 플레이 초기 유효 진행도 = 143일.**
  - 143일 상태 — 비갈라짐 **중간잎 2장**, 새순 없음
  - 적정광 약 **3턴 뒤 146일** — 말린 새순 등장(첫 보상)
  - **160일** — 펴짐 시작(후속 변화)
  - 이 숫자는 확정이다. 전역 성장 곡선과 밸런스는 건드리지 않는다

판단필요:
- **저광 정지 반응 속도** — 7일 이동평균이라 저광 전환 후 3일은 관성으로 더 자란다. 즉시 끊으려면 `dliAvg(3)`로 좁히면 된다
- **개체 소질 2층 구조** — 지금은 무늬종이 흔하고 실생 선발이 성립하지 않는다. 값만 정해지면 붙인다. 선택지·수치는 `growth-to-plan.md` §8
- (아파트 갈라짐 — plan이 ⑶으로 확정. "자연광 갈라짐은 온실뿐". 종결)

---

## house
갱신: 2026-08-02 (커밋 4e3ecd4)
완료:
- ★ **반지하 창 높이 슬롯** `banjiha-sill:0` @ (0, 1.585, −1.95) · **peak_summer 3.77** / avg7 2.42
  (몬스테라 min 3.0 대비 +26%). 어두운 자리 `banjiha-dresser:1` peak 0.04, ≤0.3 슬롯 9칸 전부 시루 가능
- **`data/profiles/*.json` 6개 재생성** + 라이브 `daily_light/1` ↔ 정적 `room_profile/1` 대조.
  반지하 14슬롯 최대 오차 0.005, best 일치 ✅
- space 격자에서 **칸막이·도려낸 경계 벽 속**도 제외. `space.peak` 여섯 방 전부 불변,
  칸막이 있는 방의 면적·부피만 감소(학원 61.8→60.6 · 아파트 94.6→91.6 · 온실 114.6→112.4㎡)
- 면적 라벨 `net`/`gross`/명목 분리 · `utilization_pct` → `sampled_peak_ratio_pct`
- `docs/engine/verify_tools.md` 신설 — 검증 도구 목적·통과 기준 등록

발견 (다른 창이 알아야 할 것):
- ★★ **`light_adapter.build()` 가 `winFromHouse` 9번째 인자 `w.cz`(천창 z중심)를 안 넘긴다.**
  온실 천창이 z=0 에 놓여 **프로필 10.49 vs 라이브 14.55 (4.06 차)**. 다른 5방은 무영향.
  온실 프로필에 `_house_warning` 을 넣어 뒀다. core 가 고치면 재생성 필요
- ★★ **slotId 가 편집만 해도 바뀐다.** `light_adapter` 가 `uid` 없는 가구에 전역 seq 로 붙여서,
  반지하에 창턱 1개를 넣자 `oneroom-shelf-15:5` → `-6:5` 로 변했다.
  **세이브에 slotId 를 넣으면 화분이 갈 곳을 잃는다.** 반지하만 데이터에 uid 를 못 박았다 —
  나머지 5방도 못 박을지 core 판단 필요(지금 바꾸면 프로필 ID 가 바뀐다)
- ★ 아파트 `_place_test` 통과는 **반지하 슬롯의 증거가 아니다.** `_bj_place.html` 로 따로 본다
- 반지하 `sampled_peak_ratio_pct` **101%** 는 오차가 아니라 표본 차이다(격자 y=1.60 vs 상판 1.585).
  100 으로 자르지 않았다

대기:
- **core** — ① `light_adapter` 의 `w.cz` 누락 ② 나머지 5방 uid 고정 여부
  ③ **`firstPlayPot.realMaxDiameterM <= 0.21`** (화분 에셋에 치수가 없어 엔진 규칙만 검증함)
- core — 슬롯 UI 선택·드래그·세이브 복원·Day4 지급 연결 (HOUSE 범위 밖)
- plan — 기본 가구 배치(반지하 외 5방) · 온실 기본 차광값

판단필요 (plan·사용자):
1. **화면 체감** — 창턱이 방 안쪽보다 밝아 보이는지, "과한 가구"로 안 읽히는지
   (`docs/engine/shots/banjiha_*.png`). ★ 최종 체감 판정은 사용자
2. **온실 `space.peak` 15.09** — 과광 마진이 `max` 16 대비 −6%. 기본 차광값에 영향
3. 나머지 5방 slotId 고정 시점 — 세이브 붙기 전이 싸다

## core
갱신: 2026-08-02 (커밋 680c1e0)
완료 (2026-08-02 · 첫 플레이 유효 생장 연결):
- ★ **일일 루프를 `advanceTo` 로 교체.** `setGrowth` 는 **개체 도착 때 1회**뿐이다(감시 카운터 있음).
  `ready()` 는 **여섯 함수**(위 다섯 + `setGrowth`)가 다 있고 **임계값 정본까지 실린 뒤**에만 연다.
  진행 버튼은 그전까지 disabled, 실패하면 잠긴 채로 둔다
- ★ **도착 진행도 143의 두 축 분리** — `daysPlanted`(돌본 날) 0 / `growthDays`(형태) 143.
  `newState().pots` 는 **빈 배열**이다. 앱을 열었다고 식물이 생기지 않는다(도착 이벤트가 만든다)
- ★ **오늘 빛은 매일 넘긴다 — `null` 도.** 호출을 생략하면 growth 안에 어제 값이 남아
  "빛이 없는데 어제 빛으로 자라는" 상태가 된다
- 턴 결과에 `growthCalendarDay·effectiveGrowthDays·grew·growthBlocked` 를 낸다(빈 값으로 숨기지 않음)
- 안정 ID 계약 마감 — **중복 slotId·중복 uid 도 프로파일 중단** 사유에 넣었다. 로더도 거부
- 재현 전부 PASS (아래 발견) · `data/balance/` 변경 0건 · 남의 파일 변경 0건

완료 (2026-08-02 · 프로파일 마감 전 선행 계약 수정):
- ★ **`winFromHouse` 9번째 인자 `w.cz` 누락 수정** — 코어만 빠져 있었다.
  **온실 best 10.49 → 14.55 재현**, 천창 없는 5방은 불변
- ★ **안정 `slotId` 계약 확정** — 전역 순번 fallback 폐기. 화분 슬롯을 내는 가구는 명시 uid 필수,
  없으면 `TEMP~` 표시 + `console.error`, **영속 프로파일은 throw**(조용히 보완 안 함)
- **명시 uid 필요 가구 45건을 방별로 회신**(`core-to-house.md` ③). 반지하는 이미 완료(14칸 안정)
- **첫 화분 `assets/monstera/pot.glb` 확정** — 회전 무관 지름 0.202 ≤ 창턱 0.21
- `data/profiles/` 6개는 **전부 무효**. 로드하면 거부된다. house 재생성 대기

완료 (2026-08-01):
- **최소 루프 v0** (`game.html` + `src/game/*`) — [다음 날] → 날씨 → DLI → 생장 1틱 → HUD. 고사·경제는 0줄
- ★ **재미 확인 시나리오 5개** — `game.html` 에서 눌러 돌린다. 갈라짐이 켜지는 순간에 배너가 뜨고,
  7일평균·문턱선·전환 시점이 그래프로 보인다. **"등을 켰더니 갈라지기 시작했어요"가 24일째에 뜬다**
- **밸런스 자동 시뮬 준비** — `loop.js` 를 UI에서 분리(순수. `{S, turn}` 반환),
  `room_profile.js` 신설: 방 조도 기하를 JSON으로 뽑아 **THREE 없이** 헤드리스 재생(6방 `data/profiles/`).
  `sim.js` 에 시나리오·결과 형식 정의(`balance_sim/1`) — 경제·목표 칸은 `null` 로 비워 둠
- growth `canFenestrate()` 반영(갈라짐 표시를 7일평균 기준으로), house `glazed`·`weekStats`·`measured` 새 라벨 반영

발견 (다른 창이 알아야 할 것):
- ★ **`ready()` 통과 뒤에도 계약은 사라진다**(iframe 새로고침). 예전엔 어댑터가 null 을 돌려줘서
  **달력만 가고 형태는 그대로인 턴이 조용히 지나갔다.** 이제 계약 함수 6개는 없으면 전부 던지고,
  루프는 상태를 건드리기 전에 `assertContract()` 로 막는다 — 재현: day 3→3 · 이력 3→3 · 돌본 3→3
- ★ **이동평균의 정본 의미 = "최근 7개 유효 관측값"** (달력 7일 창이 아니다). growth 의 `dliAvg` 와 같다.
  코어는 `loop.rollingAvg` **하나만** 쓴다 — 루프·HUD 그래프·`fenDayOf` 가 각자 창을 짜면 어긋난다.
  화면 규칙: **하루선은 결측일에서 끊기고, 평균선은 이어진다**(직전 유효 7개가 그날의 판정값이라서)
- ★★ **`plant_grow` 의 전역 함수가 있다고 준비된 게 아니다.** 임계값 정본(`growth_tuning.json`)이
  **비동기로** 실려서, 함수만 보고 루프를 돌리면 **첫 며칠이 통째로 정지**한다
  (브라우저에서 유효 143 그대로, 사유 "임계값 정본이 안 실렸습니다"). 헤드리스에선 안 잡혔다.
  → **해소.** growth 가 `thLoaded()` 를 내줬고 `ready()` 가 그 상태를 직접 확인한다(추정 프로브는 지웠다).
  ⚠ `growthMin()` 은 판정에 못 쓴다 — 코드 기본값이 있어 정본 없이도 값을 돌려준다(그래서 안 던진다)
- ★ **저광 정지는 즉시가 아니다 — 낙차가 클수록 오래 버틴다.** 7일 이동평균이라
  3.1→2.9 는 **3일**, 반지하 live 12.2→0.09 는 **5일** 더 자란 뒤 멈춘다.
  growth 의 "저광 정지 반응 속도" 판단필요와 같은 현상 — 코어는 손대지 않았다.
  ★ 플레이어에게는 "옮겼는데 며칠째 그대로"로 보인다. 말 피드백(A4)이 이걸 설명해야 한다
- ★ **`plant_grow.html` 은 헤드리스로 돌릴 수 있다** — 3D(`init`)만 스텁으로 걷어내면
  생장 로직은 그대로 vm 에서 돈다(함수 선언 호이스팅 + `let` 은 파일 끝 `init()` 전에 초기화됨).
  다개체 리팩터 뒤에도 이 방법이면 **진짜 생장으로 밸런스 시뮬을 돌릴 수 있다**(`growth_real: true`)
- ★ **`maxPotD` 검사를 bbox 로 하면 안 된다.** `pot_concrete_square.glb` 는 bbox 0.200이라
  통과처럼 보이는데 **대각선 0.275**라 창턱(0.21)에 못 올라간다. 회전 무관 지름
  `2×max√(x²+z²)` 로 비교할 것. `real_max_m` 은 *최대 치수*(높이일 수 있음)라 그대로 쓰면 안 된다 —
  세 화분 모두 `real_max_m` 0.20인데 하나만 못 올라간다 (leaf·house 공통)
- ★ **천창은 부분 개구부라 `cz` 가 0이 아니다.** `winFromHouse` 9번째 인자를 빠뜨리면
  천창이 방 한가운데로 밀린다 — **온실만 10.49 vs 14.55 로 갈리고 다른 방은 티가 안 난다**
- ★ **v0 완료 조건이 돈다.** 반지하 90일: 등0 → 0% / 등1·나쁜자리 → 0% / 등1·좋은자리 → 100%.
  **20일 뒤 등을 켜면 24일째 갈라짐이 켜진다** — 켜자마자가 아니라 **3일 뒤**다(판정이 7일평균이라).
  이 지연이 오히려 학습을 만든다. 즉시 반응하면 "버튼을 눌렀다"가 되고, 사흘 뒤면 "환경을 바꿨다"가 된다
- ★ **등을 켜도 나쁜 자리는 90일 내내 아무 일도 없다.** 전기만 3,420원. 같은 등·같은 전기인데
  자리 하나로 **162배**가 갈린다. `acceptance.json` 의 빛 2줄 **통과**(`accepted: true`)
- ★ **자리가 등보다 크다** — 등을 하나 더 사면 +0.15, 한 칸 옮기면 ×2.
  **박사님 확정: 축은 "등 개수"가 아니라 "자리"다**
- ★ **프로파일 경로가 어댑터와 소수점까지 같다**(전 슬롯 오차 0.0000). 시나리오 90일이 **7ms** —
  방 조립 없이 도니 매트릭스 수십 번이 초 단위다. 시뮬은 `room_profile.json` 만 있으면 된다
- ★ **하루 값과 7일평균이 갈라지는 자리가 실재한다** — 반지하·등1개 자연광 최고 슬롯은
  하루 6.02(문턱 넘음) / 7일평균 5.82(못 넘음). 표시를 하루 값으로 하면 플레이어에게 거짓말이 된다
- **내 숫자는 전부 `slots` 기준**이다(가구 위 화분 자리). house의 `space` 기준과 다른 축이니
  섞어 읽으면 안 된다 — house의 라벨 규약(`{방}.{space|slots}.{peak|avg7}`)을 따른다
- `weekStats` 의 "등은 밖에서 더하라" 처방은 **`mean` 에만** 통한다. `p10/p50/p90`·`overPct` 는
  등 포함 값으로 세야 맞다(자연광만 넘기면 "등을 켜도 안 갈라진다"가 된다) — core-to-house 참고

대기:
- **house — ① `house_rooms.json` 에 명시 uid 45건(방별 목록은 `core-to-house.md` ③)
  ② `_profile_gen.html` 을 fail-loud·전 방 대조로 보완하고 `w.cz` + `uidStable`·`roomRev` 를 찍어
  프로파일 6개 최종 재생성.** 그 전까지 코어의 `profile()` 은 throw 한다(의도된 것)
- ★ **사용자 — 첫 플레이 화면 판정.** 조건: `novice · 등 0 · banjiha-sill:0 · 다음 날 3회`.
  `_core_shot.html?days=1`(144) / `?days=3`(146) 을 브라우저로 열어 **말린 새순이 눈에 구분되는지**.
  숫자는 통과했다 — DLI 3.77 · 유효 143→146 · 갈라짐 ✕ · 잎 2→3 · 새순 렌더 0→1
- growth — ① ~~`thLoaded()`~~ **[받음]** — 프로브를 지우고 `thLoaded()` 를 직접 확인한다
  ② **`growthPhase()` 구현**(계약 초안은 받음: `{phaseId, progress01, nextPhaseId}`).
  그전까지 형태 진행도 게이지는 안 만든다 — HUD 는 `growthDays()` 숫자만.
  코어에 남은 생장일은 도착 143 하나뿐이고 146 같은 경계는 없다
  ③ 다개체 리팩터 — ③까지는 시뮬의 생장 수치를 못 낸다(`growth_real: false`)
- **활력(vigor) 표시는 취소됐다** — novice 는 형태 진행도만, 활력은 숨김·감소 없음,
  고수 모드 활력은 보류(박사님 2026-08-02). 코어는 `vigor()` 를 읽지 않는다
- house — `weekStats.mean` 확률 가중 (`roomRev` 복사·프로파일 위치는 반영·승인 완료)

(정정 2026-08-02: 아래 두 줄은 **완료**되어 내렸다 —
 `setGrowth`→`advanceTo` 교체 · 초기 진행도 143 반영 및 `banjiha-sill:0` 배치)

판단필요 (plan·사용자):
1. ~~등 축 → 자리 축~~ · ~~합격선 50%~~ · ~~프로파일 리포에 두기~~ — **셋 다 확정, 반영 완료**
2. **합격선 50%를 `acceptance.json` 으로 옮겨 주기(plan)** — 지금은 `sim.js FEN_PASS_PCT` 에
   기본값으로 들고 있다. 밸런싱 숫자가 코드에 있는 상태라 plan 소유로 가는 게 맞다
3. **다음 재미 축이 A4 말 피드백인지 확인** — "오늘 안 자란 게 날씨 탓인지 등을 안 켠 탓인지"를
   말로 설명하는 것. 계약이 `dli_daylight`/`dli_lamp` 를 나눠 주므로 코어가 바로 붙일 수 있다.
   plan 흐름을 기다리지 않고 진행할지 판단 부탁

---

## leaf
갱신: 2026-08-01 (커밋 0a4156a)
완료:
- **식용작물 확장** — 원화 21장 + 절차적 3D 14종 + Meshy 3D 12종(상추·바질·딸기본체·딸기열매)
- 앞서 **콩나물 1종 완주** — 공용 씨앗·새순 4 + 본체 3 + **시루(슬롯 15칸)**, 전부 절차적
- manifest **448 items** · 전 GLB에 실제 크기(`scale_to_real`) 부여
- 잎 결함 수정 — 잎자루 접합 6건 + **법선(NORMAL) 누락 21개** 복구

발견:
- ★ **Meshy 크레딧은 창 공유인데 `rigging`·`animation` 은 `list_tasks` 로 조회가 안 된다.**
  char 소비 약 145cr이 그래서 안 보였음 → **`manifest._credit_log` 기록 규칙 신설.
  전 창이 유료 API 쓸 때 한 줄씩 남겨 주세요.** 없으면 충전 시점을 못 잡습니다
- ★ **plan의 「자취생 경제 미성립」에 직접 관련** — 방 슬롯이 13개라 부족하다 하셨는데,
  **콩나물 시루는 방 슬롯 1칸을 쓰면서 재배 슬롯 15칸을 줍니다.** 재배트레이도 12·24칸.
  즉 **방 슬롯 ≠ 재배 슬롯**이라, 시루 2개면 30포기입니다. 재계산 때 이 배수를 반영해 주세요
- **절차적/Meshy 판단 기준**: 곧은 잎·회전체·구체 = 절차적 / 주름·겹침·표면패턴 = Meshy.
  딸기 열매는 절차적로 씨앗이 안 읽혀 실패 → Meshy로 해결
- **시루·재배트레이 슬롯이 관엽 선반과 완전 동일 규격** → 가구·작물용기를 한 배치 로직으로 처리 가능
- GLB 내보낼 때 **`vertex_normals` 를 미리 계산해야 NORMAL 이 들어간다.** 안 하면 three.js가
  flat shading만 켜고 **잎 두께 조절이 통째로 무효**가 됨(실제로 겪음)
- house 실측 대입: **반지하 0.55~0.48 짜리 4칸은 저광작물(새싹채소·상추) 자리**

대기:
- **plan** — 물 시스템 2건(수분 게이지 통일 / 물을 공통 시스템으로). 7/26 요청분, **사용자가 재촉함**
- **plan** — `docs/assets/` leaf 소유 배정. **사용자가 승인 요청함**
- **growth** — 콩나물 게임 검증. growth 답변: *"다개체/작물 배선이 없어 지금은 못 한다.
  코어 루프에 작물이 붙는 시점에 하겠다"* → ⚠ **작물 양산이 core 루프 진척에 묶였다**
- **growth** — `stem_knob`·`leaf_sheath` `bumpFit` 재조정 (같은 이유로 대기)

판단필요: **없음** (2026-08-01 사용자 결정으로 4건 모두 종결)
- ~~Meshy 충전~~ → **안 함.** 22cr 유지. 토마토·고추는 콩나물 게임 검증 후
- ~~house_mod GLB 4종 삭제~~ → **보류.** 8MB는 부담 없고 참고 가치 있음 (house에 전달 완료)
- ~~작물 9종 실사용~~ → **검증 후 진행**으로 확정
- `docs/assets/` 소유는 plan 승인 대기로 이동(위 대기 항목)

메모: char가 확인해 줌 — **Meshy ~145cr 은 리깅·애니 소비가 맞았다.** `_credit_log` 규칙 전 창 전파 완료.
⚠ leaf 는 **작물 신규 제작을 멈추고 대기**한다. 재개 조건 = core 루프에 작물 배선 등장.

---

## char
갱신: 2026-08-01 (커밋 1372b41)
완료:
- **캐릭터 8종 에셋 완료** — 실스케일(여1.40/남1.50/몬이0.375) · 경량본 `lq/`(폴리곤 1/10) · 모션 19종 128클립
- 색 커스터마이징(머리·눈썹·피부·홍채) **접음.** 기본형 확정. 함정 8개는 `assets/characters/README.md` §5
- 도구 8개 `tools/char/` 이관 · house 인계 3건 처리 확인. **표정만 남기고 대기 중**
- **캐릭터별 성격(㉮안) 준비 완료 — 새 모션 0건 · Meshy 0cr.** 기존 16종을 전수 측정해
  서서 하고 끝자세가 돌아오는 **10종**을 골라 배정표+재생코드를 `char-to-house.md` 에 올림

발견 (다른 창이 알아야 할 것):
- ★ **캐릭터 키가 1.50/1.70 → 1.40/1.50 으로 줄었다**(2026-07-26, 방이 좁아 보여서).
  키를 하드코딩한 곳은 어긋난다. **몬이는 비연동 0.375 고정**이다
- ★ **게임 화면은 `lq/` 를 써야 한다** — 원본은 평균 22.5만 삼각형이라 방과 같이 돌리면 버벅인다.
  클립은 노드이름 바인딩이라 원본·경량본 **어느 쪽에도 그대로 붙는다**
- ★ **색 커스터마이징은 사후 마스크로 불가능하다.** Meshy가 전신을 아틀라스 한 장에 굽는다.
  남주부 셔츠는 그의 얼굴과 색도거리 **0.0247** 로 남연구원 **손(0.0374)보다 얼굴에 가깝다**
  → 어떤 색공간으로도 못 가른다. 필요하면 **모델 생성 단계에서 부위를 나눠 뽑아야** 한다
- 모션 클립 128개는 **캐릭터 비종속**(24본 표준골격 · 노드명 8종 동일).
  키를 또 바꾸셔도 **클립은 손 안 대도 된다**(씬 최상단 래퍼 스케일 방식)
- ★ **캐릭터 성격 표현에 Meshy 를 안 써도 된다.** 새 idle 4종을 뽑으면 12cr인데
  **기존 16종 중 10종이 그대로 idle 변주로 쓸 수 있다**(서서 함 + 끝자세가 시작으로 복귀).
  `sit`(Hips 72%)·`harvest_crouch`(68%)·`doze`(55%)만 앉음이라 제외.
  **leaf 의 토마토·고추 90cr 과 경합하지 않는다** — 잔액 22cr을 leaf 가 다 써도 된다
- 클립을 idle 사이에 끼울 때 **루트가 최대 42%(Hips 높이 대비) 움직인다.**
  걷기와 같은 Hips XZ 고정이 필요하다. `char-to-house.md` 에 코드로 올림
- leaf 의 `_credit_log` 규칙 확인했습니다. **char 의 Meshy 소비 약 145cr 은 리깅·애니라
  `list_tasks` 로 안 잡히는 게 맞습니다.** 앞으로 유료 호출 시 한 줄씩 남기겠습니다

결정됨 (박사님 2026-08-01):
1. **표정** — core 루프 재미 확인 후 착수. 신호 대기
2. **2D 초상화 56장** — ★**착수 반대에 동의.** 대화 UI 없이 뽑으면 낭비 확정
3. **캐릭터별 성격** — ★**㉮ idle 변주 확정.** 위 발견대로 **크레딧 0으로 구현 가능**

대기:
- **house** — idle 변주 배정표·재생코드 적용 (`char-to-house.md` 2026-08-01 절).
  엔진 코드가 house 소유라 제가 못 붙입니다
- **plan** — 소유표에 `tools/char/*`·`assets/derived/*` 추가 / `character_material_constraint.md`
  경로 4곳 + 보류 표시 (`char-to-plan.md` ⓐⓑ)
- **core** — 최소 루프 재미 확인. **그게 끝나야 표정 착수**(박사님 지시)

판단필요 (plan·사용자):
- **없음.** 3건 모두 2026-08-01에 박사님이 답을 주셨다 (위 「결정됨」 참조).
  다음 판단이 필요한 시점은 **core 루프의 재미 확인이 끝났을 때**다 — 그때 표정 착수 신호만
  주시면 된다. 그 전까지 char 창은 새로 여쭐 것이 없다

---

## plan
갱신: 2026-08-02
완료:
- **★ 다섯 창 대기 전부 해소** — growth 6건 · house 4건 · core 4건 · leaf 2건 · char 2건 답변
- **자취생 경제 재계산** — 세 창 발견 반영. 결론이 뒤집혔다(아래 발견)
- **`docs/hidden_depth.md` 신설** — 숨겨진 공략 14건 + 「몰라도 되나」 판정
- `acceptance.json` 갱신 — `fenWeekPct` 합격선 60% · 개체 소질 2층 값

발견:
- ★★ **자취생 경제가 "불가능"에서 "자본 부족"으로 바뀌었다.**
  선반 6개(관엽 29칸)에서 흑자 전환(+2,348원/일). 전엔 13칸이 천장이라 아예 안 됐다.
  다만 29그루 증식에 80일 · 필요자본 112~158만 · 시작 100만 → **12~58만 부족.**
  **월세 첫 달 유예(30만) 하나면 성립한다.** 상세는 `game_flow.md` 부록
- ★ **벽 선반이 "비밀"이 아니라 "벽"이다** — 선반 0개면 −9,118원/일로 구조적 적자.
  house에 **반지하 기본 가구로 선반 1개**를 요청했다(0개=존재를 모름 / 6개=발견이 없음)
- ★ **개체 소질 2층 구조 값 확정** — `base 0.10` × 등급배율(plain 0 / normal 1.0 /
  potential 2.5 / elite 6.0), 계열은 **`MAT_TREE` 주 갈래 10그룹**.
  첫 잎 무늬율이 plain 0% ↔ elite 100%라 **실생 선발이 성립**하고,
  논문 1편이 **33~44일**로 목표(30~45턴)에 든다. growth가 붙이면 연구자 트랙이 코드로 성립
- **갈라짐 합격선 = 문턱 넘는 주 ≥ 60%.** 경계(20~60%)는 **실패로 본다** —
  40%짜리 방은 밸런싱이 우연에 걸린 것이다(아파트 마진 0.3% 사고가 그 유형)
- **계절별 최적 배치는 지금 안 바뀐다** — 천공이 방향 무관이라 모든 자리가 같은 계수로
  어두워진다. ★온도가 들어오면 바뀐다(창가는 겨울에 춥다). 그때 의도적으로 만들 값어치가 있다

대기:
- ~~house — 반지하 창 높이 슬롯~~ → ✅ **해소** `banjiha-sill:0` peak_summer **3.77**
- **house** — 방별 목표 활용률 배치 · `measured.avg7` · `grid`/`furnished`
- **growth** — 개체 소질 2층 반영 · `matRare` 과광 조건부 · `growth_tuning.json` 이동
- **leaf** — 물꽂이 용기 슬롯 수 (증식 단계가 슬롯을 얼마나 먹나)
- **core** — 최소 루프 ①의 **재미 확인**. 그게 나와야 ③경제로 넘어간다

판단필요:
- **자취생 지출을 13,000원/일로 어떻게 내릴지** — 월세↓ / 식비↓ / 콩나물 상한↑ 중 선택.
  월세 첫 달 유예(서사 장치)를 권한다. 시작 자금·슬롯은 캐릭터 정체성에 직접 닿는다
- **포토스 마디 주기 확정** (10 / 7 / 5일) — 생물학상 **7일이 현실적**. 자본 요구가 138→120만으로 내려간다
- **선반 값 3만원 · 선반당 3칸은 임의값** — house·leaf와 맞춰야 한다
- ⏸ 첫 플레이 이후 검토 — 스토리 모드 전용 고정 외부광 프로필. 상세: plan-to-house.md의 [잠재 설정] 스토리 모드 전용 고정 외부광 프로필
- ⏸ 첫 통합 시뮬 이후 검토 — 계절 진행·식물등·반지하 튜토리얼 종료(원룸 이사). 상세: time_modes.md §9 · first_play.md §8
- ✅ 첫 플레이 초기 진행도 **143 확정** · 슬롯 `banjiha-sill:0` 3.77. 형태 진행도/활력 **별도 축**으로 확정(novice: 형태 게이지 보임·감소 없음 / 활력 숨김). 상세: first_play.md §3·§5·§9
- ✅ **게이지 표시 사상 = A(원비율) 확정.** `spear_ready` 진입 0% → 단계 내부 진행 그대로 0~100% · 저광에선 정지(감소 없음) · 단계 완료 시 다음 phase 0%부터. 사상 근거가 아닌 것: 생존 여유 · `daysPlanted` · 절대 `growthDays`. 상세: first_play.md §9
