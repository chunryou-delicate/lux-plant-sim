# 창별 상태 — 요약만. 상세는 {창}-to-{상대}.md

> **자기 섹션만 고친다.** 섹션이 나뉘어 있어 git 이 자동 병합해 준다.
>
> ⚠ **파일을 통째로 다시 쓰지 말 것.** 2026-08-01에 실제로 사고가 났다 —
> 한 창이 전체를 재작성하고 다른 창이 그걸 커밋하면서 **두 창의 섹션이 날아갔다.**
> (git 이력에서 복구함) **자기 `## {창}` 블록만 편집한다.**

```markdown
## {내창}
갱신: 2026-08-01 (커밋 abc1234)
완료: (이번에 한 것 2~4줄)
발견: (★다른 창이 알아야 할 것 — 없으면 생략)
대기: (누구의 무엇을 기다리나)
판단필요: (★사용자·plan이 정해야 할 것)
```

★ **"발견"과 "판단필요"가 핵심.** 완료는 커밋 로그에도 있지만 저 둘은 여기 없으면 어디에도 없다.
커밋·**푸시**까지 해야 읽힌다.


---

## growth
갱신: 2026-08-01 (커밋 대기)
완료:
- core가 잡아준 **슬롯 매칭 버그** 수정 — 계약 객체 키는 `slotId`인데 `id`로만 찾아 항상 best로 떨어졌다. 둘 다 보게 함(옛 호출부 호환). 반지하 13배·아파트 273배 차이가 나던 자리다
- **갈라짐 표시 기준 통일** — `canFenestrate()` 신설. `bandOf(오늘값).fenestrating`은 오늘만 반짝 넘어도 true라 "갈라짐 시작!"이 거짓말이 됐다(core 지적). 화면은 7일평균 기준
- `plantCtx` 18축 확장 + `fCoef` **null→계수 1.0** 규칙 + `calcVarieType()` 자리
- 밴드 키 house와 일치(`critical`/`poor`/`stagnant`) · 체력(vigor) 모델 **설계만** 기록(구현 v1)

발견:
- ★ **`varieProb`는 잎마다다(개체 단위 아님).** `MAT_TREE`도 잎마다. 최적광에서 잎 10장 중 3.91장이 무늬라 안 희귀하다. 더 큰 문제는 **매 잎이 독립이라 실생 100개를 심어도 좋은 개체를 고를 수 없다** — plan의 연구자 트랙 전제가 코드에 없다. 2층 구조(개체 소질×잎 발현) 제안을 `growth-to-plan.md` §8에 수치와 함께 올림
- ★ **NaN·음수 DLI가 확률을 NaN으로 만들던 버그.** `rng()<NaN`은 항상 false라 **오류 없이 조용히** 무늬·갈라짐이 영영 안 난다. 막아뒀다 — 계약에서 `dli`가 null/누락으로 오는 경우가 있으면 알려주기 바란다
- `plant_grow.html`은 **한 그루 전용**이다(개체 상태가 전부 전역). 다개체는 리팩터 필요
- 튜닝값은 `data/growth_tuning.json`. 밸런싱은 이 파일만 고치면 되고 `plant_grow.html`은 열 필요 없다. ⚠ `thresholds`·`variegated.need_mult`는 `light_thresholds.json`과 같은 값이어야 한다
- ★ **`judgeDLI`류 밴드 키가 바뀌면 반드시 알려주기 바란다** — 생장 창이 같은 키로 확률을 가른다

대기:
- **plan** — 개체 소질 값(등급·배율·분포) / `f_propagation` 배율 / `calcVarieType` 조건 / vigor 표시 토글 위치 / `matRare` 과광 조건부 / `growth_tuning.json` → `data/balance/` 이동 여부
- **core** — 루프가 다개체를 실제로 요구하는 시점. 그때 개체 리팩터 착수(어댑터 형태는 core와 협의)
- **leaf** — 콩나물 게임 검증·`bumpFit` 재조정 요청 받았다. 다개체/작물 배선이 없어 **지금은 못 한다**. 코어 루프에 작물이 붙는 시점에 하겠다

판단필요:
- **개체 소질 2층 구조** — 지금은 무늬종이 흔하고 실생 선발이 성립하지 않는다. 값만 정해지면 붙인다. 선택지·수치는 `growth-to-plan.md` §8
- **아파트 갈라짐** — 문서엔 "아파트에 가야 갈라진다"인데 7일평균이 문턱을 못 넘는다. ⑴문턱 6.0→4.5(원룸 4.77도 걸려 구분 흐려짐) ⑵아파트 peak 상향(실측을 흔듦) ⑶문서 수정. **⑶ 권고**

---

## house
갱신: 2026-08-01 (커밋 e16c514)
완료:
- **방 6종 구조 확정 + 공간 기준 전수 측정.** 정본은 `docs/engine/rooms_spec.md`
  (구조 · space/slots/area · 높이별 DLI · 방별 성격 한 줄 · 캐릭터별 조건)
- 학원교실 11×6(교실+교사방, 깊은 창턱 1m) · 온실 10×12(3면 유리+천창 / 연구실 분리) 반영
- 렌더 빛 사고 5건 수정 — 해 방향 · 천장 그림자 · 유리 정책 · 창 확산광. `docs/engine/render_light_model.md`
- core 요청 ①`ctx.glazed` 누락 ②`weekStats` 캐시+선형성 경고 **둘 다 처리**

발견 (다른 창이 알아야 할 것):
- ★ **숫자에 라벨을 붙여야 한다.** `{방}.{space|slots}.{peak_summer|avg7_summer}` 네 갈래다.
  라벨 없이 "아파트 6.02"만 오가다 **세 번 사고**가 났다. `measured` 블록을 그렇게 갈라 뒀다
- ★ **방 등급의 정본은 `space`**(가구 없는 공간)다. `slots` 는 가구를 어디 놨느냐의 결과라
  밸런스 근거로 쓰면 안 된다 (반지하는 잠재력의 15%만 쓰고 있다)
- ★ **자연광만으로 갈라진 잎은 온실뿐.** 아파트 avg7 5.79 로 문턱 6.0 에 **0.21 부족**
- ★ **축이 둘이다** — 자연광은 창이, 조명등은 **면적**이 정한다.
  면적 순: 반지하 20 < 원룸 30 < 투룸 35 < 학원 66 < 아파트 99 < 온실 120㎡
- 온실 peak 16.16 이 몬스테라 `max` 16 을 넘는다 = **과광**. 차광이 필수 도구가 된다

대기:
- plan — 기본 가구 배치 · 반지하 활용률 15% · 원룸≈투룸 동률 처리 (아래 판단필요)
- core — `game.html` 신설되면 알려주기. 셸 이관 시점을 맞춘다

판단필요 (plan·사용자):
1. **기본 가구 배치** — "적당히 나쁘게" 의도해서 놓을 사안. 지금은 겹침만 피한 결과라 근거로 못 쓴다
2. **반지하 활용률 15%** — 벽 선반 하나를 창 높이(1.6m)에 달면 0.55 → 3.74. "반지하는 관엽 불가"가
   방의 한계가 아니라 배치의 결과다. 의도인지 확인 필요
3. **원룸 6.76 ≈ 투룸 6.69 (1% 차)** — 자연광은 동률로 두고 **면적(30 vs 35㎡)으로 구분**을 권함
4. **계산↔화면 자동 대조 검사** — 이번 사고 5건 중 3건이 그 유형인데 검사가 없다. 우선순위 판단 필요

---

## core
갱신: 2026-08-01
완료:
- **최소 루프 v0** (`game.html` + `src/game/*`) — [다음 날] → 날씨 → DLI → 생장 1틱 → HUD. 고사·경제는 0줄
- **밸런스 자동 시뮬 준비** — `loop.js` 를 UI에서 분리(순수. `{S, turn}` 반환),
  `room_profile.js` 신설: 방 조도 기하를 JSON으로 뽑아 **THREE 없이** 헤드리스 재생.
  `sim.js` 에 시나리오·결과 형식 정의(`balance_sim/1`) — 경제·목표 칸은 `null` 로 비워 둠
- growth `canFenestrate()` 반영(갈라짐 표시를 7일평균 기준으로), house `glazed`·`weekStats` 수정 확인

발견 (다른 창이 알아야 할 것):
- ★ **자리가 등보다 크다.** 반지하·등 1개에서 좋은 자리 avg7 12.16(문턱 넘는 주 100%) vs
  나쁜 자리 0.08(0%) — **162배.** 등을 하나 더 사면 +0.15인데 한 칸 옮기면 ×2다.
  `acceptance.json` 의 `banjiha_lamp1_goodspot/badspot_fenestrates` 두 줄이 이걸로 측정된다
- ★ **프로파일 경로가 어댑터와 소수점까지 같다**(전 슬롯 오차 0.0000). 시나리오 90일이 **7ms** —
  방 조립 없이 도니 매트릭스 수십 번이 초 단위다. 시뮬은 `room_profile.json` 만 있으면 된다
- ★ **하루 값과 7일평균이 갈라지는 자리가 실재한다** — 반지하·등1개 자연광 최고 슬롯은
  하루 6.02(문턱 넘음) / 7일평균 5.82(못 넘음). 표시를 하루 값으로 하면 플레이어에게 거짓말이 된다
- **내 숫자는 전부 `slots` 기준**이다(가구 위 화분 자리). house의 `space` 기준과 다른 축이니
  섞어 읽으면 안 된다 — house의 라벨 규약(`{방}.{space|slots}.{peak|avg7}`)을 따른다
- `weekStats` 의 "등은 밖에서 더하라" 처방은 **`mean` 에만** 통한다. `p10/p50/p90`·`overPct` 는
  등 포함 값으로 세야 맞다(자연광만 넘기면 "등을 켜도 안 갈라진다"가 된다) — core-to-house 참고

대기:
- growth — 다개체 리팩터. **그때까지 시뮬의 생장 수치는 못 낸다**(`growth_real: false`).
  지금 시뮬이 재는 건 빛·전기뿐이다
- plan — 캐릭터 4 × 모드 4 × 초·중·후반 흐름. 그게 나와야 v1(개체별 DLI·지출 루프) 방향이 잡힌다
- house — `weekStats.mean` 을 확률 가중으로 낼지(아래 판단필요 3)

판단필요 (plan·사용자):
1. **검수 기준표를 "등 개수" 축에서 "자리" 축으로 바꿀지** — 돈으로 사는 해법(+0.15)보다
   공짜 행동(×2)이 크다. 실제 육성 경험엔 이쪽이 맞아 보이는데 기획 의도 확인 필요
2. **"갈라진다"의 합격선** — `acceptance.json` 은 expect true/false 인데 시뮬은 "문턱 넘는 주 %"를 낸다.
   몇 % 이상을 "갈라진다"로 볼지는 plan 결정 (100% vs 0% 라 지금은 안 갈려도 판정되지만, 경계 사례가 온다)
3. **`room_profile.json` 을 리포에 둘지** — 방 6종이면 ~20KB. 두면 시뮬이 THREE 없이 바로 돈다.
   지금은 `game.html` 의 [방 프로파일 ⭳] 버튼으로 내보내기만 한다 (`data/` 는 house 소유라 협의 필요)

---

## leaf
갱신: 2026-08-01 (커밋 0a4156a)
완료:
- **식용작물 확장** — 원화 21장 + 절차적 3D 14종 + Meshy 3D 12종(상추·바질·딸기본체·딸기열매)
- 앞서 **콩나물 1종 완주** — 공용 씨앗·새순 4 + 본체 3 + **시루(슬롯 15칸)**, 전부 절차적
- manifest **448 items** · 전 GLB에 실제 크기(`scale_to_real`) 부여
- 잎 결함 수정 — 잎자루 접합 6건 + **법선(NORMAL) 누락 21개** 복구

발견:
- ★ **Meshy 크레딧은 창 공유인데 `rigging`·`animation` 은 `list_tasks` 로 조회가 안 된다.**
  char 소비 약 145cr이 그래서 안 보였음 → **`manifest._credit_log` 기록 규칙 신설.
  전 창이 유료 API 쓸 때 한 줄씩 남겨 주세요.** 없으면 충전 시점을 못 잡습니다
- ★ **plan의 「자취생 경제 미성립」에 직접 관련** — 방 슬롯이 13개라 부족하다 하셨는데,
  **콩나물 시루는 방 슬롯 1칸을 쓰면서 재배 슬롯 15칸을 줍니다.** 재배트레이도 12·24칸.
  즉 **방 슬롯 ≠ 재배 슬롯**이라, 시루 2개면 30포기입니다. 재계산 때 이 배수를 반영해 주세요
- **절차적/Meshy 판단 기준**: 곧은 잎·회전체·구체 = 절차적 / 주름·겹침·표면패턴 = Meshy.
  딸기 열매는 절차적로 씨앗이 안 읽혀 실패 → Meshy로 해결
- **시루·재배트레이 슬롯이 관엽 선반과 완전 동일 규격** → 가구·작물용기를 한 배치 로직으로 처리 가능
- GLB 내보낼 때 **`vertex_normals` 를 미리 계산해야 NORMAL 이 들어간다.** 안 하면 three.js가
  flat shading만 켜고 **잎 두께 조절이 통째로 무효**가 됨(실제로 겪음)
- house 실측 대입: **반지하 0.55~0.48 짜리 4칸은 저광작물(새싹채소·상추) 자리**

대기:
- **plan** — 물 시스템 2건(수분 게이지 통일 / 물을 공통 시스템으로). 7/26 요청분 **미확정**
- **growth** — 콩나물이 게임에서 도는지 확인 (나머지 작물 양산의 전제)
- **growth** — `stem_knob`·`leaf_sheath` 비율 변경분 `bumpFit` 재조정

판단필요:
- **Meshy 잔액 22cr — 충전 여부.** 토마토·고추 본체를 마저 하려면 90cr (Higgsfield는 815cr로 넉넉)
- **`docs/assets/` 를 leaf 소유로 배정** — house가 `docs/engine/` 승인받은 선례. README 소유표는 plan이 갱신
- **작물 9종이 다 쓰이는지** — 콩나물조차 게임 미검증이라 토마토·고추 3D는 그 뒤가 맞다고 봄
- `assets/house/house_mod_*.glb` 4종(8MB) 삭제 여부 — house가 "안 쓰지만 사용자 결정"으로 넘김

---

## char
갱신: 2026-08-01 (커밋 1372b41)
완료:
- **캐릭터 8종 에셋 완료** — 실스케일(여1.40/남1.50/몬이0.375) · 경량본 `lq/`(폴리곤 1/10) · 모션 19종 128클립
- 색 커스터마이징(머리·눈썹·피부·홍채) **접음.** 기본형 확정. 함정 8개는 `assets/characters/README.md` §5
- 도구 8개 `tools/char/` 이관 · house 인계 3건 처리 확인. **표정만 남기고 대기 중**

발견 (다른 창이 알아야 할 것):
- ★ **캐릭터 키가 1.50/1.70 → 1.40/1.50 으로 줄었다**(2026-07-26, 방이 좁아 보여서).
  키를 하드코딩한 곳은 어긋난다. **몬이는 비연동 0.375 고정**이다
- ★ **게임 화면은 `lq/` 를 써야 한다** — 원본은 평균 22.5만 삼각형이라 방과 같이 돌리면 버벅인다.
  클립은 노드이름 바인딩이라 원본·경량본 **어느 쪽에도 그대로 붙는다**
- ★ **색 커스터마이징은 사후 마스크로 불가능하다.** Meshy가 전신을 아틀라스 한 장에 굽는다.
  남주부 셔츠는 그의 얼굴과 색도거리 **0.0247** 로 남연구원 **손(0.0374)보다 얼굴에 가깝다**
  → 어떤 색공간으로도 못 가른다. 필요하면 **모델 생성 단계에서 부위를 나눠 뽑아야** 한다
- 모션 클립 128개는 **캐릭터 비종속**(24본 표준골격 · 노드명 8종 동일).
  키를 또 바꾸셔도 **클립은 손 안 대도 된다**(씬 최상단 래퍼 스케일 방식)
- leaf 의 `_credit_log` 규칙 확인했습니다. **char 의 Meshy 소비 약 145cr 은 리깅·애니라
  `list_tasks` 로 안 잡히는 게 맞습니다.** 앞으로 유료 호출 시 한 줄씩 남기겠습니다

대기:
- **plan** — 소유표에 `tools/char/*`·`assets/derived/*` 추가 / `character_material_constraint.md`
  경로 4곳 + 보류 표시 (`char-to-plan.md` ⓐⓑ)
- **core** — 최소 루프 재미 확인. **그게 끝나야 표정 착수**(박사님 지시)

판단필요 (plan·사용자):
1. **표정 착수 시점** — 방식은 확정돼 있다(B안 얼굴 텍스처 교체 · 눈 4종 + 입 몇 종 · **깜빡임 먼저**).
   core 루프 확인 후 신호만 주시면 된다
2. **2D 초상화 56장**(8종×7표정) — **대화 시스템 설계 전엔 착수 반대.** 지금 대화 UI가 없어
   표정 종류·구도(전신/흉상)가 안 정해진다. 낭비가 확정적이다
3. **캐릭터별 성격 표현** — 지금 8종은 외형만 다르고 모션 19종은 전원 공용이다.
   ㉮ idle 클립 3~4종 추가(저비용) vs ㉯ 캐릭터별 전용 모션(Meshy 재생성×8, 128→수백 파일).
   **㉮ 권고** — 게임 화면에서 캐릭터는 대부분 서 있거나 걷는다

---

## plan
갱신: 2026-08-01 (커밋 cfbcc8a)
완료:
- 기획 정본 `GAME_PLAN.md` 통합 · `game_flow.md`(캐릭터×모드×초중후반) 신설
- 시간 모드 **4단** + 초보를 **튜토리얼 캠페인**(자취생 1막)으로 재정의
- 연구자를 **난이도 축에서 제외** → 확률 게임. **과광 변이 = 온실 전용** 확정
- **`balance_decisions.md` 「판정 기준」 + `data/balance/acceptance.json`** — 시뮬 합격 조건을 숫자로

발견:
- ★ **house의 "숫자에 라벨을 붙여라"에 전적으로 동의.** 저도 같은 사고를 두 번 냈다
  (peak↔7일평균, 여름↔연평균). `{방}.{space|slots}.{peak|avg7}` 4갈래 규약을 밸런스 문서에도 반영하겠다
- **판정 단위는 모드마다 다르다** — 초보 `peak` / 표준 `avg7` / 심화·실전 계절별 `avg7`.
  `data/balance/weather.json`의 `modes` 블록. **새 모드 붙일 때 반드시 확인**
- **과광 전용 변이 = 기존 `MAT_RARE` 4종**(핑크·모브·차콜). **새 에셋 불필요.**
  안토시아닌 = 강광 스트레스 방어 색소라 실제 생리와 맞는다. house의 "온실 16.16 = 과광"이 근거
- **저광 전용 변이는 물리가 막는다** — `fLight(DLI7<3.0)=0`이라 새 잎이 안 나면 변이도 없다
- ★ **시뮬이 못 잡는 것을 문서화했다** — 지루한가·답답한가·보상이 체감되나.
  *"숫자가 다 통과했으니 재밌다"* 는 착각을 막기 위한 것 (`acceptance.json._not_measurable`)
- ⚠ **STATUS.md 사고** — 한 창이 파일을 통째로 재작성하고 다른 창이 그걸 커밋해
  **두 창의 섹션이 날아갔다.** git 이력에서 복구함. **자기 `## {창}` 블록만 편집할 것**

대기:
- **house** — 판단필요 4건 답변 준비 중(기본 가구 배치·반지하 15%·원룸≈투룸·자동 대조 검사)
- **growth** — `matRare` 과광 조건부 전환 · `growth_tuning.json` → `data/balance/` 이동
- **leaf** — 물꽂이 용기 슬롯 수 (자취생 경제 재계산의 전제)
- **core** — 최소 루프 ①의 재미 확인

판단필요:
- **자취생이 지금 숫자로 성립하지 않는다.** 지출 16,125원/일 vs 10슬롯 포화 수입 7,680원/일(48%).
  방향 4개를 `game_flow.md` §4에 표로 뒀다(지출↓/시작금↑/슬롯↑/목표↓). **㉮지출↓+㉱목표↓ 권고**
- ★ **leaf·house 발견이 위 문제의 답일 수 있다** — "방 슬롯 ≠ 재배 슬롯"(leaf) +
  "반지하는 잠재력의 15%만 쓰고 있다"(house). **둘 다 "슬롯이 부족하다"는 전제를 깬다.**
  ⏸ 두 답이 오면 자취생 경제를 한 번에 재계산한다
- **`matRare` 과광 조건부가 자취생 경제를 바꾼다** — "희귀 한 방"이 *"등 2개 + 과광 감수"* 로
