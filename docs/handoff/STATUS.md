# 창별 상태 — 요약만. 상세는 {창}-to-{상대}.md

> 자기 섹션만 고친다. 섹션이 나뉘어 있어 git 이 자동 병합해 준다.

## house
갱신: 2026-08-01
완료:
- **방 6종 구조 확정 + 공간 기준 전수 측정.** 정본은 `docs/engine/rooms_spec.md`
  (구조 · space/slots/area · 높이별 DLI · 방별 성격 한 줄 · 캐릭터별 조건)
- 학원교실 11×6(교실+교사방, 깊은 창턱 1m) · 온실 10×12(3면 유리+천창 / 연구실 분리) 반영
- 렌더 빛 사고 5건 수정 — 해 방향 · 천장 그림자 · 유리 정책 · 창 확산광. `docs/engine/render_light_model.md`
- core 요청 ①`ctx.glazed` 누락 ②`weekStats` 캐시+선형성 경고 **둘 다 처리**

발견 (다른 창이 알아야 할 것):
- ★ **숫자에 라벨을 붙여야 한다.** `{방}.{space|slots}.{peak_summer|avg7_summer}` 네 갈래다.
  라벨 없이 "아파트 6.02"만 오가다 **세 번 사고**가 났다. `measured` 블록을 그렇게 갈라 뒀다
- ★ **방 등급의 정본은 `space`**(가구 없는 공간)다. `slots` 는 가구를 어디 놨느냐의 결과라
  밸런스 근거로 쓰면 안 된다 (반지하는 잠재력의 15%만 쓰고 있다)
- ★ **자연광만으로 갈라진 잎은 온실뿐.** 아파트 avg7 5.79 로 문턱 6.0 에 **0.21 부족**
- ★ **축이 둘이다** — 자연광은 창이, 조명등은 **면적**이 정한다.
  면적 순: 반지하 20 < 원룸 30 < 투룸 35 < 학원 66 < 아파트 99 < 온실 120㎡
- 온실 peak 16.16 이 몬스테라 `max` 16 을 넘는다 = **과광**. 차광이 필수 도구가 된다

대기:
- plan — 기본 가구 배치 · 반지하 활용률 15% · 원룸≈투룸 동률 처리 (아래 판단필요)
- core — `game.html` 신설되면 알려주기. 셸 이관 시점을 맞춘다

판단필요 (plan·사용자):
1. **기본 가구 배치** — "적당히 나쁘게" 의도해서 놓을 사안. 지금은 겹침만 피한 결과라 근거로 못 쓴다
2. **반지하 활용률 15%** — 벽 선반 하나를 창 높이(1.6m)에 달면 0.55 → 3.74. "반지하는 관엽 불가"가
   방의 한계가 아니라 배치의 결과다. 의도인지 확인 필요
3. **원룸 6.76 ≈ 투룸 6.69 (1% 차)** — 자연광은 동률로 두고 **면적(30 vs 35㎡)으로 구분**을 권함
4. **계산↔화면 자동 대조 검사** — 이번 사고 5건 중 3건이 그 유형인데 검사가 없다. 우선순위 판단 필요
