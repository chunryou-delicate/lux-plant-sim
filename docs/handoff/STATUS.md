# 창별 상태 — 요약만. 상세는 {창}-to-{상대}.md

> **자기 섹션만 고친다.** 섹션이 나뉘어 있어 git 이 자동 병합해 준다.
>
> ⚠ **파일을 통째로 다시 쓰지 말 것.** 2026-08-01에 실제로 사고가 났다 —
> 한 창이 전체를 재작성하고 다른 창이 그걸 커밋하면서 **두 창의 섹션이 날아갔다.**
> (git 이력에서 복구함) **자기 `## {창}` 블록만 편집한다.**

```markdown
## {내창}
갱신: 2026-08-01 (커밋 abc1234)
완료: (이번에 한 것 2~4줄)
발견: (★다른 창이 알아야 할 것 — 없으면 생략)
대기: (누구의 무엇을 기다리나)
판단필요: (★사용자·plan이 정해야 할 것)
```

★ **"발견"과 "판단필요"가 핵심.** 완료는 커밋 로그에도 있지만 저 둘은 여기 없으면 어디에도 없다.
커밋·**푸시**까지 해야 읽힌다.


---

## growth
갱신: 2026-08-09 (잎 간격표 · 몬스테라 유도 · 빈 시루 그림)
완료:
- ★★ **「40일인데 왜 50일이 나오나」를 밝혔다 — 둘 다 고장이 아니었다.** `spawnStep`(40)은 **마디** 간격이고 **생장나이** 축의 값이다. 잎 한 장은 축(생장점) 하나이고 새 축은 「중간잎(45.6) → 혹(20)」 뒤에 나므로 생장나이 축의 잎 간격은 **spawnStep × 3 = 120**, 거기에 `timeCurve`(0.72)가 걸려 실측 잎 간격이 **10·51·85·103·116·128·138·147**일이었다. 그 **51** 이 「약 50일에 한 장」이다
- ★★ **잎 간격표를 넣었다**(박사님 확정) — `data/growth_tuning.json · leaf_interval.days` = 30·40·50·70·100·150·200·300. 누적 **30·70·120·190·290·440·640·940** 유효 생장일. 실측이 박사님 값과 한 톨도 안 다르다. **격자(spawnStep·matSpan·seedEnd)는 한 글자도 안 고쳤다** — 바꾼 것은 시간 축(`ageOf`/`dayOfAge`)뿐이고 `timeCurve` 거듭제곱 곡선을 표가 대신한다. 표를 지우면 옛 곡선으로 그대로 돌아간다
- ★ **삽수/모주가 규칙에서 저절로 나온다** — 표가 「그 개체의 유효 생장일」의 함수라, 유효 0일부터 시작하는 삽수는 첫 칸(30)부터 걷고 모주는 뒤쪽(150·200·300)에 남는다. 장부를 따로 안 뒀다
- **몬스테라 유도 두 걸음**(자리 → 등) — `first_play §몬스테라 유도`. 10 **게임일**(유효일 아님 — 어두운 자리에선 유효일이 안 올라 영영 안 뜬다) 그대로면 「창턱으로」, 옮기고도 5일 그대로면 「등이 필요하다」. 대사는 `dialogue.monsteraGuideWindow`·`monsteraGuideLamp`, 순서는 `EVENT_ORDER`
- **빈 시루 계약** — `openSiruContractFromManifest` 가 `bagThumbnail`(뚜껑 덮인 시루)을 같이 낸다. 방에 서는 시루는 그대로 열린 시루다. ⚠ `game.html` 세 줄은 **안 건드렸다**(다른 워커) — 붙일 코드는 `growth-to-plan.md §4-㉣`
- ★ **곁다리 버그** — `placeCrop` 이 시루를 다시 심을 때마다 `fp.phase` 를 덮어써서 **몬스테라 안내가 도착 이틀 만에 사라졌다**. 몬스테라가 온 뒤에는 안 되감게 고쳤다

- **표시 단계를 `axis_rising` / `spear_ready` 로 갈랐다** — 143 `spear_ready` 0.000 / 144 0.333 / 145 0.667 / 146 `spear_furled` 0.000. `progress01` 재사상도 `daysPlanted` 오프셋도 아니고 **단계 자체를 나눈 것**이라 모든 새순 주기에 똑같이 걸린다
- **`growthPhase()` 구현** — `{phaseId, progress01, nextPhaseId}`. 원비율(A안) · 항상 0..1 · 저광이면 불변. 반환값에 생장일도 단계 경계 숫자도 `vigor` 도 없다. 경계는 `buildPlant`·`drawLeafStage` 가 쓰는 격자·임계값 그대로라 새 숫자를 만들지 않았다
- **`advanceTo`·`setGrowth` 의 렌더도 `drawStep` 경계로** — 논리 상태를 바꾼 뒤 그리므로 렌더 예외가 밖으로 나가면 코어는 "하루가 안 갔다", growth 는 "갔다"로 갈린다. 렌더가 터져도 달력·유효일·반환값이 일치함을 재현으로 확인
- **형태 진행도와 활력은 다른 축.** novice 는 형태 진행도 한 축만 본다 — 적정광에서 오르고 저광에서 멈추며 **줄지 않는다**. 코드가 이미 그 모양이라(`GROWTH` 는 `advanceTo` 에서 +1 만 하고 감소 경로가 없다) 손댈 게 없었다. **vigor·HP·시듦·고사는 구현 안 함**(고수 모드 범위)
- **CFG_VER 되돌림** — `phase-draft` 로 올렸던 것을 `strict5` 로 복구. phase 로직·기본값 변경이 없는데 버전을 올려 `mcfg_default` 의 P·ADJ 가 버려질 뻔했다(시드만 살아남는다). 저장값은 삭제·초기화하지 않았고 복원 재현 PASS
- ⏸ **vigor 확정안 [취소·보류]** — `growth-to-core.md` 의 체력 모델 절을 정정했다. 유효한 건 "활력은 형태 진행도와 별개 축"이라는 방향뿐이고, 밴드별 증감·잎 손실 주기·죽음 임계값은 **전부 후보**다. `vigor()`·`isDead()` 는 구현·요청 대상이 아니니 **배선하지 말 것**
- `thLoaded()` 읽기 전용 접근자 추가. 세터는 안 둔다 — 밖에서 켜면 로딩 게이트가 장식이 된다. false→true 재현 PASS, A~I 회귀 없음
- **`growthPhase()` 계약 초안 회신**(구현 완료) — `{phaseId, progress01, nextPhaseId}`. 반환값에 생장일이 없어 **core 가 143·146 을 복제할 수 없다**. 전문은 `growth-to-core.md`
- ★ **정본 로딩과 렌더 오류를 갈랐다.** 다시 그리다 난 예외가 '정본 로딩 실패'로 둔갑해 `TH_LOADED` 를 꺼버리던 길을 막았다 — 멀쩡한 정본으로 게임이 정지하고 원인은 엉뚱한 데를 가리키던 자리다. 실제 HTTP 서버 성공 경로 + 렌더 예외 분리 재현 PASS
- ★ **BLOCKER 완료 — 저광이면 형태 진행이 멈춘다.** 우회로를 전부 막았다: 오늘 DLI 를 이동평균보다 먼저 검사 · 정본 JSON 검증 통과 뒤에만 게임 경로 개방 · 유효 입력이 들어오면 튜닝 모드 자동 해제(체크박스까지). 재현 I-성공/I-실패/H-UI/G/A~F PASS, data/ 변경 0
- (이전) **보완 —** 조용히 넘어가는 길을 전부 막았다: advanceTo 는 하루만(초과 시 오류·상태 불변) · growthMin 폴백 제거 · DLI 미연결이면 게임 경로에서 정지(튜닝은 setTuningMode 로 분리). 재현 A~F PASS, data/ 변경 0
- (이전) **BLOCKER — 저광이면 형태 진행이 멈춘다.** 달력 경과일과 유효 생장 진행도를 갈랐다(`advanceTo`). 재현 A~D 전부 PASS. **코어는 `setGrowth`→`advanceTo`로 바꿔야 한다**
- core가 잡아준 **슬롯 매칭 버그** 수정 — 계약 객체 키는 `slotId`인데 `id`로만 찾아 항상 best로 떨어졌다. 둘 다 보게 함(옛 호출부 호환). 반지하 13배·아파트 273배 차이가 나던 자리다
- **갈라짐 표시 기준 통일** — `canFenestrate()` 신설. `bandOf(오늘값).fenestrating`은 오늘만 반짝 넘어도 true라 "갈라짐 시작!"이 거짓말이 됐다(core 지적). 화면은 7일평균 기준
- `plantCtx` 18축 확장 + `fCoef` **null→계수 1.0** 규칙 + `calcVarieType()` 자리
- 밴드 키 house와 일치(`critical`/`poor`/`stagnant`) · ⏸ 체력(vigor) 모델 기록은 **[취소·보류]**

발견:
- ★ **`varieProb`는 잎마다다(개체 단위 아님).** `MAT_TREE`도 잎마다. 최적광에서 잎 10장 중 3.91장이 무늬라 안 희귀하다. 더 큰 문제는 **매 잎이 독립이라 실생 100개를 심어도 좋은 개체를 고를 수 없다** — plan의 연구자 트랙 전제가 코드에 없다. 2층 구조(개체 소질×잎 발현) 제안을 `growth-to-plan.md` §8에 수치와 함께 올림
- ★ **NaN·음수 DLI가 확률을 NaN으로 만들던 버그.** `rng()<NaN`은 항상 false라 **오류 없이 조용히** 무늬·갈라짐이 영영 안 난다. 막아뒀다 — 계약에서 `dli`가 null/누락으로 오는 경우가 있으면 알려주기 바란다
- `plant_grow.html`은 **한 그루 전용**이다(개체 상태가 전부 전역). 다개체는 리팩터 필요
- 튜닝값은 `data/growth_tuning.json`. 밸런싱은 이 파일만 고치면 되고 `plant_grow.html`은 열 필요 없다. ⚠ `thresholds`·`variegated.need_mult`는 `light_thresholds.json`과 같은 값이어야 한다
- ★ **`judgeDLI`류 밴드 키가 바뀌면 반드시 알려주기 바란다** — 생장 창이 같은 키로 확률을 가른다

대기:
- **plan** — 개체 소질 값(등급·배율·분포) / `f_propagation` 배율 / `calcVarieType` 조건 / (⏸ vigor 표시 토글은 보류) / `matRare` 과광 조건부 / `growth_tuning.json` → `data/balance/` 이동 여부
- **core** — 루프가 다개체를 실제로 요구하는 시점. **설계는 끝났다**(`growth-multiplant-design.md`) — 지속 상태가 8개뿐이라 함수 42개를 안 고치고 전역 교체로 간다. 착수는 core 요청 시점
- **leaf** — 콩나물 게임 검증·`bumpFit` 재조정 요청 받았다. 다개체/작물 배선이 없어 **지금은 못 한다**. 코어 루프에 작물이 붙는 시점에 하겠다

판단필요(2026-08-09):
- ★★ **창턱이 등 없이 자란다** — 박사님 표는 창턱 DLI **1.52·안 자람**인데 저장소 값으로 재면 **4.80·자란다**(여름). ⇒ 유도의 두 번째 걸음(등)이 여름엔 안 뜬다. 1.52 가 어디서 나온 값인지는 **못 밝혔다**(짐작 금지). 선택지는 `growth-to-plan.md §4-㉠`
- ★ **튜토가 유효일로 9일 길어진다** — 첫 말린 새순이 유효 61(도착+16) → **70(도착+25)**. 줄일 손잡이는 이제 `ARRIVAL.growthDays` 를 60 언저리로 올리는 것 하나다(도착+10). 값(45)은 안 바꿨다
- **몬스테라는 책상이 아니라 서랍장에 도착한다** — `loop.js` 가 「가장 어두운 자리」를 고르고 반지하에서 그건 `banjiha-dresser:0`(0.08)이다. `loop.js` 는 이 창 소유가 아니라 안 고쳤다
- **`monstera.guide` 가 세이브에 안 실린다** — `save.js` 화이트리스트. 붙일 코드는 `growth-to-plan.md §4-㉢`

확정:
- **첫 플레이 초기 유효 진행도 = 45일**(`state.ARRIVAL`). 잎이 나는 유효 생장일은 **30·70·120·190·290·440·640·940**(잎 간격표).
  - 도착(45) 상태 — 줄기 1개 · 잎 1장
  - **유효 70** — 둘째 잎의 말린 새순 등장(첫 플레이 완료 신호)
  - ⚠ 옛 「143 → 146」은 `timeCurve` 시절 값이다. 지금 그 자리는 **117 → 120**(셋째 잎)이다
  - 3년(1,095일) 안에 나는 잎은 **여덟 장**이다(9번째는 누적 1,390일)
  - 이 숫자는 확정이다. 전역 성장 곡선과 밸런스는 건드리지 않는다

판단필요:
- **저광 정지 반응 속도** — 7일 이동평균이라 저광 전환 후 3일은 관성으로 더 자란다. 즉시 끊으려면 `dliAvg(3)`로 좁히면 된다
- **개체 소질 2층 구조** — 지금은 무늬종이 흔하고 실생 선발이 성립하지 않는다. 값만 정해지면 붙인다. 선택지·수치는 `growth-to-plan.md` §8
- (아파트 갈라짐 — plan이 ⑶으로 확정. "자연광 갈라짐은 온실뿐". 종결)

---

## house
갱신: 2026-08-02 (반지하 첫 플레이 프로필 `6e82bc6`)
완료:
- ★ **반지하 창 높이 슬롯** `banjiha-sill:0` @ (0, 1.585, −1.95) · **peak_summer 3.77** / avg7 2.42
  (몬스테라 min 3.0 대비 +26%). 어두운 자리 `banjiha-dresser:1` peak 0.04, ≤0.3 슬롯 9칸 전부 시루 가능
- **반지하 정적 프로필 재생성** — `uidStable:true` · 최신 `roomRev` · 14슬롯 전부 `maxPotD`.
  라이브 `daily_light/1` ↔ 정적 `room_profile/1` 최대 오차 0.005, best `banjiha-sill:0` 일치 ✅
  나머지 5방의 안정 uid·정적 프로필은 첫 플레이 뒤로 보류했다.
- space 격자에서 **칸막이·도려낸 경계 벽 속**도 제외. `space.peak` 여섯 방 전부 불변,
  칸막이 있는 방의 면적·부피만 감소(학원 61.8→60.6 · 아파트 94.6→91.6 · 온실 114.6→112.4㎡)
- 면적 라벨 `net`/`gross`/명목 분리 · `utilization_pct` → `sampled_peak_ratio_pct`
- `docs/engine/verify_tools.md` 신설 — 검증 도구 목적·통과 기준 등록

발견 (다른 창이 알아야 할 것):
- ✅ `light_adapter.build()`의 `w.cz` 누락은 `16d311b`에서 해소했다. 온실 10.49→14.55,
  천창 없는 5방은 불변. 온실 정적 프로필 재생성은 첫 플레이 뒤 작업이다.
- ✅ 안정 `slotId` 계약도 `16d311b`에서 해소했다. 반지하는 명시 uid 14/14와 게임 로드까지 완료,
  나머지 5방 uid 45건은 세이브·해당 방 플레이 전에 처리한다.
- ★ 아파트 `_place_test` 통과는 **반지하 슬롯의 증거가 아니다.** `_bj_place.html` 로 따로 본다
- 반지하 `sampled_peak_ratio_pct` **101%** 는 오차가 아니라 표본 차이다(격자 y=1.60 vs 상판 1.585).
  100 으로 자르지 않았다

대기:
- **첫 플레이 뒤** — 나머지 5방 uid 45건·정적 프로필 재생성, 온실 기본 차광값.
- **사용자 화면 판정** — 창턱 밝기·작은 받침으로 읽히는지·반지하 인상 유지 여부.
- 세이브 복원·드래그는 첫 플레이 범위 밖이며 현재 통합의 선행 조건이 아니다.

판단필요 (plan·사용자):
1. **화면 체감** — 창턱이 방 안쪽보다 밝아 보이는지, "과한 가구"로 안 읽히는지
   (`docs/engine/shots/banjiha_*.png`). ★ 최종 체감 판정은 사용자
2. **온실 `space.peak` 15.09** — 과광 마진이 `max` 16 대비 −6%. 기본 차광값에 영향
3. 나머지 5방 slotId 고정 시점 — 세이브 붙기 전이 싸다

## core
갱신: 2026-08-04 (커밋 전 · 물주기 `39278d3` 다음)

완료 (2026-08-04 · 수확을 행위로 · 씨앗값):
- ★ **수확이 손 동작이 됐다** — `loop.harvestCrop(S, io)`. 자라는 날이 차면 *거둘 수 있는 상태*가
  될 뿐이고 저절로 안 거둬진다. **첫 수확의 몬스테라 선물·배움 ①②·대사 사건이 전부 이 함수의
  반환값에서 나온다**(`turn.plantArrived` 는 이제 늘 false). 원자적이라 선물이 실패하면 수확도 무른다
- ★ **안 거둬도 벌은 없다.** 회전이 멈춰 절감이 0인 것이 벌의 전부다(물주기와 같은 사상).
  **다 자란 뒤에는 물도 안 요구한다** — 속도 축이 쓸 곳이 없고, 만들면 그게 둘째 벌이다
- ★ **빨리감기는 두 모드가 다 선다**(`stopOnReady` 기본 켜짐). 물과 달리 정지 비용이 작다 —
  거둘 날은 회전당 한 번이고 배속은 재파종을 안 하므로 한 번 감는 동안 최대 한 번 선다
- ★ **씨앗값 1,500 → 1,000원.** ⚠ **정본이 둘이었다** — `first_play.seedWonPerSiru`(정가·표시용)와
  `shop.CATALOG.bean_seed.listWon`(**지갑이 실제로 쓰는 값**). 앞엣것만 내렸으면 순액이 안 바뀐다
- 자동수확은 **자리만** 뒀다 — `S.perks.autoHarvest`(늘 꺼짐) · `loop.hasAutoHarvest(S)` · 세이브 왕복
- 세 경로 40판 A/B/C 전부 **100%** · 중앙값 튜토 57일 · 전부 가을. 14개 스위트 PASS
- `data/` · `assets/` · `game.html` · `room_view.js` · `plant_grow.html` 변경 0건

발견 (★다른 창):
- ★ **씨앗값의 정본이 코드에 둘이다.** `characters.json._meta` 로 옮길 때 **상점 값도 같이** 옮겨야
  자리가 하나가 된다. 지금은 값만 맞춰 뒀다
- `probe_crop_cases.mjs` 가 거둔 날 곳간을 안 열고 있었다(회전마다 하루치 유실) — 고쳤다

대기:
- **`game.html` 의 [수확하기] 버튼 배선** — 박사님이 하신다. API·예시 코드는
  [`core-crop-harvest.md`](core-crop-harvest.md) ⑥. 그때까지 화면에서는 콩나물이 다 자란 채로 멈춘다

판단필요:
- `beansprout_ready` 에 대사를 붙일지 (지금은 버튼이 말한다 — 붙이면 잔소리가 될 수 있다)
- 순액을 더 올리는 정답은 씨앗값이 아니라 **2종째 작물**이다(2,000원/회전 자리는 이미 있다)

전문: [`docs/handoff/core-crop-harvest.md`](core-crop-harvest.md)

---

이전 갱신: 2026-08-02 (`f2dca66` + 경계 보완 `ae3b070` + 화면 보완 `7fa33b0`)

완료 (2026-08-02 · 첫 플레이 얇은 통합):
- ★ **수동·자동 Day 0→7 한 흐름 연결** — 열린 콩나물 시루 배치 → 4턴 수확 → 슬롯 DLI 평균으로
  `3/2/1끼` → 하루 2끼 사용 상한과 식비 절감 표시 → Day 4 몬스테라 도착 →
  `banjiha-sill:0` 이동 → 적정광 3턴 뒤 `spear_furled`(말린 새순).
- ★ **첫 플레이 기본 UI** — 자원 3칸·아이템 카드·자리 선택·퀘스트·형태 게이지·자동 결과.
  기존 개발 시나리오는 접힌 검수 영역으로 유지. `?embed=game`에서만 growth 튜닝 패널을 숨긴다.
- 독립 Chrome 자동 3회와 수동 1회 모두 Day 7 · 3끼 · 5,000원 절감 · `spear_furled` 일치.
  `test_first_play` · 16개 공격 케이스 · `loop_errors` · `test_banjiha_profile` PASS.
  Day 4 수확·식비와 도착 배너 동시 표시, 이동 뒤 관찰 안내 전환도 `7fa33b0`에서 보완했다.
- 최종 화면 증거는 이름 있는 Remote 세션 `1e82669a-552b-49ed-8cb8-aec808bf6743`에서
  새 탭 로딩 뒤 3~4초 대기하고 촬영했다. Day 4의 두 배너 순서와 Day 7의 정확한
  `spear_furled`·오류 0건을 확인했다. **말린 새순의 형태 자체가 한눈에 읽히는지는 사용자 QUESTION**이다.
- **전체 첫 플레이 PASS·재미 판정은 선언하지 않음.** 사용자가 실제 화면·조작 체감을 판단해야 한다.
- `data/balance/` · `docs/GAME_PLAN.md` · 에셋 · 전역 밸런스 변경 0건.

완료 (2026-08-02 · 첫 플레이 유효 생장 연결):
- ★ **일일 루프를 `advanceTo` 로 교체.** `setGrowth` 는 **개체 도착 때 1회**뿐이다(감시 카운터 있음).
  `ready()` 는 **일곱 함수**(`growthPhase` 포함)가 다 있고 **임계값 정본까지 실린 뒤**에만 연다.
  진행 버튼은 그전까지 disabled, 실패하면 잠긴 채로 둔다
- ★ **도착 진행도 143의 두 축 분리** — `daysPlanted`(돌본 날) 0 / `growthDays`(형태) 143.
  `newState().pots` 는 **빈 배열**이다. 앱을 열었다고 식물이 생기지 않는다(도착 이벤트가 만든다)
- ★ **오늘 빛은 매일 넘긴다 — `null` 도.** 호출을 생략하면 growth 안에 어제 값이 남아
  "빛이 없는데 어제 빛으로 자라는" 상태가 된다
- 턴 결과에 `growthCalendarDay·effectiveGrowthDays·grew·growthBlocked` 를 낸다(빈 값으로 숨기지 않음)
- 안정 ID 계약 마감 — **중복 slotId·중복 uid 도 프로파일 중단** 사유에 넣었다. 로더도 거부
- 재현 전부 PASS (아래 발견) · `data/balance/` 변경 0건 · 남의 파일 변경 0건

완료 (2026-08-02 · 프로파일 마감 전 선행 계약 수정):
- ★ **`winFromHouse` 9번째 인자 `w.cz` 누락 수정** — 코어만 빠져 있었다.
  **온실 best 10.49 → 14.55 재현**, 천창 없는 5방은 불변
- ★ **안정 `slotId` 계약 확정** — 전역 순번 fallback 폐기. 화분 슬롯을 내는 가구는 명시 uid 필수,
  없으면 `TEMP~` 표시 + `console.error`, **영속 프로파일은 throw**(조용히 보완 안 함)
- **명시 uid 필요 가구 45건을 방별로 회신**(`core-to-house.md` ③). 반지하는 이미 완료(14칸 안정)
- **첫 화분 `assets/monstera/pot.glb` 확정** — 회전 무관 지름 0.202 ≤ 창턱 0.21
- `data/profiles/room_profile.banjiha.json`은 `6e82bc6`에서 최신 계약으로 재생성되어 게임 로드 PASS.
  나머지 5방은 안정 uid가 없어 의도적으로 거부하며 첫 플레이 뒤로 보류한다.

완료 (2026-08-01):
- **최소 루프 v0** (`game.html` + `src/game/*`) — [다음 날] → 날씨 → DLI → 생장 1틱 → HUD. 고사·경제는 0줄
- ★ **재미 확인 시나리오 5개** — `game.html` 에서 눌러 돌린다. 갈라짐이 켜지는 순간에 배너가 뜨고,
  7일평균·문턱선·전환 시점이 그래프로 보인다. **"등을 켰더니 갈라지기 시작했어요"가 24일째에 뜬다**
- **밸런스 자동 시뮬 준비** — `loop.js` 를 UI에서 분리(순수. `{S, turn}` 반환),
  `room_profile.js` 신설: 방 조도 기하를 JSON으로 뽑아 **THREE 없이** 헤드리스 재생(6방 `data/profiles/`).
  `sim.js` 에 시나리오·결과 형식 정의(`balance_sim/1`) — 경제·목표 칸은 `null` 로 비워 둠
- growth `canFenestrate()` 반영(갈라짐 표시를 7일평균 기준으로), house `glazed`·`weekStats`·`measured` 새 라벨 반영

발견 (다른 창이 알아야 할 것):
- ★ **`ready()` 통과 뒤에도 계약은 사라진다**(iframe 새로고침). 예전엔 어댑터가 null 을 돌려줘서
  **달력만 가고 형태는 그대로인 턴이 조용히 지나갔다.** 이제 계약 함수 7개는 없으면 전부 던지고,
  루프는 상태를 건드리기 전에 `assertContract()` 로 막는다 — 재현: day 3→3 · 이력 3→3 · 돌본 3→3
- ★ **"예외면 양쪽 상태 그대로"는 철회한다.** 코어는 **코어 상태만** 되감을 수 있다.
  `advanceTo` 는 `CAL_DAY`·`GROWTH` 를 먼저 올리고 그리므로, 그 사이에 터지면 growth 는 이미 갔다.
  그래서 되감기 전에 growth 달력을 물어보고 **안 갔을 때만** 되감는다(`e.coreRolledBack`).
  갔으면 되감지 않고 날짜를 맞춘 뒤 `S.desync` 에 남긴다 — 화면 문구도 그렇게 고쳤다
- ★ **growth 의 렌더 오류 경계가 실제로 동작한다**(확인함). `buildPlant()` 를 던지게 바꿔 넣어도
  `advanceTo` 는 정상 반환하고 상태는 한 칸만 간다 → 렌더 오류로는 어긋남이 생기지 않는다
- ★ **이동평균의 정본 의미 = "최근 7개 유효 관측값"** (달력 7일 창이 아니다). growth 의 `dliAvg` 와 같다.
  코어는 `loop.rollingAvg` **하나만** 쓴다 — 루프·HUD 그래프·`fenDayOf` 가 각자 창을 짜면 어긋난다.
  화면 규칙: **하루선은 결측일에서 끊기고, 평균선은 이어진다**(직전 유효 7개가 그날의 판정값이라서)
- ★★ **`plant_grow` 의 전역 함수가 있다고 준비된 게 아니다.** 임계값 정본(`growth_tuning.json`)이
  **비동기로** 실려서, 함수만 보고 루프를 돌리면 **첫 며칠이 통째로 정지**한다
  (브라우저에서 유효 143 그대로, 사유 "임계값 정본이 안 실렸습니다"). 헤드리스에선 안 잡혔다.
  → **해소.** growth 가 `thLoaded()` 를 내줬고 `ready()` 가 그 상태를 직접 확인한다(추정 프로브는 지웠다).
  ⚠ `growthMin()` 은 판정에 못 쓴다 — 코드 기본값이 있어 정본 없이도 값을 돌려준다(그래서 안 던진다)
- ★ **저광 정지는 즉시가 아니다 — 낙차가 클수록 오래 버틴다.** 7일 이동평균이라
  3.1→2.9 는 **3일**, 반지하 live 12.2→0.09 는 **5일** 더 자란 뒤 멈춘다.
  growth 의 "저광 정지 반응 속도" 판단필요와 같은 현상 — 코어는 손대지 않았다.
  ★ 플레이어에게는 "옮겼는데 며칠째 그대로"로 보인다. 말 피드백(A4)이 이걸 설명해야 한다
- ★ **`plant_grow.html` 은 헤드리스로 돌릴 수 있다** — 3D(`init`)만 스텁으로 걷어내면
  생장 로직은 그대로 vm 에서 돈다(함수 선언 호이스팅 + `let` 은 파일 끝 `init()` 전에 초기화됨).
  다개체 리팩터 뒤에도 이 방법이면 **진짜 생장으로 밸런스 시뮬을 돌릴 수 있다**(`growth_real: true`)
- ★ **`maxPotD` 검사를 bbox 로 하면 안 된다.** `pot_concrete_square.glb` 는 bbox 0.200이라
  통과처럼 보이는데 **대각선 0.275**라 창턱(0.21)에 못 올라간다. 회전 무관 지름
  `2×max√(x²+z²)` 로 비교할 것. `real_max_m` 은 *최대 치수*(높이일 수 있음)라 그대로 쓰면 안 된다 —
  세 화분 모두 `real_max_m` 0.20인데 하나만 못 올라간다 (leaf·house 공통)
- ★ **천창은 부분 개구부라 `cz` 가 0이 아니다.** `winFromHouse` 9번째 인자를 빠뜨리면
  천창이 방 한가운데로 밀린다 — **온실만 10.49 vs 14.55 로 갈리고 다른 방은 티가 안 난다**
- ★ **v0 완료 조건이 돈다.** 반지하 90일: 등0 → 0% / 등1·나쁜자리 → 0% / 등1·좋은자리 → 100%.
  **20일 뒤 등을 켜면 24일째 갈라짐이 켜진다** — 켜자마자가 아니라 **3일 뒤**다(판정이 7일평균이라).
  이 지연이 오히려 학습을 만든다. 즉시 반응하면 "버튼을 눌렀다"가 되고, 사흘 뒤면 "환경을 바꿨다"가 된다
- ★ **등을 켜도 나쁜 자리는 90일 내내 아무 일도 없다.** 전기만 3,420원. 같은 등·같은 전기인데
  자리 하나로 **162배**가 갈린다. `acceptance.json` 의 빛 2줄 **통과**(`accepted: true`)
- ★ **자리가 등보다 크다** — 등을 하나 더 사면 +0.15, 한 칸 옮기면 ×2.
  **박사님 확정: 축은 "등 개수"가 아니라 "자리"다**
- ★ **프로파일 경로가 어댑터와 소수점까지 같다**(전 슬롯 오차 0.0000). 시나리오 90일이 **7ms** —
  방 조립 없이 도니 매트릭스 수십 번이 초 단위다. 시뮬은 `room_profile.json` 만 있으면 된다
- ★ **하루 값과 7일평균이 갈라지는 자리가 실재한다** — 반지하·등1개 자연광 최고 슬롯은
  하루 6.02(문턱 넘음) / 7일평균 5.82(못 넘음). 표시를 하루 값으로 하면 플레이어에게 거짓말이 된다
- **내 숫자는 전부 `slots` 기준**이다(가구 위 화분 자리). house의 `space` 기준과 다른 축이니
  섞어 읽으면 안 된다 — house의 라벨 규약(`{방}.{space|slots}.{peak|avg7}`)을 따른다
- `weekStats` 의 "등은 밖에서 더하라" 처방은 **`mean` 에만** 통한다. `p10/p50/p90`·`overPct` 는
  등 포함 값으로 세야 맞다(자연광만 넘기면 "등을 켜도 안 갈라진다"가 된다) — core-to-house 참고

대기:
- **첫 플레이 뒤** — 반지하 외 5방 uid·정적 프로필. 반지하 게임 경로는 더 이상 house 대기가 아니다.
- ★ **사용자 — 첫 플레이 화면 판정.** 조건: `novice · 등 0 · banjiha-sill:0 · 다음 날 3회`.
  `_core_shot.html?days=1`(144) / `?days=3`(146) 을 브라우저로 열어 **말린 새순이 눈에 구분되는지**.
  숫자는 통과했다 — DLI 3.77 · 유효 143→146 · 갈라짐 ✕ · 잎 2→3 · 새순 렌더 0→1
- growth — ① `thLoaded()` **[받음]** ② `growthPhase()` **[받음·배선 완료]**.
  코어는 `{phaseId, progress01, nextPhaseId}`만 표시하며 146 경계를 복제하지 않는다.
  ③ 다개체 리팩터는 첫 플레이 이후 보류.
- **활력(vigor)은 취소·보류다** — novice 는 형태 진행도만, 활력은 숨김·감소 없음,
  고수 모드 활력도 보류(박사님 2026-08-02). 코어는 훅조차 두지 않는다.
  이전에 적었던 "v1에 읽겠다"류 문구는 전부 철회했다(코드 주석 포함)
- house — `weekStats.mean` 확률 가중 (`roomRev` 복사·프로파일 위치는 반영·승인 완료)

(정정 2026-08-02: 아래 두 줄은 **완료**되어 내렸다 —
 `setGrowth`→`advanceTo` 교체 · 초기 진행도 143 반영 및 `banjiha-sill:0` 배치)

판단필요 (plan·사용자):
1. ~~등 축 → 자리 축~~ · ~~합격선 50%~~ · ~~프로파일 리포에 두기~~ — **셋 다 확정, 반영 완료**
2. **합격선 50%를 `acceptance.json` 으로 옮겨 주기(plan)** — 지금은 `sim.js FEN_PASS_PCT` 에
   기본값으로 들고 있다. 밸런싱 숫자가 코드에 있는 상태라 plan 소유로 가는 게 맞다
3. **다음 재미 축이 A4 말 피드백인지 확인** — "오늘 안 자란 게 날씨 탓인지 등을 안 켠 탓인지"를
   말로 설명하는 것. 계약이 `dli_daylight`/`dli_lamp` 를 나눠 주므로 코어가 바로 붙일 수 있다.
   plan 흐름을 기다리지 않고 진행할지 판단 부탁

---

## leaf
갱신: 2026-08-01 (커밋 0a4156a)
완료:
- **식용작물 확장** — 원화 21장 + 절차적 3D 14종 + Meshy 3D 12종(상추·바질·딸기본체·딸기열매)
- 앞서 **콩나물 1종 완주** — 공용 씨앗·새순 4 + 본체 3 + **시루(슬롯 15칸)**, 전부 절차적
- manifest **448 items** · 전 GLB에 실제 크기(`scale_to_real`) 부여
- 잎 결함 수정 — 잎자루 접합 6건 + **법선(NORMAL) 누락 21개** 복구

발견:
- ★ **Meshy 크레딧은 창 공유인데 `rigging`·`animation` 은 `list_tasks` 로 조회가 안 된다.**
  char 소비 약 145cr이 그래서 안 보였음 → **`manifest._credit_log` 기록 규칙 신설.
  전 창이 유료 API 쓸 때 한 줄씩 남겨 주세요.** 없으면 충전 시점을 못 잡습니다
- ★ **plan의 「자취생 경제 미성립」에 직접 관련** — 방 슬롯이 13개라 부족하다 하셨는데,
  **콩나물 시루는 방 슬롯 1칸을 쓰면서 재배 슬롯 15칸을 줍니다.** 재배트레이도 12·24칸.
  즉 **방 슬롯 ≠ 재배 슬롯**이라, 시루 2개면 30포기입니다. 재계산 때 이 배수를 반영해 주세요
- **절차적/Meshy 판단 기준**: 곧은 잎·회전체·구체 = 절차적 / 주름·겹침·표면패턴 = Meshy.
  딸기 열매는 절차적로 씨앗이 안 읽혀 실패 → Meshy로 해결
- **시루·재배트레이 슬롯이 관엽 선반과 완전 동일 규격** → 가구·작물용기를 한 배치 로직으로 처리 가능
- GLB 내보낼 때 **`vertex_normals` 를 미리 계산해야 NORMAL 이 들어간다.** 안 하면 three.js가
  flat shading만 켜고 **잎 두께 조절이 통째로 무효**가 됨(실제로 겪음)
- house 실측 대입: **반지하 0.55~0.48 짜리 4칸은 저광작물(새싹채소·상추) 자리**

대기:
- **plan** — 물 시스템 2건(수분 게이지 통일 / 물을 공통 시스템으로). 7/26 요청분, **사용자가 재촉함**
- **plan** — `docs/assets/` leaf 소유 배정. **사용자가 승인 요청함**
- **growth** — 콩나물 게임 검증. growth 답변: *"다개체/작물 배선이 없어 지금은 못 한다.
  코어 루프에 작물이 붙는 시점에 하겠다"* → ⚠ **작물 양산이 core 루프 진척에 묶였다**
- **growth** — `stem_knob`·`leaf_sheath` `bumpFit` 재조정 (같은 이유로 대기)

판단필요: **없음** (2026-08-01 사용자 결정으로 4건 모두 종결)
- ~~Meshy 충전~~ → **안 함.** 22cr 유지. 토마토·고추는 콩나물 게임 검증 후
- ~~house_mod GLB 4종 삭제~~ → **보류.** 8MB는 부담 없고 참고 가치 있음 (house에 전달 완료)
- ~~작물 9종 실사용~~ → **검증 후 진행**으로 확정
- `docs/assets/` 소유는 plan 승인 대기로 이동(위 대기 항목)

메모: char가 확인해 줌 — **Meshy ~145cr 은 리깅·애니 소비가 맞았다.** `_credit_log` 규칙 전 창 전파 완료.
⚠ leaf 는 **작물 신규 제작을 멈추고 대기**한다. 재개 조건 = core 루프에 작물 배선 등장.

---

## char
갱신: 2026-08-01 (커밋 1372b41)
완료:
- **캐릭터 8종 에셋 완료** — 실스케일(여1.40/남1.50/몬이0.375) · 경량본 `lq/`(폴리곤 1/10) · 모션 19종 128클립
- 색 커스터마이징(머리·눈썹·피부·홍채) **접음.** 기본형 확정. 함정 8개는 `assets/characters/README.md` §5
- 도구 8개 `tools/char/` 이관 · house 인계 3건 처리 확인. **표정만 남기고 대기 중**
- **캐릭터별 성격(㉮안) 준비 완료 — 새 모션 0건 · Meshy 0cr.** 기존 16종을 전수 측정해
  서서 하고 끝자세가 돌아오는 **10종**을 골라 배정표+재생코드를 `char-to-house.md` 에 올림

발견 (다른 창이 알아야 할 것):
- ★ **캐릭터 키가 1.50/1.70 → 1.40/1.50 으로 줄었다**(2026-07-26, 방이 좁아 보여서).
  키를 하드코딩한 곳은 어긋난다. **몬이는 비연동 0.375 고정**이다
- ★ **게임 화면은 `lq/` 를 써야 한다** — 원본은 평균 22.5만 삼각형이라 방과 같이 돌리면 버벅인다.
  클립은 노드이름 바인딩이라 원본·경량본 **어느 쪽에도 그대로 붙는다**
- ★ **색 커스터마이징은 사후 마스크로 불가능하다.** Meshy가 전신을 아틀라스 한 장에 굽는다.
  남주부 셔츠는 그의 얼굴과 색도거리 **0.0247** 로 남연구원 **손(0.0374)보다 얼굴에 가깝다**
  → 어떤 색공간으로도 못 가른다. 필요하면 **모델 생성 단계에서 부위를 나눠 뽑아야** 한다
- 모션 클립 128개는 **캐릭터 비종속**(24본 표준골격 · 노드명 8종 동일).
  키를 또 바꾸셔도 **클립은 손 안 대도 된다**(씬 최상단 래퍼 스케일 방식)
- ★ **캐릭터 성격 표현에 Meshy 를 안 써도 된다.** 새 idle 4종을 뽑으면 12cr인데
  **기존 16종 중 10종이 그대로 idle 변주로 쓸 수 있다**(서서 함 + 끝자세가 시작으로 복귀).
  `sit`(Hips 72%)·`harvest_crouch`(68%)·`doze`(55%)만 앉음이라 제외.
  **leaf 의 토마토·고추 90cr 과 경합하지 않는다** — 잔액 22cr을 leaf 가 다 써도 된다
- 클립을 idle 사이에 끼울 때 **루트가 최대 42%(Hips 높이 대비) 움직인다.**
  걷기와 같은 Hips XZ 고정이 필요하다. `char-to-house.md` 에 코드로 올림
- leaf 의 `_credit_log` 규칙 확인했습니다. **char 의 Meshy 소비 약 145cr 은 리깅·애니라
  `list_tasks` 로 안 잡히는 게 맞습니다.** 앞으로 유료 호출 시 한 줄씩 남기겠습니다

결정됨 (박사님 2026-08-01):
1. **표정** — core 루프 재미 확인 후 착수. 신호 대기
2. **2D 초상화 56장** — ★**착수 반대에 동의.** 대화 UI 없이 뽑으면 낭비 확정
3. **캐릭터별 성격** — ★**㉮ idle 변주 확정.** 위 발견대로 **크레딧 0으로 구현 가능**

대기:
- **house** — idle 변주 배정표·재생코드 적용 (`char-to-house.md` 2026-08-01 절).
  엔진 코드가 house 소유라 제가 못 붙입니다
- **plan** — 소유표에 `tools/char/*`·`assets/derived/*` 추가 / `character_material_constraint.md`
  경로 4곳 + 보류 표시 (`char-to-plan.md` ⓐⓑ)
- **core** — 최소 루프 재미 확인. **그게 끝나야 표정 착수**(박사님 지시)

판단필요 (plan·사용자):
- **없음.** 3건 모두 2026-08-01에 박사님이 답을 주셨다 (위 「결정됨」 참조).
  다음 판단이 필요한 시점은 **core 루프의 재미 확인이 끝났을 때**다 — 그때 표정 착수 신호만
  주시면 된다. 그 전까지 char 창은 새로 여쭐 것이 없다

---

## plan
갱신: **2026-08-05** (QA·창밖·원룸·밸런스 통합 `c9fe689`)

### 2026-08-05 — 워커 넷을 받아 통합했다

완료:
- **③ 원룸이 실제로 열린다.** `moveOut` 은 돈과 판정만 했다 — 「이사했습니다」가 떠도
  `S.home.room` 이 `banjiha` 그대로라 조도·자리·화면이 전부 반지하였다.
  `oneroom.moveIntoOneroom(S, io)` 으로 바꿨다. `room_view` 에는 방을 갈아 끼우는 문이
  없어서(만들 때 한 번 짓고 끝) `remountRoomView()` 로 **부수고 다시 짓는다.**
- **오류가 다시 말을 한다.** `#side` 는 폰 세로 레이아웃으로 바꾸며 없앴는데 `guard()` 가
  아직 불렀다 — 오류를 적으려다 거기서 또 터져 **잠금만 걸리고 설명은 사라졌다.**
  화면이 통째로 회색인데 아무 말도 안 뜨는 그 증상이 이것이었다.
- **완주한 판에서 [빨리감기] 한 번이 게임을 잠갔다.** `timeModeOf` 가 점핑→배속으로
  바뀌는데 배속은 `maxDays` 가 없으면 반드시 던진다. 이제 배속일 때만 그 모드의
  자기 상한(`FAST_MODE_MAX_DAYS`)을 넘긴다.
- **물주기가 한 발도 안 뗐다.** `cropKey()` 가 안 놓은 작물에 `free:crop_01` 을 지어 냈고
  방이 모르는 이름이라 즉시 실패했다. `doAct` 도 `resolveKey` 로 먼저 묻는다.
- **판을 지우는 버튼이 플레이어 가방에 있었다** — [첫 플레이 자동 시뮬레이션].
  `disabled` 로 적혀 있었지만 부팅 끝의 `GATED` 해제가 다시 켜 줬다. 이제 안 보여 준다.
- 손가락 안내가 [다음 날] 대신 **오늘 할 일**(거두기·물주기)을 가리킨다 ·
  끄는 라벨이 작물 제 표를 읽는다(콩나물을 어두운 데 끌 때 「고사」라고 했었다) ·
  「새로운 화분이 옵니다」가 **몇 회전 남았는지** 말한다 · 주문·팔기가 한 번 되묻는다 ·
  상점에 소지금이 뜬다
- 창밖 골목(+34 삼각형 · 드로우콜 +1) · 놓을 자리 **세 색** 판정 배선 · 걷는 동안만 60fps

발견:
- ★★ **두 워커가 독립적으로 같은 병목을 짚었다 — `data/house_rooms.json` 이다.**
  · 원룸: 가장 밝은 자리 **3.07** (갈라짐 문턱 6.0) · 슬롯 11칸(반지하 14) ·
    식물등 자리 **0개** · `uidStable` 없음(프로파일이 안 열린다)
  · 반지하: `tau` 0.55 라 A/B/C 세 경로가 **바이트 단위로 같다**(20/24 성공 · 유효
    생장일 249 · 잎 4장). 25,000원짜리 등이 사는 것은 −25,000원 잔고뿐이다
- ★ **계절 달력이 둘이다.** `tutorial.seasonAt` 과 `weather.seasonOf(S.day)` 가
  135일 어긋난다 — 게임일 50 이 화면엔 `autumn`, 빛 계산엔 `spring` 이다. **고장이다.**
- ★ **밝기가 빠르기를 안 바꾼다.** DLI 3.77 과 12.0 이 **둘 다 잎당 72일**이다.
  밝기는 품질(갈라짐)만 정하고 속도는 안 정한다 — 의도인지 확인이 필요하다
- ★ 진짜 데드라인은 73일이 아니라 **60일**이다(30·60일 월세 목돈이 빠져 있었다).
  실제 이사까지는 **219 게임일**이 걸린다
- ★ 작물 겹침은 **이미 종류마다 따로 센다**(박사님 ⑦ 앞부분은 되어 있었다).
  다만 재 보니 무순이 값에서 밀린다 — 아래 판단필요

대기:
- **무순 3D** — 워커가 `room_view.js` 에 `kind:'musun'` 분기를 붙이는 중.
  코어·에셋·상점은 다 있는데 방이 못 그려서 **지금 사면 돈만 없어진다**
- **박사님** — 아래 판단필요 여섯

### 박사님 확정 (2026-08-05 밤) — 넷 다 반영 끝

- ✅ **㉠ 반지하 `tau` 0.55 → 0.70.** 창턱 DLI 등 0개 3.77 → **4.80** · 등 1개 5.61 → **6.64**.
  이제 **등을 사야 갈라짐 문턱 6.0 을 넘는다.** 반지하 가로창 하나만 바꿨다 —
  같은 0.55 를 쓰던 욕실·다락·루버창 넷은 그대로다.
  프로파일과 `test_banjiha_profile` 의 LIVE 골든 값을 브라우저에서 다시 재서 갱신했다.
  열네 칸이 **전부 정확히 1.2727배**(=0.70/0.55)인 것이 그 갱신의 증거다.
- ✅ **㉡ 계절 달력을 하나로.** 화면은 연중 135일(여름 45일차)에서 시작하는데 빛은
  `seasonOf(S.day)` 로 0일을 봄 0일로 봤다. `S.sim.yearDay0` 한 줄로 맞췄다 —
  **빛에게 튜토리얼을 가르치지 않는다.** 오프셋 정본은 `tutorial.yearDay0Of()` 하나다.
- ✅ **㉢ 밝기가 속도도 바꾼다.** 밝은 자리는 달력 하루에 **1.25 걸음**을 걷는다.
  ★ 전역 생장 곡선은 한 글자도 안 건드렸다 — 곡선은 그대로고 **그 위를 걷는 속도**만 바뀐다.
  ★ 워커가 "slow 를 내리자"는 내 권고를 **재 보고 뒤집었다**: 내리면 세이브 재생이 깨지고
    (저장 53일 → 복원 57일) A 경로가 78% → 21% 로 죽는다(반지하 창턱이 곧 slow 밴드다).
    그래서 **기준선을 slow 에 두고 올리기만 한다.**
  ⇒ 세 경로가 처음으로 갈렸다: `A 189일 잎4 (83%)` vs `B 161일 잎4 (100%)`. **A 는 안 바뀌었다.**
- ✅ **㉣ 무순은 콩나물 3회전 뒤에 나온다.** 문턱 값을 새로 안 지었다 —
  몬스테라 도착 조건을 그대로 쓴다. **금액은 안 건드렸다**(아래 발견).

### ㉮ 무순 판 0.20m (박사님 확정)
GLB 원본 대각선 0.4327 로는 반지하 14칸 중 **책상 2칸**만 받았고 그 둘은 어두웠다.
무순은 밝아야 좋은데 제일 밝은 창턱(4.80)이 0.21m 다. **0.20 = 격자 4칸**으로 줄여
14칸 전부에 올라간다. 에셋을 새로 안 만들었다 — 그릴 때 줄인다.
⚠ 무순 포기는 판을 따라 안 줄인다(대 단계 4.4cm → 7mm 가 되어 인형 소품이 된다).
  **제 칸을 넘칠 때만** 칸에 맞춘다 — 상수를 새로 안 지었다, 칸 간격이 자를 정한다.

발견 (2026-08-05):
- ★★ **무순의 값어치는 제 수확금이 아니라 「하루 천장을 올리는 것」이다.**
  내가 처음에 "무순이 값에서 밀린다(229원/일 vs 콩나물 500원/일)"고 했는데 **틀렸다** —
  한 판만 떼어 본 값이었다. 끼니 상한을 넣고 보면:
  | 들인 종류 | 하루 꺼낼 상한 |
  |---|---|
  | 콩나물만 | 3,000원 |
  | 콩나물+무순 | **5,000원** |
  | 3종째 | 5,000원 (끼니 상한이 이긴다) |
  같은 날 두 개를 거두면 곳간에 들어오는 돈은 둘 다 5,000원인데, **1종이면 3,000원만 쓰고
  2,000원이 곳간에 남는다.** 순수익 2,500 → 4,100원/일. ⇒ 표를 안 고친 이유가 이것이다.
- ★ **세이브 거짓 경고를 고쳤다** — 밝은 날은 `dliHist` 가 하루 두 칸인데 교차검증이
  `daysPlanted`(하루 1)와 견주고 있어 멀쩡한 판이 「깨진 턴」으로 잡혔다.
  **재생은 정확했고 재는 자가 틀렸다.** `p.fedDays` 를 따로 세어 그쪽을 검사한다.

대기:
- **워커** — 튜토 219일을 줄이는 길과 그 대가를 재는 중(`tools/probe_tutorial_length.mjs`)

판단필요 (남은 것):
- ★★ **튜토가 통째로 219일이다**(`balance_decisions §D` 목표 80일의 2.7배).
  `test_balance_routes` 의 ①-2·③-b 가 그것 하나를 두 번 찍는 것이다.
  줄이려면 **어두운 자리도 같이 빨라져야** 하는데, 그건 「자리가 결과를 바꾼다」를
  약하게 만드는 결정이라 이 창이 못 정한다. 워커가 후보별 대가를 재고 있다
- ㉤ 완주 뒤 [빨리감기]가 한 번에 며칠을 감을지 (지금은 배속 상한 30일)
- ㉥ 첫 플레이 중 [몬스테라 팔기]를 아예 막을지 (지금은 되묻기만 한다)
- ㉦ `good`·`over` 밴드 속도는 **못 쟀다** — 반지하가 그 밴드를 안 지난다.
  `good` 만 `best` 와 같게 뒀다(1.0 이면 "밝을수록 느려지는 구간"을 새로 만드는 셈이라서).
  원룸·아파트 때 재서 채울 자리다
- ★ 첫 플레이 동안 **놓을 자리 세 색을 숨겼다.** 놓기 전에 초록이 켜지면
  「자리가 결과를 바꾼다」를 결과가 아니라 색으로 배우게 된다 —
  `fillSlots` 가 정답순·원시 DLI 를 숨기는 것과 같은 판단이다. 튜토에서도 보이길
  원하시면 `rankSlots` 의 한 줄만 지우면 됩니다

---

### 2026-08-02 (이전)
갱신: 2026-08-02 (첫 플레이 계약 정정 `ec0fe77`)
완료:
- **★ 다섯 창 대기 전부 해소** — growth 6건 · house 4건 · core 4건 · leaf 2건 · char 2건 답변
- **자취생 경제 재계산** — 세 창 발견 반영. 결론이 뒤집혔다(아래 발견)
- **`docs/hidden_depth.md` 신설** — 숨겨진 공략 14건 + 「몰라도 되나」 판정
- `acceptance.json` 갱신 — `fenWeekPct` 합격선 60% · 개체 소질 2층 값

발견:
- ★★ **자취생 경제가 "불가능"에서 "자본 부족"으로 바뀌었다.**
  선반 6개(관엽 29칸)에서 흑자 전환(+2,348원/일). 전엔 13칸이 천장이라 아예 안 됐다.
  다만 29그루 증식에 80일 · 필요자본 112~158만 · 시작 100만 → **12~58만 부족.**
  **월세 첫 달 유예(30만) 하나면 성립한다.** 상세는 `game_flow.md` 부록
- ★ **벽 선반이 "비밀"이 아니라 "벽"이다** — 선반 0개면 −9,118원/일로 구조적 적자.
  house에 **반지하 기본 가구로 선반 1개**를 요청했다(0개=존재를 모름 / 6개=발견이 없음)
- ★ **개체 소질 2층 구조 값 확정** — `base 0.10` × 등급배율(plain 0 / normal 1.0 /
  potential 2.5 / elite 6.0), 계열은 **`MAT_TREE` 주 갈래 10그룹**.
  첫 잎 무늬율이 plain 0% ↔ elite 100%라 **실생 선발이 성립**하고,
  논문 1편이 **33~44일**로 목표(30~45턴)에 든다. growth가 붙이면 연구자 트랙이 코드로 성립
- **갈라짐 합격선 = 문턱 넘는 주 ≥ 60%.** 경계(20~60%)는 **실패로 본다** —
  40%짜리 방은 밸런싱이 우연에 걸린 것이다(아파트 마진 0.3% 사고가 그 유형)
- **계절별 최적 배치는 지금 안 바뀐다** — 천공이 방향 무관이라 모든 자리가 같은 계수로
  어두워진다. ★온도가 들어오면 바뀐다(창가는 겨울에 춥다). 그때 의도적으로 만들 값어치가 있다

대기:
- ~~house — 반지하 창 높이 슬롯~~ → ✅ **해소** `banjiha-sill:0` peak_summer **3.77**
- **house** — 방별 목표 활용률 배치 · `measured.avg7` · `grid`/`furnished`
- **growth** — 개체 소질 2층 반영 · `matRare` 과광 조건부 · `growth_tuning.json` 이동
- **leaf** — 물꽂이 용기 슬롯 수 (증식 단계가 슬롯을 얼마나 먹나)
- **사용자 직접 플레이** — 최소 루프 구현·기계 검증은 완료. 화면 가독성·재미·학습 확인 뒤에만 ③경제로 넘어간다

판단필요:
- **자취생 지출을 13,000원/일로 어떻게 내릴지** — 월세↓ / 식비↓ / 콩나물 상한↑ 중 선택.
  월세 첫 달 유예(서사 장치)를 권한다. 시작 자금·슬롯은 캐릭터 정체성에 직접 닿는다
- **포토스 마디 주기 확정** (10 / 7 / 5일) — 생물학상 **7일이 현실적**. 자본 요구가 138→120만으로 내려간다
- **선반 값 3만원 · 선반당 3칸은 임의값** — house·leaf와 맞춰야 한다
- ⏸ 첫 플레이 이후 검토 — 스토리 모드 전용 고정 외부광 프로필. 상세: plan-to-house.md의 [잠재 설정] 스토리 모드 전용 고정 외부광 프로필
- ⏸ 첫 통합 시뮬 이후 검토 — 계절 진행·식물등·반지하 튜토리얼 종료(원룸 이사). 상세: time_modes.md §9 · first_play.md §8
- ✅ 첫 플레이 초기 진행도 **143 확정** · 슬롯 `banjiha-sill:0` 3.77. 형태 진행도/활력 **별도 축**으로 확정(novice: 형태 게이지 보임·감소 없음 / 활력 숨김). 상세: first_play.md §3·§5·§9
- ✅ **게이지 표시 사상 = A(원비율) 확정.** `spear_ready` 진입 0% → 단계 내부 진행 그대로 0~100% · 저광에선 정지(감소 없음) · 단계 완료 시 다음 phase 0%부터. 사상 근거가 아닌 것: 생존 여유 · `daysPlanted` · 절대 `growthDays`. 상세: first_play.md §9

## lampaim
갱신: 2026-08-06 (식물등 겨누기·꼬리 — `docs/growlight_aim.md` §2 §3 §6-1~3)
완료:
- 식물등을 **겨눌 수 있게** 했다 — `yaw`·`tilt` 로 빛의 축이 움직인다.
  집게 ±180°/0~75° · 스탠드 ±180°/0~60° · **바 등은 못 돌린다(던진다)**
- `t>1` 꼬리를 멱함수 `0.55·t^-1.636` 로 바꿨다 — 옛 `t=1.15` 절벽 제거, t=1 에서 C¹ 연속
- 세이브에 `lamps.aim` 추가. **옛 세이브는 「안 겨눔」으로 열린다**
- 검사 `tools/test_lampaim.mjs` 14항목 신규. 전체 29개 통과
  (`test_balance_routes` 내부 2건은 작업 전과 똑같이 실패 — 이 작업 것이 아니다)
- 상세: `docs/handoff/lampaim-to-plan.md`

발견:
- ★ **회귀는 비트 단위로 지켜졌다** — 6개 방 전 슬롯 × 등 개수 **696개 값이 전부 동일**.
  창턱 `4.80 / 6.64` 그대로. 프로파일 파일은 안 건드렸고 `live_vs_static` 오차 **0.00000**
- ★ **꼬리는 지금 게임에서 한 번도 안 불린다.** `off ≤ dist` 라 `t ≤ ref/r` 이 상한인데
  아홉 기구 전부 1 미만이다(최대 0.857 = 집게등). 옛 `t=1.15` 절벽도 **도달 불가능한 죽은 코드**였다.
  ⇒ 문서 §1 의 "범인은 감쇠 곡선이 아니라 거리다"가 곡선 쪽에서도 확인됐다.
  실제로 빛을 옮기는 것은 꼬리가 아니라 **겨누기**다
- ★ **`banjiha-sill:0` 이 두 식물등보다 0.52m 위에 있다.** 옛 식이 `Math.abs(dy)` 라
  등 위쪽도 아래와 똑같이 비췄고, **창턱 등 PPFD 42.62 가 전부 거기서 나온다**
- 바 등 발광점이 mount 보다 4.6cm 위다(`house.js` 가 바닥 스탠드용 식을
  선반 밑 부착에도 쓴다). 창턱과는 0.52m 차이라 위 판단엔 영향 없다 — 별건

판단필요:
- ★ **「뒤쪽을 안 비춘다」를 어디까지 걸 것인가.**
  안 겨눈 등에까지 걸면 창턱 DLI 가 **6.64 → 4.80** 으로 내려앉아 갈라짐 문턱 6.0 을 못 넘는다
  = 어젯밤 확정한 창 `tau` 0.70 의 근거가 사라진다.
  지금은 **겨눈 등에만** 걸어 두었다(합격선 유지). 대가는 "똑바로 아래로 겨누기"가
  "안 겨눔"과 한 칸에서 다른 것(창턱 6.78 → 6.64).
  선택지 셋과 숫자는 `lampaim-to-plan.md` §6

## lampswitch
갱신: 2026-08-08 (등을 손으로 켜고 끈다 · 켠 시간을 센다 · 켜면 살짝 밝아진다)
완료:
- **방에서 등을 누르면 켜지고 꺼진다.** `resolveTap` 에 등을 넣었다(화분 뒤·퍼지 앞).
  누르는 순간 방뷰가 먼저 켜고 끄고, 호스트에는 `onLampTap(uid, on, state)` 로 알린다
- **켠 시간을 센다** — `setDaylight` 이 올 때마다 게임 시각으로 적산한다(실제 초가 아니다).
  `lampSwitches()` 가 등마다 `hours`·`wh` 를, 합계로 `wh`·`growLitHours`·`growOn` 을 낸다.
  하루는 `resetLampHours()` 로 닫는다
- **등을 켜면 화면이 실제로 밝아진다** — 등 아래 밤 **+89%** · 낮 **+21~51%**,
  화면 전체는 **+1.4~3.4%** 뿐이다(탄 픽셀 0.65% 그대로 = 방이 안 하얘진다)
- 검사 `tools/test_lampswitch.mjs` **38건 신규 전부 통과** · 측정 `tools/probe_lampswitch.mjs` 신규
- `room_view.js` 하나만 고쳤다. `game.html`·`loop.js`·`state.js`·`data/**` 는 안 건드렸다
- 상세: `docs/handoff/lampswitch-to-plan.md`

발견:
- ★★ **식물등은 켜고 끄는 손이 아예 없었고 화면에서는 24시간 켜져 있었다.**
  그리기 조건이 `r.schedule !== 'off'` 인데 그 값은 `house_rooms.json` 의 `photo12`·`photo16` 이라
  **언제나 참**이다. 「밤에만 자동」은 생활등 쪽 규칙이었다
- ★★ **`setGrowLights(n)` 은 기구 메시만 숨겼다.** 광원 PointLight 는 `house.js:828` 이
  개별 가구 그룹이 아니라 `furnGroup`(방 전체 컨테이너)에 넣기 때문에 **안 산 등이 계속 방을 밝혔다**
- ★ 그 둘이 겹쳐서 **등 2개를 켜도 화면 평균 밝기가 75.23 → 75.26(+0.0%)** 이었다.
  같은 조작으로 DLI 는 `etagere:7` 에서 **0.48 → 12.41** 로 뛴다.
  박사님 "숫자만 바뀌고 눈에 잘 안 든다"의 정체가 이것이다
- ★ **조도(DLI)는 한 자리도 안 움직였다** — 추천 자리 14칸이 변경 전후·손으로 껐다 켠 뒤까지
  소수 넷째 자리로 동일(검사 F-2·F-3). 그림과 계산은 갈라져 있다
- ★ 프리셋 와트로 세면 하루 전기세가 **바만 12h 38원 · 바+집게 12h 61원 · 24h 123원**이다.
  지금 코드(`tutorial.lampWatt` 12W 고정)는 각각 23·46·92원 — **와트가 두 벌이다**

대기:
- 코어(`light_adapter.rigsOn` · `state.lamps.on` · `loop.js` 전기세 차감) — 상세 §5
- 조정 창(`game.html`) — 붙일 코드는 §4 에 그대로 적어 두었다

판단필요:
- ★ **끄면 요금이 주는데 끄면 식물도 어두워져야 하는가.** 지금 계약(`rigsOn` = 앞에서부터 n개)은
  **어느 등을 껐는지를 담을 칸이 없다.** 개수만 맞추면(㉡, 오늘 붙일 수 있다) 「둘째 등만 켜기」가
  엔진에서는 첫 등으로 읽힌다. 제대로 하려면 코어가 uid 집합을 받아야 한다(㉠). 셋 다 §7-①
- ★ **전기세가 밸런스 손잡이가 되는지는 아직 안 풀렸다.** `lampecon-to-plan.md` §3~4 가 이미
  단가 60배로도 이사일이 하루도 안 움직인다고 쟀다 — 원인은 **0원인 날의 지출을 clamp 가 삼키는 것**이다.
  켠 시간으로 매겨도 그 사실은 안 바뀐다. 손잡이로 쓰시려면 clamp 를 어떻게 할지가 먼저다
- 밝기를 더 올릴 여지는 있다(세기 1.0 까지 탄 픽셀이 안 는다). 지금은 「살짝」에 맞춰 0.34 다

## econ (전기세 · 살림 재조정)
갱신: 2026-08-09

완료(2026-08-09 밤 — **세 층 시뮬**):
- **`tools/probe_three_layers.mjs` 신설.** 채소·몬스테라 증식·가구 **세 층을 다 켜고** 400일을 쟀다.
  자르기·분갈이·모주 물주기가 체력을 쓰게 해서 **손 다툼**을 처음으로 재현했다. 표는 `econ-to-plan.md §7.12`.
- 박사님 새 지시 둘을 반영했다: **최대체력 상한 폐지**(10 위로 「최대체력×10」마다 +1, 끝 없음) ·
  **「채소만으로 어거지 이사」** 경로(회전분·판매가율·공과금·무순을 축별로 스윕).

발견(★다른 창이 알아야 할 것):
- ★★ **하루 곳간 상한은 「작물 종류 수」로 안 오른다.** `dailyCropSaveWon =
  min(cropSavedWonPerCycle, 끼니상한)` 인데 `cropSavedWonPerCycle` 이 **`CROP_KINDS` 표에
  정의된 종류 수**(실제로 심은 것이 아니다)로 셈해서, **콩나물만 심어도 이미 5,000원**이다.
  ⇒ **무순은 천장을 안 올리고 손만 가져간다 — 넣으면 손해다.**
- ★★ **반지하에서 등이 없으면 몬스테라가 한 걸음도 안 자란다**(창가 DLI 1.52 < 최소 3).
  증식 층의 스위치는 자르기가 아니라 **식물등**이고 등은 가을(정본 45일째)에 열린다.
- ★★ **증식은 하루 230원**이다(월세+공과금 12,500원의 1.8%). 400일에 삽수를 **두 개**밖에 못 뜬다 —
  모주가 새 잎을 **50일에 한 장** 내고, 잘라낸 잎은 영영 안 돌아오기 때문이다.
- ★★ **공략은 「채소 우선」**이다. 증식이 하루에 쓰는 손 2개가 초반 체력 5의 40%라 복리로 손해다.
- ★★ **이사일은 배분 정책과 무관하게 300일**이다 — 시계가 돈이 아니라 **확정 무늬 12일 박자**다.
- ⚠ **자리 제약이 코드에 사실상 없다.** 시루 140개가 놓였다(방은 14칸). 선반(가구)이 값을 가지려면
  먼저 자리 한도가 걸려야 한다.
- ⚠ **하루 탭 수가 42~50까지 간다**(장기 판 · 누적 3만 4천). 「체력=탭 수」와 「일괄 버튼 폐지」가
  정면으로 부딪힌다.

이어서(⏸ 미완 · §7.13):
- **삽수를 팔지 말고 키워 모주로 쓰는 길**에 착수했으나 **결론을 못 냈다.** 「복리가 안 붙는다」로
  읽으면 안 된다 — 못 재고 멈춘 것이다.
- ★ 확정된 것: **등 1개로 몬스테라가 자라는 반지하 자리는 넷뿐**(`etagere:6·7·8` + 창가)이고
  창가는 원래 모주가 쓴다 ⇒ **모주 수의 상한은 손도 돈도 아니라 「밝은 자리 수」**다.
  등을 2개로 늘려도 그 자리들의 ppfd 가 거의 안 오른다(273.7 → 276.1) — **등이 아니라 가구가 푼다.**
  ⇒ **선반이 여기서 처음으로 진짜 값을 갖는다**(시루 자리가 아니라 **모주 자리**로서).
  ⚠ 선반이 늘리는 칸이 **밝은 칸인지**는 house 창이 정해야 한다.
- ★ 함정 둘(게임에서도 똑같이 걸린다): **뿌리내린 그날 판매가 삽수를 먼저 데려간다**(자를 때
  「모주로 쓴다」고 찍어 둬야 한다) · **검은 모종포트는 배송 2일**(미리 시켜야 한다).
- ⚠ **다음 사람이 볼 곳** — `loop.cuttingLightOf` 가 `io.light.dliOfSlot(c, {})` 로 부르는데
  **등 개수를 안 넘긴다.** 삽수를 밝은 자리(DLI 11.8)에 세웠는데도 안 자랐다.
  **엔진 배선인지 하네스인지 못 갈랐다.** 여기가 안 풀리면 증식 복리는 아예 안 열린다.

완료(2026-08-09 밤 늦게 — **버그 고친 뒤 다시 쟀다 · §7.14**):
- `f5e0bdb`(삽수 빛) 뒤에 §7.12·§7.13 을 **다시 쟀다.** 모주 정책 셋(㉠ 전부 판다 / ㉡ 처음 N개 /
  ㉢ M그루까지)과 **몬스테라 유지비**를 새로 넣었다(`probe_three_layers.mjs --only G · --only H`).
  자리는 **DLI ≥ 3 인 넷으로 잘라서** 쟀다 — 있지도 않은 자리에 모주를 세우지 않게.

발견(★다른 창이 알아야 할 것):
- ★★ **§7.13-③ 「모주가 안 자랐다」의 원인을 밝혔다 — 둘 다 하네스였다.**
  ① `repotCutting(S,c,{at:'자리이름'})` — `makeAt` 은 **좌표만** 받아서 던진다
  ② `takeCutting({motherCuttingId})` 에 **`nodeId` 를 안 넘겼다** — 매일 조용히 던지고 있었다
- ★★ ⚠ **코어가 볼 것: `repotCutting` 은 `potted=true`·`established` 를 먼저 찍고 자리를 나중에 준다.**
  자리 주기가 던지면 **「흙에 섰는데 자리 없는」 삽수**가 남고, 빛을 잴 데가 없어 **영영 안 자란다.**
  오류도 안 뜬다. 그리고 **삽수만 자리 이름 문자열을 못 받는다**(화분·몬스테라는 받는다).
- ★★ **증식은 하루 1,354원**이다(모주 4그루가 다 선 뒤). 230원에서 **여섯 배**지만 월세+공과금
  12,500원의 **10.8%** 다. 천장의 정체 = **잎 32일에 한 장 × 삽수 하한 12,000원 = 모주 한 그루당
  하루 375원** × 밝은 자리 넷. ⇒ **12,500원에 닿으려면 모주가 약 33그루**라야 한다(유도값).
- ★★ **㉡ 과 ㉢ 이 같은 값을 낸다.** 정책의 모양이 아니라 **모주 수**만이 값을 정하고 **선형**이다
  (1·2·3·4그루 = 231·570·951·1,354원). **밝은 칸 하나 = 하루 375원** — 선반 값의 새 기준이다.
- ★★ **몬스테라 유지비가 거의 0 이다** — 모주 1~4그루에 **하루 0.16~0.27 손**(콩나물 20시루는 12 손).
  **물주기는 원래 화분 하나에만 들고 모주가 늘어도 안 는다**(삽수-모주에는 가뭄이 없다).
  ⇒ 「체력이 많이 들게」 하시려면 **물주기가 모주 수를 따라가게** 하는 것이 유일하게 듣는 손잡이다.
- ★ **돈은 그대로 62일째 무너지고 이사일도 그대로 300일**이다. 증식이 여섯 배가 돼도 안 움직인다 —
  증식이 도는 것은 **146일째부터**인데 현금은 **62일째**에 마른다.
- ★ **잎 한 장씩 자르는 것이 20% 이득**이다(삽수 하한 12,000원 > 잎당 10,000원). 틈인지 의도인지 판단 필요.

판단필요: `econ-to-plan.md §7.12` 의 다섯(㉠~㉤) + **§7.14 의 다섯**(repot 순서 · 삽수 배치 입력 ·
밝은 자리를 늘릴지 · 삽수 하한 12,000원 · 몬스테라 유지비를 올릴지).

완료:
- **전기세 정본을 한 벌로 모았다.** `TUTORIAL_RULES.lampWatt = 12` 고정값을 버리고
  등을 산 순서대로의 와트 표(`lampWattsByOrder: [20, 12]`)로 바꿨다 — 기구 프리셋·방 프로파일과
  같은 값이 됐다. 숫자는 `data/balance/electricity.json`(신설)이 갖고, 누진(`tariffTiers`) 셈도
  넣어 뒀다(지금은 `null` 이라 예전과 똑같이 돈다).
- **살림 값을 다시 짰다**(박사님 확정): 시작돈 130만 · 월세 30만(30일) · **첫 달 유예 폐지**
  (`rentGraceDays` → `rentFirstDueDay: 1`) · 하루 지출 20,000원. 월세·하루지출은 이제
  `banjihaRulesFrom(homes)` 로 `data/balance/homes.json` 에서 유도할 수 있다.
- 월세 예고(`rent_soon`)를 **달마다** 뜨게 살리고, `rentFirst` 대사를 「돈 설명」 자리로 다시 썼다.
- 잉여 판매가율의 정본을 `data/balance/characters.json._meta.cropSurplusSaleRate` 로 옮겼다(값은 0.70 그대로).
- 새 검사 `tools/test_elec.mjs` — **코드 기본값과 정본 JSON, 기구 프리셋, 방 프로파일이 같은지**를 고정한다.
- 새 측정 `tools/probe_elec.mjs` · `tools/probe_econ.mjs`.

발견:
- ★★ **와트가 두 벌이었다.** 등 2개 24시간이 화면은 123원인데 지갑에서는 **92원**이 빠졌다.
  `lighting_presets`(바 20W·집게 12W)와 `room_profile.banjiha`(`lampWatts [0,20,32]`)는 맞았고
  `TUTORIAL_RULES` 만 12W 하나로 뭉개고 있었다. 지금은 셋 다 같다.
- ★★ **「0원인 날의 지출을 clamp 가 삼킨다」는 낡은 기록이 아니다 — 지금도 맞다.**
  clamp 가 **둘**이라 헷갈린 것이다: `Math.max(0, base − saved) + power`(전기세는 처음부터 밖에 있었다.
  `git log -L` 로 확인)와 `if (ts.cashWon < 0) ts.cashWon = 0`(이쪽이 삼키는 그것).
  실측 — 전기세 명목 14,222원 중 **실제로 낸 것은 2,906원(80%가 삼켜졌다)**. 단가를 10배로
  올려도 82%가 삼켜진다. ⇒ `lampswitch` 섹션의 그 줄은 **그대로 두는 것이 맞다.**
- ★ **전기세 후보 여섯 중 어느 것도 이사일을 하루도 못 움직였다**(189/161 고정, 등 이득비 ×1.17 고정).
  단가 ×10 · 실제 식물등 와트 · 누진 전부. 하루 지출은 이 판에서 구조적으로 이사일에 안 닿는다.
- ★★ **「채소를 다량으로 하면 월세+식대 유지」는 지금 값으로 성립하지 않는다.**
  시루를 1 → 40개로 늘려도 하루 순현금이 −19,673 → −9,482 다(필요한 것은 0).
  0 이 되는 지점은 **시루 77개**이고 반지하는 **14칸**이다.
- ★★ **다만 「천장에 막혀서」가 아니라 「기울기가 완만해서」다.** 한 개당 이득이 시루 3~40개
  내내 **+241~257원/일로 완전히 선형**이고 꺾이는 데가 없다. 처방이 정반대다 —
  상한을 푸는 게 아니라 **회전분·판매가율·주기를 올려야** 한다. 손잡이 표는 `econ-to-plan.md §7.5`.
  현실감 안에서 「식대만 유지」는 **회전분 4,500원 + 판매가 85% → 12칸**으로 닿고,
  「월세+식대 유지」는 **어느 조합으로도 못 닿는다.**
- ⚠⚠ **처음 낸 표(−19,740 → −17,569 · 72개 · 「천장에 막혔다」)는 틀렸다.**
  `probe_econ.mjs` 가 **달력일로 나눴는데** 살림은 튜토 시계가 돌 때만 나간다. 시루가 많을수록
  첫 플레이가 길어져 **같은 달력 90일 안의 튜토일이 90 → 68 로 줄었고**, 그래서 이득이 짧은
  기간에 눌려 담겨 곡선이 납작해 보였다. **재는 자가 틀린 것**이다. 도구는 고쳤다
  (`play({ tutorialDays })`) 고 그 함정을 파일 머리말에 적어 두었다.
- ★★ **끼니 상한을 2끼 → 8끼로 올려도 값이 한 푼도 안 바뀐다.** 곳간 상한은
  `min(한 회전분 합계 5,000, 끼니상한)` 인데 **이기고 있는 것은 회전분 합계**이고,
  그 표(`cropKindSavedWon [3,000·2,000·1,000]`)는 `first_play.js` 안에 있어 `data/balance` 에서 못 고친다.
  ⇒ **거기부터 옮겨야 손잡이가 생긴다.** 붙일 코드는 `econ-to-plan.md §7`.
  ★ 다만 천장의 뜻은 좁다 — **「밥」 쪽만 하루 1,200원에 막혀 있고**(식비절감이 시루 3~40개 내내
  108,000원/90일로 고정. 곳간이 넘쳐 쉰 몫은 모든 후보에서 0원이라 5,000원 상한은 안 닿는다),
  **「잉여 현금」 쪽에는 천장이 없다.**
- ★ **증식체(삽수) 판매는 막혀 있지 않다.** 다만 이사 자금의 **0.4%(12,000원)** 뿐이다 —
  돈이 아니라 **확정 무늬를 여는 열쇠**로 일한다. 이사 자금의 58.5%는 모주 한 번(1,830,000원)이다.
- ⚠ **「시작 첫날」이 어디인지가 아직 안 정해졌다.** 튜토 시계는 첫 플레이(돈이 멈춘 16일)가
  끝나야 돌므로, 지금 「1일차 월세」는 **게임 17일쯤**이다. 게임 첫 [다음 날]로 당기려면
  `first_play.js`(코어)가 살림을 시작해야 한다.
- ⚠ 커밋 `1da8e99` 메시지의 *"시루 하나가 14,000원"* 은 틀렸다 — **7,000원**이다
  (`buyPriceOf('siru')`). 인계 문서의 *"시루 2개(14,000원)"* 는 맞다.
- ⚠ `tools/probe_lamp_econ.mjs`(2026-08-06)는 지금 **아무것도 못 잰다.** 물주기가 규칙이 되면서
  `waterPot(S)` 이 빠진 하네스는 화분이 말라 첫 플레이가 안 끝나고 튜토 시계가 시작조차 안 한다.

대기:
- 코어 — `loop.js` 전기세 장부 통합(`elec-to-plan.md §4` 에 붙일 코드) · `ui.js` 「차감 없음」 문구
- 코어 — 회전분 표를 `data/balance` 로(`econ-to-plan.md §7` 에 붙일 코드)
- house — 기구 와트를 실제 식물등 수준으로 올릴지(`lighting_presets.json`) · 시루 20~30개를 놓을 자리

판단필요:
- ★ **전기세를 무엇으로 쓰실 것인가.** 후보 어느 것도 이사일을 안 움직이므로 고르실 것은
  「얼마나 아프게」가 아니라 「배움이냐 손잡이냐」다. `elec-to-plan.md §5` 에 셋으로 냈다
  (권하는 것은 실제 와트 + 누진 — 세서가 아니라 **거짓말을 안 해서**)
- ★★ **0원 clamp 를 어떻게 하시겠습니까.** 이것 때문에 하루 지출로 밸런스를 잡는 길이
  통째로 막혀 있다. 후보 셋은 `elec-to-plan.md §5`
- ★★ **「채소로 유지」의 목표가 무엇입니까** — 월세+식대(20,000원/일·시루 72개) /
  **식대만**(7,500원/일·시루 22~27개) / 하루 현금차감(10,000원/일·시루 28~36개).
  `econ-to-plan.md §8`
- ★ **잉여 판매가율** — 70%(시루값 5회전·25일 회수) / 85%(4회전·20일) / 50%(9회전·45일).
  ⚠ **1.00 을 넘기면 「밥으로 먹는 것보다 파는 게 낫다」가 되어 뼈대가 뒤집힌다**
- ★ **「시작 첫날」이 첫 플레이 첫날입니까, 그 뒤입니까**(`econ-to-plan.md §1`)

---

## siru (시루 각개)
갱신: 2026-08-09
완료:
- **콩나물 시루가 「무리」에서 「하나하나의 물건」이 됐다.** 자리(`slotId`·`at`)의 정본이
  자리(site)에서 **시루(pot)로 내려왔다.** 자리 사본은 대표 시루를 비추는 읽기용으로 남는다
- **가방에는 빈 용기만.** 「열린 콩나물 시루 · 1/5일 자람」이 가방에 뜨던 것이 사라졌다.
  끌면 **하나**가 서고 나머지는 가방에 남는다(예전엔 가방의 시루가 전부 한 자리에 쏟아졌다)
- **각개 물주기·수확·다시심기.** 방 말풍선이 시루마다 뜨고 눌리는 것도 그 시루 하나다
  (「💧 물 주기 3개」가 사라졌다). [식물] 탭에 시루마다 한 줄 + 게이지
- **게이지는 표시일 뿐이다** — 생장은 그대로 하루 단위이고, 하루 안의 흐름은 연한 색으로만 그린다
발견:
- ★ **자유 좌표 열쇠가 바뀌었다**: `free:crop_01` → `free:crop_01_01`(시루 id). 안 바꾸면
  계약(`light_adapter.slotsFor` 의 Map)에서 뒤엣것이 앞엣것을 덮어 시루마다의 DLI 를 못 찾는다.
  **옛 세이브의 `free:crop_01` 은 그대로 산다**
- ★ **같은 날 품질이 갈린다.** 시루를 어두운 데와 밝은 데에 나눠 놓으면 결과가 다르다 —
  「자리가 결과를 바꾼다」가 콩나물에서도 손에 잡히게 됐다
- ★ `oneroom.js` 두 함수(`clearPlacements`·`reseatCrops`)를 시루마다 돌게 고쳤다. 안 고치면
  이사한 판이 떠나온 방의 좌표를 들고 와 다음 하루에 던진다(게임이 통째로 멈춘다)
- 조도 엔진(`light_adapter`·`data/profiles`·`daily_light`)은 **한 줄도 안 건드렸다**
대기:
- plan — 아래 판단 둘
판단필요:
- ★★ **각개 수확의 체력값.** `harvestCrop` 은 부를 때마다 손 1을 쓴다. 시루 5개를 각개로
  거두면 **손 5**다(예전 [모두 거두기] 는 1). 하루 손이 10인 판에서 살림이 바뀌는 값이다.
  선택지 셋은 `siru-to-plan.md §7 ①`
- ★ **「익은 콩나물 모두 거두기」 버튼을 남길까.** 지금은 남겨 뒀고 방 말풍선이 보일 때는
  안 뜬다(일괄은 「방이 안 보일 때의 뒷길」). 없앨지 정해 주십시오 — `siru-to-plan.md §7 ②`
- 폰 배치 방식(눌러서 배치 → 상대이동 → [확인])은 **안 했다.** 설계와 밟지 말아야 할 지뢰는
  `siru-to-plan.md §8`. 붙일 부품은 이미 다 있다(가구 메뉴의 자리잡이·상대이동 모드)

### 2차 (2026-08-09)
완료:
- **[빨리감기]를 화면에서 걷어냈다**(박사님: *"빨리감기는 없애. 하루넘기기만 살리자."*).
  하루를 넘기는 손은 [다음 날] 하나다
- **폰 배치** — [방에 배치하기] → 임시로 서고 곳바로 **이동 상태** → 상대이동 드래그 →
  놓으면 그 옆에 **[확인]**. 끌어놓기도 그대로 남는다(PC 용)
- **놓은 것이 길을 막는다** — 그 사실이 없어서 경고할 근거 자체가 없었다.
  이제 `roomView.reach()` · `unreachable()` 이 답하고, [확인] 바가 말로 알린다. **막지는 않는다**
발견:
- ★ **빨리감기 코어는 안 지웠다**(`loop.startFastForward`). 헤드리스 재현·밸런스 측정이
  그 길로 돌고(`test_fastforward` 20항목), `test_balance_routes`·`test_oneroom`·
  `probe_tutorial_length` 이 상수를 빌려 쓴다. 화면이 안 부르므로 게임에는 영향이 없다
- ★ **「배속 상태로 저장된 옆 판」은 애초에 없다**(재서 확인). 시간 모드는 저장되는 칸이
  아니라 `timeModeOf(S)` 가 `firstPlay.enabled`·`completed` 에서 매번 유도한다
- ★ `placeFurnMenu` 를 `placeFloatMenu(el, p)` 로 **일반화**해 가구 메뉴와 [확인] 바가 같이 쓴다
판단필요:
- ★ **`startFastForward` 와 `test_fastforward` 20항목을 어떻게 할까** — 그대로 둘까 ·
  「헤드리스 전용」이라고 문서에 박고 둘까 · 검사째 지울까. `siru-to-plan.md §13 ①`
- **[30일 자동](`#run30`)도 없앨까** — 검수 탭의 개발자 도구다. 지시는 「빨리감기」였다

### 3차 (2026-08-09)
완료:
- **체력·경험치를 붙였다** — 시작 체력 **5** · 물/수확/심기가 **시루마다 1** ·
  **경험치 = 그날 쓴 체력** · 차면 그 자리에서 최대체력이 오른다(남은 손도 같이).
  숫자는 **새 파일 `data/balance/stamina.json`** 이 갖는다(코드에 안 박았다)
- **최대체력에 상한이 없다** — `maxCap: null`. 큰 수로 흉내 내지 않았다.
  10 위는 `최대체력 × 10`(11→110, 30→300 … 끝없음)
- **일괄 경로를 화면에서 걷었다** — [모두 거두기]·[모두 심기]가 **한 번에 한 시루**다.
  ⚠ 코어(`waterBeansprout(all)`·`harvest/resow(potIds 없이 = 전부)`)는 **그대로 있고
  화면이 부르는 곳은 0곳**이다 — 자동 급수 장치가 정해지면 다시 부르기만 하면 된다
- **★★ 삽수가 식물등 빛을 아예 못 받던 버그를 고쳤다**(§22) — `dliOfSlot(c, {})` 였다.
  등 밑 선반 **0.48 → 12.31 (25.6배)**. 화분 DLI 는 한 자리도 안 움직였다
발견:
- ★★ **씨앗값 정본이 두 벌이다**: `first_play.CROP_KINDS[0].seedWonPerPot`(500) 와
  `shop.CATALOG.bean_seed.listWon`(500). **둘 다 코드에 박혀 있고 `data/balance/` 에 없다.**
  500 → 100 으로 내리실 때 **두 곳을 같이** 고쳐야 한다 — `siru-to-plan.md §20`
- ★ **자리가 진짜 상한이다** — 등 1개에서 몬스테라가 자라는 자리는 **넷뿐**
  (`sill:0`·`etagere:6·7·8`). 등을 2개로 늘려도 `etagere:7` 이 12.31 → 12.41(**+0.8%**).
  모주를 늘리려면 **가구가 밝은 칸을 만들어야** 한다
- ★ 실측(50일·부지런한 플레이): 시루 6개째에 못 하는 날 **7일** · 첫 레벨업 시루 2개 **10일**,
  5개 **5일**. ⚠ 다른 워커 값(6개=1일 · 2개=13일 · 5개=7일)과 **안 맞는다** — 전제가 다르다(§16)
판단필요:
- ★★ 위 실측 불일치를 **같은 전제로 다시 재야 한다**(내 전제 셋은 `siru-to-plan.md §16`)
- 퀘스트 「시루 5개 분배 5주기 완주」의 **완주 판정**을 누가 붙이나 — 창구·값은 준비됨, 부르는 데만 없음
- 씨앗값 두 벌을 `data/balance/` 로 뺄 것인가(권함 — 이번 `stamina.json` 과 같은 모양)
대기:
- **침대(가구) 팔기 = 파산 처리는 손도 안 댔다** — 체력·경험치와 삽수 빛 버그가 먼저였다.
  요구사항 여섯이 그대로 남아 있다(`siru-to-plan.md §21`)

### 4차 (2026-08-09) — 가방 카드 · 칸 게이지
갱신: 2026-08-09 · 상세는 `docs/handoff/bagcell-to-plan.md`
완료:
- **가방 카드가 드디어 「빈 시루」를 말한다.** 앞 작업은 카드의 **자리**만 고치고
  카드를 눌러 열리는 **상세 창**은 안 고쳤다 — 거기 「열린 콩나물 시루 · 자란 날 1/5일 ·
  받은 빛 · 품질」이 그대로 있어서 박사님이 똑같은 그림을 다시 보셨다.
  이제 제목은 **「콩나물 시루」**, 내용은 **가방에 몇 개**와 **쓰는 법**뿐이다
- **게이지가 칸으로 갈렸다** (박사님 *"5칸짜리 빈 게이지 … 1칸씩 … 직관적으로"*).
  칸 수 = 그 작물의 주기다 — **콩나물 5칸 · 무순 7칸**. `harvestDays` 를 읽고 **5를 안 박았다**
- **물을 안 준 시루가 화면에서 갈린다** — 물빛 테두리 칸(`.cells.dry`).
  물을 안 주면 [다음 날]을 눌러도 칸이 **안 찬다**는 것이 눈에 보인다
- **퀘스트 줄의 거짓말을 잡았다** — 물을 안 준 첫날 「자라는 중입니다. 수확까지 5일」이라
  떠서 게이지·손가락과 **셋이 다른 말**을 했다. 이제 「물을 주면 칸이 돌기 시작합니다」다
- 새 검사 **`tools/test_bagcell.mjs`** (26항목) — 상태 값이 아니라 **화면에 찍힌 글자**를 읽고
  사진 6장을 남긴다(`docs/engine/shots/bagcard_empty·cells_*.png`)
발견:
- ★ **`.siru .meter` 와 `#musunMeter` 가 없어졌다.** 시루 게이지는 `.cells > i` 이고
  무순 게이지는 `#musunGauge` 안에 산다. 화면을 훑는 도구가 옛 이름을 보면 못 찾는다
- ★★ **검사 다섯이 깨져 있는데 내 것이 아니다** — `test_cropsale` · `test_tutorial` ·
  `test_oneroom` · `test_pots` · `test_dialogue_coverage`. 내 `game.html` 을 stash 로 걷고
  **HEAD 판으로 다시 돌려도 똑같이 깨진다**(직접 확인했다). 지금 작업 트리에 **커밋 안 된 채**
  고쳐져 있는 `data/balance/characters·homes.json` 과
  `src/game/shop·tutorial·first_play·dialogue·propagation.js` 쪽이다 —
  값이 옮겨 가는 중이고 검사가 아직 옛 숫자를 본다(예: 500 vs 700 · 월 50만 vs 60만).
  **숫자를 어느 쪽에 맞출지 그 창과 정해 주십시오.** 나는 그 파일들을 안 건드렸고 커밋에도 안 넣었다
- 규칙·밸런스는 한 톨도 안 바뀌었다. 칸은 하루를 세는 **옷**일 뿐이고 생장 판정 밖이다
판단필요:
- ★ **「빈 시루」 그림이 없다.** 카드 그림은 아직 다 자란 콩나물이 담긴 원화라
  글과 그림이 다른 말을 한다. 셋 중 고르실 것 — ㉠그대로 ㉡`container_siru_closed.png`
  (뚜껑 덮인 시루 · 에셋 이미 있음 · 오늘 바로 됨) ㉢빈 시루 원화를 leaf 에 요청.
  ⚠ ㉡·㉢ 는 `OPEN_SIRU` 에셋 계약과 끌기 미리보기를 같이 건드린다 — `bagcell-to-plan.md §6`

## balance (확정 값 반영 · 2026-08-09)
갱신: 2026-08-09
완료:
- **박사님 확정 밸런스를 게임에 넣었다** — 시작돈 130만 → **150만** · 반지하 월세 30만 → **20만**
  (하루 지출 20,000 → **16,667**이 유도로 따라온다) · 잉여 판매가율 0.70 → **0.85** ·
  콩 씨앗 실구매 700 → **500**(정가 350) · 시루 실구매 7,000 → **5,000**(정가 **3,550**)
- **회전분 표를 갈랐다** — 「작물 기본값(콩나물 3,000 · 무순 **2,800**) × 질림 배율(1 · 2/3 · 1/3 · 0)」.
  ★질림은 **여전히 한 표**다(2026-08-04 확정 유지). 콩나물 겹침 3,000/2,000/1,000 은 한 푼도 안 움직였고
  **무순만** 2,000 → 1,867 로 내려갔다
- **`repotCutting` 조용한 실패를 고쳤다** — 자리를 먼저 정하고 다 되면 그때 상태를 찍는다.
  옛 코드에서 `potted=true · slotId=null` 을 실제로 재현했다(`tools/test_repot_atomic.mjs` 신설)
- **정본을 한 벌로 모았다** — `shop.CATALOG` 의 씨앗 정가가 `CROP_KINDS[i].seedWonPerPot` 을 **읽는다**.
  넉 달째 없던 **`tools/test_econ.mjs` 를 새로 만들어** 월세·하루지출·판매가율·씨앗값 등식을 고정했다
발견:
- ★★ **시작돈 1,500,000원 = 이사비 1,500,000원.** `canMoveOut` 의 돈 조건이 **첫날부터 참**이라
  배움 넷만 채우면 나간다 — 실측으로 튜토가 **여름(게임 48일)에 끝난다.** 가을·식물등·겨울이
  통째로 건너뛰어진다. 확정 무늬도 첫날부터 「필요 없음」으로 닫힌다
- ★ **원룸으로 이사하면 하루 살림이 1,667원이 된다** (16,667 − 450,000/30). 식비만 7,500원인데.
  원룸이 자기 `dailySpendWon` 을 갖는 일이 이제 미룰 수 없다
- ★ 손익분기가 **두 작물 반대로** 움직였다 — 콩나물 23.3 → **16.7%**(씨앗이 싸졌다) ·
  무순 30.0 → **32.1%**(회전분이 내려갔다)
- ★ 곳간 하루 상한 5,000 → **4,867원**. 「3,000+2,000 이 정확히 2끼 5,000원」이 이제 틀린 말이다
  (`game.html` 2886줄 주석 — **화면 소유 창이 고쳐 주십시오**)
- ★ **튜토 목표 줄에 「돈을 모으는 단계」가 없다.** 배움 넷을 채우는 순간 `learn` → `ready` 다 —
  「이사 자금 N원 남았습니다」를 한 번도 안 말한다. 위 시작돈 = 이사비의 또 다른 얼굴이다
- ★ bagcell 창이 STATUS 에 적어 둔 **「검사 다섯이 깨져 있는데 내 것이 아니다」는 이제 다 통과한다.**
  그 다섯은 값이 옮겨 가는 중이던 내 미커밋 변경이었다 — 숫자는 확정값 쪽으로 맞췄다
- ★ 실측: 파산일 83/74 → **181/181일** · 0원날 57/46 → **4/0일** · **이사일은 189/161 그대로**
  (돈을 풀어도 이사는 안 빨라진다 — 확정 무늬 12일 박자가 정한다)
판단필요:
- ★★ **씨앗·시루가 「실구매」인가 「정가」인가.** 실구매로 넣었다. 정가 해석이면 **아무것도 안 바뀐다**
- ⚠ **시루 정가 3,500 은 틀린 답이다** — 3,500 × 1.4 = 4,900 이고 그건 이미 100원 단위라
  실구매가 **4,900원**이 된다. 5,000원을 내는 정가는 3,500 < L ≤ 3,571 이라 **3,550** 을 넣었다
- ★★ 시작돈 = 이사비를 어떻게 할지 (이사비를 올린다 / 시작돈을 내린다 / 계절 관문을 단다)
- 「두 달, 밥값 아끼면 세 달」 — 실측이 **90일 / 121일**이라 대사를 **석 달 / 넉 달**로 옮겼다
- 상세는 **`docs/handoff/balance-to-plan.md`**

---

## nextdaygate (Day 92 정지 · [다음 날] 문지기 · 오류 상자)
갱신: 2026-08-10
완료:
- **[다음 날]이 안 넘어가던 것을 고쳤다.** 문지기가 자리 사본(`beansprout.slotId`) 하나를
  보던 것을 **방에 선 시루를 세는** 것으로 바꿨다(`cropPotList(fp, day).placed`).
  콩나물만 안 본다 — **무순만 놓은 판도 하루가 간다**. 하나도 안 놓았으면 그대로 막는다
- **오류 상자가 쌓여 화면을 덮던 것을 고쳤다.** `#errBox` 하나만 산다 —
  같은 말 접기(`×N`) · 많아야 셋 + 「…외 N개」 · 눌러서 닫기 · 높이 상한 38dvh ·
  **꺼진 상자는 포인터를 안 먹는다** · 아래 여백을 하단 액션바 실제 높이에서 잰다
- `guard()` 머리말이 어떤 실패에든 「오늘은 진행되지 않았습니다」라고 하던 것을 고쳤다 —
  다시 심기가 실패해도 화면이 **하루가 막혔다고 말했다**
- 물주기 버튼 두 줄에 **주어를 넣었다**(`작물은 전부 자라는 중` · `몬스테라는 아직 촉촉합니다`)
- 새 검사 `tools/test_nextday_gate.mjs` **25항목** — 폰 390px 에서 실제로 눌러 잰다
발견:
- ★ **문지기만 고치면 반쪽이다** — `loop.js:594` 가 아직 자리 사본으로 「작물을 자라게 할까」를
  가른다. 사본이 빈 채로 하루가 가면 **작물만 조용히 안 자란다.** 지금은 화면이
  `syncCropLead` 로 사본을 고쳐 넘겨 막았다(보험이다)
- ★★ **이사하면 거둔 적 있는 작물이 통째로 가방으로 간다** — `oneroom.js:288` `reseatCrops` 가
  `site.harvested`(=하나라도 거뒀나)로 자리 통째를 건너뛴다. 아무 말도 안 해서
  「이사했더니 판이 멈췄다」로 보인다. Day 92 가 이 길일 가능성이 높다
- ★ `first_play.js:1349` `advanceBeansproutDay` 도 같은 문지기다. 이 예외에는 `firstPlayInput`
  표가 없어서 화면이 **복구 불가로 읽고 버튼을 전부 잠근다**(hardLock). 재서 확인했다
대기:
- core — 위 세 줄(`loop.js:594` · `first_play.js:1349` · `loop.js:1329`). 붙일 코드는
  `docs/handoff/nextdaygate-to-plan.md §다른 창이 알아야 할 것`
판단필요:
- ★ **이사 시 시루 회수를 시루마다 판단할까** (거둔 것만 가방 / 지금대로 두되 말은 하기)
- ★ **하나도 안 놓은 판을 튜토 종료 뒤에도 계속 막을까.** 지금은 막는다(지시대로)
