# 고칠 목록 — 2026-08-16 박사님 한 뭉치

> ★ 박사님이 한 번에 주신 목록을 **한 줄도 안 버리고** 옮겼다.
> 「묶을 건 묶고 차근차근, 전체 다 진행해」가 지시다.
> ⚠ 이 문서는 **할 일**이다. 끝난 줄에는 커밋 해시를 적는다.

---

## ✅ 이미 끝난 것 (오늘)

| | 무엇 | 커밋 |
|---|---|---|
| ✅ | 한번씩 캐릭이 사라져 | `1e7ba81` — 가구 옮길 때마다 100%. 순번이 하나여서 자취생이 늘 졌다 |
| ✅ | 몬이가 캐릭을 안 바라보고 안 따라감 | `1e7ba81` — 원래 없던 것을 **새로 만들었다** |
| ✅ | 가방 탭에 할 일은 이제 없어도 되지 | `dfe592f` |
| ✅ | 씨앗이 자동으로 들어감 → 화분만 놓이게 | `76d3145` — 놓기·심기 두 걸음 |
| ✅ | 배달 몬스테라가 안 와서 먹통 | `c926a08` — 첫 플레이 중 씨앗 심기를 막았다 |
| ✅ | 없는 몬스테라가 식물탭에 켜져 있음 | `c926a08` |
| ✅ | 시루 수확물 날짜별 분류 없애기 | `12dedf9` |
| ✅ | 검은 모종포트를 화분 쪽으로 | `12dedf9` |

---

## A. 안내(가이드)가 없는 것 — **한 묶음**

말이 없어서 못 하는 것들이다. 규칙은 이미 다 있고 **안내만 없다.**

| | 무엇 | 커밋 |
|---|---|---|
| ✅ A-1 | 캐릭 눌러서 **자유 이동**하는 것 — 처음 가이드가 없다 | `20d299b` — 새 통로 「첫걸음 쪽지」(`#coach`) + 안내판 한 줄 |
| ✅ A-2 | **가구 이동** 가이드 | `20d299b` — 쪽지 + 안내판. ★ 안내판이 **없는 단추 이름**([이동]·[회전])을 대고 있었다 |
| ✅ A-3 | **식물 배치·이동** 가이드 — 그냥 눌러 배치하면 책상 위 같은 아무 자리로 감 · 상대 이동 | `20d299b` — 「끌면 손가락 자리 · 누르면 게임이 빈 자리를 고른다」를 가방 줄이 갈라 말한다 + 쪽지(상대 이동) |
| ✅ A-4 | 초기 **콩나물 시루 4개쯤 놓기** 가이드 | `20d299b` — ⚠ 첫 판 시루는 **하나뿐**이라 그 하나를 놓으면 안내줄이 같이 사라졌다. `.bagmore` 새 줄이 칸과 무관하게 뜬다 |
| ✅ A-5 | **무순 배치** 가이드 | `20d299b` — 카드가 첫 걸음만 말하고 있었다. **놓기 → 심기 → 물** 세 걸음을 걸음마다 갈라 말한다 |

★ 묶는 까닭 — 다섯 다 `updateHint`·`dialogue` 한 계통이다. 따로 붙이면 말이 겹친다.

★★ **세고 나서 알게 된 것** (`docs/handoff/guide-to-plan.md §0`) — 안내 통로가 **아홉**인데
  전부 「할 일」을 말하는 자리다. A-1·A-2·A-3 은 **할 일이 아니라 「할 수 있는 것」**이라
  아홉 중 어디에도 자리가 없었다. `#dropLabel` 은 이미 그 모드에 들어간 사람에게만 보인다 —
  **들어가는 법을 모르는 사람에게는 영영 안 보였다.** 그 틈이 이 셋이다.
⚠ **`dialogue.js` 는 한 글자도 안 고쳤다.** 「왜 무순인가」·「왜 시루를 늘리나」는
  `questCropMix`·`questSiru5` 가 이미 말한다. 없던 것은 **손짓**(어떻게)이지 **까닭**(왜)이 아니었다.
⚠ 덤으로 겹침 하나를 더 잡았다 — 시트가 열려 있을 때 손가락이 **시트 뒤에 가린 [다음 날]**을
  가리켰고, `#hint` 는 `position:fixed` 라 시트 **위에** 그려져 시루 줄 글씨를 덮었다.
  `updateHint` 의 마지막 갈래만 `!sheetOpen` 을 안 보고 있었다.

---

## B. 배치·그리드 — **한 묶음**

| | 무엇 | 커밋 |
|---|---|---|
| ✅ B-1 | **책상·서랍장 위 2곳씩 남는 칸** 버그 (사진) | `d1986cd` — **책상은 원래 맞았고 서랍장만 어긋나 있었다**(칸 한가운데에서 0.125m). 서랍장 상판 표시 10 → **8**(= 4×2). `test_floorlight ①-1` 이 ✘→✔ |
| ✅ B-2 | 방 가구 이동이 **내부 그리드 끝에서 스냅**, 턱 막혀 옆방으로 안 가게 | `bba1f7f`(다른 창이 내 room_view 변경을 같이 커밋했다) — 침대 (−9,0) 이 그대로 (−9,0) 이던 것이 **(−1.875,0)** 에서 막힌다. ⚠ `test_roomview_place F-6` 이 옛 계약이라 빨개졌다 |
| ✅ B-3 | 가구 이동 시 나오는 **그리드 빨간색이 이상** | `bba1f7f` — 침대 옮길 때 285칸 중 **218칸(76%) 붉던 것이 43칸**. 빨강의 뜻을 「이미 뭔가 서 있는 칸」으로 바꿨다 |
| ✅ B-4 | 옮긴 뒤 팝업에 [옮기기]가 또 있어 헷갈림 → **[확정]/[취소]** 로 | `bba1f7f` — [취소]는 **옮기기 전 자리로 정확히** 되돌린다(화면 길로 실측) |
| ✅ B-5 | 가구 옮길 때 **위에 놓인 식물도 같이** 옮겨지고 돌아가야 함 | `d1986cd` + `bba1f7f` — **3D 는 원래 맞았고 세이브가 안 따라갔다.** 새로고침이면 화분이 허공으로 돌아갔다. 옮기기·돌리기 둘 다 실측 |
| ✅ B-6 | 시작할 때 **가구가 모서리에 붙어** 있게 (침대·서랍장) | `d1986cd` — 벽까지 넷 다 **0.00m**. 조도 14칸 중 **서랍장 2칸만** 움직였다(0.08→0.06 · 0.05→0.04) · 창턱 4.80 그대로 |

★ 상세·실측표·사진: **`docs/handoff/place-to-plan.md`**
⚠ 해시가 둘로 갈린 까닭 — 창이 겹쳐 돌아서 **다른 창의 커밋이 내 작업 파일을 같이 실어 갔다.**
  `bba1f7f` 에 `game.html`·`room_view.js` 가, `d1986cd` 에 `place.js`·`state.js`·`furniture_pastel.js`·`house_rooms.json`·문서가 들어 있다. 내용은 전부 들어갔다.
⚠ 남긴 것 셋 — ① `room_profile.banjiha.json` 이 낡았다(내 변경 **전부터**. `gen_room_profile.mjs --write` 필요)
  ② `test_roomview_place F-6` 은 **빨간 채로 뒀다**(옛 계약을 못 박은 검사)
  ③ 다른 방(원룸·투룸·아파트·온실)의 같은 슬롯 어긋남은 **안 건드렸다**(「반지하 먼저」)

---

## C. 상점·용어 — **한 묶음**

| | 무엇 |
|---|---|
| C-1 | 세부 탭 순서에서 **[전체]를 맨 뒤로** |
| C-2 | **「곳간」이라는 말 없애기** → 「보유 채소」 |
| C-3 | 「기르는 그릇」 → **「채소 키우는 그릇」** (채소용만) · 화분은 몬스테라용 |
| C-4 | **무순 재배판이 왜 가방에** 있나 |
| C-5 | **식물등 구매 자리**를 상점으로 (새 탭) |

---

## D. 방·바깥 3D — **한 묶음**

| | 무엇 | 커밋 |
|---|---|---|
| ✅ D-1 | 바깥 **도로와 건너편 건물 사이가 벌어짐** — 붙이기 | `20d299b` — 빌라 줄이 담보다 **1.0~2.8m 뒤**에 서 있었다(담 뒤 4.49 vs 빌라 5.50~7.30). 담 바로 뒤(4.51~4.96)로 당기고 땅을 담 **앞**에서부터 깔았다. 삼각형·드로우콜 **+0** · `test_outside` 통과. 전·후 사진 `img/guide/{before,after}_grid/grid-phone-mid-out.png` |
| ✅ D-2 | **식물등 최초 위치가 이상** — 몬스테라 위쪽으로 | `e9d576f` (§G-16) — 옮겼다. 표·판단필요는 `docs/handoff/midlamp-to-plan.md` |

★★ **D-2 는 박사님 지적이 맞다 — 숫자로 확인했다.** 등을 하나 사면 `light_adapter.rigsOn` 이
  `growRigs.slice(0,1)` 이라 **선반 밑 붙박이 바 등**이 켜진다:
    창턱(몬스테라 자리) 3.68 → **4.03** (+0.35) · 책상(도착 자리) 0.61 → 1.09
    **3단 선반 맨 윗칸 0.65 → 12.47** (×19) ← 여기만 밝아진다
  ⇒ 이만오천 원을 낸 사람이 **화면에서 아무 변화를 못 본다.**
⚠⚠ ~~그런데 지금은 만지면 안 된다~~ — **2026-08-16 밤 해결됐다.** 창턱 3.68 은 `bba1f7f`
  (방을 키운 것을 되돌림)로 **4.80 으로 복구**됐다. 다시 재고(`probe_lamphome`) 옮겼다 — `e9d576f`.
  ★ 그리고 **위 「+0.35」 표는 3.68 판에서 잰 것이라 낡았다.** 4.80 판에서는 등1 이 5.15 였다
    (그래도 갈라짐 문턱 6.0 은 못 넘는다 — 박사님 지적은 여전히 맞았다).

---

## E. 손맛 — **한 묶음**

| | 무엇 | 커밋 |
|---|---|---|
| E-1 | **휠이 아직 턱턱 막힘** (`dfe592f` 로 두 가지 고쳤으나 남음) | |
| E-2 | **손화살표** — 다른 것 클릭했을 때 안 없어짐 · 시점 바꾸면 안 따라옴 (`c926a08` 일부) | ⚠ 한 갈래를 `20d299b` 가 잡았다 — 시트가 열려 있을 때 손가락이 **시트 뒤에 가린 [다음 날]**을 가리켜 시트 **위에** 얹혀 있었다 |
| ✅ E-3 | 캐릭 눌러 자유 이동할 때도 **걷기 소리** | `20d299b` — 소리는 있었고 **켜는 자리가 `doAct` 하나뿐**이었다. 자유 이동은 그 길을 안 탄다. 방뷰에 창구를 못 내므로 **120ms 마다 `characters()[].walking` 을 센다.** 끝나는 길 **열 개**를 세어 전부 막았다 — 도착·중단·사라짐·방뷰 사망·확대(멈춤)·탭 뒤로·창 흐림·페이지 떠남·`doAct` 중복·소리 끔. ★ 창 흐림은 **한 번 끄는 것으로 모자랐다**(다음 틱이 도로 켠다) — 걸쇠로 잡고 `focus` 에서만 푼다 |

---

## F. 밥·무늬

| | 무엇 |
|---|---|
| F-1 | 콩나물·무순 **식량 적용 시 칸을 갈라 고르도록** |
| F-2 | 둘째·셋째 변이가 확정인데 **성숙잎도 확정**이어야 — 이 판에 한해 |

★ F-2 는 워커가 도는 중(중간잎 42갈래를 등급으로 묶는 일).

---

## ⚠ 이 목록을 쓸 때
- 끝낸 줄에 **커밋 해시**를 적어라. 안 적으면 다음 창이 또 한다.
- **못 한 것은 못 했다고 남겨라.** 지우지 마라.
- 화면으로 확인 안 한 것에 ✅ 를 달지 마라(이 저장소의 계율).

---

## G. 2026-08-16 밤 — 박사님이 이어서 주신 것 (한 줄도 안 버렸다)

> ★ 박사님: *"하다 많이 요구한 요청사항 **순차로 내가 말 안 해도 계속 돌려서 끝까지 수정해.**"*
> ⇒ 이 목록은 **묻지 말고 위에서부터 차례로** 하면 된다. 끝낸 줄에 커밋 해시를 적어라.

| | 무엇 | 상태 |
|---|---|---|
| ✅ G-1 | 화분 끌면 **선물 몬스테라가 끌려감** | `e2edd89` — 갈래가 콩나물 블록 **안**에 있었다. 밖으로 빼고, 「모르는 갈래는 던진다」 문을 세웠다 |
| ✅ G-2 | 새싹 재배판을 **가방 칸에서 끌어 놓기** · 큰 카드 없애기 | `e2edd89` |
| ✅ G-3 | 시루 늘리기 **퀘스트 둘**(체력 +1씩) | `31e4fd4` — 문턱 **여덟·열여섯**(재서 정함. 16개가 본전) |
| ✅ G-4 | 몬스테라 것은 **선물 온 뒤에** 상점에 | `31e4fd4` — 첫날 상점은 콩 씨앗·시루 둘뿐 |
| ✅ G-5 | 상점 **[전체] 맨 뒤로** | `31e4fd4` |
| ✅ G-6 | 상판 위 **초록 강조 두 칸** 걷기 | `31e4fd4` |
| ✅ G-7 | **3단 선반 눌러도 안 뜸** | `31e4fd4` — 빈 자리를 눌러도 탭을 먹고 있었다 |
| ✅ G-8 | **시루가 3단 선반에 못 올라감** | 시루 지름 **0.24 → 0.20**. 실측: 못 올라가는 자리 **10곳 → 0곳** |
| ⬜ **G-9** | **물주러 못 감** — 「거기까지 못 갑니다 (1.56m)」 · 물은 줬는데 모션 스킵 | ★ 제일 급하다. 닿는 거리 `ACT_REACH` **1.45m** 인데 가구를 벽에 붙여(B-6) 설 자리가 멀어졌다 |
| ⬜ G-10 | **몬스테라 키 줄이기** — 잎 3장에 창문을 뚫는다 | |
| ✅ G-11 | **수확 `+OOOg` 초록 글자**가 시루 위에 떴다가 올라가며 사라지게 | `6d6aacc` — 자리는 말풍선과 **같은 자**(`screenPosOf`)를 프레임마다 다시 묻는다. 시점을 돌려도 따라온다(실측 글자 left **206** = screenPosOf **206**). 부르는 자리는 `noteHarvest` 하나 — 거두는 길이 둘이다. 사진 `img/terms/after/g11_float_turned.png` | 
| ⬜ G-12 | **3단 거치대를 서랍장·책상 위에** 올릴 수 있게 · 높이 맞추면 창턱만큼 빛 받게 | ★ 「자유롭게」가 박사님 뜻이다 |
| ✅ G-13 | 「곳간」 말 없애기 → **「보유 채소」** | `6d6aacc` — 화면 글자만 바꿨다. **코드 이름(`pantry…`)은 한 글자도 안 바꿨다**(세이브·검사·문서가 쓴다). 실측: 상점 탭 **2곳 → 0곳** · 밥상 창 **셋 → 0곳**. ⚠ `first_play.js` 의 오류 접두사 하나는 남았다(그 파일은 읽기만 했다 · 화면에 안 닿는 줄) |
| ✅ G-14 | 「기르는 그릇」 → **「채소 키우는 그릇」** | `6d6aacc` — ★ **이름만 바꾸면 거짓말이었다.** 그 갈래에 **유리 수경병**(몬스테라 삽수 그릇)이 들어 있었다(검수 판 실측 3개). [화분](몬스테라용)으로 옮겨서 채소만 남겼다 |
| ✅ G-15 | **식물등 구매를 상점 새 탭**으로 | `6d6aacc` — `pageRoom` → `pageShop` 하부 탭 [식물등]. **사는 규칙(`tutorial.buyLamp`)은 한 글자도 안 바꿨다.** 가을에 풀리기 전에는 **아예 안 보인다**(잠긴 회색 단추를 걷었다). 덤: 갈래 줄이 **360px 에서 [전체]를 밀어내고 있던 것**을 잡았다(줄을 접게 했다) |
| ✅ G-16 | **식물등 첫 위치**를 몬스테라 위쪽으로 | `e9d576f` — 바 등이 창턱(y 1.585)보다 **0.52m 아래**(y 1.066)에 있었다. 창 위 벽(0, 2.15, −1.85)으로 올려 **창턱 4.80 → 등1 7.07** (갈라짐 문턱 6.0 을 처음 넘는다 · 무늬종 8.4 와 과광 16.0 은 안 넘는다). ⚠ **선반 맨 윗칸 12.31 → 0.89 를 잃었다** — 판단필요 |
| ✅ G-17 | **중간잎은 하프문 하나**로 · **성숙 때 확률로 분류** | `e9d576f` — 중간잎을 등급과 무관하게 한 갈래로. **등급 정하는 시점은 안 옮겼다**(옮기면 프롤로그 그루가 196만 → 100.8만. 재서 확인) · 그림과 알림만 성숙 때로. 프롤로그 **1,960,000원 그대로** |
| ✅ G-18 | 콩나물 **날짜 계속 보내라는 안내** | `6d6aacc` — **먼저 세었다**(통로 열 · `probe_terms.mjs`). ①은 말하는데 **첫 플레이 단계일 때만**이고 퀘스트가 열리면 그 줄을 가져간다. 매일 누르는 **[다음 날] 단추가 아무 말도 안 하는 자리**였다 → 「다음 날 ▸ · 수확까지 3일」. **새 통로를 안 만들었고 손가락도 안 되살렸다**(2026-08-06 에 물리신 길) |
| ⛔ G-19 | 콩나물·무순 **식량 적용 시 칸 갈라 고르기** | **못 했다.** 고른 값이 지나는 채널이 `mealPlanWon` **수 하나**이고 `eatFromPantry` 가 **총 g 만** 받아 어느 작물을 먹을지 스스로 고른다 — 총 g 으로는 그 뜻이 **전달되지 않는다**(실측 표 `terms-to-plan.md §4`). `first_play.js` 에 창구 하나(`gramsByKind`)가 필요한데 그 파일은 이 창에서 **읽기만** 하기로 했다. 고를 수 없는 손잡이를 화면에 안 붙였다 |
| ⬜ E-1 | **휠이 아직 턱턱** | `dfe592f` 로 둘 고쳤으나 남음 |

⚠⚠ **배포가 늦게 퍼진다.** 2026-08-16 밤 실측 — 커밋 뒤 2분이 지나도 옛 판이 내려왔다
  (로컬 566KB vs 배포 554KB). 박사님이 *"여전히 몬스테라가 같이 와 버리는데?"* 하신 것이
  그것이다 — **고쳐졌는데 안 퍼진 것.** ⇒ 링크를 드리기 전에 **배포판을 받아서
  그 고침이 들어 있는지 글자로 확인해라.** 「푸시했다」는 「퍼졌다」가 아니다.
