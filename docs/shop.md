# 인터넷 주문 상점 · 잭팟 판매 — 정본

**2026-08-03 신설.** 박사님 확정을 정본화한 것이다.

> 박사님 원문
> - *"콩나물 시루를 처음에 주는데 그 이후엔 화분이나 시루용기, 씨앗들을 **인터넷 주문 상점**을 해서 **하루 이틀 뒤에 오는** 형태로 하는 건 어때?"*
> - *"무늬종 하나 **잭팟** 터뜨려서 팔고 이사 가는 형태로 해야 될 듯 (꾸준히 모으면 오래 걸리고)"*

구현은 `src/game/shop.js` 이고 재현은 `tools/test_banjiha_routes.mjs` 다.
가격 공식은 `propagation.md` §6, 상점 마진은 `sale_economy.md` §5, 씨앗값은 `food_economy.md` §3.
**이 문서에서 새 수치를 만든 곳은 §2 의 용기값 하나뿐이고, 그 근거도 아래에 적었다.**

---

# 0. ★ 왜 적립이 아니라 잭팟인가 — 숫자가 그렇게 말한다

이사 자금 150만원을 **모아서** 만들 수 있는지 먼저 역산했다.

| | |
|---|---|
| 하루 지출 합 | 20,000원 (월세 10,000 + 공과·전기 2,500 + 식비 7,500) |
| 콩나물 풀가동 절감 | 5,000원/일 (하루 2끼 상한 · `food_economy.md` §4) |
| 씨앗 재투자 | 시루 3개 × 2,100원 ÷ 4일 = 1,575원/일 |
| **순 방어** | **3,425원/일** |

콩나물을 완벽히 돌려도 **하루 16,575원씩 줄어든다.** 반지하 방 칸(14) 전체를
콩나물로 채워도 하루 2끼 상한 때문에 절감이 5,000원에서 멈춘다 — **상한이 곧 천장**이다.
즉 **반지하에는 흑자가 없다.** `sale_economy.md` §1 이 이미 낸 결론과 같다.

> **그래서 이사 자금은 "모으는 것"이 아니라 "한 번에 만드는 것"이다.**
> 콩나물은 그때까지 굶지 않게 버티는 수단이고, 실제로 **파산을 6일 늦추고 튜토 29일째에
> 64,300원을 더 남긴다**(재현 D-2).

---

# 1. ★★ 150만원 역산 — 무늬 잎이 몇 장이라야 하나

> ⚠ **이 절은 2026-08-04 값 개편으로 대체됐다.** 이 문서 맨 끝의 「★★ 2026-08-04 값 개편」을 보라.
> 여기 적힌 `× (1+60·v²)` 공식과 그 표는 **더 이상 코드가 쓰지 않는다**(기록으로만 남긴다).

`propagation.md` §6 공식 그대로다.

```
값 = 단위기본가 × 크기 × (1 + 60·v²)
      v = 그 그루의 무늬 잎 비율 (무늬 잎 ÷ 전체 잎)
      크기 = 잎(마디) 수
      잎 1~2장 → 삽수 가격표(12,000/잎) · 잎 3장~ → 성체 가격표(10,000/잎)
```

`shop.varieLeavesNeededFor(1_500_000, {leaves})` 가 낸 표다(코드와 문서가 갈리지 않게 함수로 둔다).

| 잎 수 | 등급 | 필요 v | **필요 무늬 잎** | 그때 값 | v=1 일 때 최대 |
|---|---|---|---|---|---|
| 1 | 삽수 | 1.438 | **불가능** | — | 732,000 |
| 2 | 삽수 | 1.012 | **불가능** | — | 1,464,000 |
| 3 | 성체 | 0.904 | 3 | 1,830,000 | 1,830,000 |
| 4 | 성체 | 0.780 | 4 | 2,440,000 | 2,440,000 |
| 6 | 성체 | 0.632 | 4 | 1,660,000 | 3,660,000 |
| 9 | 성체 | 0.511 | 5 | 1,756,667 | 5,490,000 |
| 12 | 성체 | 0.438 | 6 | 1,920,000 | 7,320,000 |

## ★ 여기서 바로 나오는 두 가지

**① 삽수 한 개로는 150만이 절대 안 된다.** 잎 2장이 전부 무늬여도 1,464,000원이다.
`propagation.md` §7 의 *"성체 4그루냐 삽수 16개냐"* 가 ② 이사 단계에서 미리 한 번 나온다.

**② 그래서 실제 잭팟은 「무늬 삽수 + 남은 모주」다.**
무늬 잎 한 장을 품은 마디를 잘라 팔면 `12,000 × 1 × 61 = 732,000`원이고,
남은 모주(잎 5~6장 민무늬)를 60,000원에 같이 팔면 792,000원이 된다.
그동안 콩나물로 지킨 잔액(튜토 24일째 약 76만원)과 합쳐 **150만을 넘긴다.**
재현이 실제로 그 경로로 이사한다 — *"튜토 24일에 792,000원에 팔고 그날 이사"*.

## ⚠ 정본 안에서 `v` 가 두 뜻으로 쓰인다 — plan 판단 필요

`propagation.md` §6 **본문**은 *"v 는 그 그루에 실제로 난 무늬 잎의 비율"*이라고 못 박는다.
그런데 같은 절의 **검산표**는 「몬스테라 삽수(알보) 80,000」을 `12,000 × 6.667`(v = 0.307)로 맞춘다.
잎 1장짜리 삽수의 그 한 장이 무늬면 **잎 비율로는 v = 1.0** 이고 값이 732,000원이 된다.

| 읽는 법 | 무늬 삽수(잎1) 값 | 잭팟이 성립하나 |
|---|---|---|
| **㉮ 잎 비율** (본문 정의) | **732,000** | ✅ 성립한다 |
| ㉯ 라벨 짙기(알보 = 0.307) | 80,000 | ❌ 반지하에서 절대 불가능 |

**코드는 ㉮(본문 정의)를 따랐다.** 코어가 안 보이는 값을 지어내지 않는다는 원칙과도 맞고,
플레이어가 잎을 세어 값을 읽을 수 있다. 다만 ㉮면 **작은 그루의 값이 매우 커진다** —
잎 하나가 무늬인 삽수가 73만원이다. 크기 하한이나 소품 상한이 필요할 수 있다. **plan 확인 대기.**

---

# 2. 상점 — 품목·값·배송

## 마진과 배송 규칙

| | |
|---|---|
| **살 때** | 정가 × **1.4** (`sale_economy.md` §3 머리말 — "정가. 살 때는 ×1.4 = 상점 마진"). 100원 단위 올림 |
| **팔 때** | 정가(= §1 공식이 낸 값). 변동가 없음(`sale_economy.md` §5) |
| **배송** | 가벼운 것(씨앗 봉투) **1일** · 부피가 있는 것(용기·화분) **2일**. 박사님 *"하루 이틀 뒤"* |

★ **결제는 주문한 날, 물건은 나중.** 도착 때 결제하면 *"일단 다 시켜 놓고 돈 생기면 받는"*
무한 예약이 된다. 지금 돈이 나가야 **"이번에 뭘 시킬까"** 가 결정이 된다. 취소는 없다.

## 품목표

> ⛔ **2026-08-23 — 여기 있던 표를 지웠다. 값이 셋 다 낡아 있었다.**
>
> | id | 여기 적혀 있던 것 | **지금** | 정본 |
> |---|---|---|---|
> | `bean_seed` | 정가 1,500 · 살 때 2,100 | **정가 350 · 살 때 500** | `first_play.CROP_KINDS[0].seedWonPerPot` |
> | `siru` | 정가 5,000 · 살 때 7,000 | **정가 3,550 · 살 때 5,000** | `shop.js §siru` |
> | `monstera_seed` | 정가 1,500 · 살 때 2,100 | 그대로 ✅ | `shop.js §monstera_seed` |
> | `pot` · `jar` | 정가 5,000 | 그대로 ✅ | `shop.js` |
>
> **왜 갈렸나** — 2026-08-09 에 박사님이 **실구매가**를 정하셨다(콩 씨앗 500 · 시루 5,000).
> 이 표는 **정가** 칸이라, 실구매를 그대로 적으면 ×1.4 를 한 번 더 먹는다.
> 코드는 그 역산을 해서 정가를 350·3,550 으로 넣었고(`first_play.js:110` · `shop.js:83`
> 둘 다 그 셈을 적어 두었다), **이 문서만 안 따라왔다.**
>
> ★ **그래서 표를 다시 안 적는다.** 다시 적으면 또 갈린다 —
>   START-HERE §2.8 이 「600 → 3,000 → 4,867 → 또 낡음」으로 세 번 겪은 그 사고다.
>   **`shop.catalogList()` 를 부르거나 `src/game/shop.js §CATALOG` 를 읽어라.**

**여기 있던 표가 안 담고 있던 것도 있다** — 그 뒤로 품목이 늘었다:
`sprout_tray`(새싹 재배판) · `radish_seed`(무 씨앗) · 화분 넷
(`pot_concrete_round` · `pot_terracotta` · `pot_ceramic` · `pot_concrete_square`) · **가구 117개**.
가구는 `data/furniture_presets.json` 을 **읽어서 짓는다**(`shop.js §installFurniturePresets`).

### ⏸ 「용기값 5,000원」 — 이 절의 근거는 살아 있지만 숫자는 아니다

아래 §「용기값 5,000원」은 **정가 5,000원**을 전제로 쓴 글이다. 시루의 정가는 이제 **3,550**이다.
`pot`·`jar` 는 아직 5,000 이라 **셋이 더는 같은 값이 아니다.**
⇒ 「소품 용기는 크기가 달라도 한 값」이라는 **생각은 그대로 서는데 숫자가 갈렸다.**
  `data/balance/` 에 `shop` 항목을 만들 때 정리한다(plan 몫).

### ★ 용기값 5,000원 — 이 문서가 새로 정한 유일한 값

정본 어디에도 용기값이 없다(`food_economy.md` §3 은 씨앗 1,500원만, `propagation.md` §4 는
용기의 **슬롯 수와 에셋**만 정했다). 지어내지 않기 위해 **정본에 있는 가장 싼 완제품**에 맞췄다 —
`sale_economy.md` §4 의 **묘목 5,000원**이다. 그것이 이 게임에서 "제일 싼, 손에 잡히는 물건"의
가격대이고, 용기 셋은 전부 그 자리에 있다(0.12~0.36m 소품).

검산: 시루 하나가 4일에 3끼(7,500원어치)를 내므로 **용기값을 사흘이 안 되어 뽑는다.**
비싸서 못 사는 물건이 아니고, 공짜도 아니다. **⏸ plan 확인 대기** —
`data/balance/` 에 `shop` 항목을 만들 때 이 세 값을 옮기면 된다(이 창은 `data/balance/` 수정 금지).

---

# 3. 첫 시루는 공짜, 그 뒤는 주문

```
Day 0        콩나물 시루 하나를 받는다 (지금 그대로 · 공짜)
Day 4        첫 수확
그 뒤        씨앗을 주문한다 → 하루 뒤 도착 → 다시 심는다(resowCrop)
             시루를 늘리려면 용기를 주문한다 → 이틀 뒤 도착
```

★ **재파종은 씨앗 재고를 쓴다.** 재고가 없으면 `resowCrop` 이 던진다 —
*"공짜로 무한히 나오면 경제가 아니다"*. 그래서 **수확 전에 미리 시켜 두는 것**이 플레이가 된다.

★ **시루는 셋까지만 의미가 있다.** 하루 작물 끼니 상한이 2끼(`food_economy.md` §4)인데
시루 하나가 4일에 3끼라, 셋이면 2.25끼/일로 상한을 채운다. 넷째부터는 남아서 쉰다
(`first_play.advanceBeansproutDay` 가 `상한 × 회전일` 을 넘는 끼니를 버린다).
**규칙이 아니라 셈이 상한을 만든다.**

## ★★ 막다른 길이 없어진다

`cropDark`(어두운 자리에도 쓸모가 있다)는 수확 시 4일평균 DLI ≤ 0.3 으로 판정한다.
예전에는 시루가 **한 번뿐**이라 밝은 자리에서 첫 수확을 하면 영영 못 나갔다.
지금은 씨앗을 다시 주문해 **어두운 자리에 다시 심으면 배운다.**
자동으로 채워 주지 않는다 — 다시 심어도 밝은 자리면 여전히 안 켜진다(재현 C·C-2 가 둘 다 고정).

---

# 4. ★★ 2026-08-03 재개편 — 꾸준수입 + 확정 무늬로 마무리

박사님 확정:

> *"꾸준수입도 가능하지. 삽수 팔거나 하는 걸로. 시점의 차이인 거지.
>  근데 **튜토는 어느 정도 꾸준수입 + 임의 확정 성숙 무늬로 마무리**하는 걸로 하자."*

앞의 §4(반지하 등을 올려 달라는 제안)는 **취소한다.** `data/` 를 안 건드리고 성립한다.

## 4-1. 먼저 — 재현이 대역을 버렸다. 잎 수가 절반 이하였다

`tools/test_banjiha_routes.mjs` 가 `plant_grow.html` 을 **헤드리스로 진짜 돌린다**
(`tools/test_cuttable.mjs` 와 같은 방식). 예전 대역은 `sale_economy.md` §0 의 **마디 표**를
잎 수로 읽고 있었는데, 실제로 살아 있는 잎은 그보다 훨씬 적다:

| | 예전 대역 | **실측** |
|---|---|---|
| 도착 개체(유효 143일) | 잎 6장 | **잎 2장** (마디 14개) |
| 튜토 구간(+30~90일) | 7~9장 | **3장** |
| +120일 | 12장 | 5장 |

값이 잎 수로 매겨지므로 이 차이가 이사 성립을 통째로 바꾼다. **잎은 축(가지)마다 한 장**이고
마디는 그보다 훨씬 많다 — 두 수를 섞어 쓰면 안 된다.

## 4-2. 꾸준수입 = 삽수 — 얼마에 · 얼마나 자주 · 남는가

```
병을 주문한다(7,000원 · 2일)  →  자른다  →  뿌리내린다(물꽂이 12일)  →  판다
```

| | |
|---|---|
| 값 | **12,000원** (잎 1장 민무늬 · `sale_economy.md` §3 「몬스테라 삽수(일반)」) |
| 한 바퀴 | **14일** (병 배송 2 + 뿌리 12). 직삽이면 26일(24 + 2) |
| 원가 | 유리 수경병 **7,000원**. ★**팔면 돌아온다** — 병째 보내지 않으므로 두 번째부터 공짜 |
| 빈도 | **모주가 잎을 새로 낼 때만**. 반지하 창턱에서 잎 하나가 30~40일 |
| 90일 실측 | 삽수 1개 12,000원 − 병값 7,000원 = **순 5,000원** (하루 56원) |

★ **하루 지출 20,000원의 0.3%다.** `sale_economy.md` §0 의 *"반지하에서 식물을 팔아 흑자를
내는 건 불가능하다"* 가 숫자로 다시 확인된다. 꾸준수입은 **버티는 수단이 아니라 배우는 수단**이고,
이사 자금은 확정 무늬가 만든다.

## 4-3. 무한 증식을 막는 것 — 총량 하나

growth 는 **잘라낸 것을 모른다**(형태를 되돌릴 창구가 없다). 그래서 `cuttableNodes()` 는
잘린 뒤에도 안 잘린 그루를 계속 낸다. 코어가 총량으로 막는다:

```
잘라낸 잎의 합(pot.pendingCutLoss.leaves) ≤ 모주의 잎 수(leafStats().leaves)
```

`propagation.cuttableNow(S, nodes)` 가 이 부등식으로 목록을 거르고, `takeCutting` 이
자기 안에서 한 번 더 돌린다(호출부가 잊어도 안 샌다). 같은 마디를 두 번 자르는 것도 막는다.
초보에서는 **모주에 잎이 한 장은 남는다**(예전의 "예비혹이 남나"를 잎 수로 바꿨다 —
마디 개수로 세면 이미 잘린 자리가 거짓말을 한다).

## 4-4. 튜토 확정 무늬 — 어디에 두었나

정본은 `src/game/tutorial.js` §확정 무늬 주석이다. 요약:

> 튜토 중 · **배움 넷 완료** · **모주에서 삽수를 한 번 잘라 봤다** ·
> **지금 가진 것을 다 팔아도 150만에 못 닿는다** · 마지막으로 준 지 **12일** 지났다
> → 모주의 잎 한 장을 무늬로 친다(잎이 제일 적은 마디부터).

- **확률을 안 건드린다.** growth 의 `calcVarieProb` 은 그대로 돈다. 코어는
  `ts.varieGrant.nodeIds` 라는 자기 상태를 갖고 `tutorial.varieView()` 로 **덧씌워 읽는다.**
  `plant_grow.html` 은 한 글자도 안 바뀌었다.
- **12일 간격**은 `propagation.METHODS.water.rootDays` 와 같은 수다 —
  받은 무늬를 잘라 뿌리내려 파는 데 걸리는 그 시간. 하루에 한 장씩 주면 튜토가 **이틀**에 끝났다.
- **"한 장만"이 아니라 "닿을 때까지"** 인 이유: 도착 개체의 잎이 두세 장이라 한 장만 무늬면
  v = 1/3~1/2 이고 값이 38만~83만에서 멈춘다. 한 장으로 걸었더니 **무늬는 났는데 못 나가는**
  판이 절반을 넘었다. 닿는 순간 멈추므로 수도꼭지가 아니다.
- **정식 모드에는 안 샌다** — 상태 자체가 튜토 것이고, 튜토가 꺼져 있거나 이사한 뒤에는
  판정도 덧씌우기도 통째로 지나간다. 재현 검사 H-2 가 셋을 다 확인한다.

## 4-5. 재현 결과 (시드 40판 · 수입 주입 없음) — **2026-08-03 가을 게이트 뒤**

| 경로 | 이사 성공 | 중앙값 | 최선 / 최악 | 계절 |
|---|---|---|---|---|
| **A** 등 없이 · 바로 삽수 | **100%** (40/40) | 튜토 **57일** | 57 / 81일 | 가을 40 |
| **B** 등 사고 · 바로 삽수 | **100%** (40/40) | 튜토 **57일** | 57 / 81일 | 가을 40 |
| **C** 한 박자 늦게(12일부터) | **100%** (40/40) | 튜토 **57일** | 57 / 81일 | 가을 40 |

> 게이트 전 수치는 A 83% · B 83% · C 93% 였고 **셋 다 여름 · 중앙값 13~27일**이었다.
> 올라간 이유는 잭팟이 세져서가 아니라, 예전에 낮았던 실패분이 **도착 무늬 33%가 만든 조기
> 이사**와 뒤엉켜 있었기 때문이다(아래 4-7). 그 우연을 걷어 내니 전부 같은 길로 흐른다.

대조군(확정 무늬를 걷어 내면) **0/20**. 삽수를 안 자르면(=조건 ③ 미달) **0/20**.
→ 확정 무늬가 이사를 만들고, 그 확정 무늬는 **삽수를 잘라 봐야** 온다.

### 돈이 버티는가 — 확정 수치를 하나도 안 바꾸고 잰 값 (중앙값)

| 튜토 일자 | A | B(등 25,000원) |
|---|---|---|
| 29일 (첫 월세 직전) | 779,300원 | 779,300원 |
| 30일 (첫 월세 −30만) | 474,300원 | 474,300원 |
| **45일 (가을 · 확정 무늬)** | **374,100원** | **349,100원** |
| 57일 (잭팟 판매·이사) | 259,200원 | 233,924원 |

잭팟 총액 중앙값 **1,476,000원**(무늬 잎 2장짜리 모주 1,464,000 + 민무늬 삽수 12,000).
여기에 잔액을 더해 150만을 넘는다. **확정 무늬가 오기 전에 파산한 판은 0/40** 이다 —
파산하면 병(7,000원)을 못 사서 자를 수가 없어지므로 그게 진짜 문턱인데, 그 앞에서 잭팟이 돈다.

## 4-6. ★ 해결 — 가을 게이트 (2026-08-03 박사님 확정)

> *"확정 무늬는 가을에 들어선 뒤부터 준다."*

`varieGrantCheck` 의 조건에 **계절 하나**를 더했다(`tutorial.varieGrantOpensDay`).
**확정 수치는 한 글자도 안 바꿨다** — 시작 자금 100만 · 하루 2만 · 월세 30만 · 유예 30일 ·
이사 150만 · 여름 45일차 시작 그대로다. 게이트 날짜(튜토 45일)도 새 수치가 아니라
`TUTORIAL_RULES`(여름 45일차 시작)와 `weather.js`(계절 90일)가 낸 값이다.

- 튜토가 여름 → **가을**을 지나므로 계절 전환·식물등 해금·삽수 회전을 전부 겪는다
- 경로 B 가 실제로 등을 산다(게이트 전에는 해금 전에 게임이 끝나 살 기회가 없었다)
- ⚠ **겨울까지 가는 판은 아직 없다.** 최악이 튜토 81일이고 겨울은 135일이라 닿지 않는다.
  `story_arc.md` §2 의 C(*"겨울을 맞는다"*)는 여전히 미달이고, 지금 C 는 A 와 결과가 같다
  (`startCutDay: 12` 가 45일 게이트 앞에서 아무 차이도 못 낸다). **plan 판단 대기.**

## 4-7. ★ 해결 — 도착 개체가 무늬를 달고 오던 것 (2026-08-03 · `plant_grow.html`)

40개 시드 중 **13개(33%)** 였다 → 지금 **0개**다. 원인이 **둘**이었고 둘 다 고쳤다.

| | 무엇이 틀렸나 | 고친 것 |
|---|---|---|
| ① | `calcVarieProb` 이 **빛 이력이 없으면** `varieProb`(0.20)을 그대로 돌려줬다 — "모르면 중립"이 곧 만땅이었다 | 이력이 없으면 **0**. `f_light.below_min` 이 0 인 것과 같은 판정이다 |
| ② | 무늬 굴림이 **다시 그릴 때마다 다시 돌았다.** rng 값은 잎마다 고정인데 문턱이 그날 빛으로 바뀌어, **어제 민무늬였던 잎이 오늘 무늬가 됐다** | `VARIE_STATE` 가 잎마다 한 번만 기억한다(`MAT_STATE` 와 같은 구조) |

★ **①만으로는 못 막는다.** 도착 다음 날 창턱 빛이 들어오는 순간 있던 잎이 소급해서
뒤집혔다(실측 seed 13: 도착 잎 3장 무늬 0장 → 하루 뒤 같은 3장에 무늬 1장).
그래서 그 판은 여전히 튜토 1일에 이사했다.

★ **생장 로직은 안 바뀌었다.** `rng()` 은 예전과 똑같이 같은 자리에서 같은 횟수로 뽑는다 —
난수 스트림도 마디·가지·잎의 형태도 불변이고, 바뀐 것은 *"그 굴림을 언제 확정하느냐"* 하나다.
재현이 그것을 고정한다(`test_banjiha_routes` 검사 0 · `test_cuttable` E·L · `test_maturation` A~M).

# 5. `game.html` 이 부를 공개 API

```js
import {
  catalogList, buyPriceOf, orderItem, pendingOrders, stockOf, incomingOf,
  shopStatus, priceOf, varieLeavesNeededFor, sellPot, sellCutting
} from './src/game/shop.js';
import { resowCrop } from './src/game/state.js';

catalogList();                       // 상점 화면 목록 (id·ko·정가·살때·배송일)
orderItem(S, 'bean_seed', 3, {log}); // 결제 즉시 · 1일 뒤 도착. events 를 storyteller 에 넘긴다
shopStatus(S);                       // { stock, orders:[{...,daysLeft}], spentWon, earnedWon }

resowCrop(S, { sirus: 3, at: 'banjiha-dresser:1', slots });   // 씨앗·용기 재고를 쓴다

priceOf({ leaves, variegatedLeaves });          // "지금 팔면 얼마" 미리보기 (순수 함수)
varieLeavesNeededFor(1_500_000, { leaves });    // "몇 장 더 무늬여야 하나"
sellPot(S, { leaves, variegatedLeaves, log });  // ★잎 수는 growth 가 낸 값을 넘겨야 한다
sellCutting(S, cuttingId, { log });             // 삽수는 코어가 다 안다
// 배송·도착은 loop.nextDay 안에서 자동이다 — turn.shop 에 { events, arrived }
```

## ★ 삽수·확정 무늬 (2026-08-03 추가) — 화면이 **반드시** 거쳐야 하는 두 줄

```js
import { varieView } from './src/game/tutorial.js';
import { cuttableNow, cutBudgetOf, motherStatsNow, takeCutting } from './src/game/propagation.js';
import { sellCutting, sellPot, SELLABLE_CUTTING_STATUS } from './src/game/shop.js';

// ① growth 가 낸 날값
const raw = growth.cuttableNodes();      // [{nodeId, stem, leaves, variegatedLeaves, growthDays}]
const st  = growth.leafStats();          // {leaves, variegatedLeaves, matureLeaves, growthDays}

// ② ★ 코어의 확정 무늬를 덧씌우고, ③ ★ 잘라낸 만큼을 뺀다 — 둘 중 하나만 빼먹으면 값이 틀린다
const v      = varieView(S, { nodes: raw, stats: st });
const nodes  = cuttableNow(S, v.nodes);          // 지금 실제로 자를 수 있는 마디만
const mother = motherStatsNow(S, v.stats);       // 지금 모주에 남아 있는 잎 집계
const budget = cutBudgetOf(S, v.nodes);          // {motherLeaves, lostLeaves, leftLeaves, cutNodeIds}

takeCutting(S, { nodes, nodeId, container: 'jar', at, slots, log });  // ★용기 재고를 쓴다
sellCutting(S, cuttingId, { log });   // ★뿌리내린 것만 팔린다(SELLABLE_CUTTING_STATUS)
sellPot(S, { leaves: mother.leaves, variegatedLeaves: mother.variegatedLeaves, log });

// 확정 무늬는 하루 진행 안에서 자동이다 — turn.varieGrant = { granted, nodeId, detached, why }
// 이벤트 id 는 `varie_granted` 다(dialogue 가 붙일 자리).
```

★ 자르기 UI 는 `cuttableNow` 가 낸 목록만 보여 주면 된다. 왜 못 자르는지는
`propagation.cutBlockedReason(S, nodes, nodeId)` 가 한 줄로 낸다(회색 처리 사유).

## ⏸ growth 에 필요한 것 — `handoff/core-to-growth.md` 요청

- **`leafStats()`** → `{ leaves, variegatedLeaves }`. 값은 잎으로 매기는데 잎은 growth 소유다.
  이게 없으면 브라우저에서 **판매 화면을 못 연다**(코어가 잎을 지어내지 않기로 했다).
- **`cuttableNodes()`** — 이미 요청돼 있다(`propagation.md` §7-2). 마디별로
  `{nodeId, stem, leaves, variegatedLeaves, growthDays}` 가 나와야 무늬 삽수를 잘라 팔 수 있다.
- **판 뒤의 형태 정리** — `sellPot` 은 화분을 지우지만 growth 의 3D 는 코어가 못 지운다.
  반환값 `growthNeedsReset: true` 로 알린다. 화면이 정리해야 한다.

---

# 6. 관련

| | |
|---|---|
| 네 단계·세 경로 | `story_arc.md` |
| 가격 공식·삽수 | `propagation.md` §6 |
| 가격표·상점 마진 | `sale_economy.md` §3·§5 |
| 씨앗값·끼니 상한 | `food_economy.md` §3·§4 |
| 재현 | `tools/test_banjiha_routes.mjs` |

---

# ★★ 2026-08-04 값 개편 — 잎 한 장씩 매기고, 등급은 무늬 잎 **장수**가 정한다

⚠ 위 §1·§3 의 값(`× (1+60·v²)` · 「무늬 삽수 732,000원」)은 **이 절이 대체한다.**
그 절의 「⚠ 정본 안에서 `v` 가 두 뜻으로 쓰인다 — plan 판단 필요」가 여기서 결론이 났다:
**검산표 쪽(라벨 짙기)이 맞았고 본문 쪽(잎 비율)이 틀렸다.**

## 1. 새 공식

```
값 = 민무늬 잎 수 × 크기단가  +  무늬 잎 수 × 성체단가 × 등급배수
```

한국 알보몬 시장이 실제로 값을 부르는 방식 그대로다(2026-08-04 조사) —
`엽수 5엽 · 잎당 30만` 꼴로 **「잎 1장당」** 으로 부른다. 식테크 거품기 잎당 30만~80만,
2023년 ~5만으로 붕괴. 그래서 그루값을 하나의 곱이 아니라 **잎마다의 합**으로 매긴다.

이 형태가 박사님 지시 셋을 동시에 만족시킨다. 우연이 아니라 형태가 그렇다.

1. **잎 수에 우상향** — 잎이 한 장 늘면 값이 반드시 는다(비율이 안 들어간다)
2. **떼어 팔기가 이득일 수 없다** — 합이라 쪼개도 총합이 같고, 쪼개다 등급이 내려가면 준다
3. **무늬 삽수가 싸진다** — 잎 1장짜리 무늬 삽수 732,000 → **80,000원**(−89%)

### ★ 소품 하한 — `cutting` 단가는 잎당 웃돈이 아니라 **그루당 최저가**다

정본이 삽수 단가를 12,000 으로 둔 이유는 *"작아도 **최소 이만큼은** 받는다"* 였다.
그건 **하한**을 말하는 문장인데, 코드는 잎마다 붙는 2,000원 웃돈으로 읽고 있었다.
그래서 잎이 적을수록 잎당 값이 비쌌다 — 잎 비율 `v` 와 **같은 방향의 병**이 하나 더 있었다.
문장대로 하한으로 읽으면 잎당 값이 어디서나 같아진다.

### ★ 무늬 잎에는 크기 프리미엄을 안 먹인다

안 갈라 두면 경계에서 값이 꺾인다(실측: 잎2·무늬1 92,000 → 잎3·무늬1 86,667).
무늬 잎을 성체 단가로 고정하면 잎이 늘 때 더해지기만 하므로 그 자리가 원리적으로 사라진다.

## 2. 등급 — 실제 한국 시장 용어 (2026-08-04 조사)

이름을 지어내지 않고 한국 무늬식물 시장에서 실제로 쓰는 말을 가져왔다.
선호 순서도 시장이 매긴 그대로다 — **무지 < 산반 < 섹터 < 하프문**.

| 등급 | 조건(무늬 잎 장수) | 배수 | 이름의 뜻 · 배수의 출처 |
|---|---|---|---|
| **무지**(민무늬) | 0장 | ×1 | 무늬가 없다. 「무지 잎」은 알보의 값을 깎는 말 |
| **산반**(散斑) | 1장~ | **×8** | 붓으로 튀긴 듯 잘게 흩뿌려진 무늬(=마블). 초록·흰색이 고르게 섞여 **그루가 버틴다**는 것이 시장의 추천 사유. 배수 = 「몬스테라 삽수(알보) 80,000」 ÷ 성체 잎당 10,000 |
| **섹터** | 2장~ | ×320/9 ≈ 35.556 | 흰 부분이 큼직한 덩어리로 앉은 것. 배수 = 「포토스 희귀무늬 성체 800,000」 ÷ (3,750×6) |
| **하프문** | 3장~ | ×61 | 잎이 주맥에서 반으로 갈린 것. **식테크의 정점**이고 경매 최고가가 여기서 나온다. 배수 = 옛 공식의 상한 `1+60·1²` |

★ **배수 셋 다 이미 있던 값이다.** 새 숫자를 하나도 안 만들었다.
바뀐 것은 **그 배수가 언제 붙느냐**뿐 — 예전에는 잎 비율이 높으면 붙었고(그래서 잎 1장이
제일 유리했다), 이제는 **무늬 잎 장수**가 쌓여야 붙는다(그래서 잘 큰 그루라야 한다).

### ★ 고스트(전백)는 이 사다리에 없다

잎 전체가 흰 것을 한국 시장은 「고스트」라 부르는데, 값의 꼭대기가 아니라 **경고**로 읽는다 —
고스트 잎이 연달아 나면 성장점 안의 정상 세포가 죽었다는 뜻이고 그루가 결국 죽는다.
그래서 고스트는 **등급이 아니라 죽음**이고(`propagation.md` §키메라), 값 표에 아예 안 나온다.
★★ 이게 곧 값 쪽의 천장이다 — 「가장 흰 것이 가장 비싸다」가 아니므로 무늬를 끝까지
밀어붙이는 길이 값으로 보상받지 않는다.

### ★ 삽수(잎 1~2장)에는 등급을 안 붙인다 — 산반까지다

**삽수는 그 무늬가 유지될지 아직 아무도 모른다.** 원복할 수도, 고스트로 죽을 수도 있고
그 판정은 뿌리내린 뒤에 난다. 시장이 값을 쳐 주는 것은 **무늬가 굳은 것이 확인된 그루**다.
이 한 줄이 *"무늬 삽수 가격 자체를 낮춰"* 를 실제로 성립시킨다 — 없으면 잎 2장 전부 무늬인
삽수가 섹터 등급을 받아 854,400원이 되어 고친 병이 그대로 돌아온다. 지금은 160,000원이 상한이다.

⚠ 조사가 같이 알려 준 것: 이 이름들은 원래 **잎 한 장의 무늬 모양**을 가리키는 말이고,
그루 단위 축은 시장에서 「엽수 + 성장점 상태」로 따로 말한다. 그런데 코어는 잎 하나하나의
무늬 모양을 모른다 — growth 가 내주는 것은 `variegatedLeaves`(몇 장인가)뿐이다.
그래서 그 이름들을 **그루의 무늬가 어디까지 굳었나**에 붙여 쓴다.
잎마다 다른 무늬 모양을 값에 넣으려면 growth 가 잎별 무늬 종류를 내줘야 한다
(`handoff/core-to-growth.md` 에 요청). **지금 없는 것을 지어내지 않는다.**

## 3. 새 값표

| 잎수 | 무늬0 | 무늬1 | 무늬2 | 무늬3 | 전부 무늬 |
|---|---|---|---|---|---|
| 1 | 12,000 | 80,000 | — | — | 80,000 |
| 2 | 20,000 | 90,000 | 160,000 | — | 160,000 |
| 3 | 30,000 | 100,000 | 721,111 | **1,830,000** | 1,830,000 |
| 4 | 40,000 | 110,000 | 731,111 | 1,840,000 | 2,440,000 |
| 5 | 50,000 | 120,000 | 741,111 | 1,850,000 | 3,050,000 |
| 6 | 60,000 | 130,000 | 751,111 | 1,860,000 | 3,660,000 |
| 9 | 90,000 | 160,000 | 781,111 | 1,890,000 | 5,490,000 |
| 12 | 120,000 | 190,000 | 811,111 | 1,920,000 | 7,320,000 |

**모든 칸이 오른쪽으로도 아래로도 안 꺾인다.** 옛 표의 병(잎이 적을수록 비싸다)이 사라졌다.

### 안 바뀐 값 · 크게 바뀐 값

| | 옛 | 새 | |
|---|---|---|---|
| 민무늬 삽수 잎1 | 12,000 | **12,000** | 꾸준수입의 뼈대라 안 건드렸다 |
| 무늬 삽수 잎1 | 732,000 | **80,000** | −89% · 정본이 원래 적어 둔 「삽수(알보) 80,000」 그 값이다 |
| 무늬 삽수 잎2(전부) | 1,464,000 | **160,000** | −89% |
| 성체 잎3·무늬3 (튜토 잭팟) | 1,830,000 | **1,830,000** | ±0% · **잭팟이 그대로 성립한다** |
| 성체 잎12·전부 무늬 | 7,320,000 | **7,320,000** | ±0% · 값의 꼭대기를 안 올렸다 |
| 성체 잎5·무늬2 | 530,000 | 741,111 | +40% · **큰 그루가 비싸졌다** |

## 4. 떼어 팔기 vs 키워서 팔기 — 뒤집혔다

| 그루 | 통째로 | 잎1씩 떼어 | 차이(새) | 차이(옛) |
|---|---|---|---|---|
| 잎3·무늬3 | 1,830,000 | 240,000 | **−1,590,000** | +366,000 |
| 잎5·무늬2 | 741,111 | 196,000 | **−545,111** | +970,000 |
| 잎6·무늬3 | 1,860,000 | 276,000 | **−1,584,000** | +1,272,000 |
| 잎12·전부 무늬 | 7,320,000 | 960,000 | **−6,360,000** | +1,464,000 |

둘로 나누는 **모든** 방법을 다 해 봤을 때 최대 이득은 **4,000원**(잎2·민무늬 → 1+1)이고
용기값 7,000원보다 작다 — **실비를 못 넘기므로 실제로는 손해**다.
(하한을 0 으로 없애면 정확히 0 이 되지만 정본의 「몬스테라 삽수 12,000」이 깨지고
꾸준수입이 17% 준다. **정본을 지키면서 실질 이득을 없애는 쪽**을 골랐다.)

## 5. 튜토 잭팟은 그대로 성립한다

반지하 모주는 잎 3장이 한계다(tools/test_cuttable.mjs 실측).

| | 등급 | 값 | |
|---|---|---|---|
| 잎3·무늬0 | 무지 | 30,000 | |
| 잎3·무늬1 | 산반 | 100,000 | |
| 잎3·무늬2 | 섹터 | 721,111 | |
| 잎3·무늬3 | 하프문 | **1,830,000** | ★이사 자금(150만)에 닿는다 |

옛 공식의 같은 칸과 **한 푼도 다르지 않다.**

# ⛔⛔⛔ **정정 2026-08-24 — 「이사 자금(150만)」이 낡았다. 지금 «200만»이다**

```
이 문서            이사 자금 «1,500,000»  ⇒ 1,830,000 이면 «닿는다»
tutorial.js:59     moveOutCostWon: ★ «2,000,000»
⇒ ⛔ 1,830,000 «<» 2,000,000  —  ★ «안 닿는다»
```
> ## ★★★★ **값이 올랐는데 «역산이 안 따라왔다».**
⇒ ⚠ **그리고 1,830,000 은 「반지하 모주가 낼 수 있는 «최댓값»」이다**
  (이 문서 바로 위: *"반지하 모주는 잎 3장이 한계다"*).
⇒ ⇒ ★★★ **즉 «최고로 키워 팔아도» 이사 자금에 170,000원이 모자란다.**

## ⇒ ★★ 그래서 **「한 번에 만든다」가 «거의 한 번에»가 된다**
```
모주를 팔아 만드는 돈    1,830,000  (최상일 때)
모자란 것                  170,000  ⇒ ★ «남은 살림돈»이 메워야 한다
```
⇒ ★★★★ **그러면 「언제 파느냐」가 이사를 «가른다»:**
```
살림돈이 남아 있을 때 팔면   ✅ 닿는다
살림돈이 마른 뒤에 팔면      ⛔ 안 닿는다
```
⇒ ⇒ ★★ **그리고 [core] 판에서 «봉우리가 낮아진다»** — d40 737,699 → d100 413,300.
  ⇒ **살림돈이 줄어드는 동안 무늬는 «아직 안 여물었다»(그 판은 무늬 2장에서 멈췄다).**
  ⇒ ⇒ ★ **팔 것이 여물기 전에 지갑이 마르면 «타이밍이 지나간다».**
⇒ ⛔ **단정 안 한다** — 한 씨앗이다. **씨앗 표에서 「무늬가 3장에 닿은 판이 있나」를 본다.**

## ★★ 그리고 이것이 **어제 내 「낸 적」 결정을 «살린다»**
```
이사 둘째 조건을 «지금 있나»로 했다면
⇒ 모주를 파는 «순간» 무늬 잎이 0 이 되고 ⇒ ★ 문이 «닫힌다»
⇒ ⇒ 돈을 만들려고 판 그 행동이 문을 닫는다
```
⇒ ★★★ **어제는 「삽수를 자르면」만 보고 정했는데, «모주를 팔면»이 훨씬 큰 경우였다.**
  ⇒ **같은 답이 더 센 까닭을 얻었다.**

⚠ **아래 「확정 무늬가 잎 3장을 다 채우면 이사가 된다」는 «옛 값(150만)» 기준이다.**

확정 무늬가 잎 3장을 다 채우면 이사가 된다.
