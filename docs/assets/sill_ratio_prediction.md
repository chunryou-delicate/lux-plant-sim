# 창턱 휘도비 — **[core] 가 재기 «전»에 적는 예측** (2026-08-30 · leaf)

★ 이 문서는 **재기 전에** 썼다. [core] 수가 오면 **맞았는지 틀렸는지가 바로 갈린다.**
⛔ 맞춰서 고치지 않는다. 틀리면 틀렸다고 적는다.

## 왜 예측을 먼저 쓰나

내 눈 시험은 「썸네일 → 밝기×K → 배경 위에 얹기 → 그 크기로 줄이기」다.
그 줄이는 과정이 **화소를 섞는다** — 4×7 px 이면 거의 다 섞인 화소다.
⇒ ★ 그러니 **내가 넣어야 할 K 는 «섞이기 전» 값**이다. 「당겨서」가 그것이다.
⇒ ⇒ 「멀리서」 값은 **내 파이프라인이 맞는지 재는 자**다 — 내가 시늉낸 4×7 의 휘도가
  [core] 가 잰 멀리서 휘도와 맞으면, 섞임까지 제대로 흉내낸 것이다.

## ★ 예측 — 「멀리서 ÷ 당겨서」 휘도비

배경이 무엇이냐로 **방향이 뒤집힌다.** 둘 다 적는다.

| 배경 | K=0.5 | K=0.598 | K=0.7 | 예측 |
|---|---|---|---|---|
| 어두운 바닥 (휘도 58) | 0.907 | 0.917 | 0.906 | **0.91** — 멀리서가 **어두워** 보인다 |
| ★ 창턱 (휘도 **89**, 실측) | 1.044 | 1.038 | 1.033 | **1.04** — 멀리서가 **밝아** 보인다 |

⇒ ★ K 를 바꿔도 비가 거의 안 움직인다. **섞임이 먹는 양은 K 와 무관**하다는 뜻이다.
⇒ ★★ 창턱 배경(89)은 지어낸 값이 아니다 — `05_firstplay_done.png` 의 새순 «둘레»
  화소 1,314개를 재서 나온 값이다(잎·화분 뺀 것).

## ⇒ 그래서 **내 0.598 이 «위로» 치우쳐 있다**

```
내가 잰 0.598  ⇐ 「멀리서」 판에서, 밝은 창턱을 등지고, 화소 51개로
예측한 치우침   ⇐ ×1.038
⇒ ★ 섞이기 전 값은 0.598 ÷ 1.038 ≈ 0.576
```
⇒ ⚠ 앞서 「위로 치우쳤을 수 있다」고만 적었는데, **이제 «얼마나»까지 적는다 — 약 4%.**
⚠ 다만 4% 는 작다. **화소 51개라는 것이 더 큰 흠이다.**

## ★★ 그리고 — **45% 는 배경을 바꿔도 안 흔들린다**

처음에 창턱 시험을 **어두운 바닥 배경**으로 돌렸다. 틀린 배경이었다.
창턱 실측 배경(89)으로 다시 돌려 세니 **똑같이 5갈래 45%** 다.

| | 헷갈리는 갈래 (11 중) |
|---|---|
| 창턱 · 배경 어두움(내 첫 판, 틀림) | 5 · 45% |
| ★ 창턱 · 배경 실측 89 | **5 · 45%** |

⇒ ★ 그러니 **배경을 잘못 깔았어도 답은 안 바뀌었다.** 다행이지만 — ⚠ **운이었다.**
  ⇒ 배경을 재기 전에는 그것을 몰랐다. **모르고 맞은 것은 맞은 것이 아니다.**

## ⛔ 여전히 못 하는 것

- **화소 51개**로 잰 0.598 이 이 문서 전체의 밑이다. [core] 수가 오면 갈아 끼운다
- 「낮·맑음」 한 판이다
- 4×7 은 bbox 라 **위쪽 값**이다 ⇒ 진짜 잎은 더 작고, 그러면 **더 안 갈린다**

---

# ★★ 결과 — **예측을 «시험할 수 없었다». 내 시험 설계가 틀렸다** (2026-08-30 · [core] `86c9313`)

[core] 가 두 판을 찍어 줬다 — `docs/handoff/img/leafpx/sill_far_g165.png` · `sill_near_g165.png`
(창턱 `banjiha-sill:0` · 등 0개 · 생장 165일 · seed 92158 · `varieLeafKeys []` = **민무늬**).

## ⛔ 처음 재니 **1.859** 가 나왔다 — 예측 1.04 와 한참 멀다

그런데 **수를 내기 전에 왜 그런지 봤고, 내 재는 법이 틀린 것이었다:**

```
멀리서   n0.1.1 (269,373) 9×8   ⇒ 잎으로 고른 것이 «18화소». 그중 밝은 것만 걸렸다
당겨서   n0.1.1 (239,334) 148×138 ⇒ 잎 «2,443화소». 그늘진 데까지 다 들어갔다
⇒ ★ 같은 잣대가 아니다. 「제일 초록인 18개」와 「초록인 2,443개」를 견줬다
```

## ⇒ 그래서 **같은 크기로 통째 견줘 봤다** — 이번엔 «견줄 수가 없다»는 것이 나왔다

당겨서 판(148×138)을 9×8 로 줄여 멀리서 판(9×8)과 통째로 견줬다:

```
멀리서       RGB 109.5 112.5 104.3 · 휘도 111.3
당겨서→줄임  RGB  86.8  89.1  83.8 · 휘도  88.3
화소별 차 |ΔRGB| 평균 27.6 · 최대 147.0
```

⇒ ⛔ **눈으로 보니 둘이 «같은 데»를 안 담고 있다.** 멀리서 네모에는 «밝은 창턱»이 태반이고,
  당겨서 네모에는 «벽»이 태반이다. 둘레 휘도도 **121.8 대 84.6** 으로 다르다.
⇒ ★★ **내가 낸 예측은 「같은 잎 · 같은 배경 · 두 크기」였는데, 이 두 판은 그 조건이 아니다.**
⇒ ⇒ **그러니 내 예측은 «맞지도 틀리지도» 않았다. 시험이 성립하지 않았다 — 내 설계 잘못이다.**

⚠ 만약 1.859 를 그대로 냈으면 **「내 파이프라인이 크게 틀렸다」로 갔을 것**이다.
  ⇒ 반대로 우연히 1.04 가 나왔으면 **「맞았다」고 했을 것**이다. **둘 다 틀린 결론이다.**

## ★★★ 그래도 **정작 필요한 값은 깨끗이 나왔다**

당겨서 판은 잎이 2,443화소라 섞임이 거의 없다. 그것과 텍스처를 견주면 된다:

```
당겨서 잎 휘도            91.72   (2,443화소)
monstera_leaf_mid1 텍스처  177.6   (이미지 #1 = 잎 바탕)
⇒ ★★ 창턱 휘도비 = 0.516
```

| | 값 | 화소 | |
|---|---|---|---|
| 내 임시값 | 0.598 | **51** | 밝은 창턱을 등진 «섞인 화소» |
| ★ 실측 | **0.516** | **2,443** | 당겨서 판 |

⇒ ★ **방향은 맞았다** — 「위로 치우쳤다」. ⛔ **크기는 틀렸다** — 나는 «약 4%» 라 했는데 **16%** 다.

## ⇒ 그 값으로 다시 세니 — **45% 가 그대로다**

| 조건 | 헷갈리는 갈래 (11 중) |
|---|---|
| 창턱 0.598 · 배경 어두움(첫 판, 틀린 배경) | 5 · 45% |
| 창턱 0.598 · 배경 89 | 5 · 45% |
| ★ **창턱 0.516 · 배경 122 (둘 다 실측)** | ★ **5 · 45%** |

⇒ ★★ **세 번 다른 조건에서 같은 수가 나왔다.** 이 수는 «휘도비와 배경에 안 흔들린다».
  ⇒ 헷갈리는 다섯도 그대로다 — `cream_vein` · `marble_whitegreen` · `mint_dot` ·
    `mint_dot_34` · `silver_droplet`. **넷이 청록에 흰 점이다.**

## ⛔ 여전히 못 하는 것

- [core] 가 **「낮·맑음」을 확인 못 했다**고 밝혔다(`S.sim` 을 그 시점에 못 읽음). 한 판이다
- 4×7 · 9×8 은 bbox 라 **위쪽 값**이다 ⇒ 진짜 잎은 더 작고, 그러면 **더 안 갈린다**
- 늘어놓고 봤다 ⇒ 게임에선 한 장만 본다 ⇒ **더 안 갈린다**
