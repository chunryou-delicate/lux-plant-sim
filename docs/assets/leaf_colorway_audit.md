# 잎 색변이 72장 훑기 — **내가 만든 것을 내가 다 안 봤다**

> 2026-08-23 · [leaf] · 총괄 지시.
> 계기: `mid2_v2` 가 갈색인 것을 다른 일 하다 우연히 봤고, 그때 이렇게 적었다 —
> *"36갈래 × 2 = 72장을 만들면서 **전부 눈으로 보지는 않았다.** 지금 아는 것은 이 한 장뿐이다."*
>
> ⇒ 그래서 **72장을 다 봤다.** 크레딧 0. 이미 다 찍혀 있었다.
> ⚠ **고치지 않았다.** 세고 갈라서 넘긴다.

## 무엇을 만들었던 건가

갈래마다 판이 셋이다 — **기본** · **`_v1`(쨍)** · **`_v2`(차분)**.
`_v1`·`_v2` 는 내가 채도·색상을 옮겨 만든 것이다. **36갈래(성숙 20 · 중간 16) × 2 = 72장.**

---

## 결과 — **6장은 못 쓴다 · 3장은 애매 · 63장은 쓸 만하다**

### ❌ 못 쓴다 — 6장. **전부 `_v2`(차분)다**

| 파일 | 무엇이 됐나 |
|---|---|
| `heart_lime_2672_0_v2` | 겨자·황토색. **시든 잎** |
| `mon_neon_lime_v2` | **벽돌 갈색.** 죽은 잎 |
| `monstera_leaf_mid2_v2` | **갈색.** 죽은 잎 (먼저 찾았던 것) |
| `mon_variegata_gold_v2` | 칙칙한 올리브 + **빨간 무늬**. 병든 잎 |
| `mon_green_yellow_v2` | 초록 바탕에 **주황·빨강 줄무늬**. 잎에 빨간 줄은 병이다 |
| `pothos_marble_greenyellow_v2` | 마블이 **구리·갈색**. 시든 느낌 |

### ⚠ 애매 — 3장. **사람이 정해야 한다**

| 파일 | 왜 애매한가 |
|---|---|
| `heart_albo_2672_3_v1` | 베이지·살구색. **흙색 같기도 하고**, 알보의 「크림」으로 볼 수도 있다 |
| `mon_green_lemonpatch_v2` | 노란 패치가 **주황·빨강**이 됐다. 단풍으로 우기면 우겨진다 |
| `mon_star_greenyellow_v2` | 초록에 **빨간 조각**. 위와 같은 종류 |

### ✅ 나머지 63장 — 쓸 만하다
자주(`mauve`)·분홍(`rose_pink`·`variegata_pink`·`star_pinkmint`)·검정(`charcoal`)은
**원래 그 색인 품종**이라 붉거나 어두워도 정상이다.

---

## ★ 이 훑기에서 배운 것 — **숫자가 셋을 놓쳤다**

숫자로 먼저 좁혔다: 잎 픽셀의 **평균 색상**이 원본에서 갈색대(10~55도)로 옮겨간 것.

```
숫자가 고른 것          8 장
  그중 진짜였던 것      4 장   ← 눈으로 보니 넷은 멀쩡했다(원래 분홍·자주 품종)
숫자가 못 본 것         3 장   ← ★ 눈이 찾았다
```

**숫자가 놓친 셋** — `mon_green_yellow_v2` · `pothos_marble_greenyellow_v2` · `mon_green_lemonpatch_v2`.
⇒ **왜 놓쳤나**: 이 잎들은 **바탕이 짙은 초록**이고 **무늬만 작게** 붉어졌다.
  평균을 내면 **넓은 초록이 좁은 빨강을 덮는다.** 평균은 안 움직인다.
  ⇒ ★ **평균은 「작은 조각이 망가진 것」을 못 잡는다.**

**숫자가 헛으로 고른 넷** — 원본이 이미 분홍·자주인 품종이다.
⇒ 처음에 「초록 띠 밖」으로 걸렀더니 **자주 잎이 전부 걸렸다.** 자를 두 번 고쳐야 했다.

⇒ **총괄이 시킨 순서가 맞았다** — *"숫자로 좁히고, 눈으로 판정하라. 숫자만으로 「고칠 것」에 올리지 마라."*

---

## 진단 — **「차분(`_v2`)」이 연두·노랑을 갈색으로 민다**

못 쓰는 6장이 **전부 `_v2`** 이고, **전부 원본에 연두·노랑이 있다.**

| 원본 색상 | `_v2` 결과 |
|---|---|
| `mon_variegata_gold` 72도 | → 35도 (갈색대) |
| `monstera_leaf_mid2` 74도 | → 24도 |
| `mon_neon_lime` 75도 | → 23도 |
| `heart_lime_2672_0` 100도 | → 48도 |

⚠ **다만 「연두면 반드시 그렇다」는 아니다.** 연두대(60~110도) `_v2` 가 12장인데
그중 **4장만** 갈색이 됐다. 나머지 8장은 멀쩡하다(`pothos_whitegreen_29` 89→153 등).
⇒ **원인을 다 안다고 쓰지 않는다.** 아는 것은 **「못 쓰는 것은 전부 연두 계열의 차분판」**까지다.

---

## 넘길 것

- [ ] **못 쓰는 6장 · 애매한 3장을 어떻게 할지** — 지우나, 다시 만드나, 그냥 두나.
      ⚠ **내가 안 정한다.** 이 판들이 `varie_grades.json` 에 불리고 있는지는 [Plan] 이 안다
- [ ] 다시 만든다면 **「차분」 처리를 연두 계열에만 다르게** 걸어야 한다 [추론]
- [ ] `pothos_cream_marble` 방향 문제는 **다른 건**이다(`mid_leaf_variety.md` 참조). [core] 로 갔다

## 못 본 것

- **잎만 봤다.** 화분·조명·가구의 색변이는 안 봤다 — 있는지도 확인 안 했다
- **썸네일로 봤다.** 게임 화면에서 조명을 받으면 다르게 보일 수 있다
- **작은 무늬 조각**은 150px 썸네일에서 놓쳤을 수 있다. 6장은 **크게 놓고** 다시 봤지만
  「쓸 만하다」로 넘긴 63장은 **작은 판으로만** 봤다

---

# 2026-08-23 · **여덟 장을 다시 만들었다** — 그리고 원인이 드러났다

[Plan] 실측: **못 쓸 아홉 중 여덟이 실제로 게임에 불리고 있었다**(값이 붙는 성숙잎에 다섯).
그리고 [Plan] 이 *"빼면 갈래 수가 바뀌고, 갈래 수가 바뀌면 뽑는 확률이 바뀐다 — 그건 밸런스 변경"* 이라
**뺄 수 없다**고 했다. ⇒ **다시 만드는 것이 유일한 길이었다.** 크레딧 0.

## ★ 원인 — 「차분」이 **색상을 −40도 돌린다**

텍스처를 꺼내 원본과 견주어 쟀다. 네 갈래에서 값이 같았다:

| | 색상 | 채도 | 밝기 |
|---|---|---|---|
| **차분 `_v2`** | **−40°** | ×0.55 | ×0.96 |
| 쨍 `_v1` | +18° | ×1.55 | ×1.06 |

```
진초록 140° − 40 = 100°  → 여전히 초록      ✅
연두    75° − 40 =  35°  → 주황·갈색        ❌
```
⇒ ★ **연두만 삭은 까닭이 이것이다.** 앞서 *"연두대 12장 중 4장만 삭았다"* 며
  원인을 안 적었는데, **이제 안다** — 넷은 −40 이 삭은 띠에 떨어졌고 여덟은 안 떨어졌다.
  ⇒ **그때 지어내지 않은 것이 맞았다.**

## 자를 **세 번** 고쳤다 — 지우지 않고 적는다

| 판 | 무엇을 했나 | 무엇이 틀렸나 |
|---|---|---|
| **1판** | 삭은 띠(10~55°)로 갈 화소만 **반대로(+40°)** 돌린다 | 잎 전체가 연두인 것엔 맞았다. 그런데 **금 무늬 조각이 겨자로 삭았다** — 금을 갈색과 한 덩어리로 묶었다 |
| **2판** | 금(40~70°)은 지키고 채도만 살짝 뺀다 | 무늬는 살았는데 **잎 전체가 연두인 것이 통째로 「금」으로 잡혀 아무것도 안 바뀌었다** |
| **3판** | 면적으로 가른다 — 금이 넓으면 바탕이니 돌린다 | `mon_variegata_gold` 이 **빨강**이 됐다 |

⇒ ★★ **한 자로 다 안 된다. 여덟 장이면 갈래마다 골라 주는 것이 맞다.**
  `tools/leaf/recolor_calm.py` 의 `PICK` 표가 그것이고, **눈으로 보고 골랐다.**

| 갈래 | 고른 판 | 왜 |
|---|---|---|
| `mon_neon_lime` · `pothos_marble_greenyellow` | **1** | 2·3판은 잎 전체가 「금」으로 잡혀 안 바뀐다 |
| `mon_variegata_gold` | **2** | 금 패치가 넓다. 3판은 빨강이 된다 |
| 나머지 다섯 | **3** | |

★ **금은 이 게임의 정식 잎 색이다**(`variegata_gold` · `lemonpatch` · `greenyellow`).
  **겨자는 채도가 빠진 금이다.** 그래서 금은 **덜 빼야** 한다 — 1판이 여기서 틀렸다.
  ([Plan] 이 그은 금과 같다: *로즈·핑크·마젠타 = 꽃 같은 색 ✅ / 벽돌·녹·주황·겨자·갈색 = 삭은 색 ❌*)

## 결과 — 여덟 장 다 봤다

`heart_lime_2672_0` · `mon_neon_lime` · `monstera_leaf_mid2` · `mon_variegata_gold` ·
`mon_green_yellow` · `pothos_marble_greenyellow` · `mon_star_greenyellow` · `mon_green_lemonpatch`

⇒ **여덟 다 삭은 색에서 벗어났다.** 차분판다운 낮은 채도이면서 초록·금을 지킨다.
⚠ **`mon_star_greenyellow` 이 경계다** — 올리브·금 조각이 좀 탁하다. 빨강은 없고 원본이
  본래 금 조각 잎이라 계열 안이라고 봤다. **화면에서 다시 볼 때 이 한 장을 먼저 보라.**

## 아직 아닌 것

- [ ] **`heart_albo_2672_3_v1`** — 총괄은 「고치라」, [Plan] 은 「씀」으로 **지시가 엇갈렸다.**
      **안 건드렸다.** 물어 두었다
- [ ] **화면에서 안 봤다.** 썸네일로만 봤다. [core] 가 원화를 붙일 때 같이 본다
- [ ] `_v1`(쨍) 판은 **손대지 않았다.** 이번 여덟은 전부 `_v2` 다

---

# 2026-08-23 · `_v1`(쨍) 36장 · 그리고 **잎이 아닌 것**

## ⓵ ★★ 다음에 색변이를 만들 사람은 **이 줄부터 읽어라**

**색상을 돌리면 「경계 근처의 색」이 넘어간다. 어느 쪽으로 돌리든 당하는 것은 경계에 선 색이다.**

| | 무엇을 하나 | 무엇이 넘어갔나 |
|---|---|---|
| **차분 `_v2`** | 색상 **−40°** · 채도 ×0.55 | **연두(72~100°) → 주황·갈색.** 진초록(140°)은 멀쩡 |
| **쨍 `_v1`** | 색상 **+18°** · 채도 ×1.55 | **로즈(≈350°) → 빨강.** 그리고 **거의 흰 화소 → 베이지·탄색** |

⇒ **같은 병이 양쪽으로 났다.** 방향만 반대다.
⇒ 그래서 **처방도 같은 결이다 — 경계 근처는 덜 돌리거나 안 돌린다.**
```
_v1 에 이미 넣었다:  채도 s < 0.18 인 화소는 색상을 안 돌리고 채도도 ×1.12 만
_v2 에 이미 넣었다:  −40 이 삭은 띠(10~55°)로 떨어지면 +40 으로 돌린다
```
⚠ **`_v1` 의 로즈 → 빨강은 아직 안 고쳤다**(`mon_star_pinkmint_v1`). 판단거리로 올렸다.

## ⓶ `_v1` 36장 — 둘만 고쳤다

★ 문제는 **튀는 것이 아니라 흰 무늬가 탄색이 되는 것**이었다.
이름이 `*white`·`*silver` 인 잎이 **「흰 데가 없는 잎」**이 됐다.

| | |
|---|---|
| ✅ 고침 | `pothos_whitegreen_29_v1` (그을린 갈색 사라짐) · `mon_star_pinkmint_v1` (탄색은 뺐으나 **아직 붉다**) |
| ⏸ 안 고침 9장 | `*cream`·`*silver` 계열이 따뜻해진 것. **크림 무늬로 읽을 여지가 있다** — 실제 알보 몬스테라는 흰색이 아니라 크림이다. **내가 정할 것이 아니다** |

## ⓷ ★ 잎이 아닌 것 — **찾았다. 그리고 잎의 금이 안 맞는다**

앞서 *"화분·조명·가구의 색변이는 있는지도 확인 안 했다. 모른다"* 로 비워 둔 자리를 채운다.

**있다. 6갈래 12장 (+`lamp_desk_mint` 1장).** 규약이 잎과 다르다 — **`_c1`(민트) · `_c2`(핑크)**.

| 갈래 | `_c1` | `_c2` | 봄 |
|---|---|---|---|
| `pot_concrete_round` | 민트 | 로즈 | ✅ |
| `pot_macrame_hanging` | 민트 | 연분홍 | ✅ |
| `pot_terracotta_wood` | 민트 | 로즈 | ✅ |
| `lamp_desk_mint` | **크림** | 분홍(갓 안쪽 빨강) | ⚠ |
| `lamp_clip` | **크림** | 분홍 | ⚠ |
| `lamp_table` | **베이지** | 로즈 | ⚠ |

### ⇒ **「삭은 색」 문제는 없다.** 잎의 금이 여기 안 맞는다

⚠ **물건은 갈색·흙색이 정상이다.** 화분이 테라코타인 것은 병이 아니라 제 색이다.
⇒ **「삭은 색」이라는 잣대 자체가 물건에는 성립하지 않는다.** 잎의 금을 그대로 들고 오면 안 된다.

### ⇒ 대신 걸린 것 — **조명 셋의 `_c1` 이 민트가 아니다**

규약은 `_c1` = 민트인데 **조명 셋은 크림·베이지**다. 화분 셋은 제대로 민트다.
⇒ [Plan] 이 준 신호와 같다 — ***"이름과 색이 어긋나는 것 자체가 신호"***.
⚠ **다만 이것이 흠인지 모른다.** 따뜻한 크림 조명이 **일부러 그런 것**일 수 있다.
  그림만 보고는 못 정한다. **판단거리로 올린다.**

### ⚠ 못 본 것

- **가구·소품·집 모듈에는 색변이가 아예 없다**(GLB 7·4·4 중 0장). 확인했다
- **방에 같이 놓고 안 봤다.** 화분·조명은 **어울리나**가 색 자체보다 중요한데,
  그건 **화면에서만** 갈린다. 썸네일로는 못 정한다
- `assets/gen/2026-08-12/pots/` 의 **2D 원화 색변이**(`01c1_…`·`01c2_…`)는 안 봤다.
  아직 3D 가 없어 게임에 안 나온다

---

# ★★ 2026-08-23 · **옮기기 vs 칠하기** — 다음에 색변이를 만들 사람이 먼저 고를 것

화분 색 판을 만들다 **잎과 화분이 서로 다른 방법을 쓰고 있었다**는 것을 알았다.
그 차이가 오늘 잎에서 종일 헤맨 까닭이다.

| | **옮기기** (잎 `_v1`·`_v2`) | **칠하기** (화분 `_c1`·`_c2`) |
|---|---|---|
| 하는 일 | 원래 색을 **각도만큼 돌린다** | **한 색으로 덮는다.** 밝기 무늬만 남긴다 |
| 바탕을 따지나 | **딴다.** 바탕이 어디 있느냐로 결과가 갈린다 | **안 딴다.** 테라코타(19°)든 회백(151°)이든 결과가 같다 |
| 무너지는 자리 | ★ **경계에 선 색.** 연두 −40° → 갈색 · 로즈 +18° → 빨강 | 없다 — 결과가 한 색이라 넘어갈 데가 없다 |
| 색이 고른가 | ⚠ **안 고르다** | ✅ 고르다 |
| 무늬 | 색 대비가 남는다 | ⚠ **색 대비가 사라지고 명암만 남는다** |

## 재 본 것 — 라임 잎 한 장 (민무늬)

같은 자리(색상 122 · 채도 0.38 · 밝기 0.81)로 맞춰 놓고 견주었다:

| | 색상 | 채도 | 밝기 | **색상 흔들림** |
|---|---|---|---|---|
| 기본 | 91 | 0.696 | 0.844 | 4.8 |
| **옮기기** `_v2` (오늘 만든 것) | 122 | 0.382 | 0.809 | ★ **22.7** |
| **칠하기** (시험) | 122 | 0.380 | 0.809 | ★ **0.6** |

⇒ ★★ **옮기기가 색을 두 무리로 갈라 놓는다.** 「삭은 띠로 밀리면 반대로 돌린다」는 내 규칙이
  어떤 화소는 −40°, 어떤 화소는 +40° 로 보내기 때문이다. **평균은 같은데 흩어짐이 5배다.**
⇒ 텍스처를 눈으로 보면 **옮기기 판에 베이지·탄색 얼룩**이 섞여 있다. **칠하기는 깨끗하다.**
⚠ **그 얼룩이 3D 에서 보이는지는 아직 모른다.** 썸네일에서는 안 보였다 —
  뒷면·줄기처럼 안 보이는 자리에 몰려 있을 수 있다. **브라우저 차례가 오면 확인한다.**

## ⚠ 그런데 **무늬 잎에서는 이야기가 다르다** — `pothos_marble_greenyellow` 로 재 봤다

| | 색상 흔들림 | 채도 흔들림 | **밝기 흔들림** |
|---|---|---|---|
| 기본 (노랑 무늬 + 초록 바탕) | 28.6 | 0.082 | 0.111 |
| 옮기기 `_v2` | 11.8 | 0.045 | 0.106 |
| **칠하기** (시험) | **0.3** | **0.002** | **0.116** |

⇒ ★ **무늬가 사라지지는 않는다.** 밝기 흔들림이 그대로라(0.111 → 0.116) **무늬는 남는다.**
⇒ ⚠ **다만 「색 무늬」가 「명암 무늬」로 바뀐다.**
  기본은 **노란 무늬 + 초록 바탕**(두 색)인데, 칠하기는 **한 색의 밝고 어두움**만 남는다.
  무늬가 **보이기는 하는데 「연두 무늬」가 아니라 「밝은 초록 무늬」**가 된다.

## ⇒ 그래서 다음에 쓸 방법

| 무엇을 만드나 | 권함 | 까닭 |
|---|---|---|
| **민무늬 잎**의 색 판 | ★ **칠하기** | 얼룩이 안 생기고 색이 고르다 |
| **무늬 잎**의 색 판 | ⚠ **사람이 정한다** | 「차분판」의 뜻이 **「색이 죽는 것」이면 칠하기**, **「색이 부드러워지는 것」이면 옮기기**다. 그건 기획이다 |
| **화분·소품** | ★ **칠하기** | 이미 그렇게 하고 있고 잘 돈다 |

⚠ **오늘 고친 여덟은 안 건드렸다.** 「쓸 만하다」로 판정됐고 다시 만들 이유가 없다.
  이 시험은 **임시 파일**로만 했고 저장소에 안 넣었다.

## ⚠ 이 시험이 못 말하는 것

- **텍스처로만 봤다.** 3D 로는 안 봤다. 브라우저가 다른 창 차례다
- **두 장으로 잰 것**이다(민무늬 하나 · 무늬 하나). **그 둘까지만 참이다**
- 칠하기의 **색을 무엇으로 고를지**는 안 정했다. 화분은 민트·핑크로 정해져 있는데
  **잎은 정해진 색이 없다.** 그것부터 정해야 방법을 고를 수 있다
