# `assets/gen/2026-08-12/` 유령 판정 — **유령 0개다. 전량이 「아직 안 붙음」이다**

> 2026-08-23 · [leaf] · 총괄이 [Char] 의 `tools/find_ghost_assets.py` 결과를 넘겨 판정을 청했다.
> [Char] 말: *"「유령」과 「아직 안 붙음」을 밖에서 구분할 수 없다 — **만든 사람만 안다.**"*
> 맞다. 그래서 만든 쪽이 적는다.

## 판정

| | |
|---|---|
| **유령** | **0 개** |
| **아직 안 붙음** | **206 개 (전량)** |
| 애매함 | 0 개 |

**지울 것이 없다.**

## 근거 — 이 폴더는 **게임 에셋이 아니라 원료**다

뽑을 때 내가 `README.md` 첫 줄에 이렇게 적어 뒀다:

> **원화(2D)다. 게임에 바로 물리는 그림이 아니다.** 이 저장소의 흐름은
> `원화(2D) → Meshy 3D(GLB) → tools/glb_thumb 로 납작하게 찍기 → 게임` 이다

⇒ **코드가 안 부르는 것이 정상이다.** 부르면 그게 잘못이다.
  같은 README 의 「아직 안 한 것」에도 그대로 적혀 있다 —
  *"3D 변환(Meshy) · 썸네일 · **manifest 등록** · 상점/가방 배선"* — **하나도 안 했다.**

### 실측으로 뒷받침

| 재어 본 것 | 결과 |
|---|---|
| `manifest.json` 의 `items.path` 에 등록된 것 | **0 / 206** |
| `manifest.json` 본문 어디든 이름이 나오는 것 | **0 / 206** |
| 게임 코드(`src/` · `data/`)가 `gen/2026-08-12` 를 부르는 줄 | **0 줄** |

⇒ **하나도 안 붙어 있다. 절반이 아니라 전부다.**

## ⚠ 그래서 자의 결과 **양쪽이 다 어긋난다**

자는 이렇게 갈랐다: **유령 87 (42%) · 쓰임 88 (43%)**. **둘 다 이 폴더에는 안 맞는다.**

### ⓵ 「유령 87」 → 실은 **아직 안 붙음**
지우면 안 된다. **3D 로 만들 재료**다. Meshy 잔액이 4,502 라 이제 돌릴 수 있다.

### ⓶ ★ 「쓰임 88」 → **거의 다 헛걸림이다**
자가 **이름의 조각**으로 찾는데, 그 조각이 **문서·컨택시트·데이터 파일**에 우연히 들어 있다.

셋을 골라 직접 확인했다:
```
25_wateringcan.png     자: docs/byeot_asset_pipeline_master.md (이름 조각)
241_hydro_tower.png    자: tools/probe_stack2.mjs (이름 조각)
31_garden_gloves.png   자: data/plants.json (이름 조각)

  게임 코드(src/ · data/)에서 실제로 찾은 파일 수 :  0 · 0 · 0
```
⇒ **셋 다 게임이 안 쓴다.** 「물뿌리개」라는 **말**이 문서에 있을 뿐이다.
⇒ ★ **「쓰임」이 「유령」보다 위험하다.** 유령은 사람이 다시 보지만,
  **쓰임으로 갈리면 아무도 다시 안 본다.** 88개가 조용히 「붙어 있다」로 남는다.

## ★ [Char] 께 되먹임 셋 — `tools/find_ghost_assets.py`

**① ⚠ 이 콘솔에서 안 돈다 (진짜 버그)**
```
UnicodeEncodeError: 'cp949' codec can't encode character '\u2014'
```
`—` 를 찍다 죽는다. **`--selftest` 조차 못 돌린다.**
⇒ 우회: `PYTHONIOENCODING=utf-8 python tools/find_ghost_assets.py ...`
⇒ 고치려면 파일 첫머리에 `sys.stdout.reconfigure(encoding='utf-8')` 한 줄이면 된다.
  ⚠ **내가 안 고쳤다.** `tools/` 는 만든 창 소유다(team-map §2-1). 알려만 둔다.

**② 갈래에 「원료」가 없다**
이 자는 *"에셋은 코드가 불러야 정상"* 을 전제로 한다. **원료 폴더는 그 전제가 반대**다.
⇒ 제안: **폴더가 스스로 밝히게 하자.** `thumbs/index.json` 과 같은 규약이다 —
  **만든 쪽이 적고 읽는 쪽은 찾아본다.** 폴더에 표시가 있으면 자가 「원료」로 갈리고 넘어간다.
  그러면 **다음 창이 이 판정을 다시 안 해도 된다.**

**③ 자 검사가 이 사고를 못 잡는다**
`--selftest` 4건이 **전부 캐릭터 에셋**이다(GLB 3 · 초상화 1).
⇒ **2D 원료 한 건을 넣으면** 이 갈래 사고가 자 검사에서 잡힌다.
  [Char] 이 *"떨어질 수 없는 검사였다"* 며 고친 그 자리와 같은 종류다 —
  **검사에 없는 갈래는 검사가 못 잡는다.**

## 넘길 것

- [ ] **206장 중 무엇을 3D 로 만들지** — [Plan]·[core] 의 「이번 판에 쓰는 물건」 목록 대기 중.
      목록이 오면 그만큼만 돌린다. 48장 화분을 다 만들면 44장은 버리는 돈이다
- [ ] `assets/gen/` 아래 **다른 날짜 폴더**도 같은 상태인지 안 봤다. **모른다**
