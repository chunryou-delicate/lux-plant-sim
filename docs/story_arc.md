# 스토리 아크 — 정본

**2026-08-03 확정.** 박사님이 준 구조를 정본화한 것이다.

> 정본은 `GAME_PLAN.md`. 이 문서는 **스토리 모드 한 줄기**만 다룬다.
> 첫 플레이는 `first_play.md`, 시간 모드 전반은 `time_modes.md`, 경제는 `game_flow.md`.
>
> 이 문서가 생기면서 `first_play.md` §8 과 `time_modes.md` §9 의 **⏸ 보류가 풀린다.**
> 그 두 절은 방향만 있고 숫자가 없었다. 여기서 숫자를 박는다.

---

# 0. 네 단계

```
① 반지하 (튜토리얼)
     첫 플레이   시루 → 4턴 수확 → 식비 절감 → 몬스테라 도착 → 창턱 → 말린 새순
     그 뒤       계절이 흐르기 시작한다 · 식물등이 열린다 · 이사 자금을 모은다
        ↓ 경제 조건 + 학습 체크리스트 4개를 **둘 다** 채우면
② 탈출          원룸으로 실제 이사 → 반지하 튜토리얼 종료
        ↓
③ 원룸          갈라진 잎 · 무늬 · 번식(삽수)
        ↓
④ 엔딩          삽수를 팔아 **내 집 마련**
```

## ★ 스토리 모드 전체가 **초보**다

박사님 확정: *"튜토 끝나고 스토리 모드가 이어지는데 그 스토리 모드는 초보 모드인 거야. 금방 끝나는."*

여기서 갈리는 것이 둘이다.

| | 튜토(①) | 스토리 나머지(②~④) | 그 뒤(자유·고수) |
|---|---|---|---|
| **시간** | **이벤트 점핑** | 배속만. 이벤트 점핑 **없음** | 배속·1:1 |
| **잎 손실** | 없음 | **없음** (바랜 채로 유지) | 낙엽·고사 |

시간은 박사님 확정이다 — *"초보 이후부터는 스킵은 없어. 조금 시간이 빨리 지나가겠지만 이벤트까지 엄청 빠른 시간 진행은 안 되는 것으로."*
`loop.js` 의 `timeModeOf()` 가 이 둘을 가른다.

잎 손실도 같다 — *"초보 때는 바랜 잎은 바랜 상태 유지(죽지는 않음)."*
`growth_tuning.json` 의 `health.drop_enabled=false` 가 그것이고, **스토리 내내 꺼져 있다.**

## ★ 직업 선택은 **④ 엔딩 뒤에** 한다

박사님 확정: *"튜토 시에는 직업 선택이 없는 걸로. 어차피 빨리 끝나니까. 끝나고서는 제대로 직업 선택해서 하는 걸로."*
그리고 그 범위는 *"튜토 및 초보 엔딩까지"* 다.

```
①반지하 → ②탈출 → ③원룸 → ④내 집 마련 엔딩      ← 여기까지 자취생 고정. 직업 선택 없음
                                    ↓
                              본편 — 직업을 고르고 시작
```

`first_play.md` §0 의 *"캐릭터 자취생 고정"* 이 **첫 플레이만이 아니라 초보 스토리 전체**로 넓어진 것이다.

**왜 이게 맞는가.** 직업은 시작 자금·슬롯·수입을 가르는 축이다. 그걸 아무것도 모르는 상태에서 고르게 하면 **고른 줄도 모르고 고르게** 된다. 한 번 끝까지 가 보고 나면 *"나는 이렇게 하고 싶다"* 가 생기고, 그때 고르는 것이 선택이다.

덤으로 튜토가 단순해진다 — 자취생 하나만 밸런싱하면 되고, 캐릭터 조합마다 데드라인을 다시 재지 않아도 된다.

## ★ 빨리감기는 **하루가 지나가는 것이 보여야** 한다

박사님: *"빨리감기 시 창문을 통해 빛 들어오는 거라든지 색감이 바뀌게 해서 시간이 빨리 가는 게 딱 느껴지게."*

날짜 숫자만 올라가면 시간이 흐르는 감각이 없다. 하루(약 140ms)마다 **창으로 드는 빛이 아침→낮→저녁을 한 바퀴** 돌면, 12일이 1.7초에 지나가도 눈에는 **열두 번의 하루**로 보인다. 방 뷰의 `setDaylight(t01)` 이 그 자리다.

밤을 아주 어둡게 만들지는 않는다 — 화면이 까매지면 식물이 안 보여서 빨리감기의 볼거리가 사라진다.

---

# 1. 반지하 튜토리얼 종료 — 두 축을 함께

**돈만 모으면 자동으로 끝나는 구조는 피한다.** 그렇다고 돈을 빼지도 않는다.

```
경제 조건                        학습 체크리스트
  원룸 이사 자금 150만원           ☐ ① 콩나물 첫 수확 · 식비 절감 확인
  (보증금 + 첫 달 월세 + 이사비)    ☐ ② 콩나물을 어두운 자리에 두었다
                                  ☐ ③ 몬스테라를 높은 창가 자리에 두었다
                                  ☐ ④ 말린 새순을 보았다
        ↓ 둘 다 채우고
    원룸으로 실제 이사  →  반지하 튜토리얼 종료
```

**[취소]** `time_modes.md` §6-3 의 *"☐ (본편) 삽수 1개 판매"* — 이 조건에서 뺀다. 번식은 ③ 원룸부터다.

## ★ 체크리스트 판정 조건 — 새 신호를 만들지 않는다

넷 다 **코어가 이미 내는 값**으로 판정한다. 새 이벤트 체계를 만들면 그게 또 어긋난다.

| | 조건 | 판정 |
|---|---|---|
| ① | 첫 수확 · 식비 절감 | 수확 이벤트 발생 **그리고** 절감액 > 0 |
| ② | 콩나물을 어두운 자리에 | 수확 시 4일평균 DLI **≤ 0.3** (= 100% 품질 · 3끼) |
| ③ | 몬스테라를 높은 창가에 | 몬스테라 슬롯의 7일평균 **≥ min 3.0** 인 날이 있었다 |
| ④ | 말린 새순 | `phaseId === 'spear_furled'` 를 관측 |

★ **②가 "어두운 자리"인데 `≤ 0.3` 인 이유** — 콩나물은 빛을 받으면 초록이 되고 써진다(`food_economy.md`). 3끼가 나오는 구간이 곧 어두운 자리라, **품질이 곧 배치의 증거**다. 자리를 따로 검사하지 않는다.

★ **③이 자리 이름이 아니라 DLI 인 이유** — `banjiha-sill:0` 만 인정하면 다른 방·다른 슬롯에서 성립하지 않는다. 배운 것은 *"창턱"* 이 아니라 *"밝은 자리"* 다.

---

# 2. 계절 — 시작은 **여름 45일차**

`weather.js` 에 `DAYS_PER_SEASON = 90` 이 이미 있다. 새 시간 체계를 만들지 않고 그 위에 시작점만 얹는다.

```
게임 시작 = 여름 45일차
  Day 0~7…16  첫 플레이 — novice · 맑음 · 여름 고정 (계절 정지)
  ~Day 45     계절이 흐르기 시작한다. 아직 여름
  Day 45      ★가을 진입. 자연광이 줄기 시작한다 · 식물등이 열린다
  Day 135     겨울 진입
```

★**첫 플레이 길이는 고정이 아니다** (2026-08-03 실측 · `tools/probe_first_play_len.mjs`).
끝나는 조건은 날짜가 아니라 **말린 새순**(유효 생장일 146)이라, 자리를 얼마나 빨리
맞추느냐로 갈린다:

| 자리 | DLI | 수확·도착 | 말린 새순 |
|---|---|---|---|
| 창턱 `banjiha-sill:0` | 3.77 | Day 4 | **Day 7** |
| 선반 위칸 `banjiha-etagere:5` | 0.17 | Day 4 | **안 옴** |
| 서랍장 `banjiha-dresser:1` | 0.04 | Day 4 | **안 옴** |

즉 **최선 7일**, 헤매면 그만큼 길어지고, 계속 어두운 자리에 두면 **영영 안 끝난다.**
그게 이 구간이 가르치려는 것이라 맞다 — 날짜로 끊으면 배우기 전에 통과한다.
아래 16일은 **상한(느린 경로)** 으로 읽는다.

**왜 45일차인가.** 첫 플레이를 여름 고정으로 끝내고 나면 가을까지 **29~38일**이 남는다.
그 사이에 콩나물 경제가 돌기 시작하고, 가을이 오는 것을 **한 번은 겪는다.**
표준 진행(아래 §3)이면 **가을 안에 이사**하고, 늦으면 겨울을 맞는다.

0일차에서 시작하면 90일 내내 여름이라 **계절이 아예 안 나온다.** 그러면 식물등의 존재 이유도 같이 사라진다.

## 세 경로 — 셋 다 유효해야 한다

| 경로 | | |
|---|---|---|
| **A** | 식물등 없이 **가을 안에 이사** | 표준. 등을 안 사도 된다 |
| **B** | 식물등을 사고 **가을에 이사** | 성장이 빨라지는 대신 자금이 늦는다 |
| **C** | 진행이 늦어 **겨울을 맞는다** | 식물등이 **안전장치**가 된다 |

**한쪽이 명백히 우월하면 선택이 아니다.** 첫 통합 시뮬에서 셋을 나란히 돌려 확인한다.

---

# 3. 경제 — 기존 수치를 그대로 쓰고 **한 곳만 바꾼다**

`food_economy.md` · `game_flow.md` 에 이미 있는 값이다. 새로 만들지 않았다.

| | |
|---|---|
| 하루 식비 | **7,500원** (2,500 × 3끼) |
| 하루 지출 합 | **16,667원** (월 50만) |
| 반지하 월세 | **20만원** |
| 시작 자금 | **150만원** |
| 원룸 이사 자금 | **200만원** |

> ★★ **2026-08-09 박사님 확정으로 넷이 통째로 바뀌었다.** 위 표가 정본이다.
> 아래 본문에 남아 있는 옛 숫자(하루 20,000원 · 월세 30만 · 시작 100만 · 이사 150만)는
> **그때의 근거를 남기려고 안 지웠다.** 값은 이 표를 따른다.
>
> **원룸 살림도 같이 정해졌다** — 보증금 **100만** · 월세 **35만**(`data/balance/homes.json`).
> 박사님 원문: *"200 가지고 가서 보증 100 내고 첫달 35 선납하고선 65만원 시작하는 거지."*
> ⚠ **이사하면 월세가 20만 → 35만으로 오른다.** 이사는 상이면서 동시에 부담이다.
>
> **왜 200만인가** — 시작 자금을 150만으로 올린 순간 이사비 150만이 **첫날부터 참**이 되어
> 튜토가 게임 48일에 끝났다(가을·식물등·겨울이 통째로 건너뛰어졌다).
> 시작 자금과 이사비는 **같이 움직여야 하는 한 벌**이다.
> 그리고 200만은 **하프문 하나로 넘는 자리**다(잎 8장 중 무늬 3장 = 218만).
> 섹터(무늬 2장)는 77만이라 못 넘는다 — **마지막 한 장이 이사를 만든다.**

## ★ 바꾸는 것 하나 — **월세 첫 달 유예**

`game_flow.md` 가 이미 권고한 것이다. 시작 자금과 슬롯은 **캐릭터 정체성에 직접 닿아서**(*"없음"이 정체성이다*) 손대지 않고, 월세 유예는 **서사 장치**(집주인 사정 · 보증금 상계)로 자연스럽다.

효과는 계산돼 있다 — 데드라인 **73일**. 표준 진행이 여름 45일차 시작 기준으로 **가을 안에 끝난다**(§2 경로 A).

**[안 함]** `game_flow.md` 의 ㉱ *"이사비 150만 → 80만"* — plan 자신이 *"원룸 이사 실비와 어긋남"* 이라고 적었다. 월세 유예 하나로 성립하므로 목표를 낮출 이유가 없다.

---

# 4. 식물등 — **정답이 아니라 선택**

| | |
|---|---|
| 가격 | **25,000원** (`food_economy.md` 의 천장등 실측값) |
| 전기 | **23원/일** (12W × 12h × 160원/kWh — 실측) |
| 해금 | **가을 진입(Day 45)** |

**필수 구매품이 아니다.** 그래서 선택이 생긴다 —

> 식물등을 사서 지금 방을 보완할 것인가, 그 돈을 이사 자금으로 모을 것인가?

25,000원은 하루 지출(20,000원)보다 조금 크다. **하루치를 넘기지만 감당은 되는** 크기라 망설임이 생긴다. 전기 23원/일은 거의 공짜라, 부담은 **사는 순간 한 번**이고 그 뒤로는 없다.

★ 등이 자리를 이기지 못한다는 것은 이미 증명돼 있다 — 같은 등·같은 전기인데 **자리 하나로 162배**가 갈린다(`STATUS` core). 등을 사도 어두운 자리에 두면 90일 내내 아무 일도 안 일어난다. **그 대비가 이 게임의 뼈대**이므로 식물등이 그것을 덮으면 안 된다.

---

# 4-1. ③ 원룸 — 「받는 것」에서 「만드는 것」으로

**2026-08-05 추가.** 자세한 것은 **`docs/oneroom.md` 가 정본**이다. 여기는 줄기만 적는다.

## ★ 먼저 — **② 는 사건으로만 있었고 이동이 없었다**

`tutorial.moveOut(ts)` 이 하던 일은 이사비를 빼고 `ts.movedOut = true` 를 세우는 것뿐이었다.
`S.home.room` 은 그대로 `banjiha` 라서 조도도 슬롯도 화면도 전부 반지하였다.
`src/game/oneroom.js` 의 `moveIntoOneroom(S, io)` 이 그 문을 연다 —
판정과 돈은 여전히 `tutorial.moveOut` 이 갖고, 방과 자리만 옮긴다.

## ③ 에서 달라지는 것 셋

| | ①② 반지하 | ③ 원룸 |
|---|---|---|
| 확정 무늬(잭팟) | 있다 — 코어가 준다 | **없다.** `tutorial.varieGrant` 는 이사에서 끝난다 |
| 무늬가 나는 길 | 확정 지급 | **빛으로만.** `growth.calcVarieProb` 가 굴린다 |
| 갈라진 잎 | 반지하 창턱은 **등을 켜도** 문턱을 못 넘는다(4.80 · 등1 5.15 · 문턱 6.0) | ★ **이미 난다.** 원룸 창턱 넷이 등 없이 6.16~6.69 로 넘는다 — 아래 「★★★ story4」 |

★ 그래서 ③은 **튜토의 마지막 장을 빛으로 반복하는 구간**이다. 확정 무늬를 잘라 뿌리내려
파는 동작을 이미 한 번 해 봤고(§1), 이번에는 그 무늬를 자기가 만들어야 한다.
번식 규칙 자체는 새로 배우지 않는다 — `propagation.js` 가 그대로 돈다.

## ★ 초보 모드는 **④ 까지**다 — 코드가 ②에서 끄고 있었다

§0 이 못 박은 범위는 *"④내 집 마련 엔딩 ← 여기까지"* 인데,
`propagation.isNoviceMode` 는 `!ts.movedOut` 을 보고 있었다. 이사하는 순간 삽수 유예가
16일 → 8일로 줄고 모주를 끝내는 자르기가 열린다 — **죽지 않는다는 약속이 이사 버튼에서
깨지고 있었다.** 지금은 `S.story.ending.doneOnDay` 를 본다.

## ★ 도착 장면 — **2026-08-11 에 대사가 붙었다**

②가 사건으로만 있던 것처럼(위 §★), ③의 **첫 장면도 조용했다.**
`oneroom.moveIntoOneroom` 이 `moved_in_oneroom` 을 내고 `game.html` 이 그것을
`story.events(...)` 로 넘기는데 `dialogue.EVENT_SCRIPT` 에 그 id 가 없어서 조용히 지나갔다.
화면 실측(2026-08-11 · 로컬 서버로 게임 125일을 눌러 이사까지 감): 반지하 마지막 대사
*"가자. 다음 방은 창이 높대."* 뒤에 **아무 말도 없이 새 방이 떴다.**

`dialogue.SCRIPTS.movedInOneroom` 이 그 자리를 채운다.

## ★★ 2026-08-15 — **그 도착 대사의 세 줄이 사실과 달라서 다시 썼다**

2026-08-11 에 쓸 때 근거로 삼은 것이 아래 **§③ 표(2026-08-05)** 였다. 그 표는
`data/profiles/room_profile.*.json` **옛 파일**에서 나온 값인데, 바로 다음 날
**2026-08-06 `oneroomfix` 가 원룸에 창턱 4칸과 식물등 기구 2개를 넣었고**,
같은 무렵 `lampaim` 의 BACK_REFLECT 가 반지하 창턱을 올렸다. **표가 하루 만에 낡았다.**

이 저장소의 열세 번째 「재는 자」 사고이고, 모양은 §2 가 적어 둔 그대로다 —
**값이 아니라 근거가 낡았다.**

### 지금 재서 나온 값

| | |
|---|---|
| 켠 것 | `src/game/light_adapter.js` `createLightEngine` · `data/house_rooms.json` 현재본 · **게임이 화면에서 쓰는 바로 그 창**(`window.__io.light` 로도 같은 값을 확인) |
| 조건 | 맑음 · 여름 · **등 0개** · 판정 단위 **7일 이동평균**(peak × `weatherE('summer')` 0.643) |
| 끈 것 | 계절·날씨 굴리기. 게임은 `novice` 라 계수가 1.0 이므로 **이 값이 사철 값이다**(§5 ★★) |

| | 반지하 | 원룸 |
|---|---|---|
| 자리 (`room.slots`) | 14칸 | **15칸** |
| 제일 밝은 자리 7일평균 | **3.09** (`banjiha-sill:0`) | **4.30** (`oneroom-sill:1`) |
| `min 3.0` 을 넘는 칸 | **1칸** | **5칸** (창턱 4 + `oneroom-shelf:5` 3.15) |
| 무늬종 `min 4.2` 를 넘는 칸 | 0칸 | 3칸 |
| 갈라짐 `6.0` | 등0 0칸 · 등1 1칸 | 등0 **0칸** · 등1 **1칸**(6.95) |
| 콩나물 자리(7일평균 ≤ 0.3) | **9칸** | **1칸** (`oneroom-nightstand:0` · 0.00) |
| `room.slots[0]` (이사가 시루를 몰아 앉히는 자리) | `banjiha-sill:0` | **`oneroom-nightstand:0`** |

### 그래서 셋을 고쳤다

| 옛 줄 | 무엇이 틀렸나 | 지금 |
|---|---|---|
| *"줄었어"* (14 → 11칸) | **안 줄었다. 14 → 15 로 한 칸 늘었다.** 11 은 창턱 4칸이 붙기 전 값 | *"한 칸 더 많아. 그건 별거 아니고 — 밝은 자리가 늘었어."* |
| *"저 방엔 한 칸도 없었어. 여긴 한 칸 있어"* | 반지하도 창턱 한 칸은 `min` 을 넘는다(3.09). 진짜 차이는 **1칸 → 5칸** | *"저 방은 등 없이 자라는 자리가 창턱 하나였잖아. 여긴 다섯이야."* |
| *"월세는 서른다섯이 됐고"* | **지금 게임은 그 돈을 안 뗀다** — 아래 ⚠ | *"…대신 통장이 한 번에 얇아졌고." / "응. 이백만 원."* (실측: 소지금 3,000,000 → 1,000,000) |

⚠ **원룸 월세 35만이 코드에 안 걸려 있다** (2026-08-15 확인). `tutorial.TUTORIAL_RULES.oneroomRentWon`
이 `null` 이고 `game.html` 이 `oneroom.withOneroomRent` · `oneroom.oneroomRulesFromHomes` 를
**한 번도 안 부른다**(`homes.json` 자체를 안 읽는다). 그래서 `rentWonOf(ts)` 가 이사한 뒤에도
반지하 월세 **20만**을 낸다 — 화면 실측(원룸 24·54·84일)에서 `rentSoon` 이
*"이레 남았어. **이십만 원**."* 이라고 뜬다. 한 판 안에서 몬이가 서른다섯과 이십만을 같이 말하고 있었다.
**그 배선이 붙는 날 「20만 → 35만」 대목을 도착 대사에 되살린다** — 붙일 문장은
`docs/handoff/story3-to-plan.md` §판단필요①.

## ★★ 2026-08-15 — **③ 원룸 「안에서 사는 이야기」를 채웠다**

도착 장면은 2026-08-11 에 붙었지만 **그 뒤가 통째로 반지하였다.**
화면 실측(원룸에서 100일 · 말이 난 날 38일)에서 원룸에 뜬 작은 말이 **전부 반지하 것**이었고
그중 셋은 거짓말이었다 — *"**반지하는** 여름에 덥고 겨울에 춥다"*(원룸 4·40일째) ·
*"벽에서 눅눅한 냄새가 난다"*(7·43일째) · *"집주인 아저씨가 「학생, 아직 있었네」"*(27·76일째).
⇒ **원룸이 조용했던 게 아니라 저 방 얘기를 계속하고 있었다.**

새 사건 id 를 만들지 않았다. 코어가 원룸에서 내는 것은 `rent_soon`·`rent`·`season`·`broke`·
`plant_*` 뿐이고 전부 반지하와 같은 id 다(`winter_still`·`varie_granted` 는 `!ts.movedOut` 로 잠긴다).
⇒ **`chatterContext.movedOut` 하나로 갈리는 작은 말**로 채웠다. 아홉 가지다:

| | 무엇을 |
|---|---|
| `chatOneroomMorning` | **첫 아침** — 불을 안 켰는데 방이 밝다. 오프닝 *"…불도 잘 안 드는 방이네"* 가 여기서 닫힌다 |
| `chatOneroomDark` | ★ **밝아진 대가** — 콩나물 자리가 9칸 → 1칸 |
| `chatOneroomSiru` | 이사가 시루를 한 자리에 몰아 앉힌다(`reseatCrops` → `slots[0]` = 그 유일한 어두운 칸) |
| `chatOneroomBright` | 등 없이 자라는 자리가 1칸 → 5칸 |
| `chatOneroomWindow` | 미화하지 않는다 — 골목 안 2층이라 아직 하늘을 다 못 본다(`skyViewK` 1.18) |
| `chatOneroomEmpty` | 박스 네 개를 다 풀었는데 방이 안 찬다 |
| `chatOneroomLamp` / `chatOneroomNoLamp` | 산 등이 이사를 따라온다 · **갈라진 잎을 처음 예고**(자연광 4.30 은 못 넘고 등1 이면 6.95) |
| `chatOneroomVarie` | 확정 무늬가 끝났다는 것을 살림 속에서 한 번 더 |
| `chatOneroomWinter` | 원룸에서 맞는 첫 겨울(`winter_still` 이 `!movedOut` 로 잠겨 안 오는 자리) |

그리고 `winterCame` 에서 **반지하를 못 박은 두 줄을 뺐다.** 겨울은 튜토 135일에 오고 이사는
그보다 이르므로(화면 실측 튜토 78일쯤) **이 대사는 대개 원룸에서 뜬다.** 거기서
*"반지하는 겨울이 길어"* · *"…아직 못 나갔네"* 는 이미 나간 사람에게 하는 말이 된다.
반지하에서만 참인 말은 `winterStill`(`!ts.movedOut` 로 잠김)이 그대로 갖는다.

## ★★★ 2026-08-15 밤(story4) — **위 두 절의 자가 틀렸다. `novice` 는 0.643 을 안 곱한다**

열네 번째 「재는 자」 사고다. 이번엔 **표가 낡은 것이 아니라 단위가 틀렸다.**

바로 위 표의 머리글이 스스로 이렇게 적어 두었다 —
*"게임은 `novice` 라 계수가 1.0 이므로 이 값이 사철 값이다"*.
그런데 **같은 표가 `peak × weatherE('summer')(0.643)` 로 계산돼 있다.** 둘은 같이 참일 수 없다.

`src/game/state.js` §`SIM_MODES` 가 못 박은 것 —
> *"novice 날씨·계절 계수를 1.0으로 고정한다. **맑음·여름에 못박히므로 peak가 곧 실제값이
> 되고**, peak 금지 규약이 적용되지 않는다."*

**스토리는 `novice` 로만 돈다**(`game.html` 의 `newState` 세 곳 전부 `mode: 'novice'`).
⇒ 이 판에서 식물이 매일 받는 값은 **peak 그대로**다. `weatherE` 를 곱하면 안 된다.
그 0.643 은 `oneroom.lightGateOf` 와 `tools/test_oneroom_room.mjs` 가 쓰는 자이고,
**날씨가 굴러가는 판(`real`)의 기댓값**이다. 두 도구가 틀린 게 아니라 **스토리에 쓸 자가 아니다.**

### 실제로 굴려서 확인했다

실제 `loop.nextDay` · 실제 `light_adapter` · 실제 `newState({ mode:'novice' })` · 화분을 진짜로 놓고:

| 화분 자리 | 오늘 dli | `growthSpeed.band` | `slot.fenestrating` |
|---|---|---|---|
| `banjiha-sill:0` | **4.80** | `slow` ×1.0 | **false** |
| `oneroom-sill:1` | **6.69** | **`best` ×1.25** | **true** |

### novice 로 다시 센 표 (등 0개 · 맑음·여름 · 반올림 없음)

| | 반지하 | 원룸 |
|---|---|---|
| 자리 (`room.slots`) | 14칸 | 15칸 |
| 몬스테라가 **자라는** 칸 (≥ `min` 3.0) | **1칸** | **8칸** ← 5칸이 아니다 |
| `best` 대역 (5.0~11.0) ×1.25 | **0칸** | **4칸** (창턱 전부) |
| **갈라짐 문턱 6.0** 을 넘는 칸 | **0칸** | **4칸** (창턱 6.16 · 6.62 · 6.65 · 6.69) |
| 무늬종 `min` 4.2 를 넘는 칸 | 1칸 | **6칸** |
| 어두운 칸 (≤ 0.3 · 콩나물 자리) | 9칸 | 1칸 |

★ 위 §③ 표의 *"갈라진 잎 — ⏸ §미해결이 풀려야 난다"* 는 **이미 풀려 있었다.**
  원룸 창턱은 **등 없이** 갈라짐 문턱을 넘는다. 2026-08-06 `oneroomfix` 가 넣은 창턱 4칸이
  그 일을 했는데, 판정을 `avg7` 로 하는 바람에 아무도 그걸 몰랐다.

### 등은 「되냐 안 되냐」가 아니라 「가끔이냐 자주냐」다

`data/growth_tuning.json §mature_prob` 은 문턱 아래를 0 이 아니라 **바닥 0.1** 로 두고,
6.0 → `best_hi` 11.0 사이를 0.1 → 0.9 로 선형 보간한다(`plant_grow §calcMatureProb`).

| | 등 0 | 등 1 |
|---|---|---|
| 반지하 창턱 | 4.80 → **0.10**(바닥) | 5.15 → **0.10**(바닥) — 저 방은 등을 켜도 못 넘었다 |
| 원룸 창턱 | 6.69 → **0.21** | 10.81 → **0.87** |

⇒ 그래서 대사 셋을 다시 썼다(`dialogue.js`):

| 옛 줄 | 무엇이 틀렸나 | 지금 |
|---|---|---|
| `movedInOneroom` *"여긴 다섯이야"* | avg7 로 센 값. novice 로는 **여덟**(창턱 4 + 선반 4) | *"여긴 여덟이야."* |
| `chatOneroomBright` *"거기 넷하고 선반 위 하나 … 다섯이야"* | 같은 이유 | *"거기 넷하고 선반 위 넷 … 여덟이야."* |
| `chatOneroomLamp` *"창만으로는 안 되는 게 하나 남았어. 잎에 구멍이 나는 거."* | **거짓이다.** 창만으로 넘는다 | *"이 창턱은 창만으로도 잎에 구멍이 나는 밝기야."* + *"등은 그걸 **더 자주** 나게 해."* |
| `chatOneroomNoLamp` *"자라는 데까지는 그래. 그 위는 창이 못 하고."* | 같은 이유로 거짓 | *"됐어. 이 창턱은 등을 안 켜도 저 방 창턱보다 밝아."* |

⚠ `oneroom-shelf:6` 은 정확히 **3.0000** 이라 경계에 서 있다. 방 데이터가 조금이라도
  어두워지면 **여덟이 일곱이 된다.** 그 날 위 두 줄도 같이 고쳐야 한다.

⚠⚠ **판단필요 — `lightGateOf` 를 그대로 둘지.** 지금 이 함수는 판 모드를 안 보고 늘
  `weatherE` 를 곱한다. 스토리(novice)에서는 **있는 밝기를 없다고 말한다.** 다만
  `tools/test_oneroom_room.mjs` ④ 가 그 값 위에 서 있고 밸런스에 닿는 일이라 **안 건드렸다.**

## ★★ 2026-08-15 밤(story4) — **원룸 「한참 뒤」가 비어 있었다**

원룸을 150일 굴려 화면 글자를 받아 적어 보니 **+30일부터는 새 말이 하나도 안 나온다.**
말이 없는 것은 아니다(최장 침묵 2일) — 뜨는 것이 전부 되풀이다.
박사님이 짚으신 네 마디 중 **뒤 셋이 통째로 비어 있었다.** 다섯 가지를 새로 썼다:

| | 무엇을 | 조건 |
|---|---|---|
| `chatOneroomPotDark` | ★★ **이사가 몬스테라를 이 방에서 제일 어두운 칸에 내려놓는다.** `state.rehomePot` 이 갈 곳 잃은 화분을 `slots[0]` 으로 보내는데 원룸의 `slots[0]` 이 `oneroom-nightstand:0`(peak 0.00)이다. **화면 실측: 이사 나흘째에 `plant_stalled` 가 뜨고, 안 옮기면 영영 멈춰 있다** | `movedOut && blocked` · 30일 이내 |
| `chatOneroomHarvest` | 원룸에서의 **첫 수확** — 방은 다 바뀌었는데 이것만 그대로다 | `cropHarvested` · **14일 이내** |
| `chatOneroomMoney` | **살림이 빠듯할 때** — 이사비 이백만 원. ⚠ 월세 얘기는 안 한다(정본 35만 ≠ 코드 20만) | `cashWon < 30만` · 45일 이내 |
| `chatOneroomFenestrate` | ★★ **몬스테라가 달라질 때** — 창턱이 갈라짐 문턱을 넘는다. 저 방은 창턱에서도 못 넘었다 | **`turn.slot.fenestrating`** |
| `chatOneroomSettled` | **한참 뒤** — 저 방 얘기를 안 하게 됐다 | 60일 이상 |

★★ `chatOneroomFenestrate` 는 **숫자를 대사에 안 박고 조건에 넣었다.** 조도 계약이 오늘 내는
`turn.slot.fenestrating` 이 참일 때만 후보가 된다 — 방 데이터나 문턱이 바뀌면
**대사가 거짓이 되는 게 아니라 안 뜬다.** 낡지 않는 유일한 방법이라 여기서 처음 써 봤다.
⇒ 화면 확인: 화분을 창턱에 올린 판에서만 떴고, 이사가 놓은 어두운 칸에 둔 판에서는 안 떴다.

⚠ **`state.rehomePot` 은 안 고쳤다**(이 창 소유가 아니다). 몬스테라가 어두운 칸으로 가는 것은
  그대로다 — **말이 없던 것만 고쳤다.** 배선 판단은 `docs/handoff/story4-to-plan.md` §판단필요.

## ⏸ 미해결 — **지금 방 데이터로는 ③이 성립하지 않는다**

> ⚠⚠ **2026-08-15 — 아래 표는 낡았다. 다섯 줄 중 넷이 이미 풀렸다.**
> 2026-08-06 `oneroomfix` 가 창턱 4칸과 식물등 기구 2개를 넣었는데 이 표가 안 따라왔다.
> **지금 값은 위 §「2026-08-15 재서 나온 값」 표를 본다.** 아래는 2026-08-05 의 기록으로만 남긴다.
>
> | 아래 표가 말한 것 | 2026-08-15 실측 |
> |---|---|
> | 가장 밝은 자리 7일평균 **3.07** | **4.30** |
> | 갈라짐 문턱을 넘는 칸 **0칸** | 등0 0칸 · **등1 1칸**(6.95) — *"등으로 넘는 방"* 으로 설계가 확정됐다 |
> | 방에 달린 식물등 기구 **0개** | **2개**(바 + 집게 · 반지하와 같은 종류·순서) |
> | 슬롯 **11칸**(밝은 1 · 어두운 1) | **15칸**(`min 3.0` 넘는 칸 **5** · 어두운 칸 1) |
> | 헤드리스 프로파일이 안 열린다 | **못 확인했다.** 이 창은 `light_adapter` 쪽으로만 쟀다 |

재서 나온 것이다(`tools/test_oneroom.mjs` 검사 H-2 가 매번 다시 잰다).

| | 원룸 실측 | 필요 |
|---|---|---|
| 가장 밝은 자리 7일평균 | **3.07** (가을 2.00 · 겨울 1.14) | 갈라짐 6.0 · 무늬종 min 4.2 |
| 갈라짐 문턱을 넘는 칸 | **0칸** | 모주 2~3칸 |
| 방에 달린 식물등 기구 | **0개** | 반지하에서 산 등이 여기서도 켜져야 한다 |
| 슬롯 | **11칸** (밝은 칸 1 · 어두운 칸 1) | 밝은 20 · 어두운 4 (`propagation.md` §7) |
| 헤드리스 프로파일 | **안 열린다** (`uidStable` 없음) | 밸런스 시뮬이 원룸을 돌리려면 필요 |

★ `data/house_rooms.json` 은 house 소유라 코어가 못 고친다.
요청은 `docs/oneroom.md` §3 에 적어 두었다.

---

# 4-2. ④ 엔딩 — 삽수를 팔아 내 집 마련

**② 와 같은 모양으로 짰다.** 현금이 목표에 닿으면 버튼이 열리고, 누르면 그 돈이 나가고 끝난다.
플레이어가 이미 아는 동작이라 두 번째는 설명이 필요 없다.

| | ② 탈출 | ④ 엔딩 |
|---|---|---|
| 축 | 돈 **+ 학습 체크리스트 넷** | **돈 하나** |
| 판정 | 현금 | 현금 |
| 자동 종료 | 없다(버튼) | 없다(버튼) |

★ **④ 에 체크리스트를 안 붙인 이유.** ②는 가르치는 구간이라 *"돈만 모으면 자동으로 끝나는
구조를 피한다"*(§1)가 맞았다. ④는 배운 것을 쓰는 구간이라 축이 하나다.
축을 하나 더 만들면 엔딩이 또 다른 튜토가 된다.

★ **일상 흑자로는 못 간다.** 자취생 최선이 +2,348원/일이라 1,000만원이면 4,259일이다
(`propagation.md` §7). ④도 ②처럼 **「한 방」** 이고, 그게 `sale_economy.md` §1 의
*"자취생은 흑자 게임이 아니라 타임어택"* 이 마지막까지 이어지는 모양이다.

★ **직업 선택은 여기 다음이다**(§0). `ending.finishEnding` 은 `nextChapter: 'job_select'` 를
알리기만 하고 직업 상태를 만들지 않는다.

## ⏸ ④는 아직 **화면에 없다** (2026-08-11 확인)

`game.html` 이 `src/game/ending.js` 를 **한 번도 안 읽는다.** import 도 버튼도 없다.
코어(`endingView` · `finishEnding`)와 검사(`tools/test_ending_flow.mjs` 16/16 통과)는 서 있는데
그 앞에 화면이 없다. 그래서 `ending_ready` · `ending_home` 사건은 나도 아무 데도 안 닿는다.

**대사도 일부러 안 썼다.** 목표 금액이 `targetWon = null` 로 미확정인데(아래) 대사를 지어 두면
「검사는 통과하는데 화면엔 안 뜨는 대사」가 하나 더 늘 뿐이다 —
START-HERE §2 가 제일 위험하다고 적은 모양이다. 화면과 금액이 정해질 때 같이 쓴다.

## ⏸ 목표 금액은 아직 미확정이다 — 후보는 **1,000만원**

근거와 역산은 `docs/oneroom.md` §5 · `docs/propagation.md` §7.
코드에는 `ENDING_RULES.targetWon = null` 을 두었다 — `null` 이면 「아직 안 정해졌다」이고
모든 판정이 그렇게 답한다. **0 이 아니다**(0이면 시작하자마자 엔딩이 성립한다).

⚠ `propagation.md` §7 의 역산은 **옛 가격 공식**(잎 비율 v)으로 낸 것이다.
2026-08-04 에 값이 「잎마다의 합」으로 바뀌면서 자릿수가 달라졌다 —
**금액을 확정하기 전에 새 공식으로 다시 돌려야 한다.**

---


# 5. 아직 확정하지 않은 것 — **2026-08-05 실측으로 채웠다**

> 다섯 줄 다 **재서** 답을 달았다. 여기 있는 것은 **측정값과 권고**이고,
> **확정은 박사님 판단이다** — 이 창은 `data/balance/` · `data/growth_tuning.json` 을 읽기만 했다.
>
> | 잰 도구 | 무엇을 |
> |---|---|
> | `tools/probe_season_dli.mjs` | 계절별 DLI 곡선 (방·자리·계절·등 개수별) |
> | `tools/probe_winter_growth.mjs` | 겨울에 생장이 실제로 어떻게 되나 (진짜 엔진 300일) |
> | `tools/probe_move_econ.mjs` | 데드라인·잎 생산 속도·판매가·목표 금액 역산 |
> | `tools/test_balance_routes.mjs` | ★세 경로를 나란히 |
>
> **잰 조건**(전부 공통) — 반지하 프로파일 2026-08-02(가구 배치 포함) · 몬스테라 민무늬 ·
> 몬스테라는 창턱 `banjiha-sill:0` · 콩나물은 서랍장 `banjiha-dresser:1` · 등 12h 점등 ·
> 날씨 맑음 0.55 / 흐림 0.30 / 비 0.15 · **판정 단위 = 7일 이동평균** · 씨앗 24판.

## ★★ 먼저 — 재다가 나온 것이 답보다 크다

**계절이 화면에만 있고 빛에는 안 걸려 있다.** 튜토는 `novice` 모드로 돌고
(`state.SIM_MODES.novice` = 날씨·계절 계수 1.0), 그래서 반지하 창턱은 **300일 내내 DLI 3.77** 이다.
화면은 `tutorial.seasonAt`(여름 45일차 시작)으로 "가을입니다"라고 말하는데,
조도 쪽은 `room_profile.skyFor` → `weather.seasonOf(S.day)`(0일 = **봄** 0일)를 본다.
**달력이 둘이고 오프셋 135일이 빠져 있다.** 게임 50일에 화면은 `autumn`, 빛은 `spring` 이다.

아래 ①~② 는 그래서 두 벌이다 — **지금 나는 값**과 **계절을 켜면 나는 값**.

---

## ① 가을·겨울의 실제 DLI 곡선 — 쟀다

**반지하 창턱 `banjiha-sill:0` · 7일 이동평균**(90일 × 20년 굴림 · `probe_season_dli`)

| 등 | 여름 | 가을 | 겨울 | 봄 | 겨울/여름 |
|---|---|---|---|---|---|
| **0개** | **2.38** | **1.58** | **0.91** | 1.79 | 38% |
| **1개** | 4.22 | 3.42 | **2.75** | 3.63 | 65% |
| **2개** | 4.37 | 3.57 | **2.90** | 3.77 | 66% |

밴드(`growth_tuning.thresholds`)는 die 0.5 · survive 1.2 · **min 3.0** · fenestrate 6.0 이다.

- **등 0개는 여름조차 min 을 못 넘는다**(2.38). 계절 탓이 아니라 반지하가 원래 그렇다.
- 등 1개면 여름·가을·봄은 넘고 **겨울만 못 넘는다**(2.75 — min 대비 **92%**).
- 등을 하나 더 사도 겨울은 못 넘는다(2.90). **등 개수로는 겨울이 안 풀린다.**
- 계절 낙폭은 등이 없을 때 62%, 등 1개면 35% 로 준다 — 등 DLI 는 날씨·계절과 무관해서다.
  §4 의 *"등이 사는 건 밝기가 아니라 재현성"* 이 숫자로 그대로 나온다.

**콩나물 자리**(`banjiha-dresser:1`)는 계절과 등에 상관없이 **0.01~0.12** 다 — 사철 어둡다.
콩나물 품질 판정(4일평균 ≤ 0.3)은 계절을 켜도 안 흔들린다. **여기는 손댈 게 없다.**

**지금 게임에서 나는 값** — 계절이 빛에 안 걸려 있으므로 사철 `peak` 3.77(등 1개 5.61)이다.
위 표의 어느 줄도 지금은 안 나온다.

---

## ② 겨울에 생장을 정지시킬지 — **이미 정지다. 정지를 풀 손잡이는 "점등 시간"이다**

정할 것이 아니라 **재는 것**이었다. `min 3.0` 미만이면 형태가 안 나아가는 규칙이 이미 있고,
계절을 켜는 순간 겨울이 저절로 그 밑으로 간다. 진짜 엔진 300일 실측(`probe_winter_growth`):

| 빛 공급 | 등 | 여름 | 가을 | **겨울** | 봄 | 300일 유효 생장일 | 잎 |
|---|---|---|---|---|---|---|---|
| 계절 끔(지금) | 0·1·2개 | — | — | — | — | **+300** | 5장 |
| 계절 켬 | **0개** | 0/44 | 0/90 | **0/90** | 0/76 | **+0** | 1장 |
| 계절 켬 | **1개** | 43/44 | 78/90 | **15/90 (17%)** | 69/76 | +205 | 5장 |
| 계절 켬 | **2개** | 43/44 | 87/90 | **26/90 (29%)** | 70/76 | +226 | 5장 |

*(`n/m일` = 그 계절 m 일 중 형태가 나아간 날이 n 일)*

- 계절을 켜면 **등이 없으면 300일 내내 한 번도 안 자란다.** 경로 A 가 사라진다.
- 등 1개여도 겨울은 **90일 중 15일**만 자란다 — 사실상 정지다. 경로 C 는 *"안전장치"* 가 아니라
  **"겨울에는 아무 일도 안 남"** 이 된다. §5 원문이 걱정한 그대로다.

### ★ 권고 — 정지시키지 말고 **손잡이를 준다**

`min` 을 내리지 말 것을 권한다(3.0 은 다른 방·다른 종 판정에도 다 걸려 있다).
대신 **점등 시간**을 겨울의 대응 수단으로 쓴다. 그 값은 이미 재 뒀다:

| 등 1개 · 겨울 | 7일평균 | | 전기 |
|---|---|---|---|
| 12h (지금) | 2.69 | 미달 | 23원/일 |
| **14h** | **3.00** | **★정확히 넘는다** | 27원/일 |
| 16h | 3.31 | 넘음 | 31원/일 |

**12h → 14h 두 시간이 겨울을 가른다.** 전기는 하루 4원 더다.
이러면 겨울이 *"등을 더 오래 켠다"* 는 **행동**이 되고, 경로 C 가 실패가 아니라
"겨울나기를 배우는 구간"이 된다. 광주기 상한은 `light_thresholds.json` 의 `photoperiod` 가
이미 갖고 있으므로 새 수치를 만들지 않아도 된다.

> ⚠ 계절을 켜기 전에 **①의 달력 어긋남(오프셋 135일)을 먼저 고쳐야 한다.** 안 고치고 켜면
> 화면은 가을인데 빛은 봄이 되어, 이 표의 어느 줄도 플레이어가 겪는 것과 안 맞는다.

---

## ③ 원룸 이후의 슬롯 수·월세·판매가 — 쟀다

> ⚠⚠ **2026-08-15 — 아래 표의 반지하·원룸 두 줄은 낡았다.**
> `data/profiles/room_profile.*.json`(2026-08-02)에서 낸 값인데, 그 뒤 방이 두 번 바뀌었다
> (2026-08-06 `oneroomfix` 창턱 4칸 + 등 2개 · `lampaim` BACK_REFLECT). 지금 값:
>
> | 방 | 슬롯 | avg7 여름(등0) | ≥3 자리 | 어두운 칸(≤0.3) | 월세(코드가 실제로 떼는 값) |
> |---|---|---|---|---|---|
> | 반지하 | **14** | **3.09** | **1** | 9 | 20만 |
> | 원룸 | **15** | **4.30** | **5** | **1** | **20만** ← 정본은 35만인데 안 걸려 있다(§4-1 ⚠) |
>
> ⇒ **§③ 이 제기한 「원룸이 반지하보다 자리가 적다」는 지금 성립하지 않는다**(14 → 15).
> 아래 「권고 셋 중 하나」는 2 번(*"보상을 밝기로 말한다"*)이 **이미 그렇게 됐다** —
> `≥3` 자리가 0칸이 아니라 **1칸 → 5칸**이라 *"우연으로 보인다"* 던 걱정도 같이 풀렸다.
>
> ⚠⚠ **2026-08-15 밤(story4) — 이 표의 `avg7` 열은 스토리에 쓸 자가 아니다.**
> 스토리는 `novice` 로만 돌고 novice 는 **peak 가 곧 실제값**이다(`state.SIM_MODES` 머리말).
> `weatherE('summer') 0.643` 을 곱한 이 열은 **날씨가 굴러가는 판(`real`)의 기댓값**이다.
> novice 로 다시 세면 `≥3` 자리는 **1칸 → 8칸**이고 원룸 창턱 넷은 **갈라짐 문턱 6.0 도 넘는다.**
> ⇒ **§4-1 의 「★★★ 2026-08-15 밤(story4)」 절을 볼 것.** 이 표는 `real` 용으로만 읽는다.
> 다만 §④ 의 역산(㉮ 슬롯 ≥14칸)은 **다시 돌려야 한다** — 이 창은 경제를 안 쟀다.

| 방 | 슬롯 | peak 여름 | avg7 여름 | avg7 겨울 | ≥3 자리(여름) | ≥6 자리 | 월세 | 보증금 | 이사비 |
|---|---|---|---|---|---|---|---|---|---|
| 반지하 | **14** | 3.77 | 2.42 | 0.90 | 0 | 0 | 30만 | 300만 | — |
| **원룸** | **11** | 4.77 | **3.07** | 1.14 | **1** | 0 | **45만** | 400만 | **150만** |
| 투룸 | 20 | 5.64 | 3.63 | 1.35 | 3 | 0 | 60만 | 800만 | 450만 |
| 아파트 | 83 | 6.02 | 3.87 | 1.44 | 33 | 0 | 90만 | 2,000만 | 1,250만 |

*(avg7 = 맑음 peak × E[k] 0.643 × 계절계수. 해석식이라 표본 오차가 없다. 월세·보증금은
`data/balance/homes.json`, 슬롯·밝기는 `data/profiles/room_profile.*.json` 2026-08-02)*

### ★ 여기서 나온 문제 — **원룸이 반지하보다 자리가 적다**

14칸 → **11칸**이다. 이사의 보상이 "여러 개를 키울 수 있다"가 아니게 된다
(`balance_decisions` §②″ 가 아파트의 보상을 그렇게 다시 정의했는데, 원룸에서는 그게 거꾸로 간다).

**권고 — 셋 중 하나를 고른다**

1. **원룸 슬롯을 14칸 이상으로** (house 몫). 제일 곧고, "이사했더니 놓을 데가 늘었다"가 성립한다.
2. **보상을 밝기로 말한다** — 원룸은 `≥3` 자리가 **0칸 → 1칸**이 되는 첫 집이다.
   *"등 없이 자라는 자리가 생겼다"* 는 서사가 실제로 참이다. 다만 **1칸은 우연으로 보인다** —
   §②′ 가 아파트 1칸을 두고 한 지적과 같은 문제라, 3~5칸은 돼야 배치로 읽힌다.
3. 둘 다.

**판매가**(`shop.priceOf` · 등급은 무늬 잎 **장수**가 정한다. 잎 3장부터 성체)

| 잎 | 무늬 0 | 무늬 1(산반) | 무늬 2(섹터) | 무늬 3(하프문) |
|---|---|---|---|---|
| 1 | 12,000 | 80,000 | — | — |
| 2 | 20,000 | 90,000 | 160,000 | — |
| 3 | 30,000 | 100,000 | 721,111 | **1,830,000** |
| 6 | 60,000 | 130,000 | 751,111 | 1,860,000 |
| 12 | 120,000 | 190,000 | 811,111 | 1,920,000 |

---

## ④ 내 집 마련의 목표 금액 — **역산했다. 지금 수치로는 성립하지 않는다**

먼저 판매 경제의 뼈대를 쟀다(`probe_move_econ` · 진짜 엔진 360일 · 씨앗 12판).

| | |
|---|---|
| **잎 한 장에** | **72일** — ★ **DLI 3.0 만 넘으면 3.77 이든 12.0 이든 똑같다** |
| 삽수 회전 | 병 2일 + 물꽂이 뿌리 12일 = 개당 14일 · 병 7,000원(팔면 돌아온다) |
| 360일 뒤 한 그루 | 잎 5.8장 · 무늬 잎 — 대역 밖 **0.8장**(산반 13만) / 무늬 최적 대역 **2.4장**(섹터 75만) |

★ **밝기는 빠르기를 안 바꾼다.** 문턱을 넘느냐만 가른다. 이 한 줄이 아래 ★★ 의 원인이다.

**민무늬만으로는 안 된다** — 원룸 11칸을 전부 몬스테라로 채워도 하루 **1,833원**,
하루 지출 20,000원의 **9%** 다. `sale_economy.md` §0 의 *"못 번다"* 가 그대로 유효하다.
그래서 ④ 의 자금은 **무늬 성체 몇 그루**로만 나온다.

### 역산

무늬 최적 대역(7.0~15.4 DLI)에 넣으면 그루당 **751,111원 / 360일 = 2,086원/일**.
원룸 11칸이면 총 **22,950원/일**이다. 그런데 원룸의 하루 지출은
`20,000 + (45만 − 30만)/30 = ` **25,000원/일** 이다.

> **11칸 × 2,086 = 22,950 < 25,000.** **9% 모자란다 — 정상상태가 적자다.**
> 즉 지금 수치로는 ④ 를 **얼마로 잡든 영영 안 모인다.** 목표 금액을 정하기 전에 이걸 먼저 고쳐야 한다.

**권고 — 흑자로 만드는 손잡이 셋(하나만 골라도 된다)**

| | 지금 | 바꾸면 | 효과 |
|---|---|---|---|
| ㉮ 원룸 슬롯 | 11칸 | **≥14칸** | 총수입 29,204원/일 → 순 +4,204원/일. ③의 권고1과 같은 수정이다 |
| ㉯ 원룸 월세 | 45만 | **≤35만** | 하루 지출 21,667원 → 순 +1,283원/일. 값이 `cost_provisional: true` 라 흔들어도 된다 |
| ㉰ 무늬 최적 배율 `f_light.best` | 1.5 | **1.7** | 무늬 잎 2.4 → 2.7장 근처. ⚠ 다른 방·다른 캐릭터에 다 걸린다 |

**㉮를 권한다.** ③ 의 문제와 ④ 의 문제가 **같은 한 줄**로 풀리고, 데이터 값이 아니라
방 기하라 다른 밸런스를 안 흔든다.

### 그 다음에 정할 목표 금액

㉮(원룸 14칸)를 적용하면 순수입이 하루 **4,204원**이다. ③단계 길이를 얼마로 두느냐가
곧 목표 금액이다:

| ③단계 목표 길이 | 필요 금액 | 화면에서 읽히는 말 |
|---|---|---|
| 120 게임일 | 약 50만 | 너무 작다 — "내 집"으로 안 읽힌다 |
| **360 게임일(1년)** | **약 150만** | 원룸 이사와 같은 값이라 진전이 안 느껴진다 |
| — | **★5,000,000원** | **하프문 성체 2.7그루 / 섹터 6.7그루** — "무늬 몇 그루"로 읽힌다 |

**권고 목표 금액 = 5,000,000원.** 다만 하루 4,204원으로는 1,190일이라 너무 길다 —
㉮ 만으로는 부족하고 **③단계에는 슬롯이 20~30칸(투룸급)이 필요하다**는 뜻이다.
슬롯 28칸이면 순수입 33,408원/일 → **150일**이 되어 `balance_decisions` §A 의
자취생 1막(75~120일)의 약 1.3배가 된다. 2막으로 알맞다.

> ⚠ **못 잰 것** — 원룸·투룸·아파트 프로파일에는 **등 PPFD 가 없다**(`lampCounts: [0]`).
> 그래서 "원룸에서 무늬 최적 대역을 등으로 만들 수 있나"는 **아직 못 쟀다.**
> 위 계산은 *만들 수 있다고 가정한* 상한이다. house 에 등 프로파일을 요청해야 한다.
> 또 이 프로파일들은 `uidStable` 계약(2026-08-02) 이전 파일이라 **자리 이름을 못 믿는다**
> (칸 수와 밝기 분포만 유효하다).

---

## ⑤ 이사까지의 목표 플레이 시간 — **73일부터 틀렸다**

### 먼저 73일을 검산했다

`tutorial.tutorialDay` 를 그대로 돌리면(`probe_move_econ` ①) **파산은 튜토 60일**이다.

```
잔액 — 15일 855,685 · 29일 720,991 · 30일 411,370(첫 월세) · 45일 267,055
     · 57일 151,603 · 59일 132,361 · 60일 0        ← 두 번째 월세 30만에서 끝난다
```

73 은 *"하루 순지출 = 20,000 − 월세몫 10,000"* 으로 100만을 나눈 값이다. 그런데 실제로는
30일·60일에 **30만씩 목돈**이 빠진다. 콩나물을 돌려도(순액 379원/일) 60일은 안 밀린다 —
하루 379원으로는 30만짜리 목돈을 못 막기 때문이다. **데드라인은 73일이 아니라 60일이다.**

### 그런데 이사는 훨씬 뒤다

세 경로 중앙값이 **튜토 189일 · 게임 219일**이다(`test_balance_routes`).
즉 **튜토 60일에 파산하고, 그 뒤 129일을 0원으로 산다.** 파산이 게임을 안 끝내므로
(스토리 모드는 초보라 죽지 않는다 · §0) 막히지는 않지만, **"타임어택"이 아니게 된다.**

### 몇 분인가 — 속도를 어느 것으로 재느냐로 통째로 갈린다

`time_modes.md` §1 은 빠른배속을 **1일 10~30초**로 적어 두었는데,
`game.html` 이 실제로 부르는 값은 **140ms/일**이다(`loop.DEFAULT_MS_PER_DAY` 는 120ms).
**71~214배 다르다.** 140ms 는 *점핑 중 하루가 지나가는 것을 보여주는 애니 속도*이고,
배속은 *하루 안을 압축해서 보는* 다른 모드다 — 지금 코드에는 배속 속도가 따로 없다.

| | 게임 219일(중앙값) | 데드라인 73일 |
|---|---|---|
| 점핑 애니 140ms/일 | 감기 0.5분 + 누름 5.6분 = **6.1분** | 10초 |
| **배속 10초/일** | 감기 36.5분 + 누름 5.6분 = **42.1분** | **12.2분** |
| 배속 30초/일 | 감기 109.5분 + 누름 5.6분 = **115.2분** | 36.5분 |

*(누름 112회는 실측 — 물주기·수확·주문·재파종·자르기·판매·빨리감기를 다 센 값.
누름당 3초는 **가정값**이라 따로 적는다. 감는 시간은 코드·문서에서 나온 값이다.)*

### 권고

- **배속 = 1일 10초로 확정한다.** 그러면 이사까지 **42분**이라
  `balance_decisions` §D 의 *"튜토 완주 실시간 30~50분"* 에 정확히 든다.
  30초로 두면 115분이라 §D 의 실패선(90분↑)을 넘는다.
- **다만 게임일이 219일이다.** §D 는 *"게임일 80일"* 을 목표로 적어 두었으므로 **2.7배**다.
  줄이려면 확정 무늬 간격(12일)이나 필요한 무늬 장수를 손봐야 한다 — ★ 아래 세 경로 항목 참고.
- `loop.DEFAULT_MS_PER_DAY`(120ms)와 `game.html` 의 140ms 는 **점핑 애니 값**이라고
  이름을 갈라 두는 편이 낫다. 지금은 같은 숫자가 두 가지 뜻으로 읽힌다.

---

# 5′. ★★ 세 경로 — **지금은 셋이 아니라 하나다**

`test_balance_routes.mjs` 가 A·B·C 를 같은 조건에서 24판씩 나란히 돌린 결과다.

## ① 지금 게임 그대로 (계절이 빛에 안 걸린 상태)

| 경로 | 이사 성공 | 중앙값 | 유효 생장일 | 잎 | 가을 45일 잔액 |
|---|---|---|---|---|---|
| **A** 등 없이 | 20/24 (83%) | 튜토 **189일** | 249 | 4장 | 271,500원 |
| **B** 등 1개 | 20/24 (83%) | 튜토 **189일** | **249** | **4장** | 246,500원 |
| **C** 늦게 + 등 | 20/24 (83%) | 튜토 **189일** | 249 | 4장 | 246,500원 |

> ### ★ 셋이 **한 글자도 다르지 않다.**
>
> 식물등 25,000원이 사는 것은 **잔액 −25,000원**뿐이고 얻는 것이 **하나도 없다.**
> **B 는 A 의 열등 복제다.** 선택이 아니라 함정이다.

**왜 그런가 — 원인이 둘이고 둘 다 재서 나왔다.**

1. **밝기는 빠르기를 안 바꾼다**(④ 참고). DLI 3.0 만 넘으면 3.77 이든 12.0 이든 잎이 72일에 한 장이다.
   반지하 창턱은 `novice` 고정값이 **3.77 로 이미 문턱 위**라, 등이 5.61 로 올려 줘도 **바뀔 것이 없다.**
2. **무늬 대역에도 못 닿는다.** 무늬 최적은 7.0~15.4 인데 등 1개 5.61 · 등 2개 5.76 이라
   둘 다 `below_best`(배율 0.5) 구간이다. 확률이 **13% 로 똑같다**(`test_banjiha_routes` 검사 F).

그리고 **A 도 §2 가 말한 "표준"이 아니다** — 20판 중 **가을에 끝난 것은 5판**이고
나머지 15판이 겨울이다. *"등 없이 가을 안에 이사"* 는 지금 소수 경로다.

## ② 계절을 빛에 걸면

| 경로 | 이사 성공 | 중앙값 | 유효 생장일 | 잎 |
|---|---|---|---|---|
| **A** 등 없이 | **0/24 (0%)** | **한 판도 못 나감** | 62 | 2장 |
| **B** 등 1개 | 5/24 (21%) | 튜토 216일 | 198 | 3장 |
| **C** 늦게 + 등 | 5/24 (21%) | 튜토 216일 | 198 | 3장 |

계절을 켜면 **등이 값을 갖는다**(유효 생장일 62 → 198, +136). 대비가 살아난다.
그런데 이번엔 **A 가 통째로 죽는다** — 등이 선택이 아니라 **필수**가 된다.
그리고 B·C 도 21% 라 중앙값으로는 못 나간다.

## ★ 권고 — 세 경로를 살리려면

셋 다 유효하려면 **"등이 있으면 좋고 없어도 되는" 구간이 실제로 있어야** 한다.
지금은 그 구간이 없다(등이 무의미하거나, 등이 필수거나).

**권고 ㉠ — 계절을 빛에 걸되, 반지하 창턱을 여름에 문턱 위로 둔다.**
지금 여름 avg7 이 2.38 이라 `min 3.0` 에 **26% 모자란다.** 여기만 넘기면 —

```
여름  등 없이도 자란다        → 경로 A 성립 (표준)
가을  등 없이 1.58 → 정지     → 여기서 등이 값을 갖는다 (경로 B)
겨울  등 1개 2.75 → 정지      → 점등 14h 로 대응 (경로 C · ② 참고)
```

세 계절이 세 경로에 하나씩 대응한다. 필요한 변화는 **창턱 여름 peak 3.77 → 4.7 이상**
(= avg7 3.02 이상) 이다. 손잡이는 둘 중 하나다:

| | 지금 | 바꾸면 | 결과 | 누가 |
|---|---|---|---|---|
| 반지하 창 투과율 `tau` | 0.55(반투명) | **0.70** | peak **4.80** · avg7 **3.09** | house — 방 기하다. 다른 방이 안 흔들린다 |
| 창턱 자리를 창에 더 붙인다 | ratio 0.245 | 0.306 | peak 4.70 · avg7 3.02 | house — 가구 2cm 문제라 §②′ 의 그 위험이 있다 |

**`tau` 쪽을 권한다.** 자리 하나가 아니라 방 전체가 올라가고, *"반지하는 창이 작고 반투명해서
어둡다"*(§④)는 설정을 **덜 어둡게** 조정하는 것이라 서사가 안 깨진다.

**권고 ㉡ — 그래도 등이 안 팔리면, 등을 무늬 대역에 닿게 한다.**
㉠ 만으로는 등이 *"가을·겨울용"* 일 뿐이다. 등을 사고 싶게 하려면 **무늬 확률**이 붙어야 한다 —
등 1개 5.61 을 **7.0 이상**으로 올리면 확률이 13% → 39% 로 **3배**가 된다.

★ **㉠ 없이 점등 시간만으로는 안 된다** — 지금 창턱(자연광 3.77)에서 7.0 에 닿으려면
**21.1시간**을 켜야 한다(또는 등 PPFD 42.6 → 74.8). 광주기 상한에 걸리고 전기도 40원/일이 된다.

**㉠ 을 하면 저절로 닿는다.** `tau 0.70` 이면 자연광이 4.80 이 되고 —

| 점등 | 등 1개 | 등 2개 | 전기(등 1개) | 무늬 대역(7.0) |
|---|---|---|---|---|
| 12h(지금) | 6.64 | 6.78 | 23원/일 | 못 닿음 |
| 14h | 6.94 | **7.12** | 27원/일 | 등 2개만 |
| **15h** | **7.10** | 7.28 | 29원/일 | **등 1개로 닿는다** |

즉 ㉠ 하나로 **①번 손잡이(점등 14h = 겨울나기)** 와 **②번 손잡이(점등 15h = 무늬 3배)** 가
같은 축 위에 나란히 놓인다 — *"몇 시간 켜 둘 것인가"* 가 이 게임의 관리 변수가 된다.
등 PPFD·값을 안 건드리고 나오는 결과다.

### ★★ 덤 — ㉠ 은 `balance_decisions` §B 의 **제일 중요한 두 줄**도 같이 고친다

§B 는 *"반지하 등 1개 · 좋은 자리 → 갈라짐 켜짐 / 나쁜 자리 → 안 켜짐"* 을
**이 게임에서 제일 중요한 판정**이라고 적어 두었다. 지금은 좋은 자리가 **5.61** 로
문턱 6.0 에 0.39 모자라 **한쪽이 깨져 있다.** `tau 0.70` 이면 —

| | 지금 | tau 0.70 |
|---|---|---|
| 좋은 자리 `banjiha-sill:0` · 등 1개 | 5.61 ✕ | **6.64 ○** |
| 나쁜 자리 `banjiha-dresser:1` · 등 1개 | 0.09 ○ | **0.10 ○** |
| 대비 | 62배 | **66배** |

**세 경로와 §B 의 핵심 판정이 같은 한 줄로 풀린다.** ㉠ 을 권하는 진짜 이유다.

> **㉠ + ㉡ 이면 세 경로가 이렇게 읽힌다.**
> A = *"여름 안에 부지런히 끝낸다"* · B = *"등을 사서 무늬를 노린다(3배)"* ·
> C = *"겨울을 등으로 난다(점등 14h)"*. 셋이 **서로 다른 것을 산다.**

**권고 ㉢ — 게임일 219일을 줄인다.** §⑤ 참고. 확정 무늬가 12일에 한 장씩 오고
하프문(무늬 3장)이라야 150만이 되므로, 최소 36일 + 뿌리 12일이 든다. 실측 중앙값이
189일인 이유는 **잎이 72일에 한 장**이라 무늬를 붙일 잎 자체가 모자라서다.
㉠ 을 하면 여름 구간이 살아나 잎이 빨라지고 이것도 같이 준다 — 별도 수정이 필요한지는
㉠ 을 넣고 다시 재는 것이 맞다.

## ③④ 후보와 근거 — `docs/oneroom.md`

- ③ 원룸 이후의 슬롯 수·월세·판매가 — **후보와 근거는 `docs/oneroom.md` §2·§3 에 모아 두었다.**
  후보 1,000만원 · 근거는 `docs/oneroom.md` §5

---

# 6. 근거 문서

| | |
|---|---|
| 첫 플레이 한 흐름 | `first_play.md` |
| 시간 모드·점핑 | `time_modes.md` |
| 식비·콩나물 수치 | `food_economy.md` |
| 자취생 경제·데드라인 | `game_flow.md` |
| 판매 경제 | `sale_economy.md` |
| ③ 원룸 · ④ 엔딩 | **`oneroom.md`** |
| 창별 상태 | `handoff/STATUS.md` |
