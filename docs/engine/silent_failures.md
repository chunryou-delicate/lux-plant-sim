# 조용히 잘못 도는 버그 — 사례와 대책

**공통점: 실패가 예외가 아니라 "그럴듯한 기본값"으로 나타난다.**
오류가 안 나니 발견이 늦고, 발견될 땐 이미 그 위에 결론이 쌓여 있다.

---

## 사례

| # | 무엇 | 어떻게 조용히 실패했나 | 어떻게 잡았나 |
|---|---|---|---|
| 1 | 함수 사라짐 | 호출부가 `undefined` 를 그냥 통과 | 사후 |
| 2 | 확률 NaN | `rng() < NaN` 은 조용히 `false` — 확률 0이 됨 | 사후 |
| 3 | 밴드 키 개칭 | `BAND_LOOK[band] \|\| unknown` → 흰색·꼿꼿한 잎. **빛이 부족한데 건강해 보임** | 사후 |
| 4 | 슬롯 ID | 좌표 기반이라 가구를 옮기면 화분이 사라짐 | 사후 |
| 5 | 태양 스포트라이트 | 해를 '첫 창 → 하드코딩 타깃'으로 배치. **창이 하나면 우연히 맞고 여럿이면 첫 창만 맞음** | 사후(스크린샷) |
| 6 | 유리가 그림자를 던짐 | 셸 일괄 `castShadow=true` 에 칸막이 유리가 걸림 → 거실이 캄캄 | **검사가 먼저** |
| 7 | 천장이 빛을 안 막음 | `box(...,false)`(그림자 렌더 생략)를 `transparent`(빛 통과)로 매핑 | 사후 — **검사는 통과했다** |

| 8 | 창 보조광이 해를 안 따라감 | `sunLight` 만 방향 기반으로 고치고 `winLight1` 은 첫 창에 고정. **세기만 변하고 방향은 그대로** — 침실에 움직이지 않는 빛 자국 | 사후(스크린샷) |

---

## ★ 8번의 유형 — "고쳐야 할 곳이 둘인데 하나만 고쳤다"

6번과 똑같다.

```
유리   칸막이 유리는 고치고 → 창틀 유리는 놓침
태양   sunLight 는 고치고   → winLight1 은 놓침
```

**같은 역할을 하는 것이 여러 개면 목록을 먼저 만들고 시작한다.**
`grep -n "castShadow\|Light(" src/render3d/scene.js` 처럼 **한 번 훑어서 전부 찾은 뒤** 고친다.

---

## ★ 7번이 중요하다 — 검사에 사각지대가 있었다

6번을 잡은 감사(`_shadow_audit.html`)가 7번은 **0/0/0 으로 통과시켰다.**

```
✅ 유리가 그림자를 던지나        (막으면 안 되는데 막음)   ← 위양성만 봄
❌ 천장이 그림자를 던지나        (막아야 하는데 안 막음)   ← 위음성은 안 봄
```

**검사는 한 방향만 보면 안 된다.** `auditShadowCoverage()` 로 반대 방향을 추가했다 —
바닥·천장·외벽 4면·칸막이가 각각 "빛을 막는 메시"를 하나라도 갖고 있어야 하고,
`hidden-but-blocks` 는 실제로 `castShadow=true` 여야 한다.

---

## 대책 — 세 가지

### 1. 실패를 시끄럽게

기본값으로 조용히 떨어지지 말고 세서 알린다.

```js
if(!o.userData.shadowRole){ o.userData.shadowRole = BLOCK; untagged.push(o); }
if(untagged.length) console.warn('정책이 없는 메시', untagged.length);
```

### 2. 검사는 양방향으로

"하면 안 되는 걸 했나"와 **"해야 하는 걸 안 했나"** 를 같이 본다.
후자가 없으면 7번 같은 게 통과한다.

### 3. 주체를 하나로

`castShadow` 를 쓰는 곳이 여러 군데면 **순서가 곧 버그**가 된다.
실제로 정책 루프를 `trims` 붙기 전에 돌려서 창틀이 전 방에서 빛을 막았다.
지금은 `applyShadowPolicy` 만 `castShadow` 를 쓴다 — 순서에 무관하다.

---

## 회귀 검사

| 검사 | 무엇 |
|---|---|
| `_render_vs_calc.html` | **계산 ↔ 화면 대조** — 픽셀 없이 레이캐스트로 |
| `_shadow_audit.html` | 그림자 정책 양방향 |
| `_dli_probe.html` | 슬롯 기준 조도 |
| `_space_probe.html` | 공간 기준 조도(방 등급 정본) |
| `_path_test.html` | 문·통로 막힘 |
| `_place_test.html` | 가구 배치 유효성 |

**방 구조나 렌더 정책을 고치면 이 다섯을 돌린다.**

---

## 사각지대를 하나 메웠다 — `_render_vs_calc.html`

"화면과 계산의 일치"를 자동으로 본다. 오프스크린 렌더가 필요할 줄 알았는데,
**그림자맵의 차폐가 `castShadow` 메시로 결정된다**는 걸 이용하면 레이캐스트로 충분했다.

처음 돌리자마자 **몰랐던 것 둘**을 찾았다.
- 유리벽·천창 **격자살이 그림자를 던지고 있었다**(343건) — 창틀은 빼놨는데 이건 또 빠졌다.
  6·8번과 **같은 유형이 세 번째**다: "같은 역할을 하는 것이 여럿인데 하나를 놓쳤다"
- **도려낸 경계 벽이 조도 차폐체에 없었다**(39건) — 화면은 막는데 계산은 통과

★ 이번엔 **사람이 스크린샷을 보기 전에 검사가 먼저 찾았다.**

## 남은 사각지대

- **밝기의 크기**는 아직 안 본다. `_render_vs_calc` 는 "막히나 안 막히나"(이진)만 본다.
  창 확산광 세기 계수가 조도 엔진과 얼마나 맞는지는 조도맵으로 눈 확인해야 한다
- **조도 엔진은 가구를 통짜 상자로 본다** — 책상 다리 사이를 화면은 통과시키는데 계산은 막는다.
  113건. 화분은 가구 위에 놓이므로 슬롯 값엔 영향이 없지만 공간 측정엔 있다
