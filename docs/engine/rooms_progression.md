# 방 progression — 향 · 베란다 2겹 · 실측표

> ℹ 게임 기획 결론은 **[GAME_PLAN.md](GAME_PLAN.md)** 에 있다.
> 이 문서는 **엔진 레퍼런스로 계속 유지**한다(통합 대상 아님).


`data/house_rooms.json` · 측정: `_dli_probe.html`

---

## 1. 창 향(方位) — 새로 들어감

이 모델의 천공은 **방향과 무관한 균일 휘도**다. 향을 안 넣으면
**북향창과 남향창이 똑같이 밝다.** 실제로는 수 배 차이가 난다.

창마다 `evScale`(남향 대비 비율)을 곱해 근사한다.

| 향 | k | | 향 | k |
|---|---|---|---|---|
| 남 | 1.00 | | 동·서 | 0.62 |
| 남동·남서 | 0.85 | | 북동·북서 | 0.45 |
| | | | **북** | **0.38** |

방 데이터에 `facing`(= `back` 벽 바깥이 향하는 방위)을 적으면
`back → facing`, `right → +90°`, `front → +180°`, `left → +270°`로 자동 계산된다.
창 스펙에 `orient`를 직접 쓰면 그게 우선한다.

```jsonc
"facing": "south"        // back=남, right=서, front=북, left=동
```

---

## 2. ★ 베란다 2겹 유리 — 한국 아파트 실제 구조

```
바깥 ─[베란다 새시 τ0.92]─ 베란다 ─[거실창 τ0.92]─ 거실
```

### ★ 정정 — 명당인 진짜 이유는 유리가 아니라 거리다

처음엔 "베란다 1겹 / 거실 2겹이라 베란다가 밝다"고 적었는데 **비중을 잘못 말한 것이다.**
유리는 한 겹당 τ0.92, 즉 **−8%뿐**이다. 투명한 유리를 하나 더 지난다고 어두워지지 않는다.

안쪽 유리를 아예 없앤 것으로 치고 다시 재봤다:

| 구역 | 2겹(τ0.92) | 유리 없다 치면 | 유리 몫 |
|---|---|---|---|
| 발코니 | 8.32 | 8.32 | 0% |
| 거실 | 0.31 | 0.34 | **−8%** |
| 안방 | 1.76 | 1.91 | −8% |

**발코니 ÷ 거실 = 26.8배. 유리를 없애도 24.7배다.**

차이는 거의 전부 **기하**에서 온다 — 거리와, 그 지점이 창을 보는 입체각.
같은 창·같은 유리로 거리만 바꿔 보면:

| 창에서 거리 | 조도 | 발코니 대비 |
|---|---|---|
| 0.28m (발코니 선반) | 9,057 | 100% |
| 1.2m | 4,210 | 46% |
| 2.0m | 2,153 | 24% |
| **3.7m (거실 좌탁)** | **660** | **7%** |

거실 화분이 어두운 건 유리를 두 번 지나서가 아니라
**창에서 3.7m 떨어져 있고, 거실창(2.8m 폭) 만큼만 하늘을 보기 때문**이다.
발코니 화분은 6.8m 통창을 코앞에서 본다.

그래도 2겹을 모델에 넣어둔 이유: **차광막을 어디에 거느냐**가 선택이 되려면
바깥 새시와 안쪽 창이 각각 독립된 감쇠 지점으로 존재해야 하기 때문이다(§ 차광).

> ⚠️ **모델 한계**: 이 엔진엔 **상호반사(벽·천장 반사광)가 없다.**
> 실제 거실은 흰 벽 반사로 여기 계산보다 밝다. 실내 깊은 곳 값은 **과소평가**다.
> 순위·배치 판단엔 쓸 수 있지만 절댓값은 보수적으로 봐야 한다.

### 구현 — 슬롯당이 아니라 **광선당**

처음엔 "유리 칸막이 안쪽 슬롯에 τ를 곱한다"로 짰는데 **틀렸다.**
감쇠는 슬롯의 속성이 아니라 **(창, 지점) 광선 쌍**의 속성이다 —
같은 거실 화분이라도 베란다 창에서 오는 빛은 2겹, 자기 방 창에서 오는 빛은 1겹이다.
슬롯당으로 하면 자기 창이 따로 있는 작은방까지 감쇠를 먹었다.

지금은 `opt.glazed`에 **반투과 유리판** 목록을 넘기고,
창 샘플 → 측정점 선분이 그 판을 가로지르면 **그 기여분에만** τ를 곱한다.

```jsonc
"partitions": [{
  "id": "balcony", "axis": "z", "at": -2.4, "from": -5, "to": 1.0,
  "glazing": { "tau": 0.92 },
  "openings": [{ "at": -2.0, "w": 3.4, "y0": 0.05, "y1": 2.30 }]
}]
```

개구부 **밖**(칸막이 솔리드 부분)은 이미 차폐체라 여기서 다루지 않는다.
차폐(100% 차단)와 감쇠(τ만큼 통과)가 깔끔히 분리된다.

> **확장형/비확장형**: 지금 아파트는 **비확장형**(베란다가 별도 공간 = 명당).
> 확장형은 `partitions`에서 balcony 항목을 빼고 창을 거실 벽으로 옮기면 된다.
> 데이터만 바꾸면 되므로 나중에 아파트 변형으로 넣을 수 있다.

---

## 3. 아파트 평면 (11×8m, 남향) — 판상형 비확장 발췌

```
       ─────── 남 (발코니 전면 통창 6.8×2.0) ───────
    ┌──[ 발  코  니  띠 ]──────────┬──────────┐
    │  안방창▓        거실창▓      │  침실1창  │   ▓ = 안쪽 거실창
    ├────────┬───────────┬────────┤          │
    │  안방   │   거실     │        │   침실1   │
    ├────────┴───────────┴────┬───┴──────────┤
    │    주방 / 식당 (북향창)    │    현관       │
    └──────────────────────────┴──────────────┘
```

| 구역 | 창 | 최고 DLI | 왜 |
|---|---|---|---|
| **발코니** | 남향 통창 **0.28m** | **8.32** | 큰 창을 코앞에서 봄 |
| 주방 | 북향 1.8×1.3 | 1.88 | 북향(k0.38)이지만 창가 |
| 안방 | (발코니 통해) | 1.76 | 거실창 너머 |
| 침실1 | 남향 직접창 | 0.61 | 창가에 낮은 선반뿐 |
| 거실 | (발코니 통해) 3.7m | **0.31** | 멀고 개구부가 좁음 |
| 현관 | 없음 | 0.03 | |

**격차 273배.** 같은 집 안에서 어디 두느냐가 전부다.

---

## 4. 실측표 (2026-07-26 · 맑음·여름, 차광 없음)

> 원본은 `data/house_rooms.json` 의 각 방 `measured` 블록. 여기 표는 사본이다.
> 방을 고치면 `_dli_probe.html` 로 다시 재고 **양쪽 다** 갱신할 것.

| 방 | 슬롯 | 최고 DLI | 최저 | ≥3 자리 | ≥6 자리 | <0.3 자리 | 겨울 최고 |
|---|---|---|---|---|---|---|---|
| 반지하 | 13 | **0.55** | 0.04 | 0 | 0 | **9** | 0.20 |
| 원룸 | 11 | **4.77** | 0.00 | 3 | 0 | 1 | 1.77 |
| 학원교실 | 32 | **5.49** | 0.02 | 10 | 0 | 10 | 2.04 |
| 투룸 | 20 | **5.64** | 0.00 | 3 | 0 | 2 | 2.10 |
| 아파트 | 83 | **6.02** | 0.00 | 48 | **1** | 20 | 2.24 |
| 온실 | 64 | **13.01** | 0.00 | 49 | **32** | 9 | 4.84 |

**progression 순서 유지 ✅**

```
반지하 0.55  ≪  원룸 4.77 < 학원교실 5.49 < 투룸 5.64 < 아파트 6.02  ≪  온실 13.01
```

### 슬롯 분포 — 최고값보다 이게 중요하다

| 방 | <0.3 무광 | 0.3~1.5 정체 | 1.5~3 느림 | 3~6 성장 | ≥6 갈라짐 |
|---|---|---|---|---|---|
| 반지하 | **9** | 4 | 0 | 0 | 0 |
| 원룸 | 1 | 3 | 4 | 3 | 0 |
| 투룸 | 2 | 9 | 6 | 3 | 0 |
| 학원교실 | 10 | 8 | 4 | 10 | 0 |
| 아파트 | 20 | 8 | 7 | **47** | 1 |
| 온실 | 9 | 2 | 4 | 17 | **32** |

### ★ 반지하 0.55~0.48 4칸 — 죽은 구간

```
0.55 0.54 0.53 0.48   ← 관엽엔 부족(산세 min 1.5), 콩나물엔 너무 밝음
0.26 … 0.04           ← 무광 작물 9칸
```

관엽도 콩나물도 못 쓰는 **애매한 4칸**이다.
**나중에 저광 작물(새싹채소·상추)이 들어오면 여기가 딱 맞는 자리다.**
반지하를 "완전히 못 쓰는 방"이 아니라 "저광 작물이라면 되는 방"으로 만들 수 있다.

### 아직 남은 것 — 아파트 ≥6 자리가 1개뿐

"갈라진 잎은 아파트부터"를 살리려면 발코니 선반을 창에 **5cm 더** 붙이면 18개가 된다.
다만 벽 안쪽면(±4.9)을 넘지 않는 선에서다 — 넘으면 벽에 박힌다(실제로 그랬다).
밸런스 판단이 필요해 보류 중.

---

## 5. 캘리브레이션 감도 — 주의

발코니 선반을 **0.04m** 옮기는 것만으로 DLI가 7.55 ↔ 9.25로 흔들린다.

| 발코니 선반 z | 창까지 | 최고 DLI |
|---|---|---|
| −3.78 | 0.22m | 11.58 |
| −3.74 | 0.26m | 9.25 |
| **−3.72** | **0.28m** | **8.32** ← 채택 |
| −3.70 | 0.30m | 7.55 |
| −3.66 | 0.34m | 6.38 |

큰 광원을 가까이서 볼 때의 `1/d²`라 물리적으로는 맞지만,
**밸런싱이 배치에 매우 민감하다**는 뜻이다.
가구를 옮기면 반드시 `_dli_probe.html`로 다시 재야 한다.
